(globalThis["TURBOPACK"] || (globalThis["TURBOPACK"] = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/handsontable/plugins/mergeCells/calculations/autofill.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
;
;
/**
 * Class responsible for all of the Autofill-related operations on merged cells.
 *
 * @private
 * @class AutofillCalculations
 */ class AutofillCalculations {
    /**
   * Get the direction of the autofill process.
   *
   * @param {Array} baseArea The selection area.
   * @param {Array} fullArea The final area (base + drag).
   * @returns {string} `up`, `down`, `left` or `right`.
   */ getDirection(baseArea, fullArea) {
        let direction = null;
        if (fullArea[0] === baseArea[0] && fullArea[1] === baseArea[1] && fullArea[3] === baseArea[3]) {
            direction = 'down';
        } else if (fullArea[2] === baseArea[2] && fullArea[1] === baseArea[1] && fullArea[3] === baseArea[3]) {
            direction = 'up';
        } else if (fullArea[1] === baseArea[1] && fullArea[2] === baseArea[2]) {
            direction = 'right';
        } else {
            direction = 'left';
        }
        return direction;
    }
    /**
   * Snap the drag area to the farthest merged cell, so it won't clip any of the merged cells.
   *
   * @param {Array} baseArea The base selected area.
   * @param {Array} fullArea The drag area.
   * @param {string} dragDirection The autofill drag direction.
   * @param {Array} foundMergedCells MergeCellCoords found in the base selection area.
   * @returns {Array} The new drag area.
   */ snapDragArea(baseArea, fullArea, dragDirection, foundMergedCells) {
        const newDragArea = fullArea.slice(0);
        const fillSize = this.getAutofillSize(baseArea, fullArea, dragDirection);
        const [baseAreaStartRow, baseAreaStartColumn, baseAreaEndRow, baseAreaEndColumn] = baseArea;
        const verticalDirection = [
            'up',
            'down'
        ].indexOf(dragDirection) > -1;
        const fullCycle = verticalDirection ? baseAreaEndRow - baseAreaStartRow + 1 : baseAreaEndColumn - baseAreaStartColumn + 1;
        const fulls = Math.floor(fillSize / fullCycle) * fullCycle;
        const partials = fillSize - fulls;
        const farthestCollection = this.getFarthestCollection(baseArea, fullArea, dragDirection, foundMergedCells);
        if (farthestCollection) {
            if (dragDirection === 'down') {
                const fill = farthestCollection.row + farthestCollection.rowspan - baseAreaStartRow - partials;
                const newLimit = newDragArea[2] + fill;
                if (newLimit >= this.plugin.hot.countRows()) {
                    newDragArea[2] -= partials;
                } else {
                    newDragArea[2] += partials ? fill : 0;
                }
            } else if (dragDirection === 'right') {
                const fill = farthestCollection.col + farthestCollection.colspan - baseAreaStartColumn - partials;
                const newLimit = newDragArea[3] + fill;
                if (newLimit >= this.plugin.hot.countCols()) {
                    newDragArea[3] -= partials;
                } else {
                    newDragArea[3] += partials ? fill : 0;
                }
            } else if (dragDirection === 'up') {
                const fill = baseAreaEndRow - partials - farthestCollection.row + 1;
                const newLimit = newDragArea[0] + fill;
                if (newLimit < 0) {
                    newDragArea[0] += partials;
                } else {
                    newDragArea[0] -= partials ? fill : 0;
                }
            } else if (dragDirection === 'left') {
                const fill = baseAreaEndColumn - partials - farthestCollection.col + 1;
                const newLimit = newDragArea[1] + fill;
                if (newLimit < 0) {
                    newDragArea[1] += partials;
                } else {
                    newDragArea[1] -= partials ? fill : 0;
                }
            }
        }
        this.updateCurrentFillCache({
            baseArea,
            dragDirection,
            foundMergedCells,
            fillSize,
            dragArea: newDragArea,
            cycleLength: fullCycle
        });
        return newDragArea;
    }
    /**
   * Update the current fill cache with the provided object.
   *
   * @private
   * @param {object} updateObject The current filled object cache.
   */ updateCurrentFillCache(updateObject) {
        if (!this.currentFillData) {
            this.currentFillData = {};
        }
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["extend"])(this.currentFillData, updateObject);
    }
    /**
   * Get the "length" of the drag area.
   *
   * @private
   * @param {Array} baseArea The base selection area.
   * @param {Array} fullArea The drag area (containing the base area).
   * @param {string} direction The drag direction.
   * @returns {number} The "length" (height or width, depending on the direction) of the drag.
   */ getAutofillSize(baseArea, fullArea, direction) {
        const [baseAreaStartRow, baseAreaStartColumn, baseAreaEndRow, baseAreaEndColumn] = baseArea;
        const [dragAreaStartRow, dragAreaStartColumn, dragAreaEndRow, dragAreaEndColumn] = fullArea;
        switch(direction){
            case 'up':
                return baseAreaStartRow - dragAreaStartRow;
            case 'down':
                return dragAreaEndRow - baseAreaEndRow;
            case 'left':
                return baseAreaStartColumn - dragAreaStartColumn;
            case 'right':
                return dragAreaEndColumn - baseAreaEndColumn;
            default:
                return 0;
        }
    }
    /**
   * Trim the default drag area (containing the selection area) to the drag-only area.
   *
   * @private
   * @param {Array} baseArea The base selection area.
   * @param {Array} fullArea The base selection area extended by the drag area.
   * @param {string} direction Drag direction.
   * @returns {Array} Array representing the drag area coordinates.
   */ getDragArea(baseArea, fullArea, direction) {
        const [baseAreaStartRow, baseAreaStartColumn, baseAreaEndRow, baseAreaEndColumn] = baseArea;
        const [fullAreaStartRow, fullAreaStartColumn, fullAreaEndRow, fullAreaEndColumn] = fullArea;
        switch(direction){
            case 'up':
                return [
                    fullAreaStartRow,
                    fullAreaStartColumn,
                    baseAreaStartRow - 1,
                    baseAreaEndColumn
                ];
            case 'down':
                {
                    const mergedCell = this.mergedCellsCollection.get(fullAreaEndRow, baseAreaEndColumn);
                    const rowShift = mergedCell ? mergedCell.rowspan - 1 : 0;
                    return [
                        baseAreaEndRow + 1,
                        baseAreaStartColumn,
                        fullAreaEndRow + rowShift,
                        baseAreaEndColumn
                    ];
                }
            case 'left':
                return [
                    fullAreaStartRow,
                    fullAreaStartColumn,
                    baseAreaEndRow,
                    baseAreaStartColumn - 1
                ];
            case 'right':
                {
                    const mergedCell = this.mergedCellsCollection.get(fullAreaEndRow, baseAreaEndColumn);
                    const columnShift = mergedCell ? mergedCell.colspan - 1 : 0;
                    return [
                        baseAreaStartRow,
                        baseAreaEndColumn + columnShift,
                        fullAreaEndRow,
                        fullAreaEndColumn
                    ];
                }
            default:
                return [
                    0,
                    0,
                    0,
                    0
                ];
        }
    }
    /**
   * Get the to-be-farthest merged cell in the newly filled area.
   *
   * @private
   * @param {Array} baseArea The base selection area.
   * @param {Array} fullArea The drag area (containing the base area).
   * @param {string} direction The drag direction.
   * @param {Array} mergedCellArray Array of the merged cells found in the base area.
   * @returns {MergedCellCoords|null}
   */ getFarthestCollection(baseArea, fullArea, direction, mergedCellArray) {
        const [baseAreaStartRow, baseAreaStartColumn, baseAreaEndRow, baseAreaEndColumn] = baseArea;
        const verticalDirection = [
            'up',
            'down'
        ].indexOf(direction) > -1;
        const baseEnd = verticalDirection ? baseAreaEndRow : baseAreaEndColumn;
        const baseStart = verticalDirection ? baseAreaStartRow : baseAreaStartColumn;
        const fillSize = this.getAutofillSize(baseArea, fullArea, direction);
        const fullCycle = verticalDirection ? baseAreaEndRow - baseAreaStartRow + 1 : baseAreaEndColumn - baseAreaStartColumn + 1;
        const fulls = Math.floor(fillSize / fullCycle) * fullCycle;
        const partials = fillSize - fulls;
        let inclusionFunctionName = null;
        let farthestCollection = null;
        let endOfDragRecreationIndex = null;
        switch(direction){
            case 'up':
                inclusionFunctionName = 'includesVertically';
                endOfDragRecreationIndex = baseEnd - partials + 1;
                break;
            case 'left':
                inclusionFunctionName = 'includesHorizontally';
                endOfDragRecreationIndex = baseEnd - partials + 1;
                break;
            case 'down':
                inclusionFunctionName = 'includesVertically';
                endOfDragRecreationIndex = baseStart + partials - 1;
                break;
            case 'right':
                inclusionFunctionName = 'includesHorizontally';
                endOfDragRecreationIndex = baseStart + partials - 1;
                break;
            default:
        }
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(mergedCellArray, (currentCollection)=>{
            const mc = currentCollection;
            if (mc[inclusionFunctionName](endOfDragRecreationIndex) && mc.isFarther(farthestCollection, direction)) {
                farthestCollection = mc;
            }
        });
        return farthestCollection;
    }
    /**
   * Recreate the merged cells after the autofill process.
   *
   * @param {Array} changes Changes made.
   */ recreateAfterDataPopulation(changes) {
        if (!this.currentFillData) {
            return;
        }
        const fillRange = this.getRangeFromChanges(changes);
        const foundMergedCells = this.currentFillData.foundMergedCells;
        const dragDirection = this.currentFillData.dragDirection;
        const inBounds = (current, offset)=>{
            switch(dragDirection){
                case 'up':
                    return current.row - offset >= fillRange.from.row;
                case 'down':
                    return current.row + current.rowspan - 1 + offset <= fillRange.to.row;
                case 'left':
                    return current.col - offset >= fillRange.from.column;
                case 'right':
                    return current.col + current.colspan - 1 + offset <= fillRange.to.column;
                default:
                    return null;
            }
        };
        let fillOffset = 0;
        let current = null;
        let multiplier = 1;
        do {
            for(let j = 0; j < foundMergedCells.length; j += 1){
                current = foundMergedCells[j];
                fillOffset = multiplier * this.currentFillData.cycleLength;
                if (inBounds(current, fillOffset)) {
                    switch(dragDirection){
                        case 'up':
                            this.plugin.mergedCellsCollection.add({
                                row: current.row - fillOffset,
                                rowspan: current.rowspan,
                                col: current.col,
                                colspan: current.colspan
                            }, true);
                            break;
                        case 'down':
                            this.plugin.mergedCellsCollection.add({
                                row: current.row + fillOffset,
                                rowspan: current.rowspan,
                                col: current.col,
                                colspan: current.colspan
                            }, true);
                            break;
                        case 'left':
                            this.plugin.mergedCellsCollection.add({
                                row: current.row,
                                rowspan: current.rowspan,
                                col: current.col - fillOffset,
                                colspan: current.colspan
                            }, true);
                            break;
                        case 'right':
                            this.plugin.mergedCellsCollection.add({
                                row: current.row,
                                rowspan: current.rowspan,
                                col: current.col + fillOffset,
                                colspan: current.colspan
                            }, true);
                            break;
                        default:
                    }
                }
                if (j === foundMergedCells.length - 1) {
                    multiplier += 1;
                }
            }
        }while (current !== null && inBounds(current, fillOffset))
        this.currentFillData = null;
        this.plugin.hot.render();
        if (foundMergedCells.length > 0) {
            this.plugin.ifChromeForceRepaint();
        }
    }
    /**
   * Get the drag range from the changes made.
   *
   * @private
   * @param {Array} changes The changes made.
   * @returns {object} Object with `from` and `to` properties, both containing `row` and `column` keys.
   */ getRangeFromChanges(changes) {
        let rowMin = null;
        let rowMax = null;
        let colMin = null;
        let colMax = null;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(changes, (change)=>{
            const changeArr = change;
            const rowIndex = changeArr[0];
            const columnIndex = this.plugin.hot.propToCol(changeArr[1]);
            if (rowMin === null || rowIndex < rowMin) {
                rowMin = rowIndex;
            }
            if (rowMax === null || rowIndex > rowMax) {
                rowMax = rowIndex;
            }
            if (colMin === null || columnIndex < colMin) {
                colMin = columnIndex;
            }
            if (colMax === null || columnIndex > colMax) {
                colMax = columnIndex;
            }
        });
        return {
            from: {
                row: rowMin ?? 0,
                column: colMin ?? 0
            },
            to: {
                row: rowMax ?? 0,
                column: colMax ?? 0
            }
        };
    }
    /**
   * Check if the drag area contains any merged cells.
   *
   * @param {Array} baseArea The base selection area.
   * @param {Array} fullArea The base area extended by the drag area.
   * @param {string} direction Drag direction.
   * @returns {boolean}
   */ dragAreaOverlapsCollections(baseArea, fullArea, direction) {
        const dragArea = this.getDragArea(baseArea, fullArea, direction);
        const [dragAreaStartRow, dragAreaStartColumn, dragAreaEndRow, dragAreaEndColumn] = dragArea;
        const topLeft = this.plugin.hot._createCellCoords(dragAreaStartRow, dragAreaStartColumn);
        const bottomRight = this.plugin.hot._createCellCoords(dragAreaEndRow, dragAreaEndColumn);
        const dragRange = this.plugin.hot._createCellRange(topLeft, topLeft, bottomRight);
        const mergedCellsWithPartials = this.mergedCellsCollection.getWithinRange(dragRange, true);
        if (mergedCellsWithPartials.length === 0) {
            return false;
        }
        const mergedCellsWithoutPartials = this.mergedCellsCollection.getWithinRange(dragRange, false);
        if (mergedCellsWithoutPartials.length === 0) {
            return true;
        }
        if (direction === 'up' || direction === 'down') {
            return !mergedCellsWithoutPartials.every(({ colspan })=>colspan === dragRange.getWidth());
        }
        return !mergedCellsWithoutPartials.every(({ rowspan })=>rowspan === dragRange.getHeight());
    }
    /**
   * Initializes the autofill calculations helper with a reference to the MergeCells plugin and its merged cells collection.
   */ constructor(plugin){
        /**
   * Cache of the currently processed autofill data.
   *
   * @private
   * @type {object}
   */ this.currentFillData = null;
        this.plugin = plugin;
        this.mergedCellsCollection = this.plugin.mergedCellsCollection;
    }
}
var __TURBOPACK__default__export__ = AutofillCalculations;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/mergeCells/calculations/selection.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
/**
 * Class responsible for all of the Selection-related operations on merged cells.
 *
 * @private
 * @class SelectionCalculations
 */ /**
 * Handles all selection-related calculations for merged cells, such as class name generation and full-selection detection.
 */ class SelectionCalculations {
    /**
   * Generate an additional class name for the entirely-selected merged cells.
   *
   * @param {number} currentRow Visual row index of the currently processed cell.
   * @param {number} currentColumn Visual column index of the currently cell.
   * @param {Array} cornersOfSelection Array of the current selection in a form of `[startRow, startColumn, endRow, endColumn]`.
   * @param {number|undefined} layerLevel Number indicating which layer of selection is currently processed.
   * @returns {string|undefined} A `String`, which will act as an additional `className` to be added to the currently processed cell.
   */ getSelectedMergedCellClassName(currentRow, currentColumn, cornersOfSelection, layerLevel) {
        const startRow = Math.min(cornersOfSelection[0], cornersOfSelection[2]);
        const startColumn = Math.min(cornersOfSelection[1], cornersOfSelection[3]);
        const endRow = Math.max(cornersOfSelection[0], cornersOfSelection[2]);
        const endColumn = Math.max(cornersOfSelection[1], cornersOfSelection[3]);
        if (layerLevel === undefined) {
            return;
        }
        const mergedCellsCollection = this.plugin.mergedCellsCollection;
        const isFirstRenderableMergedCell = mergedCellsCollection.isFirstRenderableMergedCell(currentRow, currentColumn);
        // We add extra classes just to the first renderable merged cell.
        if (!isFirstRenderableMergedCell) {
            return;
        }
        const mergedCell = mergedCellsCollection.get(currentRow, currentColumn);
        if (!mergedCell) {
            return;
        }
        const mergeRowEnd = mergedCell.getLastRow();
        const mergeColumnEnd = mergedCell.getLastColumn();
        const fullMergeAreaWithinSelection = startRow <= mergedCell.row && startColumn <= mergedCell.col && endRow >= mergeRowEnd && endColumn >= mergeColumnEnd;
        if (fullMergeAreaWithinSelection) {
            return `${this.fullySelectedMergedCellClassName}-${layerLevel}`;
        } else if (this.isMergeCellFullySelected(mergedCell, this.hot.getSelectedRange())) {
            return `${this.fullySelectedMergedCellClassName}-multiple`;
        }
    }
    /**
   * Check if the provided merged cell is fully selected (by one or many layers of selection).
   *
   * @param {MergedCellCoords} mergedCell The merged cell to be processed.
   * @param {CellRange[]} selectionRangesArray Array of selection ranges.
   * @returns {boolean}
   */ isMergeCellFullySelected(mergedCell, selectionRangesArray) {
        const mergedCellIndividualCoords = [];
        if (!selectionRangesArray || !mergedCell) {
            return false;
        }
        for(let r = 0; r < mergedCell.rowspan; r += 1){
            for(let c = 0; c < mergedCell.colspan; c += 1){
                mergedCellIndividualCoords.push(this.hot._createCellCoords(mergedCell.row + r, mergedCell.col + c));
            }
        }
        for(let i = 0; i < mergedCellIndividualCoords.length; i += 1){
            const insideSelections = [];
            for(let s = 0; s < selectionRangesArray.length; s += 1){
                insideSelections[s] = selectionRangesArray[s].includes(mergedCellIndividualCoords[i]);
            }
            if (!insideSelections.includes(true)) {
                return false;
            }
        }
        return true;
    }
    /**
   * Generate an array of the entirely-selected merged cells' class names.
   *
   * @returns {string[]} An `Array` of `String`s. Each of these strings will act like class names to be removed from all the cells in the table.
   */ getSelectedMergedCellClassNameToRemove() {
        const classNames = [];
        for(let i = 0; i <= 7; i += 1){
            classNames.push(`${this.fullySelectedMergedCellClassName}-${i}`);
        }
        classNames.push(`${this.fullySelectedMergedCellClassName}-multiple`);
        return classNames;
    }
    /**
   * Initializes the selection calculations helper with a reference to the MergeCells plugin instance.
   */ constructor(plugin){
        /**
   * Class name used for fully selected merged cells.
   *
   * @type {string}
   */ this.fullySelectedMergedCellClassName = 'fullySelectedMergedCell';
        this.plugin = plugin;
        this.hot = plugin.hot;
    }
}
var __TURBOPACK__default__export__ = SelectionCalculations;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/mergeCells/cellCoords.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/templateLiteralTag.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
;
var /**
   * The cached range coordinates of the merged cell.
   *
   * @type {CellRange}
   */ _cellRange = /*#__PURE__*/ new WeakMap();
/**
 * The `MergedCellCoords` class represents a single merged cell.
 *
 * @private
 * @class MergedCellCoords
 */ class MergedCellCoords {
    /**
   * Get a warning message for when the declared merged cell data contains negative values.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */ static NEGATIVE_VALUES_WARNING({ row, col, rowspan, colspan }) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`The merged cell declared with {row: ${row}, col: ${col},\x20
      rowspan: ${rowspan}, colspan: ${colspan}} contains negative values, which is\x20
      not supported. It will not be added to the collection.`;
    }
    /**
   * Get a warning message for when the declared merged cell data contains values exceeding the table limits.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */ static IS_OUT_OF_BOUNDS_WARNING({ row, col }) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`The merged cell declared at [${row}, ${col}] is positioned\x20
      (or positioned partially) outside of the table range. It was not added to the table, please fix your setup.`;
    }
    /**
   * Get a warning message for when the declared merged cell data represents a single cell.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */ static IS_SINGLE_CELL({ row, col }) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`The merged cell declared at [${row}, ${col}] has both "rowspan"\x20
      and "colspan" declared as "1", which makes it a single cell. It cannot be added to the collection.`;
    }
    /**
   * Get a warning message for when the declared merged cell data contains "colspan" or "rowspan", that equals 0.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */ static ZERO_SPAN_WARNING({ row, col }) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`The merged cell declared at [${row}, ${col}] has "rowspan"\x20
      or "colspan" declared as "0", which is not supported. It cannot be added to the collection.`;
    }
    /**
   * Check whether the values provided for a merged cell contain any negative values.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {boolean}
   */ static containsNegativeValues({ row, col, rowspan, colspan }) {
        return row < 0 || col < 0 || rowspan < 0 || colspan < 0;
    }
    /**
   * Check whether the provided merged cell information object represents a single cell.
   *
   * @private
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {boolean}
   */ static isSingleCell({ rowspan, colspan }) {
        return colspan === 1 && rowspan === 1;
    }
    /**
   * Check whether the provided merged cell information object contains a rowspan or colspan of 0.
   *
   * @private
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {boolean}
   */ static containsZeroSpan({ rowspan, colspan }) {
        return colspan === 0 || rowspan === 0;
    }
    /**
   * Check whether the provided merged cell object is to be declared out of bounds of the table.
   *
   * @param {object} mergeCell Object containing the `row`, `col`, `rowspan` and `colspan` properties.
   * @param {number} rowCount Number of rows in the table.
   * @param {number} columnCount Number of rows in the table.
   * @returns {boolean}
   */ static isOutOfBounds(mergeCell, rowCount, columnCount) {
        return mergeCell.row < 0 || mergeCell.col < 0 || mergeCell.row >= rowCount || mergeCell.row + mergeCell.rowspan - 1 >= rowCount || mergeCell.col >= columnCount || mergeCell.col + mergeCell.colspan - 1 >= columnCount;
    }
    /**
   * Sanitize (prevent from going outside the boundaries) the merged cell.
   *
   * @param {Core} hotInstance The Handsontable instance.
   */ normalize(hotInstance) {
        const totalRows = hotInstance.countRows();
        const totalColumns = hotInstance.countCols();
        if (this.row < 0) {
            this.row = 0;
        } else if (this.row > totalRows - 1) {
            this.row = totalRows - 1;
        }
        if (this.col < 0) {
            this.col = 0;
        } else if (this.col > totalColumns - 1) {
            this.col = totalColumns - 1;
        }
        if (this.row + this.rowspan > totalRows - 1) {
            this.rowspan = totalRows - this.row;
        }
        if (this.col + this.colspan > totalColumns - 1) {
            this.colspan = totalColumns - this.col;
        }
        _class_private_field_set(this, _cellRange, null);
    }
    /**
   * Returns `true` if the provided coordinates are inside the merged cell.
   *
   * @param {number} row The row index.
   * @param {number} column The column index.
   * @returns {boolean}
   */ includes(row, column) {
        return this.row <= row && this.col <= column && this.row + this.rowspan - 1 >= row && this.col + this.colspan - 1 >= column;
    }
    /**
   * Returns `true` if the provided `column` property is within the column span of the merged cell.
   *
   * @param {number} column The column index.
   * @returns {boolean}
   */ includesHorizontally(column) {
        return this.col <= column && this.col + this.colspan - 1 >= column;
    }
    /**
   * Returns `true` if the provided `row` property is within the row span of the merged cell.
   *
   * @param {number} row Row index.
   * @returns {boolean}
   */ includesVertically(row) {
        return this.row <= row && this.row + this.rowspan - 1 >= row;
    }
    /**
   * Shift (and possibly resize, if needed) the merged cell.
   *
   * @param {Array} shiftVector 2-element array containing the information on the shifting in the `x` and `y` axis.
   * @param {number} indexOfChange Index of the preceding change.
   * @returns {boolean} Returns `false` if the whole merged cell was removed.
   */ shift(shiftVector, indexOfChange) {
        const shiftValue = shiftVector[0] || shiftVector[1];
        const shiftedIndex = indexOfChange + Math.abs(shiftVector[0] || shiftVector[1]) - 1;
        const span = shiftVector[0] ? 'colspan' : 'rowspan';
        const index = shiftVector[0] ? 'col' : 'row';
        const changeStart = Math.min(indexOfChange, shiftedIndex);
        const changeEnd = Math.max(indexOfChange, shiftedIndex);
        const mergeStart = this[index];
        const mergeEnd = this[index] + this[span] - 1;
        if (mergeStart >= indexOfChange) {
            this[index] += shiftValue;
        }
        // adding rows/columns
        if (shiftValue > 0) {
            if (indexOfChange <= mergeEnd && indexOfChange > mergeStart) {
                this[span] += shiftValue;
            }
        // removing rows/columns
        } else if (shiftValue < 0) {
            // removing the whole merge
            if (changeStart <= mergeStart && changeEnd >= mergeEnd) {
                this.removed = true;
                _class_private_field_set(this, _cellRange, null);
                return false;
            // removing the merge partially, including the beginning
            } else if (mergeStart >= changeStart && mergeStart <= changeEnd) {
                const removedOffset = changeEnd - mergeStart + 1;
                const preRemovedOffset = Math.abs(shiftValue) - removedOffset;
                this[index] -= preRemovedOffset + shiftValue;
                this[span] -= removedOffset;
            // removing the middle part of the merge
            } else if (mergeStart <= changeStart && mergeEnd >= changeEnd) {
                this[span] += shiftValue;
            // removing the end part of the merge
            } else if (mergeStart <= changeStart && mergeEnd >= changeStart && mergeEnd < changeEnd) {
                const removedPart = mergeEnd - changeStart + 1;
                this[span] -= removedPart;
            }
        }
        _class_private_field_set(this, _cellRange, null);
        return true;
    }
    /**
   * Check if the second provided merged cell is "farther" in the provided direction.
   *
   * @param {MergedCellCoords} mergedCell The merged cell to check.
   * @param {string} direction Drag direction.
   * @returns {boolean|null} `true` if the second provided merged cell is "farther".
   */ isFarther(mergedCell, direction) {
        if (!mergedCell) {
            return true;
        }
        if (direction === 'down') {
            return mergedCell.row + mergedCell.rowspan - 1 < this.row + this.rowspan - 1;
        } else if (direction === 'up') {
            return mergedCell.row > this.row;
        } else if (direction === 'right') {
            return mergedCell.col + mergedCell.colspan - 1 < this.col + this.colspan - 1;
        } else if (direction === 'left') {
            return mergedCell.col > this.col;
        }
        return null;
    }
    /**
   * Get the bottom row index of the merged cell.
   *
   * @returns {number}
   */ getLastRow() {
        return this.row + this.rowspan - 1;
    }
    /**
   * Get the rightmost column index of the merged cell.
   *
   * @returns {number}
   */ getLastColumn() {
        return this.col + this.colspan - 1;
    }
    /**
   * Get the range coordinates of the merged cell.
   *
   * @returns {CellRange}
   */ getRange() {
        if (!_class_private_field_get(this, _cellRange)) {
            _class_private_field_set(this, _cellRange, this.cellRangeFactory(this.cellCoordsFactory(this.row, this.col), this.cellCoordsFactory(this.row, this.col), this.cellCoordsFactory(this.getLastRow(), this.getLastColumn())));
        }
        return _class_private_field_get(this, _cellRange);
    }
    /**
   * Initializes the merged cell coordinates with its top-left position, span dimensions, and factories for creating cell coordinate and range objects.
   */ constructor(row, column, rowspan, colspan, cellCoordsFactory, cellRangeFactory){
        _class_private_field_init(this, _cellRange, {
            writable: true,
            value: void 0
        });
        /**
   * `true` only if the merged cell is bound to be removed.
   *
   * @type {boolean}
   */ this.removed = false;
        _class_private_field_set(this, _cellRange, null);
        this.row = row;
        this.col = column;
        this.rowspan = rowspan;
        this.colspan = colspan;
        this.cellCoordsFactory = cellCoordsFactory;
        this.cellRangeFactory = cellRangeFactory;
    }
}
var __TURBOPACK__default__export__ = MergedCellCoords;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/mergeCells/cellsCollection.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/cellCoords.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/number.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/console.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/templateLiteralTag.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
;
;
var /**
   * Gets the list of the indexes that do not intersect with other merged cells within the provided range.
   *
   * @param {CellRange} range The range to search within.
   * @param {'row' | 'col'} axis The axis to search within.
   * @param {number} scanDirection  The direction to scan the range. `1` for forward, `-1` for backward.
   * @returns {number[]}
   */ _getNonIntersectingIndexes = /*#__PURE__*/ new WeakSet(), /**
   * Adds a merged cell to the matrix.
   *
   * @param {MergedCellCoords} mergedCell The merged cell to add.
   */ _addMergedCellToMatrix = /*#__PURE__*/ new WeakSet(), /**
   * Removes a merged cell from the matrix.
   *
   * @param {MergedCellCoords} mergedCell The merged cell to remove.
   */ _removeMergedCellFromMatrix = /*#__PURE__*/ new WeakSet();
/**
 * Defines a container object for the merged cells.
 *
 * @private
 * @class MergedCellsCollection
 */ class MergedCellsCollection {
    /**
   * Get a warning message for when the declared merged cell data overlaps already existing merged cells.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */ static IS_OVERLAPPING_WARNING({ row, col }) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`The merged cell declared at [${row}, ${col}], overlaps\x20
      with the other declared merged cell. The overlapping merged cell was not added to the table, please\x20
      fix your setup.`;
    }
    /**
   * Get a merged cell from the container, based on the provided arguments. You can provide either the "starting coordinates"
   * of a merged cell, or any coordinates from the body of the merged cell.
   *
   * @param {number} row Row index.
   * @param {number} column Column index.
   * @returns {MergedCellCoords|boolean} Returns a wanted merged cell on success and `false` on failure.
   */ get(row, column) {
        if (!this.mergedCellsMatrix.has(row)) {
            return false;
        }
        return this.mergedCellsMatrix.get(row).get(column) ?? false;
    }
    /**
   * Get the first-found merged cell containing the provided range.
   *
   * @param {CellRange} range The range to search merged cells for.
   * @returns {MergedCellCoords | false}
   */ getByRange(range) {
        const { row: rowStart, col: columnStart } = range.getTopStartCorner();
        const { row: rowEnd, col: columnEnd } = range.getBottomEndCorner();
        if (rowStart === null || rowEnd === null || columnStart === null || columnEnd === null) {
            return false;
        }
        const mergedCellsLength = this.mergedCells.length;
        let result = false;
        for(let i = 0; i < mergedCellsLength; i++){
            const mergedCell = this.mergedCells[i];
            const { row, col, rowspan, colspan } = mergedCell;
            if (row >= rowStart && row + rowspan - 1 <= rowEnd && col >= columnStart && col + colspan - 1 <= columnEnd) {
                result = mergedCell;
                break;
            }
        }
        return result;
    }
    /**
   * Filters merge cells objects provided by users from overlapping cells.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCellsInfo The merged cell information object.
   * Has to contain `row`, `col`, `colspan` and `rowspan` properties.
   * @returns {Array<{ row: number, col: number, rowspan: number, colspan: number }>}
   */ filterOverlappingMergeCells(mergedCellsInfo) {
        const occupiedCells = new Set();
        this.mergedCells.forEach((mergedCell)=>{
            const { row, col, colspan, rowspan } = mergedCell;
            for(let r = row; r < row + rowspan; r++){
                for(let c = col; c < col + colspan; c++){
                    occupiedCells.add(`r${r},c${c}`);
                }
            }
        });
        const filteredMergeCells = mergedCellsInfo.filter((mergedCell)=>{
            const { row, col, colspan, rowspan } = mergedCell;
            const localOccupiedCells = new Set();
            let isOverlapping = false;
            for(let r = row; r < row + rowspan; r++){
                for(let c = col; c < col + colspan; c++){
                    const cellId = `r${r},c${c}`;
                    if (occupiedCells.has(cellId)) {
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])(MergedCellsCollection.IS_OVERLAPPING_WARNING(mergedCell));
                        isOverlapping = true;
                        break;
                    }
                    localOccupiedCells.add(cellId);
                }
                if (isOverlapping) {
                    break;
                }
            }
            if (!isOverlapping) {
                localOccupiedCells.forEach((cell)=>occupiedCells.add(cell));
            }
            return !isOverlapping;
        });
        return filteredMergeCells;
    }
    /**
   * Get a merged cell contained in the provided range.
   *
   * @param {CellRange} range The range to search merged cells in.
   * @param {boolean} [countPartials=false] If set to `true`, all the merged cells overlapping the range will be taken into calculation.
   * @returns {MergedCellCoords[]} Array of found merged cells.
   */ getWithinRange(range, countPartials = false) {
        const { row: rowStart, col: columnStart } = range.getTopStartCorner();
        const { row: rowEnd, col: columnEnd } = range.getBottomEndCorner();
        if (rowStart === null || rowEnd === null || columnStart === null || columnEnd === null) {
            return [];
        }
        const result = [];
        for(let row = rowStart; row <= rowEnd; row++){
            for(let column = columnStart; column <= columnEnd; column++){
                const mergedCell = this.get(row, column);
                if (mergedCell && (countPartials || !countPartials && mergedCell.row === row && mergedCell.col === column)) {
                    result.push(mergedCell);
                }
            }
        }
        return result;
    }
    /**
   * Add a merged cell to the container.
   *
   * @param {object} mergedCellInfo The merged cell information object. Has to contain `row`, `col`, `colspan` and `rowspan` properties.
   * @param {boolean} [auto=false] `true` if called internally by the plugin (usually in batch).
   * @returns {MergedCellCoords|boolean} Returns the new merged cell on success and `false` on failure.
   */ add(mergedCellInfo, auto = false) {
        const row = mergedCellInfo.row;
        const column = mergedCellInfo.col;
        const rowspan = mergedCellInfo.rowspan;
        const colspan = mergedCellInfo.colspan;
        const newMergedCell = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](row, column, rowspan, colspan, this.hot._createCellCoords, this.hot._createCellRange);
        const alreadyExists = this.get(row, column);
        const isOverlapping = auto ? false : this.isOverlapping(newMergedCell);
        if (!alreadyExists && !isOverlapping) {
            if (this.hot) {
                newMergedCell.normalize(this.hot);
            }
            this.mergedCells.push(newMergedCell);
            _class_private_method_get(this, _addMergedCellToMatrix, addMergedCellToMatrix).call(this, newMergedCell);
            return newMergedCell;
        }
        if (isOverlapping) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])(MergedCellsCollection.IS_OVERLAPPING_WARNING(newMergedCell));
        }
        return false;
    }
    /**
   * Remove a merged cell from the container. You can provide either the "starting coordinates"
   * of a merged cell, or any coordinates from the body of the merged cell.
   *
   * @param {number} row Row index.
   * @param {number} column Column index.
   * @returns {MergedCellCoords|boolean} Returns the removed merged cell on success and `false` on failure.
   */ remove(row, column) {
        const mergedCell = this.get(row, column);
        const mergedCellIndex = mergedCell ? this.mergedCells.indexOf(mergedCell) : -1;
        if (mergedCell && mergedCellIndex !== -1) {
            this.mergedCells.splice(mergedCellIndex, 1);
            _class_private_method_get(this, _removeMergedCellFromMatrix, removeMergedCellFromMatrix).call(this, mergedCell);
            return mergedCell;
        }
        return false;
    }
    /**
   * Clear all the merged cells.
   */ clear() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(this.mergedCells, ({ row, col, rowspan, colspan })=>{
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["rangeEach"])(row, row + rowspan, (r)=>{
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["rangeEach"])(col, col + colspan, (c)=>{
                    const TD = this.hot.getCell(r, c);
                    if (TD) {
                        TD.removeAttribute('rowspan');
                        TD.removeAttribute('colspan');
                        TD.style.display = '';
                    }
                });
            });
        });
        this.mergedCells.length = 0;
        this.mergedCellsMatrix = new Map();
    }
    /**
   * Check if the provided merged cell overlaps with the others already added.
   *
   * @param {MergedCellCoords} mergedCell The merged cell to check against all others in the container.
   * @returns {boolean} `true` if the provided merged cell overlaps with the others, `false` otherwise.
   */ isOverlapping(mergedCell) {
        const mergedCellRange = mergedCell.getRange();
        if (!mergedCellRange) {
            return false;
        }
        for(let i = 0; i < this.mergedCells.length; i++){
            const otherMergedCell = this.mergedCells[i];
            const otherMergedCellRange = otherMergedCell.getRange();
            const overlappingRange = otherMergedCellRange;
            if (otherMergedCellRange && overlappingRange.overlaps(mergedCellRange)) {
                return true;
            }
        }
        return false;
    }
    /**
   * Check whether the provided row/col coordinates direct to a first not hidden cell within merge area.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @returns {boolean}
   */ isFirstRenderableMergedCell(row, column) {
        const mergeParent = this.get(row, column);
        if (!mergeParent) {
            return false;
        }
        const { row: mergeRow, col: mergeColumn, rowspan, colspan } = mergeParent;
        const overlayName = this.hot.view.getActiveOverlayName();
        const firstRenderedRow = [
            'top',
            'top_inline_start_corner'
        ].includes(overlayName) ? 0 : this.hot.getFirstRenderedVisibleRow();
        const firstRenderedColumn = [
            'inline_start',
            'top_inline_start_corner',
            'bottom_inline_start_corner'
        ].includes(overlayName) ? 0 : this.hot.getFirstRenderedVisibleColumn();
        const mergeCellsTopRow = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(firstRenderedRow, mergeRow, mergeRow + rowspan - 1);
        const mergeCellsStartColumn = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(firstRenderedColumn, mergeColumn, mergeColumn + colspan - 1);
        return this.hot.rowIndexMapper.getNearestNotHiddenIndex(mergeCellsTopRow, 1) === row && this.hot.columnIndexMapper.getNearestNotHiddenIndex(mergeCellsStartColumn, 1) === column;
    }
    /**
   * Get the first renderable coords of the merged cell at the provided coordinates.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @returns {CellCoords} A `CellCoords` object with the coordinates to the first renderable cell within the
   *                        merged cell.
   */ getFirstRenderableCoords(row, column) {
        const mergeParent = this.get(row, column);
        if (!mergeParent || this.isFirstRenderableMergedCell(row, column)) {
            return this.hot._createCellCoords(row, column);
        }
        const firstRenderableRow = this.hot.rowIndexMapper.getNearestNotHiddenIndex(mergeParent.row, 1) ?? mergeParent.row;
        const firstRenderableColumn = this.hot.columnIndexMapper.getNearestNotHiddenIndex(mergeParent.col, 1) ?? mergeParent.col;
        return this.hot._createCellCoords(firstRenderableRow, firstRenderableColumn);
    }
    /**
   * Gets the start-most visual column index that do not intersect with other merged cells within the provided range.
   *
   * @param {CellRange} range The range to search within.
   * @param {number} visualColumnIndex The visual column index to start the search from.
   * @returns {number}
   */ getStartMostColumnIndex(range, visualColumnIndex) {
        const indexes = _class_private_method_get(this, _getNonIntersectingIndexes, getNonIntersectingIndexes).call(this, range, 'col', -1);
        let startMostIndex = visualColumnIndex;
        for(let i = 0; i < indexes.length; i++){
            if (indexes[i] <= visualColumnIndex) {
                startMostIndex = indexes[i];
                break;
            }
        }
        return startMostIndex;
    }
    /**
   * Gets the end-most visual column index that do not intersect with other merged cells within the provided range.
   *
   * @param {CellRange} range The range to search within.
   * @param {number} visualColumnIndex The visual column index to start the search from.
   * @returns {number}
   */ getEndMostColumnIndex(range, visualColumnIndex) {
        const indexes = _class_private_method_get(this, _getNonIntersectingIndexes, getNonIntersectingIndexes).call(this, range, 'col', 1);
        let endMostIndex = visualColumnIndex;
        for(let i = 0; i < indexes.length; i++){
            if (indexes[i] >= visualColumnIndex) {
                endMostIndex = indexes[i];
                break;
            }
        }
        return endMostIndex;
    }
    /**
   * Gets the top-most visual row index that do not intersect with other merged cells within the provided range.
   *
   * @param {CellRange} range The range to search within.
   * @param {number} visualRowIndex The visual row index to start the search from.
   * @returns {number}
   */ getTopMostRowIndex(range, visualRowIndex) {
        const indexes = _class_private_method_get(this, _getNonIntersectingIndexes, getNonIntersectingIndexes).call(this, range, 'row', -1);
        let topMostIndex = visualRowIndex;
        for(let i = 0; i < indexes.length; i++){
            if (indexes[i] <= visualRowIndex) {
                topMostIndex = indexes[i];
                break;
            }
        }
        return topMostIndex;
    }
    /**
   * Gets the bottom-most visual row index that do not intersect with other merged cells within the provided range.
   *
   * @param {CellRange} range The range to search within.
   * @param {number} visualRowIndex The visual row index to start the search from.
   * @returns {number}
   */ getBottomMostRowIndex(range, visualRowIndex) {
        const indexes = _class_private_method_get(this, _getNonIntersectingIndexes, getNonIntersectingIndexes).call(this, range, 'row', 1);
        let bottomMostIndex = visualRowIndex;
        for(let i = 0; i < indexes.length; i++){
            if (indexes[i] >= visualRowIndex) {
                bottomMostIndex = indexes[i];
                break;
            }
        }
        return bottomMostIndex;
    }
    /**
   * Shift the merged cell in the direction and by an offset defined in the arguments.
   *
   * @param {string} direction `right`, `left`, `up` or `down`.
   * @param {number} index Index where the change, which caused the shifting took place.
   * @param {number} count Number of rows/columns added/removed in the preceding action.
   */ shiftCollections(direction, index, count) {
        const shiftVector = [
            0,
            0
        ];
        switch(direction){
            case 'right':
                shiftVector[0] += count;
                break;
            case 'left':
                shiftVector[0] -= count;
                break;
            case 'down':
                shiftVector[1] += count;
                break;
            case 'up':
                shiftVector[1] -= count;
                break;
            default:
        }
        const removedMergedCells = [];
        this.mergedCells.forEach((currentMerge)=>{
            currentMerge.shift(shiftVector, index);
            if (currentMerge.removed) {
                removedMergedCells.push(currentMerge);
            }
        });
        removedMergedCells.forEach((removedMerge)=>{
            this.mergedCells.splice(this.mergedCells.indexOf(removedMerge), 1);
        });
        this.mergedCellsMatrix.clear();
        this.mergedCells.forEach((currentMerge)=>{
            _class_private_method_get(this, _addMergedCellToMatrix, addMergedCellToMatrix).call(this, currentMerge);
        });
    }
    /**
   * Capture the physical indexes covered by every merged cell along an axis.
   *
   * Used by the manual row/column move and column freeze integrations to translate
   * merges through a reorder: the captured spans pin each merge to the underlying
   * data so the merge can be repositioned (and split, if a non-contiguous run
   * results) after the visual sequence changes.
   *
   * @param {'column' | 'row'} axis Which axis the upcoming reorder targets.
   * @returns {Map<MergedCellCoords, number[]>} Map of merge -> physical indexes along the axis.
   */ capturePhysicalSpans(axis) {
        const isColumn = axis === 'column';
        const indexProp = isColumn ? 'col' : 'row';
        const spanProp = isColumn ? 'colspan' : 'rowspan';
        const toPhysical = isColumn ? (visualIndex)=>this.hot.toPhysicalColumn(visualIndex) : (visualIndex)=>this.hot.toPhysicalRow(visualIndex);
        const snapshot = new Map();
        this.mergedCells.forEach((merge)=>{
            const physicals = [];
            for(let i = 0; i < merge[spanProp]; i++){
                physicals.push(toPhysical(merge[indexProp] + i));
            }
            snapshot.set(merge, physicals);
        });
        return snapshot;
    }
    /**
   * Group an ascending list of integers into contiguous runs.
   *
   * @param {number[]} sortedAscending Already-sorted ascending visual indexes.
   * @returns {Array<{ start: number, length: number }>}
   */ static detectContiguousRuns(sortedAscending) {
        if (sortedAscending.length === 0) {
            return [];
        }
        const runs = [];
        let runStart = sortedAscending[0];
        let runLength = 1;
        for(let i = 1; i < sortedAscending.length; i++){
            if (sortedAscending[i] === sortedAscending[i - 1] + 1) {
                runLength += 1;
            } else {
                runs.push({
                    start: runStart,
                    length: runLength
                });
                runStart = sortedAscending[i];
                runLength = 1;
            }
        }
        runs.push({
            start: runStart,
            length: runLength
        });
        return runs;
    }
    /**
   * Translate the merged cells collection after a manual row/column reorder, splitting
   * merges whose physical indexes are no longer contiguous in the new visual order.
   *
   * Note: single-cell fragments (`colspan === 1 && rowspan === 1`) are dropped because
   * they no longer represent a merge. The user-facing behavior (auto-split + silent drop
   * of singletons) is documented in `docs/content/guides/cell-features/merge-cells/merge-cells.md`
   * under "Behavior during row/column reorder and column freeze".
   *
   * @param {'column' | 'row'} axis Axis that was reordered.
   * @param {Map<MergedCellCoords, number[]>} snapshot Snapshot taken before the reorder.
   */ translateAfterAxisMove(axis, snapshot) {
        const isColumn = axis === 'column';
        const indexProp = isColumn ? 'col' : 'row';
        const spanProp = isColumn ? 'colspan' : 'rowspan';
        const otherIndexProp = isColumn ? 'row' : 'col';
        const otherSpanProp = isColumn ? 'rowspan' : 'colspan';
        const toVisual = isColumn ? (physicalIndex)=>this.hot.toVisualColumn(physicalIndex) : (physicalIndex)=>this.hot.toVisualRow(physicalIndex);
        const replacements = [];
        this.mergedCells.forEach((merge)=>{
            const physicals = snapshot.get(merge);
            if (!physicals) {
                replacements.push({
                    row: merge.row,
                    col: merge.col,
                    rowspan: merge.rowspan,
                    colspan: merge.colspan
                });
                return;
            }
            const newVisuals = physicals.map(toVisual).filter((visualIndex)=>visualIndex !== null && visualIndex >= 0).sort((a, b)=>a - b);
            if (newVisuals.length === 0) {
                return;
            }
            MergedCellsCollection.detectContiguousRuns(newVisuals).forEach((run)=>{
                const replacement = {
                    [indexProp]: run.start,
                    [spanProp]: run.length,
                    [otherIndexProp]: merge[otherIndexProp],
                    [otherSpanProp]: merge[otherSpanProp]
                };
                if (replacement.colspan === 1 && replacement.rowspan === 1) {
                    return;
                }
                replacements.push(replacement);
            });
        });
        this.mergedCells.length = 0;
        this.mergedCellsMatrix.clear();
        replacements.forEach((info)=>{
            this.add(info, true);
        });
    }
    /**
   * Initializes the cells collection with references to the MergeCells plugin and the Handsontable instance.
   */ constructor(mergeCellsPlugin){
        _class_private_method_init(this, _getNonIntersectingIndexes);
        _class_private_method_init(this, _addMergedCellToMatrix);
        _class_private_method_init(this, _removeMergedCellFromMatrix);
        /**
   * Array of merged cells.
   *
   * @type {MergedCellCoords[]}
   */ this.mergedCells = [];
        /**
   * Matrix of cells (row, col) that points to the instances of the MergedCellCoords objects.
   *
   * @type {Array}
   */ this.mergedCellsMatrix = new Map();
        this.plugin = mergeCellsPlugin;
        this.hot = mergeCellsPlugin.hot;
    }
}
function getNonIntersectingIndexes(range, axis, scanDirection = 1) {
    const indexes = new Map();
    const from = scanDirection === 1 ? range.getTopStartCorner() : range.getBottomEndCorner();
    const to = scanDirection === 1 ? range.getBottomEndCorner() : range.getTopStartCorner();
    const fromRow = from.row ?? 0;
    const fromCol = from.col ?? 0;
    const toRow = to.row ?? 0;
    const toCol = to.col ?? 0;
    for(let row = fromRow; scanDirection === 1 ? row <= toRow : row >= toRow; row += scanDirection){
        for(let column = fromCol; scanDirection === 1 ? column <= toCol : column >= toCol; column += scanDirection){
            const index = axis === 'row' ? row : column;
            const mergedCell = this.get(row, column);
            let lastIndex = index;
            if (mergedCell) {
                lastIndex = scanDirection === 1 ? mergedCell[axis] + mergedCell[`${axis}span`] - 1 : mergedCell[axis];
            }
            if (!indexes.has(index)) {
                indexes.set(index, new Set());
            }
            indexes.get(index).add(lastIndex);
        }
    }
    return Array.from(new Set(Array.from(indexes.entries()).filter(([, set])=>set.size === 1).flatMap(([, set])=>Array.from(set))));
}
function addMergedCellToMatrix(mergedCell) {
    for(let row = mergedCell.row; row < mergedCell.row + mergedCell.rowspan; row++){
        for(let col = mergedCell.col; col < mergedCell.col + mergedCell.colspan; col++){
            if (!this.mergedCellsMatrix.has(row)) {
                this.mergedCellsMatrix.set(row, new Map());
            }
            this.mergedCellsMatrix.get(row).set(col, mergedCell);
        }
    }
}
function removeMergedCellFromMatrix(mergedCell) {
    for(let row = mergedCell.row; row < mergedCell.row + mergedCell.rowspan; row++){
        for(let col = mergedCell.col; col < mergedCell.col + mergedCell.colspan; col++){
            this.mergedCellsMatrix.get(row)?.delete(col);
        }
    }
}
var __TURBOPACK__default__export__ = MergedCellsCollection;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/mergeCells/contextMenuItem/toggleMerge.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>toggleMergeItem
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$i18n$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/i18n/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/cellCoords.mjs [client] (ecmascript)");
;
;
function toggleMergeItem(plugin) {
    return {
        key: 'mergeCells',
        name () {
            const selection = this.getSelectedActive();
            if (selection && plugin.mergedCellsCollection) {
                const info = plugin.mergedCellsCollection.get(selection[0], selection[1]);
                if (info !== false && info.row === selection[0] && info.col === selection[1] && info.row + info.rowspan - 1 === selection[2] && info.col + info.colspan - 1 === selection[3]) {
                    return this.getTranslatedPhrase(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$i18n$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CONTEXTMENU_ITEMS_UNMERGE_CELLS"]);
                }
            }
            return this.getTranslatedPhrase(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$i18n$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CONTEXTMENU_ITEMS_MERGE_CELLS"]);
        },
        callback () {
            const activeRange = this.getSelectedRangeActive();
            if (!activeRange) {
                return;
            }
            activeRange.setDirection(this.isRtl() ? 'NE-SW' : 'NW-SE');
            const { from, to } = activeRange;
            plugin.toggleMerge(activeRange);
            if (from.row !== null && from.col !== null && to.row !== null && to.col !== null) {
                this.selectCell(from.row, from.col, to.row, to.col, false);
            }
        },
        disabled () {
            const selection = this.getSelectedActive();
            if (!selection) {
                return true;
            }
            const isSingleCell = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"].isSingleCell({
                row: selection[0],
                col: selection[1],
                rowspan: selection[2] - selection[0] + 1,
                colspan: selection[3] - selection[1] + 1
            });
            return isSingleCell || this.selection.isSelectedByCorner();
        },
        hidden: false
    };
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/mergeCells/focusOrder.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "FocusOrder",
    ()=>FocusOrder
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$utils$2f$dataStructures$2f$linkedList$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/utils/dataStructures/linkedList.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
var /**
   * The linked list of the all cells within the current selection in horizontal order. The list is
   * recreated every time the selection is changed.
   */ _cellsHorizontalOrder = /*#__PURE__*/ new WeakMap(), /**
   * The linked list of the all cells within the current selection in vertical order. The list is
   * recreated every time the selection is changed.
   */ _cellsVerticalOrder = /*#__PURE__*/ new WeakMap(), /**
   * The currently highlighted cell within the horizontal linked list.
   */ _currentHorizontalLinkedNode = /*#__PURE__*/ new WeakMap(), /**
   * The currently highlighted cell within the vertical linked list.
   */ _currentVerticalLinkedNode = /*#__PURE__*/ new WeakMap(), /**
   * The merged cells getter function.
   */ _mergedCellsGetter = /*#__PURE__*/ new WeakMap(), /**
   * The row index mapper.
   */ _rowIndexMapper = /*#__PURE__*/ new WeakMap(), /**
   * The column index mapper.
   */ _columnIndexMapper = /*#__PURE__*/ new WeakMap(), /**
   * Pushes a new node to the provided list order.
   *
   * @param {object} options The options object.
   * @param {CellRange} options.selectedRange The selected range to build the focus order for.
   * @param {number} options.selectionLayer The selection layer index.
   * @param {LinkedList} options.listOrder The list order to push the node to.
   * @param {WeakSet} options.mergeCellsVisitor The set of visited cells.
   * @param {number} options.row The visual row index.
   * @param {number} options.column The visual column index.
   * @returns {NodeStructure | null}
   */ _pushOrderNode = /*#__PURE__*/ new WeakSet();
class FocusOrder {
    /**
   * Gets the currently selected node data from the vertical focus order list.
   *
   * @returns {NodeStructure}
   */ getCurrentVerticalNode() {
        return _class_private_field_get(this, _currentVerticalLinkedNode)?.data;
    }
    /**
   * Gets the first node data from the vertical focus order list.
   *
   * @returns {NodeStructure}
   */ getFirstVerticalNode() {
        return _class_private_field_get(this, _cellsVerticalOrder).first?.data;
    }
    /**
   * Gets the next selected node data from the vertical focus order list.
   *
   * @returns {FocusNodeData}
   */ getNextVerticalNode() {
        return _class_private_field_get(this, _currentVerticalLinkedNode)?.next?.data;
    }
    /**
   * Gets the previous selected node data from the vertical focus order list.
   *
   * @returns {FocusNodeData}
   */ getPrevVerticalNode() {
        return _class_private_field_get(this, _currentVerticalLinkedNode)?.prev?.data;
    }
    /**
   * Gets the currently selected node data from the horizontal focus order list.
   *
   * @returns {NodeStructure}
   */ getCurrentHorizontalNode() {
        return _class_private_field_get(this, _currentHorizontalLinkedNode)?.data;
    }
    /**
   * Gets the first node data from the horizontal focus order list.
   *
   * @returns {NodeStructure}
   */ getFirstHorizontalNode() {
        return _class_private_field_get(this, _cellsHorizontalOrder).first?.data;
    }
    /**
   * Gets the next selected node data from the horizontal focus order list.
   *
   * @returns {FocusNodeData}
   */ getNextHorizontalNode() {
        return _class_private_field_get(this, _currentHorizontalLinkedNode)?.next?.data;
    }
    /**
   * Gets the previous selected node data from the horizontal focus order list.
   *
   * @returns {FocusNodeData}
   */ getPrevHorizontalNode() {
        return _class_private_field_get(this, _currentHorizontalLinkedNode)?.prev?.data;
    }
    /**
   * Sets the previous node from the vertical focus order list as active.
   */ setPrevNodeAsActive() {
        if (_class_private_field_get(this, _currentVerticalLinkedNode)) {
            _class_private_field_set(this, _currentVerticalLinkedNode, _class_private_field_get(this, _currentVerticalLinkedNode).prev);
        }
        if (_class_private_field_get(this, _currentHorizontalLinkedNode)) {
            _class_private_field_set(this, _currentHorizontalLinkedNode, _class_private_field_get(this, _currentHorizontalLinkedNode).prev);
        }
    }
    /**
   * Sets the previous node from the horizontal focus order list as active.
   */ setNextNodeAsActive() {
        if (_class_private_field_get(this, _currentVerticalLinkedNode)) {
            _class_private_field_set(this, _currentVerticalLinkedNode, _class_private_field_get(this, _currentVerticalLinkedNode).next);
        }
        if (_class_private_field_get(this, _currentHorizontalLinkedNode)) {
            _class_private_field_set(this, _currentHorizontalLinkedNode, _class_private_field_get(this, _currentHorizontalLinkedNode).next);
        }
    }
    /**
   * Rebuilds the focus order list based on the provided selection.
   *
   * @param {CellRange[]} selectedRanges The selected ranges to build the focus order for.
   */ buildFocusOrder(selectedRanges) {
        _class_private_field_set(this, _cellsHorizontalOrder, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$utils$2f$dataStructures$2f$linkedList$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]());
        selectedRanges.forEach((range, selectionLayer)=>{
            const visitedHorizontalCells = new WeakSet();
            const topStart = range.getTopStartCorner();
            const bottomEnd = range.getBottomEndCorner();
            const rowFrom = topStart.row ?? 0;
            const rowTo = bottomEnd.row ?? 0;
            const colFrom = topStart.col ?? 0;
            const colTo = bottomEnd.col ?? 0;
            for(let r = rowFrom; r <= rowTo; r++){
                if (_class_private_field_get(this, _rowIndexMapper).isHidden(r)) {
                    continue;
                }
                for(let c = colFrom; c <= colTo; c++){
                    if (_class_private_field_get(this, _columnIndexMapper).isHidden(c)) {
                        continue;
                    }
                    const node = _class_private_method_get(this, _pushOrderNode, pushOrderNode).call(this, {
                        selectedRange: range,
                        selectionLayer,
                        listOrder: _class_private_field_get(this, _cellsHorizontalOrder),
                        mergeCellsVisitor: visitedHorizontalCells,
                        row: r,
                        column: c
                    });
                    if (node) {
                        _class_private_field_set(this, _currentHorizontalLinkedNode, node);
                    }
                }
            }
        });
        // create circular linked list
        if (_class_private_field_get(this, _cellsHorizontalOrder).first && _class_private_field_get(this, _cellsHorizontalOrder).last) {
            _class_private_field_get(this, _cellsHorizontalOrder).first.prev = _class_private_field_get(this, _cellsHorizontalOrder).last;
            _class_private_field_get(this, _cellsHorizontalOrder).last.next = _class_private_field_get(this, _cellsHorizontalOrder).first;
        }
        _class_private_field_set(this, _cellsVerticalOrder, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$utils$2f$dataStructures$2f$linkedList$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]());
        selectedRanges.forEach((range, selectionLayer)=>{
            const visitedVerticalCells = new WeakSet();
            const topStart = range.getTopStartCorner();
            const bottomEnd = range.getBottomEndCorner();
            const rowFrom = topStart.row ?? 0;
            const rowTo = bottomEnd.row ?? 0;
            const colFrom = topStart.col ?? 0;
            const colTo = bottomEnd.col ?? 0;
            for(let c = colFrom; c <= colTo; c++){
                if (_class_private_field_get(this, _columnIndexMapper).isHidden(c)) {
                    continue;
                }
                for(let r = rowFrom; r <= rowTo; r++){
                    if (_class_private_field_get(this, _rowIndexMapper).isHidden(r)) {
                        continue;
                    }
                    const node = _class_private_method_get(this, _pushOrderNode, pushOrderNode).call(this, {
                        selectedRange: range,
                        selectionLayer,
                        listOrder: _class_private_field_get(this, _cellsVerticalOrder),
                        mergeCellsVisitor: visitedVerticalCells,
                        row: r,
                        column: c
                    });
                    if (node) {
                        _class_private_field_set(this, _currentVerticalLinkedNode, node);
                    }
                }
            }
        });
        // create circular linked list
        if (_class_private_field_get(this, _cellsVerticalOrder).first && _class_private_field_get(this, _cellsVerticalOrder).last) {
            _class_private_field_get(this, _cellsVerticalOrder).first.prev = _class_private_field_get(this, _cellsVerticalOrder).last;
            _class_private_field_get(this, _cellsVerticalOrder).last.next = _class_private_field_get(this, _cellsVerticalOrder).first;
        }
    }
    /**
   * Sets the active node based on the provided row and column.
   *
   * @param {number} row The visual row index.
   * @param {number} column The visual column index.
   * @param {number} selectionLayerIndex The index of the selection layer to which the focus should be marked as active.
   * @returns {FocusOrder}
   */ setActiveNode(row, column, selectionLayerIndex) {
        _class_private_field_get(this, _cellsHorizontalOrder).inorder((node)=>{
            const { selectionLayer, rowStart, rowEnd, colStart, colEnd } = node.data;
            if (selectionLayer === selectionLayerIndex && row >= rowStart && row <= rowEnd && column >= colStart && column <= colEnd) {
                _class_private_field_set(this, _currentHorizontalLinkedNode, node);
                return true;
            }
        });
        _class_private_field_get(this, _cellsVerticalOrder).inorder((node)=>{
            const { selectionLayer, rowStart, rowEnd, colStart, colEnd } = node.data;
            if (selectionLayer === selectionLayerIndex && row >= rowStart && row <= rowEnd && column >= colStart && column <= colEnd) {
                _class_private_field_set(this, _currentVerticalLinkedNode, node);
                return true;
            }
        });
        return this;
    }
    /**
   * Initializes the focus order manager with the merged cell getter and row and column index mappers used to navigate focus through merged regions.
   */ constructor({ mergedCellsGetter, rowIndexMapper, columnIndexMapper }){
        _class_private_method_init(this, _pushOrderNode);
        _class_private_field_init(this, _cellsHorizontalOrder, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _cellsVerticalOrder, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _currentHorizontalLinkedNode, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _currentVerticalLinkedNode, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _mergedCellsGetter, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _rowIndexMapper, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _columnIndexMapper, {
            writable: true,
            value: void 0
        });
        _class_private_field_set(this, _cellsHorizontalOrder, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$utils$2f$dataStructures$2f$linkedList$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]());
        _class_private_field_set(this, _cellsVerticalOrder, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$utils$2f$dataStructures$2f$linkedList$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]());
        _class_private_field_set(this, _currentHorizontalLinkedNode, null);
        _class_private_field_set(this, _currentVerticalLinkedNode, null);
        _class_private_field_set(this, _mergedCellsGetter, mergedCellsGetter);
        _class_private_field_set(this, _rowIndexMapper, rowIndexMapper);
        _class_private_field_set(this, _columnIndexMapper, columnIndexMapper);
    }
}
function pushOrderNode({ selectedRange, selectionLayer, listOrder, mergeCellsVisitor, row, column }) {
    const topStart = selectedRange.getTopStartCorner();
    const bottomEnd = selectedRange.getBottomEndCorner();
    const topStartRow = topStart.row ?? 0;
    const topStartCol = topStart.col ?? 0;
    const bottomEndRow = bottomEnd.row ?? 0;
    const bottomEndCol = bottomEnd.col ?? 0;
    const highlight = selectedRange.highlight.clone().normalize();
    const highlightRow = highlight.row ?? 0;
    const highlightCol = highlight.col ?? 0;
    const mergeParent = _class_private_field_get(this, _mergedCellsGetter).call(this, row, column);
    if (mergeParent && mergeCellsVisitor.has(mergeParent)) {
        return null;
    }
    const node = {
        selectionLayer,
        colStart: column,
        colEnd: column,
        rowStart: row,
        rowEnd: row
    };
    if (mergeParent) {
        mergeCellsVisitor.add(mergeParent);
        if (mergeParent.row < topStartRow || mergeParent.row + mergeParent.rowspan - 1 > bottomEndRow || mergeParent.col < topStartCol || mergeParent.col + mergeParent.colspan - 1 > bottomEndCol) {
            return null;
        }
        node.colStart = mergeParent.col;
        node.colEnd = mergeParent.col + mergeParent.colspan - 1;
        node.rowStart = mergeParent.row;
        node.rowEnd = mergeParent.row + mergeParent.rowspan - 1;
    }
    const linkedNode = listOrder.push(node);
    if (row === highlightRow && column === highlightCol || mergeParent && highlightRow >= mergeParent.row && highlightRow <= mergeParent.row + mergeParent.rowspan - 1 && highlightCol >= mergeParent.col && highlightCol <= mergeParent.col + mergeParent.colspan - 1) {
        return linkedNode;
    }
    return null;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/mergeCells/mergeCells.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "MergeCells",
    ()=>MergeCells,
    "PLUGIN_KEY",
    ()=>PLUGIN_KEY,
    "PLUGIN_PRIORITY",
    ()=>PLUGIN_PRIORITY
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$base$2f$base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/base/base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$core$2f$hooks$2f$index$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/core/hooks/index.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellsCollection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/cellsCollection.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/cellCoords.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$calculations$2f$autofill$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/calculations/autofill.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$calculations$2f$selection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/calculations/selection.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$contextMenuItem$2f$toggleMerge$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/contextMenuItem/toggleMerge.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/console.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/number.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/browser.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$focusOrder$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/focusOrder.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$renderer$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/renderer.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/utils.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$core$2f$hooks$2f$index$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["Hooks"].getSingleton().register('beforeMergeCells');
__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$core$2f$hooks$2f$index$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["Hooks"].getSingleton().register('afterMergeCells');
__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$core$2f$hooks$2f$index$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["Hooks"].getSingleton().register('beforeUnmergeCells');
__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$core$2f$hooks$2f$index$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["Hooks"].getSingleton().register('afterUnmergeCells');
const PLUGIN_KEY = 'mergeCells';
const PLUGIN_PRIORITY = 150;
const SHORTCUTS_GROUP = PLUGIN_KEY;
var /**
   * The holder for the last selected focus coordinates. This allows keeping the correct coordinates in cases after the
   * focus is moved out of the merged cell.
   *
   * @type {CellCoords}
   */ _lastSelectedFocus = /*#__PURE__*/ new WeakMap(), /**
   * The last used transformation delta.
   *
   * @type {{ row: number, col: number }}
   */ _lastFocusDelta = /*#__PURE__*/ new WeakMap(), /**
   * The module responsible for providing the correct focus order (vertical and horizontal) within a selection that
   * contains merged cells.
   *
   * @type {FocusOrder}
   */ _focusOrder = /*#__PURE__*/ new WeakMap(), /**
   * The cell renderer responsible for rendering the merged cells.
   *
   * @type {{before: Function, after: Function}}
   */ _cellRenderer = /*#__PURE__*/ new WeakMap(), /**
   * Snapshot of physical column indexes per merge, captured before a column move/freeze.
   *
   * @type {Map<MergedCellCoords, number[]> | null}
   */ _columnMoveSnapshot = /*#__PURE__*/ new WeakMap(), /**
   * Snapshot of physical row indexes per merge, captured before a row move.
   *
   * @type {Map<MergedCellCoords, number[]> | null}
   */ _rowMoveSnapshot = /*#__PURE__*/ new WeakMap(), /**
   * `true` once the plugin has finished its initial settings ingestion. Used to skip
   * snapshot/translate during the bootstrap-time column reorders fired by
   * `manualColumnMove: [...]` initial config, where the merge collection is empty
   * anyway but we want to be defensive against future hook-order changes.
   *
   * @type {boolean}
   */ _initialized = /*#__PURE__*/ new WeakMap(), /**
   * `afterInit` hook callback.
   */ _onAfterInit = /*#__PURE__*/ new WeakMap(), /**
   * Modifies the information on whether the current selection contains multiple cells. The `afterIsMultipleSelection`
   * hook callback.
   *
   * @param {boolean} isMultiple Determines whether the current selection contains multiple cells.
   * @returns {boolean}
   */ _onAfterIsMultipleSelection = /*#__PURE__*/ new WeakMap(), /**
   * `modifyTransformFocus` hook callback.
   *
   * @param {object} delta The transformation delta.
   */ _onModifyTransformFocus = /*#__PURE__*/ new WeakMap(), /**
   * `modifyTransformStart` hook callback.
   *
   * @param {object} delta The transformation delta.
   */ _onModifyTransformStart = /*#__PURE__*/ new WeakMap(), /**
   * The hook allows to modify the delta transformation object necessary for correct selection end transformations.
   *
   * @param {{ row: number, col: number }} delta The transformation delta.
   */ _onModifyTransformEnd = /*#__PURE__*/ new WeakMap(), /**
   * The hook corrects the range (before drawing it) after the selection was made on the merged cells.
   */ _onBeforeSelectionHighlightSet = /*#__PURE__*/ new WeakMap(), /**
   * The `modifyGetCellCoords` hook callback.
   *
   * @param {number} row Row index.
   * @param {number} column Visual column index.
   * @param {boolean} topmost Indicates if the requested element belongs to the topmost layer.
   * @param {string} [source] String that identifies how this coords change will be processed.
   * @returns {Array|undefined} Visual coordinates of the merge.
   */ _onModifyGetCellCoords = /*#__PURE__*/ new WeakMap(), /**
   * `afterContextMenuDefaultOptions` hook callback.
   *
   * @param {object} defaultOptions The default context menu options.
   */ _addMergeActionsToContextMenu = /*#__PURE__*/ new WeakSet(), /**
   * Clears the last selected coordinates before setting a new selection range.
   */ _onBeforeSetRangeStart = /*#__PURE__*/ new WeakMap(), /**
   * Detects if the last selected cell was a header cell.
   */ _onBeforeSelectionFocusSet = /*#__PURE__*/ new WeakMap(), /**
   * Changes the focus selection to the next or previous cell or merged cell position.
   *
   * @param {number} row The visual row index.
   * @param {number} column The visual column index.
   */ _onAfterSelectionFocusSet = /*#__PURE__*/ new WeakMap(), /**
   * Creates the horizontal and vertical cells order matrix (linked lists) for focused cell.
   */ _onAfterSelectionEnd = /*#__PURE__*/ new WeakMap(), /**
   * The `afterGetCellMeta` hook callback.
   *
   * @param {number} row Row index.
   * @param {number} col Column index.
   * @param {object} cellProperties The cell properties object.
   */ _onAfterGetCellMeta = /*#__PURE__*/ new WeakMap(), /**
   * `afterViewportRowCalculatorOverride` hook callback.
   *
   * @param {object} calc The row calculator object.
   */ _onAfterViewportRowCalculatorOverride = /*#__PURE__*/ new WeakMap(), /**
   * `afterViewportColumnCalculatorOverride` hook callback.
   *
   * @param {object} calc The column calculator object.
   */ _onAfterViewportColumnCalculatorOverride = /*#__PURE__*/ new WeakMap(), /**
   * The `modifyAutofillRange` hook callback.
   *
   * @param {number[]} fullArea The drag + base area coordinates (`[startRow, startColumn, endRow, endColumn]`).
   * @param {number[]} baseArea The selection area coordinates (`[startRow, startColumn, endRow, endColumn]`).
   * @returns {number[]} The new drag area (`[startRow, startColumn, endRow, endColumn]`).
   */ _onModifyAutofillRange = /*#__PURE__*/ new WeakMap(), /**
   * `afterCreateCol` hook callback.
   *
   * @param {number} column Column index.
   * @param {number} count Number of created columns.
   */ _onAfterCreateCol = /*#__PURE__*/ new WeakMap(), /**
   * `afterRemoveCol` hook callback.
   *
   * @param {number} column Column index.
   * @param {number} count Number of removed columns.
   */ _onAfterRemoveCol = /*#__PURE__*/ new WeakMap(), /**
   * `afterCreateRow` hook callback.
   *
   * @param {number} row Row index.
   * @param {number} count Number of created rows.
   * @param {string} source Source of change.
   */ _onAfterCreateRow = /*#__PURE__*/ new WeakMap(), /**
   * `afterRemoveRow` hook callback.
   *
   * @param {number} row Row index.
   * @param {number} count Number of removed rows.
   */ _onAfterRemoveRow = /*#__PURE__*/ new WeakMap(), /**
   * `beforeColumnMove` hook callback. Captures physical column positions of every merge
   * so they can be translated onto the new visual order in `afterColumnMove`.
   *
   * @param {number[]} columns Visual column indexes being moved.
   * @param {number} finalIndex Drop target visual index.
   * @param {number} dropIndex Drop index from drag.
   * @param {boolean} movePossible Whether the move is allowed.
   */ _onBeforeColumnMove = /*#__PURE__*/ new WeakMap(), /**
   * `afterColumnMove` hook callback. Translates merges using the pre-move snapshot
   * and the now-updated column index mapping. Auto-splits merges whose physical
   * columns are no longer contiguous.
   *
   * @param {number[]} columns Visual column indexes that were moved.
   * @param {number} finalIndex Drop target visual index.
   * @param {number} dropIndex Drop index from drag.
   * @param {boolean} movePossible Whether the move was allowed.
   * @param {boolean} orderChanged Whether the move actually changed the order.
   */ _onAfterColumnMove = /*#__PURE__*/ new WeakMap(), /**
   * `beforeRowMove` hook callback. Captures physical row positions of every merge
   * so they can be translated onto the new visual order in `afterRowMove`.
   *
   * @param {number[]} rows Visual row indexes being moved.
   * @param {number} finalIndex Drop target visual index.
   * @param {number} dropIndex Drop index from drag.
   * @param {boolean} movePossible Whether the move is allowed.
   */ _onBeforeRowMove = /*#__PURE__*/ new WeakMap(), /**
   * `afterRowMove` hook callback. Translates merges using the pre-move snapshot
   * and the now-updated row index mapping. Auto-splits merges whose physical
   * rows are no longer contiguous.
   *
   * @param {number[]} rows Visual row indexes that were moved.
   * @param {number} finalIndex Drop target visual index.
   * @param {number} dropIndex Drop index from drag.
   * @param {boolean} movePossible Whether the move was allowed.
   * @param {boolean} orderChanged Whether the move actually changed the order.
   */ _onAfterRowMove = /*#__PURE__*/ new WeakMap(), /**
   * `beforeColumnFreeze` / `beforeColumnUnfreeze` hook callback. `manualColumnFreeze`
   * reorders the visual sequence directly through the column index mapper, so we
   * need to translate merges through it the same way as for `manualColumnMove`.
   *
   * @param {number} column Visual column index being frozen/unfrozen.
   * @param {boolean} performed Whether the (un)freeze will actually run.
   */ _onBeforeColumnFreeze = /*#__PURE__*/ new WeakMap(), /**
   * `afterColumnFreeze` / `afterColumnUnfreeze` hook callback.
   *
   * @param {number} column Visual column index that was frozen/unfrozen.
   * @param {boolean} performed Whether the (un)freeze actually ran.
   */ _onAfterColumnFreeze = /*#__PURE__*/ new WeakMap(), /**
   * `afterChange` hook callback. Used to propagate merged cells after using Autofill.
   *
   * @param {Array} changes The changes array.
   * @param {string} source Determines the source of the change.
   */ _onAfterChange = /*#__PURE__*/ new WeakMap(), /**
   * `beforeDrawAreaBorders` hook callback.
   *
   * @param {Array} corners Visual coordinates of the area corners.
   * @param {string} className Class name for the area.
   */ _onBeforeDrawAreaBorders = /*#__PURE__*/ new WeakMap(), /**
   * `afterDrawSelection` hook callback.
   *
   * @param {number} currentRow Visual row index of the currently processed cell.
   * @param {number} currentColumn Visual column index of the currently cell.
   * @param {Array} cornersOfSelection Array of the current selection.
   * @param {number|undefined} layerLevel Number indicating which layer of selection is currently processed.
   * @returns {string|undefined}
   */ _onAfterDrawSelection = /*#__PURE__*/ new WeakMap(), /**
   * `beforeRemoveCellClassNames` hook callback.
   *
   * @returns {string[]}
   */ _onBeforeRemoveCellClassNames = /*#__PURE__*/ new WeakMap(), /**
   * Allows to prevent opening the editor while more than one merged cell is selected.
   *
   * @param {number} row Visual row index of the edited cell.
   * @param {number} column Visual column index of the edited cell.
   * @param {string | null} initialValue The initial editor value.
   * @param {MouseEvent | KeyboardEvent} event The event which was responsible for opening the editor.
   * @returns {boolean | undefined}
   */ _onBeforeBeginEditing = /*#__PURE__*/ new WeakMap(), /**
   * Hook used to modify the row height depends on the merged cells in the row.
   *
   * @param {number} height The row height value provided by the Core.
   * @param {number} row The visual row index.
   * @param {string} overlayType The overlay type that is currently rendered.
   * @returns {number}
   */ _onModifyRowHeightByOverlayName = /*#__PURE__*/ new WeakMap();
class MergeCells extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$base$2f$base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BasePlugin"] {
    /**
   * Returns the plugin key used to identify this plugin in Handsontable settings.
   */ static get PLUGIN_KEY() {
        return PLUGIN_KEY;
    }
    /**
   * Returns the priority order used to determine the order in which plugins are initialized.
   */ static get PLUGIN_PRIORITY() {
        return PLUGIN_PRIORITY;
    }
    /**
   * Returns the default settings applied when the plugin is enabled without explicit configuration.
   */ static get DEFAULT_SETTINGS() {
        const cells = [];
        return {
            [__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$base$2f$base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["defaultMainSettingSymbol"]]: 'cells',
            virtualized: false,
            cells
        };
    }
    /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link MergeCells#enablePlugin} method is called.
   *
   * @returns {boolean}
   */ isEnabled() {
        return !!this.hot.getSettings()[PLUGIN_KEY];
    }
    /**
   * Enables the plugin functionality for this Handsontable instance.
   */ enablePlugin() {
        if (this.enabled) {
            return;
        }
        this.mergedCellsCollection = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellsCollection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this);
        this.autofillCalculations = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$calculations$2f$autofill$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this);
        this.selectionCalculations = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$calculations$2f$selection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this);
        this.addHook('afterInit', _class_private_field_get(this, _onAfterInit));
        this.addHook('modifyTransformFocus', _class_private_field_get(this, _onModifyTransformFocus));
        this.addHook('modifyTransformStart', _class_private_field_get(this, _onModifyTransformStart));
        this.addHook('modifyTransformEnd', _class_private_field_get(this, _onModifyTransformEnd));
        this.addHook('beforeSelectionHighlightSet', _class_private_field_get(this, _onBeforeSelectionHighlightSet));
        this.addHook('beforeSetRangeStart', _class_private_field_get(this, _onBeforeSetRangeStart));
        this.addHook('beforeSetRangeStartOnly', _class_private_field_get(this, _onBeforeSetRangeStart));
        this.addHook('beforeSelectionFocusSet', _class_private_field_get(this, _onBeforeSelectionFocusSet));
        this.addHook('afterSelectionFocusSet', _class_private_field_get(this, _onAfterSelectionFocusSet));
        this.addHook('afterSelectionEnd', _class_private_field_get(this, _onAfterSelectionEnd));
        this.addHook('modifyGetCellCoords', _class_private_field_get(this, _onModifyGetCellCoords));
        this.addHook('modifyGetCoordsElement', _class_private_field_get(this, _onModifyGetCellCoords));
        this.addHook('afterIsMultipleSelection', _class_private_field_get(this, _onAfterIsMultipleSelection));
        this.addHook('afterRenderer', (TD, row, col)=>_class_private_field_get(this, _cellRenderer).after(TD, row, col));
        this.addHook('afterContextMenuDefaultOptions', (defaultOptions)=>_class_private_method_get(this, _addMergeActionsToContextMenu, addMergeActionsToContextMenu).call(this, defaultOptions));
        this.addHook('afterGetCellMeta', _class_private_field_get(this, _onAfterGetCellMeta));
        this.addHook('afterViewportRowCalculatorOverride', _class_private_field_get(this, _onAfterViewportRowCalculatorOverride));
        this.addHook('afterViewportColumnCalculatorOverride', _class_private_field_get(this, _onAfterViewportColumnCalculatorOverride));
        this.addHook('modifyAutofillRange', _class_private_field_get(this, _onModifyAutofillRange));
        this.addHook('afterCreateCol', _class_private_field_get(this, _onAfterCreateCol));
        this.addHook('afterRemoveCol', _class_private_field_get(this, _onAfterRemoveCol));
        this.addHook('afterCreateRow', _class_private_field_get(this, _onAfterCreateRow));
        this.addHook('afterRemoveRow', _class_private_field_get(this, _onAfterRemoveRow));
        this.addHook('beforeColumnMove', _class_private_field_get(this, _onBeforeColumnMove));
        this.addHook('afterColumnMove', _class_private_field_get(this, _onAfterColumnMove));
        this.addHook('beforeRowMove', _class_private_field_get(this, _onBeforeRowMove));
        this.addHook('afterRowMove', _class_private_field_get(this, _onAfterRowMove));
        this.addHook('beforeColumnFreeze', _class_private_field_get(this, _onBeforeColumnFreeze));
        this.addHook('afterColumnFreeze', _class_private_field_get(this, _onAfterColumnFreeze));
        this.addHook('beforeColumnUnfreeze', _class_private_field_get(this, _onBeforeColumnFreeze));
        this.addHook('afterColumnUnfreeze', _class_private_field_get(this, _onAfterColumnFreeze));
        this.addHook('afterChange', _class_private_field_get(this, _onAfterChange));
        this.addHook('beforeDrawBorders', _class_private_field_get(this, _onBeforeDrawAreaBorders));
        this.addHook('afterDrawSelection', _class_private_field_get(this, _onAfterDrawSelection));
        this.addHook('beforeRemoveCellClassNames', _class_private_field_get(this, _onBeforeRemoveCellClassNames));
        this.addHook('beforeBeginEditing', _class_private_field_get(this, _onBeforeBeginEditing));
        this.addHook('modifyRowHeightByOverlayName', _class_private_field_get(this, _onModifyRowHeightByOverlayName));
        this.addHook('beforeUndoStackChange', (action, source)=>{
            if (source === 'MergeCells') {
                return false;
            }
        });
        this.registerShortcuts();
        super.enablePlugin();
    }
    /**
   * Disables the plugin functionality for this Handsontable instance.
   */ disablePlugin() {
        this.clearCollections();
        this.unregisterShortcuts();
        this.hot.render();
        _class_private_field_set(this, _initialized, false);
        super.disablePlugin();
    }
    /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the
   * following configuration options:
   *  - [`mergeCells`](@/api/options.md#mergecells)
   */ updatePlugin() {
        this.disablePlugin();
        this.enablePlugin();
        this.generateFromSettings();
        _class_private_field_set(this, _initialized, true);
        super.updatePlugin();
    }
    /**
   * If the browser is recognized as Chrome, force an additional repaint to prevent showing the effects of a Chrome bug.
   *
   * Issue described in https://github.com/handsontable/dev-handsontable/issues/521.
   *
   * @private
   */ ifChromeForceRepaint() {
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isChrome"])()) {
            return;
        }
        const rowsToRefresh = [];
        let rowIndexesToRefresh = [];
        this.mergedCellsCollection.mergedCells.forEach((mergedCell)=>{
            const { row, rowspan } = mergedCell;
            for(let r = row + 1; r < row + rowspan; r++){
                rowIndexesToRefresh.push(r);
            }
        });
        // Remove duplicates
        rowIndexesToRefresh = [
            ...new Set(rowIndexesToRefresh)
        ];
        rowIndexesToRefresh.forEach((rowIndex)=>{
            const renderableRowIndex = this.hot.rowIndexMapper.getRenderableFromVisualIndex(rowIndex);
            if (renderableRowIndex === null) {
                return;
            }
            this.hot.view._wt.wtOverlays.getOverlays(true).map((overlay)=>overlay.name === 'master' ? overlay : overlay.clone.wtTable).forEach((wtTableRef)=>{
                const rowToRefresh = wtTableRef.getRow(renderableRowIndex);
                if (rowToRefresh) {
                    // Modify the TR's `background` property to later modify it asynchronously.
                    // The background color is getting modified only with the alpha, so the change should not be visible (and is
                    // covered by the TDs' background color).
                    rowToRefresh.style.background = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getStyle"])(rowToRefresh, 'backgroundColor')?.replace(')', ', 0.99)') ?? '';
                    rowsToRefresh.push(rowToRefresh);
                }
            });
        });
        // Asynchronously revert the TRs' `background` property to force a fresh repaint.
        this.hot._registerTimeout(()=>{
            rowsToRefresh.forEach((rowElement)=>{
                rowElement.style.background = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getStyle"])(rowElement, 'backgroundColor')?.replace(', 0.99)', '') ?? '';
            });
        }, 1);
    }
    /**
   * Validates a single setting object, represented by a single merged cell information object.
   *
   * @private
   * @param {object} setting An object with `row`, `col`, `rowspan` and `colspan` properties.
   * @returns {boolean}
   */ validateSetting(setting) {
        if (!setting) {
            return false;
        }
        if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"].containsNegativeValues(setting)) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"].NEGATIVE_VALUES_WARNING(setting));
            return false;
        }
        if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"].isOutOfBounds(setting, this.hot.countRows(), this.hot.countCols())) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"].IS_OUT_OF_BOUNDS_WARNING(setting));
            return false;
        }
        if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"].isSingleCell(setting)) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"].IS_SINGLE_CELL(setting));
            return false;
        }
        if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"].containsZeroSpan(setting)) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"].ZERO_SPAN_WARNING(setting));
            return false;
        }
        return true;
    }
    /**
   * Generates the merged cells from the settings provided to the plugin.
   *
   * @private
   */ generateFromSettings() {
        const validSettings = this.getSetting('cells').filter((mergeCellInfo)=>this.validateSetting(mergeCellInfo));
        const nonOverlappingSettings = this.mergedCellsCollection.filterOverlappingMergeCells(validSettings);
        const populatedNulls = [];
        nonOverlappingSettings.forEach((mergeCellInfo)=>{
            const { row, col, rowspan, colspan } = mergeCellInfo;
            const from = this.hot._createCellCoords(row, col);
            const to = this.hot._createCellCoords(row + rowspan - 1, col + colspan - 1);
            const mergeRange = this.hot._createCellRange(from, from, to);
            // Merging without data population.
            this.mergeRange(mergeRange, true, true);
            for(let r = row; r < row + rowspan; r++){
                for(let c = col; c < col + colspan; c++){
                    // Not resetting a cell representing a merge area's value.
                    if (r !== row || c !== col) {
                        populatedNulls.push([
                            r,
                            c,
                            null
                        ]);
                    }
                }
            }
        });
        // There are no merged cells. Thus, no data population is needed.
        if (populatedNulls.length === 0) {
            return;
        }
        // TODO: Change the `source` argument to a more meaningful value, e.g. `${this.pluginName}.clearCells`.
        this.hot.setDataAtCell(populatedNulls, undefined, undefined, this.pluginName ?? undefined);
    }
    /**
   * Clears the merged cells from the merged cell container.
   */ clearCollections() {
        this.mergedCellsCollection.clear();
    }
    /**
   * Returns `true` if a range is mergeable.
   *
   * @private
   * @param {object} newMergedCellInfo Merged cell information object to test.
   * @param {boolean} [auto=false] `true` if triggered at initialization.
   * @returns {boolean}
   */ canMergeRange(newMergedCellInfo, auto = false) {
        return auto ? true : this.validateSetting(newMergedCellInfo);
    }
    /**
   * Merges the selection provided as a cell range.
   *
   * @param {CellRange} [cellRange] Selection cell range.
   */ mergeSelection(cellRange = this.hot.getSelectedRangeActive()) {
        if (!cellRange) {
            return;
        }
        cellRange.setDirection(this.hot.isRtl() ? 'NE-SW' : 'NW-SE');
        const { from, to } = cellRange;
        this.unmergeRange(cellRange, true);
        this.mergeRange(cellRange);
        if (from.row !== null && from.col !== null && to.row !== null && to.col !== null) {
            this.hot.selectCell(from.row, from.col, to.row, to.col, false);
        }
    }
    /**
   * Unmerges the selection provided as a cell range.
   *
   * @param {CellRange} [cellRange] Selection cell range.
   */ unmergeSelection(cellRange = this.hot.getSelectedRangeActive()) {
        if (!cellRange) {
            return;
        }
        const { from, to } = cellRange;
        this.unmergeRange(cellRange, true);
        if (from.row !== null && from.col !== null && to.row !== null && to.col !== null) {
            this.hot.selectCell(from.row, from.col, to.row, to.col, false);
        }
    }
    /**
   * Merges cells in the provided cell range.
   *
   * @private
   * @param {CellRange} cellRange Cell range to merge.
   * @param {boolean} [auto=false] `true` if is called automatically, e.g. At initialization.
   * @param {boolean} [preventPopulation=false] `true`, if the method should not run `populateFromArray` at the end,
   *   but rather return its arguments.
   * @returns {Array|boolean} Returns an array of [row, column, dataUnderCollection] if preventPopulation is set to
   *   true. If the the merging process went successful, it returns `true`, otherwise - `false`.
   * @fires Hooks#beforeMergeCells
   * @fires Hooks#afterMergeCells
   */ mergeRange(cellRange, auto = false, preventPopulation = false) {
        const topStart = cellRange.getTopStartCorner();
        const bottomEnd = cellRange.getBottomEndCorner();
        if (topStart.row === null || topStart.col === null || bottomEnd.row === null || bottomEnd.col === null) {
            return false;
        }
        const mergeParent = {
            row: topStart.row,
            col: topStart.col,
            rowspan: bottomEnd.row - topStart.row + 1,
            colspan: bottomEnd.col - topStart.col + 1
        };
        const clearedData = [];
        let populationInfo = null;
        if (!this.canMergeRange(mergeParent, auto)) {
            return false;
        }
        this.hot.runHooks('beforeMergeCells', cellRange, auto);
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["rangeEach"])(0, mergeParent.rowspan - 1, (i)=>{
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["rangeEach"])(0, mergeParent.colspan - 1, (j)=>{
                let clearedValue = null;
                if (!clearedData[i]) {
                    clearedData[i] = [];
                }
                if (i === 0 && j === 0) {
                    clearedValue = this.hot.getSourceDataAtCell(this.hot.toPhysicalRow(mergeParent.row), this.hot.toPhysicalColumn(mergeParent.col));
                } else {
                    this.hot.setCellMeta(mergeParent.row + i, mergeParent.col + j, 'hidden', true);
                }
                clearedData[i][j] = clearedValue;
            });
        });
        this.hot.setCellMeta(mergeParent.row, mergeParent.col, 'spanned', true);
        const mergedCellAdded = this.mergedCellsCollection.add(mergeParent, auto);
        if (mergedCellAdded) {
            if (preventPopulation) {
                populationInfo = [
                    mergeParent.row,
                    mergeParent.col,
                    clearedData
                ];
            } else {
                // TODO: Change the `source` argument to a more meaningful value, e.g. `${this.pluginName}.clearCells`.
                this.hot.populateFromArray(mergeParent.row, mergeParent.col, clearedData, undefined, undefined, this.pluginName ?? undefined);
            }
            if (!auto) {
                this.ifChromeForceRepaint();
            }
            this.hot.runHooks('afterMergeCells', cellRange, mergeParent, auto);
            return populationInfo;
        }
        return true;
    }
    /**
   * Unmerges the selection provided as a cell range. If no cell range is provided, it uses the current selection.
   *
   * @private
   * @param {CellRange} cellRange Selection cell range.
   * @param {boolean} [auto=false] `true` if called automatically by the plugin.
   *
   * @fires Hooks#beforeUnmergeCells
   * @fires Hooks#afterUnmergeCells
   */ unmergeRange(cellRange, auto = false) {
        const mergedCells = this.mergedCellsCollection.getWithinRange(cellRange);
        if (mergedCells.length === 0) {
            return;
        }
        this.hot.runHooks('beforeUnmergeCells', cellRange, auto);
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(mergedCells, (currentCollection)=>{
            this.mergedCellsCollection.remove(currentCollection.row, currentCollection.col);
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["rangeEach"])(0, currentCollection.rowspan - 1, (i)=>{
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["rangeEach"])(0, currentCollection.colspan - 1, (j)=>{
                    this.hot.removeCellMeta(currentCollection.row + i, currentCollection.col + j, 'hidden');
                    this.hot.removeCellMeta(currentCollection.row + i, currentCollection.col + j, 'copyable');
                });
            });
            this.hot.removeCellMeta(currentCollection.row, currentCollection.col, 'spanned');
        });
        this.hot.runHooks('afterUnmergeCells', cellRange, auto);
        this.hot.render();
    }
    /**
   * Merges or unmerges, based on the cell range provided as `cellRange`.
   *
   * @private
   * @param {CellRange} cellRange The cell range to merge or unmerged.
   */ toggleMerge(cellRange) {
        const { from, to } = cellRange.clone().normalize();
        if (from.row === null || from.col === null || to.row === null || to.col === null) {
            return;
        }
        const mergedCell = this.mergedCellsCollection.get(from.row, from.col);
        const mergedCellCoversWholeRange = mergedCell !== false && mergedCell.row === from.row && mergedCell.col === from.col && mergedCell.row + mergedCell.rowspan - 1 === to.row && mergedCell.col + mergedCell.colspan - 1 === to.col;
        if (mergedCellCoversWholeRange) {
            this.unmergeRange(cellRange);
        } else {
            this.mergeSelection(cellRange);
        }
    }
    /**
   * Merges the specified range.
   *
   * @param {number} startRow Start row of the merged cell.
   * @param {number} startColumn Start column of the merged cell.
   * @param {number} endRow End row of the merged cell.
   * @param {number} endColumn End column of the merged cell.
   * @fires Hooks#beforeMergeCells
   * @fires Hooks#afterMergeCells
   */ merge(startRow, startColumn, endRow, endColumn) {
        const start = this.hot._createCellCoords(startRow, startColumn);
        const end = this.hot._createCellCoords(endRow, endColumn);
        this.mergeRange(this.hot._createCellRange(start, start, end));
    }
    /**
   * Unmerges the merged cell in the provided range.
   *
   * @param {number} startRow Start row of the merged cell.
   * @param {number} startColumn Start column of the merged cell.
   * @param {number} endRow End row of the merged cell.
   * @param {number} endColumn End column of the merged cell.
   * @fires Hooks#beforeUnmergeCells
   * @fires Hooks#afterUnmergeCells
   */ unmerge(startRow, startColumn, endRow, endColumn) {
        const start = this.hot._createCellCoords(startRow, startColumn);
        const end = this.hot._createCellCoords(endRow, endColumn);
        this.unmergeRange(this.hot._createCellRange(start, start, end));
    }
    /**
   * Register shortcuts responsible for toggling a merge.
   *
   * @private
   */ registerShortcuts() {
        const shortcutManager = this.hot.getShortcutManager();
        const gridContext = shortcutManager.getContext('grid');
        if (!gridContext) {
            return;
        }
        gridContext.addShortcut({
            keys: [
                [
                    'Control',
                    'm'
                ]
            ],
            callback: ()=>{
                const range = this.hot.getSelectedRangeActive();
                if (range && !range.isSingleHeader()) {
                    this.toggleMerge(range);
                    this.hot.render();
                }
            },
            runOnlyIf: (event)=>!event?.altKey,
            group: SHORTCUTS_GROUP
        });
    }
    /**
   * Unregister shortcuts responsible for toggling a merge.
   *
   * @private
   */ unregisterShortcuts() {
        const shortcutManager = this.hot.getShortcutManager();
        const gridContext = shortcutManager.getContext('grid');
        gridContext?.removeShortcutsByGroup(SHORTCUTS_GROUP);
    }
    /**
   * Modify viewport start when needed.
   *
   * @private
   * @param {object} calc The row calculator object.
   * @param {number} nrOfColumns Number of visual columns.
   */ modifyViewportRowStart(calc, nrOfColumns) {
        const rowMapper = this.hot.rowIndexMapper;
        const visualStartRow = rowMapper.getVisualFromRenderableIndex(calc.startRow) ?? 0;
        for(let visualColumnIndex = 0; visualColumnIndex < nrOfColumns; visualColumnIndex += 1){
            const mergeParentForViewportStart = this.mergedCellsCollection.get(visualStartRow, visualColumnIndex);
            if (mergeParentForViewportStart !== false) {
                const nearestNotHiddenRow = rowMapper.getNearestNotHiddenIndex(mergeParentForViewportStart.row, 1);
                const renderableIndexAtMergeStart = nearestNotHiddenRow !== null ? rowMapper.getRenderableFromVisualIndex(nearestNotHiddenRow) : null;
                if (renderableIndexAtMergeStart !== null && renderableIndexAtMergeStart < calc.startRow) {
                    calc.startRow = renderableIndexAtMergeStart;
                    this.modifyViewportRowStart(calc, nrOfColumns);
                    return;
                }
            }
        }
    }
    /**
   * Modify viewport end when needed.
   *
   * @private
   * @param {object} calc The row calculator object.
   * @param {number} nrOfColumns Number of visual columns.
   */ modifyViewportRowEnd(calc, nrOfColumns) {
        const rowMapper = this.hot.rowIndexMapper;
        const visualEndRow = rowMapper.getVisualFromRenderableIndex(calc.endRow) ?? 0;
        for(let visualColumnIndex = 0; visualColumnIndex < nrOfColumns; visualColumnIndex += 1){
            const mergeParentForViewportEnd = this.mergedCellsCollection.get(visualEndRow, visualColumnIndex);
            if (mergeParentForViewportEnd !== false) {
                const mergeEnd = mergeParentForViewportEnd.row + mergeParentForViewportEnd.rowspan - 1;
                const nearestRow = rowMapper.getNearestNotHiddenIndex(mergeEnd, -1);
                if (nearestRow !== null) {
                    const renderableIndexAtMergeEnd = rowMapper.getRenderableFromVisualIndex(nearestRow);
                    if (renderableIndexAtMergeEnd !== null && renderableIndexAtMergeEnd > calc.endRow) {
                        calc.endRow = renderableIndexAtMergeEnd;
                        this.modifyViewportRowEnd(calc, nrOfColumns);
                        return;
                    }
                }
            }
        }
    }
    /**
   * Modify viewport start when needed.
   *
   * @private
   * @param {object} calc The column calculator object.
   * @param {number} nrOfRows Number of visual rows.
   */ modifyViewportColumnStart(calc, nrOfRows) {
        const columnMapper = this.hot.columnIndexMapper;
        const visualStartCol = columnMapper.getVisualFromRenderableIndex(calc.startColumn) ?? 0;
        for(let visualRowIndex = 0; visualRowIndex < nrOfRows; visualRowIndex += 1){
            const mergeParentForViewportStart = this.mergedCellsCollection.get(visualRowIndex, visualStartCol);
            if (mergeParentForViewportStart !== false) {
                const nearestCol = columnMapper.getNearestNotHiddenIndex(mergeParentForViewportStart.col, 1);
                if (nearestCol !== null) {
                    const renderableIndexAtMergeStart = columnMapper.getRenderableFromVisualIndex(nearestCol);
                    if (renderableIndexAtMergeStart !== null && renderableIndexAtMergeStart < calc.startColumn) {
                        calc.startColumn = renderableIndexAtMergeStart;
                        this.modifyViewportColumnStart(calc, nrOfRows);
                        return;
                    }
                }
            }
        }
    }
    /**
   * Modify viewport end when needed.
   *
   * @private
   * @param {object} calc The column calculator object.
   * @param {number} nrOfRows Number of visual rows.
   */ modifyViewportColumnEnd(calc, nrOfRows) {
        const columnMapper = this.hot.columnIndexMapper;
        const visualEndCol = columnMapper.getVisualFromRenderableIndex(calc.endColumn) ?? 0;
        for(let visualRowIndex = 0; visualRowIndex < nrOfRows; visualRowIndex += 1){
            const mergeParentForViewportEnd = this.mergedCellsCollection.get(visualRowIndex, visualEndCol);
            if (mergeParentForViewportEnd !== false) {
                const mergeEnd = mergeParentForViewportEnd.col + mergeParentForViewportEnd.colspan - 1;
                const nearestCol = columnMapper.getNearestNotHiddenIndex(mergeEnd, -1);
                if (nearestCol !== null) {
                    const renderableIndexAtMergeEnd = columnMapper.getRenderableFromVisualIndex(nearestCol);
                    if (renderableIndexAtMergeEnd !== null && renderableIndexAtMergeEnd > calc.endColumn) {
                        calc.endColumn = renderableIndexAtMergeEnd;
                        this.modifyViewportColumnEnd(calc, nrOfRows);
                        return;
                    }
                }
            }
        }
    }
    /**
   * Translates merged cell coordinates to renderable indexes.
   *
   * @private
   * @param {number} parentRow Visual row index.
   * @param {number} rowspan Rowspan.
   * @param {number} parentColumn Visual column index.
   * @param {number} colspan Colspan.
   * @returns {Array<number>} A two-element array of `[renderableRow, renderableColumn]`.
   */ translateMergedCellToRenderable(parentRow, rowspan, parentColumn, colspan) {
        const { rowIndexMapper: rowMapper, columnIndexMapper: columnMapper } = this.hot;
        let firstNonHiddenRow;
        let firstNonHiddenColumn;
        if (rowspan === 0) {
            firstNonHiddenRow = rowMapper.getNearestNotHiddenIndex(parentRow, 1);
        } else {
            firstNonHiddenRow = rowMapper.getNearestNotHiddenIndex(parentRow + rowspan - 1, -1);
        }
        if (colspan === 0) {
            firstNonHiddenColumn = columnMapper.getNearestNotHiddenIndex(parentColumn, 1);
        } else {
            firstNonHiddenColumn = columnMapper.getNearestNotHiddenIndex(parentColumn + colspan - 1, -1);
        }
        const renderableRow = parentRow >= 0 && firstNonHiddenRow !== null ? rowMapper.getRenderableFromVisualIndex(firstNonHiddenRow) ?? parentRow : parentRow;
        const renderableColumn = parentColumn >= 0 && firstNonHiddenColumn !== null ? columnMapper.getRenderableFromVisualIndex(firstNonHiddenColumn) ?? parentColumn : parentColumn;
        return [
            renderableRow,
            renderableColumn
        ];
    }
    constructor(...args){
        super(...args), _class_private_method_init(this, _addMergeActionsToContextMenu), _class_private_field_init(this, _lastSelectedFocus, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _lastFocusDelta, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _focusOrder, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _cellRenderer, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _columnMoveSnapshot, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _rowMoveSnapshot, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _initialized, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterInit, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterIsMultipleSelection, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onModifyTransformFocus, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onModifyTransformStart, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onModifyTransformEnd, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onBeforeSelectionHighlightSet, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onModifyGetCellCoords, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onBeforeSetRangeStart, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onBeforeSelectionFocusSet, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterSelectionFocusSet, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterSelectionEnd, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterGetCellMeta, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterViewportRowCalculatorOverride, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterViewportColumnCalculatorOverride, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onModifyAutofillRange, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterCreateCol, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterRemoveCol, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterCreateRow, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterRemoveRow, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onBeforeColumnMove, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterColumnMove, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onBeforeRowMove, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterRowMove, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onBeforeColumnFreeze, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterColumnFreeze, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterChange, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onBeforeDrawAreaBorders, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onAfterDrawSelection, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onBeforeRemoveCellClassNames, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onBeforeBeginEditing, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _onModifyRowHeightByOverlayName, {
            writable: true,
            value: void 0
        }), _class_private_field_set(this, _lastSelectedFocus, null), _class_private_field_set(this, _lastFocusDelta, {
            row: 0,
            col: 0
        }), _class_private_field_set(this, _focusOrder, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$focusOrder$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["FocusOrder"]({
            mergedCellsGetter: (row, column)=>this.mergedCellsCollection.get(row, column),
            rowIndexMapper: this.hot.rowIndexMapper,
            columnIndexMapper: this.hot.columnIndexMapper
        })), _class_private_field_set(this, _cellRenderer, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$renderer$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createMergeCellRenderer"])(this)), _class_private_field_set(this, _columnMoveSnapshot, null), _class_private_field_set(this, _rowMoveSnapshot, null), _class_private_field_set(this, _initialized, false), _class_private_field_set(this, _onAfterInit, ()=>{
            this.generateFromSettings();
            this.hot.render();
            _class_private_field_set(this, _initialized, true);
        }), _class_private_field_set(this, _onAfterIsMultipleSelection, (isMultiple)=>{
            if (isMultiple) {
                const mergedCells = this.mergedCellsCollection.mergedCells;
                const selectionRange = this.hot.getSelectedRangeActive();
                if (!selectionRange) {
                    return isMultiple;
                }
                const topStartCoords = selectionRange.getTopStartCorner();
                const bottomEndCoords = selectionRange.getBottomEndCorner();
                for(let group = 0; group < mergedCells.length; group += 1){
                    if (topStartCoords.row === mergedCells[group].row && topStartCoords.col === mergedCells[group].col && bottomEndCoords.row === mergedCells[group].row + mergedCells[group].rowspan - 1 && bottomEndCoords.col === mergedCells[group].col + mergedCells[group].colspan - 1) {
                        return false;
                    }
                }
            }
            return isMultiple;
        }), _class_private_field_set(this, _onModifyTransformFocus, (delta)=>{
            _class_private_field_get(this, _lastFocusDelta).row = delta.row;
            _class_private_field_get(this, _lastFocusDelta).col = delta.col;
        }), _class_private_field_set(this, _onModifyTransformStart, (delta)=>{
            const selectedRange = this.hot.getSelectedRangeActive();
            if (!selectedRange) {
                return;
            }
            const { highlight } = selectedRange;
            const { columnIndexMapper, rowIndexMapper } = this.hot;
            if (_class_private_field_get(this, _lastSelectedFocus)) {
                if (_class_private_field_get(this, _lastSelectedFocus).row !== null && rowIndexMapper.getRenderableFromVisualIndex(_class_private_field_get(this, _lastSelectedFocus).row) !== null) {
                    highlight.row = _class_private_field_get(this, _lastSelectedFocus).row;
                }
                if (_class_private_field_get(this, _lastSelectedFocus).col !== null && columnIndexMapper.getRenderableFromVisualIndex(_class_private_field_get(this, _lastSelectedFocus).col) !== null) {
                    highlight.col = _class_private_field_get(this, _lastSelectedFocus).col;
                }
                _class_private_field_set(this, _lastSelectedFocus, null);
            }
            if (highlight.row === null || highlight.col === null) {
                return;
            }
            const mergedParent = this.mergedCellsCollection.get(highlight.row, highlight.col);
            if (!mergedParent) {
                return;
            }
            const visualColumnIndexStart = mergedParent.col;
            const visualColumnIndexEnd = mergedParent.col + mergedParent.colspan - 1;
            if (delta.col < 0) {
                const nextColumn = highlight.col >= visualColumnIndexStart && highlight.col <= visualColumnIndexEnd ? visualColumnIndexStart - 1 : visualColumnIndexEnd;
                const notHiddenColumnIndex = columnIndexMapper.getNearestNotHiddenIndex(nextColumn, -1);
                if (notHiddenColumnIndex === null) {
                    delta.col = -this.hot.view.countRenderableColumnsInRange(0, highlight.col);
                } else {
                    delta.col = -Math.max(this.hot.view.countRenderableColumnsInRange(notHiddenColumnIndex, highlight.col) - 1, 1);
                }
            } else if (delta.col > 0) {
                const nextColumn = highlight.col >= visualColumnIndexStart && highlight.col <= visualColumnIndexEnd ? visualColumnIndexEnd + 1 : visualColumnIndexStart;
                const notHiddenColumnIndex = columnIndexMapper.getNearestNotHiddenIndex(nextColumn, 1);
                if (notHiddenColumnIndex === null) {
                    delta.col = this.hot.view.countRenderableColumnsInRange(highlight.col, this.hot.countCols());
                } else {
                    delta.col = Math.max(this.hot.view.countRenderableColumnsInRange(highlight.col, notHiddenColumnIndex) - 1, 1);
                }
            }
            const visualRowIndexStart = mergedParent.row;
            const visualRowIndexEnd = mergedParent.row + mergedParent.rowspan - 1;
            if (delta.row < 0) {
                const nextRow = highlight.row >= visualRowIndexStart && highlight.row <= visualRowIndexEnd ? visualRowIndexStart - 1 : visualRowIndexEnd;
                const notHiddenRowIndex = rowIndexMapper.getNearestNotHiddenIndex(nextRow, -1);
                if (notHiddenRowIndex === null) {
                    delta.row = -this.hot.view.countRenderableRowsInRange(0, highlight.row);
                } else {
                    delta.row = -Math.max(this.hot.view.countRenderableRowsInRange(notHiddenRowIndex, highlight.row) - 1, 1);
                }
            } else if (delta.row > 0) {
                const nextRow = highlight.row >= visualRowIndexStart && highlight.row <= visualRowIndexEnd ? visualRowIndexEnd + 1 : visualRowIndexStart;
                const notHiddenRowIndex = rowIndexMapper.getNearestNotHiddenIndex(nextRow, 1);
                if (notHiddenRowIndex === null) {
                    delta.row = this.hot.view.countRenderableRowsInRange(highlight.row, this.hot.countRows());
                } else {
                    delta.row = Math.max(this.hot.view.countRenderableRowsInRange(highlight.row, notHiddenRowIndex) - 1, 1);
                }
            }
        }), _class_private_field_set(this, _onModifyTransformEnd, (delta)=>{
            const selectedRange = this.hot.getSelectedRangeActive();
            if (!selectedRange) {
                return;
            }
            const cloneRange = selectedRange.clone();
            const { to } = selectedRange;
            const toRow = to.row ?? 0;
            const toCol = to.col ?? 0;
            const { columnIndexMapper, rowIndexMapper } = this.hot;
            const expandCloneRange = (row, col)=>{
                cloneRange.expand(this.hot._createCellCoords(row, col));
                for(let i = 0; i < this.mergedCellsCollection.mergedCells.length; i += 1){
                    const range = this.mergedCellsCollection.mergedCells[i].getRange();
                    if (range) {
                        cloneRange.expandByRange(range);
                    }
                }
            };
            if (delta.col < 0) {
                let nextColumn = this.mergedCellsCollection.getStartMostColumnIndex(selectedRange, toCol) + delta.col;
                expandCloneRange(toRow, nextColumn);
                if (selectedRange.getHorizontalDirection() === 'E-W' && cloneRange.getHorizontalDirection() === 'E-W') {
                    nextColumn = cloneRange.getTopStartCorner().col ?? nextColumn;
                }
                const notHiddenColumnIndex = columnIndexMapper.getNearestNotHiddenIndex(nextColumn, 1);
                if (notHiddenColumnIndex !== null) {
                    delta.col = -Math.max(this.hot.view.countRenderableColumnsInRange(notHiddenColumnIndex, toCol) - 1, 1);
                }
            } else if (delta.col > 0) {
                let nextColumn = this.mergedCellsCollection.getEndMostColumnIndex(selectedRange, toCol) + delta.col;
                expandCloneRange(toRow, nextColumn);
                if (selectedRange.getHorizontalDirection() === 'W-E' && cloneRange.getHorizontalDirection() === 'W-E') {
                    nextColumn = cloneRange.getBottomEndCorner().col ?? nextColumn;
                }
                const notHiddenColumnIndex = columnIndexMapper.getNearestNotHiddenIndex(nextColumn, -1);
                if (notHiddenColumnIndex !== null) {
                    delta.col = Math.max(this.hot.view.countRenderableColumnsInRange(toCol, notHiddenColumnIndex) - 1, 1);
                }
            }
            if (delta.row < 0) {
                let nextRow = this.mergedCellsCollection.getTopMostRowIndex(selectedRange, toRow) + delta.row;
                expandCloneRange(nextRow, toCol);
                if (selectedRange.getVerticalDirection() === 'S-N' && cloneRange.getVerticalDirection() === 'S-N') {
                    nextRow = cloneRange.getTopStartCorner().row ?? nextRow;
                }
                const notHiddenRowIndex = rowIndexMapper.getNearestNotHiddenIndex(nextRow, 1);
                if (notHiddenRowIndex !== null) {
                    delta.row = -Math.max(this.hot.view.countRenderableRowsInRange(notHiddenRowIndex, toRow) - 1, 1);
                }
            } else if (delta.row > 0) {
                let nextRow = this.mergedCellsCollection.getBottomMostRowIndex(selectedRange, toRow) + delta.row;
                expandCloneRange(nextRow, toCol);
                if (selectedRange.getVerticalDirection() === 'N-S' && cloneRange.getVerticalDirection() === 'N-S') {
                    nextRow = cloneRange.getBottomStartCorner().row ?? nextRow;
                }
                const notHiddenRowIndex = rowIndexMapper.getNearestNotHiddenIndex(nextRow, -1);
                if (notHiddenRowIndex !== null) {
                    delta.row = Math.max(this.hot.view.countRenderableRowsInRange(toRow, notHiddenRowIndex) - 1, 1);
                }
            }
        }), _class_private_field_set(this, _onBeforeSelectionHighlightSet, ()=>{
            const selectedRange = this.hot.getSelectedRangeLast();
            if (!selectedRange) {
                return;
            }
            const { highlight } = selectedRange;
            if (this.hot.selection.isSelectedByColumnHeader() || this.hot.selection.isSelectedByRowHeader()) {
                _class_private_field_set(this, _lastSelectedFocus, highlight.clone());
                return;
            }
            for(let i = 0; i < this.mergedCellsCollection.mergedCells.length; i += 1){
                const range = this.mergedCellsCollection.mergedCells[i].getRange();
                if (range) {
                    selectedRange.expandByRange(range, false);
                }
            }
            for(let i = 0; i < this.mergedCellsCollection.mergedCells.length; i += 1){
                const range = this.mergedCellsCollection.mergedCells[i].getRange();
                if (range) {
                    selectedRange.expandByRange(range, false);
                }
            }
            if (highlight.row === null || highlight.col === null) {
                return;
            }
            const mergedParent = this.mergedCellsCollection.get(highlight.row, highlight.col);
            _class_private_field_set(this, _lastSelectedFocus, highlight.clone());
            if (mergedParent) {
                highlight.assign(mergedParent);
            }
        }), _class_private_field_set(this, _onModifyGetCellCoords, (row, column, topmost, source)=>{
            if (row < 0 || column < 0) {
                return;
            }
            const mergeParent = this.mergedCellsCollection.get(row, column);
            if (!mergeParent) {
                return;
            }
            const { row: mergeRow, col: mergeColumn, colspan, rowspan } = mergeParent;
            const topStartRow = mergeRow;
            const topStartColumn = mergeColumn;
            const bottomEndRow = mergeRow + rowspan - 1;
            const bottomEndColumn = mergeColumn + colspan - 1;
            if (source === 'render' && this.getSetting('virtualized')) {
                const overlayName = this.hot.view.getActiveOverlayName();
                const firstRenderedRow = [
                    'top',
                    'top_inline_start_corner'
                ].includes(overlayName) ? 0 : this.hot.getFirstRenderedVisibleRow();
                const firstRenderedColumn = [
                    'inline_start',
                    'top_inline_start_corner',
                    'bottom_inline_start_corner'
                ].includes(overlayName) ? 0 : this.hot.getFirstRenderedVisibleColumn();
                return [
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(firstRenderedRow, topStartRow, bottomEndRow),
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(firstRenderedColumn, topStartColumn, bottomEndColumn),
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(this.hot.getLastRenderedVisibleRow(), topStartRow, bottomEndRow),
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(this.hot.getLastRenderedVisibleColumn(), topStartColumn, bottomEndColumn)
                ];
            }
            return [
                topStartRow,
                topStartColumn,
                bottomEndRow,
                bottomEndColumn
            ];
        }), _class_private_field_set(this, _onBeforeSetRangeStart, ()=>{
            _class_private_field_set(this, _lastSelectedFocus, null);
        }), _class_private_field_set(this, _onBeforeSelectionFocusSet, ()=>{
            if (!_class_private_field_get(this, _lastSelectedFocus) || _class_private_field_get(this, _lastSelectedFocus).isCell()) {
                return;
            }
            const selectedRange = this.hot.getSelectedRangeActive();
            if (!selectedRange) {
                return;
            }
            const verticalDir = selectedRange.getVerticalDirection();
            const horizontalDir = selectedRange.getHorizontalDirection();
            const focusCoords = _class_private_field_get(this, _lastSelectedFocus).clone().normalize();
            if (focusCoords.row === null || focusCoords.col === null) {
                return;
            }
            _class_private_field_get(this, _focusOrder).setActiveNode(focusCoords.row, focusCoords.col);
            if (_class_private_field_get(this, _lastFocusDelta).row > 0 || _class_private_field_get(this, _lastFocusDelta).col > 0) {
                _class_private_field_get(this, _focusOrder).setPrevNodeAsActive();
            } else if (horizontalDir === 'E-W' && _class_private_field_get(this, _lastFocusDelta).col < 0 || verticalDir === 'S-N' && _class_private_field_get(this, _lastFocusDelta).row < 0) {
                _class_private_field_get(this, _focusOrder).setNextNodeAsActive();
            }
        }), _class_private_field_set(this, _onAfterSelectionFocusSet, (row, column)=>{
            const { columnIndexMapper, rowIndexMapper } = this.hot;
            let activeSelectionLayerIndex = this.hot.getActiveSelectionLayerIndex();
            let notHiddenRowIndex = null;
            let notHiddenColumnIndex = null;
            if (_class_private_field_get(this, _lastFocusDelta).col < 0) {
                const { rowEnd, colEnd, selectionLayer } = _class_private_field_get(this, _focusOrder).getPrevHorizontalNode();
                notHiddenColumnIndex = columnIndexMapper.getNearestNotHiddenIndex(colEnd, -1);
                notHiddenRowIndex = rowIndexMapper.getNearestNotHiddenIndex(rowEnd, -1);
                activeSelectionLayerIndex = selectionLayer;
            } else if (_class_private_field_get(this, _lastFocusDelta).col > 0) {
                const { rowStart, colStart, selectionLayer } = _class_private_field_get(this, _focusOrder).getNextHorizontalNode();
                notHiddenColumnIndex = columnIndexMapper.getNearestNotHiddenIndex(colStart, 1);
                notHiddenRowIndex = rowIndexMapper.getNearestNotHiddenIndex(rowStart, 1);
                activeSelectionLayerIndex = selectionLayer;
            } else if (_class_private_field_get(this, _lastFocusDelta).row < 0) {
                const { rowEnd, colEnd, selectionLayer } = _class_private_field_get(this, _focusOrder).getPrevVerticalNode();
                notHiddenColumnIndex = columnIndexMapper.getNearestNotHiddenIndex(colEnd, -1);
                notHiddenRowIndex = rowIndexMapper.getNearestNotHiddenIndex(rowEnd, -1);
                activeSelectionLayerIndex = selectionLayer;
            } else if (_class_private_field_get(this, _lastFocusDelta).row > 0) {
                const { rowStart, colStart, selectionLayer } = _class_private_field_get(this, _focusOrder).getNextVerticalNode();
                notHiddenColumnIndex = columnIndexMapper.getNearestNotHiddenIndex(colStart, 1);
                notHiddenRowIndex = rowIndexMapper.getNearestNotHiddenIndex(rowStart, 1);
                activeSelectionLayerIndex = selectionLayer;
            }
            if (notHiddenRowIndex !== null || notHiddenColumnIndex !== null) {
                this.hot.selection.setActiveSelectionLayerIndex(activeSelectionLayerIndex);
                const selectedRange = this.hot.getSelectedRangeActive();
                const resolvedRow = notHiddenRowIndex ?? 0;
                const resolvedCol = notHiddenColumnIndex ?? 0;
                const coords = this.hot._createCellCoords(resolvedRow, resolvedCol);
                const mergeParent = this.mergedCellsCollection.get(resolvedRow, resolvedCol);
                const focusHighlight = this.hot.selection.highlight.getFocus();
                row = resolvedRow;
                column = resolvedCol;
                if (selectedRange) {
                    if (mergeParent) {
                        selectedRange.highlight.assign({
                            row: this.hot.rowIndexMapper.getNearestNotHiddenIndex(mergeParent.row, 1) ?? undefined,
                            col: this.hot.columnIndexMapper.getNearestNotHiddenIndex(mergeParent.col, 1) ?? undefined
                        });
                    } else {
                        selectedRange.highlight.assign(coords);
                    }
                }
                focusHighlight.clear();
                focusHighlight.add(coords).commit();
            }
            _class_private_field_get(this, _focusOrder).setActiveNode(row, column, activeSelectionLayerIndex);
            _class_private_field_set(this, _lastFocusDelta, {
                row: 0,
                col: 0
            });
        }), _class_private_field_set(this, _onAfterSelectionEnd, ()=>{
            const selectedRanges = this.hot.getSelectedRange();
            if (selectedRanges) {
                _class_private_field_get(this, _focusOrder).buildFocusOrder(selectedRanges);
            }
        }), _class_private_field_set(this, _onAfterGetCellMeta, (row, col, cellProperties)=>{
            const mergeParent = this.mergedCellsCollection.get(row, col);
            if (mergeParent) {
                if (mergeParent.row !== row || mergeParent.col !== col) {
                    cellProperties.copyable = false;
                    cellProperties.hidden = true;
                } else {
                    cellProperties.rowspan = mergeParent.rowspan;
                    cellProperties.colspan = mergeParent.colspan;
                    cellProperties.spanned = true;
                }
            }
        }), _class_private_field_set(this, _onAfterViewportRowCalculatorOverride, (calc)=>{
            if (this.getSetting('virtualized')) {
                return;
            }
            const nrOfColumns = this.hot.countCols();
            this.modifyViewportRowStart(calc, nrOfColumns);
            this.modifyViewportRowEnd(calc, nrOfColumns);
        }), _class_private_field_set(this, _onAfterViewportColumnCalculatorOverride, (calc)=>{
            if (this.getSetting('virtualized')) {
                return;
            }
            const nrOfRows = this.hot.countRows();
            this.modifyViewportColumnStart(calc, nrOfRows);
            this.modifyViewportColumnEnd(calc, nrOfRows);
        }), _class_private_field_set(this, _onModifyAutofillRange, (fullArea, baseArea)=>{
            const dragDirection = this.autofillCalculations.getDirection(baseArea, fullArea);
            if (this.autofillCalculations.dragAreaOverlapsCollections(baseArea, fullArea, dragDirection)) {
                return baseArea;
            }
            const from = this.hot._createCellCoords(baseArea[0], baseArea[1]);
            const to = this.hot._createCellCoords(baseArea[2], baseArea[3]);
            const range = this.hot._createCellRange(from, from, to);
            const mergedCellsWithinSelectionArea = this.mergedCellsCollection.getWithinRange(range);
            if (mergedCellsWithinSelectionArea.length === 0) {
                return fullArea;
            }
            return this.autofillCalculations.snapDragArea(baseArea, fullArea, dragDirection, mergedCellsWithinSelectionArea);
        }), _class_private_field_set(this, _onAfterCreateCol, (column, count)=>{
            this.mergedCellsCollection.shiftCollections('right', column, count);
        }), _class_private_field_set(this, _onAfterRemoveCol, (column, count)=>{
            this.mergedCellsCollection.shiftCollections('left', column, count);
        }), _class_private_field_set(this, _onAfterCreateRow, (row, count, source)=>{
            if (source === 'auto') {
                return;
            }
            this.mergedCellsCollection.shiftCollections('down', row, count);
        }), _class_private_field_set(this, _onAfterRemoveRow, (row, count)=>{
            this.mergedCellsCollection.shiftCollections('up', row, count);
        }), _class_private_field_set(this, _onBeforeColumnMove, (_columns, _finalIndex, _dropIndex, movePossible)=>{
            if (!movePossible || !_class_private_field_get(this, _initialized)) {
                _class_private_field_set(this, _columnMoveSnapshot, null);
                return;
            }
            _class_private_field_set(this, _columnMoveSnapshot, this.mergedCellsCollection.capturePhysicalSpans('column'));
        }), _class_private_field_set(this, _onAfterColumnMove, (_columns, _finalIndex, _dropIndex, _movePossible, orderChanged)=>{
            const snapshot = _class_private_field_get(this, _columnMoveSnapshot);
            _class_private_field_set(this, _columnMoveSnapshot, null);
            if (!orderChanged || !snapshot) {
                return;
            }
            this.mergedCellsCollection.translateAfterAxisMove('column', snapshot);
            this.hot.render();
        }), _class_private_field_set(this, _onBeforeRowMove, (_rows, _finalIndex, _dropIndex, movePossible)=>{
            if (!movePossible || !_class_private_field_get(this, _initialized)) {
                _class_private_field_set(this, _rowMoveSnapshot, null);
                return;
            }
            _class_private_field_set(this, _rowMoveSnapshot, this.mergedCellsCollection.capturePhysicalSpans('row'));
        }), _class_private_field_set(this, _onAfterRowMove, (_rows, _finalIndex, _dropIndex, _movePossible, orderChanged)=>{
            const snapshot = _class_private_field_get(this, _rowMoveSnapshot);
            _class_private_field_set(this, _rowMoveSnapshot, null);
            if (!orderChanged || !snapshot) {
                return;
            }
            this.mergedCellsCollection.translateAfterAxisMove('row', snapshot);
            this.hot.render();
        }), _class_private_field_set(this, _onBeforeColumnFreeze, (_column, performed)=>{
            if (!performed || !_class_private_field_get(this, _initialized)) {
                _class_private_field_set(this, _columnMoveSnapshot, null);
                return;
            }
            _class_private_field_set(this, _columnMoveSnapshot, this.mergedCellsCollection.capturePhysicalSpans('column'));
        }), _class_private_field_set(this, _onAfterColumnFreeze, (_column, performed)=>{
            const snapshot = _class_private_field_get(this, _columnMoveSnapshot);
            _class_private_field_set(this, _columnMoveSnapshot, null);
            if (!performed || !snapshot) {
                return;
            }
            this.mergedCellsCollection.translateAfterAxisMove('column', snapshot);
            this.hot.render();
        }), _class_private_field_set(this, _onAfterChange, (changes, source)=>{
            if (source !== 'Autofill.fill') {
                return;
            }
            this.autofillCalculations.recreateAfterDataPopulation(changes);
        }), _class_private_field_set(this, _onBeforeDrawAreaBorders, (corners, className)=>{
            if (className && className === 'area') {
                const selectedRange = this.hot.getSelectedRangeActive();
                if (!selectedRange) {
                    return;
                }
                const mergedCellsWithinRange = this.mergedCellsCollection.getWithinRange(selectedRange);
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(mergedCellsWithinRange, (mergedCell)=>{
                    if (selectedRange.getBottomEndCorner().row === mergedCell.getLastRow() && selectedRange.getBottomEndCorner().col === mergedCell.getLastColumn()) {
                        corners[2] = mergedCell.row;
                        corners[3] = mergedCell.col;
                    }
                });
            }
        }), _class_private_field_set(this, _onAfterDrawSelection, (currentRow, currentColumn, cornersOfSelection, layerLevel)=>{
            // Nothing's selected (hook might be triggered by the custom borders)
            if (!cornersOfSelection) {
                return;
            }
            return this.selectionCalculations.getSelectedMergedCellClassName(currentRow, currentColumn, cornersOfSelection, layerLevel);
        }), _class_private_field_set(this, _onBeforeRemoveCellClassNames, ()=>{
            return this.selectionCalculations.getSelectedMergedCellClassNameToRemove();
        }), _class_private_field_set(this, _onBeforeBeginEditing, (row, column, initialValue, event)=>{
            if (!(event instanceof MouseEvent)) {
                return;
            }
            const selection = this.hot.getSelectedRangeActive();
            if (!selection) {
                return;
            }
            const mergeCell = this.mergedCellsCollection.getByRange(selection);
            if (!mergeCell) {
                return;
            }
            const from = this.hot._createCellCoords(mergeCell.row, mergeCell.col);
            const to = this.hot._createCellCoords(mergeCell.row + mergeCell.rowspan - 1, mergeCell.col + mergeCell.colspan - 1);
            return this.hot.selection.getLayerLevel() === 0 && selection.isEqual(this.hot._createCellRange(from, from, to));
        }), _class_private_field_set(this, _onModifyRowHeightByOverlayName, (height, row, overlayType)=>{
            if (this.hot.getSettings().rowHeaders || // merged cells do not work with the bottom overlays
            overlayType === 'bottom' || overlayType === 'bottom_inline_start_corner') {
                return height;
            }
            let firstColumn;
            let lastColumn;
            if (overlayType === 'master') {
                firstColumn = this.hot.getFirstRenderedVisibleColumn();
                lastColumn = this.hot.getLastRenderedVisibleColumn();
            } else {
                const activeOverlay = this.hot.view.getOverlayByName(overlayType);
                const overlayWtTable = activeOverlay?.clone?.wtTable;
                if (!overlayWtTable) {
                    return height;
                }
                firstColumn = this.hot.columnIndexMapper.getVisualFromRenderableIndex(overlayWtTable.getFirstRenderedColumn());
                lastColumn = this.hot.columnIndexMapper.getVisualFromRenderableIndex(overlayWtTable.getLastRenderedColumn());
            }
            if (firstColumn === null || firstColumn === undefined) {
                return height;
            }
            const firstMergedCellInRow = this.mergedCellsCollection.get(row, firstColumn);
            if (!firstMergedCellInRow) {
                return height;
            }
            const from = this.hot._createCellCoords(row, firstColumn);
            const to = this.hot._createCellCoords(row, lastColumn ?? firstColumn);
            const viewportRange = this.hot._createCellRange(from, from, to);
            const mergedCellsWithinRange = this.mergedCellsCollection.getWithinRange(viewportRange, true);
            const maxRowspan = mergedCellsWithinRange.reduce((acc, { rowspan })=>Math.max(acc, rowspan), 1);
            let rowspanCorrection = 0;
            if (mergedCellsWithinRange.length > 1 && mergedCellsWithinRange[0].rowspan < maxRowspan) {
                rowspanCorrection = maxRowspan - mergedCellsWithinRange[0].rowspan;
            }
            mergedCellsWithinRange.forEach(({ rowspan })=>{
                let rowspanAfterCorrection = 0;
                if (overlayType === 'top' || overlayType === 'top_inline_start_corner') {
                    rowspanAfterCorrection = Math.min(maxRowspan, this.hot.view.countNotHiddenFixedRowsTop() - row);
                } else {
                    rowspanAfterCorrection = rowspan - rowspanCorrection;
                }
                height = Math.max(height ?? 0, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["sumCellsHeights"])(this.hot, row, rowspanAfterCorrection));
            });
            return height;
        });
    }
}
function addMergeActionsToContextMenu(defaultOptions) {
    defaultOptions.items.push({
        name: '---------'
    }, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$contextMenuItem$2f$toggleMerge$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"])(this));
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/mergeCells/renderer.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createMergeCellRenderer",
    ()=>createMergeCellRenderer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/browser.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/mergeCells/utils.mjs [client] (ecmascript)");
;
;
/**
 * Clamps the not-hidden row and column indexes to the virtual viewport boundaries
 * based on the active overlay, ensuring merged cells don't extend beyond the visible area.
 */ function clampToVirtualViewport(hot, notHiddenRow, notHiddenColumn) {
    const overlayName = hot.view.getActiveOverlayName();
    if (![
        'top',
        'top_inline_start_corner'
    ].includes(overlayName)) {
        const firstRenderedVisibleRow = hot.getFirstRenderedVisibleRow();
        if (notHiddenRow !== null && firstRenderedVisibleRow !== null) {
            notHiddenRow = Math.max(notHiddenRow, firstRenderedVisibleRow);
        }
    }
    if (![
        'inline_start',
        'top_inline_start_corner',
        'bottom_inline_start_corner'
    ].includes(overlayName)) {
        const firstRenderedVisibleColumn = hot.getFirstRenderedVisibleColumn();
        if (notHiddenColumn !== null && firstRenderedVisibleColumn !== null) {
            notHiddenColumn = Math.max(notHiddenColumn, firstRenderedVisibleColumn);
        }
    }
    return [
        notHiddenRow,
        notHiddenColumn
    ];
}
function createMergeCellRenderer(plugin) {
    const hot = plugin.hot;
    const { rowIndexMapper: rowMapper, columnIndexMapper: columnMapper } = hot;
    const updateNextCellsHeight = new Map();
    /**
   * Runs before the cell is rendered.
   *
   * @private
   */ function before() {}
    /**
   * Runs after the cell is rendered.
   *
   * @private
   * @param {HTMLElement} TD The cell to be modified.
   * @param {number} row Visual row index.
   * @param {number} col Visual column index.
   */ function after(TD, row, col) {
        const mergedCell = plugin.mergedCellsCollection.get(row, col);
        if (mergedCell === false) {
            TD.removeAttribute('rowspan');
            TD.removeAttribute('colspan');
            if (updateNextCellsHeight.has(row) && !hot.getSettings().rowHeaders) {
                TD.style.height = `${updateNextCellsHeight.get(row)}px`;
                updateNextCellsHeight.delete(row);
            }
            TD.style.display = '';
            return;
        }
        const { row: origRow, col: origColumn, colspan: origColspan, rowspan: origRowspan } = mergedCell;
        const [lastMergedRowIndex, lastMergedColumnIndex] = plugin.translateMergedCellToRenderable(origRow, origRowspan, origColumn, origColspan);
        const isVirtualRenderingEnabled = plugin.getSetting('virtualized');
        if (origColumn === 0 && !hot.getSettings().rowHeaders) {
            const rowHeights = hot._getRowHeightFromSettings(row);
            if (rowHeights !== undefined) {
                const borderBoxSizing = hot.stylesHandler.areCellsBorderBox();
                const borderCompensation = borderBoxSizing ? 0 : 1;
                updateNextCellsHeight.set(row, rowHeights - borderCompensation);
            } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isSafari"])()) {
                // Safari bug fix - the height of the cells next to the merged cell must be defined
                // so that their height is proportional to the height of the merged cell
                // (this emulates default behavior in Chrome, FF etc.)
                const height = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$mergeCells$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["sumCellsHeights"])(hot, origRow, origRowspan);
                updateNextCellsHeight.set(row, height / origRowspan);
            }
        }
        const renderedRowIndex = rowMapper.getRenderableFromVisualIndex(row) ?? 0;
        const renderedColumnIndex = columnMapper.getRenderableFromVisualIndex(col) ?? 0;
        const maxRowSpan = lastMergedRowIndex - renderedRowIndex + 1; // Number of rendered columns.
        const maxColSpan = lastMergedColumnIndex - renderedColumnIndex + 1; // Number of rendered columns.
        let notHiddenRow = rowMapper.getNearestNotHiddenIndex(origRow, 1);
        let notHiddenColumn = columnMapper.getNearestNotHiddenIndex(origColumn, 1);
        if (isVirtualRenderingEnabled) {
            [notHiddenRow, notHiddenColumn] = clampToVirtualViewport(hot, notHiddenRow, notHiddenColumn);
        }
        const notHiddenRowspan = Math.min(origRowspan, maxRowSpan);
        const notHiddenColspan = Math.min(origColspan, maxColSpan);
        if (notHiddenRow === row && notHiddenColumn === col) {
            TD.setAttribute('rowspan', String(notHiddenRowspan));
            TD.setAttribute('colspan', String(notHiddenColspan));
        } else {
            TD.removeAttribute('rowspan');
            TD.removeAttribute('colspan');
            TD.style.display = 'none';
        }
    }
    return {
        before,
        after
    };
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/mergeCells/utils.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Calculates the total height of the merged cell.
 *
 * @param {Core} hotInstance The Handsontable instance.
 * @param {*} row The merged cell's row index.
 * @param {*} rowspan The merged cell height.
 * @returns {number}
 */ __turbopack_context__.s([
    "sumCellsHeights",
    ()=>sumCellsHeights
]);
function sumCellsHeights(hotInstance, row, rowspan) {
    const { rowIndexMapper, stylesHandler } = hotInstance;
    let height = 0;
    for(let i = row; i < row + rowspan; i++){
        if (!rowIndexMapper.isHidden(i)) {
            height += hotInstance.getRowHeight(i) ?? stylesHandler.getDefaultRowHeight(i);
        }
    }
    return height;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
]);

//# sourceMappingURL=node_modules_handsontable_plugins_mergeCells_329431a9.async.js.map