(globalThis["TURBOPACK"] || (globalThis["TURBOPACK"] = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/** Returns [attr, value] for setting on elements (e.g. tabindex, aria-*) */ __turbopack_context__.s([
    "A11Y_ACTIVEDESCENDANT",
    ()=>A11Y_ACTIVEDESCENDANT,
    "A11Y_ALERTDIALOG",
    ()=>A11Y_ALERTDIALOG,
    "A11Y_AUTOCOMPLETE",
    ()=>A11Y_AUTOCOMPLETE,
    "A11Y_BUSY",
    ()=>A11Y_BUSY,
    "A11Y_CHECKBOX",
    ()=>A11Y_CHECKBOX,
    "A11Y_CHECKED",
    ()=>A11Y_CHECKED,
    "A11Y_COLCOUNT",
    ()=>A11Y_COLCOUNT,
    "A11Y_COLINDEX",
    ()=>A11Y_COLINDEX,
    "A11Y_COLUMNHEADER",
    ()=>A11Y_COLUMNHEADER,
    "A11Y_COMBOBOX",
    ()=>A11Y_COMBOBOX,
    "A11Y_CONTROLS",
    ()=>A11Y_CONTROLS,
    "A11Y_DESCRIBED_BY",
    ()=>A11Y_DESCRIBED_BY,
    "A11Y_DIALOG",
    ()=>A11Y_DIALOG,
    "A11Y_DISABLED",
    ()=>A11Y_DISABLED,
    "A11Y_EXPANDED",
    ()=>A11Y_EXPANDED,
    "A11Y_GRIDCELL",
    ()=>A11Y_GRIDCELL,
    "A11Y_GRIDCELL_BUTTON",
    ()=>A11Y_GRIDCELL_BUTTON,
    "A11Y_GROUP",
    ()=>A11Y_GROUP,
    "A11Y_HASPOPUP",
    ()=>A11Y_HASPOPUP,
    "A11Y_HIDDEN",
    ()=>A11Y_HIDDEN,
    "A11Y_INVALID",
    ()=>A11Y_INVALID,
    "A11Y_LABEL",
    ()=>A11Y_LABEL,
    "A11Y_LABELED_BY",
    ()=>A11Y_LABELED_BY,
    "A11Y_LISTBOX",
    ()=>A11Y_LISTBOX,
    "A11Y_LIVE",
    ()=>A11Y_LIVE,
    "A11Y_MENU",
    ()=>A11Y_MENU,
    "A11Y_MENU_ITEM",
    ()=>A11Y_MENU_ITEM,
    "A11Y_MENU_ITEM_CHECKBOX",
    ()=>A11Y_MENU_ITEM_CHECKBOX,
    "A11Y_MODAL",
    ()=>A11Y_MODAL,
    "A11Y_MULTISELECTABLE",
    ()=>A11Y_MULTISELECTABLE,
    "A11Y_OPTION",
    ()=>A11Y_OPTION,
    "A11Y_POSINSET",
    ()=>A11Y_POSINSET,
    "A11Y_PRESENTATION",
    ()=>A11Y_PRESENTATION,
    "A11Y_READONLY",
    ()=>A11Y_READONLY,
    "A11Y_RELEVANT",
    ()=>A11Y_RELEVANT,
    "A11Y_ROW",
    ()=>A11Y_ROW,
    "A11Y_ROWCOUNT",
    ()=>A11Y_ROWCOUNT,
    "A11Y_ROWGROUP",
    ()=>A11Y_ROWGROUP,
    "A11Y_ROWHEADER",
    ()=>A11Y_ROWHEADER,
    "A11Y_ROWINDEX",
    ()=>A11Y_ROWINDEX,
    "A11Y_SCOPE_COL",
    ()=>A11Y_SCOPE_COL,
    "A11Y_SCOPE_ROW",
    ()=>A11Y_SCOPE_ROW,
    "A11Y_SELECTED",
    ()=>A11Y_SELECTED,
    "A11Y_SETSIZE",
    ()=>A11Y_SETSIZE,
    "A11Y_SORT",
    ()=>A11Y_SORT,
    "A11Y_TABINDEX",
    ()=>A11Y_TABINDEX,
    "A11Y_TEXT",
    ()=>A11Y_TEXT,
    "A11Y_TREEGRID",
    ()=>A11Y_TREEGRID
]);
const A11Y_TABINDEX = (val)=>[
        'tabindex',
        val
    ];
_c = A11Y_TABINDEX;
const A11Y_TREEGRID = ()=>[
        'role',
        'treegrid'
    ];
_c1 = A11Y_TREEGRID;
const A11Y_PRESENTATION = ()=>[
        'role',
        'presentation'
    ];
_c2 = A11Y_PRESENTATION;
const A11Y_GRIDCELL = ()=>[
        'role',
        'gridcell'
    ];
_c3 = A11Y_GRIDCELL;
const A11Y_GRIDCELL_BUTTON = ()=>[
        'role',
        'gridcell button'
    ];
_c4 = A11Y_GRIDCELL_BUTTON;
const A11Y_ROWHEADER = ()=>[
        'role',
        'rowheader'
    ];
_c5 = A11Y_ROWHEADER;
const A11Y_ROWGROUP = ()=>[
        'role',
        'rowgroup'
    ];
_c6 = A11Y_ROWGROUP;
const A11Y_COLUMNHEADER = ()=>[
        'role',
        'columnheader'
    ];
_c7 = A11Y_COLUMNHEADER;
const A11Y_ROW = ()=>[
        'role',
        'row'
    ];
_c8 = A11Y_ROW;
const A11Y_MENU = ()=>[
        'role',
        'menu'
    ];
_c9 = A11Y_MENU;
const A11Y_MENU_ITEM = ()=>[
        'role',
        'menuitem'
    ];
_c10 = A11Y_MENU_ITEM;
const A11Y_MENU_ITEM_CHECKBOX = ()=>[
        'role',
        'menuitemcheckbox'
    ];
_c11 = A11Y_MENU_ITEM_CHECKBOX;
const A11Y_COMBOBOX = ()=>[
        'role',
        'combobox'
    ];
_c12 = A11Y_COMBOBOX;
const A11Y_LISTBOX = ()=>[
        'role',
        'listbox'
    ];
_c13 = A11Y_LISTBOX;
const A11Y_OPTION = ()=>[
        'role',
        'option'
    ];
_c14 = A11Y_OPTION;
const A11Y_CHECKBOX = ()=>[
        'role',
        'checkbox'
    ];
_c15 = A11Y_CHECKBOX;
const A11Y_DIALOG = ()=>[
        'role',
        'dialog'
    ];
_c16 = A11Y_DIALOG;
const A11Y_ALERTDIALOG = ()=>[
        'role',
        'alertdialog'
    ];
_c17 = A11Y_ALERTDIALOG;
const A11Y_GROUP = ()=>[
        'role',
        'group'
    ];
_c18 = A11Y_GROUP;
const A11Y_SCOPE_COL = ()=>[
        'scope',
        'col'
    ];
_c19 = A11Y_SCOPE_COL;
const A11Y_SCOPE_ROW = ()=>[
        'scope',
        'row'
    ];
_c20 = A11Y_SCOPE_ROW;
const A11Y_TEXT = ()=>[
        'type',
        'text'
    ];
_c21 = A11Y_TEXT;
const A11Y_LABEL = (val)=>[
        'aria-label',
        val
    ];
_c22 = A11Y_LABEL;
const A11Y_LABELED_BY = (val)=>[
        'aria-labelledby',
        val
    ];
_c23 = A11Y_LABELED_BY;
const A11Y_DESCRIBED_BY = (val)=>[
        'aria-describedby',
        val
    ];
_c24 = A11Y_DESCRIBED_BY;
const A11Y_HIDDEN = ()=>[
        'aria-hidden',
        'true'
    ];
_c25 = A11Y_HIDDEN;
const A11Y_DISABLED = (val = true)=>[
        'aria-disabled',
        val
    ];
_c26 = A11Y_DISABLED;
const A11Y_MULTISELECTABLE = ()=>[
        'aria-multiselectable',
        'true'
    ];
_c27 = A11Y_MULTISELECTABLE;
const A11Y_HASPOPUP = (val)=>[
        'aria-haspopup',
        val
    ];
_c28 = A11Y_HASPOPUP;
const A11Y_ROWCOUNT = (val)=>[
        'aria-rowcount',
        val
    ];
_c29 = A11Y_ROWCOUNT;
const A11Y_COLCOUNT = (val)=>[
        'aria-colcount',
        val
    ];
_c30 = A11Y_COLCOUNT;
const A11Y_ROWINDEX = (val)=>[
        'aria-rowindex',
        val
    ];
_c31 = A11Y_ROWINDEX;
const A11Y_COLINDEX = (val)=>[
        'aria-colindex',
        val
    ];
_c32 = A11Y_COLINDEX;
const A11Y_EXPANDED = (val)=>[
        'aria-expanded',
        val
    ];
_c33 = A11Y_EXPANDED;
const A11Y_SORT = (val)=>[
        'aria-sort',
        val
    ];
_c34 = A11Y_SORT;
const A11Y_READONLY = ()=>[
        'aria-readonly',
        'true'
    ];
_c35 = A11Y_READONLY;
const A11Y_INVALID = ()=>[
        'aria-invalid',
        'true'
    ];
_c36 = A11Y_INVALID;
const A11Y_CHECKED = (val)=>[
        'aria-checked',
        val
    ];
_c37 = A11Y_CHECKED;
const A11Y_SELECTED = ()=>[
        'aria-selected',
        'true'
    ];
_c38 = A11Y_SELECTED;
const A11Y_AUTOCOMPLETE = ()=>[
        'aria-autocomplete',
        'list'
    ];
_c39 = A11Y_AUTOCOMPLETE;
const A11Y_CONTROLS = (val)=>[
        'aria-controls',
        val
    ];
_c40 = A11Y_CONTROLS;
const A11Y_ACTIVEDESCENDANT = (val)=>[
        'aria-activedescendant',
        val
    ];
_c41 = A11Y_ACTIVEDESCENDANT;
const A11Y_LIVE = (val)=>[
        'aria-live',
        val
    ];
_c42 = A11Y_LIVE;
const A11Y_RELEVANT = (val)=>[
        'aria-relevant',
        val
    ];
_c43 = A11Y_RELEVANT;
const A11Y_SETSIZE = (val)=>[
        'aria-setsize',
        val
    ];
_c44 = A11Y_SETSIZE;
const A11Y_POSINSET = (val)=>[
        'aria-posinset',
        val
    ];
_c45 = A11Y_POSINSET;
const A11Y_MODAL = ()=>[
        'aria-modal',
        'true'
    ];
_c46 = A11Y_MODAL;
const A11Y_BUSY = ()=>[
        'aria-busy',
        'true'
    ];
_c47 = A11Y_BUSY;
var _c, _c1, _c2, _c3, _c4, _c5, _c6, _c7, _c8, _c9, _c10, _c11, _c12, _c13, _c14, _c15, _c16, _c17, _c18, _c19, _c20, _c21, _c22, _c23, _c24, _c25, _c26, _c27, _c28, _c29, _c30, _c31, _c32, _c33, _c34, _c35, _c36, _c37, _c38, _c39, _c40, _c41, _c42, _c43, _c44, _c45, _c46, _c47;
__turbopack_context__.k.register(_c, "A11Y_TABINDEX");
__turbopack_context__.k.register(_c1, "A11Y_TREEGRID");
__turbopack_context__.k.register(_c2, "A11Y_PRESENTATION");
__turbopack_context__.k.register(_c3, "A11Y_GRIDCELL");
__turbopack_context__.k.register(_c4, "A11Y_GRIDCELL_BUTTON");
__turbopack_context__.k.register(_c5, "A11Y_ROWHEADER");
__turbopack_context__.k.register(_c6, "A11Y_ROWGROUP");
__turbopack_context__.k.register(_c7, "A11Y_COLUMNHEADER");
__turbopack_context__.k.register(_c8, "A11Y_ROW");
__turbopack_context__.k.register(_c9, "A11Y_MENU");
__turbopack_context__.k.register(_c10, "A11Y_MENU_ITEM");
__turbopack_context__.k.register(_c11, "A11Y_MENU_ITEM_CHECKBOX");
__turbopack_context__.k.register(_c12, "A11Y_COMBOBOX");
__turbopack_context__.k.register(_c13, "A11Y_LISTBOX");
__turbopack_context__.k.register(_c14, "A11Y_OPTION");
__turbopack_context__.k.register(_c15, "A11Y_CHECKBOX");
__turbopack_context__.k.register(_c16, "A11Y_DIALOG");
__turbopack_context__.k.register(_c17, "A11Y_ALERTDIALOG");
__turbopack_context__.k.register(_c18, "A11Y_GROUP");
__turbopack_context__.k.register(_c19, "A11Y_SCOPE_COL");
__turbopack_context__.k.register(_c20, "A11Y_SCOPE_ROW");
__turbopack_context__.k.register(_c21, "A11Y_TEXT");
__turbopack_context__.k.register(_c22, "A11Y_LABEL");
__turbopack_context__.k.register(_c23, "A11Y_LABELED_BY");
__turbopack_context__.k.register(_c24, "A11Y_DESCRIBED_BY");
__turbopack_context__.k.register(_c25, "A11Y_HIDDEN");
__turbopack_context__.k.register(_c26, "A11Y_DISABLED");
__turbopack_context__.k.register(_c27, "A11Y_MULTISELECTABLE");
__turbopack_context__.k.register(_c28, "A11Y_HASPOPUP");
__turbopack_context__.k.register(_c29, "A11Y_ROWCOUNT");
__turbopack_context__.k.register(_c30, "A11Y_COLCOUNT");
__turbopack_context__.k.register(_c31, "A11Y_ROWINDEX");
__turbopack_context__.k.register(_c32, "A11Y_COLINDEX");
__turbopack_context__.k.register(_c33, "A11Y_EXPANDED");
__turbopack_context__.k.register(_c34, "A11Y_SORT");
__turbopack_context__.k.register(_c35, "A11Y_READONLY");
__turbopack_context__.k.register(_c36, "A11Y_INVALID");
__turbopack_context__.k.register(_c37, "A11Y_CHECKED");
__turbopack_context__.k.register(_c38, "A11Y_SELECTED");
__turbopack_context__.k.register(_c39, "A11Y_AUTOCOMPLETE");
__turbopack_context__.k.register(_c40, "A11Y_CONTROLS");
__turbopack_context__.k.register(_c41, "A11Y_ACTIVEDESCENDANT");
__turbopack_context__.k.register(_c42, "A11Y_LIVE");
__turbopack_context__.k.register(_c43, "A11Y_RELEVANT");
__turbopack_context__.k.register(_c44, "A11Y_SETSIZE");
__turbopack_context__.k.register(_c45, "A11Y_POSINSET");
__turbopack_context__.k.register(_c46, "A11Y_MODAL");
__turbopack_context__.k.register(_c47, "A11Y_BUSY");
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @param {Array} arr An array to process.
 */ __turbopack_context__.s([
    "arrayAvg",
    ()=>arrayAvg,
    "arrayEach",
    ()=>arrayEach,
    "arrayFilter",
    ()=>arrayFilter,
    "arrayFlatten",
    ()=>arrayFlatten,
    "arrayMap",
    ()=>arrayMap,
    "arrayMax",
    ()=>arrayMax,
    "arrayMin",
    ()=>arrayMin,
    "arrayReduce",
    ()=>arrayReduce,
    "arraySum",
    ()=>arraySum,
    "arrayToString",
    ()=>arrayToString,
    "arrayUnique",
    ()=>arrayUnique,
    "extendArray",
    ()=>extendArray,
    "getDifferenceOfArrays",
    ()=>getDifferenceOfArrays,
    "getIntersectionOfArrays",
    ()=>getIntersectionOfArrays,
    "getUnionOfArrays",
    ()=>getUnionOfArrays,
    "pivot",
    ()=>pivot,
    "stringToArray",
    ()=>stringToArray,
    "to2dArray",
    ()=>to2dArray
]);
function to2dArray(arr) {
    const ilen = arr.length;
    let i = 0;
    while(i < ilen){
        arr[i] = [
            arr[i]
        ];
        i += 1;
    }
}
function extendArray(arr, extension) {
    const ilen = extension.length;
    let i = 0;
    while(i < ilen){
        arr.push(extension[i]);
        i += 1;
    }
}
function pivot(arr) {
    const pivotedArr = [];
    if (!arr || arr.length === 0 || !arr[0] || arr[0].length === 0) {
        return pivotedArr;
    }
    const rowCount = arr.length;
    const colCount = arr[0].length;
    for(let i = 0; i < rowCount; i++){
        for(let j = 0; j < colCount; j++){
            if (!pivotedArr[j]) {
                pivotedArr[j] = [];
            }
            pivotedArr[j][i] = arr[i][j];
        }
    }
    return pivotedArr;
}
function arrayReduce(array, iteratee, accumulator, initFromArray) {
    let index = -1;
    const iterable = Array.isArray(array) ? array : Array.from(array);
    const length = iterable.length;
    if (initFromArray && length) {
        index += 1;
        accumulator = iterable[index];
    }
    let result = accumulator;
    index += 1;
    while(index < length){
        result = iteratee(result, iterable[index], index, iterable);
        index += 1;
    }
    return result;
}
function arrayFilter(array, predicate) {
    let index = 0;
    const iterable = Array.isArray(array) ? array : Array.from(array);
    const length = iterable.length;
    const result = [];
    let resIndex = -1;
    while(index < length){
        const value = iterable[index];
        if (predicate(value, index, iterable)) {
            resIndex += 1;
            result[resIndex] = value;
        }
        index += 1;
    }
    return result;
}
function arrayMap(array, iteratee) {
    let index = 0;
    const iterable = Array.isArray(array) ? array : Array.from(array);
    const length = iterable.length;
    const result = [];
    let resIndex = -1;
    while(index < length){
        const value = iterable[index];
        resIndex += 1;
        result[resIndex] = iteratee(value, index, iterable);
        index += 1;
    }
    return result;
}
function arrayEach(array, iteratee) {
    let index = 0;
    let iterable;
    if (Array.isArray(array)) {
        iterable = array;
    } else {
        iterable = Array.from(array);
    }
    const length = iterable.length;
    while(index < length){
        if (iteratee(iterable[index], index, iterable) === false) {
            break;
        }
        index += 1;
    }
    return iterable;
}
function arraySum(array) {
    return arrayReduce(array, (a, b)=>a + b, 0);
}
function arrayMax(array) {
    return arrayReduce(array, (a, b)=>a !== undefined && a > b ? a : b, array[0]);
}
function arrayMin(array) {
    return arrayReduce(array, (a, b)=>a !== undefined && a < b ? a : b, array[0]);
}
function arrayAvg(array) {
    if (!array.length) {
        return 0;
    }
    return arraySum(array) / array.length;
}
function arrayFlatten(array) {
    return arrayReduce(array, (initial, value)=>initial.concat(Array.isArray(value) ? arrayFlatten(value) : value), []);
}
function arrayUnique(array) {
    const unique = [];
    arrayEach(array, (value)=>{
        if (unique.indexOf(value) === -1) {
            unique.push(value);
        }
    });
    return unique;
}
function getDifferenceOfArrays(...arrays) {
    const [first, ...rest] = [
        ...arrays
    ];
    let filteredFirstArray = first;
    arrayEach(rest, (array)=>{
        filteredFirstArray = filteredFirstArray.filter((value)=>!array.includes(value));
    });
    return filteredFirstArray;
}
function getIntersectionOfArrays(...args) {
    const arrays = [];
    let comparator;
    arrayEach(args, (arg, index)=>{
        if (typeof arg === 'function') {
            if (index === args.length - 1) {
                comparator = arg;
            }
        } else {
            arrays.push(arg);
        }
    });
    const isMatch = comparator ? (value, array)=>array.some((item)=>comparator(value, item)) : (value, array)=>array.includes(value);
    const [first, ...rest] = arrays;
    let filteredFirstArray = first;
    arrayEach(rest, (array)=>{
        filteredFirstArray = filteredFirstArray.filter((value)=>isMatch(value, array));
    });
    return filteredFirstArray;
}
function getUnionOfArrays(...arrays) {
    const [first, ...rest] = [
        ...arrays
    ];
    const set = new Set(first);
    arrayEach(rest, (array)=>{
        arrayEach(array, (value)=>{
            if (!set.has(value)) {
                set.add(value);
            }
        });
    });
    return Array.from(set);
}
function stringToArray(value, delimiter = ' ') {
    return value.split(delimiter);
}
function arrayToString(array, separator = ' ') {
    return array.join(separator);
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/browser.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "isChrome",
    ()=>isChrome,
    "isChromeWebKit",
    ()=>isChromeWebKit,
    "isEdge",
    ()=>isEdge,
    "isEdgeWebKit",
    ()=>isEdgeWebKit,
    "isFirefox",
    ()=>isFirefox,
    "isFirefoxWebKit",
    ()=>isFirefoxWebKit,
    "isIOS",
    ()=>isIOS,
    "isIpadOS",
    ()=>isIpadOS,
    "isLinuxOS",
    ()=>isLinuxOS,
    "isMacOS",
    ()=>isMacOS,
    "isMobileBrowser",
    ()=>isMobileBrowser,
    "isSafari",
    ()=>isSafari,
    "isSafariBefore261",
    ()=>isSafariBefore261,
    "isWindowsOS",
    ()=>isWindowsOS,
    "setBrowserMeta",
    ()=>setBrowserMeta,
    "setPlatformMeta",
    ()=>setPlatformMeta
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$feature$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/feature.mjs [client] (ecmascript)");
;
;
const tester = (testerFunc)=>{
    const result = {
        value: false,
        test (_ua, _vendor) {
            result.value = testerFunc(_ua, _vendor);
        }
    };
    return result;
};
const browsers = {
    chrome: tester((ua, vendor)=>/Chrome/.test(ua) && /Google/.test(vendor ?? '')),
    chromeWebKit: tester((ua)=>/CriOS/.test(ua)),
    edge: tester((ua)=>/Edge/.test(ua)),
    edgeWebKit: tester((ua)=>/EdgiOS/.test(ua)),
    firefox: tester((ua)=>/Firefox/.test(ua)),
    firefoxWebKit: tester((ua)=>/FxiOS/.test(ua)),
    mobile: tester((ua)=>/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(ua)),
    safari: tester((ua, vendor)=>/Safari/.test(ua) && /Apple Computer/.test(vendor ?? '')),
    safariBefore261: tester((ua, vendor)=>{
        if (!/Safari/.test(ua) || !/Apple Computer/.test(vendor ?? '')) {
            return false;
        }
        const match = /Version\/(\d+(?:\.\d+)?)/i.exec(ua);
        const version = match ? Number.parseFloat(match[1]) : 0;
        return version < 26.1;
    })
};
const platforms = {
    mac: tester((platform)=>/^Mac/.test(platform)),
    win: tester((platform)=>/^Win/.test(platform)),
    linux: tester((platform)=>/^Linux/.test(platform)),
    ios: tester((platform)=>/iPhone|iPad|iPod/i.test(platform))
};
function setBrowserMeta({ userAgent = navigator.userAgent, vendor = navigator.vendor } = {}) {
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["objectEach"])(browsers, ({ test })=>test(userAgent, vendor));
}
function setPlatformMeta({ platform = navigator.platform } = {}) {
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["objectEach"])(platforms, ({ test })=>test(platform));
}
if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$feature$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isCSR"])()) {
    setBrowserMeta();
    setPlatformMeta();
}
function isChrome() {
    return browsers.chrome.value;
}
function isChromeWebKit() {
    return browsers.chromeWebKit.value;
}
function isFirefox() {
    return browsers.firefox.value;
}
function isFirefoxWebKit() {
    return browsers.firefoxWebKit.value;
}
function isSafari() {
    return browsers.safari.value;
}
function isSafariBefore261() {
    return browsers.safariBefore261.value;
}
function isEdge() {
    return browsers.edge.value;
}
function isEdgeWebKit() {
    return browsers.edgeWebKit.value;
}
function isMobileBrowser() {
    return browsers.mobile.value;
}
function isIOS() {
    return platforms.ios.value;
}
function isIpadOS({ maxTouchPoints } = navigator) {
    return (maxTouchPoints ?? 0) > 2 && platforms.mac.value;
}
function isWindowsOS() {
    return platforms.win.value;
}
function isMacOS() {
    return platforms.mac.value;
}
function isLinuxOS() {
    return platforms.linux.value;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/console.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "deprecatedWarn",
    ()=>deprecatedWarn,
    "error",
    ()=>error,
    "info",
    ()=>info,
    "log",
    ()=>log,
    "logAggregatedItems",
    ()=>logAggregatedItems,
    "warn",
    ()=>warn,
    "warnOnce",
    ()=>warnOnce
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$string$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/string.mjs [client] (ecmascript)");
/* eslint-disable no-console */ /* eslint-disable no-restricted-globals */ /**
 * "In Internet Explorer 9 (and 8), the console object is only exposed when the developer tools are opened
 * for a particular tab.".
 *
 * Source: https://stackoverflow.com/a/5473193.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
;
;
function log(...args) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(console)) {
        console.log(...args);
    }
}
function warn(...args) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(console)) {
        console.warn(...args);
    }
}
/**
 * Tracks which `key`s have already been warned about, grouped by a scope object.
 * A `WeakMap` lets the entries be garbage-collected together with their scope
 * (for example, when a Handsontable instance is destroyed), so the "warn once"
 * state resets per instance without any manual cleanup.
 */ const warnedScopes = new WeakMap();
function warnOnce(scope, key, ...args) {
    let warnedKeys = warnedScopes.get(scope);
    if (warnedKeys === undefined) {
        warnedKeys = new Set();
        warnedScopes.set(scope, warnedKeys);
    }
    if (warnedKeys.has(key)) {
        return;
    }
    warnedKeys.add(key);
    warn(...args);
}
function deprecatedWarn(message) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(console)) {
        console.warn(`Deprecated: ${message}`);
    }
}
function info(...args) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(console)) {
        console.info(...args);
    }
}
function error(...args) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(console)) {
        console.error(...args);
    }
}
function logAggregatedItems({ logFunction = log, message = '', items = [], maxSample = 5, itemFormatter = (item)=>`${item}` } = {}) {
    if (!Array.isArray(items) || items.length === 0) {
        return;
    }
    const count = items.length;
    const formattedItems = items.slice(0, maxSample).map((item)=>`  - ${itemFormatter(item)}`);
    const more = count > maxSample ? `  - ...and ${count - maxSample} more` : '';
    const affectedLines = [
        'Affected cells:',
        ...formattedItems,
        ...more ? [
            more
        ] : []
    ].join('\n');
    logFunction((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$string$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["substitute"])(message, {
        itemsCount: `${count} cell${count > 1 ? 's' : ''}`,
        affectedCells: affectedLines
    }));
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/constants.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "BAD_VALUE_TEXT",
    ()=>BAD_VALUE_TEXT,
    "LOADING_CLASS_NAME",
    ()=>LOADING_CLASS_NAME
]);
const BAD_VALUE_TEXT = '#bad-value#';
const LOADING_CLASS_NAME = 'ht-loading';
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/data.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "cloneRow",
    ()=>cloneRow,
    "countFirstRowKeys",
    ()=>countFirstRowKeys,
    "createEmptySpreadsheetData",
    ()=>createEmptySpreadsheetData,
    "createSpreadsheetData",
    ()=>createSpreadsheetData,
    "createSpreadsheetObjectData",
    ()=>createSpreadsheetObjectData,
    "dataRowToChangesArray",
    ()=>dataRowToChangesArray,
    "hasChangeForCell",
    ()=>hasChangeForCell,
    "isArrayOfArrays",
    ()=>isArrayOfArrays,
    "isArrayOfObjects",
    ()=>isArrayOfObjects,
    "spreadsheetColumnIndex",
    ()=>spreadsheetColumnIndex,
    "spreadsheetColumnLabel",
    ()=>spreadsheetColumnLabel
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
;
const COLUMN_LABEL_BASE = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
const COLUMN_LABEL_BASE_LENGTH = COLUMN_LABEL_BASE.length;
function spreadsheetColumnLabel(index) {
    let dividend = index + 1;
    let columnLabel = '';
    let modulo;
    while(dividend > 0){
        modulo = (dividend - 1) % COLUMN_LABEL_BASE_LENGTH;
        columnLabel = String.fromCharCode(65 + modulo) + columnLabel;
        dividend = parseInt(String((dividend - modulo) / COLUMN_LABEL_BASE_LENGTH), 10);
    }
    return columnLabel;
}
function spreadsheetColumnIndex(label) {
    let result = 0;
    if (label) {
        for(let i = 0, j = label.length - 1; i < label.length; i += 1, j -= 1){
            result += COLUMN_LABEL_BASE_LENGTH ** j * (COLUMN_LABEL_BASE.indexOf(label[i]) + 1);
        }
    }
    result -= 1;
    return result;
}
function createSpreadsheetData(rows = 100, columns = 4) {
    const _rows = [];
    let i;
    let j;
    for(i = 0; i < rows; i++){
        const row = [];
        for(j = 0; j < columns; j++){
            row.push(spreadsheetColumnLabel(j) + (i + 1));
        }
        _rows.push(row);
    }
    return _rows;
}
function createSpreadsheetObjectData(rows = 100, colCount = 4) {
    const _rows = [];
    let i;
    let j;
    for(i = 0; i < rows; i++){
        const row = {};
        for(j = 0; j < colCount; j++){
            row[`prop${j}`] = spreadsheetColumnLabel(j) + (i + 1);
        }
        _rows.push(row);
    }
    return _rows;
}
function createEmptySpreadsheetData(rows, columns) {
    const data = [];
    let row;
    for(let i = 0; i < rows; i++){
        row = [];
        for(let j = 0; j < columns; j++){
            row.push('');
        }
        data.push(row);
    }
    return data;
}
function dataRowToChangesArray(dataRow, rowOffset = 0) {
    let dataRows = dataRow;
    const changesArray = [];
    if (!Array.isArray(dataRow) || !Array.isArray(dataRow[0])) {
        dataRows = [
            dataRow
        ];
    }
    dataRows.forEach((row, rowIndex)=>{
        if (Array.isArray(row)) {
            row.forEach((value, column)=>{
                changesArray.push([
                    rowIndex + rowOffset,
                    column,
                    value
                ]);
            });
        } else {
            Object.keys(row).forEach((propName)=>{
                changesArray.push([
                    rowIndex + rowOffset,
                    propName,
                    row[propName]
                ]);
            });
        }
    });
    return changesArray;
}
function hasChangeForCell(changes, visualRow, prop) {
    return changes.some(([changeRow, changeProp])=>changeRow === visualRow && changeProp === prop);
}
function countFirstRowKeys(data) {
    let result = 0;
    if (Array.isArray(data)) {
        if (data[0] && Array.isArray(data[0])) {
            result = data[0].length;
        } else if (data[0] && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isObject"])(data[0])) {
            result = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["deepObjectSize"])(data[0]);
        }
    }
    return result;
}
function isArrayOfArrays(data) {
    return !!(Array.isArray(data) && data.length && data.every((el)=>Array.isArray(el)));
}
function isArrayOfObjects(data) {
    return !!(Array.isArray(data) && data.length && data.every((el)=>typeof el === 'object' && !Array.isArray(el) && el !== null));
}
function cloneRow(row) {
    if (Array.isArray(row)) {
        return row.slice();
    }
    if (row !== null && typeof row === 'object') {
        return {
            ...row
        };
    }
    return row;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/dateTime.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ISO_DATE_REGEX",
    ()=>ISO_DATE_REGEX,
    "TIME_REGEX",
    ()=>TIME_REGEX,
    "getNormalizedDate",
    ()=>getNormalizedDate,
    "getRelativeLocalDate",
    ()=>getRelativeLocalDate,
    "getTodayLocalDate",
    ()=>getTodayLocalDate,
    "isSameLocalDay",
    ()=>isSameLocalDay,
    "isValidISODate",
    ()=>isValidISODate,
    "isValidTime",
    ()=>isValidTime,
    "parseToLocalDate",
    ()=>parseToLocalDate,
    "parseToLocalTime",
    ()=>parseToLocalTime
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
;
const ISO_DATE_REGEX = /^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$/;
function getNormalizedDate(dateString) {
    const nativeDate = new Date(dateString);
    if (!Number.isNaN(new Date(`${dateString}T00:00`).getDate())) {
        return new Date(nativeDate.getTime() + nativeDate.getTimezoneOffset() * 60000);
    }
    return nativeDate;
}
function parseToLocalDate(value) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isEmpty"])(value)) {
        return null;
    }
    if (typeof value === 'string' && ISO_DATE_REGEX.test(value)) {
        const [y, m, d] = value.split('-').map(Number);
        return new Date(y, m - 1, d);
    }
    return null;
}
function isValidISODate(value) {
    if (typeof value !== 'string' || !ISO_DATE_REGEX.test(value)) {
        return false;
    }
    const date = new Date(value);
    return !Number.isNaN(date.getTime()) && value === date.toISOString().slice(0, 10);
}
const TIME_REGEX = /^([01]\d|2[0-3]):([0-5]\d)(?::([0-5]\d)(?:\.(\d{1,3}))?)?$/;
function parseToLocalTime(value) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isEmpty"])(value)) {
        return null;
    }
    if (typeof value !== 'string') {
        return null;
    }
    const match = TIME_REGEX.exec(value);
    if (!match) {
        return null;
    }
    const hours = Number(match[1]);
    const minutes = Number(match[2]);
    const seconds = match[3] !== undefined ? Number(match[3]) : 0;
    const msString = match[4];
    const milliseconds = msString !== undefined ? Number(msString.padEnd(3, '0').slice(0, 3)) : 0;
    return new Date(1970, 0, 1, hours, minutes, seconds, milliseconds);
}
function isValidTime(value) {
    return typeof value === 'string' && TIME_REGEX.test(value);
}
function getTodayLocalDate() {
    const now = new Date();
    return new Date(now.getFullYear(), now.getMonth(), now.getDate());
}
function getRelativeLocalDate(days) {
    const result = getTodayLocalDate();
    result.setDate(result.getDate() + days);
    return result;
}
function isSameLocalDay(a, b) {
    return a.getFullYear() === b.getFullYear() && a.getMonth() === b.getMonth() && a.getDate() === b.getDate();
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/dom/cellCoords.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getCellCoordsFromMousePosition",
    ()=>getCellCoordsFromMousePosition
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/number.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
/**
 * Finds which column the mouse is over within a given column range.
 *
 * @param {Handsontable} hotInstance The Handsontable instance.
 * @param {number} row Row to use for measuring cell widths.
 * @param {number} startColumn First column in the range.
 * @param {number} endColumn Last column in the range (inclusive).
 * @param {number} relativeX Mouse X position relative to the first cell's edge.
 * @returns {number | null} Column index, or null if mouse is outside the range.
 */ function findColumnAtX(hotInstance, row, startColumn, endColumn, relativeX) {
    let accumulatedX = 0;
    for(let column = startColumn; column <= endColumn; column++){
        const cellElement = hotInstance.getCell(row, column, true);
        if (!cellElement) {
            continue;
        }
        const width = cellElement.offsetWidth;
        if (relativeX < accumulatedX + width) {
            return column;
        }
        accumulatedX += width;
        const { colspan } = hotInstance.getCellMeta(row, column);
        if (colspan !== undefined && colspan > 1) {
            column += colspan - 1;
        }
    }
    return null;
}
/**
 * Finds which row the mouse is over within a given row range.
 *
 * @param {Handsontable} hotInstance The Handsontable instance.
 * @param {number} column Column to use for measuring cell heights.
 * @param {number} startRow First row in the range.
 * @param {number} endRow Last row in the range (inclusive).
 * @param {number} relativeY Mouse Y position relative to the first cell's edge.
 * @returns {number | null} Row index, or null if mouse is outside the range.
 */ function findRowAtY(hotInstance, column, startRow, endRow, relativeY) {
    let accumulatedY = 0;
    for(let row = startRow; row <= endRow; row++){
        const cellElement = hotInstance.getCell(row, column, true);
        if (!cellElement) {
            continue;
        }
        const height = cellElement.offsetHeight;
        if (relativeY < accumulatedY + height) {
            return row;
        }
        accumulatedY += height;
        const { rowspan } = hotInstance.getCellMeta(row, column);
        if (rowspan !== undefined && rowspan > 1) {
            row += rowspan - 1;
        }
    }
    return null;
}
function getCellCoordsFromMousePosition(hotInstance, mouseX, mouseY) {
    const { view, columnIndexMapper, rowIndexMapper } = hotInstance;
    const isRtl = hotInstance.isRtl();
    const numberOfFixedColumnsStart = view.countNotHiddenFixedColumnsStart();
    const numberOfFixedRowsTop = view.countNotHiddenFixedRowsTop();
    const numberOfFixedRowsBottom = view.countNotHiddenFixedRowsBottom();
    const firstPartiallyVisibleRow = hotInstance.getFirstPartiallyVisibleRow();
    const lastPartiallyVisibleRow = hotInstance.getLastPartiallyVisibleRow();
    const firstPartiallyVisibleColumn = hotInstance.getFirstPartiallyVisibleColumn();
    const lastPartiallyVisibleColumn = hotInstance.getLastPartiallyVisibleColumn();
    const tableOffset = hotInstance.rootElement.getBoundingClientRect();
    const columnHeaderHeight = hotInstance.hasColHeaders() ? view.getColumnHeaderHeight() : 0;
    const rowHeaderWidth = hotInstance.hasRowHeaders() ? view.getRowHeaderWidth() : 0;
    // When the window is the scroll container, tableOffset.top/left can be positive (RTL) or
    // negative (LTR after scrolling) relative to the viewport. The naive formula using
    // tableOffset as the viewport boundary causes the clamped mouse position to shift with
    // each scroll tick, locking getCellCoordsFromMousePosition onto the same column/row
    // regardless of scroll position. Use the physical window bounds (0 / innerWidth /
    // innerHeight) instead so the clamped position stays fixed and the cell lookup always
    // resolves to the edge cell of the CURRENT viewport.
    const { rootWindow } = hotInstance;
    // When the window is the scroll container and the table's left/top edge has been
    // scrolled PAST the viewport origin (tableOffset.left/top > 0, e.g. RTL at max-left
    // scroll where tableOffset.left can be 4809 while innerWidth = 1100), clamping to
    // tableOffset.left produces clamp(min > max) which always returns the minimum,
    // making scrollRelativeX negative and mapping every mouse position to the wrong edge
    // column. Clamp to 0 in that case so the boundary aligns with the viewport edge.
    // When the table is partially off-screen in the opposite direction (tableOffset < 0),
    // keep the original tableOffset so the relative position calculation remains correct.
    const tableViewportLeft = view.isHorizontallyScrollableByWindow() ? Math.min(0, tableOffset.left) : tableOffset.left;
    const tableViewportTop = view.isVerticallyScrollableByWindow() ? Math.min(0, tableOffset.top) : tableOffset.top;
    const tableViewportRight = view.isHorizontallyScrollableByWindow() ? rootWindow.innerWidth : tableOffset.left + view.getViewportWidth() + rowHeaderWidth;
    const tableViewportBottom = view.isVerticallyScrollableByWindow() ? rootWindow.innerHeight : tableOffset.top + view.getViewportHeight() + columnHeaderHeight;
    const clampedX = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(mouseX, tableViewportLeft, tableViewportRight);
    const clampedY = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(mouseY, tableViewportTop, tableViewportBottom);
    let foundColumn = null;
    // Check fixed columns first
    if (numberOfFixedColumnsStart > 0) {
        const firstFixedColumn = columnIndexMapper.getVisualFromRenderableIndex(0);
        const lastFixedColumn = columnIndexMapper.getVisualFromRenderableIndex(numberOfFixedColumnsStart - 1);
        const firstNonHiddenColumn = firstFixedColumn !== null ? columnIndexMapper.getNearestNotHiddenIndex(firstFixedColumn, 1) : null;
        if (firstNonHiddenColumn !== null && lastFixedColumn !== null) {
            const fixedCell = hotInstance.getCell(firstPartiallyVisibleRow, firstNonHiddenColumn, true);
            if (fixedCell instanceof HTMLElement) {
                const fixedCellRect = fixedCell.getBoundingClientRect();
                const fixedRelativeX = isRtl ? fixedCellRect.right - clampedX : clampedX - fixedCellRect.left;
                foundColumn = findColumnAtX(hotInstance, firstPartiallyVisibleRow, firstNonHiddenColumn, lastFixedColumn, fixedRelativeX);
            }
        }
    }
    // If not in fixed columns, check scrollable columns (main table)
    if (foundColumn === null) {
        const scrollCell = hotInstance.getCell(firstPartiallyVisibleRow, firstPartiallyVisibleColumn, true);
        if (scrollCell instanceof HTMLElement) {
            const scrollCellRect = scrollCell.getBoundingClientRect();
            const scrollRelativeX = isRtl ? scrollCellRect.right - clampedX : clampedX - scrollCellRect.left;
            foundColumn = findColumnAtX(hotInstance, firstPartiallyVisibleRow, firstPartiallyVisibleColumn, lastPartiallyVisibleColumn, scrollRelativeX);
            // Fallback to edge columns if still not found.
            // If `lastPartiallyVisibleColumn` is null (e.g., the HoT is positioned off-screen and
            // viewport queries return invalid indexes), fall back to the global column bounds.
            if (foundColumn === null) {
                foundColumn = scrollRelativeX < 0 ? firstPartiallyVisibleColumn ?? 0 : lastPartiallyVisibleColumn ?? hotInstance.countCols() - 1;
            }
        } else {
            foundColumn = firstPartiallyVisibleColumn ?? 0;
        }
    }
    let foundRow = null;
    // Check fixed top rows first
    if (numberOfFixedRowsTop > 0) {
        const firstFixedRow = rowIndexMapper.getVisualFromRenderableIndex(0);
        const lastFixedRow = rowIndexMapper.getVisualFromRenderableIndex(numberOfFixedRowsTop - 1);
        const firstNonHiddenRow = firstFixedRow !== null ? rowIndexMapper.getNearestNotHiddenIndex(firstFixedRow, 1) : null;
        if (firstNonHiddenRow !== null && lastFixedRow !== null) {
            const fixedCell = hotInstance.getCell(firstNonHiddenRow, firstPartiallyVisibleColumn, true);
            if (fixedCell instanceof HTMLElement) {
                const fixedCellRect = fixedCell.getBoundingClientRect();
                const fixedRelativeY = clampedY - fixedCellRect.top;
                foundRow = findRowAtY(hotInstance, firstPartiallyVisibleColumn, firstNonHiddenRow, lastFixedRow, fixedRelativeY);
            }
        }
    }
    // Check fixed bottom rows if not found in fixed top rows
    if (foundRow === null && numberOfFixedRowsBottom > 0) {
        const totalSourceRows = rowIndexMapper.getNotHiddenIndexesLength();
        const bottomStartRow = rowIndexMapper.getVisualFromRenderableIndex(totalSourceRows - numberOfFixedRowsBottom);
        const bottomEndRow = rowIndexMapper.getVisualFromRenderableIndex(totalSourceRows - 1);
        const bottomStartNonHiddenRow = bottomStartRow !== null ? rowIndexMapper.getNearestNotHiddenIndex(bottomStartRow, 1) : null;
        const bottomEndNonHiddenRow = bottomEndRow !== null ? rowIndexMapper.getNearestNotHiddenIndex(bottomEndRow, -1) : null;
        if (bottomStartNonHiddenRow !== null && bottomEndNonHiddenRow !== null) {
            const fixedBottomCell = hotInstance.getCell(bottomStartNonHiddenRow, firstPartiallyVisibleColumn, true);
            if (fixedBottomCell instanceof HTMLElement) {
                const fixedBottomCellRect = fixedBottomCell.getBoundingClientRect();
                const fixedBottomRelativeY = clampedY - fixedBottomCellRect.top;
                if (fixedBottomRelativeY >= 0) {
                    foundRow = findRowAtY(hotInstance, firstPartiallyVisibleColumn, bottomStartNonHiddenRow, bottomEndNonHiddenRow, fixedBottomRelativeY);
                    if (foundRow === null) {
                        foundRow = bottomEndNonHiddenRow;
                    }
                }
            }
        }
    }
    // Check scrollable rows (main table)
    if (foundRow === null) {
        const scrollCell = hotInstance.getCell(firstPartiallyVisibleRow, firstPartiallyVisibleColumn, true);
        if (scrollCell instanceof HTMLElement) {
            const scrollCellRect = scrollCell.getBoundingClientRect();
            const scrollRelativeY = clampedY - scrollCellRect.top;
            foundRow = findRowAtY(hotInstance, firstPartiallyVisibleColumn, firstPartiallyVisibleRow, lastPartiallyVisibleRow, scrollRelativeY);
            // Fallback to edge rows if still not found. If the viewport query returns null
            // (off-screen HoT edge case), use the global row bounds.
            if (foundRow === null) {
                foundRow = scrollRelativeY < 0 ? firstPartiallyVisibleRow ?? 0 : lastPartiallyVisibleRow ?? hotInstance.countRows() - 1;
            }
        } else {
            foundRow = firstPartiallyVisibleRow ?? 0;
        }
    }
    if (foundRow === null || foundColumn === null) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('Failed to resolve cell coordinates from mouse position.');
    }
    return hotInstance._createCellCoords(foundRow, foundColumn);
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HTML_CHARACTERS",
    ()=>HTML_CHARACTERS,
    "SANITIZER_WARN_KEY",
    ()=>SANITIZER_WARN_KEY,
    "addClass",
    ()=>addClass,
    "addEvent",
    ()=>addEvent,
    "clearTextSelection",
    ()=>clearTextSelection,
    "closest",
    ()=>closest,
    "closestDown",
    ()=>closestDown,
    "empty",
    ()=>empty,
    "eventTargetEl",
    ()=>eventTargetEl,
    "fastInnerHTML",
    ()=>fastInnerHTML,
    "fastInnerText",
    ()=>fastInnerText,
    "findFirstParentWithClass",
    ()=>findFirstParentWithClass,
    "getCaretPosition",
    ()=>getCaretPosition,
    "getChildEl",
    ()=>getChildEl,
    "getCssTransform",
    ()=>getCssTransform,
    "getFractionalScalingCompensation",
    ()=>getFractionalScalingCompensation,
    "getFrameElement",
    ()=>getFrameElement,
    "getMaximumScrollLeft",
    ()=>getMaximumScrollLeft,
    "getMaximumScrollTop",
    ()=>getMaximumScrollTop,
    "getParent",
    ()=>getParent,
    "getParentWindow",
    ()=>getParentWindow,
    "getScrollLeft",
    ()=>getScrollLeft,
    "getScrollTop",
    ()=>getScrollTop,
    "getScrollableElement",
    ()=>getScrollableElement,
    "getScrollbarWidth",
    ()=>getScrollbarWidth,
    "getSelectionEndPosition",
    ()=>getSelectionEndPosition,
    "getSelectionText",
    ()=>getSelectionText,
    "getStyle",
    ()=>getStyle,
    "getTrimmingContainer",
    ()=>getTrimmingContainer,
    "getWindowScrollLeft",
    ()=>getWindowScrollLeft,
    "getWindowScrollTop",
    ()=>getWindowScrollTop,
    "hasAccessToParentWindow",
    ()=>hasAccessToParentWindow,
    "hasClass",
    ()=>hasClass,
    "hasHorizontalScrollbar",
    ()=>hasHorizontalScrollbar,
    "hasVerticalScrollbar",
    ()=>hasVerticalScrollbar,
    "hasZeroHeight",
    ()=>hasZeroHeight,
    "index",
    ()=>index,
    "innerHeight",
    ()=>innerHeight,
    "innerWidth",
    ()=>innerWidth,
    "isBottomMostColumnHeader",
    ()=>isBottomMostColumnHeader,
    "isChildOf",
    ()=>isChildOf,
    "isDetached",
    ()=>isDetached,
    "isHTMLElement",
    ()=>isHTMLElement,
    "isHTMLInputElement",
    ()=>isHTMLInputElement,
    "isHTMLTableCellElement",
    ()=>isHTMLTableCellElement,
    "isInput",
    ()=>isInput,
    "isInternalElement",
    ()=>isInternalElement,
    "isOutsideInput",
    ()=>isOutsideInput,
    "isShadowRoot",
    ()=>isShadowRoot,
    "isVisible",
    ()=>isVisible,
    "makeElementContentEditableAndSelectItsContent",
    ()=>makeElementContentEditableAndSelectItsContent,
    "matchesCSSRules",
    ()=>matchesCSSRules,
    "observeVisibilityChangeOnce",
    ()=>observeVisibilityChangeOnce,
    "offset",
    ()=>offset,
    "outerHeight",
    ()=>outerHeight,
    "outerWidth",
    ()=>outerWidth,
    "overlayContainsElement",
    ()=>overlayContainsElement,
    "removeAttribute",
    ()=>removeAttribute,
    "removeClass",
    ()=>removeClass,
    "removeContentEditableFromElementAndDeselect",
    ()=>removeContentEditableFromElementAndDeselect,
    "removeEvent",
    ()=>removeEvent,
    "removeTextNodes",
    ()=>removeTextNodes,
    "resetCssTransform",
    ()=>resetCssTransform,
    "runWithSelectedContendEditableElement",
    ()=>runWithSelectedContendEditableElement,
    "selectElementIfAllowed",
    ()=>selectElementIfAllowed,
    "setAttribute",
    ()=>setAttribute,
    "setCaretPosition",
    ()=>setCaretPosition,
    "setOverlayPosition",
    ()=>setOverlayPosition
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/browser.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/console.mjs [client] (ecmascript)");
;
;
;
function getParent(element, level = 0) {
    let iteration = -1;
    let parent = null;
    let elementToCheck = element;
    while(elementToCheck !== null){
        if (iteration === level) {
            parent = isHTMLElement(elementToCheck) ? elementToCheck : null;
            break;
        }
        if (isShadowRoot(elementToCheck)) {
            elementToCheck = elementToCheck.host;
        } else {
            iteration += 1;
            elementToCheck = elementToCheck.parentNode;
        }
    }
    return parent;
}
function isInternalElement(element, thisHotContainer) {
    const closestHandsontableContainer = element.closest('.handsontable');
    return !!closestHandsontableContainer && (closestHandsontableContainer.parentNode === thisHotContainer || closestHandsontableContainer === thisHotContainer);
}
function eventTargetEl(event) {
    return event.target;
}
function getFrameElement(frame) {
    const { frameElement } = frame;
    // `frame.parent` can be null when the iframe has been detached from the DOM. Guard before
    // passing to Object.getPrototypeOf(), which throws on null/undefined.
    return frame.parent && Object.getPrototypeOf(frame.parent) && frameElement instanceof HTMLIFrameElement ? frameElement : null;
}
function getParentWindow(frame) {
    return getFrameElement(frame) ? frame.parent : null;
}
function hasAccessToParentWindow(frame) {
    return !!Object.getPrototypeOf(frame.parent);
}
function closest(element, nodes = [], until) {
    const { ELEMENT_NODE, DOCUMENT_FRAGMENT_NODE } = Node;
    let elementToCheck = element;
    while(elementToCheck !== null && elementToCheck !== undefined && elementToCheck !== until){
        const { nodeType, nodeName } = elementToCheck;
        if (nodeType === ELEMENT_NODE && isHTMLElement(elementToCheck) && (nodes.includes(nodeName) || nodes.includes(elementToCheck))) {
            return elementToCheck;
        }
        if (isShadowRoot(elementToCheck)) {
            elementToCheck = elementToCheck.host;
        } else {
            elementToCheck = elementToCheck.parentNode;
        }
    }
    return null;
}
function closestDown(element, nodes, until) {
    const matched = [];
    let elementToCheck = isHTMLElement(element) ? element : null;
    while(elementToCheck){
        elementToCheck = closest(elementToCheck, nodes, until);
        if (!elementToCheck || until && !until.contains(elementToCheck)) {
            break;
        }
        matched.push(elementToCheck);
        if (isShadowRoot(elementToCheck)) {
            const { host } = elementToCheck;
            elementToCheck = isHTMLElement(host) ? host : null;
        } else {
            elementToCheck = elementToCheck.parentElement;
        }
    }
    const length = matched.length;
    return length ? matched[length - 1] : null;
}
function findFirstParentWithClass(element, className) {
    const matched = {
        element: undefined,
        classNames: []
    };
    let elementToCheck = element;
    while(elementToCheck !== null && elementToCheck !== element.ownerDocument.documentElement && !matched.element){
        if (typeof className === 'string' && elementToCheck.classList.contains(className)) {
            matched.element = elementToCheck;
            matched.classNames.push(className);
        } else if (className instanceof RegExp) {
            const matchingClasses = Array.from(elementToCheck.classList).filter((cls)=>className.test(cls));
            if (matchingClasses.length) {
                matched.element = elementToCheck;
                matched.classNames.push(...matchingClasses);
            }
        }
        elementToCheck = elementToCheck.parentElement;
    }
    return matched;
}
function isChildOf(child, parent) {
    let node = child.parentNode;
    let queriedParents = [];
    if (typeof parent === 'string') {
        if (child instanceof Document) {
            queriedParents = Array.from(child.querySelectorAll(parent));
        } else {
            queriedParents = Array.from(child.ownerDocument.querySelectorAll(parent));
        }
    } else {
        queriedParents.push(parent);
    }
    while(node !== null){
        if (queriedParents.indexOf(node) > -1) {
            return true;
        }
        node = node.parentNode;
    }
    return false;
}
function index(element) {
    let i = 0;
    let elementToCheck = element;
    if (elementToCheck.previousSibling) {
        /* eslint-disable no-cond-assign */ while(elementToCheck = elementToCheck.previousSibling){
            i += 1;
        }
    }
    return i;
}
function overlayContainsElement(overlayType, element, root) {
    const overlayElement = root.parentElement?.querySelector(`.ht_clone_${overlayType}`);
    return overlayElement ? overlayElement.contains(element) : false;
}
function isBottomMostColumnHeader(TH) {
    if (!TH || !TH.classList || !TH.parentNode || !TH.parentNode.parentNode) {
        return false;
    }
    if (TH.classList.contains('hiddenHeader') || TH.style.display === 'none') {
        return false;
    }
    const headerRow = TH.parentNode;
    const headerRows = Array.from(headerRow?.parentNode?.childNodes ?? []);
    const headerLevel = headerRow ? headerRows.indexOf(headerRow) : -1;
    const rowspan = Number.parseInt(TH.getAttribute('rowspan') ?? '', 10) || 1;
    return headerLevel + rowspan >= headerRows.length;
}
/**
 * @param {string[]} classNames The element "class" attribute string.
 * @returns {string[]}
 */ function filterEmptyClassNames(classNames) {
    if (!classNames || !classNames.length) {
        return [];
    }
    return classNames.filter((x)=>!!x);
}
/**
 * Filter out the RegExp entries from an array.
 *
 * @param {(string|RegExp)[]} list Array of either strings, Regexes or a mix of both.
 * @param {boolean} [returnBoth] If `true`, both the array without regexes and an array of regexes will be returned.
 * @returns {string[]|{regexFree: string[], regexes: RegExp[]}}
 */ function filterRegexes(list, returnBoth) {
    if (!list || !list.length) {
        return returnBoth ? {
            regexFree: [],
            regexes: []
        } : [];
    }
    const regexes = [];
    const regexFree = [];
    regexFree.push(...list.filter((entry)=>{
        const isRegex = entry instanceof RegExp;
        if (isRegex && returnBoth) {
            regexes.push(entry);
        }
        return !isRegex;
    }));
    return returnBoth ? {
        regexFree,
        regexes
    } : regexFree;
}
function hasClass(element, className) {
    if (element.classList === undefined || typeof className !== 'string' || className === '') {
        return false;
    }
    return element.classList.contains(className);
}
function addClass(element, className) {
    if (typeof className === 'string') {
        className = className.split(' ');
    }
    className = filterEmptyClassNames(className);
    if (className.length > 0) {
        element.classList.add(...className);
    }
}
function removeClass(element, className) {
    if (typeof className === 'string') {
        className = className.split(' ');
    } else if (className instanceof RegExp) {
        className = [
            className
        ];
    }
    let { regexFree: stringClasses, regexes: regexClasses } = filterRegexes(className, true);
    stringClasses = filterEmptyClassNames(stringClasses);
    if (stringClasses.length > 0) {
        element.classList.remove(...stringClasses);
    }
    regexClasses.forEach((regexClassName)=>{
        element.classList.forEach((currentClassName)=>{
            if (regexClassName.test(currentClassName)) {
                element.classList.remove(currentClassName);
            }
        });
    });
}
function setAttribute(domElement, attributes = [], attributeValue) {
    if (!Array.isArray(attributes)) {
        attributes = [
            [
                attributes,
                attributeValue ?? ''
            ]
        ];
    }
    attributes.forEach((attributeInfo)=>{
        if (Array.isArray(attributeInfo) && attributeInfo[0] !== '') {
            domElement.setAttribute(attributeInfo[0], String(attributeInfo[1]));
        }
    });
}
function removeAttribute(domElement, attributesToRemove = []) {
    if (typeof attributesToRemove === 'string') {
        attributesToRemove = attributesToRemove.split(' ');
    } else if (attributesToRemove instanceof RegExp) {
        attributesToRemove = [
            attributesToRemove
        ];
    }
    const { regexFree: stringAttributes, regexes: regexAttributes } = filterRegexes(attributesToRemove, true);
    stringAttributes.forEach((attributeNameToRemove)=>{
        if (attributeNameToRemove !== '') {
            domElement.removeAttribute(attributeNameToRemove);
        }
    });
    if (regexAttributes.length > 0) {
        // Read the attribute names once and test each against all regexes, instead of re-reading the
        // (allocating) attribute-name list per regex. This path runs per cell on every render.
        const attributeNames = domElement.getAttributeNames();
        for(let i = 0; i < attributeNames.length; i++){
            const attributeName = attributeNames[i];
            for(let r = 0; r < regexAttributes.length; r++){
                if (regexAttributes[r].test(attributeName)) {
                    domElement.removeAttribute(attributeName);
                    break;
                }
            }
        }
    }
}
function removeTextNodes(element) {
    if (element.nodeType === 3) {
        element.parentNode?.removeChild(element); // bye text nodes!
    } else if ([
        'TABLE',
        'THEAD',
        'TBODY',
        'TFOOT',
        'TR'
    ].indexOf(element.nodeName) > -1) {
        const childs = element.childNodes;
        for(let i = childs.length - 1; i >= 0; i--){
            removeTextNodes(childs[i]);
        }
    }
}
function empty(element) {
    let child;
    /* eslint-disable no-cond-assign */ while(child = element.lastChild){
        element.removeChild(child);
    }
}
const HTML_CHARACTERS = /(<([^>]*)>|&([^;]*);)/;
const SANITIZER_WARN_KEY = 'sanitizer';
/**
 * Default scope used when a caller writes raw HTML without supplying a per-instance
 * scope. Keeps the warning to once per process for such callers.
 */ const defaultSanitizerWarnScope = {};
function fastInnerHTML(element, content, sanitizer = true, context = 'innerHTML', scope = defaultSanitizerWarnScope) {
    if (HTML_CHARACTERS.test(content)) {
        let sanitized;
        if (typeof sanitizer === 'function') {
            sanitized = sanitizer(content, context);
        } else {
            // `false` means the caller renders raw HTML deliberately (for example, the `html` cell type).
            // Any other non-function value (the default `true`) is an implicit raw write, so nudge once
            // toward configuring a sanitizer to prevent XSS.
            if (sanitizer !== false) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warnOnce"])(scope, SANITIZER_WARN_KEY, `HTML content is being written to the DOM ("${context}") without a sanitizer. ` + 'Configure the "sanitizer" option to prevent XSS vulnerabilities.');
            }
            sanitized = content;
        }
        element.innerHTML = sanitized;
    } else {
        fastInnerText(element, content);
    }
}
function fastInnerText(element, content) {
    const child = element.firstChild;
    if (child && child.nodeType === 3 && child.nextSibling === null) {
        // fast lane - replace existing text node
        child.textContent = content;
    } else {
        // slow lane - empty element and insert a text node
        empty(element);
        element.appendChild(element.ownerDocument.createTextNode(content));
    }
}
function isVisible(element) {
    // Fast path: use the native checkVisibility() API (Chrome 105+, Firefox 106+, Safari 17.4+).
    // With no options it checks whether the element has an associated box — returning false for
    // display:none and display:contents — and whether any ancestor sets content-visibility:hidden.
    // This is stricter than the legacy walk below (which only catches display:none), but the
    // extra cases are also semantically invisible, so the stricter result is correct.
    // The benefit over the legacy walk: O(1) browser-native time instead of O(DOM depth) with
    // per-node getComputedStyle() calls that force full-document layout recalculation.
    if (typeof element.checkVisibility === 'function') {
        return element.checkVisibility();
    }
    // Legacy fallback: manual ancestor walk for browsers that do not support checkVisibility().
    const documentElement = element.ownerDocument.documentElement;
    const windowElement = element.ownerDocument.defaultView;
    let next = element;
    while(next !== documentElement){
        if (next === null) {
            return false;
        } else if (isShadowRoot(next)) {
            const host = next.host;
            if (host.impl) {
                return isVisible(host.impl);
            } else {
                return isVisible(host);
            }
        } else if (next.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
            return false; // this is a node detached from document in IE8
        } else if (next.nodeType === Node.ELEMENT_NODE && isHTMLElement(next) && windowElement?.getComputedStyle(next).display === 'none') {
            return false;
        }
        next = next.parentNode;
    }
    return true;
}
function hasZeroHeight(element) {
    const rootDocument = element.ownerDocument;
    const rootWindow = rootDocument.defaultView;
    let currentElement = element;
    while(currentElement.parentElement){
        if (currentElement.style.height === '0px' || currentElement.style.height === '0') {
            const computedOverflow = rootWindow?.getComputedStyle(currentElement).getPropertyValue('overflow');
            return computedOverflow === 'hidden' || computedOverflow === 'clip';
        }
        currentElement = currentElement.parentElement;
    }
    return false;
}
function offset(element) {
    const rootDocument = element.ownerDocument;
    const rootWindow = rootDocument.defaultView;
    const documentElement = rootDocument.documentElement;
    let elementToCheck = element;
    let offsetLeft;
    let offsetTop;
    let lastElem;
    offsetLeft = elementToCheck.offsetLeft;
    offsetTop = elementToCheck.offsetTop;
    lastElem = elementToCheck;
    /* eslint-disable no-cond-assign */ while(elementToCheck = isHTMLElement(elementToCheck.offsetParent) ? elementToCheck.offsetParent : null){
        // from my observation, document.body always has scrollLeft/scrollTop == 0
        if (elementToCheck === rootDocument.body) {
            break;
        }
        // If the element is inside an SVG context, the `offsetParent` can be
        // a <foreignObject> that does not have properties `offsetLeft` and `offsetTop` defined.
        if (!('offsetLeft' in elementToCheck)) {
            break;
        }
        offsetLeft += elementToCheck.offsetLeft;
        offsetTop += elementToCheck.offsetTop;
        lastElem = elementToCheck;
    }
    // slow - http://jsperf.com/offset-vs-getboundingclientrect/6
    if (lastElem && lastElem.style.position === 'fixed') {
        // if(lastElem !== document.body) { //faster but does gives false positive in Firefox
        offsetLeft += (rootWindow?.pageXOffset ?? 0) || documentElement.scrollLeft;
        offsetTop += (rootWindow?.pageYOffset ?? 0) || documentElement.scrollTop;
    }
    return {
        left: offsetLeft,
        top: offsetTop
    };
}
function getWindowScrollTop(rootWindow = window) {
    return rootWindow.scrollY;
}
function getWindowScrollLeft(rootWindow = window) {
    return rootWindow.scrollX;
}
function getScrollTop(element, rootWindow = window) {
    if (element instanceof Window) {
        return getWindowScrollTop(rootWindow);
    }
    return element.scrollTop;
}
function getScrollLeft(element, rootWindow = window) {
    if (element instanceof Window) {
        return getWindowScrollLeft(rootWindow);
    }
    return element.scrollLeft;
}
function getScrollableElement(element) {
    const rootDocument = element.ownerDocument;
    const rootWindow = rootDocument.defaultView;
    const props = [
        'auto',
        'scroll'
    ];
    let el = element.parentElement;
    while(el && el.style && rootDocument.body !== el){
        let { overflow, overflowX, overflowY } = el.style;
        if ([
            overflow,
            overflowX,
            overflowY
        ].includes('scroll')) {
            return el;
        } else {
            if (rootWindow) {
                ({ overflow, overflowX, overflowY } = rootWindow.getComputedStyle(el));
            }
            if (props.includes(overflow) || props.includes(overflowX) || props.includes(overflowY)) {
                return el;
            }
        }
        // The '+ 1' after the scrollHeight/scrollWidth is to prevent problems with zoomed out Chrome.
        if (el.clientHeight <= el.scrollHeight + 1 && (props.includes(overflowY) || props.includes(overflow))) {
            return el;
        }
        if (el.clientWidth <= el.scrollWidth + 1 && (props.includes(overflowX) || props.includes(overflow))) {
            return el;
        }
        el = el.parentElement;
    }
    return rootWindow ?? rootDocument.documentElement;
}
function getMaximumScrollTop(element) {
    return element.scrollHeight - element.clientHeight;
}
function getMaximumScrollLeft(element) {
    return element.scrollWidth - element.clientWidth;
}
function getTrimmingContainer(base) {
    const rootDocument = base.ownerDocument;
    const rootWindow = rootDocument.defaultView;
    let el = base.parentElement;
    while(el && el.style && rootDocument.body !== el){
        if (el.style.overflow !== 'visible' && el.style.overflow !== '') {
            return el;
        }
        if (rootWindow) {
            const computedStyle = rootWindow.getComputedStyle(el);
            const allowedProperties = [
                'scroll',
                'hidden',
                'auto',
                'clip'
            ];
            const property = computedStyle.getPropertyValue('overflow');
            const propertyY = computedStyle.getPropertyValue('overflow-y');
            const propertyX = computedStyle.getPropertyValue('overflow-x');
            if (allowedProperties.includes(property) || allowedProperties.includes(propertyY) || allowedProperties.includes(propertyX)) {
                return el;
            }
        }
        el = el.parentElement;
    }
    return rootWindow ?? rootDocument.documentElement;
}
function getStyle(element, prop, rootWindow = window) {
    if (!element) {
        return;
    } else if (element instanceof Window) {
        if (prop === 'width') {
            return `${element.innerWidth}px`;
        } else if (prop === 'height') {
            return `${element.innerHeight}px`;
        }
        return;
    }
    const styleProp = element.style.getPropertyValue(prop) || element.style[prop];
    if (styleProp !== '' && styleProp !== undefined) {
        return styleProp;
    }
    const computedStyle = rootWindow.getComputedStyle(element);
    const computedProp = computedStyle.getPropertyValue(prop) || computedStyle[prop];
    if (computedProp !== '' && computedProp !== undefined) {
        return computedProp;
    }
}
function matchesCSSRules(element, rule) {
    let result = false;
    if (rule instanceof CSSStyleRule && rule.selectorText) {
        const elementWithMs = element;
        if ('msMatchesSelector' in element && typeof elementWithMs.msMatchesSelector === 'function') {
            result = elementWithMs.msMatchesSelector(rule.selectorText);
        } else if (element.matches) {
            result = element.matches(rule.selectorText);
        }
    }
    return result;
}
function outerWidth(element) {
    return element.offsetWidth;
}
function outerHeight(element) {
    return element.offsetHeight;
}
function innerHeight(element) {
    return element instanceof Window ? element.innerHeight : element.clientHeight;
}
function innerWidth(element) {
    return element instanceof Window ? element.innerWidth : element.clientWidth;
}
function addEvent(element, event, callback) {
    element.addEventListener(event, callback, false);
}
function removeEvent(element, event, callback) {
    element.removeEventListener(event, callback, false);
}
function getCaretPosition(el) {
    if (el.selectionStart) {
        return el.selectionStart;
    }
    return 0;
}
function getSelectionEndPosition(el) {
    if (el.selectionEnd) {
        return el.selectionEnd;
    }
    return 0;
}
function getSelectionText(rootWindow = window) {
    const rootDocument = rootWindow.document;
    let text = '';
    if (rootWindow.getSelection) {
        text = rootWindow.getSelection()?.toString() ?? '';
    } else {
        if ('selection' in rootDocument) {
            const docWithSel = rootDocument;
            if (docWithSel.selection && docWithSel.selection.type !== 'Control') {
                text = docWithSel.selection.createRange().text;
            }
        }
    }
    return text;
}
function clearTextSelection(rootWindow = window) {
    // http://stackoverflow.com/questions/3169786/clear-text-selection-with-javascript
    if (rootWindow.getSelection) {
        const sel = rootWindow.getSelection();
        if (sel?.empty) {
            sel.empty();
        } else if (sel?.removeAllRanges) {
            sel.removeAllRanges();
        }
    }
}
function setCaretPosition(element, pos, endPos) {
    if (endPos === undefined) {
        endPos = pos;
    }
    if (element.setSelectionRange) {
        element.focus();
        try {
            element.setSelectionRange(pos, endPos);
        } catch (err) {
            const elementParent = element.parentElement;
            if (elementParent) {
                const parentDisplayValue = elementParent.style.display;
                elementParent.style.display = 'block';
                element.setSelectionRange(pos, endPos);
                elementParent.style.display = parentDisplayValue;
            }
        }
    }
}
let cachedScrollbarWidth;
function getFractionalScalingCompensation(rootDocument = document) {
    if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isWindowsOS"])()) {
        return 0;
    }
    // On Windows, fractional scaling makes the scrollbar wider to compensate for the anti-aliasing.
    // This is a workaround to calculate the correct scrollbar width.
    return Number.isInteger(rootDocument.defaultView?.devicePixelRatio || 1) ? 0 : 2;
}
/**
 * Helper to calculate scrollbar width.
 * Source: https://stackoverflow.com/questions/986937/how-can-i-get-the-browsers-scrollbar-sizes.
 *
 * @private
 * @param {Document} rootDocument The owner of the document.
 * @returns {number}
 */ // eslint-disable-next-line no-restricted-globals
function walkontableCalculateScrollbarWidth(rootDocument = document) {
    const calculateScrollbarWidth = (shouldForceWebkitScrollbarStyles = false)=>{
        const inner = rootDocument.createElement('div');
        inner.style.height = '200px';
        inner.style.width = '100%';
        const outer = rootDocument.createElement('div');
        outer.classList.add('htScrollbarTest');
        if (shouldForceWebkitScrollbarStyles) {
            outer.classList.add('htScrollbarSafariTest');
        }
        outer.style.boxSizing = 'content-box';
        outer.style.height = '150px';
        outer.style.left = '0px';
        outer.style.overflow = 'hidden';
        outer.style.position = 'absolute';
        outer.style.top = '0px';
        outer.style.width = '200px';
        outer.style.visibility = 'hidden';
        outer.appendChild(inner);
        rootDocument.body.appendChild(outer);
        const w1 = inner.offsetWidth;
        outer.style.overflow = 'scroll';
        let w2 = inner.offsetWidth;
        if (w1 === w2) {
            w2 = outer.clientWidth;
        }
        rootDocument.body.removeChild(outer);
        return w1 - w2;
    };
    const defaultScrollbarWidth = calculateScrollbarWidth();
    // Safari around 26.x (e.g. 26.2/26.3) changed how scrollbars are rendered: overlay scrollbars
    // and the standard scrollbar-color/scrollbar-width properties are preferred. When those are set
    // (e.g. on .wtHolder via theme), they override the non-standard ::-webkit-scrollbar per spec,
    // so the real scrollbar can be 0-width. Older Safari (before 26.1) sometimes reports 0 because
    // it needs explicit ::-webkit-scrollbar size to lay out a classic scrollbar; the fallback below
    // forces that via htScrollbarSafariTest so we get a correct non-zero width. We must only run
    // this fallback when isSafariBefore261(), otherwise Safari 26.1+ with overlay scrollbars would
    // be given 9px from the probe (which has no theme) while .wtHolder actually has 0-width overlay.
    if (defaultScrollbarWidth === 0 && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isSafariBefore261"])() && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isMobileBrowser"])() && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isIpadOS"])()) {
        return calculateScrollbarWidth(true);
    }
    return defaultScrollbarWidth;
}
function getScrollbarWidth(rootDocument = document) {
    if (cachedScrollbarWidth === undefined) {
        cachedScrollbarWidth = walkontableCalculateScrollbarWidth(rootDocument);
    }
    return cachedScrollbarWidth;
}
function hasVerticalScrollbar(element) {
    if (element instanceof Window) {
        return element.document.body.scrollHeight > element.innerHeight;
    }
    return element.offsetWidth !== element.clientWidth;
}
function hasHorizontalScrollbar(element) {
    if (element instanceof Window) {
        return element.document.body.scrollWidth > element.innerWidth;
    }
    return element.offsetHeight !== element.clientHeight;
}
function setOverlayPosition(overlayElem, left, top) {
    overlayElem.style.transform = `translate3d(${left},${top},0)`;
}
function getCssTransform(element) {
    let transform;
    if (element.style.transform && (transform = element.style.transform) !== '') {
        return [
            'transform',
            transform
        ];
    }
    return -1;
}
function resetCssTransform(element) {
    if (element.style.transform && element.style.transform !== '') {
        element.style.transform = '';
    }
}
function isInput(element) {
    const inputs = [
        'INPUT',
        'SELECT',
        'TEXTAREA'
    ];
    return element && (inputs.indexOf(element.nodeName) > -1 || element.contentEditable === 'true');
}
function isOutsideInput(element) {
    return isInput(element) && 'hotInput' in element.dataset === false;
}
function selectElementIfAllowed(element) {
    const activeElement = element.ownerDocument.activeElement;
    if (isHTMLElement(activeElement) && !isOutsideInput(activeElement) && (element instanceof HTMLInputElement || element instanceof HTMLTextAreaElement)) {
        element.select();
    }
}
function isDetached(element) {
    return !element.parentNode;
}
function observeVisibilityChangeOnce(elementToBeObserved, callback) {
    const visibilityObserver = new IntersectionObserver((entries, observer)=>{
        entries.forEach((entry)=>{
            if (entry.isIntersecting) {
                callback();
                observer.disconnect();
            }
        });
    });
    visibilityObserver.observe(elementToBeObserved);
}
function makeElementContentEditableAndSelectItsContent(element, invisibleSelection = true, ariaHidden = true) {
    const ownerDocument = element.ownerDocument;
    const range = ownerDocument.createRange();
    const sel = ownerDocument.defaultView?.getSelection();
    setAttribute(element, 'contenteditable', true);
    if (ariaHidden) {
        const hiddenAttr = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_HIDDEN"])();
        setAttribute(element, hiddenAttr[0], hiddenAttr[1]);
    }
    if (invisibleSelection) {
        addClass(element, 'invisibleSelection');
    }
    range.selectNodeContents(element);
    sel?.removeAllRanges();
    sel?.addRange(range);
}
function removeContentEditableFromElementAndDeselect(selectedElement, removeInvisibleSelectionClass = true) {
    const sel = selectedElement.ownerDocument.defaultView?.getSelection();
    if (selectedElement.hasAttribute('aria-hidden')) {
        selectedElement.removeAttribute('aria-hidden');
    }
    sel?.removeAllRanges();
    if (removeInvisibleSelectionClass) {
        removeClass(selectedElement, 'invisibleSelection');
    }
    selectedElement.removeAttribute('contenteditable');
}
function runWithSelectedContendEditableElement(element, callback, invisibleSelection = true) {
    makeElementContentEditableAndSelectItsContent(element, invisibleSelection);
    callback();
    removeContentEditableFromElementAndDeselect(element, invisibleSelection);
}
function isHTMLElement(element) {
    if (typeof element !== 'object' || element === null) {
        return false;
    }
    const OwnElement = element.ownerDocument?.defaultView?.Element;
    return !!(OwnElement && element instanceof OwnElement);
}
function isHTMLTableCellElement(element) {
    return isHTMLElement(element) && (element.tagName === 'TD' || element.tagName === 'TH');
}
function isHTMLInputElement(element) {
    return element.tagName === 'INPUT';
}
function isShadowRoot(node) {
    return node.nodeType === Node.DOCUMENT_FRAGMENT_NODE && 'host' in node;
}
function getChildEl(parent, index) {
    const node = parent.childNodes[index];
    return node ? node : null;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/dom/event.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "isImmediatePropagationStopped",
    ()=>isImmediatePropagationStopped,
    "isLeftClick",
    ()=>isLeftClick,
    "isMiddleClick",
    ()=>isMiddleClick,
    "isRightClick",
    ()=>isRightClick,
    "isTouchEvent",
    ()=>isTouchEvent,
    "offsetRelativeTo",
    ()=>offsetRelativeTo,
    "stopImmediatePropagation",
    ()=>stopImmediatePropagation
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
;
function stopImmediatePropagation(event) {
    event.isImmediatePropagationEnabled = false;
    event.cancelBubble = true;
}
function isImmediatePropagationStopped(event) {
    return event.isImmediatePropagationEnabled === false;
}
function isRightClick(event) {
    return event.button === 2;
}
function isLeftClick(event) {
    return event.button === 0;
}
function isMiddleClick(event) {
    return event.button === 1;
}
function isTouchEvent(event) {
    return typeof TouchEvent !== 'undefined' && event instanceof TouchEvent;
}
function offsetRelativeTo(event, untilElement) {
    const offset = {
        x: event.offsetX,
        y: event.offsetY
    };
    let element = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event);
    if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(untilElement) || element !== untilElement && element.contains(untilElement)) {
        return offset;
    }
    while(element !== untilElement){
        offset.x += element.offsetLeft;
        offset.y += element.offsetTop;
        element = element.offsetParent;
    }
    return offset;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Throws an Error with a Handsontable-specific cause for easy identification.
 *
 * Use this helper instead of `throw new Error()` to ensure all Handsontable errors
 * can be recognized by checking `error.cause?.handsontable === true`.
 *
 * @param message The error message to display.
 * @throws {Error} Always throws an Error with `cause: { handsontable: true }`.
 */ __turbopack_context__.s([
    "throwWithCause",
    ()=>throwWithCause
]);
function throwWithCause(message) {
    // eslint-disable-next-line handsontable/no-native-error-throw
    throw new Error(message, {
        cause: {
            handsontable: true
        }
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/feature.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/* eslint-disable no-restricted-globals */ /**
 * Polyfill for requestAnimationFrame.
 *
 * @param {Function} callback The function to call when it's time.
 * @returns {number}
 */ __turbopack_context__.s([
    "cancelAnimationFrame",
    ()=>cancelAnimationFrame,
    "getComparisonFunction",
    ()=>getComparisonFunction,
    "isCSR",
    ()=>isCSR,
    "isTouchSupported",
    ()=>isTouchSupported,
    "requestAnimationFrame",
    ()=>requestAnimationFrame
]);
function requestAnimationFrame(callback) {
    return window.requestAnimationFrame(callback);
}
function cancelAnimationFrame(id) {
    window.cancelAnimationFrame(id);
}
function isTouchSupported() {
    return 'ontouchstart' in window;
}
function isCSR() {
    return typeof window !== 'undefined';
}
let comparisonFunction;
function getComparisonFunction(language, options = {}) {
    if (comparisonFunction) {
        return comparisonFunction;
    }
    if (typeof Intl === 'object') {
        comparisonFunction = new Intl.Collator(language, options).compare;
    } else if (typeof String.prototype.localeCompare === 'function') {
        comparisonFunction = (a, b)=>`${a}`.localeCompare(b);
    } else {
        comparisonFunction = (a, b)=>{
            if (a === b) {
                return 0;
            }
            return a > b ? -1 : 1;
        };
    }
    return comparisonFunction;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/function.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "curry",
    ()=>curry,
    "curryRight",
    ()=>curryRight,
    "debounce",
    ()=>debounce,
    "fastCall",
    ()=>fastCall,
    "isFunction",
    ()=>isFunction,
    "partial",
    ()=>partial,
    "pipe",
    ()=>pipe,
    "throttle",
    ()=>throttle,
    "throttleAfterHits",
    ()=>throttleAfterHits
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
;
;
function isFunction(func) {
    return typeof func === 'function';
}
function throttle(func, wait = 200) {
    let lastCalled = 0;
    const result = {
        lastCallThrottled: true
    };
    let lastTimer = null;
    /**
   * @param {...*} args The list of arguments passed during the function invocation.
   * @returns {object}
   */ function _throttle(...args) {
        const stamp = Date.now();
        let needCall = false;
        result.lastCallThrottled = true;
        if (!lastCalled) {
            lastCalled = stamp;
            needCall = true;
        }
        const remaining = wait - (stamp - lastCalled);
        if (needCall) {
            result.lastCallThrottled = false;
            func.apply(this, args);
        } else {
            if (lastTimer) {
                clearTimeout(lastTimer);
            }
            lastTimer = setTimeout(()=>{
                result.lastCallThrottled = false;
                func.apply(this, args);
                lastCalled = 0;
                lastTimer = null;
            }, remaining);
        }
        return result;
    }
    return _throttle;
}
function throttleAfterHits(func, wait = 200, hits = 10) {
    const funcThrottle = throttle(func, wait);
    let remainHits = hits;
    /**
   *
   */ function _clearHits() {
        remainHits = hits;
    }
    /**
   * @param {*} args The list of arguments passed during the function invocation.
   * @returns {*}
   */ function _throttleAfterHits(...args) {
        if (remainHits) {
            remainHits -= 1;
            return func.apply(this, args);
        }
        return funcThrottle.apply(this, args);
    }
    _throttleAfterHits.clearHits = _clearHits;
    return _throttleAfterHits;
}
function debounce(func, wait = 200) {
    let lastTimer = null;
    let result;
    /**
   * @param {*} args The list of arguments passed during the function invocation.
   * @returns {*}
   */ function _debounce(...args) {
        if (lastTimer) {
            clearTimeout(lastTimer);
        }
        lastTimer = setTimeout(()=>{
            lastTimer = null;
            result = func.apply(this, args);
        }, wait);
        return result;
    }
    /**
   * Clears the scheduled invocation so the debounced function does not run later.
   */ function cancel() {
        if (lastTimer !== null) {
            clearTimeout(lastTimer);
            lastTimer = null;
        }
    }
    _debounce.cancel = cancel;
    return _debounce;
}
function pipe(...functions) {
    const [firstFunc, ...restFunc] = functions;
    return function _pipe(...args) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayReduce"])(restFunc, (acc, fn)=>fn(acc), firstFunc.apply(this, args));
    };
}
function partial(func, ...params) {
    return function _partial(...restParams) {
        return func.apply(this, params.concat(restParams));
    };
}
function curry(func) {
    const argsLength = func.length;
    /**
   * @param {*} argsSoFar The list of arguments passed during the function invocation.
   * @returns {Function}
   */ function given(argsSoFar) {
        return function _curry(...params) {
            const passedArgsSoFar = argsSoFar.concat(params);
            let result;
            if (passedArgsSoFar.length >= argsLength) {
                result = func.apply(this, passedArgsSoFar);
            } else {
                result = given(passedArgsSoFar);
            }
            return result;
        };
    }
    return given([]);
}
function curryRight(func) {
    const argsLength = func.length;
    /**
   * @param {*} argsSoFar The list of arguments passed during the function invocation.
   * @returns {Function}
   */ function given(argsSoFar) {
        return function _curry(...params) {
            const passedArgsSoFar = argsSoFar.concat(params.reverse());
            let result;
            if (passedArgsSoFar.length >= argsLength) {
                result = func.apply(this, passedArgsSoFar);
            } else {
                result = given(passedArgsSoFar);
            }
            return result;
        };
    }
    return given([]);
}
function fastCall(func, context, arg1, arg2, arg3, arg4, arg5, arg6) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(arg6)) {
        return func.call(context, arg1, arg2, arg3, arg4, arg5, arg6);
    } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(arg5)) {
        return func.call(context, arg1, arg2, arg3, arg4, arg5);
    } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(arg4)) {
        return func.call(context, arg1, arg2, arg3, arg4);
    } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(arg3)) {
        return func.call(context, arg1, arg2, arg3);
    } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(arg2)) {
        return func.call(context, arg1, arg2);
    } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(arg1)) {
        return func.call(context, arg1);
    }
    return func.call(context);
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "_injectProductInfo",
    ()=>_injectProductInfo,
    "isDefined",
    ()=>isDefined,
    "isEmpty",
    ()=>isEmpty,
    "isRegExp",
    ()=>isRegExp,
    "isUndefined",
    ()=>isUndefined,
    "stringify",
    ()=>stringify
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/templateLiteralTag.mjs [client] (ecmascript)");
;
function stringify(value) {
    let result;
    switch(typeof value){
        case 'string':
        case 'number':
            result = `${value}`;
            break;
        case 'object':
            result = value === null ? '' : value.toString();
            break;
        case 'undefined':
            result = '';
            break;
        default:
            result = value.toString();
            break;
    }
    return result;
}
function isDefined(variable) {
    return typeof variable !== 'undefined';
}
function isUndefined(variable) {
    return typeof variable === 'undefined';
}
function isEmpty(variable) {
    return variable === null || variable === '' || isUndefined(variable);
}
function isRegExp(variable) {
    return Object.prototype.toString.call(variable) === '[object RegExp]';
}
/* eslint-disable dot-notation, no-useless-escape, max-len, no-bitwise, computed-property-spacing, jsdoc/require-jsdoc, no-restricted-globals, no-console, prefer-const, no-unused-expressions, no-plusplus, space-infix-ops, comma-spacing, no-nested-ternary */ const _m = '\x6C\x65\x6E\x67\x74\x68';
const _hd = (v)=>parseInt(v, 16);
const _pi = (v)=>parseInt(v, 10);
const _ss = (v, s, l)=>v['\x73\x75\x62\x73\x74\x72'](s, l);
const _cp = (v)=>(v.codePointAt(0) ?? 0) - 65;
const _norm = (v)=>`${v}`.replace(/\-/g, '');
const _extractTime = (v)=>_hd(_ss(_norm(v), _hd('12'), _cp('\x46'))) / (_hd(_ss(_norm(v), _cp('\x42'), ~~![][_m])) || 9);
const _ignored = ()=>typeof location !== 'undefined' && /^([a-z0-9\-]+\.)?\x68\x61\x6E\x64\x73\x6F\x6E\x74\x61\x62\x6C\x65\x2E\x63\x6F\x6D$/i.test(location.host);
let _notified = false;
const consoleMessages = {
    invalid: ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`
    The license key for Handsontable is invalid.\x20
    If you need any help, contact us at support@handsontable.com.`,
    expired: ({ keyValidityDate, hotVersion })=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`
    The license key for Handsontable expired on ${keyValidityDate}, and is not valid for the installed\x20
    version ${hotVersion}. Renew your license key at handsontable.com or downgrade to a version released prior\x20
    to ${keyValidityDate}. If you need any help, contact us at sales@handsontable.com.`,
    missing: ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`
    The license key for Handsontable is missing. Use your purchased key to activate the product.\x20
    Alternatively, you can activate Handsontable to use for non-commercial purposes by\x20
    passing the key: 'non-commercial-and-evaluation'. If you need any help, contact\x20
    us at support@handsontable.com.`,
    non_commercial: ()=>''
};
const domMessages = {
    invalid: ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`
    The license key for Handsontable is invalid.\x20
    <a href="https://handsontable.com/docs/tutorial-license-key.html" target="_blank">Read more</a> on how to\x20
    install it properly or contact us at <a href="mailto:support@handsontable.com">support@handsontable.com</a>.`,
    expired: ({ keyValidityDate, hotVersion })=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`
    The license key for Handsontable expired on ${keyValidityDate}, and is not valid for the installed\x20
    version ${hotVersion}. <a href="https://handsontable.com/pricing" target="_blank">Renew</a> your\x20
    license key or downgrade to a version released prior to ${keyValidityDate}. If you need any\x20
    help, contact us at <a href="mailto:sales@handsontable.com">sales@handsontable.com</a>.`,
    missing: ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`
    The license key for Handsontable is missing. Use your purchased key to activate the product.\x20
    Alternatively, you can activate Handsontable to use for non-commercial purposes by\x20
    passing the key: 'non-commercial-and-evaluation'.\x20
    <a href="https://handsontable.com/docs/tutorial-license-key.html" target="_blank">Read more</a> about it in\x20
    the documentation or contact us at <a href="mailto:support@handsontable.com">support@handsontable.com</a>.`,
    non_commercial: ()=>''
};
function _injectProductInfo({ className, key, element, releaseDate }) {
    const hasValidType = !isEmpty(key);
    const isNonCommercial = typeof key === 'string' && (key.toLowerCase() === 'non-commercial-and-evaluation' || key.toLowerCase() === 'ht68e-1f2b7-47158-70b05-0842f');
    const hotVersion = "18.0.0";
    let keyValidityDate;
    let consoleMessageState = 'invalid';
    let domMessageState = 'invalid';
    key = _norm(key || '');
    const schemaValidity = _checkKeySchema(key);
    if (isNonCommercial) {
        consoleMessageState = 'non_commercial';
        domMessageState = 'valid';
    } else if (hasValidType || schemaValidity) {
        if (schemaValidity) {
            const resolvedReleaseDate = releaseDate ?? "30/06/2026" ?? '';
            const [dd, mm, yyyy] = resolvedReleaseDate.split('/').map(Number);
            const releaseDays = Math.floor(Date.UTC(yyyy, mm - 1, dd) / 8.64e7);
            const keyValidityDays = _extractTime(key);
            keyValidityDate = new Intl.DateTimeFormat('en-US', {
                year: 'numeric',
                month: 'long',
                day: '2-digit',
                timeZone: 'UTC'
            }).format((keyValidityDays + 1) * 8.64e7);
            if (releaseDays > keyValidityDays) {
                consoleMessageState = 'expired';
                domMessageState = 'expired';
            } else {
                consoleMessageState = 'valid';
                domMessageState = 'valid';
            }
        } else {
            consoleMessageState = 'invalid';
            domMessageState = 'invalid';
        }
    } else {
        consoleMessageState = 'missing';
        domMessageState = 'missing';
    }
    if (_ignored()) {
        consoleMessageState = 'valid';
        domMessageState = 'valid';
    }
    if (!_notified && consoleMessageState !== 'valid') {
        const message = consoleMessages[consoleMessageState]({
            keyValidityDate,
            hotVersion
        });
        if (message) {
            console[consoleMessageState === 'non_commercial' ? 'info' : 'warn'](consoleMessages[consoleMessageState]({
                keyValidityDate,
                hotVersion
            }));
        }
        _notified = true;
    }
    if (domMessageState !== 'valid' && element) {
        const message = domMessages[domMessageState]({
            keyValidityDate,
            hotVersion
        });
        if (message) {
            const messageNode = document.createElement('div');
            const innerNode = document.createElement('div');
            messageNode.className = `handsontable ${className}`;
            innerNode.className = `${className}_inner`;
            innerNode.innerHTML = domMessages[domMessageState]({
                keyValidityDate,
                hotVersion
            });
            messageNode.appendChild(innerNode);
            element.appendChild(messageNode);
            return messageNode;
        }
    }
    return null;
}
function _checkKeySchema(v) {
    let z = [][_m];
    let p = z;
    if (v[_m] !== _cp('\x5A')) {
        return false;
    }
    for(let c = '', i = '\x42\x3C\x48\x34\x50\x2B'.split(''), j = _cp(i.shift() ?? 'A'); j; j = _cp(i.shift() ?? 'A')){
        --j < ''[_m] ? p = p | (_pi(`${_pi(_hd(c) + (_hd(_ss(v, Math.abs(j), 2)) + String([])).padStart(2, '0'))}`) % 97 || 2) >> 1 : c = _ss(v, j, !j ? 6 : i[_m] === 1 ? 9 : 8);
    }
    return p === z;
} /* eslint-enable */ 
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/moves.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getMoves",
    ()=>getMoves
]);
/**
 * Gets first position where to move element (respecting the fact that some element will be sooner or later
 * taken out of the dataset in order to move them).
 *
 * @param {Array<number>} movedIndexes Sequence of moved indexes for certain axis.
 * @param {number} finalIndex Final place where to move rows.
 * @param {number} numberOfIndexes Number of indexes in a dataset.
 * @returns {number} Index informing where to move the first element.
 */ function getMoveLine(movedIndexes, finalIndex, numberOfIndexes) {
    const notMovedElements = Array.from(Array(numberOfIndexes).keys()).filter((index)=>movedIndexes.includes(index) === false);
    if (finalIndex === 0) {
        return notMovedElements[finalIndex] ?? 0; // Moving before the first dataset's element.
    }
    return notMovedElements[finalIndex - 1] + 1; // Moving before another element.
}
/**
 * Gets initially calculated move positions.
 *
 * @param {Array<number>} movedIndexes Sequence of moved indexes for certain axis.
 * @param {number} moveLine Final place where to move rows.
 * @returns {Array<{from: number, to: number}>} Initially calculated move positions.
 */ function getInitiallyCalculatedMoves(movedIndexes, moveLine) {
    const moves = [];
    movedIndexes.forEach((movedIndex)=>{
        const move = {
            from: movedIndex,
            to: moveLine
        };
        moves.forEach((previouslyMovedIndex)=>{
            const isMovingFromEndToStart = previouslyMovedIndex.from > previouslyMovedIndex.to;
            const isMovingElementBefore = previouslyMovedIndex.to <= move.from;
            const isMovingAfterElement = previouslyMovedIndex.from > move.from;
            if (isMovingAfterElement && isMovingElementBefore && isMovingFromEndToStart) {
                move.from += 1;
            }
        });
        // Moved element from right to left (or bottom to top).
        if (move.from >= moveLine) {
            moveLine += 1;
        }
        moves.push(move);
    });
    return moves;
}
/**
 * Gets finally calculated move positions (after adjusting).
 *
 * @param {Array<{from: number, to: number}>} moves Initially calculated move positions.
 * @returns {Array<{from: number, to: number}>} Finally calculated move positions (after adjusting).
 */ function adjustedCalculatedMoves(moves) {
    moves.forEach((move, index)=>{
        const nextMoved = moves.slice(index + 1);
        nextMoved.forEach((nextMovedIndex)=>{
            const isMovingFromStartToEnd = nextMovedIndex.from < nextMovedIndex.to;
            if (nextMovedIndex.from > move.from && isMovingFromStartToEnd) {
                nextMovedIndex.from -= 1;
            }
        });
    });
    return moves;
}
function getMoves(movedIndexes, finalIndex, numberOfIndexes) {
    const moves = getInitiallyCalculatedMoves(movedIndexes, getMoveLine(movedIndexes, finalIndex, numberOfIndexes));
    return adjustedCalculatedMoves(moves);
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/number.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Checks if the passed value is numeric one. For example these values (passed as string or number)
 * are considered as numeric values:
 *  - 0.001
 *  - .001
 *  - 10000
 *  - 1e+26
 *  - 22e-26
 *  - .45e+26
 *  - 0xabcdef (hex)
 *  - 0x1 (hex)
 *
 * these values are not considered as numeric:
 *  - - 1000
 *  - 100 000
 *
 * @param {*} value The value to check.
 * @param {string[]} additionalDelimiters An additional delimiters to be used while checking the numeric value.
 * @returns {boolean}
 */ __turbopack_context__.s([
    "clamp",
    ()=>clamp,
    "getParsedNumber",
    ()=>getParsedNumber,
    "isCommaThousandsGroupedInteger",
    ()=>isCommaThousandsGroupedInteger,
    "isDotThousandsGroupedFloat",
    ()=>isDotThousandsGroupedFloat,
    "isDotThousandsGroupedInteger",
    ()=>isDotThousandsGroupedInteger,
    "isNumeric",
    ()=>isNumeric,
    "isNumericLike",
    ()=>isNumericLike,
    "isUnsignedNumber",
    ()=>isUnsignedNumber,
    "rangeEach",
    ()=>rangeEach,
    "rangeEachReverse",
    ()=>rangeEachReverse,
    "valueAccordingPercent",
    ()=>valueAccordingPercent
]);
function isNumeric(value, additionalDelimiters = []) {
    if (typeof value === 'number') {
        return !isNaN(value) && isFinite(value);
    } else if (typeof value === 'string') {
        if (value.length === 0) {
            return false;
        } else if (value.length === 1) {
            return /\d/.test(value);
        }
        const delimiter = Array.from(new Set([
            '.',
            ...additionalDelimiters
        ])).map((d)=>`\\${d}`).join('|');
        return new RegExp(`^[+-]?(((${delimiter})?\\d+((${delimiter})\\d+)?(e[+-]?\\d+)?)|(0x[a-f\\d]+))$`, 'i').test(value.trim());
    } else if (typeof value === 'object' && value !== null) {
        return typeof value.valueOf() === 'number' && !(value instanceof Date);
    }
    return false;
}
function isNumericLike(value) {
    return isNumeric(value, [
        ','
    ]);
}
function isCommaThousandsGroupedInteger(value, decimalSeparator) {
    if (decimalSeparator !== '.' || typeof value !== 'string') {
        return false;
    }
    return /^[+-]?[1-9]\d{0,2}(,\d{3})+$/.test(value.trim());
}
function isDotThousandsGroupedInteger(value, decimalSeparator) {
    if (decimalSeparator !== ',' || typeof value !== 'string') {
        return false;
    }
    return /^[+-]?[1-9]\d{0,2}(\.\d{3})+$/.test(value.trim());
}
function isDotThousandsGroupedFloat(value, decimalSeparator) {
    if (decimalSeparator !== ',' || typeof value !== 'string') {
        return false;
    }
    return /^[+-]?[1-9]\d{0,2}(\.\d{3})+,\d+$/.test(value.trim());
}
function rangeEach(rangeFrom, rangeTo, iteratee) {
    let index;
    let end;
    let fn;
    if (typeof rangeTo === 'function') {
        index = -1;
        end = rangeFrom;
        fn = rangeTo;
    } else {
        index = rangeFrom - 1;
        end = rangeTo;
        fn = iteratee;
    }
    /* eslint-disable-next-line no-plusplus */ while(++index <= end){
        if (fn(index) === false) {
            break;
        }
    }
}
function rangeEachReverse(rangeFrom, rangeTo, iteratee) {
    let index = rangeFrom + 1;
    let end;
    let fn;
    if (typeof rangeTo === 'function') {
        fn = rangeTo;
        end = 0;
    } else {
        fn = iteratee;
        end = rangeTo;
    }
    /* eslint-disable-next-line no-plusplus */ while(--index >= end){
        if (fn(index) === false) {
            break;
        }
    }
}
function valueAccordingPercent(value, percent) {
    percent = Number.parseInt(percent.toString().replace('%', ''), 10);
    percent = isNaN(percent) ? 0 : percent;
    return Number.parseInt(String(value * percent / 100), 10);
}
function clamp(value, minValue, maxValue) {
    if (Math.min(value, minValue) === value) {
        return minValue;
    } else if (Math.max(value, maxValue) === value) {
        return maxValue;
    }
    return value;
}
function getParsedNumber(numericData, options = {}) {
    const { decimalSeparator } = options;
    const normalizedNumericData = numericData.trim();
    if (isCommaThousandsGroupedInteger(numericData, decimalSeparator)) {
        return Number.parseFloat(normalizedNumericData.replaceAll(',', ''));
    }
    if (isDotThousandsGroupedInteger(numericData, decimalSeparator)) {
        return Number.parseFloat(normalizedNumericData.replaceAll('.', ''));
    }
    if (isDotThousandsGroupedFloat(numericData, decimalSeparator)) {
        return Number.parseFloat(normalizedNumericData.replaceAll('.', '').replace(',', '.'));
    }
    // Unifying "float like" string. Change from value with comma determiner to value with dot determiner,
    // for example from `450,65` to `450.65`.
    const unifiedNumericData = normalizedNumericData.replace(',', '.');
    if (isNaN(Number.parseFloat(unifiedNumericData)) === false) {
        return Number.parseFloat(unifiedNumericData);
    }
    return null;
}
function isUnsignedNumber(value) {
    return Number.isInteger(value) && typeof value === 'number' && value >= 0;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "assignObjectDefaults",
    ()=>assignObjectDefaults,
    "clone",
    ()=>clone,
    "createObjectPropListener",
    ()=>createObjectPropListener,
    "deepClone",
    ()=>deepClone,
    "deepExtend",
    ()=>deepExtend,
    "deepMerge",
    ()=>deepMerge,
    "deepObjectSize",
    ()=>deepObjectSize,
    "defineGetter",
    ()=>defineGetter,
    "duckSchema",
    ()=>duckSchema,
    "extend",
    ()=>extend,
    "getProperty",
    ()=>getProperty,
    "hasOwnProperty",
    ()=>hasOwnProperty,
    "inherit",
    ()=>inherit,
    "isKeyValueObject",
    ()=>isKeyValueObject,
    "isObject",
    ()=>isObject,
    "isObjectEqual",
    ()=>isObjectEqual,
    "isPlainObject",
    ()=>isPlainObject,
    "mixin",
    ()=>mixin,
    "objectEach",
    ()=>objectEach,
    "setProperty",
    ()=>setProperty
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
;
function duckSchema(object) {
    let schema;
    if (Array.isArray(object)) {
        schema = object.length ? new Array(object.length).fill(null) : [];
    } else {
        const schemaObj = Object.create(null);
        objectEach(object, (value, key)=>{
            if (key === '__children') {
                return;
            }
            if (value && typeof value === 'object' && !Array.isArray(value)) {
                schemaObj[key] = duckSchema(value);
            } else if (Array.isArray(value)) {
                if (value.length && typeof value[0] === 'object' && !Array.isArray(value[0])) {
                    schemaObj[key] = [
                        duckSchema(value[0])
                    ];
                } else {
                    schemaObj[key] = [];
                }
            } else {
                schemaObj[key] = null;
            }
        });
        schema = schemaObj;
    }
    return schema;
}
function inherit(Child, Parent) {
    Parent.prototype.constructor = Parent;
    Child.prototype = new Parent();
    Child.prototype.constructor = Child;
    return Child;
}
function extend(target, extension, writableKeys) {
    const hasWritableKeys = Array.isArray(writableKeys);
    objectEach(extension, (value, key)=>{
        if (key === '__proto__' || key === 'constructor' || key === 'prototype') {
            return;
        }
        if (hasWritableKeys === false || writableKeys.includes(key)) {
            target[key] = value;
        }
    });
    return target;
}
function deepExtend(target, extension) {
    objectEach(extension, (_value, key)=>{
        if (key === '__proto__' || key === 'constructor' || key === 'prototype') {
            return;
        }
        const extensionValue = extension[key];
        if (extensionValue && typeof extensionValue === 'object') {
            if (!target[key]) {
                if (Array.isArray(extensionValue)) {
                    target[key] = [];
                } else if (Object.prototype.toString.call(extensionValue) === '[object Date]') {
                    target[key] = extensionValue;
                } else {
                    target[key] = {};
                }
            }
            deepExtend(target[key], extensionValue);
        } else {
            target[key] = extensionValue;
        }
    });
}
function deepClone(obj) {
    if (Array.isArray(obj)) {
        return obj.map((item)=>deepClone(item));
    }
    if (obj instanceof Date) {
        return new Date(obj);
    }
    if (typeof obj === 'object' && obj !== null) {
        const result = {};
        for (const [key, val] of Object.entries(obj)){
            if (key === '__proto__' || key === 'constructor' || key === 'prototype') {
                continue;
            }
            result[key] = deepClone(val);
        }
        return result;
    }
    return obj;
}
function clone(object) {
    const result = {};
    objectEach(object, (value, key)=>{
        result[key] = value;
    });
    return result;
}
/**
 * Clones the initial value, performing a deep clone for objects and arrays.
 *
 * @param {unknown} newValue The value to clone.
 * @returns {unknown}
 */ function mixinInitValue(newValue) {
    let result = newValue;
    if (Array.isArray(result) || isObject(result)) {
        result = deepClone(result);
    }
    return result;
}
/**
 * Creates a getter function for a mixin property.
 *
 * @param {string} property The property name.
 * @param {unknown} initialValue The initial value for the property.
 * @returns {Function}
 */ function mixinGetter(property, initialValue) {
    const propertyName = `_${property}`;
    return function() {
        if (this[propertyName] === undefined) {
            this[propertyName] = mixinInitValue(initialValue);
        }
        return this[propertyName];
    };
}
/**
 * Creates a setter function for a mixin property.
 *
 * @param {string} property The property name.
 * @returns {Function}
 */ function mixinSetter(property) {
    const propertyName = `_${property}`;
    return function(newValue) {
        this[propertyName] = newValue;
    };
}
function mixin(Base, ...mixins) {
    if (!Base.MIXINS) {
        Base.MIXINS = [];
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(mixins, (mixinItem)=>{
        Base.MIXINS.push(mixinItem.MIXIN_NAME);
        objectEach(mixinItem, (value, key)=>{
            if (Base.prototype[key] !== undefined) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])(`Mixin conflict. Property '${key}' already exist and cannot be overwritten.`);
            }
            if (typeof value === 'function') {
                Base.prototype[key] = value;
            } else {
                Object.defineProperty(Base.prototype, key, {
                    get: mixinGetter(key, value),
                    set: mixinSetter(key),
                    configurable: true
                });
            }
        });
    });
    return Base;
}
function isObjectEqual(object1, object2) {
    const stableStringify = (obj)=>{
        if (obj === undefined) {
            return 'undefined';
        }
        if (Array.isArray(obj)) {
            return `[${obj.map(stableStringify).join(',')}]`;
        }
        if (obj instanceof Date) {
            return JSON.stringify(obj);
        }
        if (obj !== null && typeof obj === 'object') {
            const pairs = Object.entries(obj).sort(([a], [b])=>a.localeCompare(b)).map(([key, val])=>`${JSON.stringify(key)}:${stableStringify(val)}`);
            return `{${pairs.join(',')}}`;
        }
        return JSON.stringify(obj);
    };
    return stableStringify(object1) === stableStringify(object2);
}
function isObject(object) {
    return isPlainObject(object);
}
function isPlainObject(value) {
    return Object.prototype.toString.call(value) === '[object Object]';
}
function defineGetter(object, property, value, options) {
    options.value = value;
    options.writable = options.writable !== false;
    options.enumerable = options.enumerable !== false;
    options.configurable = options.configurable !== false;
    Object.defineProperty(object, property, options);
}
function objectEach(object, iteratee) {
    const rec = object;
    // eslint-disable-next-line no-restricted-syntax
    for(const key in rec){
        if (!rec.hasOwnProperty || rec.hasOwnProperty && Object.prototype.hasOwnProperty.call(rec, key)) {
            if (iteratee(rec[key], key, object) === false) {
                break;
            }
        }
    }
    return object;
}
function getProperty(object, name) {
    const names = name.split('.');
    let result = object;
    for (const nameItem of names){
        result = result[nameItem];
        if (result === undefined) {
            return undefined;
        }
    }
    return result;
}
function setProperty(object, name, value) {
    if (typeof name !== 'string') {
        return;
    }
    if (object === null || typeof object !== 'object') {
        return;
    }
    const names = name.split('.');
    let workingObject = object;
    for(let index = 0; index < names.length; index += 1){
        const propName = names[index];
        if (propName === '__proto__' || propName === 'constructor' || propName === 'prototype') {
            // Security: prototype-polluting is not allowed
            return;
        }
        if (index !== names.length - 1) {
            if (!hasOwnProperty(workingObject, propName)) {
                workingObject[propName] = {};
            }
            const nextLevel = workingObject[propName];
            if (!isPlainObject(nextLevel)) {
                return;
            }
            workingObject = nextLevel;
        } else {
            Object.defineProperty(workingObject, propName, {
                value,
                writable: true,
                enumerable: true,
                configurable: true
            });
        }
    }
}
function deepObjectSize(object) {
    if (!isObject(object)) {
        return 0;
    }
    const recursObjLen = function(obj) {
        let result = 0;
        if (isPlainObject(obj)) {
            objectEach(obj, (value, key)=>{
                if (key === '__children') {
                    return;
                }
                result += recursObjLen(value);
            });
        } else {
            result += 1;
        }
        return result;
    };
    return recursObjLen(object);
}
function createObjectPropListener(defaultValue, propertyToListen = 'value') {
    const privateProperty = `_${propertyToListen}`;
    const holder = {
        _touched: false,
        [privateProperty]: defaultValue,
        isTouched () {
            return Boolean(this._touched);
        },
        value: undefined
    };
    Object.defineProperty(holder, propertyToListen, {
        get () {
            return this[privateProperty];
        },
        set (value) {
            this._touched = true;
            this[privateProperty] = value;
        },
        enumerable: true,
        configurable: true
    });
    return holder;
}
function hasOwnProperty(object, key) {
    return Object.prototype.hasOwnProperty.call(object, key);
}
function assignObjectDefaults(target, defaults) {
    if (typeof target !== 'object' || target === null) {
        return defaults;
    }
    if (typeof defaults !== 'object' || defaults === null) {
        return target;
    }
    const result = {};
    // Assign defaults
    Object.keys(defaults).forEach((key)=>{
        const defaultValue = defaults[key];
        const targetValue = target[key];
        if (isPlainObject(defaultValue)) {
            result[key] = assignObjectDefaults(isPlainObject(targetValue) ? targetValue : {}, defaultValue);
        } else {
            result[key] = hasOwnProperty(target, key) && targetValue !== undefined ? targetValue : defaultValue;
        }
    });
    // Copy extra keys from target that aren't in defaults
    Object.keys(target).forEach((key)=>{
        if (!hasOwnProperty(result, key)) {
            result[key] = target[key];
        }
    });
    return result;
}
function deepMerge(target = {}, source = {}) {
    const result = {
        ...target
    };
    Object.keys(source).forEach((key)=>{
        // Prevent prototype pollution
        if (key === '__proto__' || key === 'constructor' || key === 'prototype') {
            return;
        }
        const sourceValue = source[key];
        const targetValue = result[key];
        if (isPlainObject(sourceValue) && isPlainObject(targetValue)) {
            result[key] = deepMerge(targetValue, sourceValue);
        } else {
            result[key] = sourceValue;
        }
    });
    return result;
}
function isKeyValueObject(value) {
    return isPlainObject(value) && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(value.key) && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(value.value);
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/string.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "equalsIgnoreCase",
    ()=>equalsIgnoreCase,
    "isJSON",
    ()=>isJSON,
    "isPercentValue",
    ()=>isPercentValue,
    "localeLowerCase",
    ()=>localeLowerCase,
    "randomString",
    ()=>randomString,
    "sanitize",
    ()=>sanitize,
    "stripTags",
    ()=>stripTags,
    "substitute",
    ()=>substitute,
    "toHyphen",
    ()=>toHyphen,
    "toUpperCaseFirst",
    ()=>toUpperCaseFirst
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
;
function toUpperCaseFirst(string) {
    return string[0].toUpperCase() + string.substr(1);
}
function equalsIgnoreCase(...strings) {
    const unique = [];
    let length = strings.length;
    while(length){
        length -= 1;
        const string = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["stringify"])(strings[length]).toLowerCase();
        if (unique.indexOf(string) === -1) {
            unique.push(string);
        }
    }
    return unique.length === 1;
}
function randomString() {
    const buf = new Uint16Array(4);
    globalThis.crypto.getRandomValues(buf);
    return Array.from(buf, (v)=>v.toString(16).padStart(4, '0')).join('');
}
function isJSON(string) {
    if (typeof string !== 'string') {
        return false;
    }
    try {
        const parsed = JSON.parse(string);
        return typeof parsed === 'object' && parsed !== null;
    } catch  {
        return false;
    }
}
function isPercentValue(value) {
    return /^(?:\d\d?%|100%)$/.test(value);
}
function substitute(template, variables = {}) {
    return `${template}`.replace(/(?:\\)?\[([^[\]]+)]/g, (match, name)=>{
        if (match.charAt(0) === '\\') {
            return match.substr(1, match.length - 1);
        }
        return variables[name] === undefined ? '' : String(variables[name]);
    });
}
function stripTags(string) {
    const str = String(string);
    let result = '';
    let depth = 0;
    let inQuote = false;
    let quoteChar = '';
    for(let i = 0; i < str.length; i++){
        const ch = str[i];
        if (depth > 0 && !inQuote && (ch === '"' || ch === '\'')) {
            inQuote = true;
            quoteChar = ch;
        } else if (inQuote && ch === quoteChar) {
            inQuote = false;
        } else if (!inQuote && ch === '<') {
            depth += 1;
        } else if (!inQuote && ch === '>' && depth > 0) {
            depth -= 1;
        } else if (depth === 0) {
            result += ch;
        }
    }
    return result;
}
function sanitize(string) {
    return string;
}
function toHyphen(str) {
    if (typeof str !== 'string') {
        return str;
    }
    return str.replace(/([A-Z])/g, '-$1').replaceAll('_', '-').toLowerCase();
}
/**
 * Most languages use the same uppercase-to-lowercase rules as plain English — for
 * example, `'A'` always becomes `'a'`, `'Ö'` always becomes `'ö'`. Only three locales
 * diverge: Turkish and Azeri treat dotted `'İ'` and dotless `'I'` as separate letters
 * (so `'I'` lowercases to `'ı'`, not `'i'`), and Lithuanian inserts an extra dot when
 * lowercasing `'Ì'`, `'Í'`, and `'Ĩ'`. These five characters are therefore the only
 * ones in Unicode where locale matters for lowercasing, which makes them a reliable
 * probe: if a locale changes how they lowercase, it needs special handling; every other
 * locale can use the much faster `toLowerCase()`.
 */ const LOCALE_LOWERCASE_PROBE = 'IİÌÍĨ';
const LOCALE_LOWERCASE_PROBE_DEFAULT = LOCALE_LOWERCASE_PROBE.toLowerCase();
const localeLowerCaseTailoringCache = new Map();
/**
 * Returns `true` when the given locale changes how letters are lowercased compared
 * with the standard rules. This is only true for Turkish, Azeri, and Lithuanian.
 *
 * The result is cached the first time a locale is seen because the answer never
 * changes — it is a fixed property of the JavaScript engine, not of the data. Calling
 * `updateSettings({ locale: 'tr-TR' })` does not invalidate the cache; it simply means
 * the next call passes `'tr-TR'` as the locale string, which is already cached.
 *
 * An invalid or empty locale tag (e.g. `''` or `'en_US'` with an underscore) is treated
 * as "does not change lowercasing" so the function never throws.
 *
 * @param {string} [locale] A BCP 47 locale tag such as `'tr-TR'`, or `undefined` to use
 *   the host default.
 * @returns {boolean} `true` for Turkish (`tr`), Azeri (`az`), and Lithuanian (`lt`);
 *   `false` for everything else.
 */ function localeAffectsLowerCase(locale) {
    const cached = localeLowerCaseTailoringCache.get(locale);
    if (cached !== undefined) {
        return cached;
    }
    let affects;
    try {
        // eslint-disable-next-line no-restricted-syntax
        affects = LOCALE_LOWERCASE_PROBE.toLocaleLowerCase(locale) !== LOCALE_LOWERCASE_PROBE_DEFAULT;
    } catch  {
        affects = false;
    }
    localeLowerCaseTailoringCache.set(locale, affects);
    return affects;
}
function localeLowerCase(value, locale) {
    // eslint-disable-next-line no-restricted-syntax
    return localeAffectsLowerCase(locale) ? value.toLocaleLowerCase(locale) : value.toLowerCase();
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/templateLiteralTag.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "html",
    ()=>html,
    "toSingleLine",
    ()=>toSingleLine
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
;
function toSingleLine(strings, ...expressions) {
    const result = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayReduce"])(Array.from(strings), (previousValue, currentValue, index)=>{
        const valueWithoutWhiteSpaces = currentValue.replace(/\r?\n\s*/g, '');
        const expressionForIndex = expressions[index] ? expressions[index] : '';
        return previousValue + valueWithoutWhiteSpaces + expressionForIndex;
    }, '');
    return result.trim();
}
function html(strings, ...values) {
    // eslint-disable-next-line no-restricted-globals
    const template = document.createElement('template');
    template.innerHTML = Array.from(strings).reduce((acc, string, i)=>{
        return acc + string + (values[i] ?? '');
    }, '');
    const fragment = template.content.cloneNode(true);
    const refs = {};
    fragment.querySelectorAll('[data-ref]').forEach((element)=>{
        const name = element.dataset.ref ?? null;
        delete element.dataset.ref;
        if (name !== null) {
            refs[name] = element;
        }
    });
    return {
        fragment,
        refs
    };
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/themes.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getThemeClassName",
    ()=>getThemeClassName
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
;
function getThemeClassName(rootElement) {
    const { classNames } = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["findFirstParentWithClass"])(rootElement, /ht-theme-[a-zA-Z0-9_-]+/);
    return classNames.pop();
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/helpers/unicode.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "KEY_CODES",
    ()=>KEY_CODES,
    "isCtrlKey",
    ()=>isCtrlKey,
    "isCtrlMetaKey",
    ()=>isCtrlMetaKey,
    "isFunctionKey",
    ()=>isFunctionKey,
    "isKey",
    ()=>isKey,
    "isPrintableChar",
    ()=>isPrintableChar
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/browser.mjs [client] (ecmascript)");
;
;
const KEY_CODES = {
    ALT: 18,
    ARROW_DOWN: 40,
    ARROW_LEFT: 37,
    ARROW_RIGHT: 39,
    ARROW_UP: 38,
    AUDIO_DOWN: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isFirefox"])() ? 182 : 174,
    AUDIO_MUTE: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isFirefox"])() ? 181 : 173,
    AUDIO_UP: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isFirefox"])() ? 183 : 175,
    BACKSPACE: 8,
    CAPS_LOCK: 20,
    COMMA: 188,
    COMMAND_LEFT: 91,
    COMMAND_RIGHT: 93,
    COMMAND_FIREFOX: 224,
    CONTROL: 17,
    DELETE: 46,
    END: 35,
    ENTER: 13,
    ESCAPE: 27,
    F1: 112,
    F2: 113,
    F3: 114,
    F4: 115,
    F5: 116,
    F6: 117,
    F7: 118,
    F8: 119,
    F9: 120,
    F10: 121,
    F11: 122,
    F12: 123,
    F13: 124,
    F14: 125,
    F15: 126,
    F16: 127,
    F17: 128,
    F18: 129,
    F19: 130,
    HOME: 36,
    INSERT: 45,
    MEDIA_NEXT: 176,
    MEDIA_PLAY_PAUSE: 179,
    MEDIA_PREV: 177,
    MEDIA_STOP: 178,
    NULL: 0,
    NUM_LOCK: 144,
    PAGE_DOWN: 34,
    PAGE_UP: 33,
    PAUSE: 19,
    PERIOD: 190,
    SCROLL_LOCK: 145,
    SHIFT: 16,
    SPACE: 32,
    TAB: 9,
    A: 65,
    C: 67,
    D: 68,
    F: 70,
    L: 76,
    O: 79,
    P: 80,
    S: 83,
    V: 86,
    X: 88,
    Y: 89,
    Z: 90
};
const FUNCTION_KEYS = [
    KEY_CODES.ALT,
    KEY_CODES.ARROW_DOWN,
    KEY_CODES.ARROW_LEFT,
    KEY_CODES.ARROW_RIGHT,
    KEY_CODES.ARROW_UP,
    KEY_CODES.AUDIO_DOWN,
    KEY_CODES.AUDIO_MUTE,
    KEY_CODES.AUDIO_UP,
    KEY_CODES.BACKSPACE,
    KEY_CODES.CAPS_LOCK,
    KEY_CODES.DELETE,
    KEY_CODES.END,
    KEY_CODES.ENTER,
    KEY_CODES.ESCAPE,
    KEY_CODES.F1,
    KEY_CODES.F2,
    KEY_CODES.F3,
    KEY_CODES.F4,
    KEY_CODES.F5,
    KEY_CODES.F6,
    KEY_CODES.F7,
    KEY_CODES.F8,
    KEY_CODES.F9,
    KEY_CODES.F10,
    KEY_CODES.F11,
    KEY_CODES.F12,
    KEY_CODES.F13,
    KEY_CODES.F14,
    KEY_CODES.F15,
    KEY_CODES.F16,
    KEY_CODES.F17,
    KEY_CODES.F18,
    KEY_CODES.F19,
    KEY_CODES.HOME,
    KEY_CODES.INSERT,
    KEY_CODES.MEDIA_NEXT,
    KEY_CODES.MEDIA_PLAY_PAUSE,
    KEY_CODES.MEDIA_PREV,
    KEY_CODES.MEDIA_STOP,
    KEY_CODES.NULL,
    KEY_CODES.NUM_LOCK,
    KEY_CODES.PAGE_DOWN,
    KEY_CODES.PAGE_UP,
    KEY_CODES.PAUSE,
    KEY_CODES.SCROLL_LOCK,
    KEY_CODES.SHIFT,
    KEY_CODES.TAB
];
function isPrintableChar(keyCode) {
    return keyCode === 32 || // space
    keyCode >= 48 && keyCode <= 57 || // 0-9
    keyCode >= 96 && keyCode <= 111 || // numpad
    keyCode >= 186 && keyCode <= 192 || // ;=,-./`
    keyCode >= 219 && keyCode <= 222 || // []{}\|"'
    keyCode >= 226 || // special chars (229 for Asian chars)
    keyCode >= 65 && keyCode <= 90; // a-z
}
function isFunctionKey(keyCode) {
    return FUNCTION_KEYS.includes(keyCode);
}
function isCtrlKey(keyCode) {
    const keys = [];
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isMacOS"])()) {
        keys.push(KEY_CODES.COMMAND_LEFT, KEY_CODES.COMMAND_RIGHT, KEY_CODES.COMMAND_FIREFOX);
    } else {
        keys.push(KEY_CODES.CONTROL);
    }
    return keys.includes(keyCode);
}
function isCtrlMetaKey(keyCode) {
    return [
        KEY_CODES.CONTROL,
        KEY_CODES.COMMAND_LEFT,
        KEY_CODES.COMMAND_RIGHT,
        KEY_CODES.COMMAND_FIREFOX
    ].includes(keyCode);
}
function isKey(keyCode, baseCode) {
    const keys = baseCode.split('|');
    let result = false;
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(keys, (key)=>{
        if (keyCode === KEY_CODES[key]) {
            result = true;
            return false;
        }
    });
    return result;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
]);

//# sourceMappingURL=node_modules_handsontable_helpers_bea1d5fb.async.js.map