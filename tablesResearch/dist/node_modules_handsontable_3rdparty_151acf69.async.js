(globalThis["TURBOPACK"] || (globalThis["TURBOPACK"] = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/handsontable/3rdparty/SheetClip/SheetClip.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "parse",
    ()=>parse,
    "stringify",
    ()=>stringify
]);
/**
 * SheetClip - Spreadsheet Clipboard Parser.
 * version 0.2
 *
 * This tiny library transforms JavaScript arrays to strings that are pasteable by LibreOffice, OpenOffice,
 * Google Docs and Microsoft Excel.
 *
 * Copyright 2012, Marcin Warpechowski
 * Licensed under the MIT license.
 * http://github.com/warpech/sheetclip/
 */ const regUniversalNewLine = /^(\r\n|\n\r|\r|\n)/;
const regNextCellNoQuotes = /^[^\t\r\n]+/;
const regNextEmptyCell = /^\t/;
/**
 * Locate the structural closing `"` for a quoted cell starting at `str[0]`. A structural close is a
 * `"` that is followed by a separator (`\t`, `\r`, `\n`) or end-of-string and is not part of an
 * escaped pair `""`.
 *
 * @param {string} str Remaining input where `str[0]` is the first char after the opening `"`.
 * @returns {number} Index of the structural closing `"` within `str`, or -1 when not found.
 */ function findQuoteCloseIndex(str) {
    let i = 0;
    while(i < str.length){
        if (str[i] === '"') {
            if (str[i + 1] === '"') {
                i += 2;
                continue;
            }
            const next = str[i + 1];
            if (next === undefined || next === '\t' || next === '\r' || next === '\n') {
                return i;
            }
        }
        i += 1;
    }
    return -1;
}
function parse(str) {
    const arr = [
        [
            ''
        ]
    ];
    if (str.length === 0) {
        return arr;
    }
    let column = 0;
    let row = 0;
    let lastLength;
    while(str.length > 0){
        if (lastLength === str.length) {
            break;
        }
        lastLength = str.length;
        if (str.match(regNextEmptyCell)) {
            str = str.replace(regNextEmptyCell, '');
            column += 1;
            arr[row][column] = '';
        } else if (str.match(regUniversalNewLine)) {
            str = str.replace(regUniversalNewLine, '');
            column = 0;
            row += 1;
            arr[row] = [
                ''
            ];
        } else {
            let nextCell = '';
            if (str.startsWith('"')) {
                const closeIndex = findQuoteCloseIndex(str.slice(1));
                if (closeIndex !== -1) {
                    // Properly terminated quoted cell: read content up to the structural close, decoding
                    // escaped `""` to literal `"` and preserving any bare `"` chars that appear mid-cell.
                    let i = 0;
                    const content = str.slice(1);
                    while(i < closeIndex){
                        if (content[i] === '"' && content[i + 1] === '"') {
                            nextCell += '"';
                            i += 2;
                        } else {
                            nextCell += content[i];
                            i += 1;
                        }
                    }
                    str = str.slice(closeIndex + 2);
                } else {
                    // Fallback for malformed input without a structural closing quote: keep the legacy
                    // behavior of consuming quotes by parity and halving runs of `"` chars.
                    let quoteNo = 0;
                    let isStillCell = true;
                    while(isStillCell){
                        const nextChar = str.slice(0, 1);
                        if (nextChar === '"') {
                            quoteNo += 1;
                        }
                        nextCell += nextChar;
                        str = str.slice(1);
                        if (str.length === 0 || str.match(/^[\t\r\n]/) && quoteNo % 2 === 0) {
                            isStillCell = false;
                        }
                    }
                    nextCell = nextCell.replace(/^"/, '').replace(/"$/, '').replace(/["]*/g, (match)=>new Array(Math.floor(match.length / 2)).fill('"').join(''));
                }
            } else {
                const matchedText = str.match(regNextCellNoQuotes);
                nextCell = matchedText ? matchedText[0] : '';
                str = str.slice(nextCell.length);
            }
            arr[row][column] = nextCell;
        }
    }
    return arr;
}
function stringify(arr) {
    let r;
    let rLen;
    let c;
    let cLen;
    let str = '';
    let val;
    for(r = 0, rLen = arr.length; r < rLen; r += 1){
        cLen = arr[r].length;
        for(c = 0; c < cLen; c += 1){
            if (c > 0) {
                str += '\t';
            }
            val = arr[r][c];
            if (typeof val === 'string') {
                if (val.indexOf('\n') > -1) {
                    str += `"${val.replaceAll('"', '""')}"`;
                } else {
                    str += val;
                }
            } else if (val === null || val === undefined) {
                str += '';
            } else {
                str += val;
            }
        }
        if (r !== rLen - 1) {
            str += '\n';
        }
    }
    return str;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/axisCalculation.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Shared axis calculation used by both row and column viewport calculators.
 *
 * Iterates through items (rows or columns) starting from the cache-derived
 * skip position, fills `startPositions`, and delegates to the calculation
 * types via the `_process` / `_initialize` / `_finalize` callbacks on the
 * provided context.
 *
 * @param {object} context The viewport calculator instance (rows or columns).
 * @param {object} params Parameters that differ between the row and column axes.
 * @param {number} params.totalCount Total number of items.
 * @param {number} params.zeroBasedScrollOffset Clamped scroll offset (>= 0).
 * @param {number} params.scrollEnd Pixel threshold at which iteration stops
 *   (rows: `innerViewportHeight`, columns: `zeroBasedScrollOffset + viewportWidth`).
 * @param {PositionCache} params.positionCache Built prefix sum cache.
 * @param {Function} params.setSizeField Callback `(ctx, size) => {}` that writes the
 *   per-item size onto the context (e.g. `ctx.rowHeight = size`).
 * @param {Function} params.setTotalCalculated Callback `(ctx, value) => {}` that writes
 *   the running cumulative total onto the context.
 * @param {Function} params.getTotalCalculated Callback `(ctx) => number` that reads the
 *   running cumulative total from the context.
 */ __turbopack_context__.s([
    "calculateAxis",
    ()=>calculateAxis
]);
function calculateAxis(context, { totalCount, zeroBasedScrollOffset, scrollEnd, positionCache, setSizeField, setTotalCalculated, getTotalCalculated }) {
    context._initialize(context);
    let startIndex = 0;
    if (zeroBasedScrollOffset > 0 && totalCount > 0) {
        const cachedIndex = positionCache.findIndexAtOffset(zeroBasedScrollOffset);
        startIndex = cachedIndex;
        setTotalCalculated(context, positionCache.getOffset(startIndex));
    }
    let lastProcessedIndex = -1;
    for(let i = startIndex; i < totalCount; i++){
        const size = positionCache.getSizeAt(i);
        setSizeField(context, size);
        context._process(i, context);
        lastProcessedIndex = i;
        setTotalCalculated(context, getTotalCalculated(context) + size);
        if (getTotalCalculated(context) >= scrollEnd) {
            context.needReverse = false;
            break;
        }
    }
    context.lastProcessedIndex = lastProcessedIndex;
    context._finalize(context);
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/fullyVisibleColumns.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @class FullyVisibleColumnsCalculationType
 */ __turbopack_context__.s([
    "FullyVisibleColumnsCalculationType",
    ()=>FullyVisibleColumnsCalculationType
]);
class FullyVisibleColumnsCalculationType {
    /**
   * Initializes the calculation.
   */ initialize() {}
    /**
   * Processes the column.
   *
   * @param {number} column The column index.
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */ process(column, viewportCalculator) {
        const { totalCalculatedWidth, zeroBasedScrollOffset, viewportWidth, columnWidth } = viewportCalculator;
        const compensatedViewportWidth = zeroBasedScrollOffset > 0 ? viewportWidth + 1 : viewportWidth;
        if (totalCalculatedWidth >= zeroBasedScrollOffset && totalCalculatedWidth + columnWidth <= zeroBasedScrollOffset + compensatedViewportWidth) {
            if (this.startColumn === null || this.startColumn === undefined) {
                this.startColumn = column;
            }
            this.endColumn = column;
        }
    }
    /**
   * Finalizes the calculation.
   *
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */ finalize(viewportCalculator) {
        const { scrollOffset, viewportWidth, inlineStartOffset, zeroBasedScrollOffset, totalColumns, needReverse, positionCache, columnWidth } = viewportCalculator;
        if (!positionCache) {
            return;
        }
        // If the estimation has reached the last column and there is still some space available in the viewport,
        // we need to render in reverse in order to fill the whole viewport with columns
        if (this.endColumn === totalColumns - 1 && needReverse) {
            this.startColumn = this.endColumn;
            while(this.startColumn > 0){
                const calculatedViewportHeight = positionCache.getOffset(this.endColumn) + columnWidth - positionCache.getOffset(this.startColumn - 1);
                if (calculatedViewportHeight <= viewportWidth) {
                    this.startColumn -= 1;
                }
                if (calculatedViewportHeight >= viewportWidth) {
                    break;
                }
            }
        }
        this.startPosition = this.startColumn !== null ? positionCache.getOffset(this.startColumn) : null;
        const compensatedViewportWidth = zeroBasedScrollOffset > 0 ? viewportWidth + 1 : viewportWidth;
        const mostRightScrollOffset = scrollOffset + viewportWidth - compensatedViewportWidth;
        const inlineStartColumnOffset = this.startColumn === null ? 0 : viewportCalculator.getColumnWidth(this.startColumn);
        if (mostRightScrollOffset < -1 * inlineStartOffset || scrollOffset > positionCache.getOffset(viewportCalculator.lastProcessedIndex) || // the table is to the right of the viewport
        -1 * scrollOffset - viewportWidth > -1 * inlineStartColumnOffset) {
            this.isVisibleInTrimmingContainer = false;
        } else {
            this.isVisibleInTrimmingContainer = true;
        }
        if (this.endColumn !== null && totalColumns < this.endColumn) {
            this.endColumn = totalColumns - 1;
        }
        if (this.startColumn !== null && this.endColumn !== null) {
            this.count = this.endColumn - this.startColumn + 1;
        }
    }
    constructor(){
        /**
   * Total number of fully visible columns in the viewport.
   *
   * @type {number}
   */ this.count = 0;
        /**
   * The column index of the first fully visible column in the viewport.
   *
   * @type {number|null}
   */ this.startColumn = null;
        /**
   * The column index of the last fully visible column in the viewport.
   *
   * @type {number|null}
   */ this.endColumn = null;
        /**
   * Position of the first fully visible column (in px).
   *
   * @type {number|null}
   */ this.startPosition = null;
        /**
   * Determines if the viewport is visible in the trimming container.
   *
   * @type {boolean}
   */ this.isVisibleInTrimmingContainer = false;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/fullyVisibleRows.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @class FullyVisibleRowsCalculationType
 */ __turbopack_context__.s([
    "FullyVisibleRowsCalculationType",
    ()=>FullyVisibleRowsCalculationType
]);
class FullyVisibleRowsCalculationType {
    /**
   * Initializes the calculation.
   */ initialize() {}
    /**
   * Processes the row.
   *
   * @param {number} row The row index.
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */ process(row, viewportCalculator) {
        const { totalCalculatedHeight, zeroBasedScrollOffset, innerViewportHeight, rowHeight } = viewportCalculator;
        if (totalCalculatedHeight >= zeroBasedScrollOffset && totalCalculatedHeight + rowHeight <= innerViewportHeight) {
            if (this.startRow === null) {
                this.startRow = row;
            }
            this.endRow = row;
        }
    }
    /**
   * Finalizes the calculation.
   *
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */ finalize(viewportCalculator) {
        const { scrollOffset, viewportHeight, horizontalScrollbarHeight, totalRows, needReverse, positionCache, rowHeight } = viewportCalculator;
        if (!positionCache) {
            return;
        }
        // If the estimation has reached the last row and there is still some space available in the viewport,
        // we need to render in reverse in order to fill the whole viewport with rows
        if (this.endRow === totalRows - 1 && needReverse) {
            this.startRow = this.endRow;
            while(this.startRow > 0){
                const calculatedViewportHeight = positionCache.getOffset(this.endRow) + rowHeight - positionCache.getOffset(this.startRow - 1);
                if (calculatedViewportHeight <= viewportHeight - horizontalScrollbarHeight) {
                    this.startRow -= 1;
                }
                if (calculatedViewportHeight >= viewportHeight - horizontalScrollbarHeight) {
                    break;
                }
            }
        }
        this.startPosition = this.startRow !== null ? positionCache.getOffset(this.startRow) : null;
        const mostBottomScrollOffset = scrollOffset + viewportHeight - horizontalScrollbarHeight;
        const topRowOffset = this.startRow === null ? 0 : viewportCalculator.getRowHeight(this.startRow);
        if (mostBottomScrollOffset < topRowOffset || scrollOffset > positionCache.getOffset(viewportCalculator.lastProcessedIndex)) {
            this.isVisibleInTrimmingContainer = false;
        } else {
            this.isVisibleInTrimmingContainer = true;
        }
        if (this.endRow !== null && totalRows < this.endRow) {
            this.endRow = totalRows - 1;
        }
        if (this.startRow !== null && this.endRow !== null) {
            this.count = this.endRow - this.startRow + 1;
        }
    }
    constructor(){
        /**
   * Total number of fully visible rows in the viewport.
   *
   * @type {number}
   */ this.count = 0;
        /**
   * The row index of the first fully visible row in the viewport.
   *
   * @type {number|null}
   */ this.startRow = null;
        /**
   * The row index of the last fully visible row in the viewport.
   *
   * @type {number|null}
   */ this.endRow = null;
        /**
   * Position of the first fully visible row (in px).
   *
   * @type {number|null}
   */ this.startPosition = null;
        /**
   * Determines if the viewport is visible in the trimming container.
   *
   * @type {boolean}
   */ this.isVisibleInTrimmingContainer = false;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/partiallyVisibleColumns.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @class PartiallyVisibleColumnsCalculationType
 */ __turbopack_context__.s([
    "PartiallyVisibleColumnsCalculationType",
    ()=>PartiallyVisibleColumnsCalculationType
]);
class PartiallyVisibleColumnsCalculationType {
    /**
   * Initializes the calculation.
   */ initialize() {}
    /**
   * Processes the column.
   *
   * @param {number} column The column index.
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */ process(column, viewportCalculator) {
        const { totalCalculatedWidth, zeroBasedScrollOffset, viewportWidth } = viewportCalculator;
        if (totalCalculatedWidth <= zeroBasedScrollOffset) {
            this.startColumn = column;
        }
        const compensatedViewportWidth = zeroBasedScrollOffset > 0 ? viewportWidth + 1 : viewportWidth;
        if (totalCalculatedWidth >= zeroBasedScrollOffset && totalCalculatedWidth <= zeroBasedScrollOffset + compensatedViewportWidth) {
            if (this.startColumn === null || this.startColumn === undefined) {
                this.startColumn = column;
            }
        }
        this.endColumn = column;
    }
    /**
   * Finalizes the calculation.
   *
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */ finalize(viewportCalculator) {
        const { scrollOffset, viewportWidth, inlineStartOffset, zeroBasedScrollOffset, totalColumns, needReverse, positionCache, columnWidth } = viewportCalculator;
        if (!positionCache) {
            return;
        }
        // If the estimation has reached the last column and there is still some space available in the viewport,
        // we need to render in reverse in order to fill the whole viewport with columns
        if (this.endColumn === totalColumns - 1 && needReverse) {
            this.startColumn = this.endColumn;
            while(this.startColumn > 0){
                const calculatedViewportWidth = positionCache.getOffset(this.endColumn) + columnWidth - positionCache.getOffset(this.startColumn - 1);
                this.startColumn -= 1;
                if (calculatedViewportWidth > viewportWidth) {
                    break;
                }
            }
        }
        this.startPosition = this.startColumn !== null ? positionCache.getOffset(this.startColumn) : null;
        const compensatedViewportWidth = zeroBasedScrollOffset > 0 ? viewportWidth + 1 : viewportWidth;
        const mostRightScrollOffset = scrollOffset + viewportWidth - compensatedViewportWidth;
        if (mostRightScrollOffset < -1 * inlineStartOffset || scrollOffset > positionCache.getOffset(viewportCalculator.lastProcessedIndex) + columnWidth || // the table is to the right of the viewport
        -1 * scrollOffset - viewportWidth > 0) {
            this.isVisibleInTrimmingContainer = false;
        } else {
            this.isVisibleInTrimmingContainer = true;
        }
        if (this.endColumn !== null && totalColumns < this.endColumn) {
            this.endColumn = totalColumns - 1;
        }
        if (this.startColumn !== null && this.endColumn !== null) {
            this.count = this.endColumn - this.startColumn + 1;
        }
    }
    constructor(){
        /**
   * Total number of partially visible columns in the viewport.
   *
   * @type {number}
   */ this.count = 0;
        /**
   * The column index of the first partially visible column in the viewport.
   *
   * @type {number|null}
   */ this.startColumn = null;
        /**
   * The column index of the last partially visible column in the viewport.
   *
   * @type {number|null}
   */ this.endColumn = null;
        /**
   * Position of the first partially visible column (in px).
   *
   * @type {number|null}
   */ this.startPosition = null;
        /**
   * Determines if the viewport is visible in the trimming container.
   *
   * @type {boolean}
   */ this.isVisibleInTrimmingContainer = false;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/partiallyVisibleRows.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @class PartiallyVisibleRowsCalculationType
 */ __turbopack_context__.s([
    "PartiallyVisibleRowsCalculationType",
    ()=>PartiallyVisibleRowsCalculationType
]);
class PartiallyVisibleRowsCalculationType {
    /**
   * Initializes the calculation.
   */ initialize() {}
    /**
   * Processes the row.
   *
   * @param {number} row The row index.
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */ process(row, viewportCalculator) {
        const { totalCalculatedHeight, zeroBasedScrollOffset, innerViewportHeight } = viewportCalculator;
        if (totalCalculatedHeight <= zeroBasedScrollOffset) {
            this.startRow = row;
        }
        if (totalCalculatedHeight >= zeroBasedScrollOffset && totalCalculatedHeight <= innerViewportHeight) {
            if (this.startRow === null) {
                this.startRow = row;
            }
        }
        this.endRow = row;
    }
    /**
   * Finalizes the calculation.
   *
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */ finalize(viewportCalculator) {
        const { scrollOffset, viewportHeight, horizontalScrollbarHeight, totalRows, needReverse, positionCache, rowHeight } = viewportCalculator;
        if (!positionCache) {
            return;
        }
        // If the estimation has reached the last row and there is still some space available in the viewport,
        // we need to render in reverse in order to fill the whole viewport with rows
        if (this.endRow === totalRows - 1 && needReverse) {
            this.startRow = this.endRow;
            while(this.startRow > 0){
                const calculatedViewportHeight = positionCache.getOffset(this.endRow) + rowHeight - positionCache.getOffset(this.startRow - 1);
                this.startRow -= 1;
                if (calculatedViewportHeight >= viewportHeight - horizontalScrollbarHeight) {
                    break;
                }
            }
        }
        this.startPosition = this.startRow !== null ? positionCache.getOffset(this.startRow) : null;
        const mostBottomScrollOffset = scrollOffset + viewportHeight - horizontalScrollbarHeight;
        if (mostBottomScrollOffset < 0 || scrollOffset > positionCache.getOffset(viewportCalculator.lastProcessedIndex) + rowHeight) {
            this.isVisibleInTrimmingContainer = false;
        } else {
            this.isVisibleInTrimmingContainer = true;
        }
        if (this.endRow !== null && totalRows < this.endRow) {
            this.endRow = totalRows - 1;
        }
        if (this.startRow !== null && this.endRow !== null) {
            this.count = this.endRow - this.startRow + 1;
        }
    }
    constructor(){
        /**
   * Total number of partially visible rows in the viewport.
   *
   * @type {number}
   */ this.count = 0;
        /**
   * The row index of the first partially visible row in the viewport.
   *
   * @type {number|null}
   */ this.startRow = null;
        /**
   * The row index of the last partially visible row in the viewport.
   *
   * @type {number|null}
   */ this.endRow = null;
        /**
   * Position of the first partially visible row (in px).
   *
   * @type {number|null}
   */ this.startPosition = null;
        /**
   * Determines if the viewport is visible in the trimming container.
   *
   * @type {boolean}
   */ this.isVisibleInTrimmingContainer = false;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/renderedAllColumns.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @typedef {object} RenderedAllColumnsCalculatorOptions
 * @property {number} totalColumns Total number of columns.
 */ /**
 * Holds all calculations needed to perform the rendering of all columns.
 *
 * @class RenderedAllColumnsCalculationType
 */ __turbopack_context__.s([
    "RenderedAllColumnsCalculationType",
    ()=>RenderedAllColumnsCalculationType
]);
class RenderedAllColumnsCalculationType {
    /**
   * Initializes the calculation.
   *
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */ initialize({ totalColumns }) {
        this.count = totalColumns;
        this.endColumn = this.count - 1;
    }
    /**
   * Processes the column.
   */ process() {}
    /**
   * Finalizes the calculation.
   */ finalize() {}
    constructor(){
        /**
   * Number of rendered/visible columns.
   *
   * @type {number}
   */ this.count = 0;
        /**
   * Index of the first rendered/visible column.
   *
   * @type {number}
   */ this.startColumn = 0;
        /**
   * Index of the last rendered/visible column.
   *
   * @type {number}
   */ this.endColumn = 0;
        /**
   * Position of the first rendered/visible column (in px).
   *
   * @type {number}
   */ this.startPosition = 0;
        /**
   * Determines if the viewport is visible in the trimming container.
   *
   * @type {boolean}
   */ this.isVisibleInTrimmingContainer = true;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/renderedAllRows.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @typedef {object} RenderedAllRowsCalculatorOptions
 * @property {number} totalRows Total number of rows.
 */ /**
 * Holds all calculations needed to perform the rendering of all rows.
 *
 * @class RenderedAllRowsCalculationType
 */ __turbopack_context__.s([
    "RenderedAllRowsCalculationType",
    ()=>RenderedAllRowsCalculationType
]);
class RenderedAllRowsCalculationType {
    /**
   * Initializes the calculation.
   *
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */ initialize({ totalRows }) {
        this.count = totalRows;
        this.endRow = this.count - 1;
    }
    /**
   * Processes the row.
   */ process() {}
    /**
   * Finalizes the calculation.
   */ finalize() {}
    constructor(){
        /**
   * Number of rendered/visible rows.
   *
   * @type {number}
   */ this.count = 0;
        /**
   * Index of the first rendered/visible row.
   *
   * @type {number}
   */ this.startRow = 0;
        /**
   * Index of the last rendered/visible row.
   *
   * @type {number}
   */ this.endRow = 0;
        /**
   * Position of the first rendered/visible row (in px).
   *
   * @type {number}
   */ this.startPosition = 0;
        /**
   * Determines if the viewport is visible in the trimming container.
   *
   * @type {boolean}
   */ this.isVisibleInTrimmingContainer = true;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/renderedColumns.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "RenderedColumnsCalculationType",
    ()=>RenderedColumnsCalculationType
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$partiallyVisibleColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/partiallyVisibleColumns.mjs [client] (ecmascript)");
;
class RenderedColumnsCalculationType extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$partiallyVisibleColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["PartiallyVisibleColumnsCalculationType"] {
    /**
   * Finalizes the calculation.
   *
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */ finalize(viewportCalculator) {
        super.finalize(viewportCalculator);
        const { overrideFn, totalColumns, positionCache } = viewportCalculator;
        if (this.startColumn !== null && typeof overrideFn === 'function') {
            const startColumn = this.startColumn;
            const endColumn = this.endColumn;
            overrideFn(this);
            this.columnStartOffset = startColumn - this.startColumn;
            this.columnEndOffset = this.endColumn !== null && endColumn !== null ? this.endColumn - endColumn : 0;
        }
        if (this.startColumn !== null && this.startColumn < 0) {
            this.startColumn = 0;
        }
        this.startPosition = this.startColumn !== null && positionCache ? positionCache.getOffset(this.startColumn) : null;
        if (this.endColumn !== null && totalColumns < this.endColumn) {
            this.endColumn = totalColumns - 1;
        }
        if (this.startColumn !== null && this.endColumn !== null) {
            this.count = this.endColumn - this.startColumn + 1;
        }
    }
    constructor(...args){
        super(...args), /**
   * The property holds the offset applied in the `overrideFn` function to the `startColumn` value.
   *
   * @type {number}
   */ this.columnStartOffset = 0, /**
   * The property holds the offset applied in the `overrideFn` function to the `endColumn` value.
   *
   * @type {number}
   */ this.columnEndOffset = 0;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/renderedRows.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "RenderedRowsCalculationType",
    ()=>RenderedRowsCalculationType
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$partiallyVisibleRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/partiallyVisibleRows.mjs [client] (ecmascript)");
;
class RenderedRowsCalculationType extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$partiallyVisibleRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["PartiallyVisibleRowsCalculationType"] {
    /**
   * Finalizes the calculation.
   *
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */ finalize(viewportCalculator) {
        super.finalize(viewportCalculator);
        const { overrideFn, totalRows, positionCache } = viewportCalculator;
        if (this.startRow !== null && typeof overrideFn === 'function') {
            const startRow = this.startRow;
            const endRow = this.endRow;
            overrideFn(this);
            this.rowStartOffset = startRow - this.startRow;
            this.rowEndOffset = this.endRow !== null && endRow !== null ? this.endRow - endRow : 0;
        }
        if (this.startRow !== null && this.startRow < 0) {
            this.startRow = 0;
        }
        this.startPosition = this.startRow !== null && positionCache ? positionCache.getOffset(this.startRow) : null;
        if (this.endRow !== null && totalRows < this.endRow) {
            this.endRow = totalRows - 1;
        }
        if (this.startRow !== null && this.endRow !== null) {
            this.count = this.endRow - this.startRow + 1;
        }
    }
    constructor(...args){
        super(...args), /**
   * The property holds the offset applied in the `overrideFn` function to the `startColumn` value.
   *
   * @type {number}
   */ this.rowStartOffset = 0, /**
   * The property holds the offset applied in the `overrideFn` function to the `endColumn` value.
   *
   * @type {number}
   */ this.rowEndOffset = 0;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/viewportBase.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @typedef {object} ColumnsCalculationType
 * @property {number | null} startColumn The column index of the first column in the viewport.
 * @property {number | null} endColumn The column index of the last column in the viewport.
 * @property {number} count Total number of columns.
 * @property {number | null} startPosition Position of the first fully column (in px).
 * @property {boolean} isVisibleInTrimmingContainer Determines if the viewport is visible in the trimming container.
 */ /**
 * @class ViewportBaseCalculator
 */ __turbopack_context__.s([
    "ViewportBaseCalculator",
    ()=>ViewportBaseCalculator
]);
class ViewportBaseCalculator {
    /**
   * Initializes all calculators (triggers all calculators before calculating the rows/columns sizes).
   *
   * @param {*} context The context object (rows or columns viewport calculator).
   */ _initialize(context) {
        this.calculationTypes.forEach(([id, calculator])=>{
            this.calculationResults.set(id, calculator);
            calculator.initialize(context);
        });
    }
    /**
   * Processes the row/column at the given index.
   *
   * @param {number} index The index of the row/column.
   * @param {*} context The context object (rows or columns viewport calculator).
   */ _process(index, context) {
        this.calculationTypes.forEach(([, calculator])=>calculator.process(index, context));
    }
    /**
   * Finalizes all calculators (triggers all calculators after calculating the rows/columns sizes).
   *
   * @param {*} context The context object (rows or columns viewport calculator).
   */ _finalize(context) {
        this.calculationTypes.forEach(([, calculator])=>calculator.finalize(context));
    }
    /**
   * Gets the results for the given calculator.
   *
   * @param {string} calculatorId The id of the calculator.
   * @returns {ColumnsCalculationType | RowsCalculationType}
   */ getResultsFor(calculatorId) {
        return this.calculationResults.get(calculatorId);
    }
    /**
   * Creates a new ViewportBaseCalculator instance.
   *
   * @param {Array<[string, CalculationTypeLike]>} calculationTypes - The calculation types to be performed.
   */ constructor(calculationTypes){
        /**
   * The calculation types to be performed.
   *
   * @type {Array}
   */ this.calculationTypes = [];
        /**
   * The calculation results.
   *
   * @type {Map<string, ColumnsCalculationType | RowsCalculationType>}
   */ this.calculationResults = new Map();
        this.calculationTypes = calculationTypes;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/viewportColumns.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ViewportColumnsCalculator",
    ()=>ViewportColumnsCalculator
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$axisCalculation$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/axisCalculation.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$viewportBase$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/viewportBase.mjs [client] (ecmascript)");
;
;
class ViewportColumnsCalculator extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$viewportBase$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ViewportBaseCalculator"] {
    /**
   * Calculates viewport.
   */ calculate() {
        if (!this.positionCache) {
            return;
        }
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$axisCalculation$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["calculateAxis"])(this, {
            totalCount: this.totalColumns,
            zeroBasedScrollOffset: this.zeroBasedScrollOffset,
            scrollEnd: this.zeroBasedScrollOffset + this.viewportWidth,
            positionCache: this.positionCache,
            setSizeField: (ctx, size)=>{
                ctx.columnWidth = size;
            },
            setTotalCalculated: (ctx, v)=>{
                ctx.totalCalculatedWidth = v;
            },
            getTotalCalculated: (ctx)=>ctx.totalCalculatedWidth
        });
    }
    /**
   * @param {string} calculatorId The id of the calculator.
   * @returns {ColumnsCalculationType | undefined}
   */ getResultsFor(calculatorId) {
        return this.calculationResults.get(calculatorId);
    }
    /**
   * Gets the column width at the specified column index.
   *
   * @param {number} column Column index.
   * @returns {number}
   */ getColumnWidth(column) {
        return this.positionCache?.getSizeAt(column) ?? 0;
    }
    /**
   * @param {ViewportColumnsCalculatorOptions} options Object with all options specified for column viewport calculation.
   */ constructor({ calculationTypes, viewportWidth, scrollOffset, totalColumns, overrideFn, inlineStartOffset, columnWidthCache }){
        super(calculationTypes), /**
   * @type {number}
   */ this.viewportWidth = 0, /**
   * @type {number}
   */ this.scrollOffset = 0, /**
   * @type {number}
   */ this.zeroBasedScrollOffset = 0, /**
   * @type {number}
   */ this.totalColumns = 0, /**
   * @type {number}
   */ this.columnWidth = 0, /**
   * @type {((calc: unknown) => void) | null}
   */ this.overrideFn = null, /**
   * @type {number}
   */ this.inlineStartOffset = 0, /**
   * @type {number}
   */ this.totalCalculatedWidth = 0, /**
   * @type {boolean}
   */ this.needReverse = true, /**
   * @type {PositionCache | null}
   */ this.positionCache = null, /**
   * @type {number}
   */ this.lastProcessedIndex = -1;
        this.viewportWidth = viewportWidth;
        this.scrollOffset = scrollOffset;
        this.zeroBasedScrollOffset = Math.max(scrollOffset, 0);
        this.totalColumns = totalColumns;
        this.overrideFn = overrideFn;
        this.inlineStartOffset = inlineStartOffset;
        this.positionCache = columnWidthCache;
        this.calculate();
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/viewportRows.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ViewportRowsCalculator",
    ()=>ViewportRowsCalculator
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$axisCalculation$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/axisCalculation.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$viewportBase$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/viewportBase.mjs [client] (ecmascript)");
;
;
class ViewportRowsCalculator extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$viewportBase$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ViewportBaseCalculator"] {
    /**
   * Calculates viewport.
   */ calculate() {
        if (!this.positionCache) {
            return;
        }
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$axisCalculation$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["calculateAxis"])(this, {
            totalCount: this.totalRows,
            zeroBasedScrollOffset: this.zeroBasedScrollOffset,
            scrollEnd: this.innerViewportHeight,
            positionCache: this.positionCache,
            setSizeField: (ctx, size)=>{
                ctx.rowHeight = size;
            },
            setTotalCalculated: (ctx, v)=>{
                ctx.totalCalculatedHeight = v;
            },
            getTotalCalculated: (ctx)=>ctx.totalCalculatedHeight
        });
    }
    /**
   * @param {string} calculatorId The id of the calculator.
   * @returns {RowsCalculationType | undefined}
   */ getResultsFor(calculatorId) {
        return this.calculationResults.get(calculatorId);
    }
    /**
   * Gets the row height at the specified row index.
   *
   * @param {number} row Row index.
   * @returns {number}
   */ getRowHeight(row) {
        return this.positionCache?.getSizeAt(row) ?? 0;
    }
    /**
   * @param {ViewportRowsCalculatorOptions} options Object with all options specified for row viewport calculation.
   */ constructor({ calculationTypes, viewportHeight, scrollOffset, totalRows, overrideFn, horizontalScrollbarHeight, rowHeightCache }){
        super(calculationTypes), /**
   * @type {number}
   */ this.viewportHeight = 0, /**
   * @type {number}
   */ this.scrollOffset = 0, /**
   * @type {number}
   */ this.zeroBasedScrollOffset = 0, /**
   * @type {number}
   */ this.totalRows = 0, /**
   * @type {number}
   */ this.rowHeight = 0, /**
   * @type {((calc: unknown) => void) | null}
   */ this.overrideFn = null, /**
   * @type {number}
   */ this.horizontalScrollbarHeight = 0, /**
   * @type {number}
   */ this.innerViewportHeight = 0, /**
   * @type {number}
   */ this.totalCalculatedHeight = 0, /**
   * @type {boolean}
   */ this.needReverse = true, /**
   * @type {PositionCache | null}
   */ this.positionCache = null, /**
   * @type {number}
   */ this.lastProcessedIndex = -1;
        this.viewportHeight = viewportHeight;
        this.scrollOffset = scrollOffset;
        this.zeroBasedScrollOffset = Math.max(scrollOffset, 0);
        this.totalRows = totalRows;
        this.overrideFn = overrideFn;
        this.horizontalScrollbarHeight = horizontalScrollbarHeight ?? 0;
        this.innerViewportHeight = this.zeroBasedScrollOffset + this.viewportHeight - this.horizontalScrollbarHeight;
        this.positionCache = rowHeightCache;
        this.calculate();
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/coords.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
var /**
   * A flag which determines if the coordinates run in RTL mode.
   *
   * @type {boolean}
   */ _isRtl = /*#__PURE__*/ new WeakMap();
/**
 * @description
 *
 * The `CellCoords` class holds the coordinates (`row`, `col`) of a single cell.
 *
 * It also contains methods for validating the coordinates
 * and retrieving them as an object.
 *
 * To import the `CellCoords` class:
 *
 * ```js
 * import Handsontable, { CellCoords } from '/handsontable';
 *
 * // or, using modules
 * import Handsontable, { CellCoords } from '/handsontable/base';
 * ```
 */ class CellCoords {
    /**
   * Checks if the coordinates in your `CellCoords` instance are valid
   * in the context of given table parameters.
   *
   * The `row` index:
   * - Must be an integer.
   * - Must be higher than the number of column headers in the table.
   * - Must be lower than the total number of rows in the table.
   *
   * The `col` index:
   * - Must be an integer.
   * - Must be higher than the number of row headers in the table.
   * - Must be lower than the total number of columns in the table.
   *
   * @param {object} [tableParams] An object with a defined table size.
   * @param {number} [tableParams.countRows=0] The total number of rows.
   * @param {number} [tableParams.countCols=0] The total number of columns.
   * @param {number} [tableParams.countRowHeaders=0] A number of row headers.
   * @param {number} [tableParams.countColHeaders=0] A number of column headers.
   * @returns {boolean} `true`: The coordinates are valid.
   */ isValid(tableParams) {
        const { countRows, countCols, countRowHeaders, countColHeaders } = {
            countRows: 0,
            countCols: 0,
            countRowHeaders: 0,
            countColHeaders: 0,
            ...tableParams
        };
        if (!Number.isInteger(this.row) || !Number.isInteger(this.col)) {
            return false;
        }
        const row = this.row;
        const col = this.col;
        if (row < -countColHeaders || col < -countRowHeaders) {
            return false;
        }
        if (row >= countRows || col >= countCols) {
            return false;
        }
        return true;
    }
    /**
   * Checks whether both row and col coordinates are set (not null).
   *
   * @returns {boolean}
   */ isSet() {
        return this.row !== null && this.col !== null;
    }
    /**
   * Checks if another set of coordinates (`coords`)
   * is equal to the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} coords Coordinates to check.
   * @returns {boolean}
   */ isEqual(coords) {
        if (coords === this) {
            return true;
        }
        return this.row === coords.row && this.col === coords.col;
    }
    /**
   * Checks if the coordinates point to the headers range. If one of the axis (row or col) point to
   * the header (negative value) then method returns `true`.
   *
   * @returns {boolean}
   */ isHeader() {
        return !this.isCell();
    }
    /**
   * Checks if the coordinates point to the cells range. If all axis (row and col) point to
   * the cell (positive value) then method returns `true`.
   *
   * @returns {boolean}
   */ isCell() {
        return this.row !== null && this.col !== null && this.row >= 0 && this.col >= 0;
    }
    /**
   * Checks if the coordinates runs in RTL mode.
   *
   * @returns {boolean}
   */ isRtl() {
        return _class_private_field_get(this, _isRtl);
    }
    /**
   * Checks if another set of coordinates (`testedCoords`)
   * is south-east of the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} testedCoords Coordinates to check.
   * @returns {boolean}
   */ isSouthEastOf(testedCoords) {
        if (this.row === null || this.col === null || testedCoords.row === null || testedCoords.col === null) {
            return false;
        }
        const row = this.row;
        const col = this.col;
        const testedRow = testedCoords.row;
        const testedCol = testedCoords.col;
        return row >= testedRow && (_class_private_field_get(this, _isRtl) ? col <= testedCol : col >= testedCol);
    }
    /**
   * Checks if another set of coordinates (`testedCoords`)
   * is north-west of the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} testedCoords Coordinates to check.
   * @returns {boolean}
   */ isNorthWestOf(testedCoords) {
        if (this.row === null || this.col === null || testedCoords.row === null || testedCoords.col === null) {
            return false;
        }
        const row = this.row;
        const col = this.col;
        const testedRow = testedCoords.row;
        const testedCol = testedCoords.col;
        return row <= testedRow && (_class_private_field_get(this, _isRtl) ? col >= testedCol : col <= testedCol);
    }
    /**
   * Checks if another set of coordinates (`testedCoords`)
   * is south-west of the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} testedCoords Coordinates to check.
   * @returns {boolean}
   */ isSouthWestOf(testedCoords) {
        if (this.row === null || this.col === null || testedCoords.row === null || testedCoords.col === null) {
            return false;
        }
        const row = this.row;
        const col = this.col;
        const testedRow = testedCoords.row;
        const testedCol = testedCoords.col;
        return row >= testedRow && (_class_private_field_get(this, _isRtl) ? col >= testedCol : col <= testedCol);
    }
    /**
   * Checks if another set of coordinates (`testedCoords`)
   * is north-east of the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} testedCoords Coordinates to check.
   * @returns {boolean}
   */ isNorthEastOf(testedCoords) {
        if (this.row === null || this.col === null || testedCoords.row === null || testedCoords.col === null) {
            return false;
        }
        const row = this.row;
        const col = this.col;
        const testedRow = testedCoords.row;
        const testedCol = testedCoords.col;
        return row <= testedRow && (_class_private_field_get(this, _isRtl) ? col <= testedCol : col >= testedCol);
    }
    /**
   * Normalizes the coordinates in your `CellCoords` instance to the nearest valid position.
   *
   * Coordinates that point to headers (negative values) are normalized to `0`.
   *
   * @returns {CellCoords}
   */ normalize() {
        this.row = this.row === null ? this.row : Math.max(this.row, 0);
        this.col = this.col === null ? this.col : Math.max(this.col, 0);
        return this;
    }
    /**
   * Assigns the coordinates from another `CellCoords` instance (or compatible literal object)
   * to your `CellCoords` instance.
   *
   * @param {CellCoords | { row: number | undefined, col: number | undefined }} coords The CellCoords
   * instance or compatible literal object.
   * @returns {CellCoords}
   */ assign(coords) {
        if (Number.isInteger(coords?.row)) {
            this.row = coords.row;
        }
        if (Number.isInteger(coords?.col)) {
            this.col = coords.col;
        }
        if (coords instanceof CellCoords) {
            _class_private_field_set(this, _isRtl, coords.isRtl());
        }
        return this;
    }
    /**
   * Clones your `CellCoords` instance.
   *
   * @returns {CellCoords}
   */ clone() {
        return new CellCoords(this.row ?? undefined, this.col ?? undefined, _class_private_field_get(this, _isRtl));
    }
    /**
   * Converts your `CellCoords` instance into an object literal with `row` and `col` properties.
   *
   * @returns {{row: number, col: number}} An object literal with `row` and `col` properties.
   */ toObject() {
        return {
            row: this.row,
            col: this.col
        };
    }
    /**
   * Creates a new CellCoords instance.
   *
   * @param {number} [row] A visual row index.
   * @param {number} [column] A visual column index.
   * @param {boolean} [isRtl=false] A flag which determines if the coordinates run in RTL mode.
   */ constructor(row, column, isRtl = false){
        _class_private_field_init(this, _isRtl, {
            writable: true,
            value: void 0
        });
        /**
   * A visual row index.
   *
   * @type {number}
   */ this.row = null;
        /**
   * A visual column index.
   *
   * @type {number}
   */ this.col = null;
        _class_private_field_set(this, _isRtl, false);
        _class_private_field_set(this, _isRtl, isRtl);
        if (typeof row !== 'undefined') {
            this.row = row;
        }
        if (typeof column !== 'undefined') {
            this.col = column;
        }
    }
}
var __TURBOPACK__default__export__ = CellCoords;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/coords.mjs [client] (ecmascript) <export default as CellCoords>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "CellCoords",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$coords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/coords.mjs [client] (ecmascript)"))["default"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$coords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/coords.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/range.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$coords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/coords.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
var /**
   * @type {boolean}
   */ _isRtl = /*#__PURE__*/ new WeakMap(), /**
   * Returns a numeric coordinate value, treating null as 0.
   *
   * @private
   * @param {number | null} v The coordinate value.
   * @returns {number}
   */ _n = /*#__PURE__*/ new WeakSet();
/**
 * @description
 *
 * The `CellRange` class holds a set of cell coordinates ([`CellCoords`](@/api/cellCoords.md) instances)
 * that form a [selection range](@/guides/cell-features/selection/selection.md#select-ranges).
 *
 * A single `CellRange` instance represents a single unit of selection
 * that contains either a single cell or multiple adjacent cells.
 *
 * To import the `CellRange` class:
 *
 * ```js
 * import Handsontable, { CellRange } from '/handsontable';
 *
 * // or, using modules
 * import Handsontable, { CellRange } from '/handsontable/base';
 * ```
 */ class CellRange {
    /**
   * Highlights cell selection at the `coords` coordinates.
   *
   * @param {CellCoords} coords Coordinates to use.
   * @returns {CellRange}
   */ setHighlight(coords) {
        this.highlight = coords.clone();
        return this;
    }
    /**
   * Sets the `coords` coordinates as the start of your range.
   *
   * @param {CellCoords} coords Coordinates to use.
   * @returns {CellRange}
   */ setFrom(coords) {
        this.from = coords.clone();
        return this;
    }
    /**
   * Sets the `coords` coordinates as the end of your range.
   *
   * @param {CellCoords} coords Coordinates to use.
   * @returns {CellRange}
   */ setTo(coords) {
        this.to = coords.clone();
        return this;
    }
    /**
   * Normalizes the coordinates in your `CellRange` instance to the nearest valid position.
   *
   * Coordinates that point to headers (negative values) are normalized to `0`.
   *
   * @returns {CellRange}
   */ normalize() {
        this.highlight.normalize();
        this.from.normalize();
        this.to.normalize();
        return this;
    }
    /**
   * Checks if the coordinates in your `CellRange` instance are valid
   * in the context of given table parameters.
   *
   * See the [`isValid()`](@/api/cellCoords.md#isvalid) method of the [`CellCoords`](@/api/cellCoords.md) class.
   *
   * @param {object} tableParams An object with a defined table size.
   * @param {number} tableParams.countRows The total number of rows.
   * @param {number} tableParams.countCols The total number of columns.
   * @param {number} tableParams.countRowHeaders A number of row headers.
   * @param {number} tableParams.countColHeaders A number of column headers.
   * @returns {boolean}
   */ isValid(tableParams) {
        return this.from.isValid(tableParams) && this.to.isValid(tableParams);
    }
    /**
   * Checks if your range is just a single cell or header.
   *
   * @returns {boolean}
   */ isSingle() {
        return this.isSingleCell() || this.isSingleHeader();
    }
    /**
   * Checks if your range is just a single cell.
   *
   * @returns {boolean}
   */ isSingleCell() {
        if (this.from.row === null || this.from.col === null || this.to.row === null || this.to.col === null) {
            return false;
        }
        return _class_private_method_get(this, _n, n).call(this, this.from.row) >= 0 && this.from.row === this.to.row && _class_private_method_get(this, _n, n).call(this, this.from.col) >= 0 && this.from.col === this.to.col;
    }
    /**
   * Checks if your range is just a single header.
   *
   * @returns {boolean}
   */ isSingleHeader() {
        if (this.from.row === null || this.from.col === null || this.to.row === null || this.to.col === null) {
            return false;
        }
        return (_class_private_method_get(this, _n, n).call(this, this.from.row) < 0 || _class_private_method_get(this, _n, n).call(this, this.from.col) < 0) && this.from.row === this.to.row && this.from.col === this.to.col;
    }
    /**
   * Checks if your range covers only headers range (negative coordinates, without any cells).
   *
   * @returns {boolean}
   */ isHeader() {
        if (this.from.isHeader() && this.to.isHeader()) {
            return true;
        }
        return _class_private_method_get(this, _n, n).call(this, this.from.col) < 0 && _class_private_method_get(this, _n, n).call(this, this.to.col) < 0 || _class_private_method_get(this, _n, n).call(this, this.from.row) < 0 && _class_private_method_get(this, _n, n).call(this, this.to.row) < 0;
    }
    /**
   * Checks if your range overlaps headers range (negative coordinates).
   *
   * @returns {boolean}
   */ containsHeaders() {
        return this.from.isHeader() || this.to.isHeader();
    }
    /**
   * Returns the height of your range (as a number of rows, including row headers).
   *
   * @returns {number}
   */ getOuterHeight() {
        return Math.max(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)) - Math.min(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)) + 1;
    }
    /**
   * Returns the width of your range (as a number of columns, including column headers).
   *
   * @returns {number}
   */ getOuterWidth() {
        return Math.max(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col)) - Math.min(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col)) + 1;
    }
    /**
   * Returns the height of your range (as a number of rows, excluding row headers).
   *
   * @returns {number}
   */ getHeight() {
        // if the selection contains only row headers, return 0
        if (_class_private_method_get(this, _n, n).call(this, this.from.row) < 0 && _class_private_method_get(this, _n, n).call(this, this.to.row) < 0) {
            return 0;
        }
        const fromRow = Math.max(_class_private_method_get(this, _n, n).call(this, this.from.row), 0);
        const toRow = Math.max(_class_private_method_get(this, _n, n).call(this, this.to.row), 0);
        return Math.max(fromRow, toRow) - Math.min(fromRow, toRow) + 1;
    }
    /**
   * Returns the width of your range (as a number of columns, excluding column headers).
   *
   * @returns {number}
   */ getWidth() {
        // if the selection contains only column headers, return 0
        if (_class_private_method_get(this, _n, n).call(this, this.from.col) < 0 && _class_private_method_get(this, _n, n).call(this, this.to.col) < 0) {
            return 0;
        }
        const fromCol = Math.max(_class_private_method_get(this, _n, n).call(this, this.from.col), 0);
        const toCol = Math.max(_class_private_method_get(this, _n, n).call(this, this.to.col), 0);
        return Math.max(fromCol, toCol) - Math.min(fromCol, toCol) + 1;
    }
    /**
   * Returns the number of cells within your range (excluding column and row headers).
   *
   * @returns {number}
   */ getCellsCount() {
        return this.getWidth() * this.getHeight();
    }
    /**
   * Checks if another set of coordinates (`cellCoords`)
   * is within the `from` and `to` coordinates of your range.
   *
   * @param {CellCoords} cellCoords Coordinates to check.
   * @returns {boolean}
   */ includes(cellCoords) {
        if (this.from.row === null || this.from.col === null || this.to.row === null || this.to.col === null || cellCoords.row === null || cellCoords.col === null) {
            return false;
        }
        const row = _class_private_method_get(this, _n, n).call(this, cellCoords.row);
        const col = _class_private_method_get(this, _n, n).call(this, cellCoords.col);
        const topStart = this.getOuterTopStartCorner();
        const bottomEnd = this.getOuterBottomEndCorner();
        return _class_private_method_get(this, _n, n).call(this, topStart.row) <= row && _class_private_method_get(this, _n, n).call(this, bottomEnd.row) >= row && _class_private_method_get(this, _n, n).call(this, topStart.col) <= col && _class_private_method_get(this, _n, n).call(this, bottomEnd.col) >= col;
    }
    /**
   * Checks if another range (`cellRange`) is within your range.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */ includesRange(cellRange) {
        return this.includes(cellRange.getOuterTopStartCorner()) && this.includes(cellRange.getOuterBottomEndCorner());
    }
    /**
   * Checks if another range (`cellRange`) is equal to your range.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */ isEqual(cellRange) {
        if (this.from.row === null || this.from.col === null || this.to.row === null || this.to.col === null || cellRange.from.row === null || cellRange.from.col === null || cellRange.to.row === null || cellRange.to.col === null) {
            return false;
        }
        const fromRowMin = Math.min(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row));
        const fromRowMax = Math.max(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row));
        const fromColMin = Math.min(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col));
        const fromColMax = Math.max(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col));
        const toRowMin = Math.min(_class_private_method_get(this, _n, n).call(this, cellRange.from.row), _class_private_method_get(this, _n, n).call(this, cellRange.to.row));
        const toRowMax = Math.max(_class_private_method_get(this, _n, n).call(this, cellRange.from.row), _class_private_method_get(this, _n, n).call(this, cellRange.to.row));
        const toColMin = Math.min(_class_private_method_get(this, _n, n).call(this, cellRange.from.col), _class_private_method_get(this, _n, n).call(this, cellRange.to.col));
        const toColMax = Math.max(_class_private_method_get(this, _n, n).call(this, cellRange.from.col), _class_private_method_get(this, _n, n).call(this, cellRange.to.col));
        return fromRowMin === toRowMin && fromRowMax === toRowMax && fromColMin === toColMin && fromColMax === toColMax;
    }
    /**
   * Checks if another range (`cellRange`) overlaps your range.
   *
   * Range A overlaps range B if the intersection of A and B (or B and A) is not empty.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */ overlaps(cellRange) {
        return cellRange.isSouthEastOf(this.getOuterTopLeftCorner()) && cellRange.isNorthWestOf(this.getOuterBottomRightCorner());
    }
    /**
   * Checks if coordinates point is south-east of your range.
   *
   * @param {CellCoords} cellCoords Coordinates to check.
   * @returns {boolean}
   */ isSouthEastOf(cellCoords) {
        return this.getOuterTopLeftCorner().isSouthEastOf(cellCoords) || this.getOuterBottomRightCorner().isSouthEastOf(cellCoords);
    }
    /**
   * Checks if coordinates point is north-west of your range.
   *
   * @param {CellRange} cellCoords Coordinates to check.
   * @returns {boolean}
   */ isNorthWestOf(cellCoords) {
        return this.getOuterTopLeftCorner().isNorthWestOf(cellCoords) || this.getOuterBottomRightCorner().isNorthWestOf(cellCoords);
    }
    /**
   * Checks if another range (`cellRange`) overlaps your range horizontally.
   *
   * For example: returns `true` if the last column of your range is `5`
   * and the first column of the `cellRange` range is `3`.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */ isOverlappingHorizontally(cellRange) {
        return _class_private_method_get(this, _n, n).call(this, this.getOuterTopEndCorner().col) >= _class_private_method_get(this, _n, n).call(this, cellRange.getOuterTopStartCorner().col) && _class_private_method_get(this, _n, n).call(this, this.getOuterTopEndCorner().col) <= _class_private_method_get(this, _n, n).call(this, cellRange.getOuterTopEndCorner().col) || _class_private_method_get(this, _n, n).call(this, this.getOuterTopStartCorner().col) <= _class_private_method_get(this, _n, n).call(this, cellRange.getOuterTopEndCorner().col) && _class_private_method_get(this, _n, n).call(this, this.getOuterTopStartCorner().col) >= _class_private_method_get(this, _n, n).call(this, cellRange.getOuterTopStartCorner().col);
    }
    /**
   * Checks if another range (`cellRange`) overlaps your range vertically.
   *
   * For example: returns `true` if the last row of your range is `5`
   * and the first row of the `cellRange` range is `3`.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */ isOverlappingVertically(cellRange) {
        return _class_private_method_get(this, _n, n).call(this, this.getOuterBottomStartCorner().row) >= _class_private_method_get(this, _n, n).call(this, cellRange.getOuterTopRightCorner().row) && _class_private_method_get(this, _n, n).call(this, this.getOuterBottomStartCorner().row) <= _class_private_method_get(this, _n, n).call(this, cellRange.getOuterBottomStartCorner().row) || _class_private_method_get(this, _n, n).call(this, this.getOuterTopEndCorner().row) <= _class_private_method_get(this, _n, n).call(this, cellRange.getOuterBottomStartCorner().row) && _class_private_method_get(this, _n, n).call(this, this.getOuterTopEndCorner().row) >= _class_private_method_get(this, _n, n).call(this, cellRange.getOuterTopRightCorner().row);
    }
    /**
   * Adds a cell to your range, at `cellCoords` coordinates.
   *
   * The `cellCoords` coordinates must exceed a corner of your range.
   *
   * @param {CellCoords} cellCoords A new cell's coordinates.
   * @param {boolean} [changeDirection=true] If `true`, the direction of your range is changed to the direction
   * of the `cellCoords` coordinates.
   * @returns {boolean}
   */ expand(cellCoords, changeDirection = true) {
        const topStart = this.getOuterTopStartCorner();
        const bottomEnd = this.getOuterBottomEndCorner();
        if (_class_private_method_get(this, _n, n).call(this, cellCoords.row) < _class_private_method_get(this, _n, n).call(this, topStart.row) || _class_private_method_get(this, _n, n).call(this, cellCoords.col) < _class_private_method_get(this, _n, n).call(this, topStart.col) || _class_private_method_get(this, _n, n).call(this, cellCoords.row) > _class_private_method_get(this, _n, n).call(this, bottomEnd.row) || _class_private_method_get(this, _n, n).call(this, cellCoords.col) > _class_private_method_get(this, _n, n).call(this, bottomEnd.col)) {
            const verticalDirection = this.getVerticalDirection();
            const horizontalDirection = this.getHorizontalDirection();
            this.from = this._createCellCoords(Math.min(_class_private_method_get(this, _n, n).call(this, topStart.row), _class_private_method_get(this, _n, n).call(this, cellCoords.row)), Math.min(_class_private_method_get(this, _n, n).call(this, topStart.col), _class_private_method_get(this, _n, n).call(this, cellCoords.col)));
            this.to = this._createCellCoords(Math.max(_class_private_method_get(this, _n, n).call(this, bottomEnd.row), _class_private_method_get(this, _n, n).call(this, cellCoords.row)), Math.max(_class_private_method_get(this, _n, n).call(this, bottomEnd.col), _class_private_method_get(this, _n, n).call(this, cellCoords.col)));
            if (changeDirection) {
                if (_class_private_method_get(this, _n, n).call(this, cellCoords.row) < _class_private_method_get(this, _n, n).call(this, topStart.row) && verticalDirection === 'N-S') {
                    this.flipDirectionVertically();
                } else if (_class_private_method_get(this, _n, n).call(this, cellCoords.row) > _class_private_method_get(this, _n, n).call(this, bottomEnd.row) && verticalDirection === 'S-N') {
                    this.flipDirectionVertically();
                }
                if (_class_private_method_get(this, _n, n).call(this, cellCoords.col) < _class_private_method_get(this, _n, n).call(this, topStart.col) && horizontalDirection === 'W-E') {
                    this.flipDirectionHorizontally();
                } else if (_class_private_method_get(this, _n, n).call(this, cellCoords.col) > _class_private_method_get(this, _n, n).call(this, bottomEnd.col) && horizontalDirection === 'E-W') {
                    this.flipDirectionHorizontally();
                }
            }
            return true;
        }
        return false;
    }
    /**
   * Expand your range with another range (`expandingRange`).
   *
   * @param {CellRange} expandingRange A new range.
   * @param {boolean} [changeDirection=true] If `true`, the direction of your range is changed to the direction
   * of the `expandingRange` range.
   * @returns {boolean}
   */ expandByRange(expandingRange, changeDirection = true) {
        if (this.includesRange(expandingRange) || !this.overlaps(expandingRange)) {
            return false;
        }
        const topStart = this.getOuterTopStartCorner();
        const bottomEnd = this.getOuterBottomEndCorner();
        const initialDirection = this.getDirection();
        const expandingTopStart = expandingRange.getOuterTopStartCorner();
        const expandingBottomEnd = expandingRange.getOuterBottomEndCorner();
        const resultTopRow = Math.min(_class_private_method_get(this, _n, n).call(this, topStart.row), _class_private_method_get(this, _n, n).call(this, expandingTopStart.row));
        const resultTopCol = Math.min(_class_private_method_get(this, _n, n).call(this, topStart.col), _class_private_method_get(this, _n, n).call(this, expandingTopStart.col));
        const resultBottomRow = Math.max(_class_private_method_get(this, _n, n).call(this, bottomEnd.row), _class_private_method_get(this, _n, n).call(this, expandingBottomEnd.row));
        const resultBottomCol = Math.max(_class_private_method_get(this, _n, n).call(this, bottomEnd.col), _class_private_method_get(this, _n, n).call(this, expandingBottomEnd.col));
        const finalFrom = this._createCellCoords(resultTopRow, resultTopCol);
        const finalTo = this._createCellCoords(resultBottomRow, resultBottomCol);
        this.from = finalFrom;
        this.to = finalTo;
        this.setDirection(initialDirection);
        if (changeDirection) {
            if (this.highlight.row === this.getOuterBottomRightCorner().row && this.getVerticalDirection() === 'N-S') {
                this.flipDirectionVertically();
            }
            if (this.highlight.col === this.getOuterTopRightCorner().col && this.getHorizontalDirection() === 'W-E') {
                this.flipDirectionHorizontally();
            }
        }
        return true;
    }
    /**
   * Gets the direction of the selection.
   *
   * @returns {string} Returns one of the values: `'NW-SE'`, `'NE-SW'`, `'SE-NW'`, `'SW-NE'`.
   */ getDirection() {
        if (this.from.isNorthWestOf(this.to)) {
            return 'NW-SE';
        } else if (this.from.isNorthEastOf(this.to)) {
            return 'NE-SW';
        } else if (this.from.isSouthEastOf(this.to)) {
            return 'SE-NW';
        } else if (this.from.isSouthWestOf(this.to)) {
            return 'SW-NE';
        }
        return 'NW-SE';
    }
    /**
   * Sets the direction of the selection.
   *
   * @param {string} direction One of the values: `'NW-SE'`, `'NE-SW'`, `'SE-NW'`, `'SW-NE'`.
   */ setDirection(direction) {
        switch(direction){
            case 'NW-SE':
                [this.from, this.to] = [
                    this.getOuterTopLeftCorner(),
                    this.getOuterBottomRightCorner()
                ];
                break;
            case 'NE-SW':
                [this.from, this.to] = [
                    this.getOuterTopRightCorner(),
                    this.getOuterBottomLeftCorner()
                ];
                break;
            case 'SE-NW':
                [this.from, this.to] = [
                    this.getOuterBottomRightCorner(),
                    this.getOuterTopLeftCorner()
                ];
                break;
            case 'SW-NE':
                [this.from, this.to] = [
                    this.getOuterBottomLeftCorner(),
                    this.getOuterTopRightCorner()
                ];
                break;
            default:
                break;
        }
    }
    /**
   * Gets the vertical direction of the selection.
   *
   * @returns {string} Returns one of the values: `N-S` (north->south), `S-N` (south->north).
   */ getVerticalDirection() {
        return [
            'NE-SW',
            'NW-SE'
        ].indexOf(this.getDirection()) > -1 ? 'N-S' : 'S-N';
    }
    /**
   * Gets the horizontal direction of the selection.
   *
   * @returns {string} Returns one of the values: `W-E` (west->east), `E-W` (east->west).
   */ getHorizontalDirection() {
        return [
            'NW-SE',
            'SW-NE'
        ].indexOf(this.getDirection()) > -1 ? 'W-E' : 'E-W';
    }
    /**
   * Gets the inline (horizontal) direction of the selection.
   *
   * @returns {string} Returns one of the values: `start-end`, `end-start`.
   */ getInlineDirection() {
        return this.getHorizontalDirection() === (_class_private_field_get(this, _isRtl) ? 'E-W' : 'W-E') ? 'start-end' : 'end-start';
    }
    /**
   * Flips the direction of your range vertically (e.g., `NW-SE` changes to `SW-NE`).
   */ flipDirectionVertically() {
        const direction = this.getDirection();
        switch(direction){
            case 'NW-SE':
                this.setDirection('SW-NE');
                break;
            case 'NE-SW':
                this.setDirection('SE-NW');
                break;
            case 'SE-NW':
                this.setDirection('NE-SW');
                break;
            case 'SW-NE':
                this.setDirection('NW-SE');
                break;
            default:
                break;
        }
    }
    /**
   * Flips the direction of your range horizontally (e.g., `NW-SE` changes to `NE-SW`).
   */ flipDirectionHorizontally() {
        const direction = this.getDirection();
        switch(direction){
            case 'NW-SE':
                this.setDirection('NE-SW');
                break;
            case 'NE-SW':
                this.setDirection('NW-SE');
                break;
            case 'SE-NW':
                this.setDirection('SW-NE');
                break;
            case 'SW-NE':
                this.setDirection('SE-NW');
                break;
            default:
                break;
        }
    }
    /**
   * Gets the top-left (in LTR) or top-right (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */ getTopStartCorner() {
        return this._createCellCoords(Math.min(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)), Math.min(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col))).normalize();
    }
    /**
   * Gets the top-left corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */ getTopLeftCorner() {
        return _class_private_field_get(this, _isRtl) ? this.getTopEndCorner() : this.getTopStartCorner();
    }
    /**
   * Gets the bottom right (in LTR) or bottom left (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */ getBottomEndCorner() {
        return this._createCellCoords(Math.max(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)), Math.max(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col))).normalize();
    }
    /**
   * Gets the bottom right corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */ getBottomRightCorner() {
        return _class_private_field_get(this, _isRtl) ? this.getBottomStartCorner() : this.getBottomEndCorner();
    }
    /**
   * Gets the top right (in LTR) or top left (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */ getTopEndCorner() {
        return this._createCellCoords(Math.min(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)), Math.max(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col))).normalize();
    }
    /**
   * Gets the top right corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */ getTopRightCorner() {
        return _class_private_field_get(this, _isRtl) ? this.getTopStartCorner() : this.getTopEndCorner();
    }
    /**
   * Gets the bottom left (in LTR) or bottom right (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */ getBottomStartCorner() {
        return this._createCellCoords(Math.max(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)), Math.min(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col))).normalize();
    }
    /**
   * Gets the bottom left corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */ getBottomLeftCorner() {
        return _class_private_field_get(this, _isRtl) ? this.getBottomEndCorner() : this.getBottomStartCorner();
    }
    /**
   * Gets the top left (in LTR) or top right (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the top and start coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */ getOuterTopStartCorner() {
        return this._createCellCoords(Math.min(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)), Math.min(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col)));
    }
    /**
   * Gets the top left corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the top and left coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */ getOuterTopLeftCorner() {
        return _class_private_field_get(this, _isRtl) ? this.getOuterTopEndCorner() : this.getOuterTopStartCorner();
    }
    /**
   * Gets the bottom right (in LTR) or bottom left (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the top and start coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */ getOuterBottomEndCorner() {
        return this._createCellCoords(Math.max(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)), Math.max(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col)));
    }
    /**
   * Gets the bottom right corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the top and left coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */ getOuterBottomRightCorner() {
        return _class_private_field_get(this, _isRtl) ? this.getOuterBottomStartCorner() : this.getOuterBottomEndCorner();
    }
    /**
   * Gets the top right (in LTR) or top left (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the top and start coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */ getOuterTopEndCorner() {
        return this._createCellCoords(Math.min(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)), Math.max(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col)));
    }
    /**
   * Gets the top right corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the top and left coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */ getOuterTopRightCorner() {
        return _class_private_field_get(this, _isRtl) ? this.getOuterTopStartCorner() : this.getOuterTopEndCorner();
    }
    /**
   * Gets the bottom left (in LTR) or bottom right (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the top and start coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */ getOuterBottomStartCorner() {
        return this._createCellCoords(Math.max(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)), Math.min(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col)));
    }
    /**
   * Gets the bottom left corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the top and left coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */ getOuterBottomLeftCorner() {
        return _class_private_field_get(this, _isRtl) ? this.getOuterBottomEndCorner() : this.getOuterBottomStartCorner();
    }
    /**
   * Checks if a set of coordinates (`coords`) matches one of the 4 corners of your range.
   *
   * @param {CellCoords} coords Coordinates to check.
   * @returns {boolean}
   */ isCorner(coords) {
        return coords.isEqual(this.getOuterTopLeftCorner()) || coords.isEqual(this.getOuterTopRightCorner()) || coords.isEqual(this.getOuterBottomLeftCorner()) || coords.isEqual(this.getOuterBottomRightCorner());
    }
    /**
   * Gets the coordinates of a range corner opposite to the provided `coords`.
   *
   * For example: if the `coords` coordinates match the bottom-right corner of your range,
   * the coordinates of the top-left corner of your range are returned.
   *
   * @param {CellCoords} coords Coordinates to check.
   * @returns {CellCoords}
   */ getOppositeCorner(coords) {
        if (!(coords instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$coords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"])) {
            return undefined;
        }
        if (coords.isEqual(this.getOuterBottomEndCorner())) {
            return this.getOuterTopStartCorner();
        } else if (coords.isEqual(this.getOuterTopStartCorner())) {
            return this.getOuterBottomEndCorner();
        } else if (coords.isEqual(this.getOuterTopEndCorner())) {
            return this.getOuterBottomStartCorner();
        } else if (coords.isEqual(this.getOuterBottomStartCorner())) {
            return this.getOuterTopEndCorner();
        }
        return undefined;
    }
    /**
   * Indicates which borders (top, right, bottom, left) are shared between
   * your `CellRange`instance and another `range` that's within your range.
   *
   * @param {CellRange} range A range to compare with.
   * @returns {Array<'top' | 'right' | 'bottom' | 'left'>}
   */ getBordersSharedWith(range) {
        if (!this.includesRange(range)) {
            return [];
        }
        const thisBorders = {
            top: Math.min(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)),
            bottom: Math.max(_class_private_method_get(this, _n, n).call(this, this.from.row), _class_private_method_get(this, _n, n).call(this, this.to.row)),
            left: Math.min(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col)),
            right: Math.max(_class_private_method_get(this, _n, n).call(this, this.from.col), _class_private_method_get(this, _n, n).call(this, this.to.col))
        };
        const rangeBorders = {
            top: Math.min(_class_private_method_get(this, _n, n).call(this, range.from.row), _class_private_method_get(this, _n, n).call(this, range.to.row)),
            bottom: Math.max(_class_private_method_get(this, _n, n).call(this, range.from.row), _class_private_method_get(this, _n, n).call(this, range.to.row)),
            left: Math.min(_class_private_method_get(this, _n, n).call(this, range.from.col), _class_private_method_get(this, _n, n).call(this, range.to.col)),
            right: Math.max(_class_private_method_get(this, _n, n).call(this, range.from.col), _class_private_method_get(this, _n, n).call(this, range.to.col))
        };
        const result = [];
        if (thisBorders.top === rangeBorders.top) {
            result.push('top');
        }
        if (thisBorders.right === rangeBorders.right) {
            result.push(_class_private_field_get(this, _isRtl) ? 'left' : 'right');
        }
        if (thisBorders.bottom === rangeBorders.bottom) {
            result.push('bottom');
        }
        if (thisBorders.left === rangeBorders.left) {
            result.push(_class_private_field_get(this, _isRtl) ? 'right' : 'left');
        }
        return result;
    }
    /**
   * Gets the coordinates of the inner cells of your range.
   *
   * @returns {CellCoords[]}
   */ getInner() {
        const topStart = this.getOuterTopStartCorner();
        const bottomEnd = this.getOuterBottomEndCorner();
        const out = [];
        if (topStart.row === null || topStart.col === null || bottomEnd.row === null || bottomEnd.col === null) {
            return out;
        }
        const tsRow = _class_private_method_get(this, _n, n).call(this, topStart.row);
        const tsCol = _class_private_method_get(this, _n, n).call(this, topStart.col);
        const beRow = _class_private_method_get(this, _n, n).call(this, bottomEnd.row);
        const beCol = _class_private_method_get(this, _n, n).call(this, bottomEnd.col);
        for(let r = tsRow; r <= beRow; r++){
            for(let c = tsCol; c <= beCol; c++){
                if (!(this.from.row === r && this.from.col === c) && !(this.to.row === r && this.to.col === c)) {
                    out.push(this._createCellCoords(r, c));
                }
            }
        }
        return out;
    }
    /**
   * Gets the coordinates of all cells of your range.
   *
   * @returns {CellCoords[]}
   */ getAll() {
        const topStart = this.getOuterTopStartCorner();
        const bottomEnd = this.getOuterBottomEndCorner();
        const out = [];
        if (topStart.row === null || topStart.col === null || bottomEnd.row === null || bottomEnd.col === null) {
            return out;
        }
        const tsRow = _class_private_method_get(this, _n, n).call(this, topStart.row);
        const tsCol = _class_private_method_get(this, _n, n).call(this, topStart.col);
        const beRow = _class_private_method_get(this, _n, n).call(this, bottomEnd.row);
        const beCol = _class_private_method_get(this, _n, n).call(this, bottomEnd.col);
        for(let r = tsRow; r <= beRow; r++){
            for(let c = tsCol; c <= beCol; c++){
                if (tsRow === r && tsCol === c) {
                    out.push(topStart);
                } else if (beRow === r && beCol === c) {
                    out.push(bottomEnd);
                } else {
                    out.push(this._createCellCoords(r, c));
                }
            }
        }
        return out;
    }
    /**
   * Runs a callback function on all cells within your range.
   *
   * You can break the iteration by returning `false` in the callback function.
   *
   * @param {function(number, number): boolean} callback A callback function.
   */ forAll(callback) {
        const topStart = this.getOuterTopStartCorner();
        const bottomEnd = this.getOuterBottomEndCorner();
        if (topStart.row === null || topStart.col === null || bottomEnd.row === null || bottomEnd.col === null) {
            return;
        }
        const tsRow = _class_private_method_get(this, _n, n).call(this, topStart.row);
        const tsCol = _class_private_method_get(this, _n, n).call(this, topStart.col);
        const beRow = _class_private_method_get(this, _n, n).call(this, bottomEnd.row);
        const beCol = _class_private_method_get(this, _n, n).call(this, bottomEnd.col);
        for(let r = tsRow; r <= beRow; r++){
            for(let c = tsCol; c <= beCol; c++){
                const breakIteration = callback(r, c);
                if (breakIteration === false) {
                    return;
                }
            }
        }
    }
    /**
   * Clones your `CellRange` instance.
   *
   * @returns {CellRange}
   */ clone() {
        return new CellRange(this.highlight, this.from, this.to, _class_private_field_get(this, _isRtl));
    }
    /**
   * Converts your `CellRange` instance into an object literal with the following properties:
   *
   * - `from`
   *    - `row`
   *    - `col`
   * - `to`
   *    - `row`
   *    - `col`
   *
   * @returns {{from: {row: number, col: number}, to: {row: number, col: number}}} An object literal with `from` and `to` properties.
   */ toObject() {
        return {
            from: this.from.toObject(),
            to: this.to.toObject()
        };
    }
    /**
   * Creates and returns a new instance of the `CellCoords` class.
   *
   * The new `CellCoords` instance automatically inherits the LTR/RTL flag
   * from your `CellRange` instance.
   *
   * @private
   * @param {number | null} row A row index.
   * @param {number | null} column A column index.
   * @returns {CellCoords}
   */ _createCellCoords(row, column) {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$coords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](row ?? undefined, column ?? undefined, _class_private_field_get(this, _isRtl));
    }
    /**
   * Creates a new CellRange instance.
   *
   * @param {CellCoords} highlight The cell where selection was started.
   * @param {CellCoords} from The start of the range.
   * @param {CellCoords} to The end of the range.
   * @param {boolean} isRtl A flag indicating whether to use right-to-left (RTL) layout.
   */ constructor(highlight, from = highlight, to = highlight, isRtl = false){
        _class_private_method_init(this, _n);
        _class_private_field_init(this, _isRtl, {
            writable: true,
            value: void 0
        });
        _class_private_field_set(this, _isRtl, false);
        this.highlight = highlight.clone();
        this.from = from.clone();
        this.to = to.clone();
        _class_private_field_set(this, _isRtl, isRtl);
    }
}
function n(v) {
    return v ?? 0;
}
var __TURBOPACK__default__export__ = CellRange;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/range.mjs [client] (ecmascript) <export default as CellRange>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "CellRange",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$range$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/range.mjs [client] (ecmascript)"))["default"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$range$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/range.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/constants.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "DEFAULT_COLUMN_WIDTH",
    ()=>DEFAULT_COLUMN_WIDTH
]);
const DEFAULT_COLUMN_WIDTH = 50;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/core/_base.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>CoreAbstract
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$string$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/string.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$eventManager$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/eventManager.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$scroll$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/scroll.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$coords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/coords.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$range$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/range.mjs [client] (ecmascript)");
;
;
;
;
;
;
class CoreAbstract {
    /**
   * Gets or creates the event manager instance.
   *
   * @returns {EventManager} The event manager instance.
   */ get eventManager() {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$eventManager$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this);
    }
    /**
   * Gets the root document from dom bindings.
   *
   * @returns {Document} The root document.
   */ get rootDocument() {
        return this.domBindings.rootDocument;
    }
    /**
   * Gets the root window from dom bindings.
   *
   * @returns {Window} The root window.
   */ get rootWindow() {
        return this.domBindings.rootWindow;
    }
    /**
   * Delegates to wtSettings.getSetting.
   *
   * @param {string} key The settings key.
   * @param {...unknown} args Additional arguments.
   * @returns {unknown}
   */ getSetting(key, ...args) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        return this.wtSettings.getSetting(key, ...args);
    }
    /**
   * Delegates to wtSettings.update.
   *
   * @param {string} key The settings key.
   * @param {unknown} value The value to set.
   */ update(key, value) {
        this.wtSettings.update(key, value);
    }
    /**
   * Finds and stores original table headers, setting up column header rendering callbacks if not already configured.
   */ findOriginalHeaders() {
        const originalHeaders = [];
        // find original headers
        if (this.wtTable.THEAD && this.wtTable.THEAD.childNodes.length && this.wtTable.THEAD.childNodes[0].childNodes.length) {
            for(let c = 0, clen = this.wtTable.THEAD.childNodes[0].childNodes.length; c < clen; c++){
                originalHeaders.push(this.wtTable.THEAD.childNodes[0].childNodes[c].innerHTML);
            }
            if (!this.wtSettings.getSetting('columnHeaders').length) {
                this.wtSettings.update('columnHeaders', [
                    function(column, TH) {
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["fastInnerText"])(TH, originalHeaders[column]);
                    }
                ]);
            }
        }
    }
    /**
   * Creates and returns the CellCoords object.
   *
   * @param {*} row The row index.
   * @param {*} column The column index.
   * @returns {CellCoords}
   */ createCellCoords(row, column) {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$coords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](row, column, this.wtSettings.getSetting('rtlMode'));
    }
    /**
   * Creates and returns the CellRange object.
   *
   * @param {CellCoords} highlight The highlight coordinates.
   * @param {CellCoords} from The from coordinates.
   * @param {CellCoords} to The to coordinates.
   * @returns {CellRange}
   */ createCellRange(highlight, from, to) {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$range$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](highlight, from, to, this.wtSettings.getSetting('rtlMode'));
    }
    /**
   * Force rerender of Walkontable.
   *
   * @param {boolean} [fastDraw=false] When `true`, try to refresh only the positions of borders without rerendering
   *                                   the data. It will only work if Table.draw() does not force
   *                                   rendering anyway.
   * @returns {Walkontable}
   */ draw(fastDraw = false) {
        this.drawInterrupted = false;
        if (!this.wtTable.isVisible() || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasZeroHeight"])(this.wtTable.wtRootElement.parentNode)) {
            // draw interrupted because TABLE is not visible or has the height set to 0
            this.drawInterrupted = true;
        } else {
            this.wtTable.draw(fastDraw);
        }
        return this;
    }
    /**
   * Returns the TD at coords. If topmost is set to true, returns TD from the topmost overlay layer,
   * if not set or set to false, returns TD from the master table.
   *
   * @param {CellCoords} coords The cell coordinates.
   * @param {boolean} [topmost=false] If set to `true`, it returns the TD element from the topmost overlay. For example,
   *                                  if the wanted cell is in the range of fixed rows, it will return a TD element
   *                                  from the top overlay.
   * @returns {HTMLElement}
   */ getCell(coords, topmost = false) {
        if (coords.row === null || coords.col === null) {
            return undefined;
        }
        if (!topmost) {
            return this.wtTable.getCell(coords);
        }
        const totalRows = this.wtSettings.getSetting('totalRows');
        const fixedRowsTop = this.wtSettings.getSetting('fixedRowsTop');
        const fixedRowsBottom = this.wtSettings.getSetting('fixedRowsBottom');
        const fixedColumnsStart = this.wtSettings.getSetting('fixedColumnsStart');
        if (coords.row < fixedRowsTop && coords.col < fixedColumnsStart) {
            return this.wtOverlays.topInlineStartCornerOverlay.clone?.wtTable.getCell(coords);
        } else if (coords.row < fixedRowsTop) {
            return this.wtOverlays.topOverlay.clone?.wtTable.getCell(coords);
        } else if (coords.col < fixedColumnsStart && coords.row >= totalRows - fixedRowsBottom) {
            return this.wtOverlays.bottomInlineStartCornerOverlay?.clone?.wtTable.getCell(coords);
        } else if (coords.col < fixedColumnsStart) {
            return this.wtOverlays.inlineStartOverlay.clone?.wtTable.getCell(coords);
        } else if (coords.row < totalRows && coords.row >= totalRows - fixedRowsBottom) {
            return this.wtOverlays.bottomOverlay?.clone?.wtTable.getCell(coords);
        }
        return this.wtTable.getCell(coords);
    }
    /**
   * Scrolls the viewport to a cell (rerenders if needed).
   *
   * @param {CellCoords} coords The cell coordinates to scroll to.
   * @param {'auto' | 'start' | 'end'} [horizontalSnap='auto'] If `'start'`, viewport is scrolled to show
   * the cell on the left of the table. If `'end'`, viewport is scrolled to show the cell on the right of
   * the table. When `'auto'`, the viewport is scrolled only when the column is outside of the viewport.
   * @param {'auto' | 'top' | 'bottom'} [verticalSnap='auto'] If `'top'`, viewport is scrolled to show
   * the cell on the top of the table. If `'bottom'`, viewport is scrolled to show the cell on the bottom of
   * the table. When `'auto'`, the viewport is scrolled only when the row is outside of the viewport.
   * @returns {boolean}
   */ scrollViewport(coords, horizontalSnap, verticalSnap) {
        if (coords.row === null || coords.col === null) {
            return false;
        }
        return this.wtScroll.scrollViewport(coords, horizontalSnap, verticalSnap);
    }
    /**
   * Scrolls the viewport to a column (rerenders if needed).
   *
   * @param {number} column Visual column index.
   * @param {'auto' | 'start' | 'end'} [snapping='auto'] If `'start'`, viewport is scrolled to show
   * the cell on the left of the table. If `'end'`, viewport is scrolled to show the cell on the right of
   * the table. When `'auto'`, the viewport is scrolled only when the column is outside of the viewport.
   * @returns {boolean}
   */ scrollViewportHorizontally(column, snapping) {
        return this.wtScroll.scrollViewportHorizontally(column, snapping);
    }
    /**
   * Scrolls the viewport to a row (rerenders if needed).
   *
   * @param {number} row Visual row index.
   * @param {'auto' | 'top' | 'bottom'} [snapping='auto'] If `'top'`, viewport is scrolled to show
   * the cell on the top of the table. If `'bottom'`, viewport is scrolled to show the cell on
   * the bottom of the table. When `'auto'`, the viewport is scrolled only when the row is outside of
   * the viewport.
   * @returns {boolean}
   */ scrollViewportVertically(row, snapping) {
        return this.wtScroll.scrollViewportVertically(row, snapping);
    }
    /**
   * @returns {Array}
   */ getViewport() {
        return [
            this.wtTable.getFirstVisibleRow(),
            this.wtTable.getFirstVisibleColumn(),
            this.wtTable.getLastVisibleRow(),
            this.wtTable.getLastVisibleColumn()
        ];
    }
    /**
   * Gets the overlay instance by its name. Returns null in the base implementation.
   *
   * @param {string} _overlayName The overlay name.
   * @returns {object | null}
   */ getOverlayByName(_overlayName) {
        return null;
    }
    /**
   * Exports settings as CSS class names. No-op in the base implementation.
   */ exportSettingsAsClassNames() {
    // no-op; overridden in Walkontable (master table)
    }
    /**
   * Destroy instance.
   */ destroy() {
        this.wtOverlays.destroy();
        this.wtEvent.destroy();
    }
    /**
   * Create data access object for scroll.
   *
   * @protected
   * @returns {ScrollDao}
   */ createScrollDao() {
        const wot = this;
        return {
            get drawn () {
                return wot.drawn; // TODO refactoring: consider about injecting `isDrawn` function : ()=>return wot.drawn. (it'll enables remove dao layer)
            },
            get topOverlay () {
                return wot.wtOverlays.topOverlay; // TODO refactoring: move outside dao, use IOC
            },
            get inlineStartOverlay () {
                return wot.wtOverlays.inlineStartOverlay; // TODO refactoring: move outside dao, use IOC
            },
            get wtTable () {
                return wot.wtTable; // TODO refactoring: move outside dao, use IOC
            },
            get wtViewport () {
                return wot.wtViewport; // TODO refactoring: move outside dao, use IOC
            },
            get wtSettings () {
                return wot.wtSettings;
            },
            get rootWindow () {
                return wot.domBindings.rootWindow; // TODO refactoring: move outside dao
            },
            // TODO refactoring, consider about using injecting wtSettings into scroll (it'll enables remove dao layer)
            get totalRows () {
                return wot.wtSettings.getSetting('totalRows');
            },
            get totalColumns () {
                return wot.wtSettings.getSetting('totalColumns');
            },
            get fixedRowsTop () {
                return wot.wtSettings.getSetting('fixedRowsTop');
            },
            get fixedRowsBottom () {
                return wot.wtSettings.getSetting('fixedRowsBottom');
            },
            get fixedColumnsStart () {
                return wot.wtSettings.getSetting('fixedColumnsStart');
            }
        };
    }
    // TODO refactoring: it will be much better to not use DAO objects. They are needed for now to provide
    // dynamically access to related objects
    /**
   * Create data access object for wtTable.
   *
   * @protected
   * @returns {Record<string, unknown>}
   */ getTableDao() {
        const wot = this;
        return {
            get wot () {
                return wot;
            },
            get parentTableOffset () {
                return wot.cloneSource.wtTable.tableOffset; // TODO rethink: cloneSource exists only in Clone type.
            },
            get cloneSource () {
                return wot.cloneSource; // TODO rethink: cloneSource exists only in Clone type.
            },
            get workspaceWidth () {
                return wot.wtViewport.getWorkspaceWidth();
            },
            get wtViewport () {
                return wot.wtViewport; // TODO refactoring: move outside dao, use IOC
            },
            get wtOverlays () {
                return wot.wtOverlays; // TODO refactoring: move outside dao, use IOC
            },
            get selectionManager () {
                return wot.selectionManager; // TODO refactoring: move outside dao, use IOC
            },
            get drawn () {
                return wot.drawn;
            },
            set drawn (v){
                wot.drawn = v;
            },
            get wtTable () {
                return wot.wtTable; // TODO refactoring: it provides itself
            },
            get startColumnRendered () {
                return wot.wtViewport.columnsRenderCalculator?.startColumn ?? null;
            },
            get startColumnVisible () {
                return wot.wtViewport.columnsVisibleCalculator?.startColumn ?? null;
            },
            get startColumnPartiallyVisible () {
                return wot.wtViewport.columnsPartiallyVisibleCalculator?.startColumn ?? null;
            },
            get endColumnRendered () {
                return wot.wtViewport.columnsRenderCalculator?.endColumn ?? null;
            },
            get endColumnVisible () {
                return wot.wtViewport.columnsVisibleCalculator?.endColumn ?? null;
            },
            get endColumnPartiallyVisible () {
                return wot.wtViewport.columnsPartiallyVisibleCalculator?.endColumn ?? null;
            },
            get countColumnsRendered () {
                return wot.wtViewport.columnsRenderCalculator?.count ?? 0;
            },
            get countColumnsVisible () {
                return wot.wtViewport.columnsVisibleCalculator?.count ?? 0;
            },
            get startRowRendered () {
                return wot.wtViewport.rowsRenderCalculator?.startRow ?? null;
            },
            get startRowVisible () {
                return wot.wtViewport.rowsVisibleCalculator?.startRow ?? null;
            },
            get startRowPartiallyVisible () {
                return wot.wtViewport.rowsPartiallyVisibleCalculator?.startRow ?? null;
            },
            get endRowRendered () {
                return wot.wtViewport.rowsRenderCalculator?.endRow ?? null;
            },
            get endRowVisible () {
                return wot.wtViewport.rowsVisibleCalculator?.endRow ?? null;
            },
            get endRowPartiallyVisible () {
                return wot.wtViewport.rowsPartiallyVisibleCalculator?.endRow ?? null;
            },
            get countRowsRendered () {
                return wot.wtViewport.rowsRenderCalculator?.count ?? 0;
            },
            get countRowsVisible () {
                return wot.wtViewport.rowsVisibleCalculator?.count ?? 0;
            },
            get columnHeaders () {
                return wot.wtSettings.getSetting('columnHeaders');
            },
            get rowHeaders () {
                return wot.wtSettings.getSetting('rowHeaders');
            }
        };
    }
    /**
   * @param {HTMLTableElement} table Main table.
   * @param {Settings} settings The Walkontable settings.
   */ constructor(table, settings){
        /**
   * The walkontable instance id.
   *
   * @public
   * @type {Readonly<string>}
   */ this.guid = `wt_${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$string$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["randomString"])()}`;
        /**
   * Flag indicating whether the current drawing operation was interrupted.
   *
   * @type {boolean}
   */ this.drawInterrupted = false;
        /**
   * Flag indicating whether the table has been drawn.
   *
   * @type {boolean}
   */ this.drawn = false;
        /**
   * The name of the overlay that currently renders the table.
   *
   * @public
   * @type {string}
   */ this.activeOverlayName = 'master';
        this.domBindings = {
            rootTable: table,
            rootDocument: table.ownerDocument,
            rootWindow: table.ownerDocument.defaultView
        };
        this.wtSettings = settings;
        this.wtScroll = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$scroll$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this.createScrollDao());
    }
}
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/core/clone.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>Clone
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$event$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/event.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/core/_base.mjs [client] (ecmascript)");
;
;
class Clone extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * @param {HTMLTableElement} table Main table.
   * @param {SettingsPure|Settings} settings The Walkontable settings.
   * @param {WalkontableCloneOptions} clone Clone data.
   */ constructor(table, settings, clone){
        super(table, settings);
        const facadeGetter = this.wtSettings.getSetting('facade', this);
        this.cloneSource = clone.source;
        this.cloneOverlay = clone.overlay;
        this.wtTable = this.cloneOverlay.createTable(this.getTableDao(), facadeGetter, this.domBindings, this.wtSettings);
        this.wtViewport = clone.viewport;
        this.selectionManager = clone.selectionManager;
        this.wtEvent = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$event$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](facadeGetter, this.domBindings, this.wtSettings, this.eventManager, this.wtTable, this.selectionManager, clone.event);
        this.findOriginalHeaders();
    }
}
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/core/core.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>Walkontable
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$event$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/event.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlays$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlays.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$settings$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/settings.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$master$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/master.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$viewport$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/viewport.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/core/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$manager$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/manager.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
;
;
;
;
;
;
;
;
;
;
class Walkontable extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * Export settings as class names added to the parent element of the table.
   */ exportSettingsAsClassNames() {
        const toExport = {
            rowHeaders: 'htRowHeaders',
            columnHeaders: 'htColumnHeaders'
        };
        const allClassNames = [];
        const newClassNames = [];
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["objectEach"])(toExport, (className, key)=>{
            if (this.wtSettings.getSetting(key).length) {
                newClassNames.push(className);
            }
            allClassNames.push(className);
        });
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(this.wtTable.wtRootElement.parentNode, allClassNames);
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(this.wtTable.wtRootElement.parentNode, newClassNames);
    }
    /**
   * Destroy instance.
   */ destroy() {
        this.wtTable.destroy();
        super.destroy();
    }
    /**
   * Gets the overlay instance by its name.
   *
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'} overlayName The overlay name.
   * @returns {Overlay | null}
   */ getOverlayByName(overlayName) {
        if (!__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TYPES"].includes(overlayName)) {
            return null;
        }
        const camelCaseOverlay = overlayName.replace(/_([a-z])/g, (_match, letter)=>letter.toUpperCase());
        return this.wtOverlays[`${camelCaseOverlay}Overlay`] ?? null;
    }
    /**
   * @returns {Record<string, unknown>}
   */ getViewportDao() {
        return ((instance)=>({
                get wot () {
                    return instance;
                },
                get topOverlayTrimmingContainer () {
                    return instance.wtOverlays.topOverlay.trimmingContainer;
                },
                get inlineStartOverlayTrimmingContainer () {
                    return instance.wtOverlays.inlineStartOverlay.trimmingContainer;
                },
                get topScrollPosition () {
                    return instance.wtOverlays.topOverlay.getScrollPosition();
                },
                get topParentOffset () {
                    return instance.wtOverlays.topOverlay.getTableParentOffset();
                },
                get inlineStartScrollPosition () {
                    return instance.wtOverlays.inlineStartOverlay.getScrollPosition();
                },
                get inlineStartParentOffset () {
                    return instance.wtOverlays.inlineStartOverlay.getTableParentOffset();
                },
                get topOverlay () {
                    return instance.wtOverlays.topOverlay; // TODO refactoring: move outside dao, use IOC
                },
                get inlineStartOverlay () {
                    return instance.wtOverlays.inlineStartOverlay; // TODO refactoring: move outside dao, use IOC
                },
                get bottomOverlay () {
                    return instance.wtOverlays.bottomOverlay; // TODO refactoring: move outside dao, use IOC
                }
            }))(this);
    }
    /**
   * @param {HTMLTableElement} table Main table.
   * @param {SettingsPure} settings The Walkontable settings.
   */ constructor(table, settings){
        super(table, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$settings$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](settings));
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const facadeGetter = this.wtSettings.getSetting('facade', this); // todo rethink. I would like to have no access to facade from the internal scope.
        this.wtTable = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$master$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this.getTableDao(), facadeGetter, this.domBindings, this.wtSettings);
        this.wtViewport = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$viewport$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this.getViewportDao(), this.domBindings, this.wtSettings, this.eventManager, this.wtTable);
        this.selectionManager = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$manager$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["SelectionManager"](this.wtSettings.getSetting('selections'));
        this.wtEvent = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$event$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](facadeGetter, this.domBindings, this.wtSettings, this.eventManager, this.wtTable, this.selectionManager);
        this.wtOverlays = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlays$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this, facadeGetter, this.domBindings, this.wtSettings, this.eventManager, this.wtTable);
        this.exportSettingsAsClassNames();
        this.findOriginalHeaders();
    }
}
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/event.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/number.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/cellCoords.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$feature$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/feature.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/browser.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
;
;
const LONG_PRESS_DELAY = 500;
const LONG_PRESS_MOVE_THRESHOLD = 10;
var /**
   * Walkontable settings instance.
   *
   * @type {Settings}
   */ _wtSettings = /*#__PURE__*/ new WeakMap(), /**
   * DOM bindings for walkontable elements.
   *
   * @type {DomBindings}
   */ _domBindings = /*#__PURE__*/ new WeakMap(), /**
   * Reference to the walkontable table.
   *
   * @type {Table}
   */ _wtTable = /*#__PURE__*/ new WeakMap(), /**
   * Selection manager instance.
   *
   * @type {SelectionManager}
   */ _selectionManager = /*#__PURE__*/ new WeakMap(), /**
   * Parent Event instance, or null for the root instance.
   *
   * @type {Event | null}
   */ _parent = /*#__PURE__*/ new WeakMap(), /**
   * Instance of {@link EventManager}.
   *
   * @type {EventManager}
   */ _eventManager = /*#__PURE__*/ new WeakMap(), /**
   * Should be use only for passing face called external origin methods, like registered event listeners.
   * It provides backward compatibility by getting instance facade.
   *
   * @todo Consider about removing this from Event class, because it make relationship into facade (implicit circular
   *   dependency).
   * @todo Con. Maybe passing listener caller as an ioc from faced resolves this issue. To rethink later.
   *
   * @type {FacadeGetter}
   */ _facadeGetter = /*#__PURE__*/ new WeakMap(), /**
   * @type {boolean}
   */ _selectedCellBeforeTouchEnd = /*#__PURE__*/ new WeakMap(), /**
   * @type {number[]}
   */ _dblClickTimeout = /*#__PURE__*/ new WeakMap(), /**
   * @type {number[]}
   */ _dblClickOrigin = /*#__PURE__*/ new WeakMap(), /**
   * Timer ID for the long-press gesture detection.
   *
   * @type {number|null}
   */ _longPressTimeout = /*#__PURE__*/ new WeakMap(), /**
   * Marks that the long-press contextmenu gesture has been triggered for the current touch.
   *
   * @type {boolean}
   */ _longPressFired = /*#__PURE__*/ new WeakMap(), /**
   * Starting coordinates of a touch gesture (used to detect movement that cancels long-press
   * and to distinguish a tap from a scroll).
   *
   * @type {{ x: number, y: number }|null}
   */ _touchStartCoords = /*#__PURE__*/ new WeakMap(), /**
   * Marks that the current touch gesture has moved beyond the threshold and should be treated
   * as a scroll rather than a tap.
   *
   * @type {boolean}
   */ _touchWasMoved = /*#__PURE__*/ new WeakMap(), /**
   * The original `touchstart` event captured so the synthesized mousedown can be deferred to
   * `touchend` and only fired when the gesture is a tap (not a scroll).
   *
   * @type {TouchEvent|null}
   */ _deferredTouchStartEvent = /*#__PURE__*/ new WeakMap(), /**
   * @type {boolean}
   */ _mouseDown = /*#__PURE__*/ new WeakMap(), /**
   * The last renderable coords seen in `onMouseMove` while the mouse was outside the viewport.
   * Used to skip the `onCellMouseOverOutside` listener call (and therefore `setRangeEnd`) when
   * repeated mousemove events land on the same edge cell.
   *
   * @type {{ row: number, col: number } | null}
   */ _mouseOverOutsideLastCoords = /*#__PURE__*/ new WeakMap(), /**
   * Returns the cell coordinates for the given mouse position and whether the mouse is
   * outside the visible viewport. When the mouse is outside, the nearest edge cell is returned.
   *
   * @private
   * @param {number} mouseX Client X coordinate of the mouse.
   * @param {number} mouseY Client Y coordinate of the mouse.
   * @returns {{ coords: CellCoords, isOutside: boolean }}
   */ _getCellCoordsFromMousePosition = /*#__PURE__*/ new WeakSet(), /**
   * Starts the long-press timer. When the timer fires, a synthetic `contextmenu` event is
   * dispatched on the original touch target so that the existing contextmenu hook chain
   * (and ContextMenu plugin) work without changes.
   *
   * @private
   * @param {TouchEvent} event The original `touchstart` event.
   */ _startLongPressTimer = /*#__PURE__*/ new WeakSet(), /**
   * Cancels the pending long-press timer.
   *
   * @private
   */ _cancelLongPressTimer = /*#__PURE__*/ new WeakSet();
/**
 * @class Event
 */ class Event {
    /**
   * Adds listeners for mouse and touch events.
   *
   * @private
   */ registerEvents() {
        _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _wtTable).holder, 'contextmenu', (event)=>this.onContextMenu(event));
        _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _wtTable).TABLE, 'mouseover', (event)=>this.onMouseOver(event));
        _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _wtTable).TABLE, 'mouseout', (event)=>this.onMouseOut(event));
        if (_class_private_field_get(this, _wtTable).isMaster) {
            _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _domBindings).rootDocument, 'mousemove', (event)=>this.onMouseMove(event));
            _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _domBindings).rootDocument, 'mouseup', ()=>{
                _class_private_field_set(this, _mouseDown, false);
                _class_private_field_set(this, _mouseOverOutsideLastCoords, null);
            });
        }
        const initTouchEvents = ()=>{
            _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _wtTable).holder, 'touchstart', (event)=>this.onTouchStart(event));
            _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _wtTable).holder, 'touchend', (event)=>this.onTouchEnd(event));
            _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _wtTable).holder, 'touchmove', (event)=>this.onTouchMove(event));
            _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _wtTable).holder, 'scroll', ()=>this.onHolderScroll());
        };
        const initMouseEvents = ()=>{
            _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _wtTable).holder, 'mouseup', (event)=>this.onMouseUp(event));
            _class_private_field_get(this, _eventManager).addEventListener(_class_private_field_get(this, _wtTable).holder, 'mousedown', (event)=>this.onMouseDown(event));
        };
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isMobileBrowser"])()) {
            initTouchEvents();
        } else {
            // PC like devices which support both methods (touchscreen and ability to plug-in mouse).
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$feature$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isTouchSupported"])()) {
                initTouchEvents();
            }
            initMouseEvents();
        }
    }
    /**
   * Checks if an element is already selected.
   *
   * @private
   * @param {Element} touchTarget An element to check.
   * @returns {boolean}
   */ selectedCellWasTouched(touchTarget) {
        const cellUnderFinger = this.parentCell(touchTarget);
        const coordsOfCellUnderFinger = cellUnderFinger.coords;
        if (_class_private_field_get(this, _selectedCellBeforeTouchEnd) && coordsOfCellUnderFinger) {
            const [rowTouched, rowSelected] = [
                coordsOfCellUnderFinger.row,
                _class_private_field_get(this, _selectedCellBeforeTouchEnd).from.row
            ];
            const [colTouched, colSelected] = [
                coordsOfCellUnderFinger.col,
                _class_private_field_get(this, _selectedCellBeforeTouchEnd).from.col
            ];
            return rowTouched === rowSelected && colTouched === colSelected;
        }
        return false;
    }
    /**
   * Gets closest TD or TH element.
   *
   * @private
   * @param {Element} elem An element from the traversing starts.
   * @returns {object} Contains coordinates and reference to TD or TH if it exists. Otherwise it's empty object.
   */ parentCell(elem) {
        const cell = {
            coords: null,
            TD: null
        };
        const TABLE = _class_private_field_get(this, _wtTable).TABLE;
        const TD = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["closestDown"])(elem, [
            'TD',
            'TH'
        ], TABLE);
        const elemEl = elem;
        if (TD) {
            cell.coords = _class_private_field_get(this, _wtTable).getCoords(TD);
            cell.TD = TD;
        } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(elemEl, 'wtBorder') && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(elemEl, 'current')) {
            const focusCellRange = _class_private_field_get(this, _selectionManager).getFocusSelection()?.cellRange;
            if (focusCellRange) {
                cell.coords = focusCellRange.highlight;
                cell.TD = _class_private_field_get(this, _wtTable).getCell(cell.coords);
            }
        } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(elemEl, 'wtBorder') && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(elemEl, 'area')) {
            const areaCellRange = _class_private_field_get(this, _selectionManager).getAreaSelection()?.cellRange;
            if (areaCellRange) {
                cell.coords = areaCellRange.to;
                cell.TD = _class_private_field_get(this, _wtTable).getCell(cell.coords);
            }
        }
        return cell;
    }
    /**
   * OnMouseDown callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */ onMouseDown(event) {
        const activeElement = _class_private_field_get(this, _domBindings).rootDocument.activeElement;
        const targetEl = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event);
        const getParentNode = (level)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getParent"])(targetEl, level);
        const realTarget = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event);
        // ignore non-TD focusable elements from mouse down processing
        // (https://github.com/handsontable/handsontable/issues/3555)
        if (activeElement && ![
            'TD',
            'TH'
        ].includes(activeElement.nodeName) && (realTarget === activeElement || getParentNode(0) === activeElement || getParentNode(1) === activeElement)) {
            return;
        }
        const cell = this.parentCell(realTarget);
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(realTarget, 'corner')) {
            _class_private_field_get(this, _wtSettings).getSetting('onCellCornerMouseDown', event, realTarget);
        } else if (cell.TD) {
            _class_private_field_set(this, _mouseDown, true);
            _class_private_field_set(this, _mouseOverOutsideLastCoords, null);
            if (_class_private_field_get(this, _wtSettings).has('onCellMouseDown')) {
                this.callListener('onCellMouseDown', event, cell.coords, cell.TD);
            }
        }
        // doubleclick reacts only for left mouse button or from touch events
        if ((event.button === 0 || this.touchApplied) && cell.TD) {
            _class_private_field_get(this, _dblClickOrigin)[0] = cell.TD;
            if (_class_private_field_get(this, _dblClickTimeout)[0] !== null) {
                clearTimeout(_class_private_field_get(this, _dblClickTimeout)[0]);
            }
            _class_private_field_get(this, _dblClickTimeout)[0] = setTimeout(()=>{
                _class_private_field_get(this, _dblClickOrigin)[0] = null;
            }, 1000);
        }
    }
    /**
   * OnContextMenu callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */ onContextMenu(event) {
        _class_private_method_get(this, _cancelLongPressTimer, cancelLongPressTimer).call(this);
        if (_class_private_field_get(this, _wtSettings).has('onCellContextMenu')) {
            const cell = this.parentCell((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event));
            if (cell.TD) {
                this.callListener('onCellContextMenu', event, cell.coords, cell.TD);
            }
        }
    }
    /**
   * OnMouseOver callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */ onMouseOver(event) {
        if (!_class_private_field_get(this, _wtSettings).has('onCellMouseOver')) {
            return;
        }
        const table = _class_private_field_get(this, _wtTable).TABLE;
        const td = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["closestDown"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event), [
            'TD',
            'TH'
        ], table);
        const parent = _class_private_field_get(this, _parent) || this;
        if (td && td !== parent.lastMouseOver && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isChildOf"])(td, table)) {
            parent.lastMouseOver = td;
            const tdCoords = _class_private_field_get(this, _wtTable).getCoords(td);
            if (tdCoords) {
                this.callListener('onCellMouseOver', event, tdCoords, td);
            }
        }
    }
    /**
   * OnMouseMove callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */ onMouseMove(event) {
        if (!_class_private_field_get(this, _mouseDown)) {
            return;
        }
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const { coords, isOutside } = _class_private_method_get(this, _getCellCoordsFromMousePosition, getCellCoordsFromMousePosition).call(this, event.clientX, event.clientY);
        if (isOutside) {
            const lastCoords = _class_private_field_get(this, _mouseOverOutsideLastCoords);
            if (!lastCoords || lastCoords.row !== coords.row || lastCoords.col !== coords.col) {
                const TD = _class_private_field_get(this, _wtTable).getCell(coords);
                if (TD instanceof HTMLElement) {
                    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
                    _class_private_field_set(this, _mouseOverOutsideLastCoords, {
                        row: coords.row,
                        col: coords.col
                    });
                    this.callListener('onCellMouseOverOutside', event, coords, TD);
                }
            }
        } else {
            _class_private_field_set(this, _mouseOverOutsideLastCoords, null);
        }
    }
    /**
   * OnMouseOut callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */ onMouseOut(event) {
        if (!_class_private_field_get(this, _wtSettings).has('onCellMouseOut')) {
            return;
        }
        const table = _class_private_field_get(this, _wtTable).TABLE;
        const lastTD = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["closestDown"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event), [
            'TD',
            'TH'
        ], table);
        const nextTD = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["closestDown"])(event.relatedTarget, [
            'TD',
            'TH'
        ], table);
        const parent = _class_private_field_get(this, _parent) || this;
        if (lastTD && lastTD !== nextTD && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isChildOf"])(lastTD, table)) {
            const lastTDCoords = _class_private_field_get(this, _wtTable).getCoords(lastTD);
            if (lastTDCoords) {
                this.callListener('onCellMouseOut', event, lastTDCoords, lastTD);
            }
            if (nextTD === null) {
                parent.lastMouseOver = null;
            }
        }
    }
    /**
   * OnMouseUp callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */ onMouseUp(event) {
        _class_private_field_set(this, _mouseDown, false);
        _class_private_field_set(this, _mouseOverOutsideLastCoords, null);
        const cell = this.parentCell((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event));
        if (cell.TD && _class_private_field_get(this, _wtSettings).has('onCellMouseUp')) {
            this.callListener('onCellMouseUp', event, cell.coords, cell.TD);
        }
        // if not left mouse button, and the origin event is not comes from touch
        if (event.button !== 0 && !this.touchApplied) {
            return;
        }
        if (cell.TD && cell.TD === _class_private_field_get(this, _dblClickOrigin)[0] && cell.TD === _class_private_field_get(this, _dblClickOrigin)[1]) {
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event), 'corner')) {
                this.callListener('onCellCornerDblClick', event, cell.coords, cell.TD);
            } else {
                this.callListener('onCellDblClick', event, cell.coords, cell.TD);
            }
            _class_private_field_get(this, _dblClickOrigin)[0] = null;
            _class_private_field_get(this, _dblClickOrigin)[1] = null;
        } else if (cell.TD && cell.TD === _class_private_field_get(this, _dblClickOrigin)[0]) {
            _class_private_field_get(this, _dblClickOrigin)[1] = cell.TD;
            if (_class_private_field_get(this, _dblClickTimeout)[1] !== null) {
                clearTimeout(_class_private_field_get(this, _dblClickTimeout)[1]);
            }
            _class_private_field_get(this, _dblClickTimeout)[1] = setTimeout(()=>{
                _class_private_field_get(this, _dblClickOrigin)[1] = null;
            }, 500);
        }
    }
    /**
   * OnTouchStart callback. Captures the gesture start so the synthesized mousedown can be
   * deferred to `touchend`; this lets a touch-drag gesture scroll the grid without
   * re-triggering the cell selection (see issue #11659).
   *
   * @private
   * @param {TouchEvent} event The touch event object.
   */ onTouchStart(event) {
        _class_private_field_set(this, _selectedCellBeforeTouchEnd, _class_private_field_get(this, _selectionManager).getFocusSelection()?.cellRange ?? null);
        this.touchApplied = true;
        _class_private_field_set(this, _touchWasMoved, false);
        _class_private_field_set(this, _longPressFired, false);
        _class_private_field_set(this, _deferredTouchStartEvent, event);
        _class_private_method_get(this, _startLongPressTimer, startLongPressTimer).call(this, event);
    }
    /**
   * OnTouchEnd callback. Fires the deferred mousedown only when the gesture is a tap
   * (no movement past the threshold and no long-press); for a scroll gesture the
   * selection stays untouched (see issue #11659).
   *
   * @private
   * @param {TouchEvent} event The touch event object.
   */ onTouchEnd(event) {
        const wasScrolled = _class_private_field_get(this, _touchWasMoved);
        const isTap = !wasScrolled && !_class_private_field_get(this, _longPressFired) && _class_private_field_get(this, _deferredTouchStartEvent) !== null;
        const deferredTouchStartEvent = _class_private_field_get(this, _deferredTouchStartEvent);
        _class_private_method_get(this, _cancelLongPressTimer, cancelLongPressTimer).call(this);
        _class_private_field_set(this, _deferredTouchStartEvent, null);
        if (isTap && deferredTouchStartEvent !== null) {
            this.onMouseDown(deferredTouchStartEvent);
        }
        const target = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event);
        const parentCellCoords = this.parentCell(target)?.coords;
        const isCellsRange = parentCellCoords !== null && parentCellCoords !== undefined && (parentCellCoords.row ?? -1) >= 0 && (parentCellCoords.col ?? -1) >= 0;
        const isEventCancelable = event.cancelable && isCellsRange && _class_private_field_get(this, _wtSettings).getSetting('isDataViewInstance');
        // To prevent accidental redirects or other actions that the interactive elements (e.q "A" link) do
        // while the cell is highlighted, all touch events that are triggered on different cells are
        // "preventDefault"'ed. The user can interact with the element (e.q. click on the link that opens
        // a new page) only when the same cell was previously selected (see related PR #7980).
        if (isEventCancelable) {
            const interactiveElements = [
                'A',
                'BUTTON',
                'INPUT'
            ];
            // For browsers that use the WebKit as an engine (excluding Safari), there is a bug. The prevent
            // default has to be called all the time. Otherwise, the second tap won't be triggered (probably
            // caused by the native ~300ms delay - https://webkit.org/blog/5610/more-responsive-tapping-on-ios/).
            // To make the interactive elements work, the event target element has to be check. If the element
            // matches the allow-list, the event is not prevented.
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isIOS"])() && ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isChromeWebKit"])() || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isFirefoxWebKit"])()) && this.selectedCellWasTouched(target) && !interactiveElements.includes(target.tagName)) {
                event.preventDefault();
            } else if (!this.selectedCellWasTouched(target) && !(target.tagName === 'INPUT' && target.type === 'checkbox')) {
                // For other browsers, prevent default is fired only for the first tap and only when the previous
                // highlighted cell was different. Checkbox inputs are excluded so that checkboxes with
                // disableVisualSelection can still be toggled via touch (the focus cellRange is never populated
                // when visual selection is disabled, so selectedCellWasTouched always returns false for them).
                event.preventDefault();
            }
        }
        // Fire mouseUp whenever the gesture also produced a mouseDown - either a tap
        // (deferred mousedown above) or a long-press (mousedown fired from the timer
        // callback). Skipping it for a long-press that ended in a scroll would leave
        // an unpaired mousedown and break plugins listening to onCellMouseUp /
        // before/after hooks (e.g. nestedHeaders, ContextMenu close logic).
        // Suppress only for pure scroll gestures, where onMouseDown was never fired.
        if (!wasScrolled || _class_private_field_get(this, _longPressFired)) {
            this.onMouseUp(event);
        }
        this.touchApplied = false;
        _class_private_field_set(this, _touchWasMoved, false);
        _class_private_field_set(this, _longPressFired, false);
    }
    /**
   * Holder `scroll` callback. Cancels the long-press timer and runs the momentum-scroll
   * bookkeeping. When called during an active touch sequence it also marks the gesture
   * as a scroll so the deferred mousedown is not fired on `touchend` - native scrolling
   * can start at ~8px, before the 10px LONG_PRESS_MOVE_THRESHOLD that `onTouchMove`
   * watches (issue #11659).
   *
   * @private
   */ onHolderScroll() {
        if (!this.momentumScrolling) {
            this.momentumScrolling = {};
        }
        if (this.touchApplied) {
            _class_private_field_set(this, _touchWasMoved, true);
        }
        _class_private_method_get(this, _cancelLongPressTimer, cancelLongPressTimer).call(this);
        clearTimeout(this.momentumScrolling._timeout);
        if (!this.momentumScrolling.ongoing) {
            _class_private_field_get(this, _wtSettings).getSetting('onBeforeTouchScroll');
        }
        this.momentumScrolling.ongoing = true;
        this.momentumScrolling._timeout = setTimeout(()=>{
            if (!this.touchApplied) {
                this.momentumScrolling.ongoing = false;
                _class_private_field_get(this, _wtSettings).getSetting('onAfterMomentumScroll');
            }
        }, 200);
    }
    /**
   * OnTouchMove callback. Once the finger moves beyond the threshold, marks the gesture as a
   * scroll so `touchend` skips firing the deferred mousedown, and cancels the long-press timer.
   *
   * @private
   * @param {TouchEvent} event The touch event object.
   */ onTouchMove(event) {
        if (_class_private_field_get(this, _touchStartCoords) === null) {
            return;
        }
        const touch = event.touches[0];
        if (!touch) {
            _class_private_field_set(this, _touchWasMoved, true);
            _class_private_method_get(this, _cancelLongPressTimer, cancelLongPressTimer).call(this);
            return;
        }
        const dx = Math.abs(touch.clientX - _class_private_field_get(this, _touchStartCoords).x);
        const dy = Math.abs(touch.clientY - _class_private_field_get(this, _touchStartCoords).y);
        if (dx > LONG_PRESS_MOVE_THRESHOLD || dy > LONG_PRESS_MOVE_THRESHOLD) {
            _class_private_field_set(this, _touchWasMoved, true);
            _class_private_method_get(this, _cancelLongPressTimer, cancelLongPressTimer).call(this);
        }
    }
    /**
   * Call listener with backward compatibility.
   *
   * @private
   * @param {string} name Name of listener.
   * @param {MouseEvent} event The event object.
   * @param {CellCoords} coords Coordinates.
   * @param {HTMLElement} target Event target.
   */ callListener(name, event, coords, target) {
        const listener = _class_private_field_get(this, _wtSettings).getSettingPure(name);
        if (listener) {
            listener(event, coords, target, _class_private_field_get(this, _facadeGetter).call(this));
        }
    }
    /**
   * Clears double-click timeouts and destroys the internal eventManager instance.
   */ destroy() {
        if (_class_private_field_get(this, _dblClickTimeout)[0] !== null) {
            clearTimeout(_class_private_field_get(this, _dblClickTimeout)[0]);
        }
        if (_class_private_field_get(this, _dblClickTimeout)[1] !== null) {
            clearTimeout(_class_private_field_get(this, _dblClickTimeout)[1]);
        }
        _class_private_method_get(this, _cancelLongPressTimer, cancelLongPressTimer).call(this);
        if (this.momentumScrolling) {
            clearTimeout(this.momentumScrolling._timeout);
        }
        _class_private_field_get(this, _eventManager).destroy();
    }
    /**
   * @param {FacadeGetter} facadeGetter Gets an instance facade.
   * @param {DomBindings} domBindings Bindings into dom.
   * @param {Settings} wtSettings The walkontable settings.
   * @param {EventManager} eventManager The walkontable event manager.
   * @param {Table} wtTable The table.
   * @param {SelectionManager} selectionManager Selections.
   * @param {Event} [parent=null] The main Event instance.
   */ constructor(facadeGetter, domBindings, wtSettings, eventManager, wtTable, selectionManager, parent = null){
        _class_private_method_init(this, _getCellCoordsFromMousePosition);
        _class_private_method_init(this, _startLongPressTimer);
        _class_private_method_init(this, _cancelLongPressTimer);
        _class_private_field_init(this, _wtSettings, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _domBindings, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _wtTable, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _selectionManager, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _parent, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _eventManager, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _facadeGetter, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _selectedCellBeforeTouchEnd, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _dblClickTimeout, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _dblClickOrigin, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _longPressTimeout, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _longPressFired, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _touchStartCoords, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _touchWasMoved, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _deferredTouchStartEvent, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _mouseDown, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _mouseOverOutsideLastCoords, {
            writable: true,
            value: void 0
        });
        _class_private_field_set(this, _selectedCellBeforeTouchEnd, null);
        _class_private_field_set(this, _dblClickTimeout, [
            null,
            null
        ]);
        _class_private_field_set(this, _dblClickOrigin, [
            null,
            null
        ]);
        _class_private_field_set(this, _longPressTimeout, null);
        _class_private_field_set(this, _longPressFired, false);
        _class_private_field_set(this, _touchStartCoords, null);
        _class_private_field_set(this, _touchWasMoved, false);
        _class_private_field_set(this, _deferredTouchStartEvent, null);
        _class_private_field_set(this, _mouseDown, false);
        _class_private_field_set(this, _mouseOverOutsideLastCoords, null);
        _class_private_field_set(this, _wtSettings, wtSettings);
        _class_private_field_set(this, _domBindings, domBindings);
        _class_private_field_set(this, _wtTable, wtTable);
        _class_private_field_set(this, _selectionManager, selectionManager);
        _class_private_field_set(this, _parent, parent);
        _class_private_field_set(this, _eventManager, eventManager);
        _class_private_field_set(this, _facadeGetter, facadeGetter);
        this.registerEvents();
    }
}
function getCellCoordsFromMousePosition(mouseX, mouseY) {
    const isRtl = _class_private_field_get(this, _wtSettings).getSetting('rtlMode');
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const wot = _class_private_field_get(this, _facadeGetter).call(this);
    const numberOfFixedColumnsStart = _class_private_field_get(this, _wtSettings).getSetting('fixedColumnsStart');
    const numberOfFixedRowsTop = _class_private_field_get(this, _wtSettings).getSetting('fixedRowsTop');
    const numberOfFixedRowsBottom = _class_private_field_get(this, _wtSettings).getSetting('fixedRowsBottom');
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const firstPartiallyVisibleRow = wot.wtScroll.getFirstPartiallyVisibleRow();
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const lastPartiallyVisibleRow = wot.wtScroll.getLastPartiallyVisibleRow();
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const firstPartiallyVisibleColumn = wot.wtScroll.getFirstPartiallyVisibleColumn();
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const lastPartiallyVisibleColumn = wot.wtScroll.getLastPartiallyVisibleColumn();
    const tableOffset = _class_private_field_get(this, _wtTable).wtRootElement.getBoundingClientRect();
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const columnHeaderHeight = _class_private_field_get(this, _wtSettings).getSetting('columnHeaders').length > 0 ? wot.wtViewport.getColumnHeaderHeight() : 0;
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const rowHeaderWidth = _class_private_field_get(this, _wtSettings).getSetting('rowHeaders').length > 0 ? wot.wtViewport.getRowHeaderWidth() : 0;
    const { rootWindow } = _class_private_field_get(this, _domBindings);
    // When the window is the scroll container and tableOffset.left/top > 0 (e.g. RTL
    // at max-left scroll where tableOffset.left can exceed innerWidth), using it as the
    // clamp minimum causes clamp(min > max) to always return min, mapping every mouse
    // position to the wrong edge column. Math.min(0, tableOffset) corrects this while
    // preserving the original boundary when the table is partially off-screen to the
    // left/top (tableOffset < 0), which is the normal scrolled-past-origin case.
    const tableViewportLeft = wot.wtViewport.isHorizontallyScrollableByWindow() ? Math.min(0, tableOffset.left) : tableOffset.left;
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const tableViewportTop = wot.wtViewport.isVerticallyScrollableByWindow() ? Math.min(0, tableOffset.top) : tableOffset.top;
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const tableViewportRight = wot.wtViewport.isHorizontallyScrollableByWindow() ? rootWindow.innerWidth : tableOffset.left + wot.wtViewport.getViewportWidth() + rowHeaderWidth;
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const tableViewportBottom = wot.wtViewport.isVerticallyScrollableByWindow() ? rootWindow.innerHeight : tableOffset.top + wot.wtViewport.getViewportHeight() + columnHeaderHeight;
    const clampedX = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(mouseX, tableViewportLeft, tableViewportRight);
    const clampedY = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(mouseY, tableViewportTop, tableViewportBottom);
    let foundColumn = null;
    if (numberOfFixedColumnsStart > 0) {
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const fixedCell = wot.getCell({
            row: firstPartiallyVisibleRow,
            col: 0
        }, true);
        if (fixedCell instanceof HTMLElement) {
            const fixedCellRect = fixedCell.getBoundingClientRect();
            const fixedRelativeX = isRtl ? fixedCellRect.right - clampedX : clampedX - fixedCellRect.left;
            foundColumn = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["findColumnAtX"])(wot, firstPartiallyVisibleRow, 0, numberOfFixedColumnsStart - 1, fixedRelativeX);
        }
    }
    if (foundColumn === null) {
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const scrollCell = wot.getCell({
            row: firstPartiallyVisibleRow,
            col: firstPartiallyVisibleColumn
        }, true);
        if (scrollCell instanceof HTMLElement) {
            const scrollCellRect = scrollCell.getBoundingClientRect();
            const scrollRelativeX = isRtl ? scrollCellRect.right - clampedX : clampedX - scrollCellRect.left;
            foundColumn = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["findColumnAtX"])(wot, firstPartiallyVisibleRow, firstPartiallyVisibleColumn, lastPartiallyVisibleColumn, scrollRelativeX);
            if (foundColumn === null) {
                foundColumn = scrollRelativeX < 0 ? firstPartiallyVisibleColumn : lastPartiallyVisibleColumn;
            }
        } else {
            foundColumn = firstPartiallyVisibleColumn;
        }
    }
    let foundRow = null;
    if (numberOfFixedRowsTop > 0) {
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const fixedCell = wot.getCell({
            row: 0,
            col: firstPartiallyVisibleColumn
        }, true);
        if (fixedCell instanceof HTMLElement) {
            const fixedCellRect = fixedCell.getBoundingClientRect();
            const fixedRelativeY = clampedY - fixedCellRect.top;
            foundRow = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["findRowAtY"])(wot, firstPartiallyVisibleColumn, 0, numberOfFixedRowsTop - 1, fixedRelativeY);
        }
    }
    if (foundRow === null && numberOfFixedRowsBottom > 0) {
        const totalRows = _class_private_field_get(this, _wtSettings).getSetting('totalRows');
        const bottomStartRow = totalRows - numberOfFixedRowsBottom;
        const bottomEndRow = totalRows - 1;
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const fixedBottomCell = wot.getCell({
            row: bottomStartRow,
            col: firstPartiallyVisibleColumn
        }, true);
        if (fixedBottomCell instanceof HTMLElement) {
            const fixedBottomCellRect = fixedBottomCell.getBoundingClientRect();
            const fixedBottomRelativeY = clampedY - fixedBottomCellRect.top;
            if (fixedBottomRelativeY >= 0) {
                foundRow = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["findRowAtY"])(wot, firstPartiallyVisibleColumn, bottomStartRow, bottomEndRow, fixedBottomRelativeY);
                if (foundRow === null) {
                    foundRow = bottomEndRow;
                }
            }
        }
    }
    if (foundRow === null) {
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const scrollCell = wot.getCell({
            row: firstPartiallyVisibleRow,
            col: firstPartiallyVisibleColumn
        }, true);
        if (scrollCell instanceof HTMLElement) {
            const scrollCellRect = scrollCell.getBoundingClientRect();
            const scrollRelativeY = clampedY - scrollCellRect.top;
            foundRow = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$cellCoords$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["findRowAtY"])(wot, firstPartiallyVisibleColumn, firstPartiallyVisibleRow, lastPartiallyVisibleRow, scrollRelativeY);
            if (foundRow === null) {
                foundRow = lastPartiallyVisibleRow;
            }
        } else {
            foundRow = firstPartiallyVisibleRow;
        }
    }
    return {
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        coords: wot.createCellCoords(foundRow, foundColumn),
        isOutside: mouseX < tableViewportLeft || mouseX > tableViewportRight || mouseY < tableViewportTop || mouseY > tableViewportBottom
    };
}
function startLongPressTimer(event) {
    _class_private_method_get(this, _cancelLongPressTimer, cancelLongPressTimer).call(this);
    const touch = event.touches[0];
    if (!touch) {
        return;
    }
    _class_private_field_set(this, _touchStartCoords, {
        x: touch.clientX,
        y: touch.clientY
    });
    _class_private_field_set(this, _longPressTimeout, setTimeout(()=>{
        _class_private_field_set(this, _longPressTimeout, null);
        _class_private_field_set(this, _longPressFired, true);
        _class_private_field_set(this, _touchStartCoords, null);
        // Select the long-pressed cell so context-menu commands (e.g. "Insert row above")
        // operate on it. With the deferred-mousedown flow, touchend skips onMouseDown
        // when #longPressFired is true, so we fire it here before opening the menu.
        this.onMouseDown(event);
        _class_private_field_get(this, _dblClickOrigin)[0] = null;
        _class_private_field_get(this, _dblClickOrigin)[1] = null;
        if (_class_private_field_get(this, _dblClickTimeout)[0] !== null) {
            clearTimeout(_class_private_field_get(this, _dblClickTimeout)[0]);
        }
        if (_class_private_field_get(this, _dblClickTimeout)[1] !== null) {
            clearTimeout(_class_private_field_get(this, _dblClickTimeout)[1]);
        }
        const target = event.target;
        if (!target) {
            return;
        }
        const contextMenuEvent = new MouseEvent('contextmenu', {
            bubbles: true,
            cancelable: true,
            clientX: touch.clientX,
            clientY: touch.clientY,
            screenX: touch.screenX,
            screenY: touch.screenY
        });
        target.dispatchEvent(contextMenuEvent);
    }, LONG_PRESS_DELAY));
}
function cancelLongPressTimer() {
    if (_class_private_field_get(this, _longPressTimeout) !== null) {
        clearTimeout(_class_private_field_get(this, _longPressTimeout));
        _class_private_field_set(this, _longPressTimeout, null);
    }
    _class_private_field_set(this, _touchStartCoords, null);
}
var __TURBOPACK__default__export__ = Event;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/facade/core.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>WalkontableFacade
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$core$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/core/core.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/core/_base.mjs [client] (ecmascript)");
;
;
class WalkontableFacade {
    /**
   * Initializes the facade from a settings object.
   *
   * @param {Record<string, any>} settings - The Walkontable settings configuration.
   */ // eslint-disable-next-line @typescript-eslint/no-explicit-any
    _initFromSettings(settings) {
        settings.facade = (instance)=>{
            const facade = new WalkontableFacade(instance);
            return ()=>facade;
        };
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        this._wot = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$core$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](settings.table, settings);
    }
    /**
   * Gets the unique identifier of the Walkontable instance.
   *
   * @returns {*} The unique identifier.
   */ get guid() {
        return this._wot.guid;
    }
    /**
   * Gets the root document used by the Walkontable instance.
   *
   * @returns {*} The root document.
   */ get rootDocument() {
        return this._wot.domBindings.rootDocument;
    }
    /**
   * Gets the root window used by the Walkontable instance.
   *
   * @returns {*} The root window.
   */ get rootWindow() {
        return this._wot.domBindings.rootWindow;
    }
    /**
   * Gets the settings manager for the Walkontable instance.
   *
   * @returns {*} The settings manager.
   */ get wtSettings() {
        return this._wot.wtSettings; // todo create facade
    }
    /**
   * Gets the clone source of the Walkontable instance.
   *
   * @returns {*} The clone source.
   */ get cloneSource() {
        return this._wot.cloneSource; // todo create facade
    }
    /**
   * Gets the clone overlay of the Walkontable instance.
   *
   * @returns {Overlay} The clone overlay.
   */ get cloneOverlay() {
        return this._wot.cloneOverlay; // todo create facade
    }
    /**
   * Gets the selection manager of the Walkontable instance.
   *
   * @returns {*} The selection manager.
   */ get selectionManager() {
        return this._wot.selectionManager; // todo create facade
    }
    /**
   * Gets the viewport manager of the Walkontable instance.
   *
   * @returns {*} The viewport manager.
   */ get wtViewport() {
        return this._wot.wtViewport; // todo create facade
    }
    /**
   * Gets the overlays manager of the Walkontable instance.
   *
   * @returns {*} The overlays manager.
   */ get wtOverlays() {
        return this._wot.wtOverlays; // todo create facade
    }
    /**
   * Gets the table manager of the Walkontable instance.
   *
   * @returns {*} The table manager.
   */ get wtTable() {
        return this._wot.wtTable; // todo create facade
    }
    /**
   * Gets the event manager of the Walkontable instance.
   *
   * @returns {*} The event manager.
   */ get wtEvent() {
        return this._wot.wtEvent; // todo create facade
    }
    /**
   * Gets the scroll manager of the Walkontable instance.
   *
   * @returns {*} The scroll manager.
   */ get wtScroll() {
        return this._wot.wtScroll; // todo create facade
    }
    /**
   * Gets whether the Walkontable instance has been drawn.
   *
   * @returns {*} True if drawn, false otherwise.
   */ get drawn() {
        return this._wot.drawn;
    }
    /**
   * Sets whether the Walkontable instance has been drawn.
   *
   * @param {*} value - The drawn state.
   */ set drawn(value) {
        this._wot.drawn = value;
    }
    /**
   * Gets the name of the active overlay.
   *
   * @returns {*} The active overlay name.
   */ get activeOverlayName() {
        return this._wot.activeOverlayName;
    }
    /**
   * Gets whether drawing has been interrupted.
   *
   * @returns {*} True if drawing is interrupted, false otherwise.
   */ get drawInterrupted() {
        return this._wot.drawInterrupted;
    }
    /**
   * Sets whether drawing has been interrupted.
   *
   * @param {*} value - The interrupted state.
   */ set drawInterrupted(value) {
        this._wot.drawInterrupted = value;
    }
    /**
   * Gets the last element that was moused over.
   *
   * @returns {HTMLElement | null} The last moused-over element, or null.
   */ get lastMouseOver() {
        return this._wot.lastMouseOver;
    }
    /**
   * Sets the last element that was moused over.
   *
   * @param {*} value - The element to set.
   */ set lastMouseOver(value) {
        this._wot.lastMouseOver = value;
    }
    /**
   * Gets the momentum scrolling state.
   *
   * @returns {{ ongoing?: boolean; _timeout?: ReturnType<typeof setTimeout> }} The momentum scrolling state.
   */ get momentumScrolling() {
        return this._wot.momentumScrolling;
    }
    /**
   * Sets the momentum scrolling state.
   *
   * @param {*} value - The momentum scrolling state.
   */ set momentumScrolling(value) {
        this._wot.momentumScrolling = value;
    }
    /**
   * Gets whether touch input has been applied.
   *
   * @returns {boolean} True if touch was applied, false otherwise.
   */ get touchApplied() {
        return this._wot.touchApplied;
    }
    /**
   * Sets whether touch input has been applied.
   *
   * @param {*} value - The touch applied state.
   */ set touchApplied(value) {
        this._wot.touchApplied = value;
    }
    /**
   * Gets the DOM bindings configuration.
   *
   * @returns {*} The DOM bindings.
   */ get domBindings() {
        return this._wot.domBindings;
    }
    /**
   * Gets the list of event listeners.
   *
   * @returns {unknown[]} The array of event listeners.
   */ get eventListeners() {
        return this._wot.eventListeners;
    }
    /**
   * Sets the list of event listeners.
   *
   * @param {*} value - The event listeners array.
   */ set eventListeners(value) {
        this._wot.eventListeners = value;
    }
    /**
   * Gets the event manager instance.
   *
   * @returns {*} The event manager.
   */ get eventManager() {
        return this._wot.eventManager;
    }
    /**
   * Creates a new cell coordinates object.
   *
   * @param {number} row - The row index.
   * @param {number} column - The column index.
   * @returns {*} The new cell coordinates object.
   */ createCellCoords(row, column) {
        return this._wot.createCellCoords(row, column);
    }
    /**
   * Creates a new cell range.
   *
   * @param {CellCoords} highlight - The highlighted cell coordinates.
   * @param {CellCoords} from - The starting cell coordinates.
   * @param {CellCoords} to - The ending cell coordinates.
   * @returns {*} The new cell range.
   */ createCellRange(highlight, from, to) {
        return this._wot.createCellRange(highlight, from, to);
    }
    /**
   * Redraws the table.
   *
   * @param {boolean} [fastDraw=false] - Whether to perform a fast draw.
   * @returns {WalkontableFacade} This instance for method chaining.
   */ draw(fastDraw = false) {
        this._wot.draw(fastDraw);
        return this;
    }
    /**
   * Gets the DOM cell element at the specified coordinates.
   *
   * @param {CellCoords} coords - The cell coordinates.
   * @param {boolean} [topmost=false] - Whether to get the topmost element.
   * @returns {*} The DOM cell element.
   */ getCell(coords, topmost = false) {
        return this._wot.getCell(coords, topmost);
    }
    /**
   * Scrolls the viewport to the specified cell coordinates.
   *
   * @param {CellCoords} coords - The cell coordinates to scroll to.
   * @param {string} horizontalSnap - The horizontal snap setting.
   * @param {string} verticalSnap - The vertical snap setting.
   * @returns {*} The result of the scroll operation.
   */ scrollViewport(coords, horizontalSnap, verticalSnap) {
        return this._wot.scrollViewport(coords, horizontalSnap, verticalSnap);
    }
    /**
   * Scrolls the viewport horizontally.
   *
   * @param {number} column - The column index to scroll to.
   * @param {string} snapping - The snapping setting.
   * @returns {*} The result of the scroll operation.
   */ scrollViewportHorizontally(column, snapping) {
        return this._wot.scrollViewportHorizontally(column, snapping);
    }
    /**
   * Scrolls the viewport vertically.
   *
   * @param {number} row - The row index to scroll to.
   * @param {string} snapping - The snapping setting.
   * @returns {*} The result of the scroll operation.
   */ scrollViewportVertically(row, snapping) {
        return this._wot.scrollViewportVertically(row, snapping);
    }
    /**
   * Gets the current viewport dimensions.
   *
   * @returns {*} The viewport dimensions.
   */ getViewport() {
        return this._wot.getViewport();
    }
    /**
   * Gets the name of the currently active overlay.
   *
   * @returns {string} The overlay type name, or 'master' if no clone overlay.
   */ getOverlayName() {
        return this._wot.cloneOverlay ? this._wot.cloneOverlay.type : 'master';
    }
    /**
   * Gets an overlay by its name.
   *
   * @param {string} overlayName - The name of the overlay to retrieve.
   * @returns {Overlay | null} The overlay, or null if not found.
   */ getOverlayByName(overlayName) {
        return this._wot.getOverlayByName(overlayName);
    }
    /**
   * Exports the current settings as CSS class names on the table element.
   */ exportSettingsAsClassNames() {
        this._wot.exportSettingsAsClassNames();
    }
    /**
   * Updates Walkontable settings.
   *
   * @param {string | Record<string, unknown>} settings - The setting key or settings object.
   * @param {unknown} value - The value to set (when settings is a string).
   * @returns {WalkontableFacade} This instance for method chaining.
   */ update(settings, value) {
        this._wot.wtSettings.update(settings, value);
        return this;
    }
    /**
   * Gets a setting value.
   *
   * @param {string} key - The setting key.
   * @param {unknown} param1 - Optional parameter.
   * @param {unknown} param2 - Optional parameter.
   * @param {unknown} param3 - Optional parameter.
   * @param {unknown} param4 - Optional parameter.
   * @returns {unknown} The setting value.
   */ getSetting(key, param1, param2, param3, param4) {
        return this._wot.wtSettings.getSetting(key, param1, param2, param3, param4);
    }
    /**
   * Checks whether a setting exists.
   *
   * @param {string} key - The setting key.
   * @returns {*} True if the setting exists, false otherwise.
   */ hasSetting(key) {
        return this._wot.wtSettings.has(key);
    }
    /**
   * Destroys the Walkontable instance and cleans up resources.
   */ destroy() {
        this._wot.destroy();
    }
    /**
   * @param {SettingsPure|Walkontable} settingsOrInstance The Walkontable settings.
   */ constructor(settingsOrInstance){
        if (settingsOrInstance instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]) {
            this._wot = settingsOrInstance;
        } else {
            this._initFromSettings(settingsOrInstance);
        }
    }
}
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/filter/column.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
/**
 * @class ColumnFilter
 */ class ColumnFilter {
    /**
   * @param {number} index The visual column index.
   * @returns {number}
   */ offsetted(index) {
        return index + this.offset;
    }
    /**
   * @param {number} index The visual column index.
   * @returns {number}
   */ unOffsetted(index) {
        return index - this.offset;
    }
    /**
   * @param {number} index The visual column index.
   * @returns {number}
   */ renderedToSource(index) {
        return this.offsetted(index);
    }
    /**
   * @param {number} index The visual column index.
   * @returns {number}
   */ sourceToRendered(index) {
        return this.unOffsetted(index);
    }
    /**
   * @param {number} index The visual column index.
   * @returns {number}
   */ offsettedTH(index) {
        return index - this.countTH;
    }
    /**
   * @param {number} index The visual column index.
   * @returns {number}
   */ unOffsettedTH(index) {
        return index + this.countTH;
    }
    /**
   * @param {number} index The visual column index.
   * @returns {number}
   */ visibleRowHeadedColumnToSourceColumn(index) {
        return this.renderedToSource(this.offsettedTH(index));
    }
    /**
   * @param {number} index The visual column index.
   * @returns {number}
   */ sourceColumnToVisibleRowHeadedColumn(index) {
        return this.unOffsettedTH(this.sourceToRendered(index));
    }
    /**
   * @param {number} offset The scroll horizontal offset.
   * @param {number} total The total width of the table.
   * @param {number} countTH The number of rendered row headers.
   */ constructor(offset, total, countTH){
        this.offset = offset;
        this.total = total;
        this.countTH = countTH;
    }
}
var __TURBOPACK__default__export__ = ColumnFilter;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/filter/row.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
/**
 * @class RowFilter
 */ class RowFilter {
    /**
   * @param {number} index The visual row index.
   * @returns {number}
   */ offsetted(index) {
        return index + this.offset;
    }
    /**
   * @param {number} index The visual row index.
   * @returns {number}
   */ unOffsetted(index) {
        return index - this.offset;
    }
    /**
   * @param {number} index The visual row index.
   * @returns {number}
   */ renderedToSource(index) {
        return this.offsetted(index);
    }
    /**
   * @param {number} index The visual row index.
   * @returns {number}
   */ sourceToRendered(index) {
        return this.unOffsetted(index);
    }
    /**
   * @param {number} index The visual row index.
   * @returns {number}
   */ offsettedTH(index) {
        return index - this.countTH;
    }
    /**
   * @param {number} index The visual row index.
   * @returns {number}
   */ unOffsettedTH(index) {
        return index + this.countTH;
    }
    /**
   * @param {number} index The visual row index.
   * @returns {number}
   */ visibleColHeadedRowToSourceRow(index) {
        return this.renderedToSource(this.offsettedTH(index));
    }
    /**
   * @param {number} index The visual row index.
   * @returns {number}
   */ sourceRowToVisibleColHeadedRow(index) {
        return this.unOffsettedTH(this.sourceToRendered(index));
    }
    /**
   * @param {number} offset The scroll vertical offset.
   * @param {number} total The total height of the table.
   * @param {number} countTH The number of rendered column headers.
   */ constructor(offset, total, countTH){
        this.offset = offset;
        this.total = total;
        this.countTH = countTH;
    }
}
var __TURBOPACK__default__export__ = RowFilter;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/_base.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "Overlay",
    ()=>Overlay
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/console.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$clone$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/core/clone.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
;
;
;
;
;
class Overlay {
    /**
   * Checks if the overlay rendering state has changed.
   *
   * @returns {boolean}
   */ hasRenderingStateChanged() {
        return this.needFullRender !== this.shouldBeRendered();
    }
    /**
   * Updates internal state with an information about the need of full rendering of the overlay in the next draw cycles.
   *
   * If the state is changed to render the overlay, the `needFullRender` property is set to `true` which means that
   * the overlay will be fully rendered in the current draw cycle. If the state is changed to not render the overlay,
   * the `needFullRender` property is set to `false` which means that the overlay will be fully rendered in the
   * current draw cycle but it will not be rendered in the next draw cycles.
   *
   * @param {'before' | 'after'} drawPhase The phase of the rendering process.
   */ updateStateOfRendering(drawPhase) {
        if (drawPhase === 'before' && this.shouldBeRendered()) {
            this.needFullRender = true;
        } else if (drawPhase === 'after' && !this.shouldBeRendered()) {
            this.needFullRender = false;
        }
    }
    /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */ shouldBeRendered() {
        return true;
    }
    /**
   * Update the trimming container.
   */ updateTrimmingContainer() {
        this.trimmingContainer = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getTrimmingContainer"])(this.hider.parentNode?.parentNode ?? this.hider);
    }
    /**
   * Update the main scrollable element.
   */ updateMainScrollableElement() {
        const { wtTable } = this.wot;
        const { rootWindow } = this.domBindings;
        const computedOverflow = rootWindow.getComputedStyle(wtTable.wtRootElement.parentNode).getPropertyValue('overflow');
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        if (computedOverflow === 'hidden' || computedOverflow === 'clip') {
            this.mainTableScrollableElement = this.wot.wtTable.holder;
        } else if (preventOverflow === 'horizontal' && this.type === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TOP"] || preventOverflow === 'vertical' && this.type === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_INLINE_START"]) {
            this.mainTableScrollableElement = rootWindow;
        } else {
            this.mainTableScrollableElement = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollableElement"])(wtTable.TABLE);
        }
    }
    /**
   * Calculates coordinates of the provided element, relative to the root Handsontable element.
   * NOTE: The element needs to be a child of the overlay in order for the method to work correctly.
   *
   * @param {HTMLElement} element The cell element to calculate the position for.
   * @param {number} rowIndex Visual row index.
   * @param {number} columnIndex Visual column index.
   * @returns {{top: number, start: number}|undefined}
   */ getRelativeCellPosition(element, rowIndex, columnIndex) {
        if (!this.clone || this.clone.wtTable.holder.contains(element) === false) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])(`The provided element is not a child of the ${this.type} overlay`);
            return;
        }
        const windowScroll = this.mainTableScrollableElement === this.domBindings.rootWindow;
        const fixedColumnStart = columnIndex < this.wtSettings.getSetting('fixedColumnsStart');
        const fixedRowTop = rowIndex < this.wtSettings.getSetting('fixedRowsTop');
        const fixedRowBottom = rowIndex >= this.wtSettings.getSetting('totalRows') - this.wtSettings.getSetting('fixedRowsBottom');
        const spreader = this.clone.wtTable.spreader;
        const spreaderOffset = {
            start: this.getRelativeStartPosition(spreader),
            top: spreader.offsetTop
        };
        const elementOffset = {
            start: this.getRelativeStartPosition(element),
            top: element.offsetTop
        };
        let offsetObject = null;
        if (windowScroll) {
            offsetObject = this.getRelativeCellPositionWithinWindow(fixedRowTop, fixedColumnStart, elementOffset, spreaderOffset);
        } else {
            offsetObject = this.getRelativeCellPositionWithinHolder(fixedRowTop, fixedRowBottom, fixedColumnStart, elementOffset, spreaderOffset);
        }
        return offsetObject;
    }
    /**
   * Get inline start value depending of direction.
   *
   * @param {HTMLElement} el Element.
   * @returns {number}
   */ getRelativeStartPosition(el) {
        return this.isRtl() ? el.offsetParent.offsetWidth - el.offsetLeft - el.offsetWidth : el.offsetLeft;
    }
    /**
   * Calculates coordinates of the provided element, relative to the root Handsontable element within a table with window
   * as a scrollable element.
   *
   * @private
   * @param {boolean} onFixedRowTop `true` if the coordinates point to a place within the top fixed rows.
   * @param {boolean} onFixedColumn `true` if the coordinates point to a place within the fixed columns.
   * @param {number} elementOffset Offset position of the cell element.
   * @param {number} spreaderOffset Offset position of the spreader element.
   * @returns {{top: number, left: number}}
   */ getRelativeCellPositionWithinWindow(onFixedRowTop, onFixedColumn, elementOffset, spreaderOffset) {
        const absoluteRootElementPosition = this.wot.wtTable.wtRootElement.getBoundingClientRect(); // todo refactoring: DEMETER
        // `preventOverflow` can force this overlay onto the window (see `makeClone()`) while the
        // master still scrolls its holder. `wtRootElement` does not move with that scroll, so
        // subtract the master scroll from spreader-based offsets to align with the visible cell (#10403).
        const masterScrollsHolder = this.wot.wtOverlays.scrollableElement !== this.domBindings.rootWindow;
        const tableScrollPosition = {
            horizontal: masterScrollsHolder ? this.wot.wtOverlays.inlineStartOverlay.getScrollPosition() : 0,
            vertical: masterScrollsHolder ? this.wot.wtOverlays.topOverlay.getScrollPosition() : 0
        };
        let horizontalOffset = 0;
        let verticalOffset = 0;
        if (!onFixedColumn) {
            horizontalOffset = spreaderOffset.start - tableScrollPosition.horizontal;
        } else {
            let absoluteRootElementStartPosition = absoluteRootElementPosition.left;
            if (this.isRtl()) {
                absoluteRootElementStartPosition = this.domBindings.rootWindow.innerWidth - (absoluteRootElementPosition.left + absoluteRootElementPosition.width + (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])());
            }
            horizontalOffset = absoluteRootElementStartPosition <= 0 ? -1 * absoluteRootElementStartPosition : 0;
        }
        if (onFixedRowTop && this.clone) {
            const absoluteOverlayPosition = this.clone.wtTable.TABLE.getBoundingClientRect();
            verticalOffset = absoluteOverlayPosition.top - absoluteRootElementPosition.top;
        } else {
            verticalOffset = spreaderOffset.top - tableScrollPosition.vertical;
        }
        return {
            start: elementOffset.start + horizontalOffset,
            top: elementOffset.top + verticalOffset
        };
    }
    /**
   * Calculates coordinates of the provided element, relative to the root Handsontable element within a table with window
   * as a scrollable element.
   *
   * @private
   * @param {boolean} onFixedRowTop `true` if the coordinates point to a place within the top fixed rows.
   * @param {boolean} onFixedRowBottom `true` if the coordinates point to a place within the bottom fixed rows.
   * @param {boolean} onFixedColumn `true` if the coordinates point to a place within the fixed columns.
   * @param {number} elementOffset Offset position of the cell element.
   * @param {number} spreaderOffset Offset position of the spreader element.
   * @returns {{top: number, left: number}}
   */ getRelativeCellPositionWithinHolder(onFixedRowTop, onFixedRowBottom, onFixedColumn, elementOffset, spreaderOffset) {
        const tableScrollPosition = {
            horizontal: this.wot.wtOverlays.inlineStartOverlay.getScrollPosition(),
            vertical: this.wot.wtOverlays.topOverlay.getScrollPosition()
        };
        let horizontalOffset = 0;
        let verticalOffset = 0;
        if (!onFixedColumn) {
            horizontalOffset = tableScrollPosition.horizontal - spreaderOffset.start;
        }
        if (onFixedRowBottom && this.clone) {
            const absoluteRootElementPosition = this.wot.wtTable.wtRootElement.getBoundingClientRect(); // todo refactoring: DEMETER
            const absoluteOverlayPosition = this.clone.wtTable.TABLE.getBoundingClientRect(); // todo refactoring: DEMETER
            verticalOffset = absoluteOverlayPosition.top * -1 + absoluteRootElementPosition.top;
        } else if (!onFixedRowTop) {
            verticalOffset = tableScrollPosition.vertical - spreaderOffset.top;
        }
        return {
            start: elementOffset.start - horizontalOffset,
            top: elementOffset.top - verticalOffset
        };
    }
    /**
   * Make a clone of table for overlay.
   *
   * @returns {Clone}
   */ makeClone() {
        if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TYPES"].indexOf(this.type) === -1) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])(`Clone type "${this.type}" is not supported.`);
        }
        const { wtTable, wtSettings } = this.wot;
        const { rootDocument, rootWindow } = this.domBindings;
        const clone = rootDocument.createElement('div');
        const clonedTable = rootDocument.createElement('table');
        const tableParent = wtTable.wtRootElement.parentNode;
        if (!tableParent) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('The Walkontable root element has no parent node.');
        }
        clone.className = `${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_CLASS_NAMES"].get(this.type)} handsontable`;
        clone.setAttribute('dir', this.isRtl() ? 'rtl' : 'ltr');
        clone.style.position = 'absolute';
        clone.style.top = '0';
        clone.style.overflow = 'visible';
        if (this.isRtl()) {
            clone.style.right = '0';
        } else {
            clone.style.left = '0';
        }
        if (wtSettings.getSetting('ariaTags')) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(clone, [
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_PRESENTATION"])()
            ]);
        }
        clonedTable.className = wtTable.TABLE.className;
        // Clone the main table's `role` attribute to the cloned table.
        const mainTableRole = wtTable.TABLE.getAttribute('role');
        if (mainTableRole) {
            clonedTable.setAttribute('role', mainTableRole);
        }
        clone.appendChild(clonedTable);
        tableParent.appendChild(clone);
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        const computedOverflow = rootWindow.getComputedStyle(tableParent).getPropertyValue('overflow');
        if (computedOverflow === 'hidden' || computedOverflow === 'clip') {
            this.mainTableScrollableElement = wtTable.holder;
        } else if (preventOverflow === 'horizontal' && this.type === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TOP"] || preventOverflow === 'vertical' && this.type === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_INLINE_START"]) {
            this.mainTableScrollableElement = rootWindow;
        } else {
            this.mainTableScrollableElement = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollableElement"])(wtTable.TABLE);
        }
        // Create a new instance of the Walkontable class
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$core$2f$clone$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](clonedTable, this.wtSettings, {
            source: this.wot,
            overlay: this,
            viewport: this.wot.wtViewport,
            event: this.wot.wtEvent,
            selectionManager: this.wot.selectionManager
        });
    }
    /**
   * Refresh/Redraw overlay.
   *
   * @param {boolean} [fastDraw=false] When `true`, try to refresh only the positions of borders without rerendering
   *                                   the data. It will only work if Table.draw() does not force
   *                                   rendering anyway.
   */ refresh(fastDraw = false) {
        if (this.needFullRender && this.clone) {
            const cloneSource = this.clone.cloneSource;
            cloneSource.activeOverlayName = this.clone.wtTable.name;
            this.clone.draw(fastDraw);
            cloneSource.activeOverlayName = 'master';
        }
    }
    /**
   * Reset overlay styles to initial values.
   */ reset() {
        if (!this.clone) {
            return;
        }
        const holder = this.clone.wtTable.holder; // todo refactoring: DEMETER
        const hider = this.clone.wtTable.hider; // todo refactoring: DEMETER
        const holderStyle = holder.style;
        const hiderStyle = hider.style;
        const rootStyle = holder.parentNode.style;
        [
            holderStyle,
            hiderStyle,
            rootStyle
        ].forEach((style)=>{
            style.width = '';
            style.height = '';
        });
    }
    /**
   * Determine if Walkontable is running in RTL mode.
   *
   * @returns {boolean}
   */ isRtl() {
        return this.wtSettings.getSetting('rtlMode');
    }
    /**
   * Destroy overlay instance.
   */ destroy() {
        this.clone?.eventManager.destroy(); // todo check if it is good place for that operation
    }
    /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {CLONE_TYPES_ENUM} type The overlay type name (clone name).
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   */ constructor(wotInstance, facadeGetter, type, wtSettings, domBindings){
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["defineGetter"])(this, 'wot', wotInstance, {
            writable: false
        });
        this.domBindings = domBindings;
        this.facadeGetter = facadeGetter;
        this.wtSettings = wtSettings;
        const { TABLE, hider, spreader, holder, wtRootElement } = this.wot.wtTable; // todo ioc
        // legacy support, deprecated in the future
        this.instance = this.wot;
        this.type = type;
        this.TABLE = TABLE;
        this.hider = hider;
        this.spreader = spreader;
        this.holder = holder;
        this.wtRootElement = wtRootElement;
        this.trimmingContainer = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getTrimmingContainer"])(this.hider.parentNode?.parentNode ?? this.hider);
        this.needFullRender = this.shouldBeRendered();
        this.clone = this.makeClone();
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/bottom.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "BottomOverlay",
    ()=>BottomOverlay
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$bottom$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/bottom.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
;
;
;
class BottomOverlay extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["Overlay"] {
    /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {BottomOverlayTable}
   */ createTable(...args) {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$bottom$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](...args);
    }
    /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */ shouldBeRendered() {
        return this.wtSettings.getSetting('shouldRenderBottomOverlay');
    }
    /**
   * Updates the top overlay position.
   *
   * @returns {boolean}
   */ resetFixedPosition() {
        if (!this.needFullRender || !this.shouldBeRendered() || !this.wot.wtTable.holder.parentNode || !this.clone) {
            // removed from DOM
            return false;
        }
        const { rootWindow } = this.domBindings;
        const overlayRoot = this.clone.wtTable.holder.parentNode;
        overlayRoot.style.top = '';
        let overlayPosition = 0;
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        if (this.trimmingContainer === rootWindow && (!preventOverflow || preventOverflow !== 'vertical')) {
            overlayPosition = this.getOverlayOffset();
            // At non-integer zoom levels (e.g. 90%) the browser physically rounds each row's
            // border to the nearest physical pixel, causing the rendered TABLE to extend a
            // fractional CSS pixel past the holder's integer CSS height. Subtract this overflow
            // so the overlay sits flush against the actual table content instead of the
            // CSS-integer hider boundary.
            const masterTableRect = this.wot.wtTable.TABLE.getBoundingClientRect();
            const masterHolderRect = this.wot.wtTable.holder.getBoundingClientRect();
            const masterTableOverflow = Math.max(0, masterTableRect.bottom - masterHolderRect.bottom);
            overlayRoot.style.bottom = `${overlayPosition - masterTableOverflow}px`;
        } else {
            overlayPosition = this.getScrollPosition();
            this.repositionOverlay();
        }
        const positionChanged = this.adjustHeaderBordersPosition(overlayPosition);
        this.adjustElementsSize();
        return positionChanged;
    }
    /**
   * Updates the bottom overlay position.
   */ repositionOverlay() {
        if (!this.clone) {
            return;
        }
        const { wtTable, wtViewport } = this.wot;
        const { rootDocument } = this.domBindings;
        const cloneRoot = this.clone.wtTable.holder.parentNode;
        let bottomOffset = 0;
        if (!wtViewport.hasVerticalScroll()) {
            bottomOffset += wtViewport.getWorkspaceHeight() - wtTable.getTotalHeight();
        }
        if (wtViewport.hasVerticalScroll() && wtViewport.hasHorizontalScroll()) {
            bottomOffset += (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(rootDocument);
        }
        cloneRoot.style.bottom = `${bottomOffset}px`;
    }
    /**
   * Sets the main overlay's vertical scroll position.
   *
   * @param {number} pos The scroll position.
   * @returns {boolean}
   */ setScrollPosition(pos) {
        const { rootWindow } = this.domBindings;
        const scrollableElement = this.mainTableScrollableElement;
        const scrollEl = scrollableElement;
        const getScrollPosition = ()=>{
            return scrollableElement === rootWindow ? rootWindow.scrollY : scrollEl.scrollTop;
        };
        const setScrollPosition = (newPosition)=>{
            if (scrollableElement === rootWindow) {
                rootWindow.scrollTo(rootWindow.scrollX, newPosition);
            } else {
                scrollEl.scrollTop = newPosition;
            }
        };
        const oldScrollPosition = getScrollPosition();
        let result = false;
        if (pos !== oldScrollPosition) {
            setScrollPosition(pos);
            result = oldScrollPosition !== getScrollPosition();
        }
        return result;
    }
    /**
   * Triggers onScroll hook callback.
   */ onScroll() {
        this.wtSettings.getSetting('onScrollHorizontally');
    }
    /**
   * Calculates total sum cells height.
   *
   * @param {number} from Row index which calculates started from.
   * @param {number} to Row index where calculation is finished.
   * @returns {number} Height sum.
   */ sumCellSizes(from, to) {
        const { wtTable, wtSettings } = this.wot;
        const defaultRowHeight = wtSettings.getSetting('stylesHandler').getDefaultRowHeight();
        let row = from;
        let sum = 0;
        while(row < to){
            const height = wtTable.getRowHeight(row);
            sum += height === undefined ? defaultRowHeight : height;
            row += 1;
        }
        return sum;
    }
    /**
   * Adjust overlay root element, children and master table element sizes (width, height).
   */ adjustElementsSize() {
        this.updateTrimmingContainer();
        if (this.needFullRender) {
            this.adjustRootElementSize();
            this.adjustRootChildrenSize();
        }
    }
    /**
   * Adjust overlay root element size (width and height).
   */ adjustRootElementSize() {
        if (!this.clone) {
            return;
        }
        const { wtTable, wtViewport } = this.wot;
        const { rootDocument, rootWindow } = this.domBindings;
        const overlayRoot = this.clone.wtTable.holder.parentNode;
        const overlayRootStyle = overlayRoot.style;
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        if (this.trimmingContainer !== rootWindow || preventOverflow === 'horizontal') {
            let width = wtViewport.getWorkspaceWidth();
            if (wtViewport.hasVerticalScroll()) {
                width -= (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(rootDocument);
            }
            width = Math.min(width, wtTable.wtRootElement.scrollWidth);
            overlayRootStyle.width = `${width}px`;
        } else {
            overlayRootStyle.width = '';
        }
        this.clone.wtTable.holder.style.width = overlayRootStyle.width;
        let tableHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(this.clone.wtTable.TABLE);
        if (!wtTable.hasDefinedSize()) {
            tableHeight = 0;
        }
        overlayRootStyle.height = `${tableHeight}px`;
    }
    /**
   * Adjust overlay root childs size.
   */ adjustRootChildrenSize() {
        if (!this.clone) {
            return;
        }
        const { holder } = this.clone.wtTable;
        this.clone.wtTable.hider.style.width = this.hider.style.width;
        const holderParent = holder.parentNode;
        holder.style.width = holderParent.style.width;
        holder.style.height = holderParent.style.height;
    }
    /**
   * Adjust the overlay dimensions and position.
   */ applyToDOM() {
        const total = this.wtSettings.getSetting('totalRows');
        if (typeof this.wot.wtViewport.rowsRenderCalculator?.startPosition === 'number') {
            this.spreader.style.top = `${this.wot.wtViewport.rowsRenderCalculator.startPosition}px`;
        } else if (total === 0) {
            // can happen if there are 0 rows
            this.spreader.style.top = '0';
        } else {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('Incorrect value of the rowsRenderCalculator');
        }
        this.spreader.style.bottom = '';
        if (this.needFullRender) {
            this.syncOverlayOffset();
        }
    }
    /**
   * Synchronize calculated left position to an element.
   */ syncOverlayOffset() {
        if (!this.clone) {
            return;
        }
        const styleProperty = this.isRtl() ? 'right' : 'left';
        const { spreader } = this.clone.wtTable;
        if (typeof this.wot.wtViewport.columnsRenderCalculator?.startPosition === 'number') {
            spreader.style[styleProperty] = `${this.wot.wtViewport.columnsRenderCalculator.startPosition}px`;
        } else {
            spreader.style[styleProperty] = '';
        }
    }
    /**
   * Scrolls vertically to a row.
   *
   * @param {number} sourceRow Row index which you want to scroll to.
   * @param {boolean} [bottomEdge=false] If `true`, scrolls according to the bottom edge (top edge is by default).
   */ scrollTo(sourceRow, bottomEdge) {
        let newY = this.getTableParentOffset();
        const sourceInstance = this.wot.cloneSource ? this.wot.cloneSource : this.wot;
        const mainHolder = sourceInstance.wtTable.holder;
        let scrollbarCompensation = 0;
        if (bottomEdge && mainHolder.offsetHeight !== mainHolder.clientHeight) {
            scrollbarCompensation = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(this.domBindings.rootDocument);
        }
        if (bottomEdge) {
            newY += this.sumCellSizes(0, sourceRow + 1);
            newY -= this.wot.wtViewport.getViewportHeight();
            // Fix 1 pixel offset when cell is selected
            newY += 1;
        } else {
            newY += this.sumCellSizes(this.wtSettings.getSetting('fixedRowsBottom'), sourceRow);
        }
        newY += scrollbarCompensation;
        return this.setScrollPosition(newY);
    }
    /**
   * Gets table parent top position.
   *
   * @returns {number}
   */ getTableParentOffset() {
        if (this.mainTableScrollableElement === this.domBindings.rootWindow) {
            return this.wot.wtTable.holderOffset.top;
        }
        return 0;
    }
    /**
   * Gets the main overlay's vertical scroll position.
   *
   * @returns {number} Main table's vertical scroll position.
   */ getScrollPosition() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollTop"])(this.mainTableScrollableElement, this.domBindings.rootWindow);
    }
    /**
   * Gets the main overlay's vertical overlay offset.
   *
   * @returns {number} Main table's vertical overlay offset.
   */ getOverlayOffset() {
        const { rootWindow } = this.domBindings;
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        let overlayOffset = 0;
        if (this.trimmingContainer === rootWindow && (!preventOverflow || preventOverflow !== 'vertical') && this.clone) {
            const rootHeight = this.wot.wtTable.getTotalHeight();
            const overlayRootHeight = this.clone.wtTable.getTotalHeight();
            const maxOffset = rootHeight - overlayRootHeight;
            const docClientHeight = this.domBindings.rootDocument.documentElement.clientHeight;
            overlayOffset = Math.max(this.getTableParentOffset() - this.getScrollPosition() - docClientHeight + rootHeight, 0);
            if (overlayOffset > maxOffset) {
                overlayOffset = 0;
            }
        }
        return overlayOffset;
    }
    /**
   * Adds css classes to hide the header border's header (cell-selection border hiding issue).
   *
   * @param {number} position Header Y position if trimming container is window or scroll top if not.
   * @returns {boolean}
   */ adjustHeaderBordersPosition(position) {
        const fixedRowsBottom = this.wtSettings.getSetting('fixedRowsBottom');
        const areFixedRowsBottomChanged = this.cachedFixedRowsBottom !== fixedRowsBottom;
        const columnHeaders = this.wtSettings.getSetting('columnHeaders');
        let positionChanged = false;
        if ((areFixedRowsBottomChanged || fixedRowsBottom === 0) && columnHeaders.length > 0) {
            const masterParent = this.wot.wtTable.holder.parentNode;
            const previousState = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(masterParent, 'innerBorderBottom');
            this.cachedFixedRowsBottom = this.wtSettings.getSetting('fixedRowsBottom');
            if (position || this.wtSettings.getSetting('totalRows') === 0) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(masterParent, 'innerBorderBottom');
                positionChanged = !previousState;
            } else {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(masterParent, 'innerBorderBottom');
                positionChanged = previousState;
            }
        }
        return positionChanged;
    }
    /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   */ constructor(wotInstance, facadeGetter, wtSettings, domBindings){
        super(wotInstance, facadeGetter, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM"], wtSettings, domBindings), /**
   * Cached value which holds the previous value of the `fixedRowsBottom` option.
   * It is used as a comparison value that can be used to detect changes in that value.
   *
   * @type {number}
   */ this.cachedFixedRowsBottom = -1;
        this.cachedFixedRowsBottom = this.wtSettings.getSetting('fixedRowsBottom');
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/bottomInlineStartCorner.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "BottomInlineStartCornerOverlay",
    ()=>BottomInlineStartCornerOverlay
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$bottomInlineStartCorner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/bottomInlineStartCorner.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
;
;
;
;
class BottomInlineStartCornerOverlay extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["Overlay"] {
    /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {BottomInlineStartCornerOverlayTable}
   */ createTable(...args) {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$bottomInlineStartCorner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](...args);
    }
    /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */ shouldBeRendered() {
        return this.wtSettings.getSetting('shouldRenderBottomOverlay') && this.wtSettings.getSetting('shouldRenderInlineStartOverlay');
    }
    /**
   * Updates the corner overlay position.
   *
   * @returns {boolean}
   */ resetFixedPosition() {
        const { wot } = this;
        this.updateTrimmingContainer();
        const { clone } = this;
        if (!wot.wtTable.holder.parentNode || !clone) {
            // removed from DOM
            return false;
        }
        const overlayRoot = clone.wtTable.holder.parentNode;
        overlayRoot.style.top = '';
        if (this.trimmingContainer === this.domBindings.rootWindow) {
            const inlineStartOffset = this.inlineStartOverlay.getOverlayOffset();
            const masterTableRect = this.wot.wtTable.TABLE.getBoundingClientRect();
            const masterHolderRect = this.wot.wtTable.holder.getBoundingClientRect();
            const masterTableOverflow = Math.max(0, masterTableRect.bottom - masterHolderRect.bottom);
            const bottom = this.bottomOverlay.getOverlayOffset() - masterTableOverflow;
            overlayRoot.style[this.isRtl() ? 'right' : 'left'] = `${inlineStartOffset}px`;
            overlayRoot.style.bottom = `${bottom}px`;
        } else {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["resetCssTransform"])(overlayRoot);
            this.repositionOverlay();
        }
        let tableHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(clone.wtTable.TABLE);
        const tableWidth = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(clone.wtTable.TABLE);
        if (!this.wot.wtTable.hasDefinedSize()) {
            tableHeight = 0;
        }
        overlayRoot.style.height = `${tableHeight}px`;
        overlayRoot.style.width = `${tableWidth}px`;
        return true;
    }
    /**
   * Sets the scroll position (no-op for corner overlay).
   * @param {number} _pos The scroll position (unused).
   * @returns {boolean} Always returns false.
   */ setScrollPosition(_pos) {
        return false;
    }
    /**
   * Gets the scroll position (no-op for corner overlay).
   * @returns {number} Always returns 0.
   */ getScrollPosition() {
        return 0;
    }
    /**
   * Gets the table parent offset (no-op for corner overlay).
   * @returns {number} Always returns 0.
   */ getTableParentOffset() {
        return 0;
    }
    /**
   * Gets the overlay offset (no-op for corner overlay).
   * @returns {number} Always returns 0.
   */ getOverlayOffset() {
        return 0;
    }
    /**
   * Handles scroll events (no-op for corner overlay).
   */ onScroll() {}
    /**
   * Sums the size of cells between two indexes (no-op for corner overlay).
   * @param {number} _from The starting cell index (unused).
   * @param {number} _to The ending cell index (unused).
   * @returns {number} Always returns 0.
   */ sumCellSizes(_from, _to) {
        return 0;
    }
    /**
   * Adjusts the size of overlay elements (intentionally empty).
   */ adjustElementsSize() {}
    /**
   * Applies changes to the DOM (intentionally empty).
   */ applyToDOM() {}
    /**
   * Scrolls to a specified cell index (no-op for corner overlay).
   * @param {number} _sourceIndex The source row or column index (unused).
   * @param {boolean} _snapToEdge Whether to snap to the edge (unused).
   * @returns {boolean} Always returns false.
   */ scrollTo(_sourceIndex, _snapToEdge) {
        return false;
    }
    /**
   * Reposition the overlay.
   */ repositionOverlay() {
        if (!this.clone) {
            return;
        }
        const { wtTable, wtViewport } = this.wot;
        const { rootDocument } = this.domBindings;
        const cloneRoot = this.clone.wtTable.holder.parentNode;
        let bottomOffset = 0;
        if (!wtViewport.hasVerticalScroll()) {
            bottomOffset += wtViewport.getWorkspaceHeight() - wtTable.getTotalHeight();
        }
        if (wtViewport.hasVerticalScroll() && wtViewport.hasHorizontalScroll()) {
            bottomOffset += (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(rootDocument);
        }
        cloneRoot.style.bottom = `${bottomOffset}px`;
    }
    /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   * @param {BottomOverlay} bottomOverlay The instance of the Top overlay.
   * @param {InlineStartOverlay} inlineStartOverlay The instance of the InlineStart overlay.
   */ constructor(wotInstance, facadeGetter, wtSettings, domBindings, bottomOverlay, inlineStartOverlay){
        super(wotInstance, facadeGetter, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM_INLINE_START_CORNER"], wtSettings, domBindings);
        this.bottomOverlay = bottomOverlay;
        this.inlineStartOverlay = inlineStartOverlay;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @typedef {'top'|'bottom'|'inline_start'|'top_inline_start_corner'|'bottom_inline_start_corner'} CLONE_TYPES_ENUM
 */ __turbopack_context__.s([
    "CLONE_BOTTOM",
    ()=>CLONE_BOTTOM,
    "CLONE_BOTTOM_INLINE_START_CORNER",
    ()=>CLONE_BOTTOM_INLINE_START_CORNER,
    "CLONE_CLASS_NAMES",
    ()=>CLONE_CLASS_NAMES,
    "CLONE_INLINE_START",
    ()=>CLONE_INLINE_START,
    "CLONE_TOP",
    ()=>CLONE_TOP,
    "CLONE_TOP_INLINE_START_CORNER",
    ()=>CLONE_TOP_INLINE_START_CORNER,
    "CLONE_TYPES",
    ()=>CLONE_TYPES
]);
const CLONE_TOP = 'top';
const CLONE_BOTTOM = 'bottom';
const CLONE_INLINE_START = 'inline_start';
const CLONE_TOP_INLINE_START_CORNER = 'top_inline_start_corner';
const CLONE_BOTTOM_INLINE_START_CORNER = 'bottom_inline_start_corner';
const CLONE_TYPES = [
    CLONE_TOP,
    CLONE_BOTTOM,
    CLONE_INLINE_START,
    CLONE_TOP_INLINE_START_CORNER,
    CLONE_BOTTOM_INLINE_START_CORNER
];
const CLONE_CLASS_NAMES = new Map([
    [
        CLONE_TOP,
        `ht_clone_${CLONE_TOP}`
    ],
    [
        CLONE_BOTTOM,
        `ht_clone_${CLONE_BOTTOM}`
    ],
    [
        CLONE_INLINE_START,
        `ht_clone_${CLONE_INLINE_START} ht_clone_left`
    ],
    [
        CLONE_TOP_INLINE_START_CORNER,
        `ht_clone_${CLONE_TOP_INLINE_START_CORNER} ht_clone_top_left_corner`
    ],
    [
        CLONE_BOTTOM_INLINE_START_CORNER,
        `ht_clone_${CLONE_BOTTOM_INLINE_START_CORNER} ht_clone_bottom_left_corner`
    ]
]);
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/inlineStart.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "InlineStartOverlay",
    ()=>InlineStartOverlay
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$inlineStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/inlineStart.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$border$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/border/utils.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
;
;
;
;
class InlineStartOverlay extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["Overlay"] {
    /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {InlineStartOverlayTable}
   */ createTable(...args) {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$inlineStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](...args);
    }
    /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */ shouldBeRendered() {
        return this.wtSettings.getSetting('shouldRenderInlineStartOverlay');
    }
    /**
   * Updates the left overlay position.
   *
   * @returns {boolean}
   */ resetFixedPosition() {
        const { wtTable } = this.wot;
        if (!this.needFullRender || !this.shouldBeRendered() || !wtTable.holder.parentNode) {
            // removed from DOM
            return false;
        }
        if (!this.clone) {
            return false;
        }
        const { rootWindow } = this.domBindings;
        const overlayRoot = this.clone.wtTable.holder.parentNode;
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        let overlayPosition = 0;
        if (this.trimmingContainer === rootWindow && (!preventOverflow || preventOverflow !== 'horizontal')) {
            overlayPosition = this.getOverlayOffset() * (this.isRtl() ? -1 : 1);
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setOverlayPosition"])(overlayRoot, `${overlayPosition}px`, '0px');
        } else {
            overlayPosition = this.getScrollPosition();
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["resetCssTransform"])(overlayRoot);
        }
        const positionChanged = this.adjustHeaderBordersPosition(overlayPosition);
        this.adjustElementsSize();
        return positionChanged;
    }
    /**
   * Sets the main overlay's horizontal scroll position.
   *
   * @param {number} pos The scroll position.
   * @returns {boolean}
   */ setScrollPosition(pos) {
        const { rootWindow } = this.domBindings;
        const scrollableElement = this.mainTableScrollableElement;
        const scrollEl = scrollableElement;
        const getScrollPosition = ()=>{
            return scrollableElement === rootWindow ? rootWindow.scrollX : scrollEl.scrollLeft;
        };
        const setScrollPosition = (newPosition)=>{
            if (scrollableElement === rootWindow) {
                rootWindow.scrollTo(newPosition, rootWindow.scrollY);
            } else {
                scrollEl.scrollLeft = newPosition;
            }
        };
        if (this.isRtl()) {
            pos = -pos;
        }
        const oldScrollPosition = getScrollPosition();
        let result = false;
        if (pos !== oldScrollPosition) {
            setScrollPosition(pos);
            result = oldScrollPosition !== getScrollPosition();
        }
        return result;
    }
    /**
   * Triggers onScroll hook callback.
   */ onScroll() {
        this.wtSettings.getSetting('onScrollVertically');
    }
    /**
   * Calculates total sum cells width.
   *
   * @param {number} from Column index which calculates started from.
   * @param {number} to Column index where calculation is finished.
   * @returns {number} Width sum.
   */ sumCellSizes(from, to) {
        const defaultColumnWidth = this.wtSettings.getSetting('defaultColumnWidth');
        let column = from;
        let sum = 0;
        while(column < to){
            sum += this.wot.wtTable.getColumnWidth(column) || defaultColumnWidth;
            column += 1;
        }
        return sum;
    }
    /**
   * Adjust overlay root element, children and master table element sizes (width, height).
   */ adjustElementsSize() {
        this.updateTrimmingContainer();
        if (this.needFullRender) {
            this.adjustRootElementSize();
            this.adjustRootChildrenSize();
        }
    }
    /**
   * Adjust overlay root element size (width and height).
   */ adjustRootElementSize() {
        if (!this.clone) {
            return;
        }
        const { wtTable, wtViewport } = this.wot;
        const { rootDocument, rootWindow } = this.domBindings;
        const overlayRoot = this.clone.wtTable.holder.parentNode;
        const overlayRootStyle = overlayRoot.style;
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        if (this.trimmingContainer !== rootWindow || preventOverflow === 'vertical') {
            let height = wtViewport.getWorkspaceHeight();
            if (wtViewport.hasHorizontalScroll()) {
                // Match the master holder's actual inner height instead of subtracting a rounded scrollbar
                // width. `clientHeight` natively accounts for the horizontal scrollbar at the browser's
                // sub-pixel accuracy; under fractional browser zoom a rounded `getScrollbarWidth()` diverges
                // from the real scrollbar size, giving the frozen overlay a different vertical scroll range
                // than the master. That mismatch clamps the overlay's scrollTop ~1px short at the bottom and
                // shifts the frozen rows out of alignment (#12632).
                const masterClientHeight = wtTable.holder.clientHeight;
                height = masterClientHeight > 0 ? masterClientHeight : height - (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(rootDocument);
            }
            height = Math.min(height, wtTable.wtRootElement.scrollHeight);
            overlayRootStyle.height = `${height}px`;
        } else {
            overlayRootStyle.height = '';
        }
        this.clone.wtTable.holder.style.height = overlayRootStyle.height;
        const tableWidth = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(this.clone.wtTable.TABLE);
        overlayRootStyle.width = `${tableWidth}px`;
    }
    /**
   * Adjust overlay root childs size.
   */ adjustRootChildrenSize() {
        if (!this.clone) {
            return;
        }
        const { holder } = this.clone.wtTable;
        const cornerStyle = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$border$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getCornerStyle"])(this.wot);
        const selectionCornerOffset = this.wot.selectionManager.getFocusSelection() ? parseInt(cornerStyle.width, 10) / 2 : 0;
        this.clone.wtTable.hider.style.height = this.hider.style.height;
        const holderParent = holder.parentNode;
        holder.style.height = holderParent.style.height;
        // Add selection corner protruding part to the holder total width to make sure that
        // borders' corner won't be cut after horizontal scroll (#6937).
        holder.style.width = `${parseInt(holderParent.style.width, 10) + selectionCornerOffset}px`;
    }
    /**
   * Adjust the overlay dimensions and position.
   */ applyToDOM() {
        const total = this.wtSettings.getSetting('totalColumns');
        const styleProperty = this.isRtl() ? 'right' : 'left';
        if (typeof this.wot.wtViewport.columnsRenderCalculator?.startPosition === 'number') {
            this.spreader.style[styleProperty] = `${this.wot.wtViewport.columnsRenderCalculator.startPosition}px`;
        } else if (total === 0) {
            this.spreader.style[styleProperty] = '0';
        } else {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('Incorrect value of the columnsRenderCalculator');
        }
        if (this.isRtl()) {
            this.spreader.style.left = '';
        } else {
            this.spreader.style.right = '';
        }
        if (this.needFullRender) {
            this.syncOverlayOffset();
        }
    }
    /**
   * Synchronize calculated top position to an element.
   */ syncOverlayOffset() {
        if (!this.clone) {
            return;
        }
        if (typeof this.wot.wtViewport.rowsRenderCalculator?.startPosition === 'number') {
            this.clone.wtTable.spreader.style.top = `${this.wot.wtViewport.rowsRenderCalculator.startPosition}px`;
        } else {
            this.clone.wtTable.spreader.style.top = '';
        }
    }
    /**
   * Scrolls horizontally to a column at the left edge of the viewport.
   *
   * @param {number} sourceCol  Column index which you want to scroll to.
   * @param {boolean} [beyondRendered]  If `true`, scrolls according to the right
   *                                    edge (left edge is by default).
   * @returns {boolean}
   */ scrollTo(sourceCol, beyondRendered) {
        const { wtSettings } = this;
        const rowHeaders = wtSettings.getSetting('rowHeaders');
        const fixedColumnsStart = wtSettings.getSetting('fixedColumnsStart');
        const sourceInstance = this.wot.cloneSource ? this.wot.cloneSource : this.wot;
        const mainHolder = sourceInstance.wtTable.holder;
        const rowHeaderBorderCompensation = fixedColumnsStart === 0 && rowHeaders.length > 0 && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(mainHolder.parentNode, 'innerBorderInlineStart') ? 1 : 0;
        let newX = this.getTableParentOffset();
        let scrollbarCompensation = 0;
        if (beyondRendered) {
            const columnWidth = this.wot.wtTable.getColumnWidth(sourceCol);
            const viewportWidth = this.wot.wtViewport.getViewportWidth();
            if (columnWidth > viewportWidth) {
                beyondRendered = false;
            }
        }
        if (beyondRendered && mainHolder.offsetWidth !== mainHolder.clientWidth) {
            scrollbarCompensation = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(this.domBindings.rootDocument);
        }
        if (beyondRendered) {
            newX += this.sumCellSizes(0, sourceCol + 1);
            newX -= this.wot.wtViewport.getViewportWidth();
            // Compensate for the right header border if scrolled from the absolute left.
            newX += rowHeaderBorderCompensation;
        } else {
            newX += this.sumCellSizes(this.wtSettings.getSetting('fixedColumnsStart'), sourceCol);
        }
        newX += scrollbarCompensation;
        // If the table is scrolled all the way left when starting the scroll and going to be scrolled to the far right,
        // we need to compensate for the potential header border width.
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getMaximumScrollLeft"])(this.mainTableScrollableElement) === newX - rowHeaderBorderCompensation && rowHeaderBorderCompensation > 0) {
            this.wot.wtOverlays.expandHiderHorizontallyBy(rowHeaderBorderCompensation);
        }
        return this.setScrollPosition(newX);
    }
    /**
   * Gets table parent left position.
   *
   * @returns {number}
   */ getTableParentOffset() {
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        let offset = 0;
        if (!preventOverflow && this.trimmingContainer === this.domBindings.rootWindow) {
            offset = this.wot.wtTable.holderOffset.left;
        }
        return offset;
    }
    /**
   * Gets the main overlay's horizontal scroll position.
   *
   * @returns {number} Main table's horizontal scroll position.
   */ getScrollPosition() {
        return Math.abs((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollLeft"])(this.mainTableScrollableElement, this.domBindings.rootWindow));
    }
    /**
   * Gets the main overlay's horizontal overlay offset.
   *
   * @returns {number} Main table's horizontal overlay offset.
   */ getOverlayOffset() {
        const { rootWindow } = this.domBindings;
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        let overlayOffset = 0;
        if (this.trimmingContainer === rootWindow && (!preventOverflow || preventOverflow !== 'horizontal')) {
            if (this.isRtl()) {
                overlayOffset = Math.abs(Math.min(this.getTableParentOffset() - this.getScrollPosition(), 0));
            } else {
                overlayOffset = Math.max(this.getScrollPosition() - this.getTableParentOffset(), 0);
            }
            const rootWidth = this.wot.wtTable.getTotalWidth();
            const overlayRootWidth = this.clone ? this.clone.wtTable.getTotalWidth() : 0;
            const maxOffset = rootWidth - overlayRootWidth;
            if (overlayOffset > maxOffset) {
                overlayOffset = 0;
            }
        }
        return overlayOffset;
    }
    /**
   * Adds css classes to hide the header border's header (cell-selection border hiding issue).
   *
   * @param {number} position Header X position if trimming container is window or scroll top if not.
   * @returns {boolean}
   */ adjustHeaderBordersPosition(position) {
        const { wtSettings } = this;
        const masterParent = this.wot.wtTable.holder.parentNode;
        const rowHeaders = wtSettings.getSetting('rowHeaders');
        const fixedColumnsStart = wtSettings.getSetting('fixedColumnsStart');
        const totalRows = wtSettings.getSetting('totalRows');
        const preventVerticalOverflow = wtSettings.getSetting('preventOverflow') === 'vertical';
        if (totalRows) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(masterParent, 'emptyRows');
        } else {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(masterParent, 'emptyRows');
        }
        let positionChanged = false;
        if (!preventVerticalOverflow) {
            if (fixedColumnsStart && !rowHeaders.length) {
                // "innerBorderLeft" is for backward compatibility
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(masterParent, 'innerBorderLeft innerBorderInlineStart');
            } else if (!fixedColumnsStart && rowHeaders.length) {
                const previousState = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(masterParent, 'innerBorderInlineStart');
                if (position) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(masterParent, 'innerBorderLeft innerBorderInlineStart');
                    positionChanged = !previousState;
                } else {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(masterParent, 'innerBorderLeft innerBorderInlineStart');
                    positionChanged = previousState;
                }
            }
        }
        return positionChanged;
    }
    /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   */ constructor(wotInstance, facadeGetter, wtSettings, domBindings){
        super(wotInstance, facadeGetter, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_INLINE_START"], wtSettings, domBindings);
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/top.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "TopOverlay",
    ()=>TopOverlay
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$top$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/top.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$border$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/border/utils.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
;
;
;
;
class TopOverlay extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["Overlay"] {
    /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {TopOverlayTable}
   */ createTable(...args) {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$top$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](...args);
    }
    /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */ shouldBeRendered() {
        return this.wtSettings.getSetting('shouldRenderTopOverlay');
    }
    /**
   * Updates the top overlay position.
   *
   * @returns {boolean}
   */ resetFixedPosition() {
        if (!this.needFullRender || !this.shouldBeRendered() || !this.wot.wtTable.holder.parentNode || !this.clone) {
            // removed from DOM
            return false;
        }
        const overlayRoot = this.clone.wtTable.holder.parentNode;
        const { rootWindow } = this.domBindings;
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        let overlayPosition = 0;
        let skipInnerBorderAdjusting = false;
        if (this.trimmingContainer === rootWindow && (!preventOverflow || preventOverflow !== 'vertical')) {
            const { wtTable } = this.wot;
            const hiderRect = wtTable.hider.getBoundingClientRect();
            const bottom = Math.ceil(hiderRect.bottom);
            const rootHeight = overlayRoot.offsetHeight;
            // This checks if the overlay is going to an infinite loop caused by added (or removed)
            // `innerBorderTop` class name. Toggling the class name shifts the viewport by 1px and
            // triggers the `scroll` event. It causes the table to render. The new render cycle takes into,
            // account the shift and toggles the class name again. This causes the next loops. This
            // happens only on Chrome (#7256).
            //
            // When we detect that the table bottom position is the same as the overlay bottom,
            // do not toggle the class name.
            //
            // This workaround will be able to be cleared after merging the SVG borders, which introduces
            // frozen lines (no more `innerBorderTop` workaround).
            skipInnerBorderAdjusting = bottom === rootHeight;
            overlayPosition = this.getOverlayOffset();
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setOverlayPosition"])(overlayRoot, '0px', `${overlayPosition}px`);
        } else {
            overlayPosition = this.getScrollPosition();
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["resetCssTransform"])(overlayRoot);
        }
        const positionChanged = this.adjustHeaderBordersPosition(overlayPosition, skipInnerBorderAdjusting);
        this.adjustElementsSize();
        return positionChanged;
    }
    /**
   * Sets the main overlay's vertical scroll position.
   *
   * @param {number} pos The scroll position.
   * @returns {boolean}
   */ setScrollPosition(pos) {
        const { rootWindow } = this.domBindings;
        const scrollableElement = this.mainTableScrollableElement;
        const scrollEl = scrollableElement;
        const getScrollPosition = ()=>{
            return scrollableElement === rootWindow ? rootWindow.scrollY : scrollEl.scrollTop;
        };
        const setScrollPosition = (newPosition)=>{
            if (scrollableElement === rootWindow) {
                rootWindow.scrollTo(rootWindow.scrollX, newPosition);
            } else {
                scrollEl.scrollTop = newPosition;
            }
        };
        const oldScrollPosition = getScrollPosition();
        let result = false;
        if (pos !== oldScrollPosition) {
            setScrollPosition(pos);
            result = oldScrollPosition !== getScrollPosition();
        }
        return result;
    }
    /**
   * Triggers onScroll hook callback.
   */ onScroll() {
        this.wtSettings.getSetting('onScrollHorizontally');
    }
    /**
   * Calculates total sum cells height.
   *
   * @param {number} from Row index which calculates started from.
   * @param {number} to Row index where calculation is finished.
   * @returns {number} Height sum.
   */ sumCellSizes(from, to) {
        const stylesHandler = this.wtSettings.getSetting('stylesHandler');
        const defaultRowHeight = stylesHandler.getDefaultRowHeight();
        let row = from;
        let sum = 0;
        while(row < to){
            const height = this.wot.wtTable.getRowHeight(row);
            sum += height === undefined ? defaultRowHeight : height;
            row += 1;
        }
        return sum;
    }
    /**
   * Adjust overlay root element, children and master table element sizes (width, height).
   */ adjustElementsSize() {
        this.updateTrimmingContainer();
        if (this.needFullRender) {
            this.adjustRootElementSize();
            this.adjustRootChildrenSize();
        }
    }
    /**
   * Adjust overlay root element size (width and height).
   */ adjustRootElementSize() {
        if (!this.clone) {
            return;
        }
        const { wtTable, wtViewport } = this.wot;
        const { rootDocument, rootWindow } = this.domBindings;
        const overlayRoot = this.clone.wtTable.holder.parentNode;
        const overlayRootStyle = overlayRoot.style;
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        if (this.trimmingContainer !== rootWindow || preventOverflow === 'horizontal') {
            let width = wtViewport.getWorkspaceWidth();
            if (wtViewport.hasVerticalScroll()) {
                width -= (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(rootDocument);
            }
            width = Math.min(width, wtTable.wtRootElement.scrollWidth);
            overlayRootStyle.width = `${width}px`;
        } else {
            overlayRootStyle.width = '';
        }
        this.clone.wtTable.holder.style.width = overlayRootStyle.width;
        let tableHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(this.clone.wtTable.TABLE);
        if (!wtTable.hasDefinedSize()) {
            tableHeight = 0;
        }
        overlayRootStyle.height = `${tableHeight}px`;
    }
    /**
   * Adjust overlay root childs size.
   */ adjustRootChildrenSize() {
        if (!this.clone) {
            return;
        }
        const { holder } = this.clone.wtTable;
        const cornerStyle = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$border$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getCornerStyle"])(this.wot);
        const selectionCornerOffset = this.wot.selectionManager.getFocusSelection() ? parseInt(cornerStyle.height, 10) / 2 : 0;
        this.clone.wtTable.hider.style.width = this.hider.style.width;
        const holderParent = holder.parentNode;
        holder.style.width = holderParent.style.width;
        // Add selection corner protruding part to the holder total height to make sure that
        // borders' corner won't be cut after vertical scroll (#6937).
        holder.style.height = `${parseInt(holderParent.style.height, 10) + selectionCornerOffset}px`;
    }
    /**
   * Adjust the overlay dimensions and position.
   */ applyToDOM() {
        const total = this.wtSettings.getSetting('totalRows') ?? 0;
        if (typeof this.wot.wtViewport.rowsRenderCalculator?.startPosition === 'number') {
            this.spreader.style.top = `${this.wot.wtViewport.rowsRenderCalculator.startPosition}px`;
        } else if (total === 0) {
            // can happen if there are 0 rows
            this.spreader.style.top = '0';
        } else {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('Incorrect value of the rowsRenderCalculator');
        }
        this.spreader.style.bottom = '';
        if (this.needFullRender) {
            this.syncOverlayOffset();
        }
    }
    /**
   * Synchronize calculated left position to an element.
   */ syncOverlayOffset() {
        if (!this.clone) {
            return;
        }
        const styleProperty = this.isRtl() ? 'right' : 'left';
        const { spreader } = this.clone.wtTable;
        if (typeof this.wot.wtViewport.columnsRenderCalculator?.startPosition === 'number') {
            spreader.style[styleProperty] = `${this.wot.wtViewport.columnsRenderCalculator.startPosition}px`;
        } else {
            spreader.style[styleProperty] = '';
        }
    }
    /**
   * Scrolls vertically to a row.
   *
   * @param {number} sourceRow Row index which you want to scroll to.
   * @param {boolean} [bottomEdge] If `true`, scrolls according to the bottom edge (top edge is by default).
   * @returns {boolean}
   */ scrollTo(sourceRow, bottomEdge) {
        const { wot, wtSettings } = this;
        const sourceInstance = wot.cloneSource ? wot.cloneSource : wot;
        const mainHolder = sourceInstance.wtTable.holder;
        const columnHeaders = wtSettings.getSetting('columnHeaders');
        const fixedRowsTop = wtSettings.getSetting('fixedRowsTop');
        const columnHeaderBorderCompensation = fixedRowsTop === 0 && columnHeaders.length > 0 && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(mainHolder.parentNode, 'innerBorderTop') ? 1 : 0;
        let newY = this.getTableParentOffset();
        let scrollbarCompensation = 0;
        if (bottomEdge) {
            const rowHeight = this.wot.wtTable.getRowHeight(sourceRow) ?? 0;
            const viewportHeight = this.wot.wtViewport.getViewportHeight();
            if (rowHeight > viewportHeight) {
                bottomEdge = false;
            }
        }
        if (bottomEdge && mainHolder.offsetHeight !== mainHolder.clientHeight) {
            scrollbarCompensation = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(this.domBindings.rootDocument);
        }
        if (bottomEdge) {
            const fixedRowsBottom = wtSettings.getSetting('fixedRowsBottom');
            const totalRows = wtSettings.getSetting('totalRows');
            newY += this.sumCellSizes(0, sourceRow + 1);
            newY -= wot.wtViewport.getViewportHeight() - this.sumCellSizes(totalRows - fixedRowsBottom, totalRows);
            // Fix 1 pixel offset when cell is selected
            newY += 1;
            // Compensate for the bottom header border if scrolled from the absolute top.
            newY += columnHeaderBorderCompensation;
        } else {
            newY += this.sumCellSizes(wtSettings.getSetting('fixedRowsTop'), sourceRow);
        }
        newY += scrollbarCompensation;
        // If the table is scrolled all the way up when starting the scroll and going to be scrolled to the bottom,
        // we need to compensate for the potential header bottom border height.
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getMaximumScrollTop"])(this.mainTableScrollableElement) === newY - columnHeaderBorderCompensation && columnHeaderBorderCompensation > 0) {
            this.wot.wtOverlays.expandHiderVerticallyBy(columnHeaderBorderCompensation);
        }
        return this.setScrollPosition(newY);
    }
    /**
   * Gets table parent top position.
   *
   * @returns {number}
   */ getTableParentOffset() {
        if (this.mainTableScrollableElement === this.domBindings.rootWindow) {
            return this.wot.wtTable.holderOffset.top;
        }
        return 0;
    }
    /**
   * Gets the main overlay's vertical scroll position.
   *
   * @returns {number} Main table's vertical scroll position.
   */ getScrollPosition() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollTop"])(this.mainTableScrollableElement, this.domBindings.rootWindow);
    }
    /**
   * Gets the main overlay's vertical overlay offset.
   *
   * @returns {number} Main table's vertical overlay offset.
   */ getOverlayOffset() {
        const { rootWindow } = this.domBindings;
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        let overlayOffset = 0;
        if (this.trimmingContainer === rootWindow && (!preventOverflow || preventOverflow !== 'vertical')) {
            const rootHeight = this.wot.wtTable.getTotalHeight();
            const overlayRootHeight = this.clone ? this.clone.wtTable.getTotalHeight() : 0;
            const maxOffset = rootHeight - overlayRootHeight;
            overlayOffset = Math.max(this.getScrollPosition() - this.getTableParentOffset(), 0);
            if (overlayOffset > maxOffset) {
                overlayOffset = 0;
            }
        }
        return overlayOffset;
    }
    /**
   * Adds css classes to hide the header border's header (cell-selection border hiding issue).
   *
   * @param {number} position Header Y position if trimming container is window or scroll top if not.
   * @param {boolean} [skipInnerBorderAdjusting=false] If `true` the inner border adjusting will be skipped.
   * @returns {boolean}
   */ adjustHeaderBordersPosition(position, skipInnerBorderAdjusting = false) {
        const { wtSettings } = this;
        const masterParent = this.wot.wtTable.holder.parentNode;
        const totalColumns = wtSettings.getSetting('totalColumns') ?? 0;
        const preventHorizontalOverflow = wtSettings.getSetting('preventOverflow') === 'horizontal';
        if (totalColumns) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(masterParent, 'emptyColumns');
        } else {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(masterParent, 'emptyColumns');
        }
        let positionChanged = false;
        if (!skipInnerBorderAdjusting && !preventHorizontalOverflow) {
            const fixedRowsTop = wtSettings.getSetting('fixedRowsTop');
            const areFixedRowsTopChanged = this.cachedFixedRowsTop !== fixedRowsTop;
            const columnHeaders = wtSettings.getSetting('columnHeaders');
            if ((areFixedRowsTopChanged || fixedRowsTop === 0) && columnHeaders.length > 0) {
                const previousState = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(masterParent, 'innerBorderTop');
                this.cachedFixedRowsTop = wtSettings.getSetting('fixedRowsTop');
                if (position || wtSettings.getSetting('totalRows') === 0) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(masterParent, 'innerBorderTop');
                    positionChanged = !previousState;
                } else {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(masterParent, 'innerBorderTop');
                    positionChanged = previousState;
                }
            }
        }
        return positionChanged;
    }
    /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   */ constructor(wotInstance, facadeGetter, wtSettings, domBindings){
        super(wotInstance, facadeGetter, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TOP"], wtSettings, domBindings), /**
   * Cached value which holds the previous value of the `fixedRowsTop` option.
   * It is used as a comparison value that can be used to detect changes in this value.
   *
   * @type {number}
   */ this.cachedFixedRowsTop = -1;
        this.cachedFixedRowsTop = this.wtSettings.getSetting('fixedRowsTop');
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/topInlineStartCorner.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "TopInlineStartCornerOverlay",
    ()=>TopInlineStartCornerOverlay
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$topInlineStartCorner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/topInlineStartCorner.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
;
;
;
;
class TopInlineStartCornerOverlay extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["Overlay"] {
    /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {TopInlineStartCornerOverlayTable}
   */ createTable(...args) {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$topInlineStartCorner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](...args);
    }
    /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */ shouldBeRendered() {
        return this.wtSettings.getSetting('shouldRenderTopOverlay') && this.wtSettings.getSetting('shouldRenderInlineStartOverlay');
    }
    /**
   * Prevents scroll position changes for this overlay.
   * @param {number} _pos The scroll position to set (ignored).
   * @returns {boolean} Always returns false.
   */ setScrollPosition(_pos) {
        return false;
    }
    /**
   * Gets the scroll position for this overlay.
   * @returns {number} Always returns 0.
   */ getScrollPosition() {
        return 0;
    }
    /**
   * Gets the parent table offset for this overlay.
   * @returns {number} Always returns 0.
   */ getTableParentOffset() {
        return 0;
    }
    /**
   * Gets the overlay offset for this corner overlay.
   * @returns {number} Always returns 0.
   */ getOverlayOffset() {
        return 0;
    }
    /**
   * Handles scroll events (intentionally empty for corner overlay).
   */ onScroll() {}
    /**
   * Sums cell sizes within a range.
   * @param {number} _from The starting cell index (ignored).
   * @param {number} _to The ending cell index (ignored).
   * @returns {number} Always returns 0.
   */ sumCellSizes(_from, _to) {
        return 0;
    }
    /**
   * Adjusts overlay element sizes (intentionally empty for corner overlay).
   */ adjustElementsSize() {}
    /**
   * Applies changes to the DOM (intentionally empty for corner overlay).
   */ applyToDOM() {}
    /**
   * Scrolls the overlay to a specified position.
   * @param {number} _sourceIndex The source index to scroll to (ignored).
   * @param {boolean} _snapToEdge Whether to snap to edge (ignored).
   * @returns {boolean} Always returns false.
   */ scrollTo(_sourceIndex, _snapToEdge) {
        return false;
    }
    /**
   * Updates the corner overlay position.
   *
   * @returns {boolean}
   */ resetFixedPosition() {
        this.updateTrimmingContainer();
        if (!this.wot.wtTable.holder.parentNode || !this.clone) {
            // removed from DOM
            return false;
        }
        const overlayRoot = this.clone.wtTable.holder.parentNode;
        if (this.trimmingContainer === this.domBindings.rootWindow) {
            const left = this.inlineStartOverlay.getOverlayOffset() * (this.isRtl() ? -1 : 1);
            const top = this.topOverlay.getOverlayOffset();
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setOverlayPosition"])(overlayRoot, `${left}px`, `${top}px`);
        } else {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["resetCssTransform"])(overlayRoot);
        }
        let tableHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(this.clone.wtTable.TABLE);
        const tableWidth = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(this.clone.wtTable.TABLE);
        if (!this.wot.wtTable.hasDefinedSize()) {
            tableHeight = 0;
        }
        overlayRoot.style.height = `${tableHeight}px`;
        overlayRoot.style.width = `${tableWidth}px`;
        return true;
    }
    /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   * @param {TopOverlay} topOverlay The instance of the Top overlay.
   * @param {InlineStartOverlay} inlineStartOverlay The instance of the InlineStart overlay.
   */ constructor(wotInstance, facadeGetter, wtSettings, domBindings, topOverlay, inlineStartOverlay){
        super(wotInstance, facadeGetter, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TOP_INLINE_START_CORNER"], wtSettings, domBindings);
        this.topOverlay = topOverlay;
        this.inlineStartOverlay = inlineStartOverlay;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/overlays.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$feature$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/feature.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$function$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/function.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$unicode$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/unicode.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/console.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$inlineStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/inlineStart.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$top$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/top.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$topInlineStartCorner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/topInlineStartCorner.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$bottom$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/bottom.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$bottomInlineStartCorner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/bottomInlineStartCorner.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$stickyScrollStrategy$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/stickyScrollStrategy.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
/**
 * Type predicate that checks whether a WheelEvent carries the legacy
 * non-standard `wheelDeltaX`/`wheelDeltaY` properties emitted by older browsers.
 *
 * @param {WheelEvent} event The wheel event to test.
 * @returns {boolean}
 */ function isWheelEventWithLegacyDelta(event) {
    return 'wheelDeltaY' in event || 'wheelDeltaX' in event;
}
;
;
;
;
;
;
;
;
var /**
   * An array of the all overlays.
   *
   * @type {Overlay[]}
   */ _overlays = /*#__PURE__*/ new WeakMap(), /**
   * Indicates whether the rendering state has changed for one of the overlays.
   *
   * @type {boolean}
   */ _hasRenderingStateChanged = /*#__PURE__*/ new WeakMap(), /**
   * Cached vertical scroll position used to deduplicate `onScrollVertically` callbacks.
   * Tracks the value returned by `topOverlay.getScrollPosition()` at the time the callback
   * was last fired so that a second call with an unchanged position is suppressed.
   *
   * @type {number | null}
   */ _lastVerticalScrollPositionForCallback = /*#__PURE__*/ new WeakMap(), /**
   * Cached horizontal scroll position used to deduplicate `onScrollHorizontally` callbacks.
   * Tracks the value returned by `inlineStartOverlay.getScrollPosition()` at the time the
   * callback was last fired so that a second call with an unchanged position is suppressed.
   *
   * @type {number | null}
   */ _lastHorizontalScrollPositionForCallback = /*#__PURE__*/ new WeakMap(), /**
   * The amount of times the ResizeObserver callback was fired in direct succession.
   *
   * @type {number}
   */ _containerDomResizeCount = /*#__PURE__*/ new WeakMap(), /**
   * The timeout ID for the ResizeObserver endless-loop-blocking logic.
   *
   * @type {number}
   */ _containerDomResizeCountTimeout = /*#__PURE__*/ new WeakMap(), /**
   * Debounced `updateLastSpreaderSize` / `adjustElementsSize` used during scroll so rapid
   * `refresh` calls do not repeat layout work every frame.
   *
   * @type {Function}
   */ _postponedAdjustElementsSize = /*#__PURE__*/ new WeakMap(), /**
   * Strategy that manages the sticky-scroll optimization during native
   * scrollbar drag. Extracted as a separate class to isolate the sticky
   * positioning lifecycle from the overlay coordinator.
   *
   * @type {StickyScrollStrategy}
   */ _stickyScroll = /*#__PURE__*/ new WeakMap(), /**
   * Caches the initial vertical and horizontal scroll positions for callback deduplication.
   */ _cacheScrollCallbackPositions = /*#__PURE__*/ new WeakSet(), /**
   * Checks whether the vertical scroll position has changed since the last `onScrollVertically`
   * callback and updates the cache. Returns `true` when the callback should fire.
   *
   * @returns {boolean}
   */ _didVerticalScrollPositionChange = /*#__PURE__*/ new WeakSet(), /**
   * Checks whether the horizontal scroll position has changed since the last `onScrollHorizontally`
   * callback and updates the cache. Returns `true` when the callback should fire.
   *
   * @returns {boolean}
   */ _didHorizontalScrollPositionChange = /*#__PURE__*/ new WeakSet(), /**
   * Adjust the elements size if needed.
   */ _adjustElementsSizeIfNeeded = /*#__PURE__*/ new WeakSet();
/**
 * @class Overlays
 */ class Overlays {
    /**
   * Get the list of references to all overlays.
   *
   * @param {boolean} [includeMaster = false] If set to `true`, the list will contain the master table as the last
   * element.
   * @returns {(TopOverlay|TopInlineStartCornerOverlay|InlineStartOverlay|BottomOverlay|BottomInlineStartCornerOverlay)[]}
   */ getOverlays(includeMaster = false) {
        const overlays = [
            ..._class_private_field_get(this, _overlays)
        ];
        if (includeMaster) {
            overlays.push(this.wtTable);
        }
        return overlays;
    }
    /**
   * Retrieve browser line height and apply its value to `browserLineHeight`.
   *
   * @private
   */ initBrowserLineHeight() {
        const { rootWindow, rootDocument } = this.domBindings;
        const computedStyle = rootWindow.getComputedStyle(rootDocument.body);
        /**
     * Sometimes `line-height` might be set to 'normal'. In that case, a default `font-size` should be multiplied by roughly 1.2.
     * Https://developer.mozilla.org/pl/docs/Web/CSS/line-height#Values.
     */ const lineHeight = parseInt(computedStyle.lineHeight, 10);
        const lineHeightFalback = parseInt(computedStyle.fontSize, 10) * 1.2;
        this.browserLineHeight = lineHeight || lineHeightFalback;
    }
    /**
   * Prepare overlays based on user settings.
   *
   * @private
   */ initOverlays() {
        const args = [
            this.wot,
            this.facadeGetter,
            this.wtSettings,
            this.domBindings
        ];
        // todo refactoring: IOC, collection or factories.
        // TODO refactoring, conceive about using generic collection of overlays.
        this.topOverlay = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$top$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["TopOverlay"](args[0], args[1], args[2], args[3]);
        this.bottomOverlay = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$bottom$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BottomOverlay"](args[0], args[1], args[2], args[3]);
        this.inlineStartOverlay = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$inlineStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["InlineStartOverlay"](args[0], args[1], args[2], args[3]);
        // TODO discuss, the controversial here would be removing the lazy creation mechanism for corners.
        // TODO cond. Has no any visual impact. They're initially hidden in same way like left, top, and bottom overlays.
        this.topInlineStartCornerOverlay = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$topInlineStartCorner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["TopInlineStartCornerOverlay"](args[0], args[1], args[2], args[3], this.topOverlay, this.inlineStartOverlay);
        this.bottomInlineStartCornerOverlay = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$bottomInlineStartCorner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BottomInlineStartCornerOverlay"](args[0], args[1], args[2], args[3], this.bottomOverlay, this.inlineStartOverlay);
        _class_private_field_set(this, _overlays, [
            this.topOverlay,
            this.bottomOverlay,
            this.inlineStartOverlay,
            this.topInlineStartCornerOverlay,
            this.bottomInlineStartCornerOverlay
        ]);
    }
    /**
   * Runs logic for the overlays before the table is drawn.
   */ beforeDraw() {
        _class_private_field_set(this, _hasRenderingStateChanged, _class_private_field_get(this, _overlays).reduce((acc, overlay)=>{
            return overlay.hasRenderingStateChanged() || acc;
        }, false));
        _class_private_field_get(this, _overlays).forEach((overlay)=>overlay.updateStateOfRendering('before'));
    }
    /**
   * Runs logic for the overlays after the table is drawn.
   */ afterDraw() {
        this.syncScrollWithMaster();
        _class_private_field_get(this, _overlays).forEach((overlay)=>{
            const hasRenderingStateChanged = overlay.hasRenderingStateChanged();
            overlay.updateStateOfRendering('after');
            if (hasRenderingStateChanged && !overlay.needFullRender) {
                overlay.reset();
            }
        });
    }
    /**
   * Refresh and redraw table.
   */ refreshAll() {
        if (!this.wot.drawn) {
            return;
        }
        if (!this.wtTable.holder.parentNode) {
            // Walkontable was detached from DOM, but this handler was not removed
            this.destroy();
            return;
        }
        this.wot.draw(true);
        if (this.verticalScrolling && _class_private_method_get(this, _didVerticalScrollPositionChange, didVerticalScrollPositionChange).call(this)) {
            this.inlineStartOverlay.onScroll(); // todo the inlineStartOverlay.onScroll() fires hook. Why is it needed there, not in any another place?
        }
        if (this.horizontalScrolling && _class_private_method_get(this, _didHorizontalScrollPositionChange, didHorizontalScrollPositionChange).call(this)) {
            this.topOverlay.onScroll();
        }
        this.verticalScrolling = false;
        this.horizontalScrolling = false;
    }
    /**
   * Register all necessary event listeners.
   */ registerListeners() {
        const { rootDocument, rootWindow } = this.domBindings;
        const { mainTableScrollableElement: topOverlayScrollableElement } = this.topOverlay;
        const { mainTableScrollableElement: inlineStartOverlayScrollableElement } = this.inlineStartOverlay;
        this.eventManager.addEventListener(rootDocument.documentElement, 'keydown', (event)=>this.onKeyDown(event));
        this.eventManager.addEventListener(rootDocument.documentElement, 'keyup', ()=>this.onKeyUp());
        this.eventManager.addEventListener(rootDocument, 'visibilitychange', ()=>this.onKeyUp());
        _class_private_field_get(this, _stickyScroll).registerListeners();
        this.eventManager.addEventListener(topOverlayScrollableElement, 'scroll', (event)=>this.onTableScroll(event), {
            passive: true
        });
        if (topOverlayScrollableElement !== inlineStartOverlayScrollableElement) {
            this.eventManager.addEventListener(inlineStartOverlayScrollableElement, 'scroll', (event)=>this.onTableScroll(event), {
                passive: true
            });
        }
        const isScrollOnWindow = this.scrollableElement === rootWindow;
        const preventWheel = this.wtSettings.getSetting('preventWheel');
        const wheelEventOptions = {
            passive: isScrollOnWindow
        };
        this.eventManager.addEventListener(this.wtTable.wtRootElement, 'wheel', (event)=>this.onCloneWheel(event, preventWheel), wheelEventOptions);
        const overlays = [
            this.topOverlay,
            this.bottomOverlay,
            this.inlineStartOverlay,
            this.topInlineStartCornerOverlay,
            this.bottomInlineStartCornerOverlay
        ];
        overlays.forEach((overlay)=>{
            if (!overlay.clone) {
                return;
            }
            this.eventManager.addEventListener(overlay.clone.wtTable.holder, 'wheel', (event)=>this.onCloneWheel(event, preventWheel), wheelEventOptions);
        });
        let resizeTimeout;
        this.eventManager.addEventListener(rootWindow, 'resize', ()=>{
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$feature$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["requestAnimationFrame"])(()=>{
                clearTimeout(resizeTimeout);
                this.wtSettings.getSetting('onWindowResize');
                resizeTimeout = setTimeout(()=>{
                    // Remove resizing the window from the ResizeObserver's endless-loop-blocking logic.
                    _class_private_field_set(this, _containerDomResizeCount, 0);
                }, 200);
            });
        });
        if (!isScrollOnWindow) {
            if (this.wtTable.wtRootElement.parentElement) {
                this.resizeObserver.observe(this.wtTable.wtRootElement.parentElement);
            }
        }
    }
    /**
   * Scroll listener.
   *
   * @param {Event} event The mouse event object.
   */ onTableScroll(event) {
        // There was if statement which controlled flow of this function. It avoided the execution of the next lines
        // on mobile devices. It was changed. Broader description of this case is included within issue #4856.
        const rootWindow = this.domBindings.rootWindow;
        const masterHorizontal = this.inlineStartOverlay.mainTableScrollableElement;
        const masterVertical = this.topOverlay.mainTableScrollableElement;
        const target = event.target;
        // For key press, sync only master -> overlay position because while pressing Walkontable.render is triggered
        // by hot.refreshBorder
        if (this.keyPressed) {
            if (masterVertical !== rootWindow && target !== rootWindow && !(masterVertical instanceof HTMLElement && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event).contains(masterVertical)) || masterHorizontal !== rootWindow && target !== rootWindow && !(masterHorizontal instanceof HTMLElement && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["eventTargetEl"])(event).contains(masterHorizontal))) {
                return;
            }
        }
        this.syncScrollPositions();
    }
    /**
   * Wheel listener for cloned overlays.
   *
   * @param {Event} event The mouse event object.
   * @param {boolean} preventDefault If `true`, the `preventDefault` will be called on event object.
   */ onCloneWheel(event, preventDefault) {
        // Fix for Windows OS, where the ctrl key is used to zoom the page (issue #dev-2405).
        if (event.ctrlKey) {
            return;
        }
        const { rootWindow } = this.domBindings;
        // There was if statement which controlled flow of this function. It avoided the execution of the next lines
        // on mobile devices. It was changed. Broader description of this case is included within issue #4856.
        const masterHorizontal = this.inlineStartOverlay.mainTableScrollableElement;
        const masterVertical = this.topOverlay.mainTableScrollableElement;
        const target = event.target;
        // For key press, sync only master -> overlay position because while pressing Walkontable.render is triggered
        // by hot.refreshBorder
        const shouldNotWheelVertically = masterVertical !== rootWindow && target !== rootWindow && !(target instanceof Node && masterVertical instanceof HTMLElement && target.contains(masterVertical));
        const shouldNotWheelHorizontally = masterHorizontal !== rootWindow && target !== rootWindow && !(target instanceof Node && masterHorizontal instanceof HTMLElement && target.contains(masterHorizontal));
        if (this.keyPressed && (shouldNotWheelVertically || shouldNotWheelHorizontally) || this.scrollableElement === rootWindow) {
            return;
        }
        const isScrollPossible = this.translateMouseWheelToScroll(event);
        if (preventDefault || this.scrollableElement !== rootWindow && isScrollPossible) {
            event.preventDefault();
        }
    }
    /**
   * Key down listener.
   *
   * @param {Event} event The keyboard event object.
   */ onKeyDown(event) {
        this.keyPressed = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$unicode$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isKey"])(event.keyCode, 'ARROW_UP|ARROW_RIGHT|ARROW_DOWN|ARROW_LEFT');
    }
    /**
   * Key up listener.
   */ onKeyUp() {
        this.keyPressed = false;
    }
    /**
   * Translate wheel event into scroll event and sync scroll overlays position.
   *
   * @private
   * @param {Event} event The mouse event object.
   * @returns {boolean}
   */ translateMouseWheelToScroll(event) {
        let deltaY;
        let deltaX;
        if (isWheelEventWithLegacyDelta(event)) {
            deltaY = isNaN(event.deltaY) ? -1 * (event.wheelDeltaY ?? 0) : event.deltaY;
            deltaX = isNaN(event.deltaX) ? -1 * (event.wheelDeltaX ?? 0) : event.deltaX;
        } else {
            deltaY = event.deltaY;
            deltaX = event.deltaX;
        }
        if (event.deltaMode === 1) {
            deltaX += deltaX * this.browserLineHeight;
            deltaY += deltaY * this.browserLineHeight;
        }
        const isScrollVerticallyPossible = this.scrollVertically(deltaY);
        const isScrollHorizontallyPossible = this.scrollHorizontally(deltaX);
        return isScrollVerticallyPossible || isScrollHorizontallyPossible;
    }
    /**
   * Scrolls main scrollable element horizontally.
   *
   * @param {number} delta Relative value to scroll.
   * @returns {boolean}
   */ scrollVertically(delta) {
        if (!(this.scrollableElement instanceof HTMLElement)) {
            return false;
        }
        const el = this.scrollableElement;
        const previousScroll = el.scrollTop;
        el.scrollTop += delta;
        return previousScroll !== el.scrollTop;
    }
    /**
   * Scrolls main scrollable element horizontally.
   *
   * @param {number} delta Relative value to scroll.
   * @returns {boolean}
   */ scrollHorizontally(delta) {
        if (!(this.scrollableElement instanceof HTMLElement)) {
            return false;
        }
        const el = this.scrollableElement;
        const previousScroll = el.scrollLeft;
        el.scrollLeft += delta;
        return previousScroll !== el.scrollLeft;
    }
    /**
   * Synchronize scroll position between master table and overlay table.
   *
   * @private
   */ syncScrollPositions() {
        if (this.destroyed) {
            return;
        }
        const topHolder = this.topOverlay.clone?.wtTable.holder; // todo rethink
        const leftHolder = this.inlineStartOverlay.clone?.wtTable.holder; // todo rethink
        const preventOverflow = this.wtSettings.getSetting('preventOverflow');
        let scrollX = this.scrollableElement instanceof HTMLElement ? this.scrollableElement.scrollLeft : 0;
        let scrollY = this.scrollableElement instanceof HTMLElement ? this.scrollableElement.scrollTop : 0;
        if (this.wot.wtViewport.isHorizontallyScrollableByWindow() && (typeof preventOverflow === 'boolean' && preventOverflow || preventOverflow !== 'horizontal') && this.scrollableElement instanceof Window) {
            scrollX = this.scrollableElement.scrollX;
        }
        if (this.wot.wtViewport.isVerticallyScrollableByWindow() && (typeof preventOverflow === 'boolean' && preventOverflow || preventOverflow !== 'vertical') && this.scrollableElement instanceof Window) {
            scrollY = this.scrollableElement.scrollY;
        }
        this.horizontalScrolling = this.lastScrollX !== scrollX;
        this.verticalScrolling = this.lastScrollY !== scrollY;
        this.lastScrollX = scrollX;
        this.lastScrollY = scrollY;
        _class_private_field_get(this, _stickyScroll).tryActivate(this.verticalScrolling, this.horizontalScrolling);
        if (this.horizontalScrolling) {
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(topHolder)) {
                topHolder.scrollLeft = scrollX;
            }
            const bottomHolder = this.bottomOverlay.needFullRender ? this.bottomOverlay.clone?.wtTable.holder : null; // todo rethink
            if (bottomHolder) {
                bottomHolder.scrollLeft = scrollX;
            }
        }
        if (this.verticalScrolling) {
            // In window-scroll mode the left overlay's row positions are driven by
            // spreader.style.top; the holder must not accumulate scroll offset.
            // Setting scrollTop to window.scrollY would be capped to the tiny
            // hider/holder size difference caused by fractional zoom rounding,
            // shifting the visible rows and misaligning them with the master table.
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(leftHolder)) {
                if (this.wot.wtViewport.isVerticallyScrollableByWindow()) {
                    leftHolder.scrollTop = 0;
                } else {
                    leftHolder.scrollTop = scrollY;
                }
            }
        }
        this.refreshAll();
        _class_private_field_get(this, _stickyScroll).syncOffsets();
    }
    /**
   * Synchronize overlay scrollbars with the master scrollbar.
   */ syncScrollWithMaster() {
        if (!_class_private_field_get(this, _hasRenderingStateChanged)) {
            return;
        }
        const masterScrollable = this.topOverlay.mainTableScrollableElement;
        if (!(masterScrollable instanceof HTMLElement)) {
            return;
        }
        const { scrollLeft, scrollTop } = masterScrollable;
        if (this.topOverlay.needFullRender && this.topOverlay.clone) {
            this.topOverlay.clone.wtTable.holder.scrollLeft = scrollLeft; // todo rethink, *overlay.setScroll*()
        }
        if (this.bottomOverlay.needFullRender && this.bottomOverlay.clone) {
            this.bottomOverlay.clone.wtTable.holder.scrollLeft = scrollLeft; // todo rethink, *overlay.setScroll*()
        }
        if (this.inlineStartOverlay.needFullRender && this.inlineStartOverlay.clone) {
            this.inlineStartOverlay.clone.wtTable.holder.scrollTop = scrollTop; // todo rethink, *overlay.setScroll*()
        }
        _class_private_field_set(this, _hasRenderingStateChanged, false);
    }
    /**
   * Update the main scrollable elements for all the overlays.
   */ updateMainScrollableElements() {
        this.eventManager.clearEvents(true);
        this.inlineStartOverlay.updateMainScrollableElement();
        this.topOverlay.updateMainScrollableElement();
        if (this.bottomOverlay.needFullRender) {
            this.bottomOverlay.updateMainScrollableElement();
        }
        const { wtTable } = this;
        const { rootWindow } = this.domBindings;
        const tableParentNode = wtTable.wtRootElement.parentNode;
        // Use nodeType === 1 instead of instanceof Element so the check works across realms (iframes).
        // Falls back to '' when there is no element parent (null or detached).
        const computedOverflow = tableParentNode !== null && tableParentNode.nodeType === 1 ? rootWindow.getComputedStyle(tableParentNode).getPropertyValue('overflow') : '';
        if (computedOverflow === 'hidden' || computedOverflow === 'clip') {
            this.scrollableElement = wtTable.holder;
        } else {
            this.scrollableElement = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollableElement"])(wtTable.TABLE);
        }
        this.registerListeners();
    }
    /**
   *
   */ destroy() {
        _class_private_field_get(this, _postponedAdjustElementsSize).cancel();
        if (_class_private_field_get(this, _containerDomResizeCountTimeout) !== null) {
            clearTimeout(_class_private_field_get(this, _containerDomResizeCountTimeout));
            _class_private_field_set(this, _containerDomResizeCountTimeout, null);
        }
        this.resizeObserver.disconnect();
        _class_private_field_get(this, _stickyScroll).destroy();
        this.eventManager.destroy();
        // todo, probably all below `destroy` calls has no sense. To analyze
        this.topOverlay.destroy();
        if (this.bottomOverlay.clone) {
            this.bottomOverlay.destroy();
        }
        this.inlineStartOverlay.destroy();
        if (this.topInlineStartCornerOverlay) {
            this.topInlineStartCornerOverlay.destroy();
        }
        if (this.bottomInlineStartCornerOverlay && this.bottomInlineStartCornerOverlay.clone) {
            this.bottomInlineStartCornerOverlay.destroy();
        }
        this.destroyed = true;
    }
    /**
   * @param {boolean} [fastDraw=false] When `true`, try to refresh only the positions of borders without rerendering
   *                                   the data. It will only work if Table.draw() does not force
   *                                   rendering anyway.
   */ refresh(fastDraw = false) {
        const isScrollTriggered = this.verticalScrolling || this.horizontalScrolling;
        if (isScrollTriggered) {
            _class_private_field_get(this, _postponedAdjustElementsSize).call(this);
        } else {
            _class_private_method_get(this, _adjustElementsSizeIfNeeded, adjustElementsSizeIfNeeded).call(this);
        }
        if (this.bottomOverlay.clone) {
            this.bottomOverlay.refresh(fastDraw);
        }
        this.inlineStartOverlay.refresh(fastDraw);
        this.topOverlay.refresh(fastDraw);
        if (this.topInlineStartCornerOverlay) {
            this.topInlineStartCornerOverlay.refresh(fastDraw);
        }
        if (this.bottomInlineStartCornerOverlay && this.bottomInlineStartCornerOverlay.clone) {
            this.bottomInlineStartCornerOverlay.refresh(fastDraw);
        }
    }
    /**
   * Update the last cached spreader size with the current size.
   *
   * @returns {boolean} `true` if the lastSpreaderSize cache was updated, `false` otherwise.
   */ updateLastSpreaderSize() {
        const spreader = this.wtTable.spreader;
        const width = spreader.clientWidth;
        const height = spreader.clientHeight;
        const needsUpdating = width !== this.spreaderLastSize.width || height !== this.spreaderLastSize.height;
        if (needsUpdating) {
            this.spreaderLastSize.width = width;
            this.spreaderLastSize.height = height;
        }
        return needsUpdating;
    }
    /**
   * Adjust overlays elements size and master table size.
   */ adjustElementsSize() {
        const { wtViewport } = this.wot;
        const { wtTable } = this;
        const { rootWindow } = this.domBindings;
        const isWindowScrolled = this.scrollableElement === rootWindow;
        const totalColumns = this.wtSettings.getSetting('totalColumns');
        const totalRows = this.wtSettings.getSetting('totalRows');
        const headerRowSize = wtViewport.getRowHeaderWidth();
        const headerColumnSize = wtViewport.getColumnHeaderHeight();
        // The internal row height calculator contains a known issue that results in a 1px miscalculation.
        // Ideally, this should be addressed at the core level. However, resolving it is non-trivial,
        // as the flaw is embedded across multiple core modules and corresponding test cases.
        // This limitation does not affect when the external calculator is used (AutoRowSize), which
        // computes heights accurately, so no adjustment is required when using it.
        const hiderHeightComp = this.wtSettings.getSetting('externalRowCalculator') ? 0 : 1;
        const proposedHiderHeight = headerColumnSize + this.topOverlay.sumCellSizes(0, totalRows) + hiderHeightComp;
        const proposedHiderWidth = headerRowSize + this.inlineStartOverlay.sumCellSizes(0, totalColumns);
        const hiderElement = wtTable.hider;
        const hiderStyle = hiderElement.style;
        const isScrolledBeyondHiderHeight = ()=>{
            if (isWindowScrolled || !(this.scrollableElement instanceof HTMLElement)) {
                return false;
            }
            return this.scrollableElement.scrollTop > Math.max(0, proposedHiderHeight - wtTable.holder.clientHeight);
        };
        const isScrolledBeyondHiderWidth = ()=>{
            if (isWindowScrolled || !(this.scrollableElement instanceof HTMLElement)) {
                return false;
            }
            return this.scrollableElement.scrollLeft > Math.max(0, proposedHiderWidth - wtTable.holder.clientWidth);
        };
        const columnHeaderBorderCompensation = isScrolledBeyondHiderHeight() ? 1 : 0;
        const rowHeaderBorderCompensation = isScrolledBeyondHiderWidth() ? 1 : 0;
        // If the elements are being adjusted after scrolling the table from the very beginning to the very end,
        // we need to adjust the hider dimensions by the header border size. (https://github.com/handsontable/dev-handsontable/issues/1772)
        hiderStyle.width = `${proposedHiderWidth + rowHeaderBorderCompensation}px`;
        hiderStyle.height = `${proposedHiderHeight + columnHeaderBorderCompensation}px`;
        this.topOverlay.adjustElementsSize();
        this.inlineStartOverlay.adjustElementsSize();
        this.bottomOverlay.adjustElementsSize();
    }
    /**
   * Expand the hider vertically element by the provided delta value.
   *
   * @param {number} heightDelta The delta value to expand the hider element by.
   */ expandHiderVerticallyBy(heightDelta) {
        const { wtTable } = this;
        wtTable.hider.style.height = `${parseInt(wtTable.hider.style.height, 10) + heightDelta}px`;
    }
    /**
   * Expand the hider horizontally element by the provided delta value.
   *
   * @param {number} widthDelta The delta value to expand the hider element by.
   */ expandHiderHorizontallyBy(widthDelta) {
        const { wtTable } = this;
        wtTable.hider.style.width = `${parseInt(wtTable.hider.style.width, 10) + widthDelta}px`;
    }
    /**
   *
   */ applyToDOM() {
        if (!this.wtTable.isVisible()) {
            return;
        }
        this.topOverlay.applyToDOM();
        if (this.bottomOverlay.clone) {
            this.bottomOverlay.applyToDOM();
        }
        this.inlineStartOverlay.applyToDOM();
        _class_private_field_get(this, _stickyScroll).syncOffsets();
    }
    /**
   * Get the parent overlay of the provided element.
   *
   * @param {HTMLElement} element An element to process.
   * @returns {WalkontableInstance|null}
   */ getParentOverlay(element) {
        if (!element) {
            return null;
        }
        const overlays = [
            this.topOverlay,
            this.inlineStartOverlay,
            this.bottomOverlay,
            this.topInlineStartCornerOverlay,
            this.bottomInlineStartCornerOverlay
        ];
        let result = null;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(overlays, (overlay)=>{
            if (!overlay) {
                return;
            }
            if (overlay.clone && overlay.clone.wtTable.TABLE.contains(element)) {
                result = overlay.clone;
            }
        });
        return result;
    }
    /**
   * Synchronize the class names between the main overlay table and the tables on the other overlays.
   *
   */ syncOverlayTableClassNames() {
        const masterTable = this.wtTable.TABLE;
        const overlays = [
            this.topOverlay,
            this.inlineStartOverlay,
            this.bottomOverlay,
            this.topInlineStartCornerOverlay,
            this.bottomInlineStartCornerOverlay
        ];
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(overlays, (elem)=>{
            if (!elem) {
                return;
            }
            if (elem.clone) {
                elem.clone.wtTable.TABLE.className = masterTable.className; // todo demeter
            }
        });
    }
    /**
   * @param {Walkontable} wotInstance The Walkontable instance. @todo refactoring remove.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {EventManager} eventManager The walkontable event manager.
   * @param {MasterTable} wtTable The master table.
   */ constructor(wotInstance, facadeGetter, domBindings, wtSettings, eventManager, wtTable){
        _class_private_method_init(this, _cacheScrollCallbackPositions);
        _class_private_method_init(this, _didVerticalScrollPositionChange);
        _class_private_method_init(this, _didHorizontalScrollPositionChange);
        _class_private_method_init(this, _adjustElementsSizeIfNeeded);
        _class_private_field_init(this, _overlays, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _hasRenderingStateChanged, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _lastVerticalScrollPositionForCallback, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _lastHorizontalScrollPositionForCallback, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _containerDomResizeCount, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _containerDomResizeCountTimeout, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _postponedAdjustElementsSize, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _stickyScroll, {
            writable: true,
            value: void 0
        });
        /**
   * The width of the scrollbar.
   *
   * @protected
   * @type {number}
   */ this.scrollbarSize = 0;
        /**
   * Flag indicating whether the overlay has been destroyed.
   *
   * @protected
   * @type {boolean}
   */ this.destroyed = false;
        /**
   * Flag indicating whether a key is currently pressed.
   *
   * @protected
   * @type {boolean}
   */ this.keyPressed = false;
        /**
   * The last cached spreader size.
   *
   * @protected
   * @type {object}
   */ this.spreaderLastSize = {
            width: null,
            height: null
        };
        /**
   * Flag indicating whether the table is being scrolled vertically.
   *
   * @protected
   * @type {boolean}
   */ this.verticalScrolling = false;
        /**
   * Flag indicating whether the table is being scrolled horizontally.
   *
   * @protected
   * @type {boolean}
   */ this.horizontalScrolling = false;
        /**
   * The last horizontal scroll position.
   *
   * @protected
   * @type {number}
   */ this.lastScrollX = 0;
        /**
   * The last vertical scroll position.
   *
   * @protected
   * @type {number}
   */ this.lastScrollY = 0;
        _class_private_field_set(this, _overlays, []);
        _class_private_field_set(this, _hasRenderingStateChanged, false);
        _class_private_field_set(this, _lastVerticalScrollPositionForCallback, null);
        _class_private_field_set(this, _lastHorizontalScrollPositionForCallback, null);
        _class_private_field_set(this, _containerDomResizeCount, 0);
        _class_private_field_set(this, _containerDomResizeCountTimeout, null);
        _class_private_field_set(this, _postponedAdjustElementsSize, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$function$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["debounce"])(_class_private_method_get(this, _adjustElementsSizeIfNeeded, adjustElementsSizeIfNeeded).bind(this), 200));
        _class_private_field_set(this, _stickyScroll, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$stickyScrollStrategy$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["StickyScrollStrategy"](this));
        /**
   * The instance of the ResizeObserver that observes the size of the Walkontable wrapper element.
   * In case of the size change detection the `onContainerElementResize` is fired.
   *
   * @private
   * @type {ResizeObserver}
   */ this.resizeObserver = new ResizeObserver((entries)=>{
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$feature$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["requestAnimationFrame"])(()=>{
                if (!Array.isArray(entries) || !entries.length) {
                    return;
                }
                _class_private_field_set(this, _containerDomResizeCount, _class_private_field_get(this, _containerDomResizeCount) + 1);
                if (_class_private_field_get(this, _containerDomResizeCount) === 300) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])('The ResizeObserver callback was fired too many times in direct succession.' + '\nThis may be due to an infinite loop caused by setting a dynamic height/width (for example, ' + 'with the `dvh` units) to a Handsontable container\'s parent. ' + '\nThe observer will be disconnected.');
                    this.resizeObserver.disconnect();
                }
                // This logic is required to prevent an endless loop of the ResizeObserver callback.
                // https://github.com/handsontable/dev-handsontable/issues/1898#issuecomment-2154794817
                if (_class_private_field_get(this, _containerDomResizeCountTimeout) !== null) {
                    clearTimeout(_class_private_field_get(this, _containerDomResizeCountTimeout));
                }
                _class_private_field_set(this, _containerDomResizeCountTimeout, setTimeout(()=>{
                    _class_private_field_set(this, _containerDomResizeCount, 0);
                }, 100));
                this.wtSettings.getSetting('onContainerElementResize');
            });
        });
        this.wot = wotInstance;
        this.wtSettings = wtSettings;
        this.domBindings = domBindings;
        this.facadeGetter = facadeGetter;
        this.wtTable = wtTable;
        const { rootDocument, rootWindow } = this.domBindings;
        // legacy support
        this.instance = this.wot; // todo refactoring: move to facade
        this.eventManager = eventManager;
        // TODO refactoring: probably invalid place to this logic
        this.scrollbarSize = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(rootDocument);
        const rootElementParent = wtTable.wtRootElement.parentNode;
        // Use nodeType === 1 instead of instanceof Element so the check works across realms (iframes).
        // Falls back to getScrollableElement when there is no element parent (null or detached).
        const isOverflowClip = rootElementParent !== null && rootElementParent.nodeType === 1 && (()=>{
            const overflow = rootWindow.getComputedStyle(rootElementParent).getPropertyValue('overflow');
            return overflow === 'hidden' || overflow === 'clip';
        })();
        this.scrollableElement = isOverflowClip ? wtTable.holder : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollableElement"])(wtTable.TABLE);
        this.initOverlays();
        _class_private_method_get(this, _cacheScrollCallbackPositions, cacheScrollCallbackPositions).call(this);
        this.destroyed = false;
        this.keyPressed = false;
        this.spreaderLastSize = {
            width: null,
            height: null
        };
        this.verticalScrolling = false;
        this.horizontalScrolling = false;
        this.initBrowserLineHeight();
        this.registerListeners();
        this.lastScrollX = rootWindow.scrollX;
        this.lastScrollY = rootWindow.scrollY;
    }
}
function cacheScrollCallbackPositions() {
    _class_private_field_set(this, _lastVerticalScrollPositionForCallback, this.topOverlay.getScrollPosition());
    _class_private_field_set(this, _lastHorizontalScrollPositionForCallback, this.inlineStartOverlay.getScrollPosition());
}
function didVerticalScrollPositionChange() {
    const current = this.topOverlay.getScrollPosition();
    if (_class_private_field_get(this, _lastVerticalScrollPositionForCallback) === current) {
        return false;
    }
    _class_private_field_set(this, _lastVerticalScrollPositionForCallback, current);
    return true;
}
function didHorizontalScrollPositionChange() {
    const current = this.inlineStartOverlay.getScrollPosition();
    if (_class_private_field_get(this, _lastHorizontalScrollPositionForCallback) === current) {
        return false;
    }
    _class_private_field_set(this, _lastHorizontalScrollPositionForCallback, current);
    return true;
}
function adjustElementsSizeIfNeeded() {
    if (this.destroyed) {
        return;
    }
    const wasSpreaderSizeUpdated = this.updateLastSpreaderSize();
    if (wasSpreaderSizeUpdated) {
        this.adjustElementsSize();
    }
}
var __TURBOPACK__default__export__ = Overlays;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/_base.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "BaseRenderer",
    ()=>BaseRenderer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$nodesPool$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/nodesPool.mjs [client] (ecmascript)");
;
class BaseRenderer {
    /**
   * Sets the table renderer instance to the current renderer.
   *
   * @param {TableRenderer} table The TableRenderer instance.
   */ setTable(table) {
        if (this.nodesPool) {
            this.nodesPool.setRootDocument(table.rootDocument);
        }
        this.table = table;
    }
    /**
   * Renders the contents to the elements.
   */ render() {}
    /**
   * Creates a new BaseRenderer instance.
   *
   * @param {string | null} nodeType - The node type which the renderer will manage while building the table (eg. 'TD', 'TR', 'TH'), or null.
   * @param {HTMLElement} rootNode - The root node to which newly created elements will be inserted.
   */ constructor(nodeType, rootNode){
        /**
   * Factory for newly created DOM elements.
   *
   * @type {NodesPool|null}
   */ this.nodesPool = null;
        /**
   * Counter of nodes already added.
   *
   * @type {number}
   */ this.renderedNodes = 0;
        this.nodesPool = typeof nodeType === 'string' ? new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$nodesPool$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["NodesPool"](nodeType) : null;
        this.nodeType = nodeType;
        if (rootNode !== undefined) {
            this.rootNode = rootNode;
        }
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/cells.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "CellsRenderer",
    ()=>CellsRenderer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$sharedView$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/sharedView.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
;
;
;
;
class CellsRenderer extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BaseRenderer"] {
    /**
   * Obtains the instance of the SharedOrderView class which is responsible for rendering the nodes to the root node.
   *
   * @param {HTMLTableRowElement} rootNode The TR element, which is root element for cells (TD).
   * @returns {SharedOrderView}
   */ obtainOrderView(rootNode) {
        if (!this.orderViews.has(rootNode)) {
            this.orderViews.set(rootNode, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$sharedView$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["SharedOrderView"](rootNode, ()=>this.nodesPool.obtain(), this.nodeType));
        }
        return this.orderViews.get(rootNode);
    }
    /**
   * Renders the cells.
   */ render() {
        const { rowsToRender, columnsToRender, rows, rowHeaders } = this.table;
        for(let visibleRowIndex = 0; visibleRowIndex < rowsToRender; visibleRowIndex++){
            const sourceRowIndex = this.table.renderedRowToSource(visibleRowIndex);
            const TR = rows.getRenderedNode(visibleRowIndex);
            if (!TR) {
                continue; // eslint-disable-line no-continue
            }
            const orderView = this.obtainOrderView(TR);
            const rowHeadersView = rowHeaders.obtainOrderView(TR);
            orderView.prependView(rowHeadersView).setSize(columnsToRender).setOffset(0).start();
            for(let visibleColumnIndex = 0; visibleColumnIndex < columnsToRender; visibleColumnIndex++){
                orderView.render();
                const sourceColumnIndex = this.table.renderedColumnToSource(visibleColumnIndex);
                const TD = orderView.getCurrentNode();
                if (!TD) {
                    continue; // eslint-disable-line no-continue
                }
                if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(TD, 'hide')) {
                    TD.className = '';
                }
                TD.removeAttribute('style');
                TD.removeAttribute('dir');
                // Remove all accessibility-related attributes for the cell to start fresh.
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeAttribute"])(TD, [
                    /aria-(.*)/,
                    /role/
                ]);
                this.table.cellRenderer(sourceRowIndex, sourceColumnIndex, TD);
                if (this.table.isAriaEnabled()) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(TD, [
                        ...TD.hasAttribute('role') ? [] : [
                            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_GRIDCELL"])()
                        ],
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_TABINDEX"])(-1),
                        // `aria-colindex` is incremented by both tbody and thead rows.
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_COLINDEX"])(sourceColumnIndex + (this.table.rowUtils?.dataAccessObject?.rowHeaders?.length ?? 0) + 1)
                    ]);
                }
            }
            orderView.end();
        }
    }
    /**
   * Creates a new CellsRenderer instance.
   */ constructor(){
        super('TD'), /**
   * Cache for OrderView classes connected to specified node.
   *
   * @type {WeakMap}
   */ this.orderViews = new WeakMap();
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/colGroup.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ColGroupRenderer",
    ()=>ColGroupRenderer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/console.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/templateLiteralTag.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/view.mjs [client] (ecmascript)");
;
;
;
;
;
let performanceWarningAppeared = false;
class ColGroupRenderer extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BaseRenderer"] {
    /**
   * Renders the col group elements.
   */ render() {
        const { columnsToRender, rowHeadersCount } = this.table;
        const allColumnsToRender = columnsToRender + rowHeadersCount;
        if (!performanceWarningAppeared && columnsToRender > 1000) {
            performanceWarningAppeared = true;
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`Performance tip: Handsontable rendered more than 1000 visible columns.\x20
        Consider limiting the number of rendered columns by specifying the table width and/or\x20
        turning off the "renderAllColumns" option.`);
        }
        this.orderView.setSize(allColumnsToRender).setOffset(0).start();
        for(let visibleColumnIndex = 0; visibleColumnIndex < allColumnsToRender; visibleColumnIndex++){
            this.orderView.render();
            const COL = this.orderView.getCurrentNode();
            if (!COL) {
                continue; // eslint-disable-line no-continue
            }
            if (visibleColumnIndex < rowHeadersCount) {
                const sourceColumnIndex = this.table.renderedColumnToSource(visibleColumnIndex);
                COL.style.width = `${this.table.columnUtils.getHeaderWidth(sourceColumnIndex)}px`;
            } else {
                const sourceColumnIndex = this.table.renderedColumnToSource(visibleColumnIndex - rowHeadersCount);
                COL.style.width = `${this.table.columnUtils.getWidth(sourceColumnIndex)}px`;
            }
        }
        this.orderView.end();
        const firstChild = this.rootNode.firstChild;
        if (firstChild) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(firstChild, 'rowHeader');
        }
    }
    /**
   * Creates a new ColGroupRenderer instance.
   *
   * @param {HTMLElement} rootNode - The root HTML element (colgroup) for rendering COL elements.
   */ constructor(rootNode){
        super('COL', rootNode);
        this.orderView = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["OrderView"](rootNode, ()=>this.nodesPool.obtain(), this.nodeType);
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/columnHeaderRows.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ColumnHeaderRowsRenderer",
    ()=>ColumnHeaderRowsRenderer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/view.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
;
;
;
;
class ColumnHeaderRowsRenderer extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BaseRenderer"] {
    /**
   * Returns currently rendered node.
   *
   * @param {number} visualIndex Visual index of the rendered node (it always goes from 0 to N).
   * @returns {HTMLTableRowElement}
   */ getRenderedNode(visualIndex) {
        return this.orderView.getNode(visualIndex);
    }
    /**
   * Renders the TR elements.
   */ render() {
        const { columnHeadersCount } = this.table;
        if (this.table.isAriaEnabled()) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(this.rootNode, [
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_ROWGROUP"])()
            ]);
        }
        this.orderView.setSize(columnHeadersCount).setOffset(0).start();
        for(let visibleRowIndex = 0; visibleRowIndex < columnHeadersCount; visibleRowIndex++){
            this.orderView.render();
            const TR = this.orderView.getCurrentNode();
            if (TR && this.table.isAriaEnabled()) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(TR, [
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_ROW"])(),
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_ROWINDEX"])(visibleRowIndex + 1)
                ]);
            }
        }
        this.orderView.end();
    }
    /**
   * Creates a new ColumnHeaderRowsRenderer instance.
   *
   * @param {HTMLElement} rootNode - The root HTML element (THEAD) to manage TR elements within.
   */ constructor(rootNode){
        super('TR', rootNode);
        this.orderView = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["OrderView"](rootNode, ()=>this.nodesPool.obtain(), this.nodeType);
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/columnHeaders.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ColumnHeadersRenderer",
    ()=>ColumnHeadersRenderer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/view.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
;
;
;
;
class ColumnHeadersRenderer extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BaseRenderer"] {
    /**
   * Obtains the instance of the OrderView class which is responsible for rendering the nodes to the root node.
   *
   * @param {HTMLTableRowElement} rootNode The TR element, which is root element for column headers (TH).
   * @returns {OrderView}
   */ obtainOrderView(rootNode) {
        if (!this.orderViews.has(rootNode)) {
            this.orderViews.set(rootNode, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["OrderView"](rootNode, ()=>this.nodesPool.obtain(), this.nodeType));
        }
        return this.orderViews.get(rootNode);
    }
    /**
   * Renders the TH elements.
   */ render() {
        const { columnHeadersCount, columnHeaderFunctions, columnsToRender, rowHeadersCount, columnHeaderRows } = this.table;
        const allColumnsToRender = columnsToRender + rowHeadersCount;
        for(let visibleRowIndex = 0; visibleRowIndex < columnHeadersCount; visibleRowIndex++){
            const TR = columnHeaderRows.getRenderedNode(visibleRowIndex);
            if (!TR) {
                continue; // eslint-disable-line no-continue
            }
            const orderView = this.obtainOrderView(TR);
            orderView.setSize(allColumnsToRender).setOffset(0).start();
            for(let visibleColumnIndex = 0; visibleColumnIndex < allColumnsToRender; visibleColumnIndex++){
                orderView.render();
                const renderedColumnIndex = visibleColumnIndex - rowHeadersCount;
                const sourceColumnIndex = this.table.renderedColumnToSource(renderedColumnIndex);
                const TH = orderView.getCurrentNode();
                if (!TH) {
                    continue; // eslint-disable-line no-continue
                }
                TH.className = '';
                TH.removeAttribute('style');
                // Remove all accessibility-related attributes for the header to start fresh.
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeAttribute"])(TH, [
                    /aria-(.*)/,
                    /role/
                ]);
                if (this.table.isAriaEnabled()) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(TH, [
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_COLINDEX"])(visibleColumnIndex + 1),
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_TABINDEX"])(-1),
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_COLUMNHEADER"])(),
                        ...renderedColumnIndex >= 0 ? [
                            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_SCOPE_COL"])()
                        ] : [
                            // Adding `role=row` to the corner headers to prevent
                            // https://github.com/handsontable/dev-handsontable/issues/1574
                            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_GRIDCELL_BUTTON"])(),
                            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_LABEL"])('Select whole grid')
                        ]
                    ]);
                }
                columnHeaderFunctions[visibleRowIndex](sourceColumnIndex, TH, visibleRowIndex);
            }
            orderView.end();
        }
    }
    /**
   * Creates a new ColumnHeadersRenderer instance.
   */ constructor(){
        super('TH'), /**
   * Cache for OrderView classes connected to specified node.
   *
   * @type {WeakMap}
   */ this.orderViews = new WeakMap();
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/index.mjs [client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "Renderer",
    ()=>Renderer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$rowHeaders$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/rowHeaders.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$columnHeaderRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/columnHeaderRows.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$columnHeaders$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/columnHeaders.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$colGroup$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/colGroup.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$rows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/rows.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$cells$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/cells.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/table.mjs [client] (ecmascript)");
;
;
;
;
;
;
;
/**
 * Content renderer.
 *
 * @class Renderer
 */ class Renderer {
    /**
   * Sets the overlay that is currently rendered. If `null` is provided, the master overlay is set.
   *
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'|'master'} overlayName The overlay name.
   * @returns {Renderer}
   */ setActiveOverlayName(overlayName) {
        this.renderer.setActiveOverlayName(overlayName);
        return this;
    }
    /**
   * Sets filter calculators for newly calculated row and column position. The filters are used to transform visual
   * indexes (0 to N) to source indexes provided by Handsontable.
   *
   * @param {RowFilter} rowFilter The row filter instance.
   * @param {ColumnFilter} columnFilter The column filter instance.
   * @returns {Renderer}
   */ setFilters(rowFilter, columnFilter) {
        this.renderer.setFilters(rowFilter, columnFilter);
        return this;
    }
    /**
   * Sets the viewport size of the rendered table.
   *
   * @param {number} rowsCount An amount of rows to render.
   * @param {number} columnsCount An amount of columns to render.
   * @returns {Renderer}
   */ setViewportSize(rowsCount, columnsCount) {
        this.renderer.setViewportSize(rowsCount, columnsCount);
        return this;
    }
    /**
   * Sets row and column header functions.
   *
   * @param {Function[]} rowHeaders Row header functions. Factories for creating content for row headers.
   * @param {Function[]} columnHeaders Column header functions. Factories for creating content for column headers.
   * @returns {Renderer}
   */ setHeaderContentRenderers(rowHeaders, columnHeaders) {
        this.renderer.setHeaderContentRenderers(rowHeaders, columnHeaders);
        return this;
    }
    /**
   * Renders the table.
   */ render() {
        this.renderer.render();
    }
    /**
   * Creates a new Renderer instance.
   *
   * @param {RendererOptions} options The renderer configuration options.
   */ constructor({ TABLE, THEAD, COLGROUP, TBODY, rowUtils, columnUtils, cellRenderer, stylesHandler } = {}){
        /**
     * General renderer class used to render Walkontable content on screen.
     *
     * @type {TableRenderer}
     */ this.renderer = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["TableRenderer"](TABLE, {
            cellRenderer,
            stylesHandler
        });
        this.renderer.setRenderers({
            rowHeaders: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$rowHeaders$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["RowHeadersRenderer"](),
            columnHeaderRows: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$columnHeaderRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ColumnHeaderRowsRenderer"](THEAD),
            columnHeaders: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$columnHeaders$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ColumnHeadersRenderer"](),
            colGroup: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$colGroup$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ColGroupRenderer"](COLGROUP),
            rows: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$rows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["RowsRenderer"](TBODY),
            cells: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$cells$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CellsRenderer"]()
        });
        this.renderer.setAxisUtils(rowUtils, columnUtils);
    }
}
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/rowHeaders.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "RowHeadersRenderer",
    ()=>RowHeadersRenderer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$sharedView$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/sharedView.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
;
;
;
;
class RowHeadersRenderer extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BaseRenderer"] {
    /**
   * Obtains the instance of the SharedOrderView class which is responsible for rendering the nodes to the root node.
   *
   * @param {HTMLTableRowElement} rootNode The TR element, which is root element for row headers (TH).
   * @returns {SharedOrderView}
   */ obtainOrderView(rootNode) {
        if (!this.orderViews.has(rootNode)) {
            this.orderViews.set(rootNode, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$sharedView$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["SharedOrderView"](rootNode, ()=>this.nodesPool.obtain(), this.nodeType));
        }
        return this.orderViews.get(rootNode);
    }
    /**
   * Renders the cells.
   */ render() {
        const { rowsToRender, rowHeaderFunctions, rowHeadersCount, rows, cells } = this.table;
        for(let visibleRowIndex = 0; visibleRowIndex < rowsToRender; visibleRowIndex++){
            const sourceRowIndex = this.table.renderedRowToSource(visibleRowIndex);
            const TR = rows.getRenderedNode(visibleRowIndex);
            if (!TR) {
                continue; // eslint-disable-line no-continue
            }
            const orderView = this.obtainOrderView(TR);
            const cellsView = cells.obtainOrderView(TR);
            orderView.appendView(cellsView).setSize(rowHeadersCount).setOffset(0).start();
            // Render the row header levels in order: the renderer fills the row header nodes positionally
            // (childNodes[0], [1], …), so level 0 must be written first to land in the leftmost position.
            for(let visibleColumnIndex = 0; visibleColumnIndex < rowHeadersCount; visibleColumnIndex++){
                orderView.render();
                const TH = orderView.getCurrentNode();
                if (!TH) {
                    continue; // eslint-disable-line no-continue
                }
                TH.className = '';
                TH.removeAttribute('style');
                // Remove all accessibility-related attributes for the header to start fresh.
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeAttribute"])(TH, [
                    /aria-(.*)/,
                    /role/
                ]);
                if (this.table.isAriaEnabled()) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(TH, [
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_ROWHEADER"])(),
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_SCOPE_ROW"])(),
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_COLINDEX"])(visibleColumnIndex + 1),
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_TABINDEX"])(-1)
                    ]);
                }
                rowHeaderFunctions[visibleColumnIndex](sourceRowIndex, TH, visibleColumnIndex);
            }
            orderView.end();
        }
    }
    /**
   * Creates a new RowHeadersRenderer instance.
   */ constructor(){
        super('TH'), /**
   * Cache for OrderView classes connected to specified node.
   *
   * @type {WeakMap}
   */ this.orderViews = new WeakMap();
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/rows.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "RowsRenderer",
    ()=>RowsRenderer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/console.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/templateLiteralTag.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/view.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
;
;
;
;
;
;
const ROW_CLASSNAMES = {
    rowEven: 'ht__row_even',
    rowOdd: 'ht__row_odd'
};
let performanceWarningAppeared = false;
class RowsRenderer extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BaseRenderer"] {
    /**
   * Returns currently rendered node.
   *
   * @param {string} visualIndex Visual index of the rendered node (it always goeas from 0 to N).
   * @returns {HTMLTableRowElement}
   */ getRenderedNode(visualIndex) {
        return this.orderView.getNode(visualIndex);
    }
    /**
   * Renders the cells.
   */ render() {
        const { rowsToRender } = this.table;
        if (!performanceWarningAppeared && rowsToRender > 1000) {
            performanceWarningAppeared = true;
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["warn"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`Performance tip: Handsontable rendered more than 1000 visible rows.\x20
        Consider limiting the number of rendered rows by specifying the table height and/or\x20
        turning off the "renderAllRows" option.`);
        }
        if (this.table.isAriaEnabled()) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(this.rootNode, [
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_ROWGROUP"])()
            ]);
        }
        this.orderView.setSize(rowsToRender).setOffset(this.table.renderedRowToSource(0)).start();
        for(let visibleRowIndex = 0; visibleRowIndex < rowsToRender; visibleRowIndex++){
            this.orderView.render();
            const TR = this.orderView.getCurrentNode();
            const sourceRowIndex = this.table.renderedRowToSource(visibleRowIndex);
            if (!TR) {
                continue; // eslint-disable-line no-continue
            }
            if (this.table.isAriaEnabled()) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(TR, [
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_ROW"])(),
                    // `aria-rowindex` is incremented by both tbody and thead rows.
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_ROWINDEX"])(sourceRowIndex + (this.table.rowUtils?.dataAccessObject?.columnHeaders?.length ?? 0) + 1)
                ]);
            }
            if ((sourceRowIndex + 1) % 2 === 0) {
                if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(TR, ROW_CLASSNAMES.rowEven)) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(TR, ROW_CLASSNAMES.rowOdd);
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(TR, ROW_CLASSNAMES.rowEven);
                }
            } else if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(TR, ROW_CLASSNAMES.rowOdd)) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(TR, ROW_CLASSNAMES.rowEven);
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(TR, ROW_CLASSNAMES.rowOdd);
            }
        }
        this.orderView.end();
    }
    /**
   * Creates a new RowsRenderer instance.
   *
   * @param {HTMLElement} rootNode - The root node (TBODY element) for managing TR elements.
   */ constructor(rootNode){
        super('TR', rootNode);
        this.orderView = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["OrderView"](rootNode, ()=>this.nodesPool.obtain(), this.nodeType);
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/table.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * TableRenderer class collects all renderers and properties necessary for table creation. It's
 * responsible for adjusting and rendering each renderer.
 *
 * Below is a diagram of the renderers together with an indication of what they are responisble for.
 *   <table>
 *     <colgroup>  \ (root node)
 *       <col>      \
 *       <col>       \___ ColGroupRenderer
 *       <col>       /
 *       <col>      /
 *     </colgroup> /
 *     <thead>     \ (root node)
 *       <tr>       --- ColumnHeaderRowsRenderer
 *         <th>      \
 *         <th>       \__ ColumnHeadersRenderer
 *         <th>       /
 *         <th>      /
 *       </tr>      /
 *     </thead>    /
 *     <tbody>   ¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯\ (root node)
 *       <tr>   (root node)          \
 *         <th>  --- RowHeadersRenderer
 *         <td>  \                     \
 *         <td>   -- CellsRenderer      \
 *         <td>  /                       \
 *       </tr>                            \
 *       <tr>   (root node)                \
 *         <th>  --- RowHeadersRenderer     \
 *         <td>  \                           \___ RowsRenderer
 *         <td>   -- CellsRenderer           /
 *         <td>  /                          /
 *       </tr>                             /
 *       <tr>   (root node)               /
 *         <th>  --- RowHeadersRenderer  /
 *         <td>  \                      /
 *         <td>   -- CellsRenderer     /
 *         <td>  /                    /
 *       </tr>                       /
 *     </tbody>  ___________________/
 *   </table>.
 *
 * @class {RowsRenderer}
 */ __turbopack_context__.s([
    "TableRenderer",
    ()=>TableRenderer
]);
class TableRenderer {
    /**
   * Sets the overlay that is currently rendered. If `null` is provided, the master overlay is set.
   *
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'|'master'} overlayName The overlay name.
   */ setActiveOverlayName(overlayName) {
        this.activeOverlayName = overlayName;
    }
    /**
   * Set row and column util classes.
   *
   * @param {RowUtils} rowUtils RowUtils instance which provides useful methods related to row sizes.
   * @param {ColumnUtils} columnUtils ColumnUtils instance which provides useful methods related to row sizes.
   */ setAxisUtils(rowUtils, columnUtils) {
        this.rowUtils = rowUtils;
        this.columnUtils = columnUtils;
    }
    /**
   * Sets viewport size of the table.
   *
   * @param {number} rowsCount An amount of rows to render.
   * @param {number} columnsCount An amount of columns to render.
   */ setViewportSize(rowsCount, columnsCount) {
        this.rowsToRender = rowsCount;
        this.columnsToRender = columnsCount;
    }
    /**
   * Sets row and column filter instances.
   *
   * @param {RowFilter} rowFilter Row filter instance which contains all necessary information about row index transformation.
   * @param {ColumnFilter} columnFilter Column filter instance which contains all necessary information about row
   * index transformation.
   */ setFilters(rowFilter, columnFilter) {
        this.rowFilter = rowFilter;
        this.columnFilter = columnFilter;
    }
    /**
   * Sets row and column header functions.
   *
   * @param {Function[]} rowHeaders Row header functions. Factories for creating content for row headers.
   * @param {Function[]} columnHeaders Column header functions. Factories for creating content for column headers.
   */ setHeaderContentRenderers(rowHeaders, columnHeaders) {
        this.rowHeaderFunctions = rowHeaders;
        this.rowHeadersCount = rowHeaders.length;
        this.columnHeaderFunctions = columnHeaders;
        this.columnHeadersCount = columnHeaders.length;
    }
    /**
   * Sets table renderers.
   *
   * @param {renderers} renderers The renderer units.
   * @param {RowHeadersRenderer} renderers.rowHeaders Row headers renderer.
   * @param {ColumnHeaderRowsRenderer} renderers.columnHeaderRows Column header rows renderer.
   * @param {ColumnHeadersRenderer} renderers.columnHeaders Column headers renderer.
   * @param {ColGroupRenderer} renderers.colGroup Col group renderer.
   * @param {RowsRenderer} renderers.rows Rows renderer.
   * @param {CellsRenderer} renderers.cells Cells renderer.
   */ setRenderers({ rowHeaders, columnHeaderRows, columnHeaders, colGroup, rows, cells }) {
        rowHeaders.setTable(this);
        columnHeaderRows.setTable(this);
        columnHeaders.setTable(this);
        colGroup.setTable(this);
        rows.setTable(this);
        cells.setTable(this);
        this.rowHeaders = rowHeaders;
        this.columnHeaderRows = columnHeaderRows;
        this.columnHeaders = columnHeaders;
        this.colGroup = colGroup;
        this.rows = rows;
        this.cells = cells;
    }
    /**
   * Transforms visual/rendered row index to source index.
   *
   * @param {number} rowIndex Rendered index.
   * @returns {number}
   */ renderedRowToSource(rowIndex) {
        return this.rowFilter.renderedToSource(rowIndex);
    }
    /**
   * Transforms visual/rendered column index to source index.
   *
   * @param {number} columnIndex Rendered index.
   * @returns {number}
   */ renderedColumnToSource(columnIndex) {
        return this.columnFilter.renderedToSource(columnIndex);
    }
    /**
   * Returns `true` if the accessibility-related ARIA tags should be added to the table, `false` otherwise.
   *
   * @returns {boolean}
   */ isAriaEnabled() {
        return this.rowUtils.wtSettings.getSetting('ariaTags');
    }
    /**
   * Renders the table.
   */ render() {
        this.columnHeaderRows.render();
        this.columnHeaders.render();
        this.rows.render();
        this.rowHeaders.render();
        this.cells.render();
        // After the cells are rendered calculate columns width to prepare proper values
        // for colGroup renderer (which renders COL elements).
        this.columnUtils.calculateWidths();
        this.colGroup.render();
        const { rowsToRender, rows } = this;
        // Fix for multi-line content and for supporting `rowHeights` option.
        for(let visibleRowIndex = 0; visibleRowIndex < rowsToRender; visibleRowIndex++){
            const TR = rows.getRenderedNode(visibleRowIndex);
            const rowUtils = this.rowUtils;
            if (TR && TR.firstChild) {
                const sourceRowIndex = this.renderedRowToSource(visibleRowIndex);
                const rowHeight = rowUtils.getHeightByOverlayName(sourceRowIndex, this.activeOverlayName);
                const isBorderBoxSizing = this.stylesHandler.areCellsBorderBox();
                const borderCompensation = isBorderBoxSizing ? 0 : 1;
                if (rowHeight) {
                    // Decrease height. 1 pixel will be "replaced" by 1px border top
                    TR.firstChild.style.height = `${rowHeight - borderCompensation}px`;
                } else {
                    TR.firstChild.style.height = '';
                }
            }
        }
    }
    /**
   * Creates a new TableRenderer instance.
   *
   * @param {HTMLTableElement} rootNode The HTML table element to use as the root node for rendering.
   * @param {object} options The configuration options.
   * @param {Function} [options.cellRenderer] The cell renderer function.
   * @param {StylesHandler} [options.stylesHandler] The styles handler instance.
   */ constructor(rootNode, { cellRenderer, stylesHandler } = {}){
        /**
   * Renderer class responsible for rendering row headers.
   *
   * @type {RowHeadersRenderer}
   */ this.rowHeaders = null;
        /**
   * Renderer class responsible for rendering column header rows (TR elements in THEAD).
   *
   * @type {ColumnHeaderRowsRenderer}
   */ this.columnHeaderRows = null;
        /**
   * Renderer class responsible for rendering column headers (TH elements in TR).
   *
   * @type {ColumnHeadersRenderer}
   */ this.columnHeaders = null;
        /**
   * Renderer class responsible for rendering col in colgroup.
   *
   * @type {ColGroupRenderer}
   */ this.colGroup = null;
        /**
   * Renderer class responsible for rendering rows in tbody.
   *
   * @type {RowsRenderer}
   */ this.rows = null;
        /**
   * Renderer class responsible for rendering cells.
   *
   * @type {CellsRenderer}
   */ this.cells = null;
        /**
   * Row filter which contains all necessary information about row index transformation.
   *
   * @type {RowFilter}
   */ this.rowFilter = null;
        /**
   * Column filter which contains all necessary information about column index transformation.
   *
   * @type {ColumnFilter}
   */ this.columnFilter = null;
        /**
   * Row utils class which contains all necessary information about sizes of the rows.
   *
   * @type {RowUtils}
   */ this.rowUtils = null;
        /**
   * Column utils class which contains all necessary information about sizes of the columns.
   *
   * @type {ColumnUtils}
   */ this.columnUtils = null;
        /**
   * Indicates how much rows should be rendered to fill whole table viewport.
   *
   * @type {number}
   */ this.rowsToRender = 0;
        /**
   * Indicates how much columns should be rendered to fill whole table viewport.
   *
   * @type {number}
   */ this.columnsToRender = 0;
        /**
   * An array of functions to be used as a content factory to row headers.
   *
   * @type {Function[]}
   */ this.rowHeaderFunctions = [];
        /**
   * Count of the function used to render row headers.
   *
   * @type {number}
   */ this.rowHeadersCount = 0;
        /**
   * An array of functions to be used as a content factory to column headers.
   *
   * @type {Function[]}
   */ this.columnHeaderFunctions = [];
        /**
   * Count of the function used to render column headers.
   *
   * @type {number}
   */ this.columnHeadersCount = 0;
        this.rootNode = rootNode;
        this.rootDocument = this.rootNode.ownerDocument;
        this.cellRenderer = cellRenderer;
        this.stylesHandler = stylesHandler;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/scroll.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
var /**
   * Get last visible column based on virtual dom and how table is visible in browser window viewport.
   *
   * @param {number} lastColumnIndex The last visible column index.
   * @returns {number}
   */ _getLastColumnIndex = /*#__PURE__*/ new WeakSet(), /**
   * Get last visible row based on virtual dom and how table is visible in browser window viewport.
   *
   * @param {number} lastRowIndex The last visible row index.
   * @returns {number}
   */ _getLastRowIndex = /*#__PURE__*/ new WeakSet();
/**
 * @class Scroll
 */ class Scroll {
    /**
   * Scrolls viewport to a cell.
   *
   * @param {CellCoords} coords The cell coordinates.
   * @param {'auto' | 'start' | 'end'} [horizontalSnap='auto'] If `'start'`, viewport is scrolled to show
   * the cell on the left of the table. If `'end'`, viewport is scrolled to show the cell on the right of
   * the table. When `'auto'`, the viewport is scrolled only when the column is outside of the viewport.
   * @param {'auto' | 'top' | 'bottom'} [verticalSnap='auto'] If `'top'`, viewport is scrolled to show
   * the cell on the top of the table. If `'bottom'`, viewport is scrolled to show the cell on the bottom of
   * the table. When `'auto'`, the viewport is scrolled only when the row is outside of the viewport.
   * @returns {boolean}
   */ scrollViewport(coords, horizontalSnap, verticalSnap) {
        if (coords.col < 0 || coords.row < 0) {
            return false;
        }
        const scrolledHorizontally = this.scrollViewportHorizontally(coords.col, horizontalSnap);
        const scrolledVertically = this.scrollViewportVertically(coords.row, verticalSnap);
        return scrolledHorizontally || scrolledVertically;
    }
    /**
   * Scrolls viewport to a column.
   *
   * @param {number} column Visual column index.
   * @param {'auto' | 'start' | 'end'} [snapping='auto'] If `'start'`, viewport is scrolled to show
   * the cell on the left of the table. If `'end'`, viewport is scrolled to show the cell on the right of
   * the table. When `'auto'`, the viewport is scrolled only when the column is outside of the viewport.
   * @returns {boolean}
   */ scrollViewportHorizontally(column, snapping = 'auto') {
        const { drawn, totalColumns } = this.dataAccessObject;
        if (!drawn) {
            return false;
        }
        const snappingObject = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createObjectPropListener"])(snapping);
        column = this.dataAccessObject.wtSettings.getSetting('onBeforeViewportScrollHorizontally', column, snappingObject);
        if (!Number.isInteger(column) || column < 0 || column > totalColumns) {
            return false;
        }
        snapping = snappingObject.value;
        const { fixedColumnsStart, inlineStartOverlay } = this.dataAccessObject;
        const autoSnapping = snapping === 'auto';
        // for auto-snapping do not scroll the viewport when the columns points to the overlays
        if (autoSnapping && column < fixedColumnsStart) {
            return false;
        }
        const firstColumn = this.getFirstVisibleColumn();
        const lastColumn = this.getLastVisibleColumn();
        let result = false;
        if (autoSnapping && (column < firstColumn || column > lastColumn) || !autoSnapping) {
            // if there is at least one fully visible column determine the snapping direction based on
            // that columns or by snapping flag, if provided.
            result = inlineStartOverlay.scrollTo(column, autoSnapping ? column >= this.getLastPartiallyVisibleColumn() : snapping === 'end');
        }
        return result;
    }
    /**
   * Scrolls viewport to a row.
   *
   * @param {number} row Visual row index.
   * @param {'auto' | 'top' | 'bottom'} [snapping='auto'] If `'top'`, viewport is scrolled to show
   * the cell on the top of the table. If `'bottom'`, viewport is scrolled to show the cell on
   * the bottom of the table. When `'auto'`, the viewport is scrolled only when the row is outside of
   * the viewport.
   * @returns {boolean}
   */ scrollViewportVertically(row, snapping = 'auto') {
        const { drawn, totalRows } = this.dataAccessObject;
        if (!drawn) {
            return false;
        }
        const snappingObject = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createObjectPropListener"])(snapping);
        row = this.dataAccessObject.wtSettings.getSetting('onBeforeViewportScrollVertically', row, snappingObject);
        if (!Number.isInteger(row) || row < 0 || row > totalRows) {
            return false;
        }
        snapping = snappingObject.value;
        const { fixedRowsBottom, fixedRowsTop, topOverlay } = this.dataAccessObject;
        const autoSnapping = snapping === 'auto';
        // for auto-snapping do not scroll the viewport when the rows points to the overlays
        if (autoSnapping && (row < fixedRowsTop || row > totalRows - fixedRowsBottom - 1)) {
            return false;
        }
        const firstRow = this.getFirstVisibleRow();
        const lastRow = this.getLastVisibleRow();
        let result = false;
        if (autoSnapping && (row < firstRow || row > lastRow) || !autoSnapping) {
            // if there is at least one fully visible row determine the snapping direction based on
            // that rows or by snapping flag, if provided.
            result = topOverlay.scrollTo(row, autoSnapping ? row >= this.getLastPartiallyVisibleRow() : snapping === 'bottom');
        }
        return result;
    }
    /**
   * Get first visible row based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */ getFirstVisibleRow() {
        return this.dataAccessObject.wtTable.getFirstVisibleRow();
    }
    /**
   * Get last visible row based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */ getLastVisibleRow() {
        return _class_private_method_get(this, _getLastRowIndex, getLastRowIndex).call(this, this.dataAccessObject.wtTable.getLastVisibleRow());
    }
    /**
   * Get first partially visible row based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */ getFirstPartiallyVisibleRow() {
        return this.dataAccessObject.wtTable.getFirstPartiallyVisibleRow();
    }
    /**
   * Get last visible row based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */ getLastPartiallyVisibleRow() {
        return _class_private_method_get(this, _getLastRowIndex, getLastRowIndex).call(this, this.dataAccessObject.wtTable.getLastPartiallyVisibleRow());
    }
    /**
   * Get first visible column based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */ getFirstVisibleColumn() {
        return this.dataAccessObject.wtTable.getFirstVisibleColumn();
    }
    /**
   * Get last visible column based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */ getLastVisibleColumn() {
        return _class_private_method_get(this, _getLastColumnIndex, getLastColumnIndex).call(this, this.dataAccessObject.wtTable.getLastVisibleColumn());
    }
    /**
   * Get first partially visible column based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */ getFirstPartiallyVisibleColumn() {
        return this.dataAccessObject.wtTable.getFirstPartiallyVisibleColumn();
    }
    /**
   * Get last partially visible column based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */ getLastPartiallyVisibleColumn() {
        return _class_private_method_get(this, _getLastColumnIndex, getLastColumnIndex).call(this, this.dataAccessObject.wtTable.getLastPartiallyVisibleColumn());
    }
    /**
   * @param {ScrollDao} dataAccessObject Tha data access object.
   */ constructor(dataAccessObject){
        _class_private_method_init(this, _getLastColumnIndex);
        _class_private_method_init(this, _getLastRowIndex);
        this.dataAccessObject = dataAccessObject;
    }
}
function getLastColumnIndex(lastColumnIndex) {
    const { wtSettings, inlineStartOverlay, wtTable, wtViewport, totalColumns, rootWindow } = this.dataAccessObject;
    if (inlineStartOverlay.mainTableScrollableElement === rootWindow) {
        const isRtl = wtSettings.getSetting('rtlMode');
        let inlineStartRootElementOffset = null;
        if (isRtl) {
            const tableRect = wtTable.TABLE.getBoundingClientRect();
            const rootDocument = rootWindow.document;
            const docOffsetWidth = rootDocument.documentElement.offsetWidth;
            inlineStartRootElementOffset = Math.abs(tableRect.right - docOffsetWidth);
        } else {
            const rootElementOffset = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(wtTable.wtRootElement);
            inlineStartRootElementOffset = rootElementOffset.left;
        }
        const windowScrollLeft = Math.abs((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollLeft"])(rootWindow, rootWindow));
        // Only calculate lastColumnIndex when table didn't filled (from right) whole viewport space
        if (inlineStartRootElementOffset > windowScrollLeft) {
            const windowWidth = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["innerWidth"])(rootWindow);
            let columnsWidth = wtViewport.getRowHeaderWidth();
            for(let column = 1; column <= totalColumns; column++){
                columnsWidth += inlineStartOverlay.sumCellSizes(column - 1, column);
                if (inlineStartRootElementOffset + columnsWidth - windowScrollLeft >= windowWidth) {
                    // Return physical column - 1 (-2 because rangeEach gives column index + 1 - sumCellSizes requirements)
                    lastColumnIndex = column - 2;
                    break;
                }
            }
        }
    }
    return lastColumnIndex;
}
function getLastRowIndex(lastRowIndex) {
    const { topOverlay, wtTable, wtViewport, totalRows, rootWindow } = this.dataAccessObject;
    if (topOverlay.mainTableScrollableElement === rootWindow) {
        const rootElementOffset = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(wtTable.wtRootElement);
        const windowScrollTop = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollTop"])(rootWindow, rootWindow);
        // Only calculate lastRowIndex when table didn't filled (from bottom) whole viewport space
        if (rootElementOffset.top > windowScrollTop) {
            const windowHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["innerHeight"])(rootWindow);
            let rowsHeight = wtViewport.getColumnHeaderHeight();
            for(let row = 1; row <= totalRows; row++){
                rowsHeight += topOverlay.sumCellSizes(row - 1, row);
                if (rootElementOffset.top + rowsHeight - windowScrollTop >= windowHeight) {
                    // Return physical row - 1 (-2 because rangeEach gives row index + 1 - sumCellSizes requirements)
                    lastRowIndex = row - 2;
                    break;
                }
            }
        }
    }
    return lastRowIndex;
}
var __TURBOPACK__default__export__ = Scroll;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/border/border.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$event$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/event.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/browser.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$border$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/border/utils.mjs [client] (ecmascript)");
;
;
;
;
const BORDER_STYLE_CLASS_PREFIX = 'ht-border-style-';
const BORDER_STYLE_VERTICAL_SUFFIX = '-vertical';
const BORDER_STYLE_HORIZONTAL_SUFFIX = '-horizontal';
/**
 *
 */ class Border {
    /**
   * Register all necessary events.
   */ registerListeners() {
        const documentBody = this.wot.rootDocument.body;
        this.eventManager.addEventListener(documentBody, 'mousedown', ()=>this.onMouseDown());
        this.eventManager.addEventListener(documentBody, 'mouseup', ()=>this.onMouseUp());
        if (this.main) {
            for(let c = 0, len = this.main.childNodes.length; c < len; c++){
                const mainNode = this.main;
                const element = mainNode.childNodes[c];
                this.eventManager.addEventListener(element, 'mouseenter', (event)=>this.onMouseEnter(event, mainNode.childNodes[c]));
            }
        }
    }
    /**
   * Mouse down listener.
   *
   * @private
   */ onMouseDown() {
        this.mouseDown = true;
    }
    /**
   * Mouse up listener.
   *
   * @private
   */ onMouseUp() {
        this.mouseDown = false;
    }
    /**
   * Mouse enter listener for fragment selection functionality.
   *
   * @private
   * @param {Event} event Dom event.
   * @param {HTMLElement} parentElement Part of border element.
   */ onMouseEnter(event, parentElement) {
        if (!this.mouseDown || !this.wot.getSetting('hideBorderOnMouseDownOver')) {
            return;
        }
        event.preventDefault();
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$event$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["stopImmediatePropagation"])(event);
        const documentBody = this.wot.rootDocument.body;
        const bounds = parentElement.getBoundingClientRect();
        // Hide border to prevents selection jumping when fragmentSelection is enabled.
        parentElement.style.display = 'none';
        /**
     * @param {Event} mouseEvent The mouse event object.
     * @returns {boolean}
     */ function isOutside(mouseEvent) {
            if (mouseEvent.clientY < Math.floor(bounds.top)) {
                return true;
            }
            if (mouseEvent.clientY > Math.ceil(bounds.top + bounds.height)) {
                return true;
            }
            if (mouseEvent.clientX < Math.floor(bounds.left)) {
                return true;
            }
            if (mouseEvent.clientX > Math.ceil(bounds.left + bounds.width)) {
                return true;
            }
        }
        /**
     * @param {Event} handlerEvent The mouse event object.
     */ const handler = (handlerEvent)=>{
            if (isOutside(handlerEvent)) {
                this.eventManager.removeEventListener(documentBody, 'mousemove', handler);
                parentElement.style.display = 'block';
            }
        };
        this.eventManager.addEventListener(documentBody, 'mousemove', handler);
    }
    /**
   * Create border elements.
   *
   * @param {object} settings The border settings.
   */ createBorders(settings) {
        const { rootDocument } = this.wot;
        this.main = rootDocument.createElement('div');
        const borderDivs = [
            'top',
            'start',
            'bottom',
            'end',
            'corner'
        ];
        let style = this.main.style;
        style.position = 'absolute';
        style.top = '0';
        style.left = '0';
        const createdDivs = [];
        for(let i = 0; i < 5; i++){
            const position = borderDivs[i];
            const div = rootDocument.createElement('div');
            const getSettingsProperty = (property)=>{
                const posSettings = this.settings[position];
                return posSettings && posSettings[property] ? posSettings[property] : settings.border?.[property];
            };
            div.className = `wtBorder ${this.settings.className || ''}`; // + borderDivs[i];
            if (this.settings[position]?.hide) {
                div.className += ' hidden';
            }
            style = div.style;
            const borderStyle = getSettingsProperty('style');
            if (borderStyle) {
                if ([
                    'start',
                    'end'
                ].includes(position)) {
                    div.className += ` ${BORDER_STYLE_CLASS_PREFIX}${borderStyle}${BORDER_STYLE_VERTICAL_SUFFIX}`;
                } else {
                    div.className += ` ${BORDER_STYLE_CLASS_PREFIX}${borderStyle}${BORDER_STYLE_HORIZONTAL_SUFFIX}`;
                }
                style.setProperty('--ht-custom-border-size', `${getSettingsProperty('width')}px`);
                style.setProperty('--ht-custom-border-color', String(getSettingsProperty('color') ?? ''));
            } else {
                style.backgroundColor = String(getSettingsProperty('color') ?? '');
            }
            style.height = `${getSettingsProperty('width')}px`;
            style.width = `${getSettingsProperty('width')}px`;
            createdDivs.push(div);
            this.main.appendChild(div);
        }
        this.top = createdDivs[0];
        this.start = createdDivs[1];
        this.bottom = createdDivs[2];
        this.end = createdDivs[3];
        this.topStyle = this.top.style;
        this.startStyle = this.start.style;
        this.bottomStyle = this.bottom.style;
        this.endStyle = this.end.style;
        this.corner = createdDivs[4];
        this.corner.className += ' corner';
        this.cornerStyle = this.corner.style;
        this.cornerStyle.width = `${this.cornerDefaultStyle.width}px`;
        this.cornerStyle.height = `${this.cornerDefaultStyle.height}px`;
        this.cornerStyle.border = [
            `${this.cornerDefaultStyle.borderWidth}px`,
            this.cornerDefaultStyle.borderStyle,
            this.cornerDefaultStyle.borderColor
        ].join(' ');
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isMobileBrowser"])() && this.instance.getSetting('isDataViewInstance')) {
            this.createMultipleSelectorHandles();
        }
        this.disappear();
        const { wtTable } = this.wot;
        let bordersHolder = wtTable.bordersHolder;
        if (!bordersHolder) {
            bordersHolder = rootDocument.createElement('div');
            bordersHolder.className = 'htBorders';
            wtTable.bordersHolder = bordersHolder;
            wtTable.spreader.appendChild(bordersHolder);
        }
        bordersHolder.appendChild(this.main);
    }
    /**
   * Create multiple selector handler for mobile devices.
   */ createMultipleSelectorHandles() {
        const { rootDocument, wtSettings } = this.wot;
        const stylesHandler = wtSettings.getSetting('stylesHandler');
        const cellMobileHandleSize = stylesHandler.getCSSVariableValue('cell-mobile-handle-size');
        const cellMobileHandleBorderRadius = stylesHandler.getCSSVariableValue('cell-mobile-handle-border-radius');
        const cellMobileHandleBackgroundColor = stylesHandler.getCSSVariableValue('cell-mobile-handle-background-color');
        const cellMobileHandleBackgroundOpacity = stylesHandler.getCSSVariableValue('cell-mobile-handle-background-opacity');
        const cellMobileHandleBorderWidth = stylesHandler.getCSSVariableValue('cell-mobile-handle-border-width');
        const cellMobileHandleBorderColor = stylesHandler.getCSSVariableValue('cell-mobile-handle-border-color');
        this.selectionHandles = {
            top: rootDocument.createElement('div'),
            topHitArea: rootDocument.createElement('div'),
            bottom: rootDocument.createElement('div'),
            bottomHitArea: rootDocument.createElement('div'),
            styles: {}
        };
        const hitAreaWidth = 40;
        this.selectionHandles.top.className = 'topSelectionHandle topLeftSelectionHandle';
        this.selectionHandles.topHitArea.className = 'topSelectionHandle-HitArea topLeftSelectionHandle-HitArea';
        this.selectionHandles.bottom.className = 'bottomSelectionHandle bottomRightSelectionHandle';
        this.selectionHandles.bottomHitArea.className = 'bottomSelectionHandle-HitArea bottomRightSelectionHandle-HitArea';
        this.selectionHandles.styles = {
            top: this.selectionHandles.top.style,
            topHitArea: this.selectionHandles.topHitArea.style,
            bottom: this.selectionHandles.bottom.style,
            bottomHitArea: this.selectionHandles.bottomHitArea.style
        };
        const hitAreaTargets = [
            this.selectionHandles.styles.bottomHitArea,
            this.selectionHandles.styles.topHitArea
        ];
        for (const hitAreaStyleTarget of hitAreaTargets){
            hitAreaStyleTarget.position = 'absolute';
            hitAreaStyleTarget.height = `${hitAreaWidth}px`;
            hitAreaStyleTarget.width = `${hitAreaWidth}px`;
            hitAreaStyleTarget.borderRadius = `${parseInt(String(hitAreaWidth / 1.5), 10)}px`;
        }
        for (const handleStyleTarget of [
            this.selectionHandles.styles.bottom,
            this.selectionHandles.styles.top
        ]){
            handleStyleTarget.position = 'absolute';
            handleStyleTarget.height = `${cellMobileHandleSize}px`;
            handleStyleTarget.width = `${cellMobileHandleSize}px`;
            handleStyleTarget.borderRadius = `${cellMobileHandleBorderRadius}px`;
            // eslint-disable-next-line max-len
            handleStyleTarget.background = `color-mix(in srgb, ${cellMobileHandleBackgroundColor} ${cellMobileHandleBackgroundOpacity}%, transparent)`;
            handleStyleTarget.border = `${cellMobileHandleBorderWidth}px solid ${cellMobileHandleBorderColor}`;
        }
    }
    /**
   * Checks if the given coordinates are south-east of the area selection. If `true` then
   * the fill handler should be visible.
   *
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @returns {boolean}
   */ isSouthEastOfAreaSelection(row, col) {
        const areaSelection = this.wot.selectionManager.getAreaSelection();
        if (!areaSelection) {
            return false;
        }
        // If the area selection is empty, that means only one cell is selected.
        // In this case, the fill handler should be visible.
        if (!areaSelection.cellRange) {
            return true;
        }
        const bottomEndCorner = areaSelection.cellRange.getBottomEndCorner();
        return bottomEndCorner.row === row && bottomEndCorner.col === col;
    }
    /**
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number} top The top position of the handler.
   * @param {number} left The left position of the handler.
   * @param {number} width The width of the handler.
   * @param {number} height The height of the handler.
   */ updateMultipleSelectionHandlesPosition(row, col, top, left, width, height) {
        const isRtl = this.wot.wtSettings.getSetting('rtlMode');
        const inlinePosProperty = isRtl ? 'right' : 'left';
        const { top: topStyles, topHitArea: topHitAreaStyles, bottom: bottomStyles, bottomHitArea: bottomHitAreaStyles } = this.selectionHandles.styles;
        const handleBorderSize = parseInt(topStyles.borderWidth, 10);
        const handleSize = parseInt(topStyles.width, 10);
        const hitAreaSize = parseInt(topHitAreaStyles.width, 10);
        const totalTableWidth = this.wot.wtTable.getWidth();
        const totalTableHeight = this.wot.wtTable.getHeight();
        topStyles.top = `${parseInt(String(top - handleSize - 1), 10)}px`;
        topStyles[inlinePosProperty] = `${parseInt(String(left - handleSize - 1), 10)}px`;
        topHitAreaStyles.top = `${parseInt(String(top - hitAreaSize / 4 * 3), 10)}px`;
        topHitAreaStyles[inlinePosProperty] = `${parseInt(String(left - hitAreaSize / 4 * 3), 10)}px`;
        const bottomHandlerInline = Math.min(parseInt(String(left + width), 10), totalTableWidth - handleSize - handleBorderSize * 2);
        const bottomHandlerAreaInline = Math.min(parseInt(String(left + width - hitAreaSize / 4), 10), totalTableWidth - hitAreaSize - handleBorderSize * 2);
        bottomStyles[inlinePosProperty] = `${bottomHandlerInline}px`;
        bottomHitAreaStyles[inlinePosProperty] = `${bottomHandlerAreaInline}px`;
        const bottomHandlerTop = Math.min(parseInt(String(top + height), 10), totalTableHeight - handleSize - handleBorderSize * 2);
        const bottomHandlerAreaTop = Math.min(parseInt(String(top + height - hitAreaSize / 4), 10), totalTableHeight - hitAreaSize - handleBorderSize * 2);
        bottomStyles.top = `${bottomHandlerTop}px`;
        bottomHitAreaStyles.top = `${bottomHandlerAreaTop}px`;
        const cornerVisible = this.settings.border?.cornerVisible;
        if (cornerVisible && typeof cornerVisible === 'function' && cornerVisible()) {
            topStyles.display = 'block';
            topHitAreaStyles.display = 'block';
            if (this.isSouthEastOfAreaSelection(row, col)) {
                bottomStyles.display = 'block';
                bottomHitAreaStyles.display = 'block';
            } else {
                bottomStyles.display = 'none';
                bottomHitAreaStyles.display = 'none';
            }
        } else {
            topStyles.display = 'none';
            bottomStyles.display = 'none';
            topHitAreaStyles.display = 'none';
            bottomHitAreaStyles.display = 'none';
        }
        if (row === this.wot.wtSettings.getSetting('fixedRowsTop') || col === this.wot.wtSettings.getSetting('fixedColumnsStart')) {
            topStyles.zIndex = '9999';
            topHitAreaStyles.zIndex = '9999';
        } else {
            topStyles.zIndex = '';
            topHitAreaStyles.zIndex = '';
        }
    }
    /**
   * Show border around one or many cells.
   *
   * @param {Array} corners The corner coordinates.
   */ appear(corners) {
        if (this.disabled) {
            return;
        }
        let [fromRow, fromColumn, toRow, toColumn] = corners;
        // borders can not be rendered on headers so hide them
        if (fromRow < 0 && toRow < 0 || fromColumn < 0 && toColumn < 0) {
            this.disappear();
            return;
        }
        const { wtTable, rootDocument, rootWindow } = this.wot;
        const isMultiple = fromRow !== toRow || fromColumn !== toColumn;
        const firstRenderedRow = wtTable.getFirstRenderedRow();
        const lastRenderedRow = wtTable.getLastRenderedRow();
        const firstRenderedColumn = wtTable.getFirstRenderedColumn();
        const lastRenderedColumn = wtTable.getLastRenderedColumn();
        if (firstRenderedColumn < 0 && lastRenderedColumn < 0 || firstRenderedRow < 0 && lastRenderedRow < 0) {
            // ...also when overlays have rendered only headers skip it
            this.disappear();
            return;
        }
        let fromTD;
        if (isMultiple) {
            fromColumn = Math.max(fromColumn, firstRenderedColumn);
            toColumn = Math.min(toColumn, lastRenderedColumn);
            fromRow = Math.max(fromRow, firstRenderedRow);
            toRow = Math.min(toRow, lastRenderedRow);
            if (toColumn < fromColumn || toRow < fromRow) {
                this.disappear();
                return;
            }
            fromTD = wtTable.getCell(this.wot.createCellCoords(fromRow, fromColumn));
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(fromTD)) {
                this.disappear();
                return;
            }
        } else {
            fromTD = wtTable.getCell(this.wot.createCellCoords(fromRow, fromColumn));
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(fromTD)) {
                this.disappear();
                return;
            }
        }
        const toTD = isMultiple ? wtTable.getCell(this.wot.createCellCoords(toRow, toColumn)) : fromTD;
        const fromTDEl = fromTD;
        const toTDEl = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(toTD) ? toTD : fromTDEl;
        const fromOffset = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(fromTDEl);
        const toOffset = isMultiple ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(toTDEl) : fromOffset;
        const containerOffset = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(wtTable.TABLE);
        const minTop = fromOffset.top;
        const minLeft = fromOffset.left;
        const isRtl = this.wot.wtSettings.getSetting('rtlMode');
        let inlineStartPos = 0;
        let width = 0;
        if (isRtl) {
            const containerWidth = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(wtTable.TABLE);
            const fromWidth = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(fromTDEl);
            const gridRightPos = rootWindow.innerWidth - containerOffset.left - containerWidth;
            width = minLeft + fromWidth - toOffset.left;
            inlineStartPos = rootWindow.innerWidth - minLeft - fromWidth - gridRightPos - 1;
        } else {
            width = toOffset.left + (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(toTDEl) - minLeft;
            inlineStartPos = minLeft - containerOffset.left - 1;
        }
        if (this.isEntireColumnSelected(fromRow, toRow)) {
            const rowHeader = fromRow;
            const modifiedValues = this.getDimensionsFromHeader('columns', fromColumn, toColumn, rowHeader, containerOffset);
            let fromTH = null;
            if (modifiedValues) {
                [fromTH, inlineStartPos, width] = modifiedValues;
            }
            if (fromTH) {
                fromTD = fromTH;
            }
        }
        let top = minTop - containerOffset.top - 1;
        let height = toOffset.top + (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(toTDEl) - minTop;
        if (this.isEntireRowSelected(fromColumn, toColumn)) {
            const columnHeader = fromColumn;
            const modifiedValues = this.getDimensionsFromHeader('rows', fromRow, toRow, columnHeader, containerOffset);
            let fromTH = null;
            if (modifiedValues) {
                [fromTH, top, height] = modifiedValues;
            }
            if (fromTH) {
                fromTD = fromTH;
            }
        }
        const style = rootWindow.getComputedStyle(fromTDEl);
        if (parseInt(style.borderTopWidth, 10) > 0) {
            top += 1;
            height = height > 0 ? height - 1 : 0;
        }
        if (parseInt(style[isRtl ? 'borderRightWidth' : 'borderLeftWidth'], 10) > 0) {
            inlineStartPos += 1;
            width = width > 0 ? width - 1 : 0;
        }
        const inlinePosProperty = isRtl ? 'right' : 'left';
        const delta = Math.ceil((this.settings.border?.width ?? 0) / 2);
        this.topStyle.top = `${top}px`;
        this.topStyle[inlinePosProperty] = `${inlineStartPos}px`;
        this.topStyle.width = `${width + delta}px`;
        this.topStyle.display = 'block';
        this.startStyle.top = `${top}px`;
        this.startStyle[inlinePosProperty] = `${inlineStartPos}px`;
        this.startStyle.height = `${height + delta}px`;
        this.startStyle.display = 'block';
        this.bottomStyle.top = `${top + height - parseInt(this.bottomStyle.height, 10) + delta}px`;
        this.bottomStyle[inlinePosProperty] = `${inlineStartPos}px`;
        this.bottomStyle.width = `${width + delta}px`;
        this.bottomStyle.display = 'block';
        this.endStyle.top = `${top}px`;
        this.endStyle[inlinePosProperty] = `${inlineStartPos + width - parseInt(this.endStyle.width, 10) + delta}px`;
        this.endStyle.height = `${height + delta}px`;
        this.endStyle.display = 'block';
        let cornerVisibleSetting = this.settings.border?.cornerVisible;
        cornerVisibleSetting = typeof cornerVisibleSetting === 'function' ? cornerVisibleSetting(this.settings.layerLevel) : cornerVisibleSetting;
        const hookResult = this.wot.getSetting('onModifyGetCellCoords', toRow, toColumn, false, 'render');
        let [checkRow, checkCol] = [
            toRow,
            toColumn
        ];
        if (hookResult && Array.isArray(hookResult)) {
            // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
            [, , checkRow, checkCol] = hookResult;
        }
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isMobileBrowser"])() || !cornerVisibleSetting || !this.isSouthEastOfAreaSelection(checkRow, checkCol)) {
            this.cornerStyle.display = 'none';
        } else {
            const cornerBorderWidth = Number(this.cornerDefaultStyle.borderWidth); // eslint-disable-line no-lonely-if
            this.cornerStyle.top = `${top + height + this.cornerCenterPointOffset - cornerBorderWidth}px`;
            this.cornerStyle[inlinePosProperty] = `${inlineStartPos + width + this.cornerCenterPointOffset - Number(this.cornerDefaultStyle.borderWidth)}px`;
            this.cornerStyle.borderRightWidth = `${this.cornerDefaultStyle.borderWidth}px`;
            this.cornerStyle.borderLeftWidth = `${this.cornerDefaultStyle.borderWidth}px`;
            this.cornerStyle.borderBottomWidth = `${this.cornerDefaultStyle.borderWidth}px`;
            this.cornerStyle.width = String(this.cornerDefaultStyle.width);
            // Hide the fill handle, so the possible further adjustments won't force unneeded scrollbars.
            this.cornerStyle.display = 'none';
            let trimmingContainer = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getTrimmingContainer"])(wtTable.TABLE);
            const trimToWindow = Object.is(trimmingContainer, rootWindow);
            if (trimToWindow) {
                trimmingContainer = rootDocument.documentElement;
            }
            // -1 was initially removed from the base position to compensate for the table border. We need to exclude it from
            // the corner width.
            const cornerBorderCompensation = parseInt(String(this.cornerDefaultStyle.borderWidth), 10) - 1;
            const cornerHalfWidth = Math.ceil(parseInt(String(this.cornerDefaultStyle.width), 10) / 2);
            const cornerHalfHeight = Math.ceil(parseInt(String(this.cornerDefaultStyle.height), 10) / 2);
            if (toColumn === this.wot.getSetting('totalColumns') - 1) {
                const toTdOffsetLeft = trimToWindow ? toTDEl.getBoundingClientRect().left : toTDEl.offsetLeft;
                let cornerOverlappingContainer = false;
                let cornerEdge = 0;
                if (isRtl) {
                    cornerEdge = toTdOffsetLeft - parseInt(String(this.cornerDefaultStyle.width), 10) / 2;
                    cornerOverlappingContainer = cornerEdge < 0;
                } else {
                    cornerEdge = toTdOffsetLeft + (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(toTDEl) + parseInt(String(this.cornerDefaultStyle.width), 10) / 2;
                    cornerOverlappingContainer = cornerEdge >= (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["innerWidth"])(trimmingContainer);
                }
                if (cornerOverlappingContainer) {
                    const inlineStartPosition = Math.floor(inlineStartPos + width + this.cornerCenterPointOffset - cornerHalfWidth - cornerBorderCompensation);
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(this.corner, 'wtCornerInlineEndEdge');
                    this.cornerStyle[inlinePosProperty] = `${inlineStartPosition - 1}px`;
                }
            } else {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(this.corner, 'wtCornerInlineEndEdge');
            }
            if (toRow === this.wot.getSetting('totalRows') - 1) {
                const toTdOffsetTop = trimToWindow ? toTDEl.getBoundingClientRect().top : toTDEl.offsetTop;
                const cornerHalfHeight = parseInt(String(this.cornerDefaultStyle.height), 10) / 2;
                const cornerBottomEdge = toTdOffsetTop + (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(toTDEl) + cornerHalfHeight;
                const cornerOverlappingContainer = cornerBottomEdge >= (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["innerHeight"])(trimmingContainer);
                if (cornerOverlappingContainer) {
                    const cornerTopPosition = Math.floor(top + height + this.cornerCenterPointOffset - cornerHalfHeight - cornerBorderCompensation);
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(this.corner, 'wtCornerBlockEndEdge');
                    this.cornerStyle.top = `${cornerTopPosition - 1}px`;
                }
            } else {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(this.corner, 'wtCornerBlockEndEdge');
            }
            this.cornerStyle.display = 'block';
        }
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isMobileBrowser"])() && this.instance.getSetting('isDataViewInstance')) {
            this.updateMultipleSelectionHandlesPosition(toRow, toColumn, top, inlineStartPos, width, height);
        }
    }
    /**
   * Check whether an entire column of cells is selected.
   *
   * @private
   * @param {number} startRowIndex Start row index.
   * @param {number} endRowIndex End row index.
   * @returns {boolean}
   */ isEntireColumnSelected(startRowIndex, endRowIndex) {
        return startRowIndex === this.wot.wtTable.getFirstRenderedRow() && endRowIndex === this.wot.wtTable.getLastRenderedRow();
    }
    /**
   * Check whether an entire row of cells is selected.
   *
   * @private
   * @param {number} startColumnIndex Start column index.
   * @param {number} endColumnIndex End column index.
   * @returns {boolean}
   */ isEntireRowSelected(startColumnIndex, endColumnIndex) {
        return startColumnIndex === this.wot.wtTable.getFirstRenderedColumn() && endColumnIndex === this.wot.wtTable.getLastRenderedColumn();
    }
    /**
   * Get left/top index and width/height depending on the `direction` provided.
   *
   * @private
   * @param {string} direction `rows` or `columns`, defines if an entire column or row is selected.
   * @param {number} fromIndex Start index of the selection.
   * @param {number} toIndex End index of the selection.
   * @param {number} headerIndex The header index as negative value.
   * @param {number} containerOffset Offset of the container.
   * @returns {Array|boolean} Returns an array of [headerElement, left, width] or [headerElement, top, height], depending on `direction` (`false` in case of an error getting the headers).
   */ getDimensionsFromHeader(direction, fromIndex, toIndex, headerIndex, containerOffset) {
        const { wtTable } = this.wot;
        const rootHotElement = wtTable.wtRootElement.parentNode;
        let getHeaderFn = null;
        let dimensionFn = null;
        let entireSelectionClassname = null;
        let index = null;
        let dimension = null;
        let dimensionProperty = null;
        let startHeader = null;
        let endHeader = null;
        switch(direction){
            case 'rows':
                getHeaderFn = (...args)=>wtTable.getRowHeader(args[0], args[1]);
                dimensionFn = (el)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(el);
                entireSelectionClassname = 'ht__selection--rows';
                dimensionProperty = 'top';
                break;
            case 'columns':
                getHeaderFn = (...args)=>wtTable.getColumnHeader(args[0], args[1]);
                dimensionFn = (el)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(el);
                entireSelectionClassname = 'ht__selection--columns';
                dimensionProperty = 'left';
                break;
            default:
        }
        if (entireSelectionClassname && rootHotElement.classList.contains(entireSelectionClassname)) {
            const columnHeaderLevelCount = this.wot.getSetting('columnHeaders').length;
            startHeader = getHeaderFn?.(fromIndex, columnHeaderLevelCount - headerIndex);
            endHeader = getHeaderFn?.(toIndex, columnHeaderLevelCount - headerIndex);
            if (!startHeader || !endHeader) {
                return false;
            }
            const startHeaderOffset = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(startHeader);
            const endOffset = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(endHeader);
            const startOff = startHeaderOffset[dimensionProperty];
            const endOff = endOffset[dimensionProperty];
            const contOff = containerOffset[dimensionProperty];
            index = startOff - contOff - 1;
            dimension = endOff + dimensionFn(endHeader) - startOff;
            return [
                startHeader,
                index,
                dimension
            ];
        }
        return false;
    }
    /**
   * Change border style.
   *
   * @private
   * @param {string} borderElement Coordinate where add/remove border: top, bottom, start, end.
   * @param {object} border The border object descriptor.
   */ changeBorderStyle(borderElement, border) {
        const element = this[borderElement];
        const style = element.style;
        const borderStyle = border[borderElement];
        if (!borderStyle || borderStyle.hide) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(element, 'hidden');
        } else {
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(element, 'hidden')) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(element, 'hidden');
            }
            style.backgroundColor = String(borderStyle.color ?? '');
            if (borderElement === 'top' || borderElement === 'bottom') {
                style.height = `${borderStyle.width}px`;
            }
            if (borderElement === 'start' || borderElement === 'end') {
                style.width = `${borderStyle.width}px`;
            }
        }
    }
    /**
   * Change border style to default.
   *
   * @private
   * @param {string} position The position type ("top", "bottom", "start", "end") to change.
   */ changeBorderToDefaultStyle(position) {
        const defaultBorder = {
            width: 1,
            color: '#000'
        };
        const style = this[position].style;
        style.backgroundColor = defaultBorder.color;
        style.width = `${defaultBorder.width}px`;
        style.height = `${defaultBorder.width}px`;
    }
    /**
   * Toggle class 'hidden' to element.
   *
   * @private
   * @param {string} borderElement Coordinate where add/remove border: top, bottom, start, end.
   * @param {boolean} [remove] Defines type of the action to perform.
   */ toggleHiddenClass(borderElement, remove) {
        this.changeBorderToDefaultStyle(borderElement);
        const element = this[borderElement];
        if (remove) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(element, 'hidden');
        } else {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(element, 'hidden');
        }
    }
    /**
   * Hide border.
   */ disappear() {
        this.topStyle.display = 'none';
        this.bottomStyle.display = 'none';
        this.startStyle.display = 'none';
        this.endStyle.display = 'none';
        this.cornerStyle.display = 'none';
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isMobileBrowser"])() && this.instance.getSetting('isDataViewInstance')) {
            this.selectionHandles.styles.top.display = 'none';
            this.selectionHandles.styles.topHitArea.display = 'none';
            this.selectionHandles.styles.bottom.display = 'none';
            this.selectionHandles.styles.bottomHitArea.display = 'none';
        }
    }
    /**
   * Cleans up all the DOM state related to a Border instance. Call this prior to deleting a Border instance.
   */ destroy() {
        this.eventManager.destroyWithOwnEventsOnly();
        this.main?.parentNode?.removeChild(this.main);
    }
    // TODO As this is an internal class, should be designed for using {Walkontable}. It uses the facade,
    // TODO Con. Because the class is created on place where the instance reference comes from external origin.
    // TODO Imho, the discrimination for handling both, facade and non-facade should be handled.
    /**
   * @param {WalkontableFacade} wotInstance The Walkontable instance.
   * @param {object} settings The border settings.
   */ constructor(wotInstance, settings){
        if (!settings) {
            return;
        }
        this.eventManager = wotInstance.eventManager;
        this.instance = wotInstance;
        this.wot = wotInstance;
        this.settings = settings;
        this.mouseDown = false;
        this.main = null;
        this.top = null;
        this.bottom = null;
        this.start = null;
        this.end = null;
        this.topStyle = null;
        this.bottomStyle = null;
        this.startStyle = null;
        this.endStyle = null;
        this.cornerDefaultStyle = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$border$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getCornerStyle"])(this.instance);
        // Offset to moving the corner to be centered relative to the grid.
        this.cornerCenterPointOffset = -Math.ceil(parseInt(String(this.cornerDefaultStyle.width), 10) / 2);
        this.corner = null;
        this.cornerStyle = null;
        this.createBorders(settings);
        this.registerListeners();
    }
}
var __TURBOPACK__default__export__ = Border;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/border/utils.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getCornerStyle",
    ()=>getCornerStyle
]);
const getCornerStyle = (wot)=>{
    const stylesHandler = wot.wtSettings.getSetting('stylesHandler');
    const cornerSizeFromVar = stylesHandler.getCSSVariableValue('cell-autofill-size');
    const cornerBorderWidthFromVar = stylesHandler.getCSSVariableValue('cell-autofill-border-width');
    const cornerColorFromVar = stylesHandler.getCSSVariableValue('cell-autofill-border-color');
    return Object.freeze({
        width: cornerSizeFromVar,
        height: cornerSizeFromVar,
        borderWidth: cornerBorderWidthFromVar,
        borderStyle: 'solid',
        borderColor: cornerColorFromVar
    });
};
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Selection type that is visible only if the row or column header is clicked. If that happened
 * all row or column header layers are highlighted.
 *
 * @type {string}
 */ __turbopack_context__.s([
    "ACTIVE_HEADER_TYPE",
    ()=>ACTIVE_HEADER_TYPE,
    "AREA_TYPE",
    ()=>AREA_TYPE,
    "COLUMN_TYPE",
    ()=>COLUMN_TYPE,
    "CUSTOM_SELECTION_TYPE",
    ()=>CUSTOM_SELECTION_TYPE,
    "FILL_TYPE",
    ()=>FILL_TYPE,
    "FOCUS_TYPE",
    ()=>FOCUS_TYPE,
    "HEADER_TYPE",
    ()=>HEADER_TYPE,
    "ROW_TYPE",
    ()=>ROW_TYPE
]);
const ACTIVE_HEADER_TYPE = 'active-header';
const HEADER_TYPE = 'header';
const AREA_TYPE = 'area';
const FOCUS_TYPE = 'focus';
const FILL_TYPE = 'fill';
const ROW_TYPE = 'row';
const COLUMN_TYPE = 'column';
const CUSTOM_SELECTION_TYPE = 'custom-selection';
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export ACTIVE_HEADER_TYPE as HIGHLIGHT_ACTIVE_HEADER_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HIGHLIGHT_ACTIVE_HEADER_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)"))["ACTIVE_HEADER_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export ACTIVE_HEADER_TYPE as HIGHLIGHT_ACTIVE_HEADER_TYPE> <export HIGHLIGHT_ACTIVE_HEADER_TYPE as ACTIVE_HEADER_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ACTIVE_HEADER_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__ACTIVE_HEADER_TYPE__as__HIGHLIGHT_ACTIVE_HEADER_TYPE$3e$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export ACTIVE_HEADER_TYPE as HIGHLIGHT_ACTIVE_HEADER_TYPE>"))["HIGHLIGHT_ACTIVE_HEADER_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__ACTIVE_HEADER_TYPE__as__HIGHLIGHT_ACTIVE_HEADER_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export ACTIVE_HEADER_TYPE as HIGHLIGHT_ACTIVE_HEADER_TYPE>");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export AREA_TYPE as HIGHLIGHT_AREA_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HIGHLIGHT_AREA_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)"))["AREA_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export AREA_TYPE as HIGHLIGHT_AREA_TYPE> <export HIGHLIGHT_AREA_TYPE as AREA_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "AREA_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__AREA_TYPE__as__HIGHLIGHT_AREA_TYPE$3e$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export AREA_TYPE as HIGHLIGHT_AREA_TYPE>"))["HIGHLIGHT_AREA_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__AREA_TYPE__as__HIGHLIGHT_AREA_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export AREA_TYPE as HIGHLIGHT_AREA_TYPE>");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export COLUMN_TYPE as HIGHLIGHT_COLUMN_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HIGHLIGHT_COLUMN_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)"))["COLUMN_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export CUSTOM_SELECTION_TYPE as HIGHLIGHT_CUSTOM_SELECTION_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HIGHLIGHT_CUSTOM_SELECTION_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)"))["CUSTOM_SELECTION_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FILL_TYPE as HIGHLIGHT_FILL_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HIGHLIGHT_FILL_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)"))["FILL_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FOCUS_TYPE as HIGHLIGHT_FOCUS_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HIGHLIGHT_FOCUS_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)"))["FOCUS_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FOCUS_TYPE as HIGHLIGHT_FOCUS_TYPE> <export HIGHLIGHT_FOCUS_TYPE as FOCUS_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "FOCUS_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FOCUS_TYPE__as__HIGHLIGHT_FOCUS_TYPE$3e$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FOCUS_TYPE as HIGHLIGHT_FOCUS_TYPE>"))["HIGHLIGHT_FOCUS_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FOCUS_TYPE__as__HIGHLIGHT_FOCUS_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FOCUS_TYPE as HIGHLIGHT_FOCUS_TYPE>");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export HEADER_TYPE as HIGHLIGHT_HEADER_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HIGHLIGHT_HEADER_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)"))["HEADER_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export HEADER_TYPE as HIGHLIGHT_HEADER_TYPE> <export HIGHLIGHT_HEADER_TYPE as HEADER_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HEADER_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__HEADER_TYPE__as__HIGHLIGHT_HEADER_TYPE$3e$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export HEADER_TYPE as HIGHLIGHT_HEADER_TYPE>"))["HIGHLIGHT_HEADER_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__HEADER_TYPE__as__HIGHLIGHT_HEADER_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export HEADER_TYPE as HIGHLIGHT_HEADER_TYPE>");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export ROW_TYPE as HIGHLIGHT_ROW_TYPE>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HIGHLIGHT_ROW_TYPE",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)"))["ROW_TYPE"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/manager.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "SelectionManager",
    ()=>SelectionManager
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$scanner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/scanner.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$border$2f$border$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/border/border.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
var /**
   * The overlay's Walkontable instance that are currently processed.
   *
   * @type {Walkontable}
   */ _activeOverlaysWot = /*#__PURE__*/ new WeakMap(), /**
   * The Highlight instance that holds Selections instances within it.
   *
   * @type {Highlight|null}
   */ _selections = /*#__PURE__*/ new WeakMap(), /**
   * The SelectionScanner allows to scan and collect the cell and header elements that matches
   * to the coords defined in the selections.
   *
   * @type {SelectionScanner}
   */ _scanner = /*#__PURE__*/ new WeakMap(), /**
   * The Map tracks applied CSS classes. It's used to reset the elements state to their initial state.
   *
   * @type {WeakMap}
   */ _appliedClasses = /*#__PURE__*/ new WeakMap(), /**
   * The Map tracks applied "destroy" listeners for Selection instances.
   *
   * @type {WeakMap}
   */ _destroyListeners = /*#__PURE__*/ new WeakMap(), /**
   * The Map holds references to Border classes for Selection instances which requires that when
   * the "border" setting is defined.
   *
   * @type {Map}
   */ _selectionBorders = /*#__PURE__*/ new WeakMap(), /**
   * Resets the elements to their initial state (remove the CSS classes that are added in the
   * previous render cycle).
   */ _resetCells = /*#__PURE__*/ new WeakSet();
class SelectionManager {
    /**
   * Sets the active Walkontable instance.
   *
   * @param {Walkontable} activeWot The overlays or master Walkontable instance.
   * @returns {SelectionManager}
   */ setActiveOverlay(activeWot) {
        _class_private_field_set(this, _activeOverlaysWot, activeWot);
        _class_private_field_get(this, _scanner).setActiveOverlay(_class_private_field_get(this, _activeOverlaysWot));
        if (!_class_private_field_get(this, _appliedClasses).has(_class_private_field_get(this, _activeOverlaysWot))) {
            _class_private_field_get(this, _appliedClasses).set(_class_private_field_get(this, _activeOverlaysWot), new Set());
        }
        return this;
    }
    /**
   * Gets the Selection instance of the "focus" type.
   *
   * @returns {Selection|null}
   */ getFocusSelection() {
        return _class_private_field_get(this, _selections) !== null ? _class_private_field_get(this, _selections).getFocus() : null;
    }
    /**
   * Gets the Selection instance of the "area" type.
   *
   * @returns {Selection|null}
   */ getAreaSelection() {
        return _class_private_field_get(this, _selections) !== null ? _class_private_field_get(this, _selections).createLayeredArea() : null;
    }
    /**
   * Gets the Border instance associated with Selection instance.
   *
   * @param {Selection} selection The selection instance.
   * @returns {Border|null} Returns the Border instance (new for each overlay Walkontable instance).
   */ getBorderInstance(selection) {
        if (!selection.settings.border) {
            return null;
        }
        if (_class_private_field_get(this, _selectionBorders).has(selection)) {
            const borders = _class_private_field_get(this, _selectionBorders).get(selection);
            if (borders.has(_class_private_field_get(this, _activeOverlaysWot))) {
                return borders.get(_class_private_field_get(this, _activeOverlaysWot));
            }
            const border = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$border$2f$border$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](_class_private_field_get(this, _activeOverlaysWot), selection.settings);
            borders.set(_class_private_field_get(this, _activeOverlaysWot), border);
            return border;
        }
        const border = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$border$2f$border$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](_class_private_field_get(this, _activeOverlaysWot), selection.settings);
        _class_private_field_get(this, _selectionBorders).set(selection, new Map([
            [
                _class_private_field_get(this, _activeOverlaysWot),
                border
            ]
        ]));
        return border;
    }
    /**
   * Gets all Border instances associated with Selection instance for all overlays.
   *
   * @param {Selection} selection The selection instance.
   * @returns {Border[]}
   */ getBorderInstances(selection) {
        return Array.from(_class_private_field_get(this, _selectionBorders).get(selection)?.values() ?? []);
    }
    /**
   * Destroys the Border instance associated with Selection instance.
   *
   * @param {Selection} selection The selection instance.
   */ destroyBorders(selection) {
        _class_private_field_get(this, _selectionBorders).get(selection)?.forEach((border)=>border.destroy());
        _class_private_field_get(this, _selectionBorders).delete(selection);
    }
    /**
   * Refreshes the multiple selector handle styles on all border instances after a theme change.
   */ refreshAllBorderHandleStyles() {
        _class_private_field_get(this, _selectionBorders).forEach((bordersMap)=>{
            bordersMap.forEach((border)=>{
                const hasMethod = 'updateMultipleSelectorHandlesStyles' in border && typeof border.updateMultipleSelectorHandlesStyles === 'function';
                if (hasMethod) {
                    border.updateMultipleSelectorHandlesStyles();
                }
            });
        });
    }
    /**
   * Renders all the selections (add CSS classes to cells and draw borders).
   *
   * @param {boolean} fastDraw Indicates the render cycle type (fast/slow).
   */ render(fastDraw) {
        if (_class_private_field_get(this, _selections) === null) {
            return;
        }
        if (fastDraw) {
            // there was no rerender, so we need to remove classNames by ourselves
            _class_private_method_get(this, _resetCells, resetCells).call(this);
        }
        const selections = Array.from(_class_private_field_get(this, _selections));
        const classNamesMap = new Map();
        const headerAttributesMap = new Map();
        for(let i = 0; i < selections.length; i++){
            const selection = selections[i];
            const { className, headerAttributes, createLayers, selectionType } = selection.settings;
            if (!_class_private_field_get(this, _destroyListeners).has(selection)) {
                _class_private_field_get(this, _destroyListeners).add(selection);
                selection.addLocalHook('destroy', ()=>this.destroyBorders(selection));
            }
            const borderInstance = this.getBorderInstance(selection);
            if (selection.isEmpty()) {
                borderInstance?.disappear();
                continue; // eslint-disable-line no-continue
            }
            if (className) {
                const elements = _class_private_field_get(this, _scanner).setActiveSelection(selection).scan();
                elements.forEach((element)=>{
                    if (classNamesMap.has(element)) {
                        const classNamesLayers = classNamesMap.get(element);
                        if (classNamesLayers.has(className) && createLayers === true) {
                            classNamesLayers.set(className, (classNamesLayers.get(className) ?? 0) + 1);
                        } else {
                            classNamesLayers.set(className, 1);
                        }
                    } else {
                        classNamesMap.set(element, new Map([
                            [
                                className,
                                1
                            ]
                        ]));
                    }
                    if (headerAttributes) {
                        if (!headerAttributesMap.has(element)) {
                            headerAttributesMap.set(element, []);
                        }
                        if (element.nodeName === 'TH') {
                            const attrs = headerAttributes;
                            headerAttributesMap.get(element).push(...attrs);
                        }
                    }
                });
            }
            const corners = selection.getCorners();
            _class_private_field_get(this, _activeOverlaysWot).getSetting('onBeforeDrawBorders', corners, selectionType);
            borderInstance?.appear(corners);
        }
        classNamesMap.forEach((classNamesLayers, element)=>{
            const classNames = Array.from(classNamesLayers).map(([className, occurrenceCount])=>{
                if (occurrenceCount === 1) {
                    return className;
                }
                return [
                    className,
                    ...Array.from({
                        length: occurrenceCount - 1
                    }, (_, i)=>`${className}-${i + 1}`)
                ];
            }).flat();
            classNames.forEach((className)=>_class_private_field_get(this, _appliedClasses).get(_class_private_field_get(this, _activeOverlaysWot))?.add(className));
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(element, classNames);
            if (element.nodeName === 'TD' && Array.isArray(_class_private_field_get(this, _selections).options?.cellAttributes)) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(element, _class_private_field_get(this, _selections).options.cellAttributes);
            }
        });
        // Set the attributes for the headers if they're focused.
        Array.from(headerAttributesMap.keys()).forEach((element)=>{
            // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(element, [
                ...headerAttributesMap.get(element)
            ]);
        });
    }
    /**
   * Creates a new SelectionManager instance.
   *
   * @param {SelectionsContainer | null} selections The Highlight instance that holds Selection instances.
   */ constructor(selections){
        _class_private_method_init(this, _resetCells);
        _class_private_field_init(this, _activeOverlaysWot, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _selections, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _scanner, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _appliedClasses, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _destroyListeners, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _selectionBorders, {
            writable: true,
            value: void 0
        });
        _class_private_field_set(this, _activeOverlaysWot, null);
        _class_private_field_set(this, _scanner, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$scanner$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["SelectionScanner"]());
        _class_private_field_set(this, _appliedClasses, new WeakMap());
        _class_private_field_set(this, _destroyListeners, new WeakSet());
        _class_private_field_set(this, _selectionBorders, new Map());
        _class_private_field_set(this, _selections, selections);
    }
}
function resetCells() {
    const appliedOverlaysClasses = _class_private_field_get(this, _appliedClasses).get(_class_private_field_get(this, _activeOverlaysWot));
    if (!appliedOverlaysClasses) {
        return;
    }
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    const classesToRemove = _class_private_field_get(this, _activeOverlaysWot).wtSettings.getSetting('onBeforeRemoveCellClassNames');
    if (Array.isArray(classesToRemove)) {
        for(let i = 0; i < classesToRemove.length; i++){
            appliedOverlaysClasses.add(classesToRemove[i]);
        }
    }
    appliedOverlaysClasses.forEach((className)=>{
        const nodes = _class_private_field_get(this, _activeOverlaysWot).wtTable.TABLE.querySelectorAll(`.${className}`);
        let cellAttributes = [];
        if (Array.isArray(_class_private_field_get(this, _selections).options?.cellAttributes)) {
            cellAttributes = _class_private_field_get(this, _selections).options.cellAttributes.map((el)=>el[0]);
        }
        if (Array.isArray(_class_private_field_get(this, _selections).options?.headerAttributes)) {
            cellAttributes = [
                ...cellAttributes,
                ..._class_private_field_get(this, _selections).options.headerAttributes.map((el)=>el[0])
            ];
        }
        for(let i = 0, len = nodes.length; i < len; i++){
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeClass"])(nodes[i], className);
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeAttribute"])(nodes[i], cellAttributes);
        }
    });
    appliedOverlaysClasses.clear();
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/scanner.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "SelectionScanner",
    ()=>SelectionScanner
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
/* eslint-disable no-continue */ function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
var /**
   * Active Selection instance to process.
   *
   * @type {Selection}
   */ _selection = /*#__PURE__*/ new WeakMap(), /**
   * The Walkontable instance that the scans depends on.
   *
   * @type {Walkontable}
   */ _activeOverlaysWot = /*#__PURE__*/ new WeakMap(), /**
   * The method triggers a callback for each rendered cell.
   *
   * @param {function(number, number): void} callback The callback function to trigger.
   */ _scanCellsRange = /*#__PURE__*/ new WeakSet(), /**
   * The method triggers a callback for each rendered cell including headers.
   *
   * @param {function(number, number): void} callback The callback function to trigger.
   */ _scanViewportRange = /*#__PURE__*/ new WeakSet();
class SelectionScanner {
    /**
   * Sets the Walkontable instance that will be taking into account while scanning the table.
   *
   * @param {Walkontable} activeOverlaysWot The Walkontable instance.
   * @returns {SelectionScanner}
   */ setActiveOverlay(activeOverlaysWot) {
        _class_private_field_set(this, _activeOverlaysWot, activeOverlaysWot);
        return this;
    }
    /**
   * Sets the Selection instance to process.
   *
   * @param {Selection} selection The Selection instance.
   * @returns {SelectionScanner}
   */ setActiveSelection(selection) {
        _class_private_field_set(this, _selection, selection);
        return this;
    }
    /**
   * Scans the rendered table with selection and returns elements that intersects
   * with selection coordinates.
   *
   * @returns {HTMLTableElement[]}
   */ scan() {
        const selectionType = _class_private_field_get(this, _selection).settings.selectionType;
        const elements = new Set();
        // TODO(improvement): use heuristics from coords to detect what type of scan
        // the Selection needs instead of using `selectionType` property.
        if (selectionType === 'active-header') {
            this.scanColumnsInHeadersRange((element)=>elements.add(element));
            this.scanRowsInHeadersRange((element)=>elements.add(element));
        } else if (selectionType === 'area') {
            this.scanCellsRange((element)=>elements.add(element));
        } else if (selectionType === 'focus') {
            this.scanColumnsInHeadersRange((element)=>elements.add(element));
            this.scanRowsInHeadersRange((element)=>elements.add(element));
            this.scanCellsRange((element)=>elements.add(element));
        } else if (selectionType === 'fill') {
            this.scanCellsRange((element)=>elements.add(element));
        } else if (selectionType === 'header') {
            this.scanColumnsInHeadersRange((element)=>elements.add(element));
            this.scanRowsInHeadersRange((element)=>elements.add(element));
        } else if (selectionType === 'row') {
            this.scanRowsInHeadersRange((element)=>elements.add(element));
            this.scanRowsInCellsRange((element)=>elements.add(element));
        } else if (selectionType === 'column') {
            this.scanColumnsInHeadersRange((element)=>elements.add(element));
            this.scanColumnsInCellsRange((element)=>elements.add(element));
        }
        return elements;
    }
    /**
   * Scans the table (only rendered headers) and collect all column headers (TH) that match
   * the coordinates passed in the Selection instance.
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */ scanColumnsInHeadersRange(callback) {
        const [topRow, topColumn, bottomRow, bottomColumn] = _class_private_field_get(this, _selection).getCorners();
        const { wtTable } = _class_private_field_get(this, _activeOverlaysWot);
        const renderedColumnsCount = wtTable.getRenderedColumnsCount();
        const columnHeadersCount = wtTable.getColumnHeadersCount();
        let cursor = 0;
        for(let column = -wtTable.getRowHeadersCount(); column < renderedColumnsCount; column++){
            const sourceColumn = wtTable.columnFilter.renderedToSource(column);
            if (sourceColumn < topColumn || sourceColumn > bottomColumn) {
                continue;
            }
            for(let headerLevel = -columnHeadersCount; headerLevel < 0; headerLevel++){
                if (headerLevel < topRow || headerLevel > bottomRow) {
                    continue;
                }
                const positiveBasedHeaderLevel = headerLevel + columnHeadersCount;
                let TH = wtTable.getColumnHeader(sourceColumn, positiveBasedHeaderLevel);
                const newSourceCol = _class_private_field_get(this, _activeOverlaysWot).getSetting('onBeforeHighlightingColumnHeader', sourceColumn, positiveBasedHeaderLevel, {
                    selectionType: _class_private_field_get(this, _selection).settings.selectionType,
                    columnCursor: cursor,
                    selectionWidth: bottomColumn - topColumn + 1
                });
                if (newSourceCol === null) {
                    continue;
                }
                if (newSourceCol !== sourceColumn) {
                    TH = wtTable.getColumnHeader(newSourceCol, positiveBasedHeaderLevel);
                }
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(TH)) {
                    callback(TH);
                }
            }
            cursor += 1;
        }
    }
    /**
   * Scans the table (only rendered headers) and collect all row headers (TH) that match
   * the coordinates passed in the Selection instance.
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */ scanRowsInHeadersRange(callback) {
        const [topRow, topColumn, bottomRow, bottomColumn] = _class_private_field_get(this, _selection).getCorners();
        const { wtTable } = _class_private_field_get(this, _activeOverlaysWot);
        const renderedRowsCount = wtTable.getRenderedRowsCount();
        const rowHeadersCount = wtTable.getRowHeadersCount();
        let cursor = 0;
        for(let row = -wtTable.getColumnHeadersCount(); row < renderedRowsCount; row++){
            const sourceRow = wtTable.rowFilter.renderedToSource(row);
            if (sourceRow < topRow || sourceRow > bottomRow) {
                continue;
            }
            for(let headerLevel = -rowHeadersCount; headerLevel < 0; headerLevel++){
                if (headerLevel < topColumn || headerLevel > bottomColumn) {
                    continue;
                }
                const positiveBasedHeaderLevel = headerLevel + rowHeadersCount;
                let TH = wtTable.getRowHeader(sourceRow, positiveBasedHeaderLevel);
                const newSourceRow = _class_private_field_get(this, _activeOverlaysWot).getSetting('onBeforeHighlightingRowHeader', sourceRow, positiveBasedHeaderLevel, {
                    selectionType: _class_private_field_get(this, _selection).settings.selectionType,
                    rowCursor: cursor,
                    selectionHeight: bottomRow - topRow + 1
                });
                if (newSourceRow === null) {
                    continue;
                }
                if (newSourceRow !== sourceRow) {
                    TH = wtTable.getRowHeader(newSourceRow, positiveBasedHeaderLevel);
                }
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(TH)) {
                    callback(TH);
                }
            }
            cursor += 1;
        }
    }
    /**
   * Scans the table (only rendered cells) and collect all cells (TR) that match
   * the coordinates passed in the Selection instance.
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */ scanCellsRange(callback) {
        const { wtTable } = _class_private_field_get(this, _activeOverlaysWot);
        _class_private_method_get(this, _scanCellsRange, scanCellsRange).call(this, (sourceRow, sourceColumn)=>{
            const cell = wtTable.getCell(_class_private_field_get(this, _activeOverlaysWot).createCellCoords(sourceRow, sourceColumn));
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(cell)) {
                return;
            }
            // support for old API
            const additionalSelectionClass = _class_private_field_get(this, _activeOverlaysWot).getSetting('onAfterDrawSelection', sourceRow, sourceColumn, _class_private_field_get(this, _selection).settings.layerLevel);
            if (typeof additionalSelectionClass === 'string') {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addClass"])(cell, additionalSelectionClass);
            }
            callback(cell);
        });
    }
    /**
   * Scans the table (only rendered cells) and collects all cells (TR) that match the coordinates
   * passed in the Selection instance but only for the X axis (rows).
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */ scanRowsInCellsRange(callback) {
        // eslint-disable-next-line comma-spacing
        const [topRow, , bottomRow] = _class_private_field_get(this, _selection).getCorners();
        const { wtTable } = _class_private_field_get(this, _activeOverlaysWot);
        _class_private_method_get(this, _scanViewportRange, scanViewportRange).call(this, (sourceRow, sourceColumn)=>{
            if (sourceRow >= topRow && sourceRow <= bottomRow) {
                const cell = wtTable.getCell(_class_private_field_get(this, _activeOverlaysWot).createCellCoords(sourceRow, sourceColumn));
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(cell)) {
                    callback(cell);
                }
            }
        });
    }
    /**
   * Scans the table (only rendered cells) and collects all cells (TR) that match the coordinates
   * passed in the Selection instance but only for the Y axis (columns).
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */ scanColumnsInCellsRange(callback) {
        const [, topColumn, , bottomColumn] = _class_private_field_get(this, _selection).getCorners();
        const { wtTable } = _class_private_field_get(this, _activeOverlaysWot);
        _class_private_method_get(this, _scanViewportRange, scanViewportRange).call(this, (sourceRow, sourceColumn)=>{
            if (sourceColumn >= topColumn && sourceColumn <= bottomColumn) {
                const cell = wtTable.getCell(_class_private_field_get(this, _activeOverlaysWot).createCellCoords(sourceRow, sourceColumn));
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(cell)) {
                    callback(cell);
                }
            }
        });
    }
    constructor(){
        _class_private_method_init(this, _scanCellsRange);
        _class_private_method_init(this, _scanViewportRange);
        _class_private_field_init(this, _selection, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _activeOverlaysWot, {
            writable: true,
            value: void 0
        });
        _class_private_field_set(this, _selection, null);
        _class_private_field_set(this, _activeOverlaysWot, null);
    }
}
function scanCellsRange(callback) {
    let [topRow, startColumn, bottomRow, endColumn] = _class_private_field_get(this, _selection).getCorners();
    if (topRow < 0 && bottomRow < 0 || startColumn < 0 && endColumn < 0) {
        return;
    }
    const { wtTable } = _class_private_field_get(this, _activeOverlaysWot);
    const isMultiple = topRow !== bottomRow || startColumn !== endColumn;
    startColumn = Math.max(startColumn ?? 0, 0);
    endColumn = Math.max(endColumn ?? 0, 0);
    topRow = Math.max(topRow ?? 0, 0);
    bottomRow = Math.max(bottomRow ?? 0, 0);
    if (isMultiple) {
        startColumn = Math.max(startColumn, wtTable.getFirstRenderedColumn());
        endColumn = Math.min(endColumn, wtTable.getLastRenderedColumn());
        topRow = Math.max(topRow, wtTable.getFirstRenderedRow());
        bottomRow = Math.min(bottomRow, wtTable.getLastRenderedRow());
        if (endColumn < startColumn || bottomRow < topRow) {
            return;
        }
    } else {
        const cell = wtTable.getCell(_class_private_field_get(this, _activeOverlaysWot).createCellCoords(topRow, startColumn));
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(cell)) {
            return;
        }
    }
    for(let row = topRow; row <= bottomRow; row += 1){
        for(let column = startColumn; column <= endColumn; column += 1){
            callback(row, column);
        }
    }
}
function scanViewportRange(callback) {
    const { wtTable } = _class_private_field_get(this, _activeOverlaysWot);
    const renderedRowsCount = wtTable.getRenderedRowsCount();
    const renderedColumnsCount = wtTable.getRenderedColumnsCount();
    for(let row = 0; row < renderedRowsCount; row += 1){
        const sourceRow = wtTable.rowFilter.renderedToSource(row);
        for(let column = 0; column < renderedColumnsCount; column += 1){
            callback(sourceRow, wtTable.columnFilter.renderedToSource(column));
        }
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/selection.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$mixins$2f$localHooks$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/mixins/localHooks.mjs [client] (ecmascript)");
;
;
/**
 * The Selection class allows highlighting (by applying CSS class) the table's cells or headers
 * and setting up the borders if defined in the settings.
 *
 * The Selection coordinates may point to the cells (positive numbers) or headers (negative numbers).
 *
 * @class Selection
 */ class Selection {
    /**
   * Checks if selection is empty.
   *
   * @returns {boolean}
   */ isEmpty() {
        return this.cellRange === null;
    }
    /**
   * Adds a cell coords to the selection.
   *
   * @param {CellCoords} coords The cell coordinates to add.
   * @returns {Selection}
   */ add(coords) {
        if (this.isEmpty()) {
            this.cellRange = this.settings.createCellRange(coords);
        } else {
            this.cellRange.expand(coords);
        }
        return this;
    }
    /**
   * If selection range from or to property equals oldCoords, replace it with newCoords. Return boolean
   * information about success.
   *
   * @param {CellCoords} oldCoords An old cell coordinates to replace.
   * @param {CellCoords} newCoords The new cell coordinates.
   * @returns {boolean}
   */ replace(oldCoords, newCoords) {
        if (!this.isEmpty()) {
            if (this.cellRange.from.isEqual(oldCoords)) {
                this.cellRange.from = newCoords;
                return true;
            }
            if (this.cellRange.to.isEqual(oldCoords)) {
                this.cellRange.to = newCoords;
                return true;
            }
        }
        return false;
    }
    /**
   * Clears selection.
   *
   * @returns {Selection}
   */ clear() {
        this.cellRange = null;
        return this;
    }
    /**
   * Returns the top left (or top right in RTL) and bottom right (or bottom left in RTL) selection coordinates.
   *
   * @returns {number[]} Returns array of coordinates for example `[1, 1, 5, 5]`.
   */ getCorners() {
        const topStart = this.cellRange.getOuterTopStartCorner();
        const bottomEnd = this.cellRange.getOuterBottomEndCorner();
        return [
            topStart.row ?? 0,
            topStart.col ?? 0,
            bottomEnd.row ?? 0,
            bottomEnd.col ?? 0
        ];
    }
    /**
   * Destroys the instance.
   */ destroy() {
        this.runLocalHooks('destroy');
    }
    /**
   * @param {object} settings The selection settings object. @todo type.
   * @param {CellRange} cellRange The cell range instance.
   */ constructor(settings, cellRange){
        this.settings = settings;
        this.cellRange = cellRange || null;
    }
}
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(Selection, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$mixins$2f$localHooks$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
var __TURBOPACK__default__export__ = Selection;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/selection.mjs [client] (ecmascript) <export default as Selection>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "Selection",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$selection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/selection.mjs [client] (ecmascript)"))["default"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$selection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/selection.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/settings.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>Settings
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
;
class Settings {
    /**
   * Generate defaults for a settings.
   * Void 0 means it is required, null means it can be empty.
   *
   * @private
   * @returns {SettingsPure}
   */ getDefaults() {
        return {
            facade: undefined,
            table: undefined,
            // Determines whether the Walkontable instance is used as dataset viewer. When its instance is used as
            // a context menu, autocomplete list, etc, the returned value is `false`.
            isDataViewInstance: true,
            // presentation mode
            externalRowCalculator: false,
            currentRowClassName: null,
            currentColumnClassName: null,
            preventOverflow () {
                return false;
            },
            preventWheel: false,
            // data source
            data: undefined,
            // Number of renderable columns for the left overlay.
            fixedColumnsStart: 0,
            // Number of renderable rows for the top overlay.
            fixedRowsTop: 0,
            // Number of renderable rows for the bottom overlay.
            fixedRowsBottom: 0,
            // Enable the inline start overlay when conditions are met (left for LTR and right for RTL document mode).
            shouldRenderInlineStartOverlay: ()=>{
                return this.getSetting('fixedColumnsStart') > 0 || this.getSetting('rowHeaders').length > 0;
            },
            // Enable the top overlay when conditions are met.
            shouldRenderTopOverlay: ()=>{
                return this.getSetting('fixedRowsTop') > 0 || this.getSetting('columnHeaders').length > 0;
            },
            // Enable the bottom overlay when conditions are met.
            shouldRenderBottomOverlay: ()=>{
                return this.getSetting('fixedRowsBottom') > 0;
            },
            minSpareRows: 0,
            // this must be array of functions: [function (row, TH) {}]
            rowHeaders () {
                return [];
            },
            // this must be array of functions: [function (column, TH) {}]
            columnHeaders () {
                return [];
            },
            totalRows: undefined,
            totalColumns: undefined,
            cellRenderer: (row, column, TD)=>{
                // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
                const cellData = this.getSetting('data', row, column);
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["fastInnerText"])(TD, cellData === undefined || cellData === null ? '' : cellData);
            },
            // columnWidth: 50,
            columnWidth () {
            // return undefined means use default size for the rendered cell content
            },
            rowHeight () {
            // return undefined means use default size for the rendered cell content
            },
            rowHeightByOverlayName () {
            // return undefined means use default size for the rendered cell content
            },
            defaultColumnWidth: 50,
            selections: null,
            hideBorderOnMouseDownOver: false,
            viewportRowCalculatorOverride: null,
            viewportColumnCalculatorOverride: null,
            viewportRowRenderingThreshold: null,
            viewportColumnRenderingThreshold: null,
            // callbacks
            onCellMouseDown: null,
            onCellContextMenu: null,
            onCellMouseOver: null,
            onCellMouseOverOutside: null,
            onCellMouseOut: null,
            onCellMouseUp: null,
            // onCellMouseOut: null,
            onCellDblClick: null,
            onCellCornerMouseDown: null,
            onCellCornerDblClick: null,
            beforeDraw: null,
            onDraw: null,
            onBeforeRemoveCellClassNames: null,
            onAfterDrawSelection: null,
            onBeforeDrawBorders: null,
            // viewport scroll hooks
            onBeforeViewportScrollHorizontally: (column)=>column,
            onBeforeViewportScrollVertically: (row)=>row,
            // native scroll hooks
            onScrollHorizontally: null,
            onScrollVertically: null,
            //
            onBeforeTouchScroll: null,
            onAfterMomentumScroll: null,
            onModifyRowHeaderWidth: null,
            onModifyGetCellCoords: null,
            onModifyGetCoordsElement: null,
            onModifyGetCoords: null,
            onBeforeHighlightingRowHeader: (sourceRow)=>sourceRow,
            onBeforeHighlightingColumnHeader: (sourceCol)=>sourceCol,
            onWindowResize: null,
            onContainerElementResize: null,
            renderAllColumns: false,
            renderAllRows: false,
            groups: false,
            rowHeaderWidth: null,
            columnHeaderHeight: null,
            headerClassName: null,
            rtlMode: false,
            ariaTags: true,
            stylesHandler: null
        };
    }
    /**
   * Update settings.
   *
   * @param {object|string} settings The singular settings to update or if passed as object to merge with.
   * @param {*} value The value to set if the first argument is passed as string.
   * @returns {Settings}
   */ update(settings, value) {
        if (value === undefined) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["objectEach"])(settings, (settingValue, key)=>{
                this.settings[key] = settingValue;
            });
        } else {
            this.settings[settings] = value;
        }
        return this;
    }
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    getSetting(key, param1, param2, param3, param4) {
        if (typeof this.settings[key] === 'function') {
            return this.settings[key](param1, param2, param3, param4);
        } else if (param1 !== undefined && Array.isArray(this.settings[key])) {
            return this.settings[key][param1];
        }
        return this.settings[key];
    }
    // eslint-disable-next-line jsdoc/require-jsdoc -- TypeScript overload implementation; documented in the overload signature above
    getSettingPure(key) {
        return this.settings[key];
    }
    /**
   * Checks if setting exists.
   *
   * @param {boolean} key The settings key to check.
   * @returns {boolean}
   */ has(key) {
        return !!this.settings[key];
    }
    /**
   * @param {SettingsPure} settings The user defined settings.
   */ constructor(settings){
        /**
   * Reference to settings.
   *
   * @protected
   * @type {SettingsPure}
   */ this.settings = {};
        /**
   * The defaults values of settings.
   * Void 0 means it is required, null means it can be empty.
   *
   * @public
   * @type {Readonly<SettingsPure>}
   */ this.defaults = Object.freeze(this.getDefaults());
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["objectEach"])(this.defaults, (value, key)=>{
            if (settings[key] !== undefined) {
                this.settings[key] = settings[key];
            } else if (value === undefined) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])(`A required setting "${key}" was not provided`);
            } else {
                this.settings[key] = value;
            }
        });
    }
}
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/stickyScrollStrategy.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "StickyScrollStrategy",
    ()=>StickyScrollStrategy
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/browser.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
var _overlays = /*#__PURE__*/ new WeakMap(), /**
   * Whether sticky-scroll mode is currently active.
   *
   * @type {boolean}
   */ _active = /*#__PURE__*/ new WeakMap(), /**
   * Whether the mouse button is currently pressed.
   *
   * @type {boolean}
   */ _mouseDown = /*#__PURE__*/ new WeakMap(), /**
   * Activates sticky-scroll mode: switches spreaders to `position: sticky`
   * using the exact visual offset so no jump occurs.
   */ _activate = /*#__PURE__*/ new WeakSet(), /**
   * Returns the current vertical scroll offset in the hider's coordinate system.
   *
   * @returns {number}
   */ _getScrollTop = /*#__PURE__*/ new WeakSet(), /**
   * Returns the current horizontal scroll offset in the hider's coordinate system.
   *
   * @returns {number}
   */ _getScrollLeft = /*#__PURE__*/ new WeakSet(), /**
   * Sets the scroll position, converting from hider coordinates back to absolute.
   *
   * @param {number} top Vertical offset (hider coordinates).
   * @param {number} left Horizontal offset (hider coordinates).
   */ _setScrollPosition = /*#__PURE__*/ new WeakSet(), /**
   * Clears inset offset inline styles so `position: relative` is not shifted
   * by leftover `top` / `left` / `right` / `bottom` from sticky mode.
   *
   * @param {HTMLElement} element The spreader element.
   */ _clearSpreaderInsetStyles = /*#__PURE__*/ new WeakSet(), /**
   * Applies position styles to the master spreader and (in element-scroll mode)
   * the overlay clone spreaders.
   *
   * @param {'sticky'|'relative'} position The CSS position value.
   * @param {number} stickyTop The vertical offset (only used for sticky).
   * @param {number} stickyLeft The horizontal offset (only used for sticky).
   */ _applySpreaderStyles = /*#__PURE__*/ new WeakSet(), /**
   * Applies position styles to a single overlay clone spreader when its overlay is fully rendered.
   *
   * @param {Overlay} overlay The overlay whose clone spreader to update.
   * @param {'sticky'|'relative'} position The CSS position value.
   * @param {boolean} isSticky Whether sticky mode is active.
   * @param {number|null} stickyTop The vertical sticky offset, or null to skip.
   * @param {number|null} stickyLeft The horizontal sticky offset, or null to skip.
   * @param {boolean} isRtl Whether RTL mode is active.
   */ _applyCloneSpreaderStyles = /*#__PURE__*/ new WeakSet(), /**
   * Returns whether the mousedown event originated from a native scrollbar.
   *
   * When a user clicks a native browser scrollbar, the event target is the
   * scrollable container itself (the scrollbar is part of the element's
   * rendering but not a child DOM node). Clicks on child elements (cells,
   * headers, etc.) set a descendant as the target, so they are excluded.
   *
   * @param {MouseEvent} event The mousedown event.
   * @returns {boolean}
   */ _isScrollbarTarget = /*#__PURE__*/ new WeakSet(), /**
   * Returns whether the scrollable element is the window.
   *
   * @returns {boolean}
   */ _isWindowScroll = /*#__PURE__*/ new WeakSet(), /**
   * Returns whether the scrollable element is in RTL mode.
   *
   * @returns {boolean}
   */ _isRtl = /*#__PURE__*/ new WeakSet();
class StickyScrollStrategy {
    /**
   * Registers mousedown/mouseup listeners for scrollbar drag detection.
   * Called from `Overlays.registerListeners()`.
   */ registerListeners() {
        const { eventManager, domBindings: { rootDocument } } = _class_private_field_get(this, _overlays);
        // Scrollable elements may have changed (e.g. updateSettings). Discard any
        // leftover sticky state so a stale deactivate() cannot compute a wrong scroll.
        if (_class_private_field_get(this, _active)) {
            _class_private_field_set(this, _active, false);
            _class_private_method_get(this, _applySpreaderStyles, applySpreaderStyles).call(this, 'relative', 0, 0);
        }
        _class_private_field_set(this, _mouseDown, false);
        eventManager.addEventListener(rootDocument, 'mousedown', (event)=>{
            if (event instanceof MouseEvent) {
                _class_private_field_set(this, _mouseDown, _class_private_method_get(this, _isScrollbarTarget, isScrollbarTarget).call(this, event));
            }
        });
        eventManager.addEventListener(rootDocument, 'mouseup', ()=>{
            _class_private_field_set(this, _mouseDown, false);
            if (_class_private_field_get(this, _active)) {
                this.deactivate();
            }
        });
    }
    /**
   * Checks whether sticky-scroll should activate based on the current state.
   * Called from `Overlays.syncScrollPositions()` after scroll direction is determined.
   *
   * @param {boolean} verticalScrolling Whether vertical scrolling occurred.
   * @param {boolean} horizontalScrolling Whether horizontal scrolling occurred.
   */ tryActivate(verticalScrolling, horizontalScrolling) {
        if (_class_private_field_get(this, _mouseDown) && !_class_private_field_get(this, _active) && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$browser$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isSafari"])() && (verticalScrolling || horizontalScrolling)) {
            _class_private_method_get(this, _activate, activate).call(this);
        }
    }
    /**
   * Recalculates and applies sticky offsets on all spreaders. Called after
   * every render (both full and fast draws) to keep the sticky offset in
   * sync with the current scroll position.
   */ syncOffsets() {
        if (!_class_private_field_get(this, _active)) {
            return;
        }
        const overlays = _class_private_field_get(this, _overlays);
        const { wtViewport } = overlays.wot;
        const startTop = wtViewport.rowsRenderCalculator?.startPosition;
        const startLeft = wtViewport.columnsRenderCalculator?.startPosition;
        // startPosition is null when nothing is rendered (empty dataset or trimmed-away rows/columns).
        // Arithmetic with null produces NaN, which would set "NaNpx" on the style. Skip the update.
        if (typeof startTop !== 'number' || typeof startLeft !== 'number') {
            return;
        }
        const stickyTop = startTop - _class_private_method_get(this, _getScrollTop, getScrollTop).call(this);
        const stickyLeft = startLeft - _class_private_method_get(this, _getScrollLeft, getScrollLeft).call(this);
        _class_private_method_get(this, _applySpreaderStyles, applySpreaderStyles).call(this, 'sticky', stickyTop, stickyLeft);
    }
    /**
   * Cleans up on destroy.
   */ destroy() {
        if (_class_private_field_get(this, _active)) {
            _class_private_method_get(this, _applySpreaderStyles, applySpreaderStyles).call(this, 'relative', 0, 0);
        }
        _class_private_field_set(this, _active, false);
        _class_private_field_set(this, _mouseDown, false);
    }
    /**
   * Deactivates sticky-scroll mode: captures the current offsets, adjusts
   * the scroll position to match, then restores `position: relative` and
   * triggers a final full render.
   */ deactivate() {
        const overlays = _class_private_field_get(this, _overlays);
        const spreader = overlays.wtTable.spreader;
        const { wtViewport } = overlays.wot;
        const isRtl = overlays.wtSettings.getSetting('rtlMode');
        const leftProp = isRtl ? 'right' : 'left';
        // Render while still active (same code path as during drag).
        overlays.refreshAll();
        // Capture the sticky offsets and computed start positions.
        const lastStickyTop = Number.parseInt(spreader.style.top, 10) || 0;
        const lastStickyLeft = Number.parseInt(spreader.style[leftProp], 10) || 0;
        const startTop = wtViewport.rowsRenderCalculator?.startPosition;
        const startLeft = wtViewport.columnsRenderCalculator?.startPosition;
        // Adjust scroll position while still active to prevent cascading renders.
        const targetScrollTop = typeof startTop === 'number' ? startTop - lastStickyTop : _class_private_method_get(this, _getScrollTop, getScrollTop).call(this);
        const targetScrollLeft = typeof startLeft === 'number' ? startLeft - lastStickyLeft : _class_private_method_get(this, _getScrollLeft, getScrollLeft).call(this);
        _class_private_method_get(this, _setScrollPosition, setScrollPosition).call(this, targetScrollTop, targetScrollLeft);
        // Deactivate and restore relative positioning.
        _class_private_field_set(this, _active, false);
        _class_private_method_get(this, _applySpreaderStyles, applySpreaderStyles).call(this, 'relative', 0, 0);
        // Final render in normal mode at the corrected scroll position.
        overlays.refreshAll();
        overlays.applyToDOM();
    }
    /**
   * @param {Overlays} overlays The parent Overlays instance.
   */ constructor(overlays){
        _class_private_method_init(this, _activate);
        _class_private_method_init(this, _getScrollTop);
        _class_private_method_init(this, _getScrollLeft);
        _class_private_method_init(this, _setScrollPosition);
        _class_private_method_init(this, _clearSpreaderInsetStyles);
        _class_private_method_init(this, _applySpreaderStyles);
        _class_private_method_init(this, _applyCloneSpreaderStyles);
        _class_private_method_init(this, _isScrollbarTarget);
        _class_private_method_init(this, _isWindowScroll);
        _class_private_method_init(this, _isRtl);
        /**
   * Reference to the parent Overlays coordinator.
   *
   * @type {OverlaysLike}
   */ _class_private_field_init(this, _overlays, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _active, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _mouseDown, {
            writable: true,
            value: void 0
        });
        _class_private_field_set(this, _active, false);
        _class_private_field_set(this, _mouseDown, false);
        _class_private_field_set(this, _overlays, overlays);
    }
}
function activate() {
    const overlays = _class_private_field_get(this, _overlays);
    const spreader = overlays.wtTable.spreader;
    const isRtl = overlays.wtSettings.getSetting('rtlMode');
    const leftProp = isRtl ? 'right' : 'left';
    const startTop = Number.parseInt(spreader.style.top, 10) || 0;
    const stickyTop = startTop - _class_private_method_get(this, _getScrollTop, getScrollTop).call(this);
    const startLeft = Number.parseInt(spreader.style[leftProp], 10) || 0;
    const stickyLeft = startLeft - _class_private_method_get(this, _getScrollLeft, getScrollLeft).call(this);
    _class_private_field_set(this, _active, true);
    _class_private_method_get(this, _applySpreaderStyles, applySpreaderStyles).call(this, 'sticky', stickyTop, stickyLeft);
}
function getScrollTop() {
    const { topOverlay } = _class_private_field_get(this, _overlays);
    return topOverlay.getScrollPosition() - topOverlay.getTableParentOffset();
}
function getScrollLeft() {
    const { inlineStartOverlay } = _class_private_field_get(this, _overlays);
    // For RTL window scrolling, scrollX increases as the user scrolls left (away from
    // the right-edge origin). getTableParentOffset() returns the left-edge offset which
    // doesn't apply to the RTL coordinate system, so skip it and return scrollX directly.
    if (_class_private_method_get(this, _isRtl, isRtl).call(this) && _class_private_method_get(this, _isWindowScroll, isWindowScroll).call(this)) {
        return Math.abs(inlineStartOverlay.getScrollPosition());
    }
    return Math.abs(inlineStartOverlay.getScrollPosition()) - inlineStartOverlay.getTableParentOffset();
}
function setScrollPosition(top, left) {
    const overlays = _class_private_field_get(this, _overlays);
    if (_class_private_method_get(this, _isWindowScroll, isWindowScroll).call(this)) {
        const { rootWindow } = overlays.domBindings;
        // RTL + window-scroll: left is in scrollX coordinates (distance from the right-edge
        // origin), so pass it directly. LTR: add back the table's left page offset.
        const absoluteLeft = _class_private_method_get(this, _isRtl, isRtl).call(this) ? left : left + overlays.inlineStartOverlay.getTableParentOffset();
        rootWindow.scrollTo(absoluteLeft, top + overlays.topOverlay.getTableParentOffset());
    } else {
        overlays.wtTable.holder.scrollTop = top;
        overlays.wtTable.holder.scrollLeft = _class_private_method_get(this, _isRtl, isRtl).call(this) ? -left : left;
    }
}
function clearSpreaderInsetStyles(element) {
    element.style.top = '';
    element.style.bottom = '';
    element.style.left = '';
    element.style.right = '';
}
function applySpreaderStyles(position, stickyTop, stickyLeft) {
    const overlays = _class_private_field_get(this, _overlays);
    const spreader = overlays.wtTable.spreader;
    const isRtl1 = _class_private_method_get(this, _isRtl, isRtl).call(this);
    const leftProp = isRtl1 ? 'right' : 'left';
    const isSticky = position === 'sticky';
    // Master spreader
    spreader.style.position = position;
    if (isSticky) {
        spreader.style.top = `${stickyTop}px`;
        spreader.style.bottom = '';
        spreader.style[leftProp] = `${stickyLeft}px`;
        spreader.style[isRtl1 ? 'left' : 'right'] = '';
    } else {
        _class_private_method_get(this, _clearSpreaderInsetStyles, clearSpreaderInsetStyles).call(this, spreader);
    }
    // Overlay clone spreaders — only in element scroll mode.
    // In window scroll mode, clones use the overlay system's own
    // absolute/fixed positioning which conflicts with sticky.
    if (_class_private_method_get(this, _isWindowScroll, isWindowScroll).call(this)) {
        return;
    }
    _class_private_method_get(this, _applyCloneSpreaderStyles, applyCloneSpreaderStyles).call(this, overlays.inlineStartOverlay, position, isSticky, stickyTop, null, isRtl1);
    _class_private_method_get(this, _applyCloneSpreaderStyles, applyCloneSpreaderStyles).call(this, overlays.topOverlay, position, isSticky, null, stickyLeft, isRtl1);
    _class_private_method_get(this, _applyCloneSpreaderStyles, applyCloneSpreaderStyles).call(this, overlays.bottomOverlay, position, isSticky, null, stickyLeft, isRtl1);
}
function applyCloneSpreaderStyles(overlay, position, isSticky, stickyTop, stickyLeft, isRtl) {
    if (!overlay.needFullRender || !overlay.clone) {
        return;
    }
    const cloneSpreader = overlay.clone.wtTable.spreader;
    const leftProp = isRtl ? 'right' : 'left';
    cloneSpreader.style.position = position;
    if (isSticky) {
        if (stickyTop !== null) {
            cloneSpreader.style.top = `${stickyTop}px`;
        }
        if (stickyLeft !== null) {
            cloneSpreader.style[leftProp] = `${stickyLeft}px`;
            cloneSpreader.style[isRtl ? 'left' : 'right'] = '';
        }
    } else {
        _class_private_method_get(this, _clearSpreaderInsetStyles, clearSpreaderInsetStyles).call(this, cloneSpreader);
    }
}
function isScrollbarTarget(event) {
    if (_class_private_method_get(this, _isWindowScroll, isWindowScroll).call(this)) {
        return event.target === _class_private_field_get(this, _overlays).domBindings.rootDocument.documentElement;
    }
    return event.target === _class_private_field_get(this, _overlays).scrollableElement;
}
function isWindowScroll() {
    return _class_private_field_get(this, _overlays).scrollableElement === _class_private_field_get(this, _overlays).domBindings.rootWindow;
}
function isRtl() {
    return _class_private_field_get(this, _overlays).wtSettings.getSetting('rtlMode');
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$filter$2f$column$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/filter/column.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$filter$2f$row$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/filter/row.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$index$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/renderer/index.mjs [client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$column$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/column.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$row$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/row.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
;
;
;
;
;
;
;
/**
 * @todo These mixes are never added to the class Table, however their members are used here.
 * @todo Continue: Potentially it works only, because some of these mixes are added to every inherited class.
 * @todo Refactoring, move code from `if(this.isMaster)` into MasterTable, and others like that.
 * @mixes stickyColumnsStart
 * @mixes stickyRowsBottom
 * @mixes stickyRowsTop
 * @mixes calculatedRows
 * @mixes calculatedColumns
 * @abstract
 */ class Table {
    // Methods defined in subclass MasterTable but called from Table via `this.isMaster` guards.
    /**
   * Aligns overlays with the trimming container.
   *
   * @returns {void}
   */ alignOverlaysWithTrimmingContainer() {}
    /**
   * Marks oversized column headers.
   *
   * @returns {void}
   */ markOversizedColumnHeaders() {}
    /**
   * Returns a boolean that is true if this Table represents a specific overlay, identified by the overlay name.
   * For MasterTable, it returns false.
   *
   * @param {string} overlayTypeName The overlay type.
   * @returns {boolean}
   */ is(overlayTypeName) {
        return this.name === overlayTypeName;
    }
    /**
   *
   */ fixTableDomTree() {
        const rootDocument = this.domBindings.rootDocument;
        this.TBODY = this.TABLE.querySelector('tbody');
        if (!this.TBODY) {
            this.TBODY = rootDocument.createElement('tbody');
            this.TABLE.appendChild(this.TBODY);
        }
        this.THEAD = this.TABLE.querySelector('thead');
        if (!this.THEAD) {
            this.THEAD = rootDocument.createElement('thead');
            this.TABLE.insertBefore(this.THEAD, this.TBODY);
        }
        this.COLGROUP = this.TABLE.querySelector('colgroup');
        if (!this.COLGROUP) {
            this.COLGROUP = rootDocument.createElement('colgroup');
            this.TABLE.insertBefore(this.COLGROUP, this.THEAD);
        }
    }
    /**
   * @param {HTMLTableElement} table An element to process.
   * @returns {HTMLElement}
   */ createSpreader(table) {
        const parent = table.parentNode;
        let spreader;
        if (!parent || parent.nodeType !== Node.ELEMENT_NODE || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(parent) || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(parent, 'wtHolder')) {
            spreader = this.domBindings.rootDocument.createElement('div');
            spreader.className = 'wtSpreader';
            if (parent) {
                // if TABLE is detached (e.g. in Jasmine test), it has no parentNode so we cannot attach holder to it
                table.before(spreader);
            }
            spreader.appendChild(table);
        }
        if (spreader) {
            spreader.style.position = 'relative';
            if (this.wtSettings.getSetting('ariaTags')) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(spreader, [
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_PRESENTATION"])()
                ]);
            }
        }
        return spreader;
    }
    /**
   * @param {HTMLElement} spreader An element to the hider element is injected.
   * @returns {HTMLElement}
   */ createHider(spreader) {
        const parent = spreader.parentNode;
        let hider;
        if (!parent || parent.nodeType !== Node.ELEMENT_NODE || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(parent) || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(parent, 'wtHolder')) {
            hider = this.domBindings.rootDocument.createElement('div');
            hider.className = 'wtHider';
            if (parent) {
                // if TABLE is detached (e.g. in Jasmine test), it has no parentNode so we cannot attach holder to it
                spreader.before(hider);
            }
            hider.appendChild(spreader);
        }
        if (hider && this.wtSettings.getSetting('ariaTags')) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(hider, [
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_PRESENTATION"])()
            ]);
        }
        return hider;
    }
    /**
   *
   * @param {HTMLElement} hider An element to the holder element is injected.
   * @returns {HTMLElement}
   */ createHolder(hider) {
        const parent = hider.parentNode;
        let holder;
        if (!parent || parent.nodeType !== Node.ELEMENT_NODE || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(parent) || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["hasClass"])(parent, 'wtHolder')) {
            holder = this.domBindings.rootDocument.createElement('div');
            holder.style.position = 'relative';
            holder.className = 'wtHolder';
            if (this.isMaster) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(holder, [
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_TABINDEX"])(-1)
                ]);
            }
            if (parent) {
                // if TABLE is detached (e.g. in Jasmine test), it has no parentNode so we cannot attach holder to it
                hider.before(holder);
            }
            if (this.isMaster) {
                const holderParent = holder.parentNode;
                // holderParent is null when TABLE is detached (e.g. in Jasmine tests); skip class assignment in that case.
                // isHTMLElement() is used instead of `instanceof HTMLElement` because the latter fails in
                // cross-frame contexts (e.g. when HoT is mounted inside an <iframe> via React portals):
                // the iframe's HTMLElement constructor !== the parent frame's HTMLElement.
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(holderParent)) {
                    holderParent.className += 'ht_master handsontable';
                    holderParent.setAttribute('dir', this.wtSettings.getSettingPure('rtlMode') ? 'rtl' : 'ltr');
                    if (this.wtSettings.getSetting('ariaTags')) {
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(holderParent, [
                            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_PRESENTATION"])()
                        ]);
                    }
                }
            }
            holder.appendChild(hider);
        }
        if (holder && this.wtSettings.getSetting('ariaTags')) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["setAttribute"])(holder, [
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_PRESENTATION"])()
            ]);
        }
        return holder;
    }
    /**
   * Redraws the table.
   *
   * @param {boolean} [fastDraw=false] If TRUE, will try to avoid full redraw and only update the border positions.
   *                                   If FALSE or UNDEFINED, will perform a full redraw.
   * @returns {Table}
   */ draw(fastDraw = false) {
        const { wtSettings } = this;
        const { wtOverlays, wtViewport } = this.dataAccessObject;
        const totalRows = wtSettings.getSetting('totalRows');
        const totalColumns = wtSettings.getSetting('totalColumns');
        const rowHeaders = wtSettings.getSetting('rowHeaders');
        const rowHeadersCount = rowHeaders.length;
        const columnHeaders = wtSettings.getSetting('columnHeaders');
        const columnHeadersCount = columnHeaders.length;
        let runFastDraw = fastDraw;
        if (this.isMaster) {
            wtOverlays.beforeDraw();
            this.holderOffset = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(this.holder);
            wtViewport.rowHeightCache.ensureBuilt();
            wtViewport.columnWidthCache.ensureBuilt();
            runFastDraw = wtViewport.createCalculators(runFastDraw);
            if (rowHeadersCount && !wtSettings.getSetting('fixedColumnsStart')) {
                const leftScrollPos = wtOverlays.inlineStartOverlay.getScrollPosition();
                const previousState = this.correctHeaderWidth;
                this.correctHeaderWidth = leftScrollPos !== 0;
                if (previousState !== this.correctHeaderWidth) {
                    runFastDraw = false;
                }
            }
        }
        if (runFastDraw) {
            if (this.isMaster) {
                wtOverlays.refresh(true);
                // Fast (scroll) draws skip the full-render header-height pass below, so the master/top
                // header heights can drift against the frozen overlays during scrolling (a tall wrapped
                // frozen header that the master never renders). Re-sync here. The method is a cheap no-op
                // unless the grid has frozen columns with column headers, so non-frozen grids are unaffected.
                this.syncOversizedColumnHeadersWithFrozenOverlays();
            }
        } else {
            if (this.isMaster) {
                this.tableOffset = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(this.TABLE);
            } else {
                this.tableOffset = this.dataAccessObject.parentTableOffset;
            }
            const startRow = Math.max(this.getFirstRenderedRow(), 0);
            const startColumn = Math.max(this.getFirstRenderedColumn(), 0);
            this.rowFilter = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$filter$2f$row$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](startRow, wtSettings.getSetting('totalRows'), columnHeadersCount);
            this.columnFilter = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$filter$2f$column$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](startColumn, wtSettings.getSetting('totalColumns'), rowHeadersCount);
            let performRedraw = true;
            // Only master table rendering can be skipped
            if (this.isMaster) {
                this.alignOverlaysWithTrimmingContainer(); // todo It calls method from child class (MasterTable).
                const skipRender = {};
                this.wtSettings.getSetting('beforeDraw', true, skipRender);
                performRedraw = skipRender.skipRender !== true;
            }
            if (performRedraw) {
                this.tableRenderer.setHeaderContentRenderers(rowHeaders, columnHeaders);
                if (this.is(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM"]) || this.is(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM_INLINE_START_CORNER"])) {
                    // do NOT render headers on the bottom or bottom-left corner overlay
                    this.tableRenderer.setHeaderContentRenderers(rowHeaders, []);
                }
                this.resetOversizedRows();
                this.tableRenderer.setActiveOverlayName(this.name).setViewportSize(this.getRenderedRowsCount(), this.getRenderedColumnsCount()).setFilters(this.rowFilter, this.columnFilter).render();
                if (this.isMaster) {
                    this.markOversizedColumnHeaders();
                }
                this.adjustColumnHeaderHeights();
                if (this.isMaster) {
                    this.syncOversizedColumnHeadersWithDOM();
                }
                if (this.isMaster || this.is(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM"])) {
                    this.markOversizedRows();
                }
                if (this.isMaster) {
                    if (!this.wtSettings.getSetting('externalRowCalculator')) {
                        wtViewport.rowHeightCache.ensureBuilt();
                        wtViewport.columnWidthCache.ensureBuilt();
                        wtViewport.createVisibleCalculators();
                    }
                    wtOverlays.refresh(false);
                    this.syncOversizedColumnHeadersWithFrozenOverlays();
                    wtOverlays.applyToDOM();
                    this.wtSettings.getSetting('onDraw', true);
                } else if (this.is(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM"])) {
                    this.dataAccessObject.cloneSource.wtOverlays.adjustElementsSize();
                }
            }
        }
        let positionChanged = false;
        if (this.isMaster) {
            positionChanged = wtOverlays.topOverlay.resetFixedPosition();
            if (wtOverlays.bottomOverlay.clone) {
                positionChanged = wtOverlays.bottomOverlay.resetFixedPosition() || positionChanged;
            }
            positionChanged = wtOverlays.inlineStartOverlay.resetFixedPosition() || positionChanged;
            if (wtOverlays.topInlineStartCornerOverlay) {
                wtOverlays.topInlineStartCornerOverlay.resetFixedPosition();
            }
            if (wtOverlays.bottomInlineStartCornerOverlay && wtOverlays.bottomInlineStartCornerOverlay.clone) {
                wtOverlays.bottomInlineStartCornerOverlay.resetFixedPosition();
            }
        }
        if (positionChanged) {
            // It refreshes the cells borders caused by a 1px shift (introduced by overlays which add or
            // remove `innerBorderTop` and `innerBorderInlineStart` CSS classes to the DOM element. This happens
            // when there is a switch between rendering from 0 to N rows/columns and vice versa).
            wtOverlays.refreshAll(); // `refreshAll()` internally already calls `refreshSelections()` method
            wtOverlays.adjustElementsSize();
        } else {
            this.dataAccessObject.selectionManager.setActiveOverlay(this.facadeGetter()).render(runFastDraw);
        }
        if (this.isMaster) {
            wtOverlays.afterDraw();
        }
        this.dataAccessObject.drawn = true;
        return this;
    }
    /**
   * @param {number} col The visual column index.
   */ markIfOversizedColumnHeader(col) {
        const sourceColIndex = this.columnFilter.renderedToSource(col);
        let level = this.wtSettings.getSetting('columnHeaders').length;
        const defaultRowHeight = this.wtSettings.getSetting('stylesHandler').getDefaultRowHeight();
        let previousColHeaderHeight;
        let currentHeader;
        let currentHeaderHeight;
        const columnHeaderHeightSetting = this.wtSettings.getSetting('columnHeaderHeight') || [];
        while(level){
            level -= 1;
            previousColHeaderHeight = this.getColumnHeaderHeight(level);
            currentHeader = this.getColumnHeader(sourceColIndex, level);
            if (!currentHeader) {
                continue;
            }
            currentHeaderHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["innerHeight"])(currentHeader);
            if (!previousColHeaderHeight && defaultRowHeight < currentHeaderHeight || previousColHeaderHeight < currentHeaderHeight) {
                this.dataAccessObject.wtViewport.oversizedColumnHeaders[level] = currentHeaderHeight;
            }
            if (Array.isArray(columnHeaderHeightSetting)) {
                if (columnHeaderHeightSetting[level] !== null && columnHeaderHeightSetting[level] !== undefined) {
                    this.dataAccessObject.wtViewport.oversizedColumnHeaders[level] = columnHeaderHeightSetting[level];
                }
            } else if (!isNaN(columnHeaderHeightSetting)) {
                this.dataAccessObject.wtViewport.oversizedColumnHeaders[level] = columnHeaderHeightSetting;
            }
            const headerHeightAtLevel = Array.isArray(columnHeaderHeightSetting) // eslint-disable-line max-len
             ? columnHeaderHeightSetting[level] ?? 0 : columnHeaderHeightSetting;
            if ((this.dataAccessObject.wtViewport.oversizedColumnHeaders[level] ?? 0) < headerHeightAtLevel) {
                this.dataAccessObject.wtViewport.oversizedColumnHeaders[level] = headerHeightAtLevel;
            }
        }
    }
    /**
   *
   */ adjustColumnHeaderHeights() {
        const { wtSettings } = this;
        const children = this.THEAD.childNodes;
        const oversizedColumnHeaders = this.dataAccessObject.wtViewport.oversizedColumnHeaders;
        const columnHeaders = wtSettings.getSetting('columnHeaders');
        for(let i = 0, len = columnHeaders.length; i < len; i++){
            if (oversizedColumnHeaders[i]) {
                if (!children[i] || children[i].childNodes.length === 0) {
                    return;
                }
                const firstChild = children[i].childNodes[0];
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(firstChild)) {
                    firstChild.style.height = `${oversizedColumnHeaders[i]}px`;
                }
            }
        }
    }
    /**
   * After the master table applies `oversizedColumnHeaders` via `adjustColumnHeaderHeights`,
   * the actual THEAD row heights may exceed the stored values when header content (e.g.,
   * wrapping text) pushes cells taller than the configured `columnHeaderHeight`. This method
   * re-reads the rendered THEAD row heights and updates `oversizedColumnHeaders` so that
   * overlay tables receive the correct values during their own `adjustColumnHeaderHeights`.
   */ syncOversizedColumnHeadersWithDOM() {
        const { wtSettings } = this;
        const children = this.THEAD.childNodes;
        const oversizedColumnHeaders = this.dataAccessObject.wtViewport.oversizedColumnHeaders;
        const columnHeaders = wtSettings.getSetting('columnHeaders');
        const borderCompensation = 1;
        for(let i = 0, len = columnHeaders.length; i < len; i++){
            if (!children[i] || !oversizedColumnHeaders[i]) {
                continue;
            }
            const child = children[i];
            const actualRowHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(child) ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["innerHeight"])(child) : 0;
            if (actualRowHeight > (oversizedColumnHeaders[i] ?? 0) + borderCompensation) {
                oversizedColumnHeaders[i] = actualRowHeight;
            }
        }
    }
    /**
   * Frozen column headers (e.g., with `white-space: normal`) are rendered only in the frozen
   * overlays, never in the master table's THEAD. When such a header is taller than the headers
   * of the scrollable columns, the master and top overlay THEADs render shorter than the corner
   * and inline-start overlays, so the frozen overlay body rows sit shifted against the master.
   * The gap can be sub-pixel: under browser zoom the frozen header content is a fraction of a
   * pixel taller than the applied height, and that fraction accumulates into a visible 1px shift.
   *
   * This method runs after the frozen overlays have rendered. It reads the corner overlay's
   * content-driven THEAD row heights with sub-pixel precision and forces the matching master and
   * top overlay header cells to the same height so every overlay THEAD ends up the same height
   * and the body rows stay pixel-aligned.
   *
   * It deliberately does NOT write to `wtViewport.oversizedColumnHeaders`. That cache is applied
   * back to the corner overlay on the next render; growing it here would inflate the corner cell,
   * which would then be re-measured taller, ratcheting the height up every render. Reading the
   * corner's natural (content-driven) height and only adjusting the master/top side keeps the
   * synchronization stable and lets the header shrink again when the content allows.
   */ syncOversizedColumnHeadersWithFrozenOverlays() {
        const { wtOverlays } = this.dataAccessObject;
        // Cheapest possible bail-out first: with no frozen columns the corner overlay is not cloned,
        // so the overwhelmingly common (non-frozen) grids pay only a couple of property reads per draw.
        const cornerClone = wtOverlays.topInlineStartCornerOverlay?.clone;
        if (!cornerClone?.wtTable?.THEAD) {
            return;
        }
        const columnHeaders = this.wtSettings.getSetting('columnHeaders');
        if (!columnHeaders.length) {
            return;
        }
        const cornerChildren = cornerClone.wtTable.THEAD.childNodes;
        const topClone = wtOverlays.topOverlay?.clone;
        const targetTheads = [
            this.THEAD,
            topClone?.wtTable?.THEAD
        ];
        // Sub-pixel tolerance to avoid rewriting heights on floating-point jitter while still
        // catching the fractional gaps (e.g. ~0.33px at 75% zoom) that read as a 1px shift.
        const epsilon = 0.1;
        for(let i = 0, len = columnHeaders.length; i < len; i++){
            const cornerChild = cornerChildren[i];
            if (!(cornerChild instanceof HTMLElement)) {
                continue;
            }
            const cornerRowHeight = cornerChild.getBoundingClientRect().height;
            targetTheads.forEach((thead)=>{
                const targetRow = thead?.childNodes[i];
                if (!(targetRow instanceof HTMLElement) || targetRow.childNodes.length === 0) {
                    return;
                }
                const firstChild = targetRow.childNodes[0];
                if (!(firstChild instanceof HTMLElement)) {
                    return;
                }
                if (Math.abs(targetRow.getBoundingClientRect().height - cornerRowHeight) > epsilon) {
                    firstChild.style.height = `${cornerRowHeight}px`;
                }
            });
        }
    }
    /**
   * Resets cache of row heights. The cache should be cached for each render cycle in a case
   * when new cell values have content which increases/decreases cell height.
   */ resetOversizedRows() {
        const { wtSettings } = this;
        const { wtViewport } = this.dataAccessObject;
        if (!this.isMaster && !this.is(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM"])) {
            return;
        }
        if (!wtSettings.getSetting('externalRowCalculator')) {
            const rowsToRender = this.getRenderedRowsCount();
            // Reset the oversized row cache for rendered rows
            for(let visibleRowIndex = 0; visibleRowIndex < rowsToRender; visibleRowIndex++){
                const sourceRow = this.rowFilter.renderedToSource(visibleRowIndex);
                if (wtViewport.oversizedRows && wtViewport.oversizedRows[sourceRow]) {
                    wtViewport.oversizedRows[sourceRow] = undefined;
                }
            }
        }
    }
    /**
   * Get cell element at coords.
   * Negative coords.row or coords.col are used to retrieve header cells. If there are multiple header levels, the
   * negative value corresponds to the distance from the working area. For example, when there are 3 levels of column
   * headers, coords.col=-1 corresponds to the most inner header element, while coords.col=-3 corresponds to the
   * outmost header element.
   *
   * In case an element for the coords is not rendered, the method returns an error code.
   * To produce the error code, the input parameters are validated in the order in which they
   * are given. Thus, if both the row and the column coords are out of the rendered bounds,
   * the method returns the error code for the row.
   *
   * @param {CellCoords} coords The cell coordinates.
   * @returns {HTMLElement|number} HTMLElement on success or Number one of the exit codes on error:
   *  -1 row before viewport
   *  -2 row after viewport
   *  -3 column before viewport
   *  -4 column after viewport.
   */ getCell(coords) {
        if (coords.row === null || coords.col === null) {
            return -5;
        }
        let row = coords.row;
        let column = coords.col;
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const hookResult = this.wtSettings.getSetting('onModifyGetCellCoords', row, column, !this.isMaster, 'render');
        if (hookResult && Array.isArray(hookResult)) {
            // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
            [row, column] = hookResult;
        }
        if (this.isRowBeforeRenderedRows(row)) {
            // row before rendered rows
            return -1;
        } else if (this.isRowAfterRenderedRows(row)) {
            // row after rendered rows
            return -2;
        } else if (this.isColumnBeforeRenderedColumns(column)) {
            // column before rendered columns
            return -3;
        } else if (this.isColumnAfterRenderedColumns(column)) {
            // column after rendered columns
            return -4;
        }
        const TR = this.getRow(row);
        if (!TR && row >= 0) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('TR was expected to be rendered but is not');
        }
        const trElement = TR !== false ? TR : null;
        const TD = trElement?.childNodes[this.columnFilter.sourceColumnToVisibleRowHeadedColumn(column)];
        if (!TD && column >= 0) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('TD or TH was expected to be rendered but is not');
        }
        // TD is a TD/TH HTMLElement guaranteed by DOM structure. TypeScript cannot narrow ChildNode
        // to HTMLElement without instanceof, but adding a throw here would change the existing contract
        // for negative column (header) lookups where TD may be undefined.
        return TD;
    }
    /**
   * Get the DOM element of the row with the provided index.
   *
   * @param {number} rowIndex Row index.
   * @returns {HTMLTableRowElement|boolean} Return the row's DOM element or `false` if the row with the provided
   * index doesn't exist.
   */ getRow(rowIndex) {
        let renderedRowIndex = null;
        let parentElement = null;
        if (rowIndex < 0) {
            renderedRowIndex = this.rowFilter?.sourceRowToVisibleColHeadedRow(rowIndex);
            parentElement = this.THEAD;
        } else {
            renderedRowIndex = this.rowFilter?.sourceToRendered(rowIndex);
            parentElement = this.TBODY;
        }
        if (renderedRowIndex !== undefined && renderedRowIndex !== null && parentElement !== null) {
            if (parentElement.childNodes.length < renderedRowIndex + 1) {
                return false;
            } else {
                return parentElement.childNodes[renderedRowIndex];
            }
        }
        return false;
    }
    /**
   * GetColumnHeader.
   *
   * @param {number} col Column index.
   * @param {number} [level=0] Header level (0 = most distant to the table).
   * @returns {object} HTMLElement on success or undefined on error.
   */ getColumnHeader(col, level = 0) {
        const TR = this.THEAD.childNodes[level];
        const TH = TR?.childNodes[this.columnFilter.sourceColumnToVisibleRowHeadedColumn(col)];
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(TH) ? TH : undefined;
    }
    /**
   * Gets all columns headers (TH elements) from the table.
   *
   * @param {number} column A source column index.
   * @returns {HTMLTableCellElement[]}
   */ getColumnHeaders(column) {
        const THs = [];
        const visibleColumn = this.columnFilter.sourceColumnToVisibleRowHeadedColumn(column);
        this.THEAD.childNodes.forEach((TR)=>{
            const TH = TR.childNodes[visibleColumn];
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLTableCellElement"])(TH)) {
                THs.push(TH);
            }
        });
        return THs;
    }
    /**
   * GetRowHeader.
   *
   * @param {number} row Row index.
   * @param {number} [level=0] Header level (0 = most distant to the table).
   * @returns {HTMLElement} HTMLElement on success or Number one of the exit codes on error: `null table doesn't have
   *   row headers`.
   */ getRowHeader(row, level = 0) {
        const rowHeadersCount = this.wtSettings.getSetting('rowHeaders').length;
        if (level >= rowHeadersCount) {
            return undefined;
        }
        const renderedRow = this.rowFilter.sourceToRendered(row);
        const visibleRow = renderedRow < 0 ? this.rowFilter.sourceRowToVisibleColHeadedRow(row) : renderedRow;
        const parentElement = renderedRow < 0 ? this.THEAD : this.TBODY;
        const TR = parentElement?.childNodes[visibleRow];
        const TH = TR?.childNodes[level];
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(TH) ? TH : undefined;
    }
    /**
   * Gets all rows headers (TH elements) from the table.
   *
   * @param {number} row A source row index.
   * @returns {HTMLTableCellElement[]}
   */ getRowHeaders(row) {
        const THs = [];
        const rowHeadersCount = this.wtSettings.getSetting('rowHeaders').length;
        for(let renderedRowIndex = 0; renderedRowIndex < rowHeadersCount; renderedRowIndex++){
            const TR = this.TBODY.childNodes[this.rowFilter.sourceToRendered(row)];
            const TH = TR?.childNodes[renderedRowIndex];
            if (TH) {
                THs.push(TH);
            }
        }
        return THs;
    }
    /**
   * Returns cell coords object for a given TD (or a child element of a TD element).
   *
   * @param {HTMLTableCellElement} TD A cell DOM element (or a child of one).
   * @returns {CellCoords|null} The coordinates of the provided TD element (or the closest TD element) or null, if the
   *   provided element is not applicable.
   */ getCoords(TD) {
        let cellElement = TD;
        if (cellElement.nodeName !== 'TD' && cellElement.nodeName !== 'TH') {
            cellElement = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["closest"])(cellElement, [
                'TD',
                'TH'
            ]);
        }
        if (cellElement === null) {
            return null;
        }
        const TR = cellElement.parentNode;
        if (!TR) {
            return null;
        }
        const CONTAINER = TR.parentNode;
        if (!CONTAINER) {
            return null;
        }
        let row = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLElement"])(TR) ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["index"])(TR) : 0;
        let col = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isHTMLTableCellElement"])(cellElement) ? cellElement.cellIndex : 0;
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["overlayContainsElement"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TOP_INLINE_START_CORNER"], cellElement, this.wtRootElement) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["overlayContainsElement"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TOP"], cellElement, this.wtRootElement)) {
            if (CONTAINER.nodeName === 'THEAD') {
                row -= CONTAINER.childNodes.length;
            }
        } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["overlayContainsElement"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM_INLINE_START_CORNER"], cellElement, this.wtRootElement) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["overlayContainsElement"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM"], cellElement, this.wtRootElement)) {
            const totalRows = this.wtSettings.getSetting('totalRows');
            row = totalRows - CONTAINER.childNodes.length + row;
        } else if (CONTAINER === this.THEAD) {
            row = this.rowFilter.visibleColHeadedRowToSourceRow(row);
        } else if (this.rowFilter) {
            row = this.rowFilter.renderedToSource(row);
        }
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["overlayContainsElement"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TOP_INLINE_START_CORNER"], cellElement, this.wtRootElement) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["overlayContainsElement"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_INLINE_START"], cellElement, this.wtRootElement) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["overlayContainsElement"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM_INLINE_START_CORNER"], cellElement, this.wtRootElement)) {
            col = this.columnFilter.offsettedTH(col);
        } else if (this.columnFilter) {
            col = this.columnFilter.visibleRowHeadedColumnToSourceColumn(col);
        }
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const hookResult = this.wtSettings.getSetting('onModifyGetCoordsElement', row, col);
        if (hookResult && Array.isArray(hookResult)) {
            // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
            [row, col] = hookResult;
        }
        return this.wot.createCellCoords(row, col);
    }
    /**
   * Check if any of the rendered rows is higher than expected, and if so, cache them.
   */ markOversizedRows() {
        if (this.wtSettings.getSetting('externalRowCalculator')) {
            return;
        }
        let rowCount = this.TBODY.childNodes.length;
        const stylesHandler = this.wtSettings.getSetting('stylesHandler');
        const expectedTableHeight = rowCount * stylesHandler.getDefaultRowHeight();
        const actualTableHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["innerHeight"])(this.TBODY) - 1;
        const borderBoxSizing = stylesHandler.areCellsBorderBox();
        const rowHeightFn = borderBoxSizing ? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"] : __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["innerHeight"];
        const borderCompensation = borderBoxSizing ? 0 : 1;
        const firstRowBorderCompensation = borderBoxSizing ? 1 : 0;
        let previousRowHeight;
        let rowCurrentHeight;
        let sourceRowIndex;
        let currentTr;
        let rowHeader;
        if (expectedTableHeight === actualTableHeight && !this.wtSettings.getSetting('fixedRowsBottom')) {
            // If the actual table height equals rowCount * default single row height, no row is oversized -> no need to iterate over them
            return;
        }
        const { wtViewport } = this.dataAccessObject;
        let hasChanges = false;
        while(rowCount){
            rowCount -= 1;
            sourceRowIndex = this.rowFilter.renderedToSource(rowCount);
            previousRowHeight = this.getRowHeight(sourceRowIndex);
            currentTr = this.getTrForRow(sourceRowIndex);
            rowHeader = currentTr.querySelector('th');
            // Use the rendered row index (rowCount === 0 is always the first <tr> in this tbody),
            // not the source row index (which would be wrong for clones whose first rendered row
            // has a different source index). Any tbody's first <tr> gets border-top: 1px from the
            // tr:first-child CSS rule, so the compensation applies regardless of source identity.
            const topBorderCompensation = rowCount === 0 ? firstRowBorderCompensation : 0;
            if (rowHeader) {
                rowCurrentHeight = rowHeightFn(rowHeader);
            } else {
                rowCurrentHeight = rowHeightFn(currentTr) - borderCompensation;
            }
            if (!previousRowHeight && stylesHandler.getDefaultRowHeight() < rowCurrentHeight - topBorderCompensation || previousRowHeight !== undefined && previousRowHeight < rowCurrentHeight) {
                if (!borderBoxSizing) {
                    rowCurrentHeight += 1;
                }
                wtViewport.oversizedRows[sourceRowIndex] = rowCurrentHeight;
                hasChanges = true;
            }
        }
        if (hasChanges) {
            wtViewport.rowHeightCache.invalidate();
        }
    }
    /**
   * @param {number} row The visual row index.
   * @returns {HTMLTableRowElement}
   */ getTrForRow(row) {
        return this.TBODY.childNodes[this.rowFilter.sourceToRendered(row)];
    }
    /**
   * Checks if the column index (negative value from -1 to N) is rendered.
   *
   * @param {number} column The column index (negative value from -1 to N).
   * @returns {boolean}
   */ isColumnHeaderRendered(column) {
        if (column >= 0) {
            return false;
        }
        const rowHeaders = this.wtSettings.getSetting('rowHeaders');
        const rowHeadersCount = rowHeaders.length;
        return Math.abs(column) <= rowHeadersCount;
    }
    /**
   * Checks if the row index (negative value from -1 to N) is rendered.
   *
   * @param {number} row The row index (negative value from -1 to N).
   * @returns {boolean}
   */ isRowHeaderRendered(row) {
        if (row >= 0) {
            return false;
        }
        const columnHeaders = this.wtSettings.getSetting('columnHeaders');
        const columnHeadersCount = columnHeaders.length;
        return Math.abs(row) <= columnHeadersCount;
    }
    /**
   * Check if the given row index is lower than the index of the first row that
   * is currently rendered and return TRUE in that case, or FALSE otherwise.
   *
   * Negative row index is used to check the columns' headers.
   *
   *  Headers
   *           +--------------+                                     │
   *       -3  │    │    │    │                                     │
   *           +--------------+                                     │
   *       -2  │    │    │    │                                     │ TRUE
   *           +--------------+                                     │
   *       -1  │    │    │    │                                     │
   *  Cells  +==================+                                   │
   *        0  ┇    ┇    ┇    ┇ <--- For fixedRowsTop: 1            │
   *           +--------------+      the master overlay do       ---+ first rendered row (index 1)
   *        1  │ A2 │ B2 │ C2 │      not render the first row.      │
   *           +--------------+                                     │ FALSE
   *        2  │ A3 │ B3 │ C3 │                                     │
   *           +--------------+                                  ---+ last rendered row
   *                                                                │
   *                                                                │ FALSE
   *
   * @param {number} row The visual row index.
   * @memberof Table#
   * @function isRowBeforeRenderedRows
   * @returns {boolean}
   */ isRowBeforeRenderedRows(row) {
        const first = this.getFirstRenderedRow();
        // Check the headers only in case when the first rendered row is -1 or 0.
        // This is an indication that the overlay is placed on the most top position.
        if (row < 0 && first <= 0) {
            return !this.isRowHeaderRendered(row);
        }
        return row < first;
    }
    /**
   * Check if the given column index is greater than the index of the last column that
   * is currently rendered and return TRUE in that case, or FALSE otherwise.
   *
   * The negative row index is used to check the columns' headers. However,
   * keep in mind that for negative indexes, the method always returns FALSE as
   * it is not possible to render headers partially. The "after" index can not be
   * lower than -1.
   *
   *  Headers
   *           +--------------+                                     │
   *       -3  │    │    │    │                                     │
   *           +--------------+                                     │
   *       -2  │    │    │    │                                     │ FALSE
   *           +--------------+                                     │
   *       -1  │    │    │    │                                     │
   *  Cells  +==================+                                   │
   *        0  ┇    ┇    ┇    ┇ <--- For fixedRowsTop: 1            │
   *           +--------------+      the master overlay do       ---+ first rendered row (index 1)
   *        1  │ A2 │ B2 │ C2 │      not render the first rows      │
   *           +--------------+                                     │ FALSE
   *        2  │ A3 │ B3 │ C3 │                                     │
   *           +--------------+                                  ---+ last rendered row
   *                                                                │
   *                                                                │ TRUE
   *
   * @param {number} row The visual row index.
   * @memberof Table#
   * @function isRowAfterRenderedRows
   * @returns {boolean}
   */ isRowAfterRenderedRows(row) {
        return row > this.getLastRenderedRow();
    }
    /**
   * Check if the given column index is lower than the index of the first column that
   * is currently rendered and return TRUE in that case, or FALSE otherwise.
   *
   * Negative column index is used to check the rows' headers.
   *
   *                            For fixedColumnsStart: 1 the master overlay
   *                            do not render this first columns.
   *  Headers    -3   -2   -1    |
   *           +----+----+----║┄ ┄ +------+------+
   *           │    │    │    ║    │  B1  │  C1  │
   *           +--------------║┄ ┄ --------------│
   *           │    │    │    ║    │  B2  │  C2  │
   *           +--------------║┄ ┄ --------------│
   *           │    │    │    ║    │  B3  │  C3  │
   *           +----+----+----║┄ ┄ +------+------+
   *                               ╷             ╷
   *      -------------------------+-------------+---------------->
   *          TRUE             first    FALSE   last         FALSE
   *                           rendered         rendered
   *                           column           column
   *
   * @param {number} column The visual column index.
   * @memberof Table#
   * @function isColumnBeforeRenderedColumns
   * @returns {boolean}
   */ isColumnBeforeRenderedColumns(column) {
        const first = this.getFirstRenderedColumn();
        // Check the headers only in case when the first rendered column is -1 or 0.
        // This is an indication that the overlay is placed on the most left position.
        if (column < 0 && first <= 0) {
            return !this.isColumnHeaderRendered(column);
        }
        return column < first;
    }
    /**
   * Check if the given column index is greater than the index of the last column that
   * is currently rendered and return TRUE in that case, or FALSE otherwise.
   *
   * The negative column index is used to check the rows' headers. However,
   * keep in mind that for negative indexes, the method always returns FALSE as
   * it is not possible to render headers partially. The "after" index can not be
   * lower than -1.
   *
   *                            For fixedColumnsStart: 1 the master overlay
   *                            do not render this first columns.
   *  Headers    -3   -2   -1    |
   *           +----+----+----║┄ ┄ +------+------+
   *           │    │    │    ║    │  B1  │  C1  │
   *           +--------------║┄ ┄ --------------│
   *           │    │    │    ║    │  B2  │  C2  │
   *           +--------------║┄ ┄ --------------│
   *           │    │    │    ║    │  B3  │  C3  │
   *           +----+----+----║┄ ┄ +------+------+
   *                               ╷             ╷
   *      -------------------------+-------------+---------------->
   *          FALSE             first    FALSE   last         TRUE
   *                           rendered         rendered
   *                           column           column
   *
   * @param {number} column The visual column index.
   * @memberof Table#
   * @function isColumnAfterRenderedColumns
   * @returns {boolean}
   */ isColumnAfterRenderedColumns(column) {
        return this.columnFilter && column > this.getLastRenderedColumn();
    }
    /**
   * Checks if the column is after the last visible column.
   *
   * @param {number} column The visual column index.
   * @returns {boolean}
   */ isColumnAfterViewport(column) {
        return this.columnFilter && column > this.getLastVisibleColumn();
    }
    /**
   * Checks if the row is after the last visible row.
   *
   * @param {number} row The visual row index.
   * @returns {boolean}
   */ isRowAfterViewport(row) {
        return this.rowFilter && row > this.getLastVisibleRow();
    }
    /**
   * Checks if the column is before the first visible column.
   *
   * @param {number} column The visual column index.
   * @returns {boolean}
   */ isColumnBeforeViewport(column) {
        return this.columnFilter && this.columnFilter.sourceToRendered(column) < 0 && column >= 0;
    }
    /**
   * Checks if the last row is fully visible.
   *
   * @returns {boolean}
   */ isLastRowFullyVisible() {
        return this.getLastVisibleRow() === this.getLastRenderedRow();
    }
    /**
   * Checks if the last column is fully visible.
   *
   * @returns {boolean}
   */ isLastColumnFullyVisible() {
        return this.getLastVisibleColumn() === this.getLastRenderedColumn();
    }
    /**
   * Checks if all rows fit in the viewport.
   *
   * @returns {boolean}
   */ allRowsInViewport() {
        return this.wtSettings.getSetting('totalRows') === this.getVisibleRowsCount();
    }
    /**
   * Checks if all columns fit in the viewport.
   *
   * @returns {boolean}
   */ allColumnsInViewport() {
        return this.wtSettings.getSetting('totalColumns') === this.getVisibleColumnsCount();
    }
    /**
   * Checks if any of the row's cells content exceeds its initial height, and if so, returns the oversized height.
   *
   * @param {number} sourceRow The physical row index.
   * @returns {number}
   */ getRowHeight(sourceRow) {
        return this.rowUtils.getHeight(sourceRow);
    }
    /**
   * @param {number} level The column level.
   * @returns {number}
   */ getColumnHeaderHeight(level) {
        return this.columnUtils.getHeaderHeight(level);
    }
    /**
   * @param {number} sourceColumn The physical column index.
   * @returns {number}
   */ getColumnWidth(sourceColumn) {
        return this.columnUtils.getWidth(sourceColumn);
    }
    /**
   * Checks if the table has defined size. It returns `true` when the table has width and height
   * set bigger than `0px`.
   *
   * @returns {boolean}
   */ hasDefinedSize() {
        return this.hasTableHeight && this.hasTableWidth;
    }
    /**
   * Gets table's width. The returned width is the width of the rendered cells that fit in the
   * current viewport. The value may change depends on the viewport position (scroll position).
   *
   * @returns {number}
   */ getWidth() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(this.TABLE);
    }
    /**
   * Gets table's height. The returned height is the height of the rendered cells that fit in the
   * current viewport. The value may change depends on the viewport position (scroll position).
   *
   * @returns {number}
   */ getHeight() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(this.TABLE);
    }
    /**
   * Gets table's total width. The returned width is the width of all rendered cells (including headers)
   * that can be displayed in the table.
   *
   * @returns {number}
   */ getTotalWidth() {
        const width = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(this.hider);
        // when the overlay's table does not have any cells the hider returns 0, get then width from the table element
        return width !== 0 ? width : this.getWidth();
    }
    /**
   * Gets table's total height. The returned height is the height of all rendered cells (including headers)
   * that can be displayed in the table.
   *
   * @returns {number}
   */ getTotalHeight() {
        const height = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(this.hider);
        // when the overlay's table does not have any cells the hider returns 0, get then height from the table element
        return height !== 0 ? height : this.getHeight();
    }
    /**
   * Checks if the table is visible. It returns `true` when the holder element (or its parents)
   * has CSS 'display' property different than 'none'.
   *
   * @returns {boolean}
   */ isVisible() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isVisible"])(this.TABLE);
    }
    /**
   * Modify row header widths provided by user in class contructor.
   *
   * @private
   * @param {Function | number | null} rowHeaderWidthFactory The function which can provide default width values for rows..
   * @returns {number}
   */ _modifyRowHeaderWidth(rowHeaderWidthFactory) {
        const rawWidths = typeof rowHeaderWidthFactory === 'function' ? rowHeaderWidthFactory() : rowHeaderWidthFactory;
        let widths = rawWidths;
        if (Array.isArray(widths)) {
            widths = [
                ...widths
            ];
            widths[widths.length - 1] = this._correctRowHeaderWidth(widths[widths.length - 1]);
        } else {
            widths = this._correctRowHeaderWidth(widths);
        }
        return widths;
    }
    /**
   * Correct row header width if necessary.
   *
   * @private
   * @param {number | null} width The width to process.
   * @returns {number}
   */ _correctRowHeaderWidth(width) {
        let rowHeaderWidth = typeof width === 'number' ? width : this.wtSettings.getSetting('defaultColumnWidth');
        if (this.correctHeaderWidth) {
            rowHeaderWidth += 1;
        }
        return rowHeaderWidth;
    }
    /**
   * Destroys the table instance. Overridden by MasterTable to release DOM resources.
   */ destroy() {
    // Intentionally empty
    }
    /**
   *
   * @abstract
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {'master'|CLONE_TYPES_ENUM} name Overlay name.
   */ constructor(dataAccessObject, facadeGetter, domBindings, wtSettings, name){
        /**
   * The table body element (TBODY).
   *
   * @type {HTMLTableSectionElement | null}
   */ this.TBODY = null;
        /**
   * The table head element (THEAD).
   *
   * @type {HTMLTableSectionElement | null}
   */ this.THEAD = null;
        /**
   * The column group element (COLGROUP).
   *
   * @type {HTMLTableColElement | null}
   */ this.COLGROUP = null;
        /**
   * Indicates if the table has height bigger than 0px.
   *
   * @type {boolean}
   */ this.hasTableHeight = true;
        /**
   * Indicates if the table has width bigger than 0px.
   *
   * @type {boolean}
   */ this.hasTableWidth = true;
        /**
   * Indicates if the table is visible. By visible, it means that the holder
   * element has CSS 'display' property different than 'none'.
   *
   * @type {boolean}
   */ this.isTableVisible = false;
        /**
   * The offset of the table element.
   *
   * @type {number | { top: number; left: number }}
   */ this.tableOffset = 0;
        /**
   * The offset of the holder element.
   *
   * @type {number | { top: number; left: number }}
   */ this.holderOffset = 0;
        this.domBindings = domBindings;
        /**
     * Indicates if this instance is of type `MasterTable` (i.e. It is NOT an overlay).
     *
     * @type {boolean}
     */ this.isMaster = name === 'master';
        this.name = name;
        this.dataAccessObject = dataAccessObject;
        this.facadeGetter = facadeGetter;
        this.wtSettings = wtSettings;
        // legacy support
        this.instance = this.dataAccessObject.wot; // TODO refactoring: it might be removed here, and provides legacy support through facade.
        this.wot = this.dataAccessObject.wot;
        this.TABLE = domBindings.rootTable;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeTextNodes"])(this.TABLE);
        // TODO refactoring, to recognize the legitimacy of moving them into domBidings
        this.spreader = this.createSpreader(this.TABLE);
        this.hider = this.createHider(this.spreader);
        this.holder = this.createHolder(this.hider);
        // parentNode is always an HTMLElement in production; null only when TABLE is detached (e.g. Jasmine tests).
        this.wtRootElement = this.holder.parentNode;
        if (this.isMaster) {
            this.alignOverlaysWithTrimmingContainer(); // todo wow, It calls method from child class (MasterTable).
        }
        this.fixTableDomTree();
        this.rowFilter = null; // TODO refactoring, eliminate all (re)creations of this object, then updates state when needed.
        this.columnFilter = null; // TODO refactoring, eliminate all (re)creations of this object, then updates state when needed.
        this.correctHeaderWidth = false;
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const origRowHeaderWidth = this.wtSettings.getSettingPure('rowHeaderWidth');
        // Fix for jumping row headers (https://github.com/handsontable/handsontable/issues/3850)
        this.wtSettings.update('rowHeaderWidth', ()=>this._modifyRowHeaderWidth(origRowHeaderWidth));
        this.rowUtils = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$row$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this.dataAccessObject, this.wtSettings); // TODO refactoring, It can be passed through IOC.
        this.columnUtils = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$column$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this.dataAccessObject, this.wtSettings); // TODO refactoring, It can be passed through IOC.
        this.tableRenderer = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$renderer$2f$index$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["Renderer"]({
            TABLE: this.TABLE,
            THEAD: this.THEAD ?? undefined,
            COLGROUP: this.COLGROUP ?? undefined,
            TBODY: this.TBODY ?? undefined,
            rowUtils: this.rowUtils,
            columnUtils: this.columnUtils,
            cellRenderer: this.wtSettings.getSettingPure('cellRenderer'),
            stylesHandler: this.wtSettings.getSetting('stylesHandler')
        });
    }
}
var __TURBOPACK__default__export__ = Table;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/bottom.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyRowsBottom$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyRowsBottom.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/calculatedColumns.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
;
;
;
;
;
/**
 * Subclass of `Table` that provides the helper methods relevant to BottomOverlay, implemented through mixins.
 *
 * @mixes stickyRowsBottom
 * @mixes calculatedColumns
 */ class BottomOverlayTable extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */ constructor(dataAccessObject, facadeGetter, domBindings, wtSettings){
        super(dataAccessObject, facadeGetter, domBindings, wtSettings, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM"]);
    }
}
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(BottomOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyRowsBottom$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(BottomOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
var __TURBOPACK__default__export__ = BottomOverlayTable;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/bottomInlineStartCorner.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyRowsBottom$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyRowsBottom.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyColumnsStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyColumnsStart.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
;
;
;
;
;
/**
 * Subclass of `Table` that provides the helper methods relevant to bottomInlineStartCornerOverlay
 * (in RTL mode the overlay sits on the right of the screen), implemented through mixins.
 *
 * @mixes stickyRowsBottom
 * @mixes stickyColumnsStart
 */ class BottomInlineStartCornerOverlayTable extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */ constructor(dataAccessObject, facadeGetter, domBindings, wtSettings){
        super(dataAccessObject, facadeGetter, domBindings, wtSettings, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_BOTTOM_INLINE_START_CORNER"]);
    }
}
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(BottomInlineStartCornerOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyRowsBottom$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(BottomInlineStartCornerOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyColumnsStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
var __TURBOPACK__default__export__ = BottomInlineStartCornerOverlayTable;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/inlineStart.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/calculatedRows.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyColumnsStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyColumnsStart.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
;
;
;
;
;
/**
 * Subclass of `Table` that provides the helper methods relevant to InlineStartOverlayTable, implemented through mixins.
 */ class InlineStartOverlayTable extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */ constructor(dataAccessObject, facadeGetter, domBindings, wtSettings){
        super(dataAccessObject, facadeGetter, domBindings, wtSettings, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_INLINE_START"]);
    }
}
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(InlineStartOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(InlineStartOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyColumnsStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
var __TURBOPACK__default__export__ = InlineStartOverlayTable;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/master.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/calculatedRows.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/calculatedColumns.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
;
;
;
;
;
var /**
   * Output and input fingerprint of the last slow-path
   * `alignOverlaysWithTrimmingContainer()` measurement. `null` when no valid
   * cache is available — either before the first measurement or after an
   * invalidation by a ResizeObserver callback. The fast path applies the
   * cached output only when the fingerprint still matches the current DOM
   * dimensions.
   */ _trimmingCache = /*#__PURE__*/ new WeakMap(), /**
   * ResizeObserver attached to the trimming container referenced by
   * `#observedTrimmingElement`. Fires asynchronously after the container's
   * size changes and nulls `#trimmingCache` so the next draw re-measures.
   * Acts as a defensive backstop — the synchronous fingerprint check at draw
   * time catches the same changes, but the observer invalidates the cache
   * eagerly, before the next draw is scheduled.
   */ _trimmingContainerObserver = /*#__PURE__*/ new WeakMap(), /**
   * ResizeObserver attached to `this.hider`. Fires asynchronously when
   * rendered content drives the hider to a new size and nulls
   * `#trimmingCache` so the next draw re-measures. Same backstop role as
   * `#trimmingContainerObserver`.
   */ _hiderObserver = /*#__PURE__*/ new WeakMap(), /**
   * The trimming container currently observed by `#trimmingContainerObserver`.
   * `alignOverlaysWithTrimmingContainer()` compares this reference against
   * `getTrimmingContainer(this.wtRootElement)` on every draw to detect when
   * the HOT instance has been reparented to a new scrollable ancestor, so the
   * observers can be rebound to the new container.
   */ _observedTrimmingElement = /*#__PURE__*/ new WeakMap();
/**
 * Subclass of `Table` that provides the helper methods relevant to the master table (not overlays), implemented through mixins.
 *
 * @mixes calculatedRows
 * @mixes calculatedColumns
 */ class MasterTable extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * Aligns overlays with the trimming container, applying cached measurements when possible
   * or performing a full measurement of the container. Uses ResizeObservers to detect
   * container and hider size changes, and validates the table visibility.
   */ alignOverlaysWithTrimmingContainer() {
        // The base-class constructor invokes this method before MasterTable's
        // field initializers have run. Accessing a private field in that window
        // throws; the brand check detects the pre-init call so the caching block
        // can be skipped. The non-caching branches (window-trimming overflow
        // reset and the trailing isVisible check) still run, matching the
        // pre-DEV-1777 behaviour and the side effects callers depend on.
        const fieldsInitialized = _trimmingCache.has(this);
        const trimmingElement = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getTrimmingContainer"])(this.wtRootElement);
        const { rootWindow } = this.domBindings;
        if (!(trimmingElement instanceof HTMLElement)) {
            const preventOverflow = this.wtSettings.getSetting('preventOverflow');
            if (!preventOverflow) {
                this.holder.style.overflow = 'visible';
                this.wtRootElement.style.overflow = 'visible';
            }
        } else if (fieldsInitialized) {
            // Bind ResizeObservers on the first call, and re-bind on the rare draw
            // where the trimming container has changed (HOT reparented in the DOM).
            // Each rebind also nulls the cache, since the previous measurement was
            // taken against a now-stale container reference.
            if (_class_private_field_get(this, _observedTrimmingElement) !== trimmingElement) {
                _class_private_field_get(this, _trimmingContainerObserver)?.disconnect();
                _class_private_field_get(this, _hiderObserver)?.disconnect();
                _class_private_field_set(this, _trimmingCache, null);
                _class_private_field_set(this, _observedTrimmingElement, trimmingElement);
                const invalidate = ()=>{
                    _class_private_field_set(this, _trimmingCache, null);
                };
                const tcObserver = new ResizeObserver(invalidate);
                tcObserver.observe(trimmingElement);
                _class_private_field_set(this, _trimmingContainerObserver, tcObserver);
                const hoObserver = new ResizeObserver(invalidate);
                hoObserver.observe(this.hider);
                _class_private_field_set(this, _hiderObserver, hoObserver);
            }
            // Fingerprint of the current trimming container and hider sizes. These
            // are cheap reads on a pure-scroll draw (no pending layout invalidation),
            // and they catch every synchronous DOM mutation that the async
            // ResizeObserver callbacks would miss between renders in the same tick —
            // for example `htEditor.loadData()` followed by an explicit
            // `alignOverlaysWithTrimmingContainer()` call in the autocomplete editor.
            const trimmingOffsetWidth = trimmingElement.offsetWidth;
            const trimmingOffsetHeight = trimmingElement.offsetHeight;
            const trimmingScrollWidth = trimmingElement.scrollWidth;
            const trimmingScrollHeight = trimmingElement.scrollHeight;
            const trimmingOverflow = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getStyle"])(trimmingElement, 'overflow', rootWindow) ?? '';
            const trimmingHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getStyle"])(trimmingElement, 'height', rootWindow) ?? '';
            const hiderOffsetHeight = this.hider.offsetHeight;
            const hiderOffsetWidth = this.hider.offsetWidth;
            const cache = _class_private_field_get(this, _trimmingCache);
            const cacheValid = cache !== null && cache.trimmingOffsetWidth === trimmingOffsetWidth && cache.trimmingOffsetHeight === trimmingOffsetHeight && cache.trimmingScrollWidth === trimmingScrollWidth && cache.trimmingScrollHeight === trimmingScrollHeight && cache.trimmingOverflow === trimmingOverflow && cache.trimmingHeight === trimmingHeight && cache.hiderOffsetHeight === hiderOffsetHeight && cache.hiderOffsetWidth === hiderOffsetWidth;
            if (cacheValid) {
                // Fast path: apply cached measurements without the expensive
                // cloneNode + getComputedStyle round-trip in the slow path. Taken on
                // every scroll draw where the container and hider dimensions have not
                // changed since the previous measurement.
                const holderStyle = this.holder.style;
                holderStyle.width = cache.holderWidth;
                holderStyle.height = cache.holderHeight;
                holderStyle.overflow = '';
                this.hasTableHeight = cache.hasTableHeight;
                this.hasTableWidth = cache.hasTableWidth;
            } else {
                // Slow path: full measurement of the trimming container. Runs on
                // the first draw, whenever the fingerprint no longer matches, and
                // whenever a ResizeObserver callback has nulled the cache.
                const trimmingElementParent = trimmingElement.parentElement;
                const holderStyle = this.holder.style;
                let width = trimmingOffsetWidth;
                let height = trimmingOffsetHeight;
                const overflowValues = [
                    'auto',
                    'hidden',
                    'scroll',
                    'clip'
                ];
                // Chrome resolves the overflow shorthand from individual properties natively
                // (e.g. overflowX='clip' → computed overflow='clip visible'). Only fall back to
                // reading overflowX/overflowY individually when the shorthand is absent — that
                // covers jsdom, where setting overflowX inline does not update the overflow shorthand.
                const hasScrollOverflow = trimmingOverflow ? trimmingOverflow.split(' ').some((v)=>overflowValues.includes(v)) : [
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getStyle"])(trimmingElement, 'overflowX', rootWindow),
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getStyle"])(trimmingElement, 'overflowY', rootWindow)
                ].some((v)=>v && overflowValues.includes(v));
                let useAutoHeight = trimmingHeight === 'auto';
                if (trimmingElementParent && hasScrollOverflow) {
                    const cloneNode = trimmingElement.cloneNode(false);
                    // Before calculating the height of the trimming element, set overflow: auto to hide scrollbars.
                    // An issue occurred on Firefox, where an empty element with overflow: scroll returns an element height higher than 0px
                    // despite an empty content within.
                    cloneNode.style.overflow = 'auto';
                    // Issue #9545 shows problem with calculating height for HOT on Firefox while placing instance in some
                    // flex containers and setting overflow for some `div` section.
                    cloneNode.style.position = 'absolute';
                    // Reset border and padding so border-box sizing does not contribute to the measured height.
                    // Without this, a container with a visible border (e.g. border: 1px solid) produces
                    // cloneHeight = 2 even though it has no intrinsic height, preventing the zero-height
                    // detection that guards against the feedback-loop bug. See issue #3119.
                    cloneNode.style.border = '0';
                    cloneNode.style.padding = '0';
                    if (trimmingElement.nextElementSibling) {
                        trimmingElementParent.insertBefore(cloneNode, trimmingElement.nextElementSibling);
                    } else {
                        trimmingElementParent.appendChild(cloneNode);
                    }
                    const cloneHeight = parseInt(rootWindow.getComputedStyle(cloneNode).height, 10);
                    trimmingElementParent.removeChild(cloneNode);
                    if (cloneHeight === 0) {
                        height = 0;
                        // The trimming container has no intrinsic height — its size is driven entirely by
                        // its content (HOT instances). When overflow-y is non-scrollable (hidden/clip),
                        // the container cannot be scrolled vertically, so its height grows with content.
                        // Setting the holder to a fixed pixel value in this case creates a feedback loop
                        // when multiple HOT instances share the same parent, expanding it to the browser's
                        // CSS height limit (~2^25 px). Using 'auto' lets the holder size to its content
                        // (the hider element) and breaks the loop. When overflow-y is scrollable (auto/
                        // scroll), the container IS a vertical scroll viewport and height=0 correctly
                        // signals that it has no defined size. See issue #3119.
                        const computedStyle = rootWindow.getComputedStyle(trimmingElement);
                        const overflowX = computedStyle.overflowX;
                        const overflowY = computedStyle.overflowY;
                        // Only switch to auto-height for horizontal-scroll containers where:
                        // - overflow-x is scrollable (auto/scroll) — the container is intentionally wider than its content
                        // - overflow-y is non-scrollable (hidden/clip) — the container height is content-driven
                        // This is the exact pattern that triggers the height feedback loop (issue #3119).
                        // All-axis scroll/auto containers (overflow: scroll/auto) keep height=0 to correctly
                        // signal no defined size. Similarly, overflow: hidden on both axes keeps height=0.
                        if ((overflowX === 'auto' || overflowX === 'scroll') && overflowY !== 'auto' && overflowY !== 'scroll') {
                            useAutoHeight = true;
                        }
                    }
                }
                height = Math.min(height, trimmingScrollHeight);
                width = Math.min(width, trimmingScrollWidth);
                const holderHeight = useAutoHeight ? 'auto' : `${height}px`;
                const holderWidth = `${width}px`;
                const hasTableHeight = holderHeight === 'auto' ? true : height > 0;
                const hasTableWidth = width > 0;
                holderStyle.height = holderHeight;
                holderStyle.width = holderWidth;
                holderStyle.overflow = '';
                this.hasTableHeight = hasTableHeight;
                this.hasTableWidth = hasTableWidth;
                // Skip caching transient zero-width states. When scrollWidth is 0
                // (e.g. the dropdown HOT before content loads), `width` collapses to
                // min(offsetWidth, 0) = 0. The fingerprint check would invalidate
                // such a cache on the next draw anyway, but suppressing the write
                // here avoids one wasted fast-path application before the next slow
                // measurement.
                if (width > 0) {
                    _class_private_field_set(this, _trimmingCache, {
                        trimmingOffsetWidth,
                        trimmingOffsetHeight,
                        trimmingScrollWidth,
                        trimmingScrollHeight,
                        trimmingOverflow,
                        trimmingHeight,
                        hiderOffsetHeight,
                        hiderOffsetWidth,
                        holderWidth,
                        holderHeight,
                        hasTableHeight,
                        hasTableWidth
                    });
                }
            }
        }
        this.isTableVisible = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isVisible"])(this.TABLE);
    }
    /**
   * Marks oversized column headers in the master overlay by iterating through all
   * rendered columns and marking those that exceed their container dimensions.
   */ markOversizedColumnHeaders() {
        const { wtSettings } = this;
        const { wtViewport } = this.dataAccessObject;
        const overlayName = 'master';
        const columnHeaders = wtSettings.getSetting('columnHeaders');
        const columnHeadersCount = columnHeaders.length;
        if (columnHeadersCount && !wtViewport.hasOversizedColumnHeadersMarked[overlayName]) {
            const rowHeaders = wtSettings.getSetting('rowHeaders');
            const rowHeaderCount = rowHeaders.length;
            const columnCount = this.getRenderedColumnsCount();
            for(let i = 0; i < columnHeadersCount; i++){
                for(let renderedColumnIndex = -1 * rowHeaderCount; renderedColumnIndex < columnCount; renderedColumnIndex++){
                    this.markIfOversizedColumnHeader(renderedColumnIndex);
                }
            }
            wtViewport.hasOversizedColumnHeadersMarked[overlayName] = true;
        }
    }
    /**
   * Disconnects the ResizeObservers and drops cache references so the
   * MasterTable instance, the trimming container, and the hider can be
   * garbage-collected after Walkontable is destroyed.
   */ destroy() {
        _class_private_field_get(this, _trimmingContainerObserver)?.disconnect();
        _class_private_field_get(this, _hiderObserver)?.disconnect();
        _class_private_field_set(this, _trimmingCache, null);
        _class_private_field_set(this, _trimmingContainerObserver, null);
        _class_private_field_set(this, _hiderObserver, null);
        _class_private_field_set(this, _observedTrimmingElement, null);
    }
    /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */ constructor(dataAccessObject, facadeGetter, domBindings, wtSettings){
        super(dataAccessObject, facadeGetter, domBindings, wtSettings, 'master'), _class_private_field_init(this, _trimmingCache, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _trimmingContainerObserver, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _hiderObserver, {
            writable: true,
            value: void 0
        }), _class_private_field_init(this, _observedTrimmingElement, {
            writable: true,
            value: void 0
        }), _class_private_field_set(this, _trimmingCache, null), _class_private_field_set(this, _trimmingContainerObserver, null), _class_private_field_set(this, _hiderObserver, null), _class_private_field_set(this, _observedTrimmingElement, null);
    }
}
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(MasterTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(MasterTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
var __TURBOPACK__default__export__ = MasterTable;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/calculatedColumns.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
;
const MIXIN_NAME = 'calculatedColumns';
/**
 * Mixin for the subclasses of `Table` with implementations of
 * helper methods that are related to columns.
 * This mixin is meant to be applied in the subclasses of `Table`
 * that use virtual rendering in the horizontal axis.
 *
 * @type {object}
 */ const calculatedColumns = {
    /**
   * Get the source index of the first rendered column. If no columns are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getFirstRenderedColumn () {
        const startColumn = this.dataAccessObject.startColumnRendered;
        if (startColumn === null) {
            return -1;
        }
        return startColumn;
    },
    /**
   * Get the source index of the first column fully visible in the viewport. If no columns are fully visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getFirstVisibleColumn () {
        const startColumn = this.dataAccessObject.startColumnVisible;
        if (startColumn === null) {
            return -1;
        }
        return startColumn;
    },
    /**
   * Get the source index of the first column partially visible in the viewport. If no columns are partially visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getFirstPartiallyVisibleColumn () {
        const startColumn = this.dataAccessObject.startColumnPartiallyVisible;
        if (startColumn === null) {
            return -1;
        }
        return startColumn;
    },
    /**
   * Get the source index of the last rendered column. If no columns are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getLastRenderedColumn () {
        const endColumn = this.dataAccessObject.endColumnRendered;
        if (endColumn === null) {
            return -1;
        }
        return endColumn;
    },
    /**
   * Get the source index of the last column fully visible in the viewport. If no columns are fully visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getLastVisibleColumn () {
        const endColumn = this.dataAccessObject.endColumnVisible;
        if (endColumn === null) {
            return -1;
        }
        return endColumn;
    },
    /**
   * Get the source index of the last column partially visible in the viewport. If no columns are partially visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getLastPartiallyVisibleColumn () {
        const endColumn = this.dataAccessObject.endColumnPartiallyVisible;
        if (endColumn === null) {
            return -1;
        }
        return endColumn;
    },
    /**
   * Get the number of rendered columns.
   *
   * @returns {number}
   * @this Table
   */ getRenderedColumnsCount () {
        return this.dataAccessObject.countColumnsRendered;
    },
    /**
   * Get the number of fully visible columns in the viewport.
   *
   * @returns {number}
   * @this Table
   */ getVisibleColumnsCount () {
        return this.dataAccessObject.countColumnsVisible;
    },
    /**
   * Get the number of rendered row headers.
   *
   * @returns {number}
   * @this Table
   */ getRowHeadersCount () {
        return this.dataAccessObject.rowHeaders.length;
    }
};
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["defineGetter"])(calculatedColumns, 'MIXIN_NAME', MIXIN_NAME, {
    writable: false,
    enumerable: false
});
var __TURBOPACK__default__export__ = calculatedColumns;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/calculatedRows.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
;
const MIXIN_NAME = 'calculatedRows';
/**
 * Mixin for the subclasses of `Table` with implementations of
 * helper methods that are related to rows.
 * This mixin is meant to be applied in the subclasses of `Table`
 * that use virtual rendering in the vertical axis.
 *
 * @type {object}
 */ const calculatedRows = {
    /**
   * Get the source index of the first rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getFirstRenderedRow () {
        const startRow = this.dataAccessObject.startRowRendered;
        if (startRow === null) {
            return -1;
        }
        return startRow;
    },
    /**
   * Get the source index of the first row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getFirstVisibleRow () {
        const startRow = this.dataAccessObject.startRowVisible;
        if (startRow === null) {
            return -1;
        }
        return startRow;
    },
    /**
   * Get the source index of the first row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getFirstPartiallyVisibleRow () {
        const startRow = this.dataAccessObject.startRowPartiallyVisible;
        if (startRow === null) {
            return -1;
        }
        return startRow;
    },
    /**
   * Get the source index of the last rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getLastRenderedRow () {
        const endRow = this.dataAccessObject.endRowRendered;
        if (endRow === null) {
            return -1;
        }
        return endRow;
    },
    /**
   * Get the source index of the last row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getLastVisibleRow () {
        const endRow = this.dataAccessObject.endRowVisible;
        if (endRow === null) {
            return -1;
        }
        return endRow;
    },
    /**
   * Get the source index of the last row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getLastPartiallyVisibleRow () {
        const endRow = this.dataAccessObject.endRowPartiallyVisible;
        if (endRow === null) {
            return -1;
        }
        return endRow;
    },
    /**
   * Get the number of rendered rows.
   *
   * @returns {number}
   * @this Table
   */ getRenderedRowsCount () {
        return this.dataAccessObject.countRowsRendered;
    },
    /**
   * Get the number of fully visible rows in the viewport.
   *
   * @returns {number}
   * @this Table
   */ getVisibleRowsCount () {
        return this.dataAccessObject.countRowsVisible;
    },
    /**
   * Get the number of rendered column headers.
   *
   * @returns {number}
   * @this Table
   */ getColumnHeadersCount () {
        return this.dataAccessObject.columnHeaders.length;
    }
};
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["defineGetter"])(calculatedRows, 'MIXIN_NAME', MIXIN_NAME, {
    writable: false,
    enumerable: false
});
var __TURBOPACK__default__export__ = calculatedRows;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyColumnsStart.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
;
const MIXIN_NAME = 'stickyColumnsStart';
/**
 * Mixin for the subclasses of `Table` with implementations of
 * helper methods that are related to columns.
 * This mixin is meant to be applied in the subclasses of `Table`
 * that use sticky rendering of the first columns in the horizontal axis.
 *
 * @type {object}
 */ const stickyColumnsStart = {
    /**
   * Get the source index of the first rendered column. If no columns are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getFirstRenderedColumn () {
        const allStickyColumns = this.getRenderedColumnsCount();
        if (allStickyColumns === 0) {
            return -1;
        }
        return 0;
    },
    /**
   * Get the source index of the first column fully visible in the viewport. If no columns are fully visible, returns an error code: -1.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getFirstVisibleColumn () {
        return this.getFirstRenderedColumn();
    },
    /**
   * Get the source index of the first column partially visible in the viewport. If no columns are partially visible, returns an error code: -1.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getFirstPartiallyVisibleColumn () {
        return this.getFirstRenderedColumn();
    },
    /**
   * Get the source index of the last rendered column. If no columns are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getLastRenderedColumn () {
        return this.getRenderedColumnsCount() - 1;
    },
    /**
   * Get the source index of the last column fully visible in the viewport. If no columns are fully visible, returns an error code: -1.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getLastVisibleColumn () {
        return this.getLastRenderedColumn();
    },
    /**
   * Get the source index of the last column partially visible in the viewport. If no columns are partially visible, returns an error code: -1.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getLastPartiallyVisibleColumn () {
        return this.getLastRenderedColumn();
    },
    /**
   * Get the number of rendered columns.
   *
   * @returns {number}
   * @this Table
   */ getRenderedColumnsCount () {
        return Math.min(this.wtSettings.getSetting('totalColumns') ?? 0, this.wtSettings.getSetting('fixedColumnsStart'));
    },
    /**
   * Get the number of fully visible columns in the viewport.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getVisibleColumnsCount () {
        return this.getRenderedColumnsCount();
    },
    /**
   * Get the number of rendered row headers.
   *
   * @returns {number}
   * @this Table
   */ getRowHeadersCount () {
        return this.dataAccessObject.rowHeaders.length;
    }
};
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["defineGetter"])(stickyColumnsStart, 'MIXIN_NAME', MIXIN_NAME, {
    writable: false,
    enumerable: false
});
var __TURBOPACK__default__export__ = stickyColumnsStart;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyRowsBottom.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
;
const MIXIN_NAME = 'stickyRowsBottom';
/**
 * Mixin for the subclasses of `Table` with implementations of
 * helper methods that are related to rows.
 * This mixin is meant to be applied in the subclasses of `Table`
 * that use sticky rendering of the bottom rows in the vertical axis.
 *
 * @type {object}
 */ const stickyRowsBottom = {
    /**
   * Get the source index of the first rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getFirstRenderedRow () {
        const allStickyRows = this.getRenderedRowsCount();
        if (allStickyRows === 0) {
            return -1;
        }
        return (this.wtSettings.getSetting('totalRows') ?? 0) - allStickyRows;
    },
    /**
   * Get the source index of the first row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getFirstVisibleRow () {
        return this.getFirstRenderedRow();
    },
    /**
   * Get the source index of the first row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getFirstPartiallyVisibleRow () {
        return this.getFirstRenderedRow();
    },
    /**
   * Get the source index of the last rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getLastRenderedRow () {
        const allStickyRows = this.getRenderedRowsCount();
        if (allStickyRows === 0) {
            return -1;
        }
        return (this.wtSettings.getSetting('totalRows') ?? 0) - 1;
    },
    /**
   * Get the source index of the last row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getLastVisibleRow () {
        return this.getLastRenderedRow();
    },
    /**
   * Get the source index of the last row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getLastPartiallyVisibleRow () {
        return this.getLastRenderedRow();
    },
    /**
   * Get the number of rendered rows.
   *
   * @returns {number}
   * @this Table
   */ getRenderedRowsCount () {
        return Math.min(this.wtSettings.getSetting('totalRows') ?? 0, this.wtSettings.getSetting('fixedRowsBottom'));
    },
    /**
   * Get the number of fully visible rows in the viewport.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getVisibleRowsCount () {
        return this.getRenderedRowsCount();
    },
    /**
   * Get the number of rendered column headers.
   *
   * @returns {number}
   * @this Table
   */ getColumnHeadersCount () {
        return 0;
    }
};
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["defineGetter"])(stickyRowsBottom, 'MIXIN_NAME', MIXIN_NAME, {
    writable: false,
    enumerable: false
});
var __TURBOPACK__default__export__ = stickyRowsBottom;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyRowsTop.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
;
const MIXIN_NAME = 'stickyRowsTop';
/**
 * Mixin for the subclasses of `Table` with implementations of
 * helper methods that are related to rows.
 * This mixin is meant to be applied in the subclasses of `Table`
 * that use sticky rendering of the top rows in the vertical axis.
 *
 * @type {object}
 */ const stickyRowsTop = {
    /**
   * Get the source index of the first rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getFirstRenderedRow () {
        const allStickyRows = this.getRenderedRowsCount();
        if (allStickyRows === 0) {
            return -1;
        }
        return 0;
    },
    /**
   * Get the source index of the first row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getFirstVisibleRow () {
        return this.getFirstRenderedRow();
    },
    /**
   * Get the source index of the first row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getFirstPartiallyVisibleRow () {
        return this.getFirstRenderedRow();
    },
    /**
   * Get the source index of the last rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */ getLastRenderedRow () {
        return this.getRenderedRowsCount() - 1;
    },
    /**
   * Get the source index of the last row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getLastVisibleRow () {
        return this.getLastRenderedRow();
    },
    /**
   * Get the source index of the last row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getLastPartiallyVisibleRow () {
        return this.getLastRenderedRow();
    },
    /**
   * Get the number of rendered rows.
   *
   * @returns {number}
   * @this Table
   */ getRenderedRowsCount () {
        return Math.min(this.wtSettings.getSetting('totalRows') ?? 0, this.wtSettings.getSetting('fixedRowsTop'));
    },
    /**
   * Get the number of fully visible rows in the viewport.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */ getVisibleRowsCount () {
        return this.getRenderedRowsCount();
    },
    /**
   * Get the number of rendered column headers.
   *
   * @returns {number}
   * @this Table
   */ getColumnHeadersCount () {
        return this.dataAccessObject.columnHeaders.length;
    }
};
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["defineGetter"])(stickyRowsTop, 'MIXIN_NAME', MIXIN_NAME, {
    writable: false,
    enumerable: false
});
var __TURBOPACK__default__export__ = stickyRowsTop;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/top.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyRowsTop$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyRowsTop.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/calculatedColumns.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
;
;
;
;
;
/**
 * Subclass of `Table` that provides the helper methods relevant to TopOverlay, implemented through mixins.
 *
 * @mixes stickyRowsTop
 * @mixes calculatedColumns
 */ class TopOverlayTable extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */ constructor(dataAccessObject, facadeGetter, domBindings, wtSettings){
        super(dataAccessObject, facadeGetter, domBindings, wtSettings, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TOP"]);
    }
}
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(TopOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyRowsTop$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(TopOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$calculatedColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
var __TURBOPACK__default__export__ = TopOverlayTable;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/table/topInlineStartCorner.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyRowsTop$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyRowsTop.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyColumnsStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/table/mixin/stickyColumnsStart.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/overlay/constants.mjs [client] (ecmascript)");
;
;
;
;
;
/**
 * Subclass of `Table` that provides the helper methods relevant to topInlineStartCornerOverlay
 * (in RTL mode the overlay sits on the right of the screen), implemented through mixins.
 *
 * @mixes stickyRowsTop
 * @mixes stickyColumnsStart
 */ class TopInlineStartCornerOverlayTable extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */ constructor(dataAccessObject, facadeGetter, domBindings, wtSettings){
        super(dataAccessObject, facadeGetter, domBindings, wtSettings, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$overlay$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CLONE_TOP_INLINE_START_CORNER"]);
    }
}
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(TopInlineStartCornerOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyRowsTop$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(TopInlineStartCornerOverlayTable, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$table$2f$mixin$2f$stickyColumnsStart$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
var __TURBOPACK__default__export__ = TopInlineStartCornerOverlayTable;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/cellCoords.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Finds which column the mouse is over within a given column range.
 *
 * @param {Walkontable} wotInstance The Walkontable instance.
 * @param {number} row Row to use for measuring cell widths.
 * @param {number} startColumn First column in the range.
 * @param {number} endColumn Last column in the range (inclusive).
 * @param {number} relativeX Mouse X position relative to the first cell's left edge (or right edge in RTL).
 * @returns {number | null} Column index, or null if the mouse is outside the range.
 */ __turbopack_context__.s([
    "findColumnAtX",
    ()=>findColumnAtX,
    "findRowAtY",
    ()=>findRowAtY
]);
function findColumnAtX(wotInstance, row, startColumn, endColumn, relativeX) {
    let accumulatedX = 0;
    for(let column = startColumn; column <= endColumn; column++){
        const cellElement = wotInstance.getCell({
            row,
            col: column
        }, true);
        if (!(cellElement instanceof HTMLElement)) {
            continue;
        }
        const width = cellElement.offsetWidth;
        if (relativeX < accumulatedX + width) {
            return column;
        }
        accumulatedX += width;
    }
    return null;
}
function findRowAtY(wotInstance, column, startRow, endRow, relativeY) {
    let accumulatedY = 0;
    for(let row = startRow; row <= endRow; row++){
        const cellElement = wotInstance.getCell({
            row,
            col: column
        }, true);
        if (!(cellElement instanceof HTMLElement)) {
            continue;
        }
        const height = cellElement.offsetHeight;
        if (relativeY < accumulatedY + height) {
            return row;
        }
        accumulatedY += height;
    }
    return null;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/column.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>ColumnUtils
]);
class ColumnUtils {
    /**
   * Returns column width based on passed source index.
   *
   * @param {number} sourceIndex Column source index.
   * @returns {number}
   */ getWidth(sourceIndex) {
        const width = this.wtSettings.getSetting('columnWidth', sourceIndex) || this.wtSettings.getSetting('defaultColumnWidth');
        return width;
    }
    /**
   * Returns column header height based on passed header level.
   *
   * @param {number} level Column header level.
   * @returns {number}
   */ getHeaderHeight(level) {
        let height = this.wtSettings.getSetting('stylesHandler').getDefaultRowHeight();
        const oversizedHeight = this.dataAccessObject.wtViewport.oversizedColumnHeaders[level];
        if (oversizedHeight !== undefined) {
            height = height ? Math.max(height, oversizedHeight) : oversizedHeight;
        }
        return height;
    }
    /**
   * Returns column header width based on passed source index.
   *
   * @param {number} sourceIndex Column source index.
   * @returns {number}
   */ getHeaderWidth(sourceIndex) {
        const { columnFilter } = this.dataAccessObject.wtTable;
        if (!columnFilter) {
            return undefined;
        }
        return this.headerWidths.get(columnFilter.sourceToRendered(sourceIndex));
    }
    /**
   * Calculates column header widths that can be retrieved from the cache.
   */ calculateWidths() {
        const { wtSettings } = this;
        let rowHeaderWidthSetting = wtSettings.getSetting('rowHeaderWidth');
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        rowHeaderWidthSetting = wtSettings.getSetting('onModifyRowHeaderWidth', rowHeaderWidthSetting);
        if (rowHeaderWidthSetting !== null && rowHeaderWidthSetting !== undefined) {
            const rowHeadersCount = wtSettings.getSetting('rowHeaders').length;
            const defaultColumnWidth = wtSettings.getSetting('defaultColumnWidth');
            for(let visibleColumnIndex = 0; visibleColumnIndex < rowHeadersCount; visibleColumnIndex++){
                // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
                let width = Array.isArray(rowHeaderWidthSetting) ? rowHeaderWidthSetting[visibleColumnIndex] : rowHeaderWidthSetting;
                // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
                width = width === null || width === undefined ? defaultColumnWidth : width;
                this.headerWidths.set(visibleColumnIndex, width);
            }
        }
    }
    /**
   * @param {TableDao} dataAccessObject The table Data Access Object.
   * @param {Settings} wtSettings The walkontable settings.
   */ constructor(dataAccessObject, wtSettings){
        /**
   * @type {Map<number, number>}
   */ this.headerWidths = new Map();
        this.dataAccessObject = dataAccessObject;
        this.wtSettings = wtSettings;
    }
}
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/nodesPool.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Factory for newly created DOM elements.
 *
 * Historically this class cached nodes keyed by source coordinate so the diffing renderer could
 * reuse them. The engine now renders directly to a fixed, viewport-sized set of DOM nodes
 * (see OrderView), so no cache is needed — `obtain()` simply creates a fresh element. The class is
 * kept as a thin factory for backward compatibility with the Walkontable export surface.
 *
 * @class {NodesPool}
 */ __turbopack_context__.s([
    "NodesPool",
    ()=>NodesPool
]);
class NodesPool {
    /**
   * Set document owner for this instance.
   *
   * @param {Document} rootDocument The document window owner.
   */ setRootDocument(rootDocument) {
        this.rootDocument = rootDocument;
    }
    /**
   * Creates and returns a new DOM element of the configured node type. The row/column arguments are
   * accepted for backward compatibility with the renderer factories and are ignored.
   *
   * @param {number} [_rowIndex] Ignored. Kept for call-site compatibility.
   * @param {number} [_columnIndex] Ignored. Kept for call-site compatibility.
   * @returns {HTMLElement}
   */ obtain(_rowIndex, _columnIndex) {
        return this.rootDocument.createElement(this.nodeType);
    }
    /**
   * Creates a new NodesPool instance.
   *
   * @param {string} nodeType The type of DOM node to generate (e.g. 'TH', 'TD').
   */ constructor(nodeType){
        this.nodeType = nodeType.toUpperCase();
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/constants.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Describes that ViewSizeSet instance doesn't share sizes with another
 * instance (root node can contain only one type of children nodes).
 *
 * @type {number}
 */ __turbopack_context__.s([
    "WORKING_SPACE_ALL",
    ()=>WORKING_SPACE_ALL,
    "WORKING_SPACE_BOTTOM",
    ()=>WORKING_SPACE_BOTTOM,
    "WORKING_SPACE_TOP",
    ()=>WORKING_SPACE_TOP
]);
const WORKING_SPACE_ALL = 0;
const WORKING_SPACE_TOP = 1;
const WORKING_SPACE_BOTTOM = 2;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/sharedView.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "SharedOrderView",
    ()=>SharedOrderView
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/view.mjs [client] (ecmascript)");
;
class SharedOrderView extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$view$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["OrderView"] {
    /**
   * The method results in merging external order view into the current order. This happens only for order views which
   * operate on the same root node.
   *
   * In the table, there is only one scenario when this happens. TR root element
   * has a common root node with cells order view and row headers order view. Both classes have to share
   * information about their order sizes to make proper diff calculations.
   *
   * @param {OrderView} orderView The order view to merging with. The view will be added at the beginning of the list.
   * @returns {SharedOrderView}
   */ prependView(orderView) {
        this.sizeSet.prepend(orderView.sizeSet);
        orderView.sizeSet.append(this.sizeSet);
        return this;
    }
    /**
   * The method results in merging external order view into the current order. This happens only for order views which
   * operate on the same root node.
   *
   * In the table, there is only one scenario when this happens. TR root element
   * has a common root node with cells order view and row headers order view. Both classes have to share
   * information about their order sizes to make proper diff calculations.
   *
   * @param {OrderView} orderView The order view to merging with. The view will be added at the end of the list.
   * @returns {SharedOrderView}
   */ appendView(orderView) {
        this.sizeSet.append(orderView.sizeSet);
        orderView.sizeSet.prepend(this.sizeSet);
        return this;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/view.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "OrderView",
    ()=>OrderView
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$viewSizeSet$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/viewSizeSet.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/constants.mjs [client] (ecmascript)");
;
;
class OrderView {
    /**
   * Sets the size for rendered elements. It can be a size for rows, cells or size for row
   * headers etc. It depends for what table renderer this instance was created.
   *
   * @param {number} size The size.
   * @returns {OrderView}
   */ setSize(size) {
        this.sizeSet.setSize(size);
        return this;
    }
    /**
   * Sets the offset for rendered elements. The offset describes the shift between 0 and
   * the first rendered element according to the scroll position.
   *
   * @param {number} offset The offset.
   * @returns {OrderView}
   */ setOffset(offset) {
        this.sizeSet.setOffset(offset);
        return this;
    }
    /**
   * Checks if this instance of the view shares the root node with another instance. This happens only once when
   * a row (TR) as a root node is managed by two OrderView instances. If this happens another DOM injection
   * algorithm is performed to achieve consistent order.
   *
   * @returns {boolean}
   */ isSharedViewSet() {
        return this.sizeSet.isShared();
    }
    /**
   * Returns rendered DOM element based on visual index.
   *
   * @param {number} visualIndex The visual index.
   * @returns {HTMLElement}
   */ getNode(visualIndex) {
        return visualIndex < this.collectedNodes.length ? this.collectedNodes[visualIndex] : null;
    }
    /**
   * Returns currently processed DOM element.
   *
   * @returns {HTMLElement}
   */ getCurrentNode() {
        const length = this.collectedNodes.length;
        return length > 0 ? this.collectedNodes[length - 1] : null;
    }
    /**
   * Returns the count of children in the root node that belong to this view (for shared root nodes
   * only the children matching this view's node type and working space are counted).
   *
   * @returns {number}
   */ getRenderedChildCount() {
        const { rootNode, sizeSet } = this;
        let childElementCount = 0;
        if (this.isSharedViewSet()) {
            let element = rootNode.firstElementChild;
            while(element){
                if (element.tagName === this.childNodeType) {
                    childElementCount += 1;
                } else if (sizeSet.isPlaceOn(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["WORKING_SPACE_TOP"])) {
                    break;
                }
                element = element.nextElementSibling;
            }
        } else {
            childElementCount = rootNode.childElementCount;
        }
        return childElementCount;
    }
    /**
   * Setups and prepares all necessary properties and starts the rendering process. It adjusts the
   * number of children in the root node to the next view size, creating or removing nodes as needed.
   * This method has to be called only once (at the start) for the render cycle.
   */ start() {
        this.collectedNodes.length = 0;
        this.visualIndex = 0;
        const { rootNode, sizeSet } = this;
        const isShared = this.isSharedViewSet();
        const { nextSize } = sizeSet.getViewSize();
        let childElementCount = this.getRenderedChildCount();
        while(childElementCount < nextSize){
            const newNode = this.nodesPool();
            if (!isShared || sizeSet.isPlaceOn(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["WORKING_SPACE_BOTTOM"])) {
                rootNode.appendChild(newNode);
            } else {
                rootNode.insertBefore(newNode, rootNode.firstChild);
            }
            childElementCount += 1;
        }
        const isSharedPlacedOnTop = isShared && sizeSet.isPlaceOn(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["WORKING_SPACE_TOP"]);
        while(childElementCount > nextSize){
            const childToRemove = isSharedPlacedOnTop ? rootNode.firstChild : rootNode.lastChild;
            if (childToRemove) {
                rootNode.removeChild(childToRemove);
            }
            childElementCount -= 1;
        }
    }
    /**
   * Renders the DOM element for the current visual index by reusing the child already in place.
   * If a child of the wrong node type occupies the slot, it is replaced with a freshly created one.
   * This method has to be called as many times as the size count is met (to cover all previously rendered DOM elements).
   */ render() {
        const { rootNode, sizeSet } = this;
        let visualIndex = this.visualIndex;
        if (this.isSharedViewSet() && sizeSet.isPlaceOn(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["WORKING_SPACE_BOTTOM"]) && sizeSet.sharedSize) {
            visualIndex += sizeSet.sharedSize.nextSize;
        }
        // Index by element children only (`children`, not `childNodes`) to stay consistent with the
        // element-based counting in `start()` and `getRenderedChildCount()`; a stray text/comment node
        // in the root would otherwise shift the slot.
        let node = rootNode.children[visualIndex];
        if (!node) {
            // Defensive guard: `start()` always provisions enough children for this render cycle, so the
            // slot should exist. Recreate it rather than dereferencing `undefined` if that ever breaks.
            node = this.nodesPool();
            rootNode.appendChild(node);
        } else if (node.tagName !== this.childNodeType) {
            const newNode = this.nodesPool();
            node.replaceWith(newNode);
            node = newNode;
        }
        this.collectedNodes.push(node);
        this.visualIndex += 1;
    }
    /**
   * Ends the render process. No cleanup is required — all work is done in `start()` and `render()`.
   */ end() {
    // intentionally empty
    }
    /**
   * Creates a new OrderView instance.
   *
   * @param {HTMLElement} rootNode The root node to manage.
   * @param {function(): HTMLElement} nodesPool Factory for creating new DOM elements.
   * @param {string} childNodeType The type of child nodes to manage.
   */ constructor(rootNode, nodesPool, childNodeType){
        /**
   * Holder for sizing and positioning of the view.
   *
   * @type {ViewSizeSet}
   */ this.sizeSet = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$viewSizeSet$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ViewSizeSet"]();
        /**
   * The list of DOM elements which are rendered for this render cycle.
   *
   * @type {HTMLElement[]}
   */ this.collectedNodes = [];
        /**
   * The visual index of the currently processed child within a render cycle.
   *
   * @type {number}
   */ this.visualIndex = 0;
        this.rootNode = rootNode;
        this.nodesPool = nodesPool;
        this.childNodeType = typeof childNodeType === 'string' ? childNodeType.toUpperCase() : childNodeType;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/viewSize.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Holder for current and next size (count of rendered and to render DOM elements) and offset.
 *
 * @class {ViewSize}
 */ __turbopack_context__.s([
    "ViewSize",
    ()=>ViewSize
]);
class ViewSize {
    /**
   * Sets new size of the rendered DOM elements.
   *
   * @param {number} size The size.
   */ setSize(size) {
        this.nextSize = size;
    }
    /**
   * Sets new offset.
   *
   * @param {number} offset The offset.
   */ setOffset(offset) {
        this.nextOffset = offset;
    }
    constructor(){
        /**
   * Size of the rendered DOM elements which should be fulfilled.
   *
   * @type {number}
   */ this.nextSize = 0;
        /**
   * Offset of the first rendered element according to the scroll position.
   *
   * @type {number}
   */ this.nextOffset = 0;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/viewSizeSet.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ViewSizeSet",
    ()=>ViewSizeSet
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$viewSize$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/viewSize.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/orderView/constants.mjs [client] (ecmascript)");
;
;
class ViewSizeSet {
    /**
   * Sets the size for rendered elements. It can be a size for rows, cells or size for row
   * headers etc.
   *
   * @param {number} size The size.
   */ setSize(size) {
        this.size.setSize(size);
    }
    /**
   * Sets the offset for rendered elements. The offset describes the shift between 0 and
   * the first rendered element according to the scroll position.
   *
   * @param {number} offset The offset.
   */ setOffset(offset) {
        this.size.setOffset(offset);
    }
    /**
   * Returns ViewSize instance.
   *
   * @returns {ViewSize}
   */ getViewSize() {
        return this.size;
    }
    /**
   * Checks if this ViewSizeSet is sharing the size with another instance.
   *
   * @returns {boolean}
   */ isShared() {
        return this.sharedSize !== null;
    }
    /**
   * Checks what working space describes this size instance.
   *
   * @param {number} workingSpace The number which describes the type of the working space (see constants.js).
   * @returns {boolean}
   */ isPlaceOn(workingSpace) {
        return this.workingSpace === workingSpace;
    }
    /**
   * Appends the ViewSizeSet instance to this instance that turns it into a shared mode.
   *
   * @param {ViewSizeSet} viewSize The instance of the ViewSizeSet class.
   */ append(viewSize) {
        this.workingSpace = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["WORKING_SPACE_TOP"];
        viewSize.workingSpace = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["WORKING_SPACE_BOTTOM"];
        this.sharedSize = viewSize.getViewSize();
    }
    /**
   * Prepends the ViewSize instance to this instance that turns it into a shared mode.
   *
   * @param {ViewSizeSet} viewSize The instance of the ViewSizeSet class.
   */ prepend(viewSize) {
        this.workingSpace = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["WORKING_SPACE_BOTTOM"];
        viewSize.workingSpace = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["WORKING_SPACE_TOP"];
        this.sharedSize = viewSize.getViewSize();
    }
    constructor(){
        /**
   * Holder for current and next view size and offset.
   *
   * @type {ViewSize}
   */ this.size = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$viewSize$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ViewSize"]();
        /**
   * Defines if this instance shares its size with another instance. If it's in the shared
   * mode it defines what space it occupies ('top' or 'bottom').
   *
   * @type {number}
   */ this.workingSpace = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$orderView$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["WORKING_SPACE_ALL"];
        /**
   * Shared Size instance.
   *
   * @type {ViewSize}
   */ this.sharedSize = null;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/positionCache.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "PositionCache",
    ()=>PositionCache
]);
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
var _totalItemsFn = /*#__PURE__*/ new WeakMap(), _sizeFn = /*#__PURE__*/ new WeakMap(), _defaultSizeFn = /*#__PURE__*/ new WeakMap();
class PositionCache {
    /**
   * Builds the prefix sum by reading the current total items, size function,
   * and default size from the configured providers.
   */ build() {
        const totalItems = _class_private_field_get(this, _totalItemsFn).call(this);
        const sizeFn = _class_private_field_get(this, _sizeFn);
        const defaultSize = _class_private_field_get(this, _defaultSizeFn).call(this);
        this.totalItems = totalItems;
        this.prefixSum = new Float64Array(totalItems + 1);
        this.prefixSum[0] = 0;
        for(let i = 0; i < totalItems; i++){
            const s = sizeFn(i);
            this.prefixSum[i + 1] = this.prefixSum[i] + (isNaN(s) ? defaultSize : s);
        }
    }
    /**
   * Returns the cumulative size at an index (sum of items 0..index-1).
   *
   * @param {number} index The item index.
   * @returns {number} The cumulative size before this index.
   */ getOffset(index) {
        if (!this.prefixSum || index <= 0) {
            return 0;
        }
        if (index >= this.totalItems) {
            return this.prefixSum[this.totalItems];
        }
        return this.prefixSum[index];
    }
    /**
   * Finds the item index at a given pixel offset using binary search.
   *
   * @param {number} offset The pixel offset.
   * @returns {number} The index whose cumulative start position is at or just before the offset.
   *   Returns `0` when there are no items (`totalItems === 0`), including when `offset > 0`.
   */ findIndexAtOffset(offset) {
        if (!this.isBuilt() || offset <= 0 || this.totalItems === 0) {
            return 0;
        }
        let lo = 0;
        let hi = this.totalItems;
        while(lo < hi){
            // eslint-disable-next-line no-bitwise
            const mid = lo + hi >>> 1;
            if (this.prefixSum[mid + 1] <= offset) {
                lo = mid + 1;
            } else {
                hi = mid;
            }
        }
        return Math.min(lo, this.totalItems - 1);
    }
    /**
   * Returns the size of a single item at the given index.
   *
   * @param {number} index The item index.
   * @returns {number} The size of the item (difference between consecutive prefix sums).
   */ getSizeAt(index) {
        if (!this.prefixSum || index < 0 || index >= this.totalItems) {
            return 0;
        }
        return this.prefixSum[index + 1] - this.prefixSum[index];
    }
    /**
   * Builds the prefix sum only when the cache is not yet built or the item
   * count has changed.
   */ ensureBuilt() {
        if (!this.isBuilt() || this.totalItems !== _class_private_field_get(this, _totalItemsFn).call(this)) {
            this.build();
        }
    }
    /**
   * Returns the total size of all items.
   *
   * @returns {number}
   */ getTotalSize() {
        if (!this.prefixSum) {
            return 0;
        }
        return this.prefixSum[this.totalItems];
    }
    /**
   * Invalidates the cache so it will be rebuilt on the next
   * {@link PositionCache#ensureBuilt} call.
   */ invalidate() {
        this.prefixSum = null;
        this.totalItems = 0;
    }
    /**
   * Returns whether the cache has been built.
   *
   * @returns {boolean}
   */ isBuilt() {
        return this.prefixSum !== null;
    }
    /**
   * @param {object} config The configuration object.
   * @param {Function} config.totalItemsFn A function that returns the total number of items (rows or columns).
   * @param {Function} config.sizeFn A function that returns the size for a given index.
   * @param {Function} config.defaultSizeFn A function that returns the default size for items
   *   that return NaN/undefined.
   */ constructor({ totalItemsFn, sizeFn, defaultSizeFn }){
        /**
   * A function that returns the total number of items (rows or columns).
   *
   * @type {Function}
   */ _class_private_field_init(this, _totalItemsFn, {
            writable: true,
            value: void 0
        });
        /**
   * A function that returns the size for a given index.
   *
   * @type {Function}
   */ _class_private_field_init(this, _sizeFn, {
            writable: true,
            value: void 0
        });
        /**
   * A function that returns the default size for items that return NaN/undefined.
   *
   * @type {Function}
   */ _class_private_field_init(this, _defaultSizeFn, {
            writable: true,
            value: void 0
        });
        /**
   * The prefix sum array. Contains the cumulative size of all items up to the index.
   *
   * @type {Float64Array|null}
   */ this.prefixSum = null;
        /**
   * The total number of items (rows or columns).
   *
   * @type {number}
   */ this.totalItems = 0;
        _class_private_field_set(this, _totalItemsFn, totalItemsFn);
        _class_private_field_set(this, _sizeFn, sizeFn);
        _class_private_field_set(this, _defaultSizeFn, defaultSizeFn);
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/row.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>RowUtils
]);
class RowUtils {
    /**
   * Returns row height based on passed source index.
   *
   * @param {number} sourceIndex Row source index.
   * @returns {number}
   */ getHeight(sourceIndex) {
        let height = this.wtSettings.getSetting('rowHeight', sourceIndex);
        const oversizedHeight = this.dataAccessObject.wtViewport.oversizedRows[sourceIndex];
        if (oversizedHeight !== undefined) {
            height = height === undefined ? oversizedHeight : Math.max(height, oversizedHeight);
        }
        return height;
    }
    /**
   * Returns row height based on passed source index for the specified overlay type.
   *
   * @param {number} sourceIndex Row source index.
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'|'master'} overlayName The overlay name.
   * @returns {number}
   */ getHeightByOverlayName(sourceIndex, overlayName) {
        let height = this.wtSettings.getSetting('rowHeightByOverlayName', sourceIndex, overlayName);
        const oversizedHeight = this.dataAccessObject.wtViewport.oversizedRows[sourceIndex];
        if (oversizedHeight !== undefined) {
            height = height === undefined ? oversizedHeight : Math.max(height, oversizedHeight);
        }
        return height;
    }
    /**
   * @param {TableDao} dataAccessObject The table Data Access Object.
   * @param {Settings} wtSettings The walkontable settings.
   */ constructor(dataAccessObject, wtSettings){
        this.dataAccessObject = dataAccessObject;
        this.wtSettings = wtSettings;
    }
}
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/3rdparty/walkontable/src/viewport.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/element.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$fullyVisibleColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/fullyVisibleColumns.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$fullyVisibleRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/fullyVisibleRows.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$partiallyVisibleColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/partiallyVisibleColumns.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$partiallyVisibleRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/partiallyVisibleRows.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$renderedAllColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/renderedAllColumns.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$renderedAllRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/renderedAllRows.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$renderedColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/renderedColumns.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$renderedRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/calculationType/renderedRows.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$viewportColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/viewportColumns.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$viewportRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/calculator/viewportRows.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$positionCache$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/utils/positionCache.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/constants.mjs [client] (ecmascript)");
;
;
;
;
;
/**
 * @class Viewport
 */ class Viewport {
    /**
   * @returns {number}
   */ getWorkspaceHeight() {
        const currentDocument = this.domBindings.rootDocument;
        const trimmingContainer = this.dataAccessObject.topOverlayTrimmingContainer;
        let height = 0;
        if (trimmingContainer === this.domBindings.rootWindow) {
            height = currentDocument.documentElement.clientHeight;
        } else {
            const elemHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(trimmingContainer);
            // returns height without DIV scrollbar
            if (elemHeight > 0 && trimmingContainer.clientHeight > 0) {
                height = trimmingContainer.clientHeight;
            } else {
                // Fall back to window height when the trimming container has zero client height
                // (e.g. a parent with overflow set but no explicit height). Returning Infinity
                // previously caused an unbounded viewport, expanding the parent to the browser's
                // ~2^25 px CSS height limit. See issue #3119.
                height = Math.max(currentDocument.documentElement.clientHeight, 1);
            }
        }
        return height;
    }
    /**
   * @returns {number}
   */ getViewportHeight() {
        let containerHeight = this.getWorkspaceHeight();
        const columnHeaderHeight = this.getColumnHeaderHeight();
        if (columnHeaderHeight > 0) {
            containerHeight -= columnHeaderHeight;
        }
        return containerHeight;
    }
    /**
   * Gets the width of the table workspace (in pixels). The workspace size in the current
   * implementation returns the width of the table holder element including scrollbar width when
   * the table has defined size and the width of the window excluding scrollbar width when
   * the table has no defined size (the window is a scrollable container).
   *
   * This is a bug, as the method should always return stable values, always without scrollbar width.
   * Changing this behavior would break the column calculators, which would also need to be adjusted.
   *
   * @returns {number}
   */ getWorkspaceWidth() {
        const { rootDocument, rootWindow } = this.domBindings;
        const trimmingContainer = this.dataAccessObject.inlineStartOverlayTrimmingContainer;
        let width;
        if (trimmingContainer === rootWindow) {
            const totalColumns = this.wtSettings.getSetting('totalColumns');
            width = this.wtTable.holder.offsetWidth;
            if (this.getRowHeaderWidth() + this.sumColumnWidths(0, totalColumns) > width) {
                width = rootDocument.documentElement.clientWidth;
            }
        } else {
            width = trimmingContainer.clientWidth;
        }
        return width;
    }
    /**
   * @returns {number}
   */ getViewportWidth() {
        const containerWidth = this.getWorkspaceWidth();
        const rowHeaderWidth = this.getRowHeaderWidth();
        if (rowHeaderWidth > 0) {
            return containerWidth - rowHeaderWidth;
        }
        return containerWidth;
    }
    /**
   * Checks if viewport has vertical scroll.
   *
   * @returns {boolean}
   */ hasVerticalScroll() {
        if (this.isVerticallyScrollableByWindow()) {
            const documentElement = this.domBindings.rootDocument.documentElement;
            return documentElement.scrollHeight > documentElement.clientHeight;
        }
        const { holder, hider } = this.wtTable;
        const holderHeight = holder.clientHeight;
        const hiderOffsetHeight = hider.offsetHeight;
        if (holderHeight < hiderOffsetHeight) {
            return true;
        }
        return hiderOffsetHeight > this.getWorkspaceHeight();
    }
    /**
   * Checks if viewport has horizontal scroll.
   *
   * @returns {boolean}
   */ hasHorizontalScroll() {
        if (this.isVerticallyScrollableByWindow()) {
            const documentElement = this.domBindings.rootDocument.documentElement;
            return documentElement.scrollWidth > documentElement.clientWidth;
        }
        const { hider } = this.wtTable;
        const hiderOffsetWidth = hider.offsetWidth;
        const scrollbarWidth = this.hasVerticalScroll() ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])() : 0;
        return hiderOffsetWidth > this.getWorkspaceWidth() - scrollbarWidth;
    }
    /**
   * Checks if the table uses the window as a viewport and if there is a vertical scrollbar.
   *
   * @returns {boolean}
   */ isVerticallyScrollableByWindow() {
        return this.dataAccessObject.topOverlayTrimmingContainer === this.domBindings.rootWindow;
    }
    /**
   * Checks if the table uses the window as a viewport and if there is a horizontal scrollbar.
   *
   * @returns {boolean}
   */ isHorizontallyScrollableByWindow() {
        return this.dataAccessObject.inlineStartOverlayTrimmingContainer === this.domBindings.rootWindow;
    }
    /**
   * @param {number} from The visual column index from the width sum is start calculated.
   * @param {number} length The length of the column to traverse.
   * @returns {number}
   */ sumColumnWidths(from, length) {
        let sum = 0;
        let column = from;
        while(column < length){
            sum += this.wtTable.getColumnWidth(column);
            column += 1;
        }
        return sum;
    }
    /**
   * @returns {number}
   */ getWorkspaceOffset() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["offset"])(this.wtTable.holder);
    }
    /**
   * @returns {number}
   */ getColumnHeaderHeight() {
        const columnHeaders = this.wtSettings.getSetting('columnHeaders');
        if (!columnHeaders.length) {
            this.columnHeaderHeight = 0;
        } else if (isNaN(this.columnHeaderHeight)) {
            this.columnHeaderHeight = this.wtTable.THEAD ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerHeight"])(this.wtTable.THEAD) : 0;
        }
        return this.columnHeaderHeight;
    }
    /**
   * @returns {number}
   */ getRowHeaderWidth() {
        const rowHeadersWidthSetting = this.wtSettings.getSetting('rowHeaderWidth');
        const rowHeaders = this.wtSettings.getSetting('rowHeaders');
        if (rowHeadersWidthSetting) {
            this.rowHeaderWidth = 0;
            for(let i = 0, len = rowHeaders.length; i < len; i++){
                const w = Array.isArray(rowHeadersWidthSetting) ? rowHeadersWidthSetting[i] : rowHeadersWidthSetting;
                this.rowHeaderWidth += w ?? NaN;
            }
        }
        if (isNaN(this.rowHeaderWidth)) {
            if (rowHeaders.length) {
                let TH = this.wtTable.TABLE.querySelector('TH');
                this.rowHeaderWidth = 0;
                for(let i = 0, len = rowHeaders.length; i < len; i++){
                    if (TH) {
                        this.rowHeaderWidth += (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["outerWidth"])(TH);
                        TH = TH.nextSibling;
                    } else {
                        // yes this is a cheat but it worked like that before, just taking assumption from CSS instead of measuring.
                        // TODO: proper fix
                        this.rowHeaderWidth += 50;
                    }
                }
            } else {
                this.rowHeaderWidth = 0;
            }
        }
        this.rowHeaderWidth = this.wtSettings.getSetting('onModifyRowHeaderWidth', this.rowHeaderWidth) || this.rowHeaderWidth;
        return this.rowHeaderWidth;
    }
    /**
   * Creates rows calculators. The type of the calculations can be chosen from the list:
   *  - 'rendered' Calculates rows that should be rendered within the current table's viewport;
   *  - 'fullyVisible' Calculates rows that are fully visible (used mostly for scrolling purposes);
   *  - 'partiallyVisible' Calculates rows that are partially visible (used mostly for scrolling purposes).
   *
   * @param {'rendered' | 'fullyVisible' | 'partiallyVisible'} calculatorTypes The list of the calculation types.
   * @returns {ViewportRowsCalculator}
   */ createRowsCalculator(calculatorTypes = [
        'rendered',
        'fullyVisible',
        'partiallyVisible'
    ]) {
        const { wtSettings, wtTable } = this;
        const totalRows = wtSettings.getSetting('totalRows');
        let height = this.getViewportHeight();
        let scrollbarHeight;
        let fixedRowsHeight;
        this.rowHeaderWidth = NaN;
        let pos = this.dataAccessObject.topScrollPosition - this.dataAccessObject.topParentOffset;
        const fixedRowsTop = wtSettings.getSetting('fixedRowsTop');
        const fixedRowsBottom = wtSettings.getSetting('fixedRowsBottom');
        if (fixedRowsTop && pos >= 0) {
            fixedRowsHeight = this.dataAccessObject.topOverlay.sumCellSizes(0, fixedRowsTop);
            pos += fixedRowsHeight;
            height -= fixedRowsHeight;
        }
        if (fixedRowsBottom && this.dataAccessObject.bottomOverlay.clone) {
            fixedRowsHeight = this.dataAccessObject.bottomOverlay.sumCellSizes(totalRows - fixedRowsBottom, totalRows);
            height -= fixedRowsHeight;
        }
        if (wtTable.holder.clientHeight === wtTable.holder.offsetHeight) {
            scrollbarHeight = 0;
        } else {
            scrollbarHeight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(this.domBindings.rootDocument);
        }
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$viewportRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ViewportRowsCalculator"]({
            calculationTypes: calculatorTypes.flatMap((type)=>{
                const factory = this.rowsCalculatorTypes.get(type);
                return factory ? [
                    [
                        type,
                        factory()
                    ]
                ] : [];
            }),
            viewportHeight: height,
            scrollOffset: pos,
            totalRows,
            overrideFn: wtSettings.getSettingPure('viewportRowCalculatorOverride'),
            horizontalScrollbarHeight: scrollbarHeight,
            rowHeightCache: this.rowHeightCache
        });
    }
    /**
   * Creates columns calculators. The type of the calculations can be chosen from the list:
   *  - 'rendered' Calculates columns that should be rendered within the current table's viewport;
   *  - 'fullyVisible' Calculates columns that are fully visible (used mostly for scrolling purposes);
   *  - 'partiallyVisible' Calculates columns that are partially visible (used mostly for scrolling purposes).
   *
   * @param {'rendered' | 'fullyVisible' | 'partiallyVisible'} calculatorTypes The list of the calculation types.
   * @returns {ViewportColumnsCalculator}
   */ createColumnsCalculator(calculatorTypes = [
        'rendered',
        'fullyVisible',
        'partiallyVisible'
    ]) {
        const { wtSettings, wtTable } = this;
        const totalColumns = wtSettings.getSetting('totalColumns');
        let width = this.getViewportWidth();
        let pos = Math.abs(this.dataAccessObject.inlineStartScrollPosition) - this.dataAccessObject.inlineStartParentOffset;
        this.columnHeaderHeight = NaN;
        const fixedColumnsStart = wtSettings.getSetting('fixedColumnsStart');
        if (fixedColumnsStart && pos >= 0) {
            const fixedColumnsWidth = this.dataAccessObject.inlineStartOverlay.sumCellSizes(0, fixedColumnsStart);
            pos += fixedColumnsWidth;
            width -= fixedColumnsWidth;
        }
        if (wtTable.holder.clientWidth !== wtTable.holder.offsetWidth) {
            width -= (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$element$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getScrollbarWidth"])(this.domBindings.rootDocument);
        }
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$viewportColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ViewportColumnsCalculator"]({
            calculationTypes: calculatorTypes.flatMap((type)=>{
                const factory = this.columnsCalculatorTypes.get(type);
                return factory ? [
                    [
                        type,
                        factory()
                    ]
                ] : [];
            }),
            viewportWidth: width,
            scrollOffset: pos,
            totalColumns,
            overrideFn: wtSettings.getSettingPure('viewportColumnCalculatorOverride'),
            inlineStartOffset: this.dataAccessObject.inlineStartParentOffset,
            columnWidthCache: this.columnWidthCache
        });
    }
    /**
   * Creates rowsRenderCalculator and columnsRenderCalculator (before draw, to determine what rows and
   * cols should be rendered).
   *
   * @param {boolean} fastDraw If `true`, will try to avoid full redraw and only update the border positions.
   *                           If `false` or `undefined`, will perform a full redraw.
   * @returns {boolean} The fastDraw value, possibly modified.
   */ createCalculators(fastDraw = false) {
        const { wtSettings } = this;
        const rowsCalculator = this.createRowsCalculator();
        const columnsCalculator = this.createColumnsCalculator();
        if (fastDraw && !wtSettings.getSetting('renderAllRows')) {
            const proposedFullyVisibleRowsCalculator = rowsCalculator.getResultsFor('fullyVisible');
            const proposedPartiallyVisibleRowsCalculator = rowsCalculator.getResultsFor('partiallyVisible');
            fastDraw = this.areAllProposedVisibleRowsAlreadyRendered(proposedFullyVisibleRowsCalculator, proposedPartiallyVisibleRowsCalculator);
        }
        if (fastDraw && !wtSettings.getSetting('renderAllColumns')) {
            const proposedFullyVisibleColumnsCalculator = columnsCalculator.getResultsFor('fullyVisible');
            const proposedPartiallyVisibleColumnsCalculator = columnsCalculator.getResultsFor('partiallyVisible');
            fastDraw = this.areAllProposedVisibleColumnsAlreadyRendered(proposedFullyVisibleColumnsCalculator, proposedPartiallyVisibleColumnsCalculator);
        }
        if (!fastDraw) {
            this.rowsRenderCalculator = rowsCalculator.getResultsFor('rendered') ?? null;
            this.columnsRenderCalculator = columnsCalculator.getResultsFor('rendered') ?? null;
        }
        this.rowsVisibleCalculator = rowsCalculator.getResultsFor('fullyVisible') ?? null;
        this.columnsVisibleCalculator = columnsCalculator.getResultsFor('fullyVisible') ?? null;
        this.rowsPartiallyVisibleCalculator = rowsCalculator.getResultsFor('partiallyVisible') ?? null;
        this.columnsPartiallyVisibleCalculator = columnsCalculator.getResultsFor('partiallyVisible') ?? null;
        return fastDraw;
    }
    /**
   * Creates rows and columns calculators (after draw, to determine what are
   * the actually fully visible and partially visible rows and columns).
   */ createVisibleCalculators() {
        const rowsCalculator = this.createRowsCalculator([
            'fullyVisible',
            'partiallyVisible'
        ]);
        const columnsCalculator = this.createColumnsCalculator([
            'fullyVisible',
            'partiallyVisible'
        ]);
        this.rowsVisibleCalculator = rowsCalculator.getResultsFor('fullyVisible') ?? null;
        this.columnsVisibleCalculator = columnsCalculator.getResultsFor('fullyVisible') ?? null;
        this.rowsPartiallyVisibleCalculator = rowsCalculator.getResultsFor('partiallyVisible') ?? null;
        this.columnsPartiallyVisibleCalculator = columnsCalculator.getResultsFor('partiallyVisible') ?? null;
    }
    /**
   * Returns information whether proposedFullyVisibleRowsCalculator viewport
   * is contained inside rows rendered in previous draw (cached in rowsRenderCalculator).
   *
   * @param {ViewportRowsCalculator} proposedFullyVisibleRowsCalculator The instance of the fully visible rows viewport calculator to compare with.
   * @param {ViewportRowsCalculator} proposedPartiallyVisibleRowsCalculator The instance of the partially visible rows viewport calculator to compare with.
   * @returns {boolean} Returns `true` if all proposed visible rows are already rendered (meaning: redraw is not needed).
   *                    Returns `false` if at least one proposed visible row is not already rendered (meaning: redraw is needed).
   */ areAllProposedVisibleRowsAlreadyRendered(proposedFullyVisibleRowsCalculator, proposedPartiallyVisibleRowsCalculator) {
        if (!this.rowsVisibleCalculator || !this.rowsRenderCalculator || !proposedFullyVisibleRowsCalculator || !proposedPartiallyVisibleRowsCalculator) {
            return false;
        }
        let { startRow, endRow } = proposedFullyVisibleRowsCalculator;
        const { startRow: partiallyVisibleStartRow, endRow: partiallyVisibleEndRow } = proposedPartiallyVisibleRowsCalculator;
        // if there are no fully visible rows at all...
        if (startRow === null && endRow === null) {
            if (!proposedFullyVisibleRowsCalculator.isVisibleInTrimmingContainer && partiallyVisibleStartRow !== null && !this.wtTable.isRowBeforeRenderedRows(partiallyVisibleStartRow) && partiallyVisibleEndRow !== null && !this.wtTable.isRowAfterRenderedRows(partiallyVisibleEndRow)) {
                return true;
            }
            // ...use partially visible rows calculator to determine what render type is needed
            startRow = partiallyVisibleStartRow;
            endRow = partiallyVisibleEndRow;
        }
        if (startRow === null || endRow === null) {
            return false;
        }
        const { startRow: renderedStartRow, endRow: renderedEndRow, rowStartOffset, rowEndOffset } = this.rowsRenderCalculator;
        if (renderedStartRow === null || renderedEndRow === null) {
            return false;
        }
        const totalRows = this.wtSettings.getSetting('totalRows') - 1;
        const renderingThreshold = this.wtSettings.getSetting('viewportRowRenderingThreshold');
        if (typeof renderingThreshold === 'number' && Number.isInteger(renderingThreshold) && renderingThreshold > 0) {
            startRow = Math.max(0, startRow - Math.min(rowStartOffset, renderingThreshold));
            endRow = Math.min(totalRows, endRow + Math.min(rowEndOffset, renderingThreshold));
        } else if (renderingThreshold === 'auto') {
            startRow = Math.max(0, startRow - Math.ceil(rowStartOffset / 2));
            endRow = Math.min(totalRows, endRow + Math.ceil(rowEndOffset / 2));
        }
        if (startRow < renderedStartRow || startRow === renderedStartRow && startRow > 0) {
            return false;
        } else if (endRow > renderedEndRow || endRow === renderedEndRow && endRow < totalRows) {
            return false;
        }
        return true;
    }
    /**
   * Returns information whether proposedFullyVisibleColumnsCalculator viewport
   * is contained inside column rendered in previous draw (cached in columnsRenderCalculator).
   *
   * @param {ViewportRowsCalculator} proposedFullyVisibleColumnsCalculator The instance of the fully visible columns viewport calculator to compare with.
   * @param {ViewportRowsCalculator} proposedPartiallyVisibleColumnsCalculator The instance of the partially visible columns viewport calculator to compare with.
   * @returns {boolean} Returns `true` if all proposed visible columns are already rendered (meaning: redraw is not needed).
   *                    Returns `false` if at least one proposed visible column is not already rendered (meaning: redraw is needed).
   */ areAllProposedVisibleColumnsAlreadyRendered(proposedFullyVisibleColumnsCalculator, proposedPartiallyVisibleColumnsCalculator) {
        if (!this.columnsVisibleCalculator || !this.columnsRenderCalculator || !proposedFullyVisibleColumnsCalculator || !proposedPartiallyVisibleColumnsCalculator) {
            return false;
        }
        let { startColumn, endColumn } = proposedFullyVisibleColumnsCalculator;
        const { startColumn: partiallyVisibleStartColumn, endColumn: partiallyVisibleEndColumn } = proposedPartiallyVisibleColumnsCalculator;
        // if there are no fully visible columns at all...
        if (startColumn === null && endColumn === null) {
            if (!proposedFullyVisibleColumnsCalculator.isVisibleInTrimmingContainer && partiallyVisibleStartColumn !== null && !this.wtTable.isColumnBeforeRenderedColumns(partiallyVisibleStartColumn) && partiallyVisibleEndColumn !== null && !this.wtTable.isColumnAfterRenderedColumns(partiallyVisibleEndColumn)) {
                return true;
            }
            // ...use partially visible columns calculator to determine what render type is needed
            startColumn = partiallyVisibleStartColumn;
            endColumn = partiallyVisibleEndColumn;
        }
        if (startColumn === null || endColumn === null) {
            return false;
        }
        const { startColumn: renderedStartColumn, endColumn: renderedEndColumn, columnStartOffset, columnEndOffset } = this.columnsRenderCalculator;
        if (renderedStartColumn === null || renderedEndColumn === null) {
            return false;
        }
        const totalColumns = this.wtSettings.getSetting('totalColumns') - 1;
        const renderingThreshold = this.wtSettings.getSetting('viewportColumnRenderingThreshold');
        if (typeof renderingThreshold === 'number' && Number.isInteger(renderingThreshold) && renderingThreshold > 0) {
            startColumn = Math.max(0, startColumn - Math.min(columnStartOffset, renderingThreshold));
            endColumn = Math.min(totalColumns, endColumn + Math.min(columnEndOffset, renderingThreshold));
        } else if (renderingThreshold === 'auto') {
            startColumn = Math.max(0, startColumn - Math.ceil(columnStartOffset / 2));
            endColumn = Math.min(totalColumns, endColumn + Math.ceil(columnEndOffset / 2));
        }
        if (startColumn < renderedStartColumn || startColumn === renderedStartColumn && startColumn > 0) {
            return false;
        } else if (endColumn > renderedEndColumn || endColumn === renderedEndColumn && endColumn < totalColumns) {
            return false;
        }
        return true;
    }
    /**
   * Marks the row height position cache as stale. The cache will be rebuilt
   * on the next viewport calculation.
   */ invalidateRowHeightCache() {
        this.rowHeightCache.invalidate();
    }
    /**
   * Marks the column width position cache as stale. The cache will be rebuilt
   * on the next viewport calculation.
   */ invalidateColumnWidthCache() {
        this.columnWidthCache.invalidate();
    }
    /**
   * Marks both the row height and column width position caches as stale.
   */ invalidateAllCaches() {
        this.rowHeightCache.invalidate();
        this.columnWidthCache.invalidate();
    }
    /**
   * Resets values in keys of the hasOversizedColumnHeadersMarked object after updateSettings.
   */ resetHasOversizedColumnHeadersMarked() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["objectEach"])(this.hasOversizedColumnHeadersMarked, (value, key, object)=>{
            object[key] = undefined;
        });
    }
    /**
   * @param {ViewportDao} dataAccessObject The Walkontable instance.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {EventManager} eventManager The instance event manager.
   * @param {Table} wtTable The table.
   */ constructor(dataAccessObject, domBindings, wtSettings, eventManager, wtTable){
        this.dataAccessObject = dataAccessObject;
        // legacy support
        this.wot = dataAccessObject.wot;
        this.instance = this.wot;
        this.domBindings = domBindings;
        this.wtSettings = wtSettings;
        this.wtTable = wtTable;
        this.oversizedRows = {};
        this.oversizedColumnHeaders = {};
        this.hasOversizedColumnHeadersMarked = {};
        this.clientHeight = 0;
        this.rowHeaderWidth = NaN;
        this.rowsVisibleCalculator = null;
        this.columnsVisibleCalculator = null;
        this.rowsCalculatorTypes = new Map([
            [
                'rendered',
                ()=>this.wtSettings.getSetting('renderAllRows') ? new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$renderedAllRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["RenderedAllRowsCalculationType"]() : new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$renderedRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["RenderedRowsCalculationType"]()
            ],
            [
                'fullyVisible',
                ()=>new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$fullyVisibleRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["FullyVisibleRowsCalculationType"]()
            ],
            [
                'partiallyVisible',
                ()=>new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$partiallyVisibleRows$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["PartiallyVisibleRowsCalculationType"]()
            ]
        ]);
        this.columnsCalculatorTypes = new Map([
            [
                'rendered',
                ()=>this.wtSettings.getSetting('renderAllColumns') ? new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$renderedAllColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["RenderedAllColumnsCalculationType"]() : new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$renderedColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["RenderedColumnsCalculationType"]()
            ],
            [
                'fullyVisible',
                ()=>new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$fullyVisibleColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["FullyVisibleColumnsCalculationType"]()
            ],
            [
                'partiallyVisible',
                ()=>new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$calculator$2f$calculationType$2f$partiallyVisibleColumns$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["PartiallyVisibleColumnsCalculationType"]()
            ]
        ]);
        /**
     * Cumulative row height prefix sum cache. Enables O(log n) scroll-to-row lookups
     * when custom row heights are configured.
     *
     * @type {PositionCache}
     */ this.rowHeightCache = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$positionCache$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["PositionCache"]({
            totalItemsFn: ()=>wtSettings.getSetting('totalRows'),
            sizeFn: (sourceRow)=>wtTable.getRowHeight(sourceRow) ?? NaN,
            defaultSizeFn: ()=>wtSettings.getSetting('stylesHandler').getDefaultRowHeight()
        });
        /**
     * Cumulative column width prefix sum cache. Enables O(log n) scroll-to-column lookups
     * when custom column widths are configured.
     *
     * @type {PositionCache}
     */ this.columnWidthCache = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$utils$2f$positionCache$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["PositionCache"]({
            totalItemsFn: ()=>wtSettings.getSetting('totalColumns'),
            sizeFn: (sourceCol)=>wtTable.getColumnWidth(sourceCol),
            defaultSizeFn: ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["DEFAULT_COLUMN_WIDTH"]
        });
        this.eventManager = eventManager;
        this.eventManager.addEventListener(this.domBindings.rootWindow, 'resize', ()=>{
            this.clientHeight = this.getWorkspaceHeight();
        });
    }
}
var __TURBOPACK__default__export__ = Viewport;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
]);

//# sourceMappingURL=node_modules_handsontable_3rdparty_151acf69.async.js.map