(globalThis["TURBOPACK"] || (globalThis["TURBOPACK"] = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/handsontable/selection/highlight/highlight.mjs [client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$activeHeader$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/types/activeHeader.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$areaLayered$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/types/areaLayered.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$area$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/types/area.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$column$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/types/column.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$focus$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/types/focus.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$customSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/types/customSelection.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$fill$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/types/fill.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$header$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/types/header.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$row$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/types/row.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__ACTIVE_HEADER_TYPE__as__HIGHLIGHT_ACTIVE_HEADER_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export ACTIVE_HEADER_TYPE as HIGHLIGHT_ACTIVE_HEADER_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__AREA_TYPE__as__HIGHLIGHT_AREA_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export AREA_TYPE as HIGHLIGHT_AREA_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FOCUS_TYPE__as__HIGHLIGHT_FOCUS_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FOCUS_TYPE as HIGHLIGHT_FOCUS_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__CUSTOM_SELECTION_TYPE__as__HIGHLIGHT_CUSTOM_SELECTION_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export CUSTOM_SELECTION_TYPE as HIGHLIGHT_CUSTOM_SELECTION_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FILL_TYPE__as__HIGHLIGHT_FILL_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FILL_TYPE as HIGHLIGHT_FILL_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__HEADER_TYPE__as__HIGHLIGHT_HEADER_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export HEADER_TYPE as HIGHLIGHT_HEADER_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__ROW_TYPE__as__HIGHLIGHT_ROW_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export ROW_TYPE as HIGHLIGHT_ROW_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__COLUMN_TYPE__as__HIGHLIGHT_COLUMN_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export COLUMN_TYPE as HIGHLIGHT_COLUMN_TYPE>");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
;
;
;
;
;
;
;
;
;
var /**
   * Creates (if not exist in the cache) Walkontable Selection instance.
   *
   * @param {Map} cacheMap The map where the instance will be cached.
   * @param {Function} highlightFactory The function factory.
   * @returns {VisualSelection}
   */ _createHighlight = /*#__PURE__*/ new WeakSet();
/**
 * Highlight class responsible for managing Walkontable Selection classes.
 *
 * With Highlight object you can manipulate four different highlight types:
 *  - `cell` can be added only to a single cell at a time and it defines currently selected cell;
 *  - `fill` can occur only once and its highlight defines selection of autofill functionality (managed by the plugin with the same name);
 *  - `areas` can be added to multiple cells at a time. This type highlights selected cell or multiple cells.
 *    The multiple cells have to be defined as an uninterrupted order (regular shape). Otherwise, the new layer of
 *    that type should be created to manage not-consecutive selection;
 *  - `header` can occur multiple times. This type is designed to highlight only headers. Like `area` type it
 *    can appear with multiple highlights (accessed under different level layers).
 *
 * @class Highlight
 * @util
 */ class Highlight {
    /**
   * Check if highlight cell rendering is disabled for specified highlight type.
   *
   * @param {string} highlightType Highlight type. Possible values are: `cell`, `area`, `fill` or `header`.
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @returns {boolean}
   */ isEnabledFor(highlightType, coords) {
        let type = highlightType;
        // Legacy compatibility.
        if (highlightType === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FOCUS_TYPE__as__HIGHLIGHT_FOCUS_TYPE$3e$__["HIGHLIGHT_FOCUS_TYPE"]) {
            type = 'current'; // One from settings for `disableVisualSelection` up to Handsontable 0.36/Handsontable Pro 1.16.0.
        }
        const disabledCellSelection = this.options.disabledCellSelection;
        if (typeof disabledCellSelection !== 'function') {
            return true;
        }
        // coords.row and coords.col are always set when isEnabledFor is called on an active selection.
        let disableHighlight = disabledCellSelection(coords.row, coords.col);
        if (typeof disableHighlight === 'string') {
            disableHighlight = [
                disableHighlight
            ];
        }
        return !disableHighlight || Array.isArray(disableHighlight) && !disableHighlight.includes(type);
    }
    /**
   * Set a new layer level to make access to the desire `area` and `header` highlights.
   *
   * @param {number} [level=0] Layer level to use.
   * @returns {Highlight}
   */ useLayerLevel(level = 0) {
        this.layerLevel = level;
        return this;
    }
    /**
   * Get Walkontable Selection instance created for controlling highlight of the currently
   * focused cell (or header).
   *
   * @returns {Selection}
   */ getFocus() {
        return this.focus;
    }
    /**
   * Get Walkontable Selection instance created for controlling highlight of the autofill functionality.
   *
   * @returns {Selection}
   */ getFill() {
        return this.fill;
    }
    /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * `area` highlights.
   *
   * @returns {Selection}
   */ createLayeredArea() {
        return _class_private_method_get(this, _createHighlight, createHighlight).call(this, this.layeredAreas, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$areaLayered$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"]);
    }
    /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the cells.
   *
   * @returns {Selection[]}
   */ getLayeredAreas() {
        return [
            ...this.layeredAreas.values()
        ];
    }
    /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * `highlight` highlights.
   *
   * @returns {Selection}
   */ createArea() {
        return _class_private_method_get(this, _createHighlight, createHighlight).call(this, this.areas, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$area$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"]);
    }
    /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the cells.
   *
   * @returns {Selection[]}
   */ getAreas() {
        return [
            ...this.areas.values()
        ];
    }
    /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * header highlight for rows.
   *
   * @returns {Selection}
   */ createRowHeader() {
        return _class_private_method_get(this, _createHighlight, createHighlight).call(this, this.rowHeaders, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$header$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"]);
    }
    /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the headers.
   *
   * @returns {Selection[]}
   */ getRowHeaders() {
        return [
            ...this.rowHeaders.values()
        ];
    }
    /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * header highlight for columns.
   *
   * @returns {Selection}
   */ createColumnHeader() {
        return _class_private_method_get(this, _createHighlight, createHighlight).call(this, this.columnHeaders, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$header$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"]);
    }
    /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the headers.
   *
   * @returns {Selection[]}
   */ getColumnHeaders() {
        return [
            ...this.columnHeaders.values()
        ];
    }
    /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight for active row headers.
   *
   * @returns {Selection}
   */ createActiveRowHeader() {
        return _class_private_method_get(this, _createHighlight, createHighlight).call(this, this.activeRowHeaders, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$activeHeader$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"]);
    }
    /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the active headers.
   *
   * @returns {Selection[]}
   */ getActiveRowHeaders() {
        return [
            ...this.activeRowHeaders.values()
        ];
    }
    /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight for active column headers.
   *
   * @returns {Selection}
   */ createActiveColumnHeader() {
        return _class_private_method_get(this, _createHighlight, createHighlight).call(this, this.activeColumnHeaders, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$activeHeader$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"]);
    }
    /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the active headers.
   *
   * @returns {Selection[]}
   */ getActiveColumnHeaders() {
        return [
            ...this.activeColumnHeaders.values()
        ];
    }
    /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight for the headers corner.
   *
   * @returns {Selection}
   */ createActiveCornerHeader() {
        return _class_private_method_get(this, _createHighlight, createHighlight).call(this, this.activeCornerHeaders, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$activeHeader$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"]);
    }
    /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the headers corner.
   *
   * @returns {Selection[]}
   */ getActiveCornerHeaders() {
        return [
            ...this.activeCornerHeaders.values()
        ];
    }
    /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight cells in a row.
   *
   * @returns {Selection}
   */ createRowHighlight() {
        return _class_private_method_get(this, _createHighlight, createHighlight).call(this, this.rowHighlights, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$row$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"]);
    }
    /**
   * Get all Walkontable Selection instances which describes the state of the rows highlighting.
   *
   * @returns {Selection[]}
   */ getRowHighlights() {
        return [
            ...this.rowHighlights.values()
        ];
    }
    /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight cells in a column.
   *
   * @returns {Selection}
   */ createColumnHighlight() {
        return _class_private_method_get(this, _createHighlight, createHighlight).call(this, this.columnHighlights, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$column$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"]);
    }
    /**
   * Get all Walkontable Selection instances which describes the state of the columns highlighting.
   *
   * @returns {Selection[]}
   */ getColumnHighlights() {
        return [
            ...this.columnHighlights.values()
        ];
    }
    /**
   * Get Walkontable Selection instance created for controlling highlight of the custom selection functionality.
   *
   * @returns {Selection}
   */ getCustomSelections() {
        return [
            ...this.customSelections.values()
        ];
    }
    /**
   * Add selection to the custom selection instance. The new selection are added to the end of the selection collection.
   *
   * @param {object} selectionInstance The selection instance.
   */ addCustomSelection(selectionInstance) {
        this.customSelections.push((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$customSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"])({
            ...this.options,
            ...selectionInstance
        }));
    }
    /**
   * Updates the CSS class names used for row, column, header, and active header highlights.
   * Necessary when the related settings change at runtime (e.g. via `updateSettings()`).
   *
   * @param {object} options Options with updated class names.
   * @param {string} [options.rowClassName] The new class name for row highlights.
   * @param {string} [options.columnClassName] The new class name for column highlights.
   * @param {string} [options.headerClassName] The new class name for header highlights.
   * @param {string} [options.activeHeaderClassName] The new class name for active header highlights.
   */ updateHighlightClassNames(options) {
        if ('rowClassName' in options) {
            this.options.rowClassName = options.rowClassName;
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(this.rowHighlights.values(), (h)=>{
                h.settings.className = options.rowClassName;
            });
        }
        if ('columnClassName' in options) {
            this.options.columnClassName = options.columnClassName;
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(this.columnHighlights.values(), (h)=>{
                h.settings.className = options.columnClassName;
            });
        }
        if ('headerClassName' in options) {
            this.options.headerClassName = options.headerClassName;
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(this.rowHeaders.values(), (h)=>{
                h.settings.className = options.headerClassName;
            });
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(this.columnHeaders.values(), (h)=>{
                h.settings.className = options.headerClassName;
            });
        }
        if ('activeHeaderClassName' in options) {
            this.options.activeHeaderClassName = options.activeHeaderClassName;
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(this.activeRowHeaders.values(), (h)=>{
                h.settings.className = options.activeHeaderClassName;
            });
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(this.activeColumnHeaders.values(), (h)=>{
                h.settings.className = options.activeHeaderClassName;
            });
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(this.activeCornerHeaders.values(), (h)=>{
                h.settings.className = options.activeHeaderClassName;
            });
        }
    }
    /**
   * Perform cleaning visual highlights for the whole table.
   */ clear() {
        this.focus.clear();
        this.fill.clear();
        this.areas.forEach((highlight)=>highlight.clear());
        this.layeredAreas.forEach((highlight)=>highlight.clear());
        this.rowHeaders.forEach((highlight)=>highlight.clear());
        this.columnHeaders.forEach((highlight)=>highlight.clear());
        this.activeRowHeaders.forEach((highlight)=>highlight.clear());
        this.activeColumnHeaders.forEach((highlight)=>highlight.clear());
        this.activeCornerHeaders.forEach((highlight)=>highlight.clear());
        this.rowHighlights.forEach((highlight)=>highlight.clear());
        this.columnHighlights.forEach((highlight)=>highlight.clear());
    }
    /**
   * This object can be iterate over using `for of` syntax or using internal `arrayEach` helper.
   *
   * @returns {Selection[]}
   */ [Symbol.iterator]() {
        return [
            this.focus,
            this.fill,
            ...this.areas.values(),
            ...this.layeredAreas.values(),
            ...this.rowHeaders.values(),
            ...this.columnHeaders.values(),
            ...this.activeRowHeaders.values(),
            ...this.activeColumnHeaders.values(),
            ...this.activeCornerHeaders.values(),
            ...this.rowHighlights.values(),
            ...this.columnHighlights.values(),
            ...this.customSelections
        ][Symbol.iterator]();
    }
    /**
   * Initializes the highlight manager with configuration options and creates the focus and fill highlight instances.
   */ constructor(options){
        _class_private_method_init(this, _createHighlight);
        /**
   * The property which describes which layer level of the visual selection will be modified.
   * This option is valid only for `area` and `header` highlight types which occurs multiple times on
   * the table (as a non-consecutive selection).
   *
   * An order of the layers is the same as the order of added new non-consecutive selections.
   *
   * @type {number}
   * @default 0
   */ this.layerLevel = 0;
        /**
   * Collection of the `area` highlights. That objects describes attributes for the borders and selection of
   * the multiple selected cells. It can occur multiple times on the table.
   *
   * @type {Map.<number, Selection>}
   */ this.layeredAreas = new Map();
        /**
   * Collection of the `highlight` highlights. That objects describes attributes for the borders and selection of
   * the multiple selected cells. It can occur multiple times on the table.
   *
   * @type {Map.<number, Selection>}
   */ this.areas = new Map();
        /**
   * Collection of the `header` highlights. That objects describes attributes for the selection of
   * the multiple selected rows in the table header. It can occur multiple times on the table.
   *
   * @type {Map.<number, Selection>}
   */ this.rowHeaders = new Map();
        /**
   * Collection of the `header` highlights. That objects describes attributes for the selection of
   * the multiple selected columns in the table header. It can occur multiple times on the table.
   *
   * @type {Map.<number, Selection>}
   */ this.columnHeaders = new Map();
        /**
   * Collection of the `active-header` highlights. That objects describes attributes for the selection of
   * the multiple selected rows in the table header. The table headers which have selected all items in
   * a row will be marked as `active-header`.
   *
   * @type {Map.<number, Selection>}
   */ this.activeRowHeaders = new Map();
        /**
   * Collection of the `active-header` highlights. That objects describes attributes for the selection of
   * the multiple selected columns in the table header. The table headers which have selected all items in
   * a row will be marked as `active-header`.
   *
   * @type {Map.<number, Selection>}
   */ this.activeColumnHeaders = new Map();
        /**
   * Collection of the `active-header` highlights. That objects describes attributes for the selection of
   * the selected corner in the table header. The table headers which have selected all items in
   * a row will be marked as `active-header`.
   *
   * @type {Map.<number, Selection>}
   */ this.activeCornerHeaders = new Map();
        /**
   * Collection of the `rows` highlights. That objects describes attributes for the selection of
   * the multiple selected cells in a row. It can occur multiple times on the table.
   *
   * @type {Map.<number, Selection>}
   */ this.rowHighlights = new Map();
        /**
   * Collection of the `columns` highlights. That objects describes attributes for the selection of
   * the multiple selected cells in a column. It can occur multiple times on the table.
   *
   * @type {Map.<number, Selection>}
   */ this.columnHighlights = new Map();
        /**
   * Collection of the `custom-selection`, holder for example borders added through CustomBorders plugin.
   *
   * @type {Selection[]}
   */ this.customSelections = [];
        this.options = options;
        this.focus = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$focus$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"])(options);
        this.fill = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$types$2f$fill$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createHighlight"])(options);
    }
}
function createHighlight(cacheMap, highlightFactory) {
    const layerLevel = this.layerLevel;
    if (cacheMap.has(layerLevel)) {
        return cacheMap.get(layerLevel);
    }
    const highlight = highlightFactory({
        layerLevel,
        ...this.options
    });
    cacheMap.set(layerLevel, highlight);
    return highlight;
}
var __TURBOPACK__default__export__ = Highlight;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/types/activeHeader.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createHighlight",
    ()=>createHighlight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__ACTIVE_HEADER_TYPE__as__HIGHLIGHT_ACTIVE_HEADER_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export ACTIVE_HEADER_TYPE as HIGHLIGHT_ACTIVE_HEADER_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)");
;
;
function createHighlight({ activeHeaderClassName, ...restOptions }) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]({
        className: activeHeaderClassName,
        ...restOptions,
        selectionType: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__ACTIVE_HEADER_TYPE__as__HIGHLIGHT_ACTIVE_HEADER_TYPE$3e$__["HIGHLIGHT_ACTIVE_HEADER_TYPE"]
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/types/area.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createHighlight",
    ()=>createHighlight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__AREA_TYPE__as__HIGHLIGHT_AREA_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export AREA_TYPE as HIGHLIGHT_AREA_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)");
;
;
function createHighlight({ ...restOptions }) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]({
        className: 'highlight',
        ...restOptions,
        selectionType: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__AREA_TYPE__as__HIGHLIGHT_AREA_TYPE$3e$__["HIGHLIGHT_AREA_TYPE"]
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/types/areaLayered.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createHighlight",
    ()=>createHighlight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__AREA_TYPE__as__HIGHLIGHT_AREA_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export AREA_TYPE as HIGHLIGHT_AREA_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)");
;
;
function createHighlight({ areaCornerVisible, ...restOptions }) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]({
        className: 'area',
        createLayers: true,
        border: {
            width: 1,
            color: '#4b89ff',
            cornerVisible: areaCornerVisible
        },
        ...restOptions,
        selectionType: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__AREA_TYPE__as__HIGHLIGHT_AREA_TYPE$3e$__["HIGHLIGHT_AREA_TYPE"]
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/types/column.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createHighlight",
    ()=>createHighlight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__COLUMN_TYPE__as__HIGHLIGHT_COLUMN_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export COLUMN_TYPE as HIGHLIGHT_COLUMN_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)");
;
;
function createHighlight({ columnClassName, ...restOptions }) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]({
        className: columnClassName,
        ...restOptions,
        selectionType: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__COLUMN_TYPE__as__HIGHLIGHT_COLUMN_TYPE$3e$__["HIGHLIGHT_COLUMN_TYPE"]
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/types/customSelection.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createHighlight",
    ()=>createHighlight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__CUSTOM_SELECTION_TYPE__as__HIGHLIGHT_CUSTOM_SELECTION_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export CUSTOM_SELECTION_TYPE as HIGHLIGHT_CUSTOM_SELECTION_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)");
;
;
function createHighlight({ border, visualCellRange, ...restOptions }) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]({
        ...border,
        ...restOptions,
        selectionType: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__CUSTOM_SELECTION_TYPE__as__HIGHLIGHT_CUSTOM_SELECTION_TYPE$3e$__["HIGHLIGHT_CUSTOM_SELECTION_TYPE"]
    }, visualCellRange);
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/types/fill.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createHighlight",
    ()=>createHighlight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FILL_TYPE__as__HIGHLIGHT_FILL_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FILL_TYPE as HIGHLIGHT_FILL_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)");
;
;
function createHighlight({ ...restOptions }) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]({
        className: 'fill',
        border: {
            width: 1,
            color: '#ff0000'
        },
        ...restOptions,
        selectionType: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FILL_TYPE__as__HIGHLIGHT_FILL_TYPE$3e$__["HIGHLIGHT_FILL_TYPE"]
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/types/focus.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createHighlight",
    ()=>createHighlight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FOCUS_TYPE__as__HIGHLIGHT_FOCUS_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FOCUS_TYPE as HIGHLIGHT_FOCUS_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
;
;
;
function createHighlight({ cellCornerVisible, ...restOptions }) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]({
        className: 'current',
        headerAttributes: [
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_SELECTED"])()
        ],
        border: {
            width: 2,
            color: '#4b89ff',
            cornerVisible: cellCornerVisible
        },
        ...restOptions,
        selectionType: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FOCUS_TYPE__as__HIGHLIGHT_FOCUS_TYPE$3e$__["HIGHLIGHT_FOCUS_TYPE"]
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/types/header.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createHighlight",
    ()=>createHighlight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__HEADER_TYPE__as__HIGHLIGHT_HEADER_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export HEADER_TYPE as HIGHLIGHT_HEADER_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)");
;
;
function createHighlight({ headerClassName, ...restOptions }) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]({
        className: headerClassName,
        ...restOptions,
        selectionType: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__HEADER_TYPE__as__HIGHLIGHT_HEADER_TYPE$3e$__["HIGHLIGHT_HEADER_TYPE"]
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/types/row.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createHighlight",
    ()=>createHighlight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__ROW_TYPE__as__HIGHLIGHT_ROW_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export ROW_TYPE as HIGHLIGHT_ROW_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)");
;
;
function createHighlight({ rowClassName, ...restOptions }) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$visualSelection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]({
        className: rowClassName,
        ...restOptions,
        selectionType: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__ROW_TYPE__as__HIGHLIGHT_ROW_TYPE$3e$__["HIGHLIGHT_ROW_TYPE"]
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/highlight/visualSelection.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$selection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__default__as__Selection$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/selection.mjs [client] (ecmascript) <export default as Selection>");
;
/**
 * Extends the Walkontable Selection class to operate on visual coordinates, translating them to
 * renderable indexes when committing the selection to the DOM.
 */ class VisualSelection extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$selection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__default__as__Selection$3e$__["Selection"] {
    /**
   * Adds a cell coords to the selection.
   *
   * @param {CellCoords} coords Visual coordinates of a cell.
   * @returns {VisualSelection}
   */ add(coords) {
        if (this.visualCellRange === null) {
            this.visualCellRange = this.settings.createCellRange(coords);
        } else {
            this.visualCellRange.expand(coords);
        }
        return this;
    }
    /**
   * Clears visual and renderable selection.
   *
   * @returns {VisualSelection}
   */ clear() {
        this.visualCellRange = null;
        return super.clear();
    }
    /**
   * Trims the passed cell range object by removing all coordinates that points to the hidden rows
   * or columns. The result is a new cell range object that points only to the visible indexes or `null`.
   *
   * @private
   * @param {CellRange} cellRange Cells range object to be trimmed.
   * @returns {CellRange} Visual non-hidden cells range coordinates.
   */ trimToVisibleCellsRangeOnly(cellRange) {
        const { from, to } = cellRange;
        const rowDirection = cellRange.getVerticalDirection() === 'N-S' ? 1 : -1;
        const columnDirection = cellRange.getInlineDirection() === 'start-end' ? 1 : -1;
        const visibleFromCoords = this.getNearestNotHiddenCoords(from, rowDirection, columnDirection);
        const visibleToCoords = this.getNearestNotHiddenCoords(to, -rowDirection, -columnDirection);
        if (visibleFromCoords === null || visibleToCoords === null) {
            return null;
        }
        // Check if the coords have crossed each other after finding nearest not hidden indexes
        const fromRow = visibleFromCoords.row ?? 0;
        const toRow = visibleToCoords.row ?? 0;
        const fromCol = visibleFromCoords.col ?? 0;
        const toCol = visibleToCoords.col ?? 0;
        const rowCrossed = rowDirection === 1 && fromRow > toRow || rowDirection === -1 && fromRow < toRow;
        const colCrossed = columnDirection === 1 && fromCol > toCol || columnDirection === -1 && fromCol < toCol;
        if (rowCrossed || colCrossed) {
            return null;
        }
        return this.settings.createCellRange(visibleFromCoords, visibleFromCoords, visibleToCoords);
    }
    /**
   * Gets nearest coordinates that points to the visible row and column indexes. If there are no visible
   * rows and/or columns the `null` value is returned.
   *
   * @private
   * @param {CellCoords} coords The coords object as starting point for finding the nearest visible coordinates.
   * @param {1|-1} rowSearchDirection The search direction. For value 1, it means searching from top to bottom for
   *                                  rows and from left to right for columns. For -1, it is the other way around.
   * @param {1|-1} columnSearchDirection The same as above but for rows.
   * @returns {CellCoords|null} Visual cell coordinates.
   */ getNearestNotHiddenCoords(coords, rowSearchDirection, columnSearchDirection = rowSearchDirection) {
        if (coords.row === null || coords.col === null) {
            return null;
        }
        const nextVisibleRow = this.getNearestNotHiddenIndex(this.settings.rowIndexMapper, coords.row, rowSearchDirection);
        // There are no more visual rows in the range.
        if (nextVisibleRow === null) {
            return null;
        }
        const nextVisibleColumn = this.getNearestNotHiddenIndex(this.settings.columnIndexMapper, coords.col, columnSearchDirection);
        // There are no more visual columns in the range.
        if (nextVisibleColumn === null) {
            return null;
        }
        return this.settings.createCellCoords(nextVisibleRow, nextVisibleColumn);
    }
    /**
   * Gets nearest visual index. If there are no visible rows or columns the `null` value is returned.
   *
   * @private
   * @param {IndexMapper} indexMapper The IndexMapper instance for specific axis.
   * @param {number} visualIndex The index as starting point for finding the nearest visible index.
   * @param {1|-1} searchDirection The search direction. For value 1, it means searching from top to bottom for
   *                               rows and from left to right for columns. For -1, it is the other way around.
   * @returns {number|null} Visual row/column index.
   */ getNearestNotHiddenIndex(indexMapper, visualIndex, searchDirection) {
        if (visualIndex < 0) {
            return visualIndex;
        }
        return indexMapper.getNearestNotHiddenIndex(visualIndex, searchDirection);
    }
    /**
   * Override internally stored visual indexes added by the Selection's `add` function. It should be executed
   * at the end of process of adding visual selection coordinates.
   *
   * @returns {VisualSelection}
   */ commit() {
        // There is no information about visual ranges, thus no selection may be displayed.
        if (this.visualCellRange === null) {
            return this;
        }
        const trimmedCellRange = this.trimToVisibleCellsRangeOnly(this.visualCellRange);
        // There is no visual start point (and also visual end point) in the range.
        if (trimmedCellRange === null) {
            this.cellRange = null;
        } else {
            this.cellRange = this.createRenderableCellRange(trimmedCellRange.from, trimmedCellRange.to);
        }
        return this;
    }
    /**
   * Some selection may be a part of broader cell range. This function sync coordinates of current selection
   * and the broader cell range when needed (current selection can't be presented visually).
   *
   * @param {CellRange} broaderCellRange Visual range. Actual cell range may be contained in the broader cell range.
   * When there is no way to represent some cell range visually we try to find range containing just the first visible cell.
   *
   * Warn: Please keep in mind that this function may change coordinates of the handled broader range.
   *
   * @returns {VisualSelection}
   */ syncWith(broaderCellRange) {
        const coordsFrom = broaderCellRange.from.clone().normalize();
        const rowDirection = broaderCellRange.getVerticalDirection() === 'N-S' ? 1 : -1;
        const columnDirection = broaderCellRange.getHorizontalDirection() === 'W-E' ? 1 : -1;
        const renderableHighlight = this.visualCellRange ? this.settings.visualToRenderableCoords(this.visualCellRange.highlight) : null;
        let cellCoordsVisual = null;
        if (renderableHighlight === null || renderableHighlight.col === null || renderableHighlight.row === null) {
            cellCoordsVisual = this.getNearestNotHiddenCoords(coordsFrom, rowDirection, columnDirection);
        }
        if (cellCoordsVisual !== null && broaderCellRange.includes(cellCoordsVisual)) {
            const currentHighlight = broaderCellRange.highlight.clone();
            if ((currentHighlight.row ?? -1) >= 0) {
                currentHighlight.row = cellCoordsVisual.row;
            }
            if ((currentHighlight.col ?? -1) >= 0) {
                currentHighlight.col = cellCoordsVisual.col;
            }
            // We can't show selection visually now, but we found first visible range in the broader cell range.
            if (this.cellRange === null) {
                const cellCoordsRenderable = this.settings.visualToRenderableCoords(currentHighlight);
                this.cellRange = this.settings.createCellRange(cellCoordsRenderable);
            }
            // TODO
            // We set new highlight as it might change (for example, when showing/hiding some cells from the broader selection range)
            // TODO: It is also handled by the `MergeCells` plugin while adjusting already modified coordinates. Should it?
            broaderCellRange.setHighlight(currentHighlight);
        }
        // TODO
        // Sync the highlight coords from the visual selection layer with logical coords.
        if (this.settings.selectionType === 'focus' && renderableHighlight !== null && cellCoordsVisual === null && this.visualCellRange !== null) {
            broaderCellRange.setHighlight(this.visualCellRange.highlight);
        }
        return this;
    }
    /**
   * Returns the top left (TL) and bottom right (BR) selection coordinates (renderable indexes).
   * The method overwrites the original method to support header selection for hidden cells.
   * To make the header selection working, the CellCoords and CellRange have to support not
   * complete coordinates (`null` values for example, `row: null`, `col: 2`).
   *
   * @returns {Array} Returns array of coordinates for example `[1, 1, 5, 5]`.
   */ getCorners() {
        const { from, to } = this.cellRange;
        return [
            Math.min(from.row ?? 0, to.row ?? 0),
            Math.min(from.col ?? 0, to.col ?? 0),
            Math.max(from.row ?? 0, to.row ?? 0),
            Math.max(from.col ?? 0, to.col ?? 0)
        ];
    }
    /**
   * Returns the top left (or top right in RTL) and bottom right (or bottom left in RTL) selection
   * coordinates (visual indexes).
   *
   * @returns {Array} Returns array of coordinates for example `[1, 1, 5, 5]`.
   */ getVisualCorners() {
        const cellRange = this.cellRange;
        const topStart = this.settings.renderableToVisualCoords(cellRange.getTopStartCorner());
        const bottomEnd = this.settings.renderableToVisualCoords(cellRange.getBottomEndCorner());
        return [
            topStart.row,
            topStart.col,
            bottomEnd.row,
            bottomEnd.col
        ];
    }
    /**
   * Creates a new CellRange object based on visual coordinates which before object creation are
   * translated to renderable indexes.
   *
   * @param {CellCoords} visualFromCoords The CellCoords object which contains coordinates that
   *                                      points to the beginning of the selection.
   * @param {CellCoords} visualToCoords The CellCoords object which contains coordinates that
   *                                    points to the end of the selection.
   * @returns {CellRange|null}
   */ createRenderableCellRange(visualFromCoords, visualToCoords) {
        const renderableFromCoords = this.settings.visualToRenderableCoords(visualFromCoords);
        const renderableToCoords = this.settings.visualToRenderableCoords(visualToCoords);
        if (renderableFromCoords.row === null || renderableFromCoords.col === null || renderableToCoords.row === null || renderableToCoords.col === null) {
            return null;
        }
        return this.settings.createCellRange(renderableFromCoords, renderableFromCoords, renderableToCoords);
    }
    /**
   * Initializes the visual selection with optional settings overrides and an initial visual cell range.
   */ constructor(settings, visualCellRange){
        super(settings, null), /**
   * Range of selection visually. Visual representation may have representation in a rendered selection.
   *
   * @type {null|CellRange}
   */ this.visualCellRange = null;
        this.visualCellRange = visualCellRange || null;
        this.commit();
    }
}
var __TURBOPACK__default__export__ = VisualSelection;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/mouseEventHandler.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "handleMouseEvent",
    ()=>handleMouseEvent,
    "mouseDown",
    ()=>mouseDown,
    "mouseOver",
    ()=>mouseOver,
    "mouseUp",
    ()=>mouseUp
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$event$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/dom/event.mjs [client] (ecmascript)");
;
function mouseDown({ isShiftKey, isLeftClick, isRightClick, coords, selection, controller, cellCoordsFactory }) {
    const sel = selection;
    const currentSelection = sel.isSelected() ? sel.getSelectedRange().current() : null;
    const selectedCorner = sel.isSelectedByCorner();
    const selectedRow = sel.isSelectedByRowHeader();
    sel.markSource('mouse');
    const coordsRow = coords.row ?? -1;
    const coordsCol = coords.col ?? -1;
    if (isShiftKey && currentSelection) {
        if (coordsRow >= 0 && coordsCol >= 0 && !controller.cell) {
            sel.setRangeEnd(coords);
        } else if (selectedCorner && coordsRow < 0 && !controller.column) {
            sel.setRangeEnd(cellCoordsFactory(currentSelection.to.row ?? 0, coordsCol));
        } else if (selectedRow && coordsCol < 0 && !controller.row) {
            sel.setRangeEnd(cellCoordsFactory(coordsRow, currentSelection.to.col ?? 0));
        } else if ((!selectedCorner && !selectedRow && coordsCol < 0 || selectedCorner && coordsCol < 0) && !controller.row) {
            sel.selectRows(Math.max(currentSelection.from.row ?? 0, 0), coordsRow, coordsCol);
        } else if ((!selectedCorner && !selectedRow && coordsRow < 0 || selectedRow && coordsRow < 0) && !controller.column) {
            sel.selectColumns(Math.max(currentSelection.from.col ?? 0, 0), coordsCol, coordsRow);
        }
    } else {
        const allowRightClickSelection = !sel.inInSelection(coords);
        const performSelection = isLeftClick || isRightClick && allowRightClickSelection;
        // clicked row header and when some column was selected
        if (coordsRow < 0 && coordsCol >= 0 && !controller.column) {
            if (performSelection) {
                sel.selectColumns(coordsCol, coordsCol, coordsRow);
            }
        // clicked column header and when some row was selected
        } else if (coordsCol < 0 && coordsRow >= 0 && !controller.row) {
            if (performSelection) {
                sel.selectRows(coordsRow, coordsRow, coordsCol);
            }
        } else if (coordsCol >= 0 && coordsRow >= 0 && !controller.cell) {
            if (performSelection) {
                sel.setRangeStart(coords);
            }
        } else if (coordsCol < 0 && coordsRow < 0) {
            sel.selectAll(true, true, {
                disableHeadersHighlight: true,
                focusPosition: {
                    row: 0,
                    col: 0
                }
            });
        }
    }
    sel.markEndSource();
}
function mouseOver({ isLeftClick, coords, selection, controller, cellCoordsFactory }) {
    if (!isLeftClick) {
        return;
    }
    const sel = selection;
    const selectedRow = sel.isSelectedByRowHeader();
    const selectedColumn = sel.isSelectedByColumnHeader();
    const countCols = sel.tableProps.countCols();
    const countRows = sel.tableProps.countRows();
    sel.markSource('mouse');
    if (selectedColumn && !controller.column) {
        sel.setRangeEnd(cellCoordsFactory(countRows - 1, coords.col ?? 0));
    } else if (selectedRow && !controller.row) {
        sel.setRangeEnd(cellCoordsFactory(coords.row ?? 0, countCols - 1));
    } else if (!controller.cell) {
        sel.setRangeEnd(coords);
    }
    sel.markEndSource();
}
function mouseUp({ isLeftClick, selection, cellRangeMapper }) {
    const sel = selection;
    const dvs = sel.settings.disableVisualSelection;
    // Treat empty string and empty array as equivalent to `false` (nothing is actually disabled),
    // matching how the rest of the codebase normalizes this setting.
    const visualSelectionDisabled = dvs === true || typeof dvs === 'string' && dvs.length > 0 || Array.isArray(dvs) && dvs.length > 0;
    // The dedup-on-second-click behavior relies on visible selection feedback; without it,
    // toggling invisible layers produces highlight jumps once any visible range repaints.
    if (!isLeftClick || sel.settings.selectionMode !== 'multiple' || visualSelectionDisabled) {
        return;
    }
    const selectionRange = sel.getSelectedRange();
    const renderableRange = selectionRange.clone().map((range)=>cellRangeMapper.toRenderable(range));
    const lastRenderableRange = renderableRange.current();
    if (lastRenderableRange && renderableRange.size() > 1 && !lastRenderableRange.isHeader() && !sel.isMultiple(lastRenderableRange)) {
        const ranges = renderableRange.findAll(lastRenderableRange);
        // Mark the selection source as 'deselect' so `afterSetRangeEnd` in core.js skips the
        // viewport scroll for this dedup-driven refresh. Other side effects (`closeEditor`,
        // per-range `render` + `prepareEditor`, and the final batched render in
        // `afterSelectionFinished`) are guarded against the 'deselect' source in the same way
        // the existing 'refresh' source is handled - so a single batched render still happens
        // once the refresh completes.
        if (ranges.length === renderableRange.size()) {
            // if the last selection range is the same as the first one (case when the single cell
            // is selected twice or more) remove duplicate ranges
            selection.markSource('deselect');
            selectionRange.pop();
            selection.refresh();
            selection.markEndSource();
        } else if (ranges.length > 1) {
            selection.markSource('deselect');
            selectionRange.removeLayers(ranges.map(({ layer })=>layer));
            selection.refresh();
            selection.markEndSource();
        }
    }
}
const handlers = new Map([
    [
        'touchstart',
        mouseDown
    ],
    [
        'touchend',
        mouseUp
    ],
    [
        'mousedown',
        mouseDown
    ],
    [
        'mouseover',
        mouseOver
    ],
    [
        'mousemove',
        mouseOver
    ],
    [
        'mouseup',
        mouseUp
    ]
]);
function handleMouseEvent(event, options) {
    const handler = handlers.get(event.type);
    if (handler) {
        handler({
            isShiftKey: event.shiftKey,
            isLeftClick: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$event$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isLeftClick"])(event) || event.type === 'touchstart',
            isRightClick: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$dom$2f$event$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isRightClick"])(event),
            ...options
        });
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/range.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
/**
 * The SelectionRange class is a simple CellRanges collection designed for easy manipulation of the multiple
 * consecutive and non-consecutive selections.
 *
 * @class SelectionRange
 * @util
 */ class SelectionRange {
    /**
   * Check if selected range is empty.
   *
   * @returns {boolean}
   */ isEmpty() {
        return this.size() === 0;
    }
    /**
   * Set coordinates to the class instance. It clears all previously added coordinates and push `coords`
   * to the collection.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @returns {SelectionRange}
   */ set(coords) {
        this.clear();
        this.ranges.push(this.createCellRange(coords));
        return this;
    }
    /**
   * Add coordinates to the class instance. The new coordinates are added to the end of the range collection.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @returns {SelectionRange}
   */ add(coords) {
        this.ranges.push(this.createCellRange(coords));
        return this;
    }
    /**
   * Pushes a new CellRange instance to the collection.
   *
   * @param {CellRange} cellRange The CellRange instance with defined visual coordinates.
   * @returns {SelectionRange}
   */ push(cellRange) {
        this.ranges.push(cellRange);
        return this;
    }
    /**
   * Removes from the stack the last added coordinates.
   *
   * @returns {CellRange}
   */ pop() {
        return this.ranges.pop();
    }
    /**
   * Get last added coordinates from ranges, it returns a CellRange instance.
   *
   * @returns {CellRange|undefined}
   */ current() {
        return this.peekByIndex(this.size() - 1);
    }
    /**
   * Get previously added coordinates from ranges, it returns a CellRange instance.
   *
   * @returns {CellRange|undefined}
   */ previous() {
        return this.peekByIndex(this.size() - 2);
    }
    /**
   * Returns `true` if coords is within any selection coords. This method iterates through
   * all selection layers to check if the coords object is within selection range.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @param {function(CellRange, number): boolean} [criteria] The function that allows injecting custom criteria.
   * @returns {boolean}
   */ includes(coords, criteria = ()=>true) {
        return this.ranges.some((cellRange, index)=>cellRange.includes(coords) && criteria(cellRange, index));
    }
    /**
   * Find all ranges that are equal to the provided range.
   *
   * @param {CellRange} cellRange The CellRange instance with defined visual coordinates.
   * @returns {Array<{range: CellRange, layer: number}>}
   */ findAll(cellRange) {
        const result = [];
        this.ranges.forEach((range, layer)=>{
            if (range.isEqual(cellRange)) {
                result.push({
                    range,
                    layer
                });
            }
        });
        return result;
    }
    /**
   * Removes all ranges that are equal to the provided ranges.
   *
   * @param {CellRange[]} cellRanges The array of CellRange instances with defined visual coordinates.
   * @returns {SelectionRange}
   */ remove(cellRanges) {
        this.ranges = this.ranges.filter((range)=>!cellRanges.some((cellRange)=>cellRange.isEqual(range)));
        return this;
    }
    /**
   * Removes the ranges based on the provided index layers (0 no N).
   *
   * @param {number[]} layerIndexes The array of indexes that will be removed from the selection.
   * @returns {SelectionRange}
   */ removeLayers(layerIndexes) {
        this.ranges = this.ranges.filter((_, index)=>!layerIndexes.includes(index));
        return this;
    }
    /**
   * Clear collection.
   *
   * @returns {SelectionRange}
   */ clear() {
        this.ranges.length = 0;
        return this;
    }
    /**
   * Get count of added all coordinates added to the selection.
   *
   * @returns {number}
   */ size() {
        return this.ranges.length;
    }
    /**
   * Creates a clone of this class.
   *
   * @returns {SelectionRange}
   */ clone() {
        const clone = new SelectionRange(this.createCellRange);
        clone.ranges = this.ranges.map((cellRange)=>cellRange.clone());
        return clone;
    }
    /**
   * Allows applying custom index translations for any range within the class instance.
   *
   * @param {function(CellRange): CellRange} mapFunction The function that allows injecting custom index translation logic.
   * @returns {SelectionRange}
   */ map(mapFunction) {
        this.ranges = this.ranges.map((cellRange, index)=>mapFunction(cellRange, index));
        return this;
    }
    /**
   * Peek the coordinates based on the index where that coordinate resides in the collection.
   *
   * @param {number} [index=0] An index where the coordinate will be retrieved from. The index '0' gets the
   * latest range.
   * @returns {CellRange|undefined}
   */ peekByIndex(index = 0) {
        let cellRange;
        if (index >= 0 && index < this.size()) {
            cellRange = this.ranges[index];
        }
        return cellRange;
    }
    /**
   * Returns an iterator over all CellRange entries stored in the collection, enabling use in for-of loops.
   */ [Symbol.iterator]() {
        return this.ranges[Symbol.iterator]();
    }
    /**
   * Initializes the collection with a factory function used to create new CellRange instances.
   */ constructor(createCellRange){
        /**
   * List of all CellRanges added to the class instance.
   *
   * @type {CellRange[]}
   */ this.ranges = [];
        this.createCellRange = createCellRange;
    }
}
var __TURBOPACK__default__export__ = SelectionRange;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/selection.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$highlight$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/highlight/highlight.mjs [client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__AREA_TYPE__as__HIGHLIGHT_AREA_TYPE$3e$__$3c$export__HIGHLIGHT_AREA_TYPE__as__AREA_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export AREA_TYPE as HIGHLIGHT_AREA_TYPE> <export HIGHLIGHT_AREA_TYPE as AREA_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__HEADER_TYPE__as__HIGHLIGHT_HEADER_TYPE$3e$__$3c$export__HIGHLIGHT_HEADER_TYPE__as__HEADER_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export HEADER_TYPE as HIGHLIGHT_HEADER_TYPE> <export HIGHLIGHT_HEADER_TYPE as HEADER_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FOCUS_TYPE__as__HIGHLIGHT_FOCUS_TYPE$3e$__$3c$export__HIGHLIGHT_FOCUS_TYPE__as__FOCUS_TYPE$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/selection/constants.mjs [client] (ecmascript) <export FOCUS_TYPE as HIGHLIGHT_FOCUS_TYPE> <export HIGHLIGHT_FOCUS_TYPE as FOCUS_TYPE>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$range$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/range.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/number.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$mixins$2f$localHooks$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/mixins/localHooks.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$transformation$2f$extender$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/transformation/extender.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$transformation$2f$focus$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/transformation/focus.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/utils.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/templateLiteralTag.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/a11y.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
;
;
;
;
;
;
;
;
/**
 * Checks if the value is a focus-position object with integer `row` and `col` fields.
 *
 * @param {unknown} value The value to check.
 * @returns {boolean}
 */ function isFocusPositionObject(value) {
    return typeof value === 'object' && value !== null && Number.isInteger(value.row) && Number.isInteger(value.col);
}
/**
 * Snaps a shifted index to the nearest non-hidden index. Used after a selection shift so a
 * single-line selection does not land on a hidden (non-rendered) row or column. Snapping is
 * limited to single-line selections - a wider range legitimately keeps hidden indexes within its
 * bounds (they stay part of copy/fill ranges), so its corners are returned unchanged. Headers
 * (negative indexes) are returned unchanged as well.
 *
 * @param {IndexMapper} indexMapper The row or column index mapper to query for visibility.
 * @param {number} index The shifted visual index to snap.
 * @param {boolean} isSingleLine Whether the selection spans a single row/column on this axis.
 * @returns {number}
 */ function snapToNearestVisible(indexMapper, index, isSingleLine) {
    if (!isSingleLine || index < 0) {
        return index;
    }
    const nearestIndex = indexMapper.getNearestNotHiddenIndex(index, 1, true);
    return nearestIndex === null ? index : nearestIndex;
}
var _extenderTransformation = /*#__PURE__*/ new WeakMap(), _focusTransformation = /*#__PURE__*/ new WeakMap(), /**
   * The flag which determines if the focus selection was changed.
   *
   * @type {boolean}
   */ _isFocusSelectionChanged = /*#__PURE__*/ new WeakMap(), /**
   * When sets disable highlighting the headers even when the logical coordinates points on them.
   *
   * @type {boolean}
   */ _disableHeadersHighlight = /*#__PURE__*/ new WeakMap(), /**
   * The source of the selection. It can be one of the following values: `mouse`, `unknown` or any other string.
   *
   * @type {'mouse' | 'unknown' | string}
   */ _selectionSource = /*#__PURE__*/ new WeakMap(), /**
   * The number of expected layers. It is used mostly to track when the last selection layer of non-contiguous
   * selection is applied, thus the viewport scroll is triggered.
   *
   * @param {number}
   */ _expectedLayersCount = /*#__PURE__*/ new WeakMap(), /**
   * The index of the active range layer. Active range layer is the layer that has visible focus highlight.
   * Focus highlight may jump between selection range layers.
   *
   * @type {number}
   */ _activeSelectionLayer = /*#__PURE__*/ new WeakMap(), /**
   * Applies header-type highlights for the given cell range.
   *
   * @param {CellRange} cellRange The cell range to highlight.
   * @param {number} countRows The total number of rows.
   * @param {number} countCols The total number of columns.
   * @param {object | null | undefined} rowHeaderHighlight The row header highlight instance.
   * @param {object | null | undefined} columnHeaderHighlight The column header highlight instance.
   * @param {object | null | undefined} activeRowHeaderHighlight The active row header highlight instance.
   * @param {object | null | undefined} activeColumnHeaderHighlight The active column header highlight instance.
   * @param {object | null | undefined} activeCornerHeaderHighlight The active corner header highlight instance.
   * @param {object | null | undefined} rowHighlight The row highlight instance.
   * @param {object | null | undefined} columnHighlight The column highlight instance.
   */ _applyHeaderHighlights = /*#__PURE__*/ new WeakSet();
/**
 * @typedef {object} SelectionState
 * @property {CellRange[]} ranges The array of all ranges.
 * @property {CellRange} activeRange The active range.
 * @property {number} activeSelectionLayer The active selection layer.
 * @property {number[]} selectedByRowHeader The state of the selected row headers.
 * @property {number[]} selectedByColumnHeader The state of the selected column headers.
 * @property {boolean} disableHeadersHighlight The state of the disable headers highlight.
 */ /**
 * @class Selection
 * @util
 */ class Selection {
    /**
   * Updates the CSS class names used for row, column, header, and active header highlights based on
   * the current settings. Call this after settings change via `updateSettings()`.
   */ updateHighlightClassNames() {
        this.highlight.updateHighlightClassNames({
            rowClassName: this.settings.currentRowClassName,
            columnClassName: this.settings.currentColClassName,
            headerClassName: this.settings.currentHeaderClassName,
            activeHeaderClassName: this.settings.activeHeaderClassName
        });
    }
    /**
   * Gets all selection range layers of the selection.
   *
   * @returns {SelectionRange}
   */ getSelectedRange() {
        return this.selectedRange;
    }
    /**
   * Gets the active selection range layer.
   *
   * @returns {CellRange}
   */ getActiveSelectedRange() {
        return this.selectedRange.peekByIndex(_class_private_field_get(this, _activeSelectionLayer));
    }
    /**
   * Gets the index of the active selection range layer.
   *
   * @returns {number}
   */ getActiveSelectionLayerIndex() {
        return _class_private_field_get(this, _activeSelectionLayer);
    }
    /**
   * Sets the index of the active selection range layer.
   *
   * @param {number} layerIndex The index of the active selection range layer.
   */ setActiveSelectionLayerIndex(layerIndex) {
        _class_private_field_set(this, _activeSelectionLayer, layerIndex);
    }
    /**
   * Marks the source of the selection. It can be one of the following values: `mouse`, or any other string.
   *
   * @param {'mouse' | 'unknown' | string} sourceName The source name.
   */ markSource(sourceName) {
        _class_private_field_set(this, _selectionSource, sourceName);
    }
    /**
   * Marks end of the selection source. It restores the selection source to default value which is 'unknown'.
   */ markEndSource() {
        _class_private_field_set(this, _selectionSource, 'unknown');
    }
    /**
   * Returns the source of the selection.
   *
   * @returns {'mouse' | 'unknown' | string}
   */ getSelectionSource() {
        return _class_private_field_get(this, _selectionSource);
    }
    /**
   * Set the number of expected layers. The method is not obligatory to call. It is used mostly internally
   * to determine when the last selection layer of non-contiguous is applied, thus the viewport scroll is triggered.
   *
   * @param {number} layersCount The number of expected layers.
   */ setExpectedLayers(layersCount) {
        _class_private_field_set(this, _expectedLayersCount, layersCount);
    }
    /**
   * Indicate that selection process began. It sets internally `.inProgress` property to `true`.
   */ begin() {
        this.inProgress = true;
    }
    /**
   * Indicate that selection process finished. It sets internally `.inProgress` property to `false`.
   */ finish() {
        this.runLocalHooks('afterSelectionFinished', Array.from(this.selectedRange));
        this.inProgress = false;
        _class_private_field_set(this, _expectedLayersCount, -1);
    }
    /**
   * Check if the process of selecting the cell/cells is in progress.
   *
   * @returns {boolean}
   */ isInProgress() {
        return this.inProgress;
    }
    /**
   * Starts selection range on given coordinate object.
   *
   * @param {CellCoords} coords Visual coords.
   * @param {boolean} [multipleSelection] If `true`, selection will be worked in 'multiple' mode. This option works
   *                                      only when 'selectionMode' is set as 'multiple'. If the argument is not defined
   *                                      the default trigger will be used.
   * @param {boolean} [fragment=false] If `true`, the selection will be treated as a partial selection where the
   *                                   `setRangeEnd` method won't be called on every `setRangeStart` call.
   * @param {CellCoords} [highlightCoords] If set, allows changing the coordinates of the highlight/focus cell.
   */ setRangeStart(coords, multipleSelection, fragment = false, highlightCoords = coords) {
        const isMultipleMode = this.settings.selectionMode === 'multiple';
        const isMultipleSelection = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isUndefined"])(multipleSelection) ? this.tableProps.getShortcutManager().isCtrlPressed() : multipleSelection;
        // We are creating copy. We would like to modify just the start of the selection by below hook. Then original coords
        // should be handled by next methods.
        const coordsClone = coords.clone();
        _class_private_field_set(this, _disableHeadersHighlight, false);
        _class_private_field_set(this, _isFocusSelectionChanged, false);
        this.runLocalHooks(`beforeSetRangeStart${fragment ? 'Only' : ''}`, coordsClone);
        if (!isMultipleMode || isMultipleMode && !isMultipleSelection && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isUndefined"])(multipleSelection)) {
            this.selectedRange.clear();
            this.highlight.getAreas().forEach((highlight)=>highlight.clear());
            this.highlight.getLayeredAreas().forEach((highlight)=>highlight.clear());
            this.highlight.getRowHeaders().forEach((highlight)=>highlight.clear());
            this.highlight.getColumnHeaders().forEach((highlight)=>highlight.clear());
            this.highlight.getActiveRowHeaders().forEach((highlight)=>highlight.clear());
            this.highlight.getActiveColumnHeaders().forEach((highlight)=>highlight.clear());
            this.highlight.getActiveCornerHeaders().forEach((highlight)=>highlight.clear());
            this.highlight.getRowHighlights().forEach((highlight)=>highlight.clear());
            this.highlight.getColumnHighlights().forEach((highlight)=>highlight.clear());
        }
        this.selectedRange.add(coordsClone).current()?.setHighlight(highlightCoords.clone());
        if (this.getLayerLevel() === 0) {
            this.selectedByRowHeader.clear();
            this.selectedByColumnHeader.clear();
        }
        if (!fragment) {
            this.setRangeEnd(coords);
        }
    }
    /**
   * Starts selection range on given coordinate object.
   *
   * @param {CellCoords} coords Visual coords.
   * @param {boolean} [multipleSelection] If `true`, selection will be worked in 'multiple' mode. This option works
   *                                      only when 'selectionMode' is set as 'multiple'. If the argument is not defined
   *                                      the default trigger will be used.
   * @param {CellCoords} [highlightCoords] If set, allows changing the coordinates of the highlight/focus cell.
   */ setRangeStartOnly(coords, multipleSelection, highlightCoords = coords) {
        this.setRangeStart(coords, multipleSelection, true, highlightCoords);
    }
    /**
   * Ends selection range on given coordinate object.
   *
   * @param {CellCoords} coords Visual coords.
   * @param {number} [layerIndex] The layer index to set the end on. If not provided, the current layer level is used.
   */ setRangeEnd(coords, layerIndex = this.getLayerLevel()) {
        if (this.selectedRange.isEmpty()) {
            return;
        }
        this.setActiveSelectionLayerIndex(layerIndex);
        const coordsClone = coords.clone();
        const countRows = this.tableProps.countRows();
        const countCols = this.tableProps.countCols();
        const isSingle = this.getActiveSelectedRange()?.clone().setTo(coords).isSingleHeader();
        // Ignore processing the end range when the header selection starts overlapping the corner and
        // the selection is not a single header highlight.
        if ((countRows > 0 || countCols > 0) && (countRows === 0 && (coordsClone.col ?? 0) < 0 && !isSingle || countCols === 0 && (coordsClone.row ?? 0) < 0 && !isSingle)) {
            return;
        }
        this.runLocalHooks('beforeSetRangeEnd', coordsClone);
        this.begin();
        const cellRange = this.getActiveSelectedRange();
        if (!cellRange) {
            return;
        }
        if (!this.settings.navigableHeaders) {
            cellRange.highlight.normalize();
        }
        if (this.settings.selectionMode === 'single') {
            cellRange.setFrom(cellRange.highlight);
            cellRange.setTo(cellRange.highlight);
        } else {
            const horizontalDir = cellRange.getHorizontalDirection();
            const verticalDir = cellRange.getVerticalDirection();
            const isMultiple = this.isMultiple();
            cellRange.setTo(coordsClone);
            if (isMultiple && (horizontalDir !== cellRange.getHorizontalDirection() || cellRange.getWidth() === 1 && !cellRange.includes(cellRange.highlight))) {
                cellRange.from.assign({
                    col: cellRange.highlight.col ?? undefined
                });
            }
            if (isMultiple && (verticalDir !== cellRange.getVerticalDirection() || cellRange.getHeight() === 1 && !cellRange.includes(cellRange.highlight))) {
                cellRange.from.assign({
                    row: cellRange.highlight.row ?? undefined
                });
            }
        }
        // Prevent creating "area" selection that overlaps headers.
        if (countRows > 0 && countCols > 0) {
            if (!this.settings.navigableHeaders || this.settings.navigableHeaders && !cellRange.isSingleHeader()) {
                cellRange.to.normalize();
            }
        }
        this.runLocalHooks('beforeHighlightSet');
        this.setRangeFocus(this.getActiveSelectedRange()?.highlight ?? cellRange.highlight, layerIndex);
        this.applyAndCommit(this.getActiveSelectedRange() ?? cellRange, layerIndex);
        const isLastLayer = _class_private_field_get(this, _expectedLayersCount) === -1 || this.selectedRange.size() === _class_private_field_get(this, _expectedLayersCount);
        this.runLocalHooks('afterSetRangeEnd', coords, isLastLayer);
    }
    /**
   * Applies and commits the selection to all layers (using the Walkontable Selection API) based on the selection (CellRanges)
   * collected in the `selectedRange` module.
   *
   * @param {CellRange} [cellRange] The cell range to apply. If not provided, the current selection is used.
   * @param {number} [layerLevel] The layer level to apply. If not provided, the current layer level is used.
   */ applyAndCommit(cellRange = this.getActiveSelectedRange(), layerLevel = this.getLayerLevel()) {
        if (!cellRange) {
            return;
        }
        const countRows = this.tableProps.countRows();
        const countCols = this.tableProps.countCols();
        this.highlight.useLayerLevel(layerLevel);
        const areaHighlight = this.highlight.createArea();
        const layeredAreaHighlight = this.highlight.createLayeredArea();
        const rowHeaderHighlight = this.highlight.createRowHeader();
        const columnHeaderHighlight = this.highlight.createColumnHeader();
        const activeRowHeaderHighlight = this.highlight.createActiveRowHeader();
        const activeColumnHeaderHighlight = this.highlight.createActiveColumnHeader();
        const activeCornerHeaderHighlight = this.highlight.createActiveCornerHeader();
        const rowHighlight = this.highlight.createRowHighlight();
        const columnHighlight = this.highlight.createColumnHighlight();
        areaHighlight?.clear();
        layeredAreaHighlight?.clear();
        rowHeaderHighlight?.clear();
        columnHeaderHighlight?.clear();
        activeRowHeaderHighlight?.clear();
        activeColumnHeaderHighlight?.clear();
        activeCornerHeaderHighlight?.clear();
        rowHighlight?.clear();
        columnHighlight?.clear();
        if (this.highlight.isEnabledFor(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__AREA_TYPE__as__HIGHLIGHT_AREA_TYPE$3e$__$3c$export__HIGHLIGHT_AREA_TYPE__as__AREA_TYPE$3e$__["AREA_TYPE"], cellRange.highlight) && (this.isMultiple() || layerLevel >= 1)) {
            areaHighlight?.add(cellRange.from).add(cellRange.to).commit();
            layeredAreaHighlight?.add(cellRange.from).add(cellRange.to).commit();
            if (layerLevel === 1) {
                // For single cell selection in the same layer, we do not create area selection to prevent blue background.
                // When non-consecutive selection is performed we have to add that missing area selection to the previous layer
                // based on previous coordinates. It only occurs when the previous selection wasn't select multiple cells.
                const previousRange = this.selectedRange.peekByIndex(layerLevel - 1);
                if (previousRange) {
                    this.highlight.useLayerLevel(layerLevel - 1);
                    this.highlight.createArea()?.add(previousRange.from).commit().syncWith(previousRange);
                    this.highlight.createLayeredArea()?.add(previousRange.from).commit().syncWith(previousRange);
                    this.highlight.useLayerLevel(layerLevel);
                }
            }
        }
        if (this.highlight.isEnabledFor(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__HEADER_TYPE__as__HIGHLIGHT_HEADER_TYPE$3e$__$3c$export__HIGHLIGHT_HEADER_TYPE__as__HEADER_TYPE$3e$__["HEADER_TYPE"], cellRange.highlight)) {
            _class_private_method_get(this, _applyHeaderHighlights, applyHeaderHighlights).call(this, cellRange, countRows, countCols, rowHeaderHighlight, columnHeaderHighlight, activeRowHeaderHighlight, activeColumnHeaderHighlight, activeCornerHeaderHighlight, rowHighlight, columnHighlight);
        }
    }
    /**
   * Sets the selection focus position at the specified coordinates.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @param {number} [layerIndex] The layer index to set the focus on.
   */ setRangeFocus(coords, layerIndex = this.getLayerLevel()) {
        if (this.selectedRange.isEmpty()) {
            return;
        }
        this.setActiveSelectionLayerIndex(layerIndex);
        _class_private_field_get(this, _extenderTransformation).setActiveLayerIndex(layerIndex);
        _class_private_field_get(this, _focusTransformation).setActiveLayerIndex(layerIndex);
        const cellRange = this.getActiveSelectedRange();
        if (!this.inProgress) {
            this.runLocalHooks('beforeSetFocus', coords);
        }
        if (!cellRange) {
            return;
        }
        const focusHighlight = this.highlight.getFocus();
        focusHighlight.clear();
        cellRange.setHighlight(coords);
        if (!this.inProgress) {
            this.runLocalHooks('beforeHighlightSet');
        }
        if (this.highlight.isEnabledFor(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FOCUS_TYPE__as__HIGHLIGHT_FOCUS_TYPE$3e$__$3c$export__HIGHLIGHT_FOCUS_TYPE__as__FOCUS_TYPE$3e$__["FOCUS_TYPE"], cellRange.highlight)) {
            focusHighlight.add(cellRange.highlight).commit().syncWith(cellRange);
        }
        if (!this.inProgress) {
            _class_private_field_set(this, _isFocusSelectionChanged, true);
            this.runLocalHooks('afterSetFocus', cellRange.highlight);
        }
    }
    /**
   * Selects cell relative to the current cell (if possible).
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   * @param {boolean} [createMissingRecords=false] If `true` the new rows/columns will be created if necessary.
   * Otherwise, row/column will be created according to `minSpareRows/minSpareCols` settings of Handsontable.
   */ transformStart(rowDelta, colDelta, createMissingRecords = false) {
        if (!this.isSelected()) {
            return;
        }
        const { visualCoords } = _class_private_field_get(this, _extenderTransformation).transformStart(rowDelta, colDelta, createMissingRecords);
        this.setRangeStart(visualCoords, undefined, false, visualCoords);
    }
    /**
   * Sets selection end cell relative to the current selection end cell (if possible).
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   */ transformEnd(rowDelta, colDelta) {
        if (!this.isSelected()) {
            return;
        }
        const { visualCoords, selectionLayer } = _class_private_field_get(this, _extenderTransformation).transformEnd(rowDelta, colDelta);
        this.setRangeEnd(visualCoords, selectionLayer);
    }
    /**
   * Transforms the focus cell selection relative to the current focus position.
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   */ transformFocus(rowDelta, colDelta) {
        if (!this.isSelected()) {
            return;
        }
        const { selectionLayer, visualCoords } = _class_private_field_get(this, _focusTransformation).transformStart(rowDelta, colDelta);
        this.setRangeFocus(visualCoords.normalize(), selectionLayer);
    }
    /**
   * Transforms the last selection layer down or up by the index count.
   *
   * @param {number} visualRowIndex Visual row index from which the selection will be shifted.
   * @param {number} amount The number of rows to shift the selection.
   */ shiftRows(visualRowIndex, amount) {
        if (!this.isSelected()) {
            return;
        }
        const range = this.getActiveSelectedRange();
        if (this.isSelectedByCorner()) {
            this.selectAll(true, true, {
                disableHeadersHighlight: true
            });
        } else if (range && (this.isSelectedByColumnHeader() || (range.getOuterTopStartCorner().row ?? 0) >= visualRowIndex)) {
            const { from, to, highlight } = range;
            const countRows = this.tableProps.countRows();
            const isSelectedByRowHeader = this.isSelectedByRowHeader();
            const isSelectedByColumnHeader = this.isSelectedByColumnHeader();
            const minRow = isSelectedByColumnHeader ? -1 : 0;
            const coordsStartAmount = isSelectedByColumnHeader ? 0 : amount;
            // After shifting, a single-row selection can land on a row that is hidden (e.g. removing a
            // row next to a hidden one), leaving the highlight on a non-rendered row. Snap it to the
            // nearest visible row (see snapToNearestVisible for the single-line scoping rationale).
            const isSingleRow = from.row === to.row;
            const clampToVisibleRow = (visualRow)=>snapToNearestVisible(this.tableProps.rowIndexMapper, visualRow, isSingleRow);
            // Remove from the stack the last added selection as that selection below will be
            // replaced by new transformed selection.
            this.getSelectedRange().pop();
            const coordsStart = this.tableProps.createCellCoords(clampToVisibleRow((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])((from.row ?? 0) + coordsStartAmount, minRow, countRows - 1)), from.col ?? 0);
            const coordsEnd = this.tableProps.createCellCoords(clampToVisibleRow((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])((to.row ?? 0) + amount, minRow, countRows - 1)), to.col ?? 0);
            this.markSource('shift');
            if ((highlight.row ?? 0) >= visualRowIndex) {
                this.setRangeStartOnly(coordsStart, true, this.tableProps.createCellCoords(clampToVisibleRow((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])((highlight.row ?? 0) + amount, 0, countRows - 1)), highlight.col ?? 0));
            } else {
                this.setRangeStartOnly(coordsStart, true);
            }
            if (isSelectedByRowHeader) {
                this.selectedByRowHeader.add(this.getLayerLevel());
            }
            if (isSelectedByColumnHeader) {
                this.selectedByColumnHeader.add(this.getLayerLevel());
            }
            this.setRangeEnd(coordsEnd);
            this.markEndSource();
        }
    }
    /**
   * Transforms the last selection layer left or right by the index count.
   *
   * @param {number} visualColumnIndex Visual column index from which the selection will be shifted.
   * @param {number} amount The number of columns to shift the selection.
   */ shiftColumns(visualColumnIndex, amount) {
        if (!this.isSelected()) {
            return;
        }
        const range = this.getActiveSelectedRange();
        if (this.isSelectedByCorner()) {
            this.selectAll(true, true, {
                disableHeadersHighlight: true
            });
        } else if (range && (this.isSelectedByRowHeader() || (range.getOuterTopStartCorner().col ?? 0) >= visualColumnIndex)) {
            const { from, to, highlight } = range;
            const countCols = this.tableProps.countCols();
            const isSelectedByRowHeader = this.isSelectedByRowHeader();
            const isSelectedByColumnHeader = this.isSelectedByColumnHeader();
            const minColumn = isSelectedByRowHeader ? -1 : 0;
            const coordsStartAmount = isSelectedByRowHeader ? 0 : amount;
            // After shifting, a single-column selection can land on a column that is hidden (e.g. removing
            // a column next to a hidden one), leaving the highlight on a non-rendered column. Snap it to
            // the nearest visible column (see snapToNearestVisible for the single-line scoping rationale).
            const isSingleColumn = from.col === to.col;
            const clampToVisibleColumn = (visualColumn)=>snapToNearestVisible(this.tableProps.columnIndexMapper, visualColumn, isSingleColumn);
            // Remove from the stack the last added selection as that selection below will be
            // replaced by new transformed selection.
            this.getSelectedRange().pop();
            const coordsStart = this.tableProps.createCellCoords(from.row ?? 0, clampToVisibleColumn((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])((from.col ?? 0) + coordsStartAmount, minColumn, countCols - 1)));
            const coordsEnd = this.tableProps.createCellCoords(to.row ?? 0, clampToVisibleColumn((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])((to.col ?? 0) + amount, minColumn, countCols - 1)));
            this.markSource('shift');
            if ((highlight.col ?? 0) >= visualColumnIndex) {
                this.setRangeStartOnly(coordsStart, true, this.tableProps.createCellCoords(highlight.row ?? 0, clampToVisibleColumn((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])((highlight.col ?? 0) + amount, 0, countCols - 1))));
            } else {
                this.setRangeStartOnly(coordsStart, true);
            }
            if (isSelectedByRowHeader) {
                this.selectedByRowHeader.add(this.getLayerLevel());
            }
            if (isSelectedByColumnHeader) {
                this.selectedByColumnHeader.add(this.getLayerLevel());
            }
            this.setRangeEnd(coordsEnd);
            this.markEndSource();
        }
    }
    /**
   * Returns currently used layer level.
   *
   * @returns {number} Returns layer level starting from 0. If no selection was added to the table -1 is returned.
   */ getLayerLevel() {
        return this.selectedRange.size() - 1;
    }
    /**
   * Returns `true` if currently there is a selection on the screen, `false` otherwise.
   *
   * @returns {boolean}
   */ isSelected() {
        return !this.selectedRange.isEmpty();
    }
    /**
   * Returns information if we have a multi-selection. This method check multi-selection only on the latest layer of
   * the selection.
   *
   * @param {CellRange} [cellRange] The cell range to check. If not provided, the latest selection layer is used.
   * @returns {boolean}
   */ isMultiple(cellRange = this.getActiveSelectedRange()) {
        if (!this.isSelected() || !cellRange) {
            return false;
        }
        const isMultipleListener = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createObjectPropListener"])(!cellRange.isSingle());
        this.runLocalHooks('afterIsMultipleSelection', isMultipleListener);
        return isMultipleListener.value;
    }
    /**
   * Checks if the last selection involves changing the focus cell position only.
   *
   * @returns {boolean}
   */ isFocusSelectionChanged() {
        return this.isSelected() && _class_private_field_get(this, _isFocusSelectionChanged);
    }
    /**
   * Returns `true` if the selection was applied by clicking to the row header. If the `layerLevel`
   * argument is passed then only that layer will be checked. Otherwise, it checks if any row header
   * was clicked on any selection layer level.
   *
   * @param {number} [layerLevel=this.getLayerLevel()] Selection layer level to check.
   * @returns {boolean}
   */ isSelectedByRowHeader(layerLevel = this.getLayerLevel()) {
        return !this.isSelectedByCorner() && (layerLevel === -1 ? this.selectedByRowHeader.size > 0 : this.selectedByRowHeader.has(layerLevel));
    }
    /**
   * Returns `true` if the selection consists of entire rows (including their headers). If the `layerLevel`
   * argument is passed then only that layer will be checked. Otherwise, it checks the selection for all layers.
   *
   * @param {number} [layerLevel=this.getLayerLevel()] Selection layer level to check.
   * @returns {boolean}
   */ isEntireRowSelected(layerLevel = this.getLayerLevel()) {
        const tester = (range)=>{
            const { col } = range.getOuterTopStartCorner();
            const rowHeaders = this.tableProps.countRowHeaders();
            const countCols = this.tableProps.countCols();
            return (rowHeaders > 0 && (col ?? 0) < 0 || rowHeaders === 0) && range.getWidth() === countCols;
        };
        if (layerLevel === -1) {
            return Array.from(this.selectedRange).some((range)=>tester(range));
        }
        const range = this.selectedRange.peekByIndex(layerLevel);
        return range ? tester(range) : false;
    }
    /**
   * Returns `true` if the selection was applied by clicking to the column header. If the `layerLevel`
   * argument is passed then only that layer will be checked. Otherwise, it checks if any column header
   * was clicked on any selection layer level.
   *
   * @param {number} [layerLevel=this.getLayerLevel()] Selection layer level to check.
   * @returns {boolean}
   */ isSelectedByColumnHeader(layerLevel = this.getLayerLevel()) {
        return !this.isSelectedByCorner() && (layerLevel === -1 ? this.selectedByColumnHeader.size > 0 : this.selectedByColumnHeader.has(layerLevel));
    }
    /**
   * Returns `true` if the selection consists of entire columns (including their headers). If the `layerLevel`
   * argument is passed then only that layer will be checked. Otherwise, it checks the selection for all layers.
   *
   * @param {number} [layerLevel=this.getLayerLevel()] Selection layer level to check.
   * @returns {boolean}
   */ isEntireColumnSelected(layerLevel = this.getLayerLevel()) {
        const tester = (range)=>{
            const { row } = range.getOuterTopStartCorner();
            const colHeaders = this.tableProps.countColHeaders();
            const countRows = this.tableProps.countRows();
            return (colHeaders > 0 && (row ?? 0) < 0 || colHeaders === 0) && range.getHeight() === countRows;
        };
        if (layerLevel === -1) {
            return Array.from(this.selectedRange).some((range)=>tester(range));
        }
        const range = this.selectedRange.peekByIndex(layerLevel);
        return range ? tester(range) : false;
    }
    /**
   * Returns `true` if the selection was applied by clicking on the row or column header on any layer level.
   *
   * @returns {boolean}
   */ isSelectedByAnyHeader() {
        return this.isSelectedByRowHeader(-1) || this.isSelectedByColumnHeader(-1) || this.isSelectedByCorner();
    }
    /**
   * Returns `true` if the selection was applied by clicking on the left-top corner overlay.
   *
   * @returns {boolean}
   */ isSelectedByCorner() {
        return this.selectedByColumnHeader.has(this.getLayerLevel()) && this.selectedByRowHeader.has(this.getLayerLevel());
    }
    /**
   * Returns `true` if coords is within selection coords. This method iterates through all selection layers to check if
   * the coords object is within selection range.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @returns {boolean}
   */ inInSelection(coords) {
        return this.selectedRange.includes(coords);
    }
    /**
   * Returns `true` if the cell corner should be visible.
   *
   * @private
   * @returns {boolean} `true` if the corner element has to be visible, `false` otherwise.
   */ isCellCornerVisible() {
        return this.settings.fillHandle && !this.tableProps.isEditorOpened() && !this.isMultiple();
    }
    /**
   * Returns `true` if the cell coordinates are visible (renderable).
   *
   * @private
   * @param {CellCoords} coords The cell coordinates to check.
   * @returns {boolean}
   */ isCellVisible(coords) {
        const renderableCoords = this.tableProps.visualToRenderableCoords(coords);
        return renderableCoords.row !== null && renderableCoords.col !== null;
    }
    /**
   * Returns `true` if the area corner should be visible.
   *
   * @param {number} layerLevel The layer level.
   * @returns {boolean} `true` if the corner element has to be visible, `false` otherwise.
   */ isAreaCornerVisible(layerLevel) {
        if (Number.isInteger(layerLevel) && layerLevel !== this.getLayerLevel()) {
            return false;
        }
        return this.settings.fillHandle && !this.tableProps.isEditorOpened() && this.isMultiple();
    }
    /**
   * Clear the selection by resetting the collected ranges and highlights.
   */ clear() {
        // TODO: collections selectedByColumnHeader and selectedByRowHeader should be clear too.
        this.selectedRange.clear();
        this.highlight.clear();
    }
    /**
   * Deselects all selected cells.
   */ deselect() {
        if (!this.isSelected()) {
            return;
        }
        this.inProgress = false;
        this.clear();
        this.runLocalHooks('afterDeselect');
    }
    /**
   * Selects all cells and headers.
   *
   * @param {boolean} [includeRowHeaders=false] `true` If the selection should include the row headers,
   * `false` otherwise.
   * @param {boolean} [includeColumnHeaders=false] `true` If the selection should include the column
   * headers, `false` otherwise.
   * @param {object} [options] Additional object with options.
   * @param {{row: number, col: number} | boolean} [options.focusPosition] The argument allows changing the cell/header
   * focus position. The value takes an object with a `row` and `col` properties from -N to N, where
   * negative values point to the headers and positive values point to the cell range. If `false`, the focus
   * position won't be changed.
   * @param {boolean} [options.disableHeadersHighlight] If `true`, disables highlighting the headers even when
   * the logical coordinates points on them.
   */ selectAll(includeRowHeaders = false, includeColumnHeaders = false, options = {
        focusPosition: false,
        disableHeadersHighlight: false
    }) {
        const nrOfRows = this.tableProps.countRows();
        const nrOfColumns = this.tableProps.countCols();
        const countRowHeaders = this.tableProps.countRowHeaders();
        const countColHeaders = this.tableProps.countColHeaders();
        const rowFrom = includeColumnHeaders ? -countColHeaders : 0;
        const columnFrom = includeRowHeaders ? -countRowHeaders : 0;
        // We can't select cells when there is no data.
        if (rowFrom === 0 && columnFrom === 0 && (nrOfRows === 0 || nrOfColumns === 0)) {
            return;
        }
        let highlight = this.getActiveSelectedRange()?.highlight;
        const { focusPosition, disableHeadersHighlight } = options;
        if (isFocusPositionObject(focusPosition)) {
            highlight = this.tableProps.createCellCoords((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(focusPosition.row, rowFrom, nrOfRows - 1), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(focusPosition.col, columnFrom, nrOfColumns - 1));
        }
        const startCoords = this.tableProps.createCellCoords(rowFrom, columnFrom);
        const endCoords = this.tableProps.createCellCoords(nrOfRows - 1, nrOfColumns - 1);
        this.clear();
        this.runLocalHooks('beforeSelectAll', startCoords, endCoords, highlight);
        this.setRangeStartOnly(startCoords, undefined, highlight);
        _class_private_field_set(this, _disableHeadersHighlight, disableHeadersHighlight ?? false);
        if (columnFrom < 0) {
            this.selectedByRowHeader.add(this.getLayerLevel());
        }
        if (rowFrom < 0) {
            this.selectedByColumnHeader.add(this.getLayerLevel());
        }
        this.setRangeEnd(endCoords);
        this.runLocalHooks('afterSelectAll', startCoords, endCoords, highlight);
        this.finish();
    }
    /**
   * Make multiple, non-contiguous selection specified by `row` and `column` values or a range of cells
   * finishing at `endRow`, `endColumn`. The method supports two input formats, first as an array of arrays such
   * as `[[rowStart, columnStart, rowEnd, columnEnd]]` and second format as an array of CellRange objects.
   * If the passed ranges have another format the exception will be thrown.
   *
   * @param {Array[]|CellRange[]} selectionRanges The coordinates which define what the cells should be selected.
   * @returns {boolean} Returns `true` if selection was successful, `false` otherwise.
   */ selectCells(selectionRanges) {
        const selectionType = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["detectSelectionType"])(selectionRanges);
        if (selectionType === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["SELECTION_TYPE_EMPTY"]) {
            return false;
        } else if (selectionType === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["SELECTION_TYPE_UNRECOGNIZED"]) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["toSingleLine"]`Unsupported format of the selection ranges was passed. To select cells pass\x20
        the coordinates as an array of arrays ([[rowStart, columnStart/columnPropStart, rowEnd,\x20
        columnEnd/columnPropEnd]]) or as an array of CellRange objects.`);
        }
        const selectionSchemaNormalizer = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["normalizeSelectionFactory"])(selectionType, {
            createCellCoords: (...args)=>this.tableProps.createCellCoords(...args),
            createCellRange: (...args)=>this.tableProps.createCellRange(...args),
            propToCol: (prop)=>this.tableProps.propToCol(prop),
            keepDirection: true
        });
        const navigableHeaders = this.settings.navigableHeaders;
        const tableParams = {
            countRows: this.tableProps.countRows(),
            countCols: this.tableProps.countCols(),
            countRowHeaders: navigableHeaders ? this.tableProps.countRowHeaders() : 0,
            countColHeaders: navigableHeaders ? this.tableProps.countColHeaders() : 0
        };
        // Check if every layer of the coordinates are valid.
        const isValid = !selectionRanges.some((selection)=>{
            const cellRange = selectionSchemaNormalizer(selection);
            const rangeValidity = cellRange.isValid(tableParams);
            return !(rangeValidity && !cellRange.containsHeaders() || rangeValidity && cellRange.containsHeaders() && cellRange.isSingleHeader());
        });
        if (isValid) {
            this.clear();
            this.setExpectedLayers(selectionRanges.length);
            selectionRanges.forEach((selection)=>{
                const { from, to } = selectionSchemaNormalizer(selection);
                this.setRangeStartOnly(from.clone(), false);
                this.setRangeEnd(to.clone());
            });
            this.finish();
        }
        return isValid;
    }
    /**
   * Select column specified by `startColumn` visual index or column property or a range of columns finishing at
   * `endColumn`.
   *
   * @param {number|string} startColumn Visual column index or column property from which the selection starts.
   * @param {number|string} [endColumn] Visual column index or column property from to the selection finishes.
   * @param {number | { row: number, col: number }} [focusPosition=0] The argument allows changing the cell/header focus
   * position. The value can take visual row index from -N to N, where negative values point to the headers and positive
   * values point to the cell range. An object with `row` and `col` properties also can be passed to change the focus
   * position horizontally.
   * @returns {boolean} Returns `true` if selection was successful, `false` otherwise.
   */ selectColumns(startColumn, endColumn = startColumn, focusPosition = 0) {
        const start = typeof startColumn === 'string' ? this.tableProps.propToCol(startColumn) : startColumn;
        const end = typeof endColumn === 'string' ? this.tableProps.propToCol(endColumn) : endColumn;
        const countRows = this.tableProps.countRows();
        const countCols = this.tableProps.countCols();
        const countColHeaders = this.tableProps.countColHeaders();
        const columnHeaderLastIndex = countColHeaders === 0 ? 0 : -countColHeaders;
        const fromCoords = this.tableProps.createCellCoords(columnHeaderLastIndex, start);
        const toCoords = this.tableProps.createCellCoords(countRows - 1, end);
        const isValid = this.tableProps.createCellRange(fromCoords, fromCoords, toCoords).isValid({
            countRows,
            countCols,
            countRowHeaders: 0,
            countColHeaders
        });
        if (isValid) {
            let highlightRow = 0;
            let highlightColumn = 0;
            if (isFocusPositionObject(focusPosition)) {
                highlightRow = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(focusPosition.row ?? 0, columnHeaderLastIndex, countRows - 1);
                highlightColumn = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(focusPosition.col ?? 0, Math.min(start, end), Math.max(start, end));
            } else {
                highlightRow = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(typeof focusPosition === 'number' ? focusPosition : 0, columnHeaderLastIndex, countRows - 1);
                highlightColumn = start;
            }
            const highlight = this.tableProps.createCellCoords(highlightRow, highlightColumn);
            const fromRow = countColHeaders === 0 ? 0 : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(highlight.row ?? 0, columnHeaderLastIndex, -1);
            const toRow = countRows - 1;
            const from = this.tableProps.createCellCoords(fromRow, start);
            const to = this.tableProps.createCellCoords(toRow, end);
            this.runLocalHooks('beforeSelectColumns', from, to, highlight);
            this.setRangeStartOnly(from, undefined, highlight);
            this.selectedByColumnHeader.add(this.getLayerLevel());
            this.setRangeEnd(to);
            this.runLocalHooks('afterSelectColumns', from, to, highlight);
            this.finish();
        }
        return isValid;
    }
    /**
   * Select row specified by `startRow` visual index or a range of rows finishing at `endRow`.
   *
   * @param {number} startRow Visual row index from which the selection starts.
   * @param {number} [endRow] Visual row index from to the selection finishes.
   * @param {number | { row: number, col: number }} [focusPosition=0] The argument allows changing the cell/header focus
   * position. The value can take visual row index from -N to N, where negative values point to the headers and positive
   * values point to the cell range. An object with `row` and `col` properties also can be passed to change the focus
   * position horizontally.
   * @returns {boolean} Returns `true` if selection was successful, `false` otherwise.
   */ selectRows(startRow, endRow = startRow, focusPosition = 0) {
        const countRows = this.tableProps.countRows();
        const countCols = this.tableProps.countCols();
        const countRowHeaders = this.tableProps.countRowHeaders();
        const rowHeaderLastIndex = countRowHeaders === 0 ? 0 : -countRowHeaders;
        const fromCoords = this.tableProps.createCellCoords(startRow, rowHeaderLastIndex);
        const toCoords = this.tableProps.createCellCoords(endRow, countCols - 1);
        const isValid = this.tableProps.createCellRange(fromCoords, fromCoords, toCoords).isValid({
            countRows,
            countCols,
            countRowHeaders,
            countColHeaders: 0
        });
        if (isValid) {
            let highlightRow = 0;
            let highlightColumn = 0;
            if (isFocusPositionObject(focusPosition)) {
                highlightRow = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(focusPosition.row ?? 0, Math.min(startRow, endRow), Math.max(startRow, endRow));
                highlightColumn = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(focusPosition.col ?? 0, rowHeaderLastIndex, countCols - 1);
            } else {
                highlightRow = startRow;
                highlightColumn = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(typeof focusPosition === 'number' ? focusPosition : 0, rowHeaderLastIndex, countCols - 1);
            }
            const highlight = this.tableProps.createCellCoords(highlightRow, highlightColumn);
            const fromColumn = countRowHeaders === 0 ? 0 : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(highlight.col ?? 0, rowHeaderLastIndex, -1);
            const toColumn = countCols - 1;
            const from = this.tableProps.createCellCoords(startRow, fromColumn);
            const to = this.tableProps.createCellCoords(endRow, toColumn);
            this.runLocalHooks('beforeSelectRows', from, to, highlight);
            this.setRangeStartOnly(from, undefined, highlight);
            this.selectedByRowHeader.add(this.getLayerLevel());
            this.setRangeEnd(to);
            this.runLocalHooks('afterSelectRows', from, to, highlight);
            this.finish();
        }
        return isValid;
    }
    /**
   * Allows importing the selection for all layers from the provided array of CellRange objects.
   * The method clears the current selection and sets the new one without triggering any
   * selection related hooks.
   *
   * @param {SelectionState} selectionState The selection state to import.
   */ importSelection({ ranges, activeRange, activeSelectionLayer, selectedByRowHeader, selectedByColumnHeader, disableHeadersHighlight }) {
        if (ranges.length === 0) {
            return;
        }
        this.selectedRange.clear();
        this.highlight.clear();
        this.inProgress = false;
        _class_private_field_set(this, _disableHeadersHighlight, disableHeadersHighlight);
        this.selectedByRowHeader = new Set(selectedByRowHeader);
        this.selectedByColumnHeader = new Set(selectedByColumnHeader);
        this.setActiveSelectionLayerIndex(0);
        ranges.forEach((cellRange, selectionLayerIndex)=>{
            this.selectedRange.push(cellRange);
            this.applyAndCommit(cellRange, selectionLayerIndex);
        });
        this.setRangeFocus(activeRange.highlight, activeSelectionLayer);
        _class_private_field_set(this, _disableHeadersHighlight, false);
        this.inProgress = false;
    }
    /**
   * Exports all selection layers with other properties related to the selection state.
   *
   * @returns {SelectionState}
   */ exportSelection() {
        return {
            ranges: Array.from(this.selectedRange).map((range)=>range.clone()),
            activeRange: this.getActiveSelectedRange(),
            activeSelectionLayer: this.getActiveSelectionLayerIndex(),
            selectedByRowHeader: Array.from(this.selectedByRowHeader),
            selectedByColumnHeader: Array.from(this.selectedByColumnHeader),
            disableHeadersHighlight: _class_private_field_get(this, _disableHeadersHighlight)
        };
    }
    /**
   * Refreshes the whole selection by clearing, reapplying and committing (calculating visual to renderable indexes)
   * the selection by using already added visual ranges. The method can be useful when underneath some indexes
   * was hidden/showed or dataset size was changed or the range of the cell ranges was modified. Then, to see the
   * changes in the selection the method may be needed to be called. The method modifies the visual ranges if needed.
   */ refresh() {
        if (!this.isSelected()) {
            return;
        }
        const countRows = this.tableProps.countRows();
        const countColumns = this.tableProps.countCols();
        if (countRows === 0 || countColumns === 0) {
            this.deselect();
            return;
        }
        const ranges = this.selectedRange.ranges.map((range)=>range.clone());
        const selectionSource = this.getSelectionSource();
        if (selectionSource === 'unknown') {
            this.markSource('refresh');
        }
        const selectedByRowHeader = new Set(this.selectedByRowHeader);
        const selectedByColumnHeader = new Set(this.selectedByColumnHeader);
        this.clear();
        this.setExpectedLayers(ranges.length);
        ranges.forEach((range)=>{
            const { from, to, highlight } = range;
            const maxRows = countRows - 1;
            const maxColumns = countColumns - 1;
            highlight.assign({
                row: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(highlight.row ?? 0, this.settings.navigableHeaders ? -Infinity : 0, maxRows),
                col: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(highlight.col ?? 0, this.settings.navigableHeaders ? -Infinity : 0, maxColumns)
            });
            from.assign({
                row: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(from.row ?? 0, -Infinity, maxRows),
                col: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(from.col ?? 0, -Infinity, maxColumns)
            });
            to.assign({
                row: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(to.row ?? 0, -Infinity, maxRows),
                col: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$number$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clamp"])(to.col ?? 0, -Infinity, maxColumns)
            });
            this.setRangeStartOnly(from, true, highlight);
            this.setRangeEnd(to);
        });
        this.selectedByRowHeader = selectedByRowHeader;
        this.selectedByColumnHeader = selectedByColumnHeader;
        this.finish();
        this.markEndSource();
    }
    /**
   * Refreshes the whole selection by only recommitting values. In terms of the selection the committing
   * values means that the cell ranges are again recalculated to the renderable indexes - the visual
   * indexes are not touched. The method can be useful when underneath some indexes was hidden/showed
   * which affects the selection. In that cases the method may be needed to be called.
   */ commit() {
        const customSelections = this.highlight.getCustomSelections();
        customSelections.forEach((customSelection)=>{
            customSelection.commit();
        });
        if (!this.isSelected()) {
            return;
        }
        const currentLayer = this.getLayerLevel();
        const cellRange = this.getActiveSelectedRange();
        if (cellRange && this.highlight.isEnabledFor(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$selection$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__FOCUS_TYPE__as__HIGHLIGHT_FOCUS_TYPE$3e$__$3c$export__HIGHLIGHT_FOCUS_TYPE__as__FOCUS_TYPE$3e$__["FOCUS_TYPE"], cellRange.highlight)) {
            this.highlight.getFocus().commit().syncWith(cellRange);
        }
        // Rewriting rendered ranges going through all layers.
        for(let layerLevel = 0; layerLevel < this.selectedRange.size(); layerLevel += 1){
            this.highlight.useLayerLevel(layerLevel);
            const areaHighlight = this.highlight.createArea();
            const areaLayeredHighlight = this.highlight.createLayeredArea();
            const rowHeaderHighlight = this.highlight.createRowHeader();
            const columnHeaderHighlight = this.highlight.createColumnHeader();
            const activeRowHeaderHighlight = this.highlight.createActiveRowHeader();
            const activeColumnHeaderHighlight = this.highlight.createActiveColumnHeader();
            const activeCornerHeaderHighlight = this.highlight.createActiveCornerHeader();
            const rowHighlight = this.highlight.createRowHighlight();
            const columnHighlight = this.highlight.createColumnHighlight();
            areaHighlight?.commit();
            areaLayeredHighlight?.commit();
            rowHeaderHighlight?.commit();
            columnHeaderHighlight?.commit();
            activeRowHeaderHighlight?.commit();
            activeColumnHeaderHighlight?.commit();
            activeCornerHeaderHighlight?.commit();
            rowHighlight?.commit();
            columnHighlight?.commit();
        }
        // Reverting starting layer for the Highlight.
        this.highlight.useLayerLevel(currentLayer);
    }
    /**
   * Initializes the Selection manager with grid settings and table API references, and sets up transformation modules and highlight layers.
   */ constructor(settings, tableProps){
        _class_private_method_init(this, _applyHeaderHighlights);
        /**
   * The module for modifying coordinates of the start and end selection.
   *
   * @type {ExtenderTransformation}
   */ _class_private_field_init(this, _extenderTransformation, {
            writable: true,
            value: void 0
        });
        /**
   * The module for modifying coordinates of the focus selection.
   *
   * @type {FocusTransformation}
   */ _class_private_field_init(this, _focusTransformation, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _isFocusSelectionChanged, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _disableHeadersHighlight, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _selectionSource, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _expectedLayersCount, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _activeSelectionLayer, {
            writable: true,
            value: void 0
        });
        /**
   * The flag which determines if the selection is in progress.
   *
   * @type {boolean}
   */ this.inProgress = false;
        /**
   * Selection data layer (handle visual coordinates).
   *
   * @type {SelectionRange}
   */ this.selectedRange = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$range$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]((highlight, from, to)=>{
            return this.tableProps.createCellRange(highlight, from, to);
        });
        /**
   * The collection of the selection layer levels where the whole row was selected using the row header or
   * the corner header.
   *
   * @type {Set<number>}
   */ this.selectedByRowHeader = new Set();
        /**
   * The collection of the selection layer levels where the whole column was selected using the column header or
   * the corner header.
   *
   * @type {Set<number>}
   */ this.selectedByColumnHeader = new Set();
        _class_private_field_set(this, _isFocusSelectionChanged, false);
        _class_private_field_set(this, _disableHeadersHighlight, false);
        _class_private_field_set(this, _selectionSource, 'unknown');
        _class_private_field_set(this, _expectedLayersCount, -1);
        _class_private_field_set(this, _activeSelectionLayer, 0);
        this.settings = settings;
        this.tableProps = tableProps;
        this.highlight = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$highlight$2f$highlight$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["default"]({
            headerClassName: settings.currentHeaderClassName,
            activeHeaderClassName: settings.activeHeaderClassName,
            rowClassName: settings.currentRowClassName,
            columnClassName: settings.currentColClassName,
            cellAttributes: [
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$a11y$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["A11Y_SELECTED"])()
            ],
            rowIndexMapper: this.tableProps.rowIndexMapper,
            columnIndexMapper: this.tableProps.columnIndexMapper,
            disabledCellSelection: (row, column)=>this.tableProps.isDisabledCellSelection(row, column),
            cellCornerVisible: ()=>this.isCellCornerVisible(),
            areaCornerVisible: (layerLevel)=>this.isAreaCornerVisible(layerLevel),
            visualToRenderableCoords: (coords)=>this.tableProps.visualToRenderableCoords(coords),
            renderableToVisualCoords: (coords)=>this.tableProps.renderableToVisualCoords(coords),
            createCellCoords: (row, column)=>this.tableProps.createCellCoords(row, column),
            createCellRange: (highlight, from, to)=>this.tableProps.createCellRange(highlight, from, to)
        });
        _class_private_field_set(this, _extenderTransformation, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$transformation$2f$extender$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["ExtenderTransformation"](this.selectedRange, {
            ...this.tableProps,
            navigableHeaders: ()=>!!settings.navigableHeaders,
            fixedRowsBottom: ()=>Number(settings.fixedRowsBottom),
            minSpareRows: ()=>Number(settings.minSpareRows),
            minSpareCols: ()=>Number(settings.minSpareCols),
            autoWrapRow: ()=>!!settings.autoWrapRow,
            autoWrapCol: ()=>!!settings.autoWrapCol
        }));
        _class_private_field_set(this, _focusTransformation, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$transformation$2f$focus$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["FocusTransformation"](this.selectedRange, {
            ...this.tableProps,
            navigableHeaders: ()=>!!settings.navigableHeaders,
            fixedRowsBottom: ()=>0,
            minSpareRows: ()=>0,
            minSpareCols: ()=>0,
            autoWrapRow: ()=>true,
            autoWrapCol: ()=>true
        }));
        _class_private_field_get(this, _extenderTransformation).addLocalHook('beforeTransformStart', (...args)=>this.runLocalHooks('beforeModifyTransformStart', ...args));
        _class_private_field_get(this, _extenderTransformation).addLocalHook('afterTransformStart', (...args)=>this.runLocalHooks('afterModifyTransformStart', ...args));
        _class_private_field_get(this, _extenderTransformation).addLocalHook('beforeTransformEnd', (...args)=>this.runLocalHooks('beforeModifyTransformEnd', ...args));
        _class_private_field_get(this, _extenderTransformation).addLocalHook('afterTransformEnd', (...args)=>this.runLocalHooks('afterModifyTransformEnd', ...args));
        _class_private_field_get(this, _extenderTransformation).addLocalHook('insertRowRequire', (...args)=>this.runLocalHooks('insertRowRequire', ...args));
        _class_private_field_get(this, _extenderTransformation).addLocalHook('insertColRequire', (...args)=>this.runLocalHooks('insertColRequire', ...args));
        _class_private_field_get(this, _extenderTransformation).addLocalHook('beforeRowWrap', (...args)=>this.runLocalHooks('beforeRowWrap', ...args));
        _class_private_field_get(this, _extenderTransformation).addLocalHook('beforeColumnWrap', (...args)=>this.runLocalHooks('beforeColumnWrap', ...args));
        _class_private_field_get(this, _focusTransformation).addLocalHook('beforeTransformStart', (...args)=>this.runLocalHooks('beforeModifyTransformFocus', ...args));
        _class_private_field_get(this, _focusTransformation).addLocalHook('afterTransformStart', (...args)=>this.runLocalHooks('afterModifyTransformFocus', ...args));
    }
}
function applyHeaderHighlights(cellRange, countRows, countCols, rowHeaderHighlight, columnHeaderHighlight, activeRowHeaderHighlight, activeColumnHeaderHighlight, activeCornerHeaderHighlight, rowHighlight, columnHighlight) {
    if (!cellRange.isSingleHeader()) {
        const rowCoordsFrom = this.tableProps.createCellCoords(Math.max(cellRange.from.row ?? 0, 0), -1);
        const rowCoordsTo = this.tableProps.createCellCoords(cellRange.to.row ?? 0, -1);
        const columnCoordsFrom = this.tableProps.createCellCoords(-1, Math.max(cellRange.from.col ?? 0, 0));
        const columnCoordsTo = this.tableProps.createCellCoords(-1, cellRange.to.col ?? 0);
        if (this.settings.selectionMode === 'single') {
            rowHeaderHighlight?.add(rowCoordsFrom).commit();
            columnHeaderHighlight?.add(columnCoordsFrom).commit();
            rowHighlight?.add(rowCoordsFrom).commit();
            columnHighlight?.add(columnCoordsFrom).commit();
        } else {
            rowHeaderHighlight?.add(rowCoordsFrom).add(rowCoordsTo).commit();
            columnHeaderHighlight?.add(columnCoordsFrom).add(columnCoordsTo).commit();
            rowHighlight?.add(rowCoordsFrom).add(rowCoordsTo).commit();
            columnHighlight?.add(columnCoordsFrom).add(columnCoordsTo).commit();
        }
    }
    const highlightRowHeaders = !_class_private_field_get(this, _disableHeadersHighlight) && this.isEntireRowSelected() && (countCols > 0 && countCols === cellRange.getWidth() || countCols === 0 && this.isSelectedByRowHeader());
    const highlightColumnHeaders = !_class_private_field_get(this, _disableHeadersHighlight) && this.isEntireColumnSelected() && (countRows > 0 && countRows === cellRange.getHeight() || countRows === 0 && this.isSelectedByColumnHeader());
    if (highlightRowHeaders) {
        activeRowHeaderHighlight?.add(this.tableProps.createCellCoords(Math.max(cellRange.from.row ?? 0, 0), Math.min(-this.tableProps.countRowHeaders(), -1))).add(this.tableProps.createCellCoords(Math.max(cellRange.to.row ?? 0, 0), -1)).commit();
    }
    if (highlightColumnHeaders) {
        activeColumnHeaderHighlight?.add(this.tableProps.createCellCoords(Math.min(-this.tableProps.countColHeaders(), -1), Math.max(cellRange.from.col ?? 0, 0))).add(this.tableProps.createCellCoords(-1, Math.max(cellRange.to.col ?? 0, 0))).commit();
    }
    if (highlightRowHeaders && highlightColumnHeaders) {
        activeCornerHeaderHighlight?.add(this.tableProps.createCellCoords(-this.tableProps.countColHeaders(), -this.tableProps.countRowHeaders())).add(this.tableProps.createCellCoords(-1, -1)).commit();
    }
}
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(Selection, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$mixins$2f$localHooks$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
var __TURBOPACK__default__export__ = Selection;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/selection.mjs [client] (ecmascript) <export default as Selection>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "Selection",
    ()=>(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$selection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ ?? __turbopack_context__.i("[project]/node_modules/handsontable/selection/selection.mjs [client] (ecmascript)"))["default"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$selection$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/selection.mjs [client] (ecmascript)");
}),
"[project]/node_modules/handsontable/selection/transformation/_base.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "BaseTransformation",
    ()=>BaseTransformation
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$mixins$2f$localHooks$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/mixins/localHooks.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
var _range = /*#__PURE__*/ new WeakMap(), /**
   * Index of the currently active selection layer.
   *
   * @type {number}
   */ _activeLayerIndex = /*#__PURE__*/ new WeakMap(), /**
   * Increases the table size by applying the offsets. The option is used by the `navigableHeaders`
   * option.
   *
   * @type {{ x: number, y: number }}
   */ _offset = /*#__PURE__*/ new WeakMap(), /**
   * Chooses the next selection layer as active one.
   */ _chooseNextSelectionLayer = /*#__PURE__*/ new WeakSet(), /**
   * Chooses the previous selection layer as active one.
   */ _choosePreviousSelectionLayer = /*#__PURE__*/ new WeakSet(), /**
   * Clamps the coords to make sure they points to the cell (or header) in the table range.
   *
   * @param {CellCoords} zeroBasedCoords The coords object to clamp.
   * @returns {{rowDir: 1|0|-1, colDir: 1|0|-1}}
   */ _clampCoords = /*#__PURE__*/ new WeakSet(), /**
   * Gets the table size in number of rows with headers as "height" and number of columns with
   * headers as "width".
   *
   * @returns {{width: number, height: number}}
   */ _getTableSize = /*#__PURE__*/ new WeakSet(), /**
   * Finds non-hidden zero-based coordinates in the current selection range.
   *
   * @param {'forward' | 'backward'} direction The direction to search within the selection.
   * @returns {CellCoords | null} Zero-based coordinates or null if not found.
   */ _findNonHiddenZeroBasedCoordsInSelection = /*#__PURE__*/ new WeakSet(), /**
   * Finds the first non-hidden zero-based row in the table range.
   *
   * @param {number} visualRowFrom The visual row from which the search should start.
   * @param {number} visualRowTo The visual row to which the search should end.
   * @returns {number | null}
   */ _findFirstNonHiddenZeroBasedRow = /*#__PURE__*/ new WeakSet(), /**
   * Finds the first non-hidden zero-based column in the table range.
   *
   * @param {number} visualColumnFrom The visual column from which the search should start.
   * @param {number} visualColumnTo The visual column to which the search should end.
   * @returns {number | null}
   */ _findFirstNonHiddenZeroBasedColumn = /*#__PURE__*/ new WeakSet(), /**
   * Translates the visual coordinates to zero-based ones.
   *
   * @param {CellCoords} visualCoords The visual coords to process.
   * @returns {CellCoords}
   */ _visualToZeroBasedCoords = /*#__PURE__*/ new WeakSet(), /**
   * Translates the zero-based coordinates to visual ones.
   *
   * @param {CellCoords} zeroBasedCoords The coordinates to process.
   * @returns {CellCoords}
   */ _zeroBasedToVisualCoords = /*#__PURE__*/ new WeakSet();
class BaseTransformation {
    /**
   * Sets the currently active selection layer index.
   *
   * @param {number} layerIndex The layer index to set as active.
   */ setActiveLayerIndex(layerIndex) {
        _class_private_field_set(this, _activeLayerIndex, layerIndex);
    }
    /**
   * Gets the currently active selection layer range.
   *
   * @returns {CellRange}
   */ getCurrentSelection() {
        return _class_private_field_get(this, _range).peekByIndex(_class_private_field_get(this, _activeLayerIndex));
    }
    /**
   * Selects cell relative to the current cell (if possible).
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   * @param {boolean} [createMissingRecords=false] If `true` the new rows/columns will be created if necessary. Otherwise, row/column will
   *                        be created according to `minSpareRows/minSpareCols` settings of Handsontable.
   * @returns {{selectionLayer: number, visualCoords: CellCoords}} Visual coordinates with selection layer after transformation.
   */ transformStart(rowDelta, colDelta, createMissingRecords = false) {
        _class_private_field_set(this, _offset, this.calculateOffset());
        const delta = this.tableApi.createCellCoords(rowDelta, colDelta);
        let visualCoords = this.getCurrentSelection().highlight;
        const highlightRenderableCoords = this.tableApi.visualToRenderableCoords(visualCoords);
        let rowTransformDir = 0;
        let colTransformDir = 0;
        this.runLocalHooks('beforeTransformStart', delta);
        if (highlightRenderableCoords.row !== null && highlightRenderableCoords.col !== null) {
            let { width, height } = _class_private_method_get(this, _getTableSize, getTableSize).call(this);
            const { row, col } = _class_private_method_get(this, _visualToZeroBasedCoords, visualToZeroBasedCoords).call(this, visualCoords);
            const fixedRowsBottom = this.tableApi.fixedRowsBottom();
            const minSpareRows = this.tableApi.minSpareRows();
            const minSpareCols = this.tableApi.minSpareCols();
            const autoWrapRow = this.tableApi.autoWrapRow();
            const autoWrapCol = this.tableApi.autoWrapCol();
            const zeroBasedCoords = this.tableApi.createCellCoords((row ?? 0) + (delta.row ?? 0), (col ?? 0) + (delta.col ?? 0));
            if ((zeroBasedCoords.row ?? 0) >= height) {
                const isActionInterrupted = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createObjectPropListener"])(createMissingRecords && minSpareRows > 0 && fixedRowsBottom === 0);
                const nextColumn = (zeroBasedCoords.col ?? 0) + 1;
                const isColumnOutOfRange = nextColumn >= width;
                const newCoords = this.tableApi.createCellCoords((zeroBasedCoords.row ?? 0) - height, isColumnOutOfRange ? nextColumn - width : nextColumn);
                this.runLocalHooks('beforeColumnWrap', isActionInterrupted, _class_private_method_get(this, _zeroBasedToVisualCoords, zeroBasedToVisualCoords).call(this, newCoords), isColumnOutOfRange);
                if (isActionInterrupted.value) {
                    this.runLocalHooks('insertRowRequire', this.countRenderableRows());
                } else if (autoWrapCol) {
                    if (this.shouldSwitchSelectionLayer() && isColumnOutOfRange && _class_private_field_get(this, _range).size() > 1) {
                        _class_private_method_get(this, _chooseNextSelectionLayer, chooseNextSelectionLayer).call(this);
                        const nextLayerCoords = _class_private_method_get(this, _findNonHiddenZeroBasedCoordsInSelection, findNonHiddenZeroBasedCoordsInSelection).call(this, 'forward');
                        if (nextLayerCoords !== null) {
                            newCoords.assign(nextLayerCoords);
                        }
                    }
                    zeroBasedCoords.assign(newCoords);
                }
            } else if ((zeroBasedCoords.row ?? 0) < 0) {
                const isActionInterrupted = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createObjectPropListener"])(autoWrapCol);
                const previousColumn = (zeroBasedCoords.col ?? 0) - 1;
                const isColumnOutOfRange = previousColumn < 0;
                const newCoords = this.tableApi.createCellCoords(height + (zeroBasedCoords.row ?? 0), isColumnOutOfRange ? width + previousColumn : previousColumn);
                this.runLocalHooks('beforeColumnWrap', isActionInterrupted, _class_private_method_get(this, _zeroBasedToVisualCoords, zeroBasedToVisualCoords).call(this, newCoords), isColumnOutOfRange);
                if (autoWrapCol) {
                    if (this.shouldSwitchSelectionLayer() && isColumnOutOfRange && _class_private_field_get(this, _range).size() > 1) {
                        _class_private_method_get(this, _choosePreviousSelectionLayer, choosePreviousSelectionLayer).call(this);
                        const nextLayerCoords = _class_private_method_get(this, _findNonHiddenZeroBasedCoordsInSelection, findNonHiddenZeroBasedCoordsInSelection).call(this, 'backward');
                        if (nextLayerCoords !== null) {
                            newCoords.assign(nextLayerCoords);
                        }
                    }
                    zeroBasedCoords.assign(newCoords);
                }
            }
            // the range size may be changed after the column wrap, so we need to recalculate it for row wrap
            ({ width, height } = _class_private_method_get(this, _getTableSize, getTableSize).call(this));
            if ((zeroBasedCoords.col ?? 0) >= width) {
                const isActionInterrupted = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createObjectPropListener"])(createMissingRecords && minSpareCols > 0);
                const nextRow = (zeroBasedCoords.row ?? 0) + 1;
                const isRowOutOfRange = nextRow >= height;
                const newCoords = this.tableApi.createCellCoords(isRowOutOfRange ? nextRow - height : nextRow, (zeroBasedCoords.col ?? 0) - width);
                this.runLocalHooks('beforeRowWrap', isActionInterrupted, _class_private_method_get(this, _zeroBasedToVisualCoords, zeroBasedToVisualCoords).call(this, newCoords), isRowOutOfRange);
                if (isActionInterrupted.value) {
                    this.runLocalHooks('insertColRequire', this.countRenderableColumns());
                } else if (autoWrapRow) {
                    if (this.shouldSwitchSelectionLayer() && isRowOutOfRange && _class_private_field_get(this, _range).size() > 1) {
                        _class_private_method_get(this, _chooseNextSelectionLayer, chooseNextSelectionLayer).call(this);
                        const nextLayerCoords = _class_private_method_get(this, _findNonHiddenZeroBasedCoordsInSelection, findNonHiddenZeroBasedCoordsInSelection).call(this, 'forward');
                        if (nextLayerCoords !== null) {
                            newCoords.assign(nextLayerCoords);
                        }
                    }
                    zeroBasedCoords.assign(newCoords);
                }
            } else if ((zeroBasedCoords.col ?? 0) < 0) {
                const isActionInterrupted = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["createObjectPropListener"])(autoWrapRow);
                const previousRow = (zeroBasedCoords.row ?? 0) - 1;
                const isRowOutOfRange = previousRow < 0;
                const newCoords = this.tableApi.createCellCoords(isRowOutOfRange ? height + previousRow : previousRow, width + (zeroBasedCoords.col ?? 0));
                this.runLocalHooks('beforeRowWrap', isActionInterrupted, _class_private_method_get(this, _zeroBasedToVisualCoords, zeroBasedToVisualCoords).call(this, newCoords), isRowOutOfRange);
                if (autoWrapRow) {
                    if (this.shouldSwitchSelectionLayer() && isRowOutOfRange && _class_private_field_get(this, _range).size() > 1) {
                        _class_private_method_get(this, _choosePreviousSelectionLayer, choosePreviousSelectionLayer).call(this);
                        const nextLayerCoords = _class_private_method_get(this, _findNonHiddenZeroBasedCoordsInSelection, findNonHiddenZeroBasedCoordsInSelection).call(this, 'backward');
                        if (nextLayerCoords !== null) {
                            newCoords.assign(nextLayerCoords);
                        }
                    }
                    zeroBasedCoords.assign(newCoords);
                }
            }
            const { rowDir, colDir } = _class_private_method_get(this, _clampCoords, clampCoords).call(this, zeroBasedCoords);
            rowTransformDir = rowDir;
            colTransformDir = colDir;
            visualCoords = _class_private_method_get(this, _zeroBasedToVisualCoords, zeroBasedToVisualCoords).call(this, zeroBasedCoords);
        }
        this.runLocalHooks('afterTransformStart', visualCoords, rowTransformDir, colTransformDir);
        return {
            selectionLayer: _class_private_field_get(this, _activeLayerIndex),
            visualCoords
        };
    }
    /**
   * Sets selection end cell relative to the current selection end cell (if possible).
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   * @returns {{selectionLayer: number, visualCoords: CellCoords}} Visual coordinates with selection layer after transformation.
   */ transformEnd(rowDelta, colDelta) {
        _class_private_field_set(this, _offset, this.calculateOffset());
        const delta = this.tableApi.createCellCoords(rowDelta, colDelta);
        const cellRange = this.getCurrentSelection();
        const highlightRenderableCoords = this.tableApi.visualToRenderableCoords(cellRange.highlight);
        const toRow = _class_private_method_get(this, _findFirstNonHiddenZeroBasedRow, findFirstNonHiddenZeroBasedRow).call(this, cellRange.to.row ?? 0, cellRange.from.row ?? 0);
        const toColumn = _class_private_method_get(this, _findFirstNonHiddenZeroBasedColumn, findFirstNonHiddenZeroBasedColumn).call(this, cellRange.to.col ?? 0, cellRange.from.col ?? 0);
        const visualCoords = cellRange.to.clone();
        let rowTransformDir = 0;
        let colTransformDir = 0;
        this.runLocalHooks('beforeTransformEnd', delta);
        if (highlightRenderableCoords.row !== null && highlightRenderableCoords.col !== null && toRow !== null && toColumn !== null) {
            const { row: highlightRow, col: highlightColumn } = _class_private_method_get(this, _visualToZeroBasedCoords, visualToZeroBasedCoords).call(this, cellRange.highlight);
            const coords = this.tableApi.createCellCoords(toRow + (delta.row ?? 0), toColumn + (delta.col ?? 0));
            const topStartCorner = cellRange.getTopStartCorner();
            const topEndCorner = cellRange.getTopEndCorner();
            const bottomEndCorner = cellRange.getBottomEndCorner();
            if ((delta.col ?? 0) < 0 && toColumn >= (highlightColumn ?? 0) && (coords.col ?? 0) < (highlightColumn ?? 0)) {
                const columnRestDelta = (coords.col ?? 0) - (highlightColumn ?? 0);
                const firstNonHidden = _class_private_method_get(this, _findFirstNonHiddenZeroBasedColumn, findFirstNonHiddenZeroBasedColumn).call(this, topStartCorner.col ?? 0, topEndCorner.col ?? 0);
                coords.col = (firstNonHidden ?? 0) + columnRestDelta;
            } else if ((delta.col ?? 0) > 0 && toColumn <= (highlightColumn ?? 0) && (coords.col ?? 0) > (highlightColumn ?? 0)) {
                const endColumnIndex = _class_private_method_get(this, _findFirstNonHiddenZeroBasedColumn, findFirstNonHiddenZeroBasedColumn).call(this, topEndCorner.col ?? 0, topStartCorner.col ?? 0);
                const columnRestDelta = Math.max((coords.col ?? 0) - (endColumnIndex ?? 0), 1);
                coords.col = (endColumnIndex ?? 0) + columnRestDelta;
            }
            if ((delta.row ?? 0) < 0 && toRow >= (highlightRow ?? 0) && (coords.row ?? 0) < (highlightRow ?? 0)) {
                const rowRestDelta = (coords.row ?? 0) - (highlightRow ?? 0);
                const firstNonHidden = _class_private_method_get(this, _findFirstNonHiddenZeroBasedRow, findFirstNonHiddenZeroBasedRow).call(this, topStartCorner.row ?? 0, bottomEndCorner.row ?? 0);
                coords.row = (firstNonHidden ?? 0) + rowRestDelta;
            } else if ((delta.row ?? 0) > 0 && toRow <= (highlightRow ?? 0) && (coords.row ?? 0) > (highlightRow ?? 0)) {
                const bottomRowIndex = _class_private_method_get(this, _findFirstNonHiddenZeroBasedRow, findFirstNonHiddenZeroBasedRow).call(this, bottomEndCorner.row ?? 0, topStartCorner.row ?? 0);
                const rowRestDelta = Math.max((coords.row ?? 0) - (bottomRowIndex ?? 0), 1);
                coords.row = (bottomRowIndex ?? 0) + rowRestDelta;
            }
            const { rowDir, colDir } = _class_private_method_get(this, _clampCoords, clampCoords).call(this, coords);
            rowTransformDir = rowDir;
            colTransformDir = colDir;
            const newVisualCoords = _class_private_method_get(this, _zeroBasedToVisualCoords, zeroBasedToVisualCoords).call(this, coords);
            if (delta.row === 0 && delta.col !== 0) {
                visualCoords.col = newVisualCoords.col;
            } else if (delta.row !== 0 && delta.col === 0) {
                visualCoords.row = newVisualCoords.row;
            } else {
                visualCoords.row = newVisualCoords.row;
                visualCoords.col = newVisualCoords.col;
            }
        }
        this.runLocalHooks('afterTransformEnd', visualCoords, rowTransformDir, colTransformDir);
        return {
            selectionLayer: _class_private_field_get(this, _activeLayerIndex),
            visualCoords
        };
    }
    /**
   * Abstract method that child classes must implement to calculate offset coordinates based on
   * the current processed selection layer.
   */ calculateOffset() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('`calculateOffset` is not implemented');
    }
    /**
   * Abstract method that child classes must implement to provide the count of renderable rows
   * based on their specific transformation logic.
   */ countRenderableRows() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('`countRenderableRows` is not implemented');
    }
    /**
   * Abstract method that child classes must implement to provide the count of renderable columns
   * based on their specific transformation logic.
   */ countRenderableColumns() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('`countRenderableColumns` is not implemented');
    }
    /**
   * Determines whether selection layer switching should occur during transformation.
   * Child classes can override this method to control the behavior.
   */ shouldSwitchSelectionLayer() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('`shouldSwitchSelectionLayer` is not implemented');
    }
    /**
   * Initializes the transformation module with the selection range collection and the table API reference.
   */ constructor(range, tableApi){
        _class_private_method_init(this, _chooseNextSelectionLayer);
        _class_private_method_init(this, _choosePreviousSelectionLayer);
        _class_private_method_init(this, _clampCoords);
        _class_private_method_init(this, _getTableSize);
        _class_private_method_init(this, _findNonHiddenZeroBasedCoordsInSelection);
        _class_private_method_init(this, _findFirstNonHiddenZeroBasedRow);
        _class_private_method_init(this, _findFirstNonHiddenZeroBasedColumn);
        _class_private_method_init(this, _visualToZeroBasedCoords);
        _class_private_method_init(this, _zeroBasedToVisualCoords);
        /**
   * Instance of the SelectionRange, holder for visual coordinates applied to the table.
   *
   * @type {SelectionRange}
   */ _class_private_field_init(this, _range, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _activeLayerIndex, {
            writable: true,
            value: void 0
        });
        _class_private_field_init(this, _offset, {
            writable: true,
            value: void 0
        });
        _class_private_field_set(this, _activeLayerIndex, 0);
        _class_private_field_set(this, _offset, {
            x: 0,
            y: 0
        });
        _class_private_field_set(this, _range, range);
        this.tableApi = tableApi;
    }
}
function chooseNextSelectionLayer() {
    const layerIndex = _class_private_field_get(this, _activeLayerIndex) + 1;
    this.setActiveLayerIndex(layerIndex >= _class_private_field_get(this, _range).size() ? 0 : layerIndex);
    _class_private_field_set(this, _offset, this.calculateOffset());
}
function choosePreviousSelectionLayer() {
    const layerIndex = _class_private_field_get(this, _activeLayerIndex) - 1;
    this.setActiveLayerIndex(layerIndex < 0 ? _class_private_field_get(this, _range).size() - 1 : layerIndex);
    _class_private_field_set(this, _offset, this.calculateOffset());
}
function clampCoords(zeroBasedCoords) {
    const { width, height } = _class_private_method_get(this, _getTableSize, getTableSize).call(this);
    let rowDir = 0;
    let colDir = 0;
    if ((zeroBasedCoords.row ?? 0) < 0) {
        rowDir = -1;
        zeroBasedCoords.row = 0;
    } else if ((zeroBasedCoords.row ?? 0) > 0 && (zeroBasedCoords.row ?? 0) >= height) {
        rowDir = 1;
        zeroBasedCoords.row = height - 1;
    }
    if ((zeroBasedCoords.col ?? 0) < 0) {
        colDir = -1;
        zeroBasedCoords.col = 0;
    } else if ((zeroBasedCoords.col ?? 0) > 0 && (zeroBasedCoords.col ?? 0) >= width) {
        colDir = 1;
        zeroBasedCoords.col = width - 1;
    }
    return {
        rowDir,
        colDir
    };
}
function getTableSize() {
    return {
        width: _class_private_field_get(this, _offset).x + this.countRenderableColumns(),
        height: _class_private_field_get(this, _offset).y + this.countRenderableRows()
    };
}
function findNonHiddenZeroBasedCoordsInSelection(direction) {
    if (![
        'forward',
        'backward'
    ].includes(direction)) {
        return null;
    }
    const topStartCoords = this.getCurrentSelection().getTopStartCorner();
    const bottomEndCoords = this.getCurrentSelection().getBottomEndCorner();
    const [visualRowFrom, visualRowTo, visualColumnFrom, visualColumnTo] = direction === 'forward' ? [
        topStartCoords.row,
        bottomEndCoords.row,
        topStartCoords.col,
        bottomEndCoords.col
    ] : [
        bottomEndCoords.row,
        topStartCoords.row,
        bottomEndCoords.col,
        topStartCoords.col
    ];
    const zeroBasedRow = _class_private_method_get(this, _findFirstNonHiddenZeroBasedRow, findFirstNonHiddenZeroBasedRow).call(this, visualRowFrom ?? 0, visualRowTo ?? 0);
    const zeroBasedCol = _class_private_method_get(this, _findFirstNonHiddenZeroBasedColumn, findFirstNonHiddenZeroBasedColumn).call(this, visualColumnFrom ?? 0, visualColumnTo ?? 0);
    if (zeroBasedRow === null || zeroBasedCol === null) {
        return null;
    }
    return this.tableApi.createCellCoords(zeroBasedRow, zeroBasedCol);
}
function findFirstNonHiddenZeroBasedRow(visualRowFrom, visualRowTo) {
    const row = this.tableApi.findFirstNonHiddenRenderableRow(visualRowFrom, visualRowTo);
    if (row === null) {
        return null;
    }
    return _class_private_field_get(this, _offset).y + row;
}
function findFirstNonHiddenZeroBasedColumn(visualColumnFrom, visualColumnTo) {
    const column = this.tableApi.findFirstNonHiddenRenderableColumn(visualColumnFrom, visualColumnTo);
    if (column === null) {
        return null;
    }
    return _class_private_field_get(this, _offset).x + column;
}
function visualToZeroBasedCoords(visualCoords) {
    const { row, col } = this.tableApi.visualToRenderableCoords(visualCoords);
    if (row === null || col === null) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('Renderable coords are not visible.');
    }
    return this.tableApi.createCellCoords(_class_private_field_get(this, _offset).y + row, _class_private_field_get(this, _offset).x + col);
}
function zeroBasedToVisualCoords(zeroBasedCoords) {
    const coords = zeroBasedCoords.clone();
    coords.col = (zeroBasedCoords.col ?? 0) - _class_private_field_get(this, _offset).x;
    coords.row = (zeroBasedCoords.row ?? 0) - _class_private_field_get(this, _offset).y;
    return this.tableApi.renderableToVisualCoords(coords);
}
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mixin"])(BaseTransformation, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$mixins$2f$localHooks$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]);
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/transformation/extender.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ExtenderTransformation",
    ()=>ExtenderTransformation
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$transformation$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/transformation/_base.mjs [client] (ecmascript)");
;
class ExtenderTransformation extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$transformation$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BaseTransformation"] {
    /**
   * Calculates offset coordinates for extender selection.
   *
   * @returns {{x: number, y: number}}
   */ calculateOffset() {
        return {
            x: this.tableApi.navigableHeaders() ? this.tableApi.countRowHeaders() : 0,
            y: this.tableApi.navigableHeaders() ? this.tableApi.countColHeaders() : 0
        };
    }
    /**
   * Gets the count of renderable rows for extender transformation.
   * Extender transformation operates on the full table size.
   *
   * @returns {number}
   */ countRenderableRows() {
        return this.tableApi.countRenderableRows();
    }
    /**
   * Gets the count of renderable columns for extender transformation.
   * Extender transformation operates on the full table size.
   *
   * @returns {number}
   */ countRenderableColumns() {
        return this.tableApi.countRenderableColumns();
    }
    /**
   * Changes the behavior of the transformation logic by not switching the selection layer
   * when the selection is out of range.
   *
   * @returns {boolean}
   */ shouldSwitchSelectionLayer() {
        return false;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/transformation/focus.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "FocusTransformation",
    ()=>FocusTransformation
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$transformation$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/selection/transformation/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
class FocusTransformation extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$selection$2f$transformation$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BaseTransformation"] {
    /**
   * Calculates offset coordinates for focus selection based on the current selection state.
   * For header focus selection, calculates offset including headers.
   * For cell focus selection, calculates offset only for selected cells.
   *
   * @returns {{x: number, y: number}}
   */ calculateOffset() {
        const range = this.getCurrentSelection();
        const { row, col } = range.getOuterTopStartCorner();
        const rowNum = row ?? 0;
        const colNum = col ?? 0;
        const columnsInRange = this.tableApi.countRenderableColumnsInRange(0, colNum - 1);
        const rowsInRange = this.tableApi.countRenderableRowsInRange(0, rowNum - 1);
        const isHeaderSelection = range.highlight.isHeader();
        const headerX = isHeaderSelection ? Math.abs(colNum) : 0;
        const headerY = isHeaderSelection ? Math.abs(rowNum) : 0;
        return {
            x: colNum < 0 ? headerX : -columnsInRange,
            y: rowNum < 0 ? headerY : -rowsInRange
        };
    }
    /**
   * Gets the count of renderable rows for focus transformation.
   * Focus transformation operates on the current selection range.
   *
   * @returns {number}
   */ countRenderableRows() {
        const range = this.getCurrentSelection();
        return this.tableApi.countRenderableRowsInRange(0, range.getOuterBottomEndCorner().row ?? 0);
    }
    /**
   * Gets the count of renderable columns for focus transformation.
   * Focus transformation operates on the current selection range.
   *
   * @returns {number}
   */ countRenderableColumns() {
        const range = this.getCurrentSelection();
        return this.tableApi.countRenderableColumnsInRange(0, range.getOuterBottomEndCorner().col ?? 0);
    }
    /**
   * Throws an error because focus transformation doesn't support `transformEnd`.
   */ transformEnd(_rowDelta, _colDelta) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('`transformEnd` is not valid for focus selection use `transformStart` instead');
    }
    /**
   * Changes the behavior of the transformation logic by switching the selection layer
   * when the selection is out of range.
   *
   * @returns {boolean}
   */ shouldSwitchSelectionLayer() {
        return true;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/selection/utils.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "SELECTION_TYPES",
    ()=>SELECTION_TYPES,
    "SELECTION_TYPE_ARRAY",
    ()=>SELECTION_TYPE_ARRAY,
    "SELECTION_TYPE_EMPTY",
    ()=>SELECTION_TYPE_EMPTY,
    "SELECTION_TYPE_OBJECT",
    ()=>SELECTION_TYPE_OBJECT,
    "SELECTION_TYPE_UNRECOGNIZED",
    ()=>SELECTION_TYPE_UNRECOGNIZED,
    "detectSelectionType",
    ()=>detectSelectionType,
    "normalizeSelectionFactory",
    ()=>normalizeSelectionFactory,
    "transformSelectionToColumnDistance",
    ()=>transformSelectionToColumnDistance,
    "transformSelectionToRowDistance",
    ()=>transformSelectionToRowDistance
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$range$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__default__as__CellRange$3e$__ = __turbopack_context__.i("[project]/node_modules/handsontable/3rdparty/walkontable/src/cell/range.mjs [client] (ecmascript) <export default as CellRange>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/array.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
;
;
const SELECTION_TYPE_UNRECOGNIZED = 0;
const SELECTION_TYPE_EMPTY = 1;
const SELECTION_TYPE_ARRAY = 2;
const SELECTION_TYPE_OBJECT = 3;
const SELECTION_TYPES = [
    SELECTION_TYPE_OBJECT,
    SELECTION_TYPE_ARRAY
];
const ARRAY_TYPE_PATTERN = [
    [
        'number'
    ],
    [
        'number',
        'string'
    ],
    [
        'number',
        'undefined'
    ],
    [
        'number',
        'string',
        'undefined'
    ]
];
const rootCall = Symbol('root');
const childCall = Symbol('child');
function detectSelectionType(selectionRanges, _callSymbol = rootCall) {
    if (_callSymbol !== rootCall && _callSymbol !== childCall) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('The second argument is used internally only and cannot be overwritten.');
    }
    const isArray = Array.isArray(selectionRanges);
    const isRootCall = _callSymbol === rootCall;
    let result = SELECTION_TYPE_UNRECOGNIZED;
    if (isArray) {
        const firstItem = selectionRanges[0];
        if (selectionRanges.length === 0) {
            result = SELECTION_TYPE_EMPTY;
        } else if (isRootCall && firstItem instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$3rdparty$2f$walkontable$2f$src$2f$cell$2f$range$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__$3c$export__default__as__CellRange$3e$__["CellRange"]) {
            result = SELECTION_TYPE_OBJECT;
        } else if (isRootCall && Array.isArray(firstItem)) {
            result = detectSelectionType(firstItem, childCall);
        } else if (selectionRanges.length >= 2 && selectionRanges.length <= 4) {
            const isArrayType = !selectionRanges.some((value, index)=>!ARRAY_TYPE_PATTERN[index].includes(typeof value));
            if (isArrayType) {
                result = SELECTION_TYPE_ARRAY;
            }
        }
    }
    return result;
}
function normalizeSelectionFactory(type, { createCellCoords, createCellRange, keepDirection = false, propToCol } = {}) {
    if (!SELECTION_TYPES.includes(type)) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('Unsupported selection ranges schema type was provided.');
    }
    return function(selection) {
        const isObjectType = type === SELECTION_TYPE_OBJECT;
        const selArr = selection;
        let rowStart = isObjectType ? selection.from.row : selArr[0];
        let columnStart = isObjectType ? selection.from.col : selArr[1];
        let rowEnd = isObjectType ? selection.to.row : selArr[2];
        let columnEnd = isObjectType ? selection.to.col : selArr[3];
        if (typeof propToCol === 'function') {
            if (typeof columnStart === 'string') {
                columnStart = propToCol(columnStart);
            }
            if (typeof columnEnd === 'string') {
                columnEnd = propToCol(columnEnd);
            }
        }
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isUndefined"])(rowEnd)) {
            rowEnd = rowStart;
        }
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isUndefined"])(columnEnd)) {
            columnEnd = columnStart;
        }
        // At this point columnStart/columnEnd are guaranteed numbers (strings were converted by propToCol above).
        let colStart = columnStart;
        let colEnd = columnEnd;
        if (!keepDirection) {
            const origRowStart = rowStart;
            const origRowEnd = rowEnd;
            const origColStart = colStart;
            const origColEnd = colEnd;
            rowStart = Math.min(origRowStart ?? 0, origRowEnd ?? 0);
            colStart = Math.min(origColStart ?? 0, origColEnd ?? 0);
            rowEnd = Math.max(origRowStart ?? 0, origRowEnd ?? 0);
            colEnd = Math.max(origColStart ?? 0, origColEnd ?? 0);
        }
        const highlight = isObjectType ? selection.highlight.clone() : createCellCoords(rowStart ?? 0, colStart ?? 0);
        const from = createCellCoords(rowStart ?? 0, colStart ?? 0);
        const to = createCellCoords(rowEnd ?? 0, colEnd ?? 0);
        return createCellRange(highlight, from, to);
    };
}
function transformSelectionToColumnDistance(hotInstance) {
    const selectionType = detectSelectionType(hotInstance.getSelected());
    if (selectionType === SELECTION_TYPE_UNRECOGNIZED || selectionType === SELECTION_TYPE_EMPTY) {
        return [];
    }
    const selectionSchemaNormalizer = normalizeSelectionFactory(selectionType, {
        createCellCoords: hotInstance._createCellCoords.bind(hotInstance),
        createCellRange: hotInstance._createCellRange.bind(hotInstance)
    });
    const unorderedIndexes = new Set();
    // Iterate through all ranges and collect all column indexes which are not saved yet.
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(hotInstance.getSelected() ?? [], (selection)=>{
        const { from, to } = selectionSchemaNormalizer(selection);
        const columnNonHeaderStart = Math.max(from.col ?? 0, 0);
        const amount = (to.col ?? 0) - columnNonHeaderStart + 1;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(Array.from(new Array(amount), (_, i)=>columnNonHeaderStart + i), (index)=>{
            if (!unorderedIndexes.has(index)) {
                unorderedIndexes.add(index);
            }
        });
    });
    // Sort indexes in ascending order to easily detecting non-consecutive columns.
    const orderedIndexes = Array.from(unorderedIndexes).sort((a, b)=>a - b);
    const normalizedColumnRanges = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayReduce"])(orderedIndexes, (acc, visualColumnIndex, index, array)=>{
        if (index !== 0 && visualColumnIndex === array[index - 1] + 1) {
            acc[acc.length - 1][1] += 1;
        } else {
            acc.push([
                visualColumnIndex,
                1
            ]);
        }
        return acc;
    }, []);
    return normalizedColumnRanges;
}
function transformSelectionToRowDistance(hotInstance) {
    const selectionType = detectSelectionType(hotInstance.getSelected());
    if (selectionType === SELECTION_TYPE_UNRECOGNIZED || selectionType === SELECTION_TYPE_EMPTY) {
        return [];
    }
    const selectionSchemaNormalizer = normalizeSelectionFactory(selectionType, {
        createCellCoords: hotInstance._createCellCoords.bind(hotInstance),
        createCellRange: hotInstance._createCellRange.bind(hotInstance)
    });
    const unorderedIndexes = new Set();
    // Iterate through all ranges and collect all column indexes which are not saved yet.
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(hotInstance.getSelected() ?? [], (selection)=>{
        const { from, to } = selectionSchemaNormalizer(selection);
        const rowNonHeaderStart = Math.max(from.row ?? 0, 0);
        const amount = (to.row ?? 0) - rowNonHeaderStart + 1;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayEach"])(Array.from(new Array(amount), (_, i)=>rowNonHeaderStart + i), (index)=>{
            if (!unorderedIndexes.has(index)) {
                unorderedIndexes.add(index);
            }
        });
    });
    // Sort indexes in ascending order to easily detecting non-consecutive columns.
    const orderedIndexes = Array.from(unorderedIndexes).sort((a, b)=>a - b);
    const normalizedRowRanges = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["arrayReduce"])(orderedIndexes, (acc, rowIndex, index, array)=>{
        if (index !== 0 && rowIndex === array[index - 1] + 1) {
            acc[acc.length - 1][1] += 1;
        } else {
            acc.push([
                rowIndex,
                1
            ]);
        }
        return acc;
    }, []);
    return normalizedRowRanges;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
]);

//# sourceMappingURL=node_modules_handsontable_selection_e2ed3b05.async.js.map