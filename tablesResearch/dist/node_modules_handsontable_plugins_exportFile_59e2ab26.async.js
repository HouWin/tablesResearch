(globalThis["TURBOPACK"] || (globalThis["TURBOPACK"] = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/handsontable/plugins/exportFile/contextMenuItem/exportItem.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>exportItem
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/console.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$i18n$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/i18n/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$exportFile$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/exportFile.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$contextMenuItem$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/contextMenuItem/utils.mjs [client] (ecmascript)");
;
;
;
;
function exportItem(exportFilePlugin) {
    return {
        key: 'export_file',
        name () {
            return this.getTranslatedPhrase(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$i18n$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CONTEXTMENU_ITEMS_EXPORT"]);
        },
        hidden () {
            return this.getSettings()[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$exportFile$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["PLUGIN_KEY"]] === undefined;
        },
        submenu: {
            items: [
                {
                    key: 'export_file:csv',
                    name () {
                        return this.getTranslatedPhrase(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$i18n$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CONTEXTMENU_ITEMS_EXPORT_FILE_CSV"]);
                    },
                    callback () {
                        exportFilePlugin.downloadFile('csv', (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$contextMenuItem$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getExportOptions"])(this));
                    },
                    disabled: false
                },
                {
                    key: 'export_file:xlsx',
                    name () {
                        return this.getTranslatedPhrase(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$i18n$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["CONTEXTMENU_ITEMS_EXPORT_FILE_XLSX"]);
                    },
                    callback () {
                        exportFilePlugin.downloadFileAsync('xlsx', (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$contextMenuItem$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getExportOptions"])(this)).catch((err)=>{
                            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$console$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["error"])('ExportFile: XLSX export failed.', err);
                        });
                    },
                    hidden () {
                        return !exportFilePlugin.supportsExportFormat('xlsx');
                    }
                }
            ]
        }
    };
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/contextMenuItem/utils.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Derives export options from the current Handsontable selection and settings.
 *
 * Always reflects the visible header state (`colHeaders`, `rowHeaders`).
 * Returns an options object with a `range` property when the selection spans
 * more than one cell. Omits `range` (full-table export) when:
 * - there is no selection,
 * - only a single cell is focused (cursor, no drag),
 * - the corner was clicked (select-all).
 *
 * Row header selections (entire rows) and column header selections (entire
 * columns) are treated as multi-cell selections — only the selected rows or
 * columns are exported. Negative coordinates from headers are clamped to 0.
 *
 * @param {object} hot Handsontable instance (`this` inside a menu item callback).
 * @returns {object}
 */ __turbopack_context__.s([
    "getExportOptions",
    ()=>getExportOptions
]);
function getExportOptions(hot) {
    const settings = hot.getSettings();
    const colHeaders = !!(settings.colHeaders || settings.nestedHeaders);
    const rowHeaders = !!settings.rowHeaders;
    const range = hot.getSelectedRangeLast();
    const opts = {
        colHeaders,
        rowHeaders
    };
    // No selection, single-cell cursor, or corner (select-all) → export entire table.
    if (!range || range.isSingleCell() || (range.from.row ?? 0) < 0 && (range.from.col ?? 0) < 0) {
        return opts;
    }
    return {
        ...opts,
        range: [
            Math.max(0, Math.min(range.from.row ?? 0, range.to.row ?? 0)),
            Math.max(0, Math.min(range.from.col ?? 0, range.to.col ?? 0)),
            Math.max(range.from.row ?? 0, range.to.row ?? 0),
            Math.max(range.from.col ?? 0, range.to.col ?? 0)
        ]
    };
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/dataProvider.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
/**
 * @private
 */ class DataProvider {
    /**
   * Set options for data provider.
   *
   * @param {object} options Object with specified options.
   */ setOptions(options) {
        if (options && 'columnHeaders' in options && !('colHeaders' in options)) {
            this.options = {
                ...options,
                colHeaders: options.columnHeaders
            };
            return;
        }
        this.options = options;
    }
    /**
   * Builds a 2D array by calling `getCellValue(rowIndex, colIndex)` for every visible
   * cell in the export range, honouring hidden-row and hidden-column exclusions.
   *
   * @private
   * @param {Function} getCellValue Called for each visible cell; its return value is
   *   pushed into the row array.
   * @returns {Array[]}
   */ // eslint-disable-next-line @typescript-eslint/no-explicit-any
    _extractDataMatrix(getCellValue) {
        const { startRow, startCol, endRow, endCol } = this._getDataRange();
        const options = this.options;
        const data = [];
        for(let rowIndex = startRow; rowIndex <= endRow; rowIndex++){
            if (options.exportHiddenRows === false && this._isHiddenRow(rowIndex)) {
                continue;
            }
            const row = [];
            for(let colIndex = startCol; colIndex <= endCol; colIndex++){
                if (options.exportHiddenColumns === false && this._isHiddenColumn(colIndex)) {
                    continue;
                }
                row.push(getCellValue(rowIndex, colIndex));
            }
            data.push(row);
        }
        return data;
    }
    /**
   * Get table data based on provided settings to the class constructor.
   *
   * @returns {Array}
   */ getData() {
        return this._extractDataMatrix((row, col)=>this.hot.getDataAtCell(row, col));
    }
    /**
   * Gets list of row headers.
   *
   * @returns {Array}
   */ getRowHeaders() {
        const headers = [];
        if (this.options.rowHeaders) {
            const { startRow, endRow } = this._getDataRange();
            const rowHeaders = this.hot.getRowHeader();
            for(let row = startRow; row <= endRow; row++){
                if (this.options.exportHiddenRows === false && this._isHiddenRow(row)) {
                    continue;
                }
                headers.push(rowHeaders[row]);
            }
        }
        return headers;
    }
    /**
   * Gets list of columns headers.
   *
   * @returns {Array}
   */ getColumnHeaders() {
        const headers = [];
        if (this.options.colHeaders) {
            const { startCol, endCol } = this._getDataRange();
            const colHeaders = this.hot.getColHeader();
            for(let column = startCol; column <= endCol; column++){
                if (this.options.exportHiddenColumns === false && this._isHiddenColumn(column)) {
                    continue;
                }
                headers.push(colHeaders[column]);
            }
        }
        return headers;
    }
    /**
   * Get data range object based on settings provided in the class constructor.
   *
   * @private
   * @returns {object} Returns object with keys `startRow`, `startCol`, `endRow` and `endCol`.
   */ _getDataRange() {
        const cols = this.hot.countCols() - 1;
        const rows = this.hot.countRows() - 1;
        let [startRow = 0, startCol = 0, endRow = rows, endCol = cols] = this.options.range;
        startRow = Math.max(startRow, 0);
        startCol = Math.max(startCol, 0);
        endRow = Math.min(endRow, rows);
        endCol = Math.min(endCol, cols);
        return {
            startRow,
            startCol,
            endRow,
            endCol
        };
    }
    /**
   * Get raw source data (formulas) based on provided settings to the class constructor.
   *
   * Mirrors {@link DataProvider#getData} but uses `getSourceDataAtCell` so that cells
   * containing HyperFormula formulas return the raw formula string (e.g. `'=SUM(A1:A3)'`)
   * rather than the calculated value.
   *
   * @returns {Array}
   */ getSourceData() {
        return this._extractDataMatrix((row, col)=>this.hot.getSourceDataAtCell(row, col));
    }
    /**
   * Returns the set of physical HOT row indices that are hidden and excluded from the
   * exported data matrix (i.e. `exportHiddenRows` is `false` and the row is hidden).
   *
   * Returns an empty set when `exportHiddenRows` is `true` or `'hide'`, because in
   * those cases all rows are included in the data matrix and no formula-offset
   * adjustment is needed.
   *
   * Used by the formula normalizer to adjust per-reference row offsets when building
   * live Excel formulas from HyperFormula formula strings.
   *
   * @returns {Set<number>}
   */ getExcludedHiddenRows() {
        const result = new Set();
        if (this.options.exportHiddenRows !== false) {
            return result;
        }
        const { startRow, endRow } = this._getDataRange();
        for(let rowIndex = startRow; rowIndex <= endRow; rowIndex++){
            if (this._isHiddenRow(rowIndex)) {
                result.add(this.hot.toPhysicalRow(rowIndex));
            }
        }
        return result;
    }
    /**
   * Returns the set of physical HOT column indices that are hidden and excluded from the
   * exported data matrix (i.e. `exportHiddenColumns` is `false` and the column is hidden).
   *
   * Returns an empty set when `exportHiddenColumns` is `true` or `'hide'`, because in
   * those cases all columns are included in the data matrix and no formula-offset
   * adjustment is needed.
   *
   * Used by the formula normalizer to adjust per-reference column offsets when building
   * live Excel formulas from HyperFormula formula strings.
   *
   * @returns {Set<number>}
   */ getExcludedHiddenColumns() {
        const result = new Set();
        if (this.options.exportHiddenColumns !== false) {
            return result;
        }
        const { startCol, endCol } = this._getDataRange();
        for(let colIndex = startCol; colIndex <= endCol; colIndex++){
            if (this._isHiddenColumn(colIndex)) {
                result.add(this.hot.toPhysicalColumn(colIndex));
            }
        }
        return result;
    }
    /**
   * Returns the 0-based data-array row indices for rows that are hidden in
   * Handsontable and are included in the exported data matrix.
   *
   * Only returns a non-empty array when `exportHiddenRows` is `'hide'`.
   * When `true`, all rows are visible in Excel. When `false`, hidden rows
   * are omitted entirely and there is nothing to mark as hidden.
   *
   * @returns {number[]}
   */ getHiddenRowDataIndices() {
        if (this.options.exportHiddenRows !== 'hide') {
            return [];
        }
        const { startRow, endRow } = this._getDataRange();
        const result = [];
        let dataIndex = 0;
        for(let rowIndex = startRow; rowIndex <= endRow; rowIndex++){
            if (this._isHiddenRow(rowIndex)) {
                result.push(dataIndex);
            }
            dataIndex += 1;
        }
        return result;
    }
    /**
   * Returns the 0-based data-array column indices for columns that are hidden in
   * Handsontable and are included in the exported data matrix.
   *
   * Only returns a non-empty array when `exportHiddenColumns` is `'hide'`.
   * When `true`, all columns are visible in Excel. When `false`, hidden columns
   * are omitted entirely and there is nothing to mark as hidden.
   *
   * @returns {number[]}
   */ getHiddenColumnDataIndices() {
        if (this.options.exportHiddenColumns !== 'hide') {
            return [];
        }
        const { startCol, endCol } = this._getDataRange();
        const result = [];
        let dataIndex = 0;
        for(let colIndex = startCol; colIndex <= endCol; colIndex++){
            if (this._isHiddenColumn(colIndex)) {
                result.push(dataIndex);
            }
            dataIndex += 1;
        }
        return result;
    }
    /**
   * Gets the function argument separator used by the HyperFormula engine, if active.
   *
   * Returns `','` when the Formulas plugin is absent or disabled, since that is the
   * separator OOXML (Excel) always expects.
   *
   * @returns {string}
   */ getFormulasSeparator() {
        const formulasPlugin = this.hot.getPlugin('formulas');
        if (!formulasPlugin || !formulasPlugin.isEnabled()) {
            return ',';
        }
        return formulasPlugin.engine?.getConfig()?.functionArgSeparator ?? ',';
    }
    /**
   * Gets cell meta for all cells in the export range.
   *
   * Returns a 2D array of cell meta objects with the same structure as {@link DataProvider#getData}.
   * Each entry corresponds to the cell at the same position in the data array.
   *
   * @returns {Array}
   */ getCellsMeta() {
        return this._extractDataMatrix((row, col)=>this.hot.getCellMeta(row, col));
    }
    /**
   * Gets rendered DOM elements for all cells in the export range.
   *
   * Returns a 2D array of `HTMLElement | null` values with the same structure as
   * {@link DataProvider#getCellsMeta}. An entry is `null` when the cell is outside
   * the rendered viewport (virtualised grid).
   *
   * @returns {Array}
   */ getCellElements() {
        return this._extractDataMatrix((row, col)=>this.hot.getCell(row, col));
    }
    /**
   * Gets the merged cells configuration filtered to the current export range.
   *
   * Returned coordinates are 0-based data-array indices (matching the same coordinate
   * space as {@link DataProvider#getData}). Hidden rows/columns excluded from the export
   * are accounted for: the row/col offsets are compressed and the rowspan/colspan values
   * are reduced to cover only the exported cells within the merge span.
   *
   * Merges whose top-left cell is excluded, or that collapse to a single cell after
   * exclusion, are omitted from the result.
   *
   * @returns {Array}
   */ getMergeCells() {
        const { startRow, startCol, endRow, endCol } = this._getDataRange();
        const mergeCellsPlugin = this.hot.getPlugin('mergeCells');
        if (!mergeCellsPlugin || !mergeCellsPlugin.isEnabled()) {
            return [];
        }
        const mergedCells = mergeCellsPlugin.mergedCellsCollection.mergedCells;
        const result = [];
        const excludeHiddenRows = this.options.exportHiddenRows === false;
        const excludeHiddenCols = this.options.exportHiddenColumns === false;
        const rowIncluded = (r)=>!excludeHiddenRows || !this._isHiddenRow(r);
        const colIncluded = (c)=>!excludeHiddenCols || !this._isHiddenColumn(c);
        mergedCells.forEach((merge)=>{
            const mergeEndRow = merge.row + merge.rowspan - 1;
            const mergeEndCol = merge.col + merge.colspan - 1;
            // Skip merges fully outside the export range.
            if (merge.row < startRow || mergeEndRow > endRow || merge.col < startCol || mergeEndCol > endCol) {
                return;
            }
            // Skip merges whose top-left cell is excluded.
            if (!rowIncluded(merge.row) || !colIncluded(merge.col)) {
                return;
            }
            // Data-array position of the merge's top-left cell.
            const dataRow = this._countVisibleBefore(merge.row, startRow, rowIncluded);
            const dataCol = this._countVisibleBefore(merge.col, startCol, colIncluded);
            // Effective span after removing excluded rows/columns inside the merge.
            const dataRowspan = this._countVisibleBefore(mergeEndRow + 1, merge.row, rowIncluded);
            const dataColspan = this._countVisibleBefore(mergeEndCol + 1, merge.col, colIncluded);
            // A 1×1 result is not a merge.
            if (dataRowspan <= 1 && dataColspan <= 1) {
                return;
            }
            result.push({
                row: dataRow,
                col: dataCol,
                rowspan: dataRowspan,
                colspan: dataColspan
            });
        });
        return result;
    }
    /**
   * Gets widths (in pixels) for all visible columns in the export range.
   *
   * Returns values in the same column order as {@link DataProvider#getData}.
   *
   * @returns {Array}
   */ getColumnsWidths() {
        const { startCol, endCol } = this._getDataRange();
        const options = this.options;
        const widths = [];
        for(let colIndex = startCol; colIndex <= endCol; colIndex++){
            if (options.exportHiddenColumns === false && this._isHiddenColumn(colIndex)) {
                continue;
            }
            // `getColWidth()` returns 0 for hidden columns (the HiddenColumns plugin
            // overrides `modifyColWidth` to zero out the rendered width). Use the
            // configured width from column meta so the Excel column has a usable size
            // when the user unhides it.
            const width = this._isHiddenColumn(colIndex) ? this._getNaturalColWidth(colIndex) : this.hot.getColWidth(colIndex);
            widths.push(width);
        }
        return widths;
    }
    /**
   * Gets heights (in pixels) for all visible rows in the export range.
   *
   * Returns values in the same row order as {@link DataProvider#getData}.
   *
   * @returns {Array}
   */ getRowsHeights() {
        const { startRow, endRow } = this._getDataRange();
        const options = this.options;
        const heights = [];
        for(let rowIndex = startRow; rowIndex <= endRow; rowIndex++){
            if (options.exportHiddenRows === false && this._isHiddenRow(rowIndex)) {
                continue;
            }
            // `getRowHeight()` returns 0 for hidden rows (the HiddenRows plugin
            // overrides `modifyRowHeight` to zero out the rendered height). Use the
            // configured height from settings so the Excel row has a usable size
            // when the user unhides it.
            const height = this._isHiddenRow(rowIndex) ? this._getNaturalRowHeight(rowIndex) : this.hot.getRowHeight(rowIndex);
            heights.push(height);
        }
        return heights;
    }
    /**
   * Gets the number of frozen rows (`fixedRowsTop` setting).
   *
   * @returns {number}
   */ getFrozenRows() {
        return this.hot.getSettings().fixedRowsTop || 0;
    }
    /**
   * Gets the number of frozen columns (`fixedColumnsStart` setting).
   *
   * @returns {number}
   */ getFrozenColumns() {
        return this.hot.getSettings().fixedColumnsStart || 0;
    }
    /**
   * Gets nested column header data from the NestedHeaders plugin for the current export range.
   *
   * Returns `null` when the NestedHeaders plugin is not enabled. Otherwise returns an array
   * of layers, where each layer is an array of `{ label, colspan, className }` objects
   * representing the visible column headers in that layer. Hidden columns (when
   * `exportHiddenColumns` is `false`) are excluded and their colspan contribution is removed
   * from spanning headers.
   *
   * @returns {Array[]|null}
   */ getNestedColumnHeaders() {
        const nestedHeadersPlugin = this.hot.getPlugin('nestedHeaders');
        if (!nestedHeadersPlugin || !nestedHeadersPlugin.isEnabled()) {
            return null;
        }
        const layersCount = nestedHeadersPlugin.getLayersCount();
        const { startCol, endCol } = this._getDataRange();
        const options = this.options;
        const includeHidden = !!options.exportHiddenColumns;
        const layers = [];
        for(let layer = 0; layer < layersCount; layer++){
            const layerHeaders = [];
            let col = startCol;
            while(col <= endCol){
                if (!includeHidden && this._isHiddenColumn(col)) {
                    col += 1;
                    continue;
                }
                if (includeHidden) {
                    col = this._appendNestedHeaderWithHidden(nestedHeadersPlugin, layer, col, endCol, layerHeaders);
                } else {
                    col = this._appendNestedHeaderWithoutHidden(nestedHeadersPlugin, layer, col, endCol, layerHeaders);
                }
            }
            layers.push(layerHeaders);
        }
        return layers;
    }
    /**
   * Appends a nested header entry for a single column when hidden columns are included in the export.
   *
   * @param {object} nestedHeadersPlugin The NestedHeaders plugin instance.
   * @param {number} layer The header layer index.
   * @param {number} col The current column index.
   * @param {number} endCol The last column index of the export range.
   * @param {Array} layerHeaders The array to push the header entry into.
   * @returns {number} The next column index to process.
   */ _appendNestedHeaderWithHidden(nestedHeadersPlugin, layer, col, endCol, layerHeaders) {
        // When including hidden columns, the NestedHeaders state matrix has been
        // modified by the HiddenColumns plugin: hidden span roots are replaced with
        // placeholders and their colspan is transferred to the next visible column.
        // Use the tree node data instead, which preserves the original columnIndex
        // and origColspan regardless of the hidden state.
        const treeNodeData = nestedHeadersPlugin.getStateManager().getHeaderTreeNodeData(layer, col);
        if (!treeNodeData || treeNodeData.columnIndex === col) {
            // This column is the root of its span (or has no tree node → single column).
            const origColspan = treeNodeData?.origColspan ?? 1;
            // Clamp to the export range end.
            const effectiveColspan = Math.min(origColspan, endCol - col + 1);
            // headerClassNames is an array of strings produced by the settings normalizer
            // from the user-supplied `headerClassName` string.
            const className = (treeNodeData?.headerClassNames ?? []).join(' ');
            layerHeaders.push({
                label: treeNodeData?.label ?? '',
                colspan: effectiveColspan,
                className
            });
            return col + origColspan;
        }
        // This column is inside a span whose root is before startCol. Emit a
        // single-column placeholder to keep the column count correct.
        layerHeaders.push({
            label: '',
            colspan: 1,
            className: ''
        });
        return col + 1;
    }
    /**
   * Appends a nested header entry for a single column when hidden columns are excluded from the export.
   *
   * @param {object} nestedHeadersPlugin The NestedHeaders plugin instance.
   * @param {number} layer The header layer index.
   * @param {number} col The current column index.
   * @param {number} endCol The last column index of the export range.
   * @param {Array} layerHeaders The array to push the header entry into.
   * @returns {number} The next column index to process.
   */ _appendNestedHeaderWithoutHidden(nestedHeadersPlugin, layer, col, endCol, layerHeaders) {
        const settings = nestedHeadersPlugin.getHeaderSettings(layer, col);
        if (!settings || settings.isRoot) {
            const spanColspan = settings?.colspan ?? 1;
            let visibleColspan = 0;
            for(let spanCol = col; spanCol < col + spanColspan && spanCol <= endCol; spanCol++){
                if (!this._isHiddenColumn(spanCol)) {
                    visibleColspan += 1;
                }
            }
            // headerClassNames is an array of strings produced by the settings normalizer
            // from the user-supplied `headerClassName` string.
            const className = (settings?.headerClassNames ?? []).join(' ');
            layerHeaders.push({
                label: settings?.label ?? '',
                colspan: visibleColspan,
                className
            });
            return col + spanColspan;
        }
        // Continuation cell of a span that started at an earlier column.
        // This can only occur when startCol falls inside a span — emit a
        // single-column placeholder so the export column count stays correct.
        layerHeaders.push({
            label: '',
            colspan: 1,
            className: ''
        });
        return col + 1;
    }
    /**
   * Gets `headerClassName` values for all visible column headers in the export range.
   *
   * Returns values in the same column order as {@link DataProvider#getColumnHeaders}.
   * An empty string is returned for columns that have no `headerClassName` configured.
   *
   * @returns {string[]}
   */ getColumnHeadersClassNames() {
        if (!this.options.colHeaders) {
            return [];
        }
        const { startCol, endCol } = this._getDataRange();
        const options = this.options;
        const classNames = [];
        for(let col = startCol; col <= endCol; col++){
            if (options.exportHiddenColumns === false && this._isHiddenColumn(col)) {
                continue;
            }
            const headerClassName = this.hot.getColumnMeta(col).headerClassName;
            let resolvedClassName = '';
            if (Array.isArray(headerClassName)) {
                resolvedClassName = headerClassName.join(' ');
            } else if (typeof headerClassName === 'string') {
                resolvedClassName = headerClassName;
            }
            classNames.push(resolvedClassName);
        }
        return classNames;
    }
    /**
   * Gets the layout direction of the Handsontable instance.
   *
   * @returns {'ltr'|'rtl'}
   */ getLayoutDirection() {
        return this.hot.getSettings().layoutDirection === 'rtl' ? 'rtl' : 'ltr';
    }
    /**
   * Gets column summary endpoint descriptors from the ColumnSummary plugin, translated
   * into the coordinate space of the exported data array.
   *
   * Returns an empty array when the plugin is absent or disabled.
   * Each descriptor contains:
   *
   * - `destRow` / `destCol` — 0-based indices into `getData()` identifying the destination cell.
   * - `type` — lowercase summary type (`'sum'`, `'min'`, `'max'`, `'count'`, `'average'`, `'custom'`).
   * - `sourceCol` — 0-based data-array column index of the source column.
   * - `sourceRanges` — array of `[startDataRow, endDataRow]` pairs (inclusive, 0-based) covering
   *   the source rows after hidden-row exclusion and range clamping.
   *
   * @returns {Array}
   */ getColumnSummaries() {
        const plugin = this.hot.getPlugin('columnSummary');
        if (!plugin || !plugin.isEnabled()) {
            return [];
        }
        const { startRow, startCol, endRow, endCol } = this._getDataRange();
        const allEndpoints = plugin.endpoints.getAllEndpoints();
        // First pass: collect all destination data-row indices so we can exclude every
        // summary destination from every formula's source range.  Without this, multiple
        // summary rows would reference each other and create circular references in Excel
        // (e.g. SUM at row 6 references MIN at row 7, and MIN at row 7 references SUM at row 6).
        const allDestRows = new Set();
        allEndpoints.forEach((endpoint)=>{
            if (endpoint.destinationRow === undefined) {
                return;
            }
            const destRow = this._physicalRowToDataIndex(endpoint.destinationRow, startRow, endRow);
            if (destRow !== null) {
                allDestRows.add(destRow);
            }
        });
        // Second pass: translate each endpoint into an export-coordinate summary descriptor.
        const summaries = [];
        allEndpoints.forEach((endpoint)=>{
            const summary = this._transformEndpointToSummary(endpoint, startRow, endRow, startCol, endCol, allDestRows);
            if (summary !== null) {
                summaries.push(summary);
            }
        });
        return summaries;
    }
    /**
   * Translates a single ColumnSummary endpoint into an export-coordinate summary descriptor.
   *
   * Converts physical row/column indices to 0-based data-array positions, builds the
   * compacted source-range array (merging consecutive indices), and excludes all
   * destination rows supplied in `allDestRows` to prevent circular Excel formula chains.
   *
   * Returns `null` when the endpoint falls outside the export range or produces no
   * valid source rows after filtering.
   *
   * @private
   * @param {object} endpoint ColumnSummary endpoint object from `getAllEndpoints()`.
   * @param {number} startRow First visual row of the export range.
   * @param {number} endRow Last visual row of the export range.
   * @param {number} startCol First visual column of the export range.
   * @param {number} endCol Last visual column of the export range.
   * @param {Set<number>} allDestRows Data-array row indices of all summary destinations.
   * @returns {object|null}
   */ _transformEndpointToSummary(endpoint, startRow, endRow, startCol, endCol, allDestRows) {
        if (endpoint.destinationRow === undefined || endpoint.destinationColumn === undefined) {
            return null;
        }
        const destRow = this._physicalRowToDataIndex(endpoint.destinationRow, startRow, endRow);
        const destCol = this._physicalColToDataIndex(endpoint.destinationColumn, startCol, endCol);
        if (destRow === null || destCol === null) {
            return null;
        }
        const physSourceCol = endpoint.sourceColumn ?? endpoint.destinationColumn;
        const sourceCol = this._physicalColToDataIndex(physSourceCol, startCol, endCol);
        if (sourceCol === null) {
            return null;
        }
        // Convert physical row ranges to sequential data-row-index ranges, merging
        // consecutive indices so the resulting array stays compact.
        const physRanges = endpoint.ranges || [
            [
                0,
                this.hot.countRows() - 1
            ]
        ];
        const sourceRanges = [];
        physRanges.forEach((range)=>{
            const physStart = range[0];
            const physEnd = range[1] !== undefined ? range[1] : range[0];
            for(let physRow = physStart; physRow <= physEnd; physRow++){
                const dataIdx = this._physicalRowToDataIndex(physRow, startRow, endRow);
                if (dataIdx === null || allDestRows.has(dataIdx)) {
                    continue;
                }
                if (sourceRanges.length > 0) {
                    const lastRange = sourceRanges[sourceRanges.length - 1];
                    if (lastRange[1] + 1 === dataIdx) {
                        lastRange[1] = dataIdx; // extend existing range
                        continue;
                    }
                }
                sourceRanges.push([
                    dataIdx,
                    dataIdx
                ]);
            }
        });
        if (sourceRanges.length === 0) {
            return null;
        }
        return {
            destRow,
            destCol,
            type: String(endpoint.type || 'sum').toLowerCase(),
            sourceCol,
            sourceRanges
        };
    }
    /**
   * Counts the number of indices in `[start, end)` for which `isIncluded` returns `true`.
   *
   * Used by {@link DataProvider#_physicalRowToDataIndex} and
   * {@link DataProvider#_physicalColToDataIndex} to compute the 0-based data-array
   * position of a visual index by counting the visible indices that precede it.
   *
   * @private
   * @param {number} end Exclusive upper bound (the target visual index).
   * @param {number} start Inclusive lower bound of the export range.
   * @param {Function} isIncluded Returns `true` for indices that appear in the exported data.
   * @returns {number}
   */ _countVisibleBefore(end, start, isIncluded) {
        let count = 0;
        for(let i = start; i < end; i++){
            if (isIncluded(i)) {
                count += 1;
            }
        }
        return count;
    }
    /**
   * Converts a physical row index to a 0-based index into the exported data array.
   *
   * Returns `null` if the row is outside the export range, is hidden and hidden rows
   * are not being exported, or has no visual equivalent.
   *
   * @private
   * @param {number} physRow Physical row index.
   * @param {number} startRow First visual row of the export range.
   * @param {number} endRow Last visual row of the export range.
   * @returns {number|null}
   */ _physicalRowToDataIndex(physRow, startRow, endRow) {
        const visualRow = this.hot.toVisualRow(physRow);
        if (visualRow === null || visualRow < startRow || visualRow > endRow) {
            return null;
        }
        if (this.options.exportHiddenRows === false && this._isHiddenRow(visualRow)) {
            return null;
        }
        return this._countVisibleBefore(visualRow, startRow, (r)=>this.options.exportHiddenRows !== false || !this._isHiddenRow(r));
    }
    /**
   * Converts a physical column index to a 0-based index into the exported data array.
   *
   * Returns `null` if the column is outside the export range, is hidden and hidden
   * columns are not being exported, or has no visual equivalent.
   *
   * @private
   * @param {number} physCol Physical column index.
   * @param {number} startCol First visual column of the export range.
   * @param {number} endCol Last visual column of the export range.
   * @returns {number|null}
   */ _physicalColToDataIndex(physCol, startCol, endCol) {
        const visualCol = this.hot.toVisualColumn(physCol);
        if (visualCol === null || visualCol < startCol || visualCol > endCol) {
            return null;
        }
        if (this.options.exportHiddenColumns === false && this._isHiddenColumn(visualCol)) {
            return null;
        }
        return this._countVisibleBefore(visualCol, startCol, (c)=>this.options.exportHiddenColumns !== false || !this._isHiddenColumn(c));
    }
    /**
   * Returns the natural (pre-hiding) width in pixels for a hidden column.
   *
   * `getColWidth()` returns 0 for hidden columns because the HiddenColumns plugin
   * overrides the `modifyColWidth` hook. This method reads the configured width
   * from column meta or the `colWidths` setting, bypassing that hook.
   *
   * @private
   * @param {number} colIndex Visual column index.
   * @returns {number}
   */ _getNaturalColWidth(colIndex) {
        const settings = this.hot.getSettings();
        const metaWidth = this.hot.getColumnMeta(colIndex).width;
        if (metaWidth !== null && metaWidth !== undefined) {
            return metaWidth;
        }
        const { colWidths, defaultColumnWidth } = settings;
        const fallback = defaultColumnWidth ?? 50;
        if (Array.isArray(colWidths)) {
            return colWidths[colIndex] ?? fallback;
        } else if (typeof colWidths === 'function') {
            return colWidths(colIndex) ?? fallback;
        } else if (typeof colWidths === 'number') {
            return colWidths;
        }
        return fallback;
    }
    /**
   * Returns the natural (pre-hiding) height in pixels for a hidden row.
   *
   * `getRowHeight()` returns 0 for hidden rows because the HiddenRows plugin
   * overrides the `modifyRowHeight` hook. This method reads the configured height
   * directly from the `rowHeights` setting, bypassing that hook.
   *
   * @private
   * @param {number} rowIndex Visual row index.
   * @returns {number}
   */ _getNaturalRowHeight(rowIndex) {
        const settings = this.hot.getSettings();
        const { rowHeights, defaultRowHeight } = settings;
        const fallback = defaultRowHeight ?? 23;
        if (Array.isArray(rowHeights)) {
            return rowHeights[rowIndex] ?? fallback;
        } else if (typeof rowHeights === 'function') {
            return rowHeights(rowIndex) ?? fallback;
        } else if (typeof rowHeights === 'number') {
            return rowHeights;
        }
        return fallback;
    }
    /**
   * Check if row at specified row index is hidden.
   *
   * @private
   * @param {number} row Row index.
   * @returns {boolean}
   */ _isHiddenRow(row) {
        return this.hot.rowIndexMapper.isHidden(this.hot.toPhysicalRow(row));
    }
    /**
   * Check if column at specified column index is hidden.
   *
   * @private
   * @param {number} column Visual column index.
   * @returns {boolean}
   */ _isHiddenColumn(column) {
        return this.hot.columnIndexMapper.isHidden(this.hot.toPhysicalColumn(column));
    }
    /**
   * Initializes the data provider with a reference to the Handsontable instance used to retrieve data for export.
   */ constructor(hotInstance){
        /**
   * Format type class options.
   *
   * @type {object}
   */ this.options = {};
        this.hot = hotInstance;
    }
}
var __TURBOPACK__default__export__ = DataProvider;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/exportFile.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ExportFile",
    ()=>ExportFile,
    "PLUGIN_KEY",
    ()=>PLUGIN_KEY,
    "PLUGIN_PRIORITY",
    ()=>PLUGIN_PRIORITY
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$base$2f$base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/base/base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$i18n$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/i18n/constants.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$dataProvider$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/dataProvider.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$typeFactory$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/typeFactory.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$contextMenuItem$2f$exportItem$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/contextMenuItem/exportItem.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/utils.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_apply_descriptor_get(receiver, descriptor) {
    if (descriptor.get) {
        return descriptor.get.call(receiver);
    }
    return descriptor.value;
}
function _class_apply_descriptor_set(receiver, descriptor, value) {
    if (descriptor.set) {
        descriptor.set.call(receiver, value);
    } else {
        if (!descriptor.writable) {
            throw new TypeError("attempted to set read only private field");
        }
        descriptor.value = value;
    }
}
function _class_extract_field_descriptor(receiver, privateMap, action) {
    if (!privateMap.has(receiver)) {
        throw new TypeError("attempted to " + action + " private field on non-instance");
    }
    return privateMap.get(receiver);
}
function _class_private_field_get(receiver, privateMap) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "get");
    return _class_apply_descriptor_get(receiver, descriptor);
}
function _class_private_field_init(obj, privateMap, value) {
    _check_private_redeclaration(obj, privateMap);
    privateMap.set(obj, value);
}
function _class_private_field_set(receiver, privateMap, value) {
    var descriptor = _class_extract_field_descriptor(receiver, privateMap, "set");
    _class_apply_descriptor_set(receiver, descriptor, value);
    return value;
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
;
;
;
;
;
;
const PLUGIN_KEY = 'exportFile';
const PLUGIN_PRIORITY = 240;
/**
 * Local type-guard narrowing `unknown` to `ExportFileSettings`.
 * `isObject` is intentionally non-narrowing in the public API, so we wrap it here.
 *
 * @param {unknown} v The value to check.
 * @returns {boolean}
 */ function isExportFileSettings(v) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isObject"])(v);
}
/**
 * Type guard returning the user-provided `exportFile` settings when they are an object.
 *
 * @param {unknown} settings Raw value read from `hot.getSettings()[PLUGIN_KEY]`.
 * @returns {ExportFileSettings|undefined}
 */ function getPluginSettings(settings) {
    return isExportFileSettings(settings) ? settings : undefined;
}
var /**
   * Add export options to the Context Menu.
   *
   * @param {object} options Contains default added options of the Context Menu.
   */ _onAfterContextMenuDefaultOptions = /*#__PURE__*/ new WeakMap(), /**
   * Triggers a browser file download for the given blob.
   *
   * @param {Blob} blob The blob to download.
   * @param {string} name The filename (including extension).
   */ _triggerDownload = /*#__PURE__*/ new WeakSet();
class ExportFile extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$base$2f$base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["BasePlugin"] {
    /**
   * Returns the plugin key used to identify this plugin in Handsontable settings.
   */ static get PLUGIN_KEY() {
        return PLUGIN_KEY;
    }
    /**
   * Returns the priority order used to determine the order in which plugins are initialized.
   */ static get PLUGIN_PRIORITY() {
        return PLUGIN_PRIORITY;
    }
    /**
   * Returns the setting keys that trigger a plugin update when changed via `updateSettings`.
   */ static get SETTING_KEYS() {
        return [
            PLUGIN_KEY
        ];
    }
    /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ExportFile#enablePlugin} method is called.
   *
   * @returns {boolean}
   */ isEnabled() {
        return true;
    }
    /**
   * Enables the plugin functionality for this Handsontable instance.
   */ enablePlugin() {
        if (this.enabled) {
            return;
        }
        this.addHook('afterContextMenuDefaultOptions', _class_private_field_get(this, _onAfterContextMenuDefaultOptions));
        super.enablePlugin();
    }
    /**
   * Disables the plugin functionality for this Handsontable instance.
   */ disablePlugin() {
        super.disablePlugin();
    }
    /**
   * Exports table data as a string.
   *
   * This method is only supported for text-based formats such as CSV.
   * Calling it with a binary format (e.g. `'xlsx'`) throws an error.
   *
   * @param {string} format Export format type eq. `'csv'`.
   * @param {object} options Export options.
   * @param {string} [options.mimeType] MIME type (e.g. `'text/csv'` for CSV). Default depends on format.
   * @param {string} [options.fileExtension] File extension (e.g. `'csv'`). Default depends on format.
   * @param {string} [options.filename='Handsontable [YYYY]-[MM]-[DD]'] File name. Placeholders `[YYYY]`, `[MM]`, `[DD]` are replaced with the current date.
   * @param {string} [options.encoding='utf-8'] Character encoding.
   * @param {boolean} [options.bom] Include BOM signature. Default depends on format (e.g. `true` for CSV).
   * @param {string} [options.columnDelimiter=','] Column delimiter (CSV only).
   * @param {string} [options.rowDelimiter='\r\n'] Row delimiter (CSV only).
   * @param {boolean} [options.colHeaders=false] Include column headers in the exported file.
   * @param {boolean} [options.rowHeaders=false] Include row headers in the exported file.
   * @param {boolean|string} [options.exportHiddenColumns=false] Controls how hidden columns are handled. `true` exports them as normal visible columns. `false` omits them entirely. `'hide'` exports them and marks them as hidden in Excel (XLSX only).
   * @param {boolean|string} [options.exportHiddenRows=false] Controls how hidden rows are handled. `true` exports them as normal visible rows. `false` omits them entirely. `'hide'` exports them and marks them as hidden in Excel (XLSX only).
   * @param {number[]} [options.range=[]] Cell range to export: `[startRow, startColumn, endRow, endColumn]` (visual indexes).
   * @param {boolean|RegExp|Function} [options.sanitizeValues=false] Controls the sanitization of cell values (CSV only).
   * @returns {string}
   */ exportAsString(format, options = {}) {
        const formatter = this._createTypeFormatter(format, options);
        if (formatter.binary) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])(`Export format type "${format}" cannot be exported as a string (binary format).`);
        }
        return formatter.export();
    }
    /**
   * Exports table data as a blob object.
   *
   * This method is only supported for text-based formats such as CSV.
   * Calling it with a binary format (e.g. `'xlsx'`) throws an error — use
   * [[ExportFile#exportAsBlobAsync]] instead.
   *
   * @param {string} format Export format type e.g. `'csv'`.
   * @param {object} options Export options.
   * @param {string} [options.mimeType] MIME type. Default depends on format.
   * @param {string} [options.fileExtension] File extension. Default depends on format.
   * @param {string} [options.filename='Handsontable [YYYY]-[MM]-[DD]'] File name.
   * @param {string} [options.encoding='utf-8'] Character encoding.
   * @param {boolean} [options.bom] Include BOM signature. Default depends on format (e.g. `true` for CSV).
   * @param {string} [options.columnDelimiter=','] Column delimiter (CSV only).
   * @param {string} [options.rowDelimiter='\r\n'] Row delimiter (CSV only).
   * @param {boolean} [options.colHeaders=false] Include column headers.
   * @param {boolean} [options.rowHeaders=false] Include row headers.
   * @param {boolean} [options.exportHiddenColumns=false] Include hidden columns.
   * @param {boolean} [options.exportHiddenRows=false] Include hidden rows.
   * @param {number[]} [options.range=[]] Cell range: `[startRow, startColumn, endRow, endColumn]`.
   * @param {boolean|RegExp|Function} [options.sanitizeValues=false] Sanitization (CSV only).
   * @returns {Blob}
   */ exportAsBlob(format, options = {}) {
        const formatter = this._createTypeFormatter(format, options);
        if (formatter.binary) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])(`exportAsBlob() does not support binary formats such as "${format}". ` + 'Use exportAsBlobAsync() instead.');
        }
        return this._createBlob(formatter);
    }
    /**
   * Exports table data as a blob object, asynchronously.
   *
   * Supports all export formats. For text-based formats (e.g. `'csv'`),
   * the `Promise` resolves immediately. For binary formats (e.g. `'xlsx'`),
   * the export runs asynchronously.
   *
   * @param {string} format Export format type e.g. `'csv'` or `'xlsx'`.
   * @param {object} options Export options.
   * @param {string} [options.mimeType] MIME type. Default depends on format.
   * @param {string} [options.fileExtension] File extension. Default depends on format.
   * @param {string} [options.filename='Handsontable [YYYY]-[MM]-[DD]'] File name.
   * @param {string} [options.encoding='utf-8'] Character encoding (text formats only).
   * @param {boolean} [options.bom] Include BOM signature (text formats only).
   * @param {string} [options.columnDelimiter=','] Column delimiter (CSV only).
   * @param {string} [options.rowDelimiter='\r\n'] Row delimiter (CSV only).
   * @param {boolean} [options.colHeaders=false] Include column headers.
   * @param {boolean} [options.rowHeaders=false] Include row headers.
   * @param {boolean} [options.exportHiddenColumns=false] Include hidden columns.
   * @param {boolean} [options.exportHiddenRows=false] Include hidden rows.
   * @param {number[]} [options.range=[]] Cell range: `[startRow, startColumn, endRow, endColumn]`.
   * @param {boolean|RegExp|Function} [options.sanitizeValues=false] Sanitization (CSV only).
   * @param {boolean} [options.exportFormulas=false] Export cell formulas instead of their computed values (XLSX only).
   * @param {boolean|number} [options.compression] Enable DEFLATE compression: `true` uses level 6; a number 1–9 sets a specific level. Omit or pass a falsy value to use no compression (XLSX only).
   * @param {ConditionalFormattingDescriptor[]} [options.conditionalFormatting=[]] Conditional formatting rules to apply to the exported file (XLSX only).
   * @param {SheetOptions[]} [options.sheets=[]] Configuration for multi-sheet export. Each entry defines one worksheet (XLSX only).
   * @returns {Promise<Blob>}
   * @since 17.1.0
   */ async exportAsBlobAsync(format, options = {}) {
        return this._createBlob(this._createTypeFormatter(format, options));
    }
    /**
   * Triggers a synchronous file download for text-based export formats (e.g. `'csv'`).
   *
   * Calling this method with a binary format (e.g. `'xlsx'`) throws an error — use
   * [[ExportFile#downloadFileAsync]] instead.
   *
   * @param {string} format Export format type e.g. `'csv'`.
   * @param {object} options Export options.
   * @param {string} [options.mimeType] MIME type. Default depends on format.
   * @param {string} [options.fileExtension] File extension. Default depends on format.
   * @param {string} [options.filename='Handsontable [YYYY]-[MM]-[DD]'] File name.
   * @param {string} [options.encoding='utf-8'] Character encoding.
   * @param {boolean} [options.bom] Include BOM signature. Default depends on format.
   * @param {string} [options.columnDelimiter=','] Column delimiter (CSV only).
   * @param {string} [options.rowDelimiter='\r\n'] Row delimiter (CSV only).
   * @param {boolean} [options.colHeaders=false] Include column headers.
   * @param {boolean} [options.rowHeaders=false] Include row headers.
   * @param {boolean} [options.exportHiddenColumns=false] Include hidden columns.
   * @param {boolean} [options.exportHiddenRows=false] Include hidden rows.
   * @param {number[]} [options.range=[]] Cell range: `[startRow, startColumn, endRow, endColumn]`.
   * @param {boolean|RegExp|Function} [options.sanitizeValues=false] Sanitization (CSV only).
   * @returns {void}
   */ downloadFile(format, options = {}) {
        const formatter = this._createTypeFormatter(format, options);
        if (formatter.binary) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])(`downloadFile() does not support binary formats such as "${format}". ` + 'Use downloadFileAsync() instead.');
        }
        const name = `${formatter.options.filename}.${formatter.options.fileExtension}`;
        _class_private_method_get(this, _triggerDownload, triggerDownload).call(this, this._createBlob(formatter), name);
    }
    /**
   * Triggers an asynchronous file download. Supports all export formats.
   *
   * For text-based formats (e.g. `'csv'`), the download is triggered immediately
   * and the returned `Promise` resolves right away.
   * For binary formats (e.g. `'xlsx'`), the export runs asynchronously. When the
   * `dialog` plugin is enabled, a progress overlay is shown for the duration of the export.
   *
   * @param {string} format Export format type e.g. `'csv'` or `'xlsx'`.
   * @param {object} options Export options.
   * @param {string} [options.mimeType] MIME type. Default depends on format.
   * @param {string} [options.fileExtension] File extension. Default depends on format.
   * @param {string} [options.filename='Handsontable [YYYY]-[MM]-[DD]'] File name.
   * @param {string} [options.encoding='utf-8'] Character encoding (text formats only).
   * @param {boolean} [options.bom] Include BOM signature (text formats only).
   * @param {string} [options.columnDelimiter=','] Column delimiter (CSV only).
   * @param {string} [options.rowDelimiter='\r\n'] Row delimiter (CSV only).
   * @param {boolean} [options.colHeaders=false] Include column headers.
   * @param {boolean} [options.rowHeaders=false] Include row headers.
   * @param {boolean} [options.exportHiddenColumns=false] Include hidden columns.
   * @param {boolean} [options.exportHiddenRows=false] Include hidden rows.
   * @param {number[]} [options.range=[]] Cell range: `[startRow, startColumn, endRow, endColumn]`.
   * @param {boolean|RegExp|Function} [options.sanitizeValues=false] Sanitization (CSV only).
   * @param {boolean} [options.exportFormulas=false] Export cell formulas instead of their computed values (XLSX only).
   * @param {boolean|number} [options.compression] Enable DEFLATE compression: `true` uses level 6; a number 1–9 sets a specific level. Omit or pass a falsy value to use no compression (XLSX only).
   * @param {ConditionalFormattingDescriptor[]} [options.conditionalFormatting=[]] Conditional formatting rules to apply to the exported file (XLSX only).
   * @param {SheetOptions[]} [options.sheets=[]] Configuration for multi-sheet export. Each entry defines one worksheet (XLSX only).
   * @returns {Promise<void>}
   * @since 17.1.0
   */ async downloadFileAsync(format, options = {}) {
        const formatter = this._createTypeFormatter(format, options);
        const dialogPlugin = this.hot.getPlugin('dialog');
        const hasDialog = dialogPlugin?.isEnabled();
        const { rootWindow } = this.hot;
        const name = `${formatter.options.filename}.${formatter.options.fileExtension}`;
        const runExport = async ()=>{
            const blob = await Promise.resolve(this._createBlob(formatter));
            _class_private_method_get(this, _triggerDownload, triggerDownload).call(this, blob, name);
        };
        if (hasDialog && formatter.binary) {
            dialogPlugin.show({
                content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["buildExportDialogContent"])(this.hot.getTranslatedPhrase(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$i18n$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["EXPORT_FILE_DIALOG_TITLE"])),
                customClassName: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["LOADING_CLASS_NAME"],
                background: 'semi-transparent',
                closable: false,
                animation: false
            });
            // rAF fires at the START of a frame, before paint. A double-rAF lets the
            // browser complete one full paint cycle (dialog becomes visible) before
            // the export blocks the main thread.
            await new Promise((resolve)=>{
                rootWindow.requestAnimationFrame(()=>{
                    rootWindow.requestAnimationFrame(resolve);
                });
            });
            try {
                await runExport();
            } finally{
                dialogPlugin.hide();
            }
            return;
        }
        await runExport();
    }
    /**
   * Returns `true` when the plugin can produce an export in the given format.
   *
   * For text-based formats such as `'csv'`, no extra setup is required and the
   * method always returns `true`.
   * For binary formats such as `'xlsx'`, the method returns `true` only when the
   * corresponding engine has been provided in the plugin's `engines` map.
   *
   * @param {string} format Export format — `'csv'` or `'xlsx'`.
   * @returns {boolean}
   */ supportsExportFormat(format) {
        if (!__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$typeFactory$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["EXPORT_TYPES"][format]) {
            return false;
        }
        if (format === 'xlsx') {
            const settings = getPluginSettings(this.hot.getSettings()[PLUGIN_KEY]);
            return settings !== undefined && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isObject"])(settings.engines) && Boolean(settings.engines?.xlsx);
        }
        return true;
    }
    /**
   * Creates and returns a class formatter for the specified export type.
   *
   * The engine for the requested format is looked up from the plugin's `engines`
   * map and merged as a default so that per-call options can override it if needed.
   *
   * @private
   * @param {string} format Export format type eq. `'csv'` or `'xlsx'`.
   * @param {object} options Export options.
   * @returns {BaseType}
   */ _createTypeFormatter(format, options = {}) {
        if (!__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$typeFactory$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["EXPORT_TYPES"][format]) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])(`Export format type "${format}" is not supported.`);
        }
        const pluginSettings = getPluginSettings(this.hot.getSettings()[PLUGIN_KEY]);
        const engines = pluginSettings && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isObject"])(pluginSettings.engines) ? pluginSettings.engines : undefined;
        const engineFromSettings = engines?.[format];
        const mergedOptions = engineFromSettings !== undefined ? {
            engine: engineFromSettings,
            ...options
        } : options;
        const formatter = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$typeFactory$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"])(format, new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$dataProvider$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](this.hot), mergedOptions);
        if (formatter === null) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])(`Export format type "${format}" is not supported.`);
        }
        return formatter;
    }
    /**
   * Creates a blob object from the provided type formatter.
   *
   * For synchronous formatters (e.g. CSV), returns a `Blob` directly.
   * For asynchronous formatters (e.g. XLSX), returns a `Promise<Blob>`.
   *
   * @private
   * @param {BaseType} typeFormatter The instance of the specific formatter/exporter.
   * @returns {Blob|Promise<Blob>}
   */ _createBlob(typeFormatter) {
        if (typeof Blob === 'undefined') {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('Blob is not available in this environment.');
        }
        const exported = typeFormatter.export();
        const { mimeType, encoding } = typeFormatter.options;
        if (exported instanceof Promise) {
            return exported.then((buffer)=>new Blob([
                    buffer
                ], {
                    type: mimeType
                }));
        }
        return new Blob([
            exported
        ], {
            type: `${mimeType};charset=${encoding}`
        });
    }
    constructor(...args){
        super(...args), _class_private_method_init(this, _triggerDownload), _class_private_field_init(this, _onAfterContextMenuDefaultOptions, {
            writable: true,
            value: void 0
        }), _class_private_field_set(this, _onAfterContextMenuDefaultOptions, (rawOptions)=>{
            if (typeof rawOptions !== 'object' || rawOptions === null) {
                return;
            }
            const options = rawOptions;
            options.items.push({
                name: '---------'
            }, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$contextMenuItem$2f$exportItem$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"])(this));
        });
    }
}
function triggerDownload(blob, name) {
    const { rootDocument, rootWindow } = this.hot;
    const URL = rootWindow.URL || rootWindow.webkitURL;
    const url = URL.createObjectURL(blob);
    const a = rootDocument.createElement('a');
    a.style.display = 'none';
    a.setAttribute('href', url);
    a.setAttribute('download', name);
    rootDocument.body.appendChild(a);
    a.dispatchEvent(new MouseEvent('click'));
    rootDocument.body.removeChild(a);
    this.hot._registerTimeout(()=>{
        URL.revokeObjectURL(url);
    }, 100);
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/typeFactory.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "EXPORT_TYPES",
    ()=>EXPORT_TYPES,
    "TYPE_CSV",
    ()=>TYPE_CSV,
    "TYPE_PDF",
    ()=>TYPE_PDF,
    "TYPE_XLSX",
    ()=>TYPE_XLSX,
    "default",
    ()=>typeFactory
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$csv$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/types/csv.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/types/xlsx.mjs [client] (ecmascript)");
;
;
const TYPE_CSV = 'csv';
const TYPE_XLSX = 'xlsx';
const TYPE_PDF = 'pdf'; // TODO
const EXPORT_TYPES = {
    [TYPE_CSV]: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$csv$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"],
    [TYPE_XLSX]: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"]
};
function typeFactory(type, dataProvider, options) {
    const ExportType = EXPORT_TYPES[type];
    if (typeof ExportType === 'function') {
        return new ExportType(dataProvider, options);
    }
    return null;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/types/_base.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$string$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/string.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
;
;
;
/**
 * @private
 */ class BaseType {
    /**
   * Default options.
   *
   * @returns {object}
   */ static get DEFAULT_OPTIONS() {
        return {
            mimeType: 'text/plain',
            fileExtension: 'txt',
            filename: 'Handsontable [YYYY]-[MM]-[DD]',
            encoding: 'utf-8',
            bom: false,
            colHeaders: false,
            rowHeaders: false,
            exportHiddenColumns: false,
            exportHiddenRows: false,
            range: []
        };
    }
    /**
   * Whether the format produces binary (asynchronous) output.
   *
   * @returns {boolean}
   */ get binary() {
        return false;
    }
    /**
   * Type predicate: narrows this formatter to a binary (asynchronous) one.
   *
   * @returns {boolean}
   */ isBinary() {
        return this.binary;
    }
    /**
   * Type predicate: narrows this formatter to a text (synchronous) one.
   *
   * @returns {boolean}
   */ isText() {
        return !this.binary;
    }
    /**
   * Performs the actual export and returns the data as a string or a Promise.
   */ export() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('export() must be implemented by subclass');
    }
    /**
   * Merge options provided by users with defaults.
   *
   * @param {object} options An object with options to merge with.
   * @returns {object} Returns new options object.
   */ _mergeOptions(options) {
        const ctorDefaults = this.constructor.DEFAULT_OPTIONS;
        let _options = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clone"])(ctorDefaults);
        const date = new Date();
        _options = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["extend"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clone"])(BaseType.DEFAULT_OPTIONS), _options);
        // Legacy alias: `columnHeaders` was renamed to `colHeaders`. When a caller
        // passes the old name and does not also pass the new name, promote it so
        // that the rest of the code only needs to read `colHeaders`.
        if (options && 'columnHeaders' in options && !('colHeaders' in options)) {
            options = {
                ...options,
                colHeaders: options.columnHeaders
            };
        }
        _options = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["extend"])(_options, options);
        _options.filename = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$string$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["substitute"])(_options.filename, {
            YYYY: date.getFullYear(),
            MM: `${date.getMonth() + 1}`.padStart(2, '0'),
            DD: `${date.getDate()}`.padStart(2, '0')
        });
        return _options;
    }
    /**
   * Initializes the export type with the data provider and merges the given options with the class defaults.
   */ constructor(dataProvider, options){
        this.dataProvider = dataProvider;
        this.options = this._mergeOptions(options);
        this.dataProvider.setOptions(this.options);
    }
}
var __TURBOPACK__default__export__ = BaseType;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/types/csv.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/types/_base.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
const CHAR_CARRIAGE_RETURN = String.fromCharCode(13);
const CHAR_DOUBLE_QUOTES = String.fromCharCode(34);
const CHAR_LINE_FEED = String.fromCharCode(10);
const CHAR_EQUAL = String.fromCharCode(61);
const CHAR_PLUS = String.fromCharCode(43);
const CHAR_MINUS = String.fromCharCode(45);
const CHAR_AT = String.fromCharCode(64);
const CHAR_TAB = String.fromCharCode(9);
var /**
   * Sanitize value that may be interpreted as a formula in spreadsheet software.
   * Following the OWASP recommendations: https://owasp.org/www-community/attacks/CSV_Injection.
   *
   * @param {string} value Cell value.
   * @returns {string}
   */ _sanitizeValueWithOWASP = /*#__PURE__*/ new WeakSet(), /**
   * Sanitize value using regular expression.
   *
   * @param {string} value Cell value.
   * @param {RegExp} regexp Regular expression to test against.
   * @returns {string}
   */ _sanitizeValueWithRegExp = /*#__PURE__*/ new WeakSet();
/**
 * @private
 */ class Csv extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * Default options for exporting CSV format.
   *
   * @returns {object}
   */ static get DEFAULT_OPTIONS() {
        return {
            mimeType: 'text/csv',
            fileExtension: 'csv',
            bom: true,
            columnDelimiter: ',',
            rowDelimiter: '\r\n',
            sanitizeValues: false
        };
    }
    /**
   * Merge options, normalizing XLSX-only values that are not applicable to CSV.
   *
   * The `'hide'` value for `exportHiddenRows` and `exportHiddenColumns` is only
   * meaningful for Excel exports (it marks rows/columns as hidden in the workbook).
   * For CSV the only sensible fallback is `false` — omit those rows/columns entirely.
   *
   * @param {object} options User-supplied options.
   * @returns {object}
   */ _mergeOptions(options) {
        const merged = super._mergeOptions(options);
        const overrides = {};
        if (merged.exportHiddenRows === 'hide') {
            overrides.exportHiddenRows = false;
        }
        if (merged.exportHiddenColumns === 'hide') {
            overrides.exportHiddenColumns = false;
        }
        return Object.keys(overrides).length > 0 ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["extend"])(merged, overrides) : merged;
    }
    /**
   * Create string body in desired format.
   *
   * @returns {string}
   */ export() {
        const options = this.options;
        const data = this.dataProvider.getData();
        let columnHeaders = this.dataProvider.getColumnHeaders();
        const hasColumnHeaders = columnHeaders.length > 0;
        const rowHeaders = this.dataProvider.getRowHeaders();
        const hasRowHeaders = rowHeaders.length > 0;
        let result = options.bom ? String.fromCharCode(0xFEFF) : '';
        if (hasColumnHeaders) {
            columnHeaders = columnHeaders.map((value)=>this._escapeCell(value, {
                    force: true,
                    sanitizeValue: options.sanitizeValues
                }));
            if (hasRowHeaders) {
                result += options.columnDelimiter;
            }
            result += columnHeaders.join(options.columnDelimiter);
            result += options.rowDelimiter;
        }
        data.forEach((value, index)=>{
            if (index > 0) {
                result += options.rowDelimiter;
            }
            if (hasRowHeaders) {
                result += this._escapeCell(rowHeaders[index], {
                    sanitizeValue: options.sanitizeValues
                });
                result += options.columnDelimiter;
            }
            const escapedValue = value.map((cellValue)=>this._escapeCell(cellValue, {
                    sanitizeValue: options.sanitizeValues
                })).join(options.columnDelimiter);
            result += escapedValue;
        });
        return result;
    }
    /**
   * Escape cell value.
   *
   * @param {*} value Cell value.
   * @param {object} options Options.
   * @param {boolean} [options.force=false] Indicates if cell value will be escaped forcefully.
   * @param {boolean|RegExp|Function} [options.sanitizeValue=false] Controls the sanitization of cell value.
   * @returns {string}
   */ _escapeCell(value, { force = false, sanitizeValue = false } = {}) {
        let returnValue = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["stringify"])(value);
        if (returnValue === '') {
            return returnValue;
        }
        if (sanitizeValue) {
            force = true;
        }
        if (sanitizeValue instanceof RegExp) {
            returnValue = _class_private_method_get(this, _sanitizeValueWithRegExp, sanitizeValueWithRegExp).call(this, returnValue, sanitizeValue);
        } else if (typeof sanitizeValue === 'function') {
            returnValue = sanitizeValue(returnValue);
        } else if (sanitizeValue) {
            returnValue = _class_private_method_get(this, _sanitizeValueWithOWASP, sanitizeValueWithOWASP).call(this, returnValue);
        }
        if (force || returnValue.indexOf(CHAR_CARRIAGE_RETURN) >= 0 || returnValue.indexOf(CHAR_DOUBLE_QUOTES) >= 0 || returnValue.indexOf(CHAR_LINE_FEED) >= 0 || returnValue.indexOf(this.options.columnDelimiter) >= 0) {
            returnValue = returnValue.replaceAll('"', '""');
            returnValue = `"${returnValue}"`;
        }
        return returnValue;
    }
    constructor(...args){
        super(...args), _class_private_method_init(this, _sanitizeValueWithOWASP), _class_private_method_init(this, _sanitizeValueWithRegExp);
    }
}
function sanitizeValueWithOWASP(value) {
    if (value.startsWith(CHAR_EQUAL) || value.startsWith(CHAR_PLUS) || value.startsWith(CHAR_MINUS) || value.startsWith(CHAR_AT) || value.startsWith(CHAR_TAB) || value.startsWith(CHAR_CARRIAGE_RETURN)) {
        return `'${value}`;
    }
    return value;
}
function sanitizeValueWithRegExp(value, regexp) {
    return regexp.test(value) ? `'${value}` : value;
}
var __TURBOPACK__default__export__ = Csv;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/types/xlsx.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/object.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/errors.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$dataProvider$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/dataProvider.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/types/_base.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$formula$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/types/xlsx/formula-utils.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/types/xlsx/cell-style.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$date$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/types/xlsx/date-utils.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$numeric$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/plugins/exportFile/types/xlsx/numeric-utils.mjs [client] (ecmascript)");
function _check_private_redeclaration(obj, privateCollection) {
    if (privateCollection.has(obj)) {
        throw new TypeError("Cannot initialize the same private elements twice on an object");
    }
}
function _class_private_method_get(receiver, privateSet, fn) {
    if (!privateSet.has(receiver)) {
        throw new TypeError("attempted to get private field on non-instance");
    }
    return fn;
}
function _class_private_method_init(obj, privateSet) {
    _check_private_redeclaration(obj, privateSet);
    privateSet.add(obj);
}
;
;
;
;
;
;
;
;
;
// Approximate number of pixels occupied by one Excel column-width unit (character width
// of the "Normal" style font at the default font size). Used to convert pixel widths
// from Handsontable into the unitless width values expected by ExcelJS.
const PIXELS_PER_EXCEL_COLUMN_WIDTH_UNIT = 7;
// Conversion factor from CSS pixels to typographic points (1 pt = 1/72 in; 1 px = 1/96 in,
// so 1 px = 72/96 = 0.75 pt). Used to convert Handsontable row heights (pixels) to the
// point-based row heights expected by ExcelJS.
const PIXELS_TO_POINTS_RATIO = 0.75;
// Default width (in Excel column-width units) assigned to the frozen row-header column
// when row headers are exported. Chosen to comfortably fit typical row-index numbers.
const ROW_HEADER_DEFAULT_WIDTH = 5;
var /**
   * Populates a single ExcelJS worksheet from the given DataProvider.
   *
   * Extracts the data writing logic shared between single-sheet and multi-sheet exports.
   *
   * @param {object} workbook The ExcelJS workbook.
   * @param {object} worksheet The ExcelJS worksheet to populate.
   * @param {DataProvider} dataProvider DataProvider configured for this sheet.
   * @param {object} options Merged options for this sheet.
   * @returns {number} Number of data rows written.
   */ _populateWorksheet = /*#__PURE__*/ new WeakSet(), /**
   * Iterates the exported data rows and writes each to the worksheet.
   *
   * Handles row heights, row-header cells, and per-cell content and styling.
   * Delegates per-cell writing to {@link Xlsx##writeRowCells}.
   *
   * @param {object} worksheet The ExcelJS worksheet.
   * @param {Array[]} data 2D data array from DataProvider.
   * @param {object} context Sheet-level context passed through from `#populateWorksheet`.
   */ _writeDataRows = /*#__PURE__*/ new WeakSet(), /**
   * Writes all data cells in a single row to the worksheet.
   *
   * Resolves each cell's value and number format, then applies styling via
   * {@link Xlsx##writeCellStyling}.
   *
   * @param {object} row The ExcelJS row object.
   * @param {Array} rowData Cell values for this row.
   * @param {number} rowIndex 0-based data-array row index.
   * @param {object} context Sheet-level context passed through from `#populateWorksheet`.
   */ _writeRowCells = /*#__PURE__*/ new WeakSet(), /**
   * Applies all visual style properties from cell meta and computed CSS to an ExcelJS cell.
   *
   * Handles alignment, borders, font, fill, dropdown validation, and cell comments.
   * Protection is handled by the caller because it depends on a sheet-level flag.
   *
   * @param {object} cell The ExcelJS cell object.
   * @param {object|undefined} meta Cell meta object.
   * @param {{ fontBold: boolean, fontItalic: boolean, fontUnderline: boolean,
   *           fontColor: string|null, backgroundColor: string|null }|null} cssStyle
   *   Computed CSS style from `getCssStyleFromElement`, or `null`.
   * @param {object} context Sheet-level context passed through from `#populateWorksheet`.
   */ _writeCellStyling = /*#__PURE__*/ new WeakSet(), /**
   * Resolves the final ExcelJS cell value and optional number format for a data cell.
   *
   * Handles (in priority order):
   * 1. ColumnSummary formula destinations (when `exportFormulas` is `true`).
   * 2. HyperFormula formula cells (when `exportFormulas` is `true`).
   * 3. Date cells (ISO 8601 string → serial number).
   * 4. Time cells (time string → fractional day serial).
   * 5. Checkbox cells (boolean from `checkedTemplate` comparison).
   * 6. Multiselect cells (comma-separated string).
   * 7. All other cells (numeric-aware or stringified).
   *
   * @param {*} cellValue Pre-calculated display value from `getData()`.
   * @param {object} meta Cell meta object.
   * @param {*} sourceValue Raw source value from `getSourceDataAtCell()` (may be a formula string).
   * @param {object|undefined} summary ColumnSummary descriptor, or `undefined` for non-summary cells.
   * @param {object} context Sheet-level context passed through from `#populateWorksheet`.
   * @returns {{ value: *, numFmt: string|null }}
   */ _resolveCellValue = /*#__PURE__*/ new WeakSet(), /**
   * Converts a raw cell value to an ExcelJS-compatible value.
   *
   * @param {*} value Raw cell value.
   * @param {object} meta Cell meta object.
   * @returns {null|number|string}
   */ _getCellValue = /*#__PURE__*/ new WeakSet(), /**
   * Returns the boolean export value for a checkbox cell.
   *
   * @param {*} value Raw cell value.
   * @param {object} meta Cell meta object.
   * @returns {boolean}
   */ _getCheckboxValue = /*#__PURE__*/ new WeakSet(), /**
   * Converts a multiselect cell value to a comma-separated display string.
   *
   * @param {*} value Raw multiselect cell value (expected to be an array).
   * @returns {string|null}
   */ _getMultiSelectExportValue = /*#__PURE__*/ new WeakSet(), /**
   * Builds an ExcelJS solid fill object from a `headerStyle` option value.
   *
   * Returns `null` when `headerStyle` is `null` / has no `backgroundColor`, so
   * callers can skip applying a fill entirely (no default ExcelJS fill sentinel is set).
   *
   * @param {object|null|undefined} headerStyle The `headerStyle` option value.
   * @returns {object|null}
   */ _buildHeaderFill = /*#__PURE__*/ new WeakSet(), /**
   * Builds an ExcelJS border object (all four sides) from a `headerStyle` option value.
   *
   * Returns `null` when `headerStyle` is `null` or has no `border` sub-option.
   *
   * @param {object|null|undefined} headerStyle The `headerStyle` option value.
   * @returns {object|null}
   */ _buildHeaderBorder = /*#__PURE__*/ new WeakSet(), /**
   * Builds the options object passed to `workbook.xlsx.writeBuffer()`.
   *
   * The `compression` option enables DEFLATE compression: `true` uses the default
   * level 6; a number 1–9 sets the JSZip `compressionOptions.level` (1 = fastest,
   * 9 = smallest). Falsy or omitted leaves ExcelJS to use no compression.
   *
   * @returns {object}
   */ _getWriteOptions = /*#__PURE__*/ new WeakSet(), /**
   * Applies conditional formatting rules to the worksheet.
   *
   * Each descriptor in `cfRules` may specify a `rows` and/or `cols` range
   * (0-based, relative to the exported data — the same coordinate space as
   * `getDataAtCell`). Both are optional and default to the full data range.
   * The `rules` array is passed directly to ExcelJS's `addConditionalFormatting`.
   *
   * @param {object} worksheet The ExcelJS worksheet.
   * @param {Array} cfRules Array of `{ rows?, cols?, rules }` descriptors.
   * @param {number} dataRowOffset 1-based Excel row where data starts.
   * @param {number} dataColOffset 1-based Excel column where data starts.
   * @param {number} dataRows Total number of exported data rows.
   * @param {number} dataCols Total number of exported data columns.
   */ _applyConditionalFormatting = /*#__PURE__*/ new WeakSet(), /**
   * Builds an Excel cell-range reference string (e.g. `'B2:E7'`) from 1-based
   * row and column indices.
   *
   * @param {number} startRow 1-based start row.
   * @param {number} startCol 1-based start column.
   * @param {number} endRow 1-based end row.
   * @param {number} endCol 1-based end column.
   * @returns {string}
   */ _buildRangeRef = /*#__PURE__*/ new WeakSet(), /**
   * Sets column widths on the worksheet, converting pixel values to Excel
   * character-width units.
   *
   * @param {object} worksheet The ExcelJS worksheet.
   * @param {number[]} widths Column widths in pixels, in data-column order.
   * @param {boolean} hasRowHeaders Whether a row-header column is prepended.
   */ _applyColumnWidths = /*#__PURE__*/ new WeakSet(), /**
   * Marks the specified Excel columns as hidden.
   *
   * @param {object} worksheet The ExcelJS worksheet.
   * @param {number[]} hiddenColIndices 0-based data-column indices to hide.
   * @param {boolean} hasRowHeaders Whether a row-header column is prepended.
   */ _applyHiddenColumns = /*#__PURE__*/ new WeakSet(), /**
   * Marks the specified Excel rows as hidden.
   *
   * @param {object} worksheet The ExcelJS worksheet.
   * @param {number[]} hiddenRowIndices 0-based data-row indices to hide.
   * @param {number} dataRowOffset 1-based Excel row number where data row 0 starts.
   */ _applyHiddenRows = /*#__PURE__*/ new WeakSet(), /**
   * Configures the worksheet view, combining frozen panes and RTL direction settings.
   *
   * @param {object} worksheet The ExcelJS worksheet.
   * @param {number} frozenRows Number of frozen data rows.
   * @param {number} frozenColumns Number of frozen data columns.
   * @param {number} headerRowCount Number of header rows prepended before data rows.
   * @param {boolean} hasRowHeaders Whether a row-header column is prepended.
   * @param {boolean} isRtl Whether the table layout direction is right-to-left.
   */ _applyWorksheetViews = /*#__PURE__*/ new WeakSet(), /**
   * Writes the column-header row to the worksheet, applying alignment derived from
   * each header's `className` where configured, and optional background fill and border.
   *
   * @param {object} worksheet The ExcelJS worksheet.
   * @param {Array} columnHeaders Header values.
   * @param {string[]} classNames Per-header className strings (same order as `columnHeaders`).
   * @param {boolean} hasRowHeaders Whether a row-header column is prepended (leaves cell A1 empty).
   * @param {number} dataColOffset 1-based column number where data columns begin.
   * @param {object|null} headerFill ExcelJS fill object for header cells, or `null` for no fill.
   * @param {object|null} headerBorder ExcelJS border object for header cells, or `null` for no border.
   */ _writeColumnHeaders = /*#__PURE__*/ new WeakSet(), /**
   * Writes multi-row nested column headers to the worksheet, applying alignment derived
   * from each header's `className` where configured, and optional background fill and border.
   *
   * @param {object} worksheet The ExcelJS worksheet.
   * @param {Array[]} nestedColumnHeaders Layers returned by DataProvider#getNestedColumnHeaders.
   * @param {boolean} hasRowHeaders Whether a row-header column is prepended.
   * @param {number} dataColOffset 1-based column number where data columns begin.
   * @param {object|null} headerFill ExcelJS fill object for header cells, or `null` for no fill.
   * @param {object|null} headerBorder ExcelJS border object for header cells, or `null` for no border.
   */ _writeNestedColumnHeaders = /*#__PURE__*/ new WeakSet(), /**
   * Merges spanning cells in nested header rows.
   *
   * @param {object} worksheet The ExcelJS worksheet.
   * @param {Array[]} nestedColumnHeaders Layers returned by DataProvider#getNestedColumnHeaders.
   * @param {boolean} hasRowHeaders Whether a row-header column is prepended.
   * @param {number} dataColOffset 1-based column number where data columns begin.
   */ _applyNestedHeaderMerges = /*#__PURE__*/ new WeakSet(), /**
   * Creates a `veryHidden` worksheet containing all unique dropdown/autocomplete
   * source arrays as columns. Returns a map from `JSON.stringify(source)` to an
   * Excel range-reference string pointing at that column.
   *
   * If no dropdown or autocomplete cells with array sources are found, returns an
   * empty map and does not add any worksheet to the workbook.
   *
   * @private
   * @param {object} workbook The ExcelJS workbook.
   * @param {Array[]} cellsMeta 2D meta array from DataProvider.
   * @returns {Map<string, string>} Map from source JSON key to range reference.
   */ _buildValidationSheet = /*#__PURE__*/ new WeakSet();
/**
 * @private
 */ class Xlsx extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$_base$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"] {
    /**
   * Marks this formatter as binary (non-stringifiable).
   *
   * @returns {boolean}
   */ static get BINARY() {
        return true;
    }
    /**
   * Returns true to indicate that XLSX data should be treated as binary output.
   */ get binary() {
        return true;
    }
    /**
   * Default options for exporting XLSX format.
   *
   * @returns {object}
   */ static get DEFAULT_OPTIONS() {
        return {
            mimeType: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
            fileExtension: 'xlsx',
            bom: false,
            engine: null,
            // DEFLATE compression: true = level 6, number 1–9 = that level, falsy = no compression.
            compression: null,
            // Array of { rows?, cols?, rules } conditional formatting descriptors.
            conditionalFormatting: [],
            // When true, HyperFormula formula cells and ColumnSummary destinations are exported as live
            // Excel formulas. When false (default) the pre-calculated static values are exported.
            exportFormulas: false,
            // Style applied to column and row header cells. `backgroundColor` accepts any CSS hex color
            // string. `border` sets the border style on all four sides; set to `null` to suppress it.
            // Set the whole option to `null` to export headers with no styling at all.
            headerStyle: {
                backgroundColor: '#F2F2F2',
                border: {
                    style: 'thin'
                }
            }
        };
    }
    /**
   * Builds an XLSX workbook and returns its binary buffer.
   *
   * When the `sheets` option is provided the workbook contains one worksheet per entry,
   * each driven by its own Handsontable instance. Otherwise a single worksheet named
   * `'Sheet1'` is produced from the primary DataProvider.
   *
   * @returns {Promise<Uint8Array>}
   */ async export() {
        const engine = this.options.engine;
        if (!engine || typeof engine.Workbook !== 'function') {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["throwWithCause"])('Missing or invalid ExcelJS engine. Pass the ExcelJS module via the `engines` option ' + 'in the exportFile plugin settings: `exportFile: { engines: { xlsx: ExcelJS } }`.');
        }
        // Clear style caches for all documents involved in this export. In multi-sheet
        // mode each sheet may come from a different Handsontable instance living in a
        // different document (e.g. an iframe), so every distinct document must be cleared.
        const sheets = this.options.sheets;
        const docsToClear = sheets && sheets.length > 0 ? new Set(sheets.map((s)=>s.instance.rootDocument)) : new Set([
            this.dataProvider.hot.rootDocument
        ]);
        docsToClear.forEach((doc)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["clearStyleCaches"])(doc));
        const workbook = new engine.Workbook();
        if (sheets && sheets.length > 0) {
            // multi-sheet mode
            const usedSheetNames = new Set();
            sheets.forEach((sheetConfig)=>{
                const dp = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$dataProvider$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["default"](sheetConfig.instance);
                // Apply the same legacy-alias promotion that _mergeOptions does for top-level
                // options: if the caller passed the deprecated `columnHeaders` on a per-sheet
                // config without also passing `colHeaders`, promote it so `dataProvider.js`
                // (which only reads `colHeaders`) picks it up correctly.
                const normalizedSheetConfig = 'columnHeaders' in sheetConfig && !('colHeaders' in sheetConfig) ? {
                    ...sheetConfig,
                    colHeaders: sheetConfig.columnHeaders
                } : sheetConfig;
                const sheetOptions = {
                    ...this.options,
                    ...normalizedSheetConfig
                };
                dp.setOptions(sheetOptions);
                const baseName = sheetConfig.name || 'Sheet';
                let name = baseName;
                let suffix = 1;
                while(usedSheetNames.has(name)){
                    name = `${baseName}${suffix}`;
                    suffix += 1;
                }
                usedSheetNames.add(name);
                const worksheet = workbook.addWorksheet(name);
                _class_private_method_get(this, _populateWorksheet, populateWorksheet).call(this, workbook, worksheet, dp, sheetOptions);
            });
        } else {
            // single-sheet mode
            const worksheet = workbook.addWorksheet('Sheet1');
            _class_private_method_get(this, _populateWorksheet, populateWorksheet).call(this, workbook, worksheet, this.dataProvider, this.options);
        }
        return workbook.xlsx.writeBuffer(_class_private_method_get(this, _getWriteOptions, getWriteOptions).call(this));
    }
    constructor(...args){
        super(...args), _class_private_method_init(this, _populateWorksheet), _class_private_method_init(this, _writeDataRows), _class_private_method_init(this, _writeRowCells), _class_private_method_init(this, _writeCellStyling), _class_private_method_init(this, _resolveCellValue), _class_private_method_init(this, _getCellValue), _class_private_method_init(this, _getCheckboxValue), _class_private_method_init(this, _getMultiSelectExportValue), _class_private_method_init(this, _buildHeaderFill), _class_private_method_init(this, _buildHeaderBorder), _class_private_method_init(this, _getWriteOptions), _class_private_method_init(this, _applyConditionalFormatting), _class_private_method_init(this, _buildRangeRef), _class_private_method_init(this, _applyColumnWidths), _class_private_method_init(this, _applyHiddenColumns), _class_private_method_init(this, _applyHiddenRows), _class_private_method_init(this, _applyWorksheetViews), _class_private_method_init(this, _writeColumnHeaders), _class_private_method_init(this, _writeNestedColumnHeaders), _class_private_method_init(this, _applyNestedHeaderMerges), _class_private_method_init(this, _buildValidationSheet);
    }
}
function populateWorksheet(workbook, worksheet, dataProvider, options) {
    const data = dataProvider.getData();
    const cellsMeta = dataProvider.getCellsMeta();
    const validationMap = _class_private_method_get(this, _buildValidationSheet, buildValidationSheet).call(this, workbook, cellsMeta);
    const cellElements = dataProvider.getCellElements();
    const columnHeaders = dataProvider.getColumnHeaders();
    const columnHeadersClassNames = dataProvider.getColumnHeadersClassNames();
    const rowHeaders = dataProvider.getRowHeaders();
    const columnsWidths = dataProvider.getColumnsWidths();
    const rowsHeights = dataProvider.getRowsHeights();
    const mergeCells = dataProvider.getMergeCells();
    const frozenRows = dataProvider.getFrozenRows();
    const frozenColumns = dataProvider.getFrozenColumns();
    const nestedColumnHeaders = dataProvider.getNestedColumnHeaders();
    const isRtl = dataProvider.getLayoutDirection() === 'rtl';
    const exportFormulas = options.exportFormulas ?? false;
    // Build a fast O(1) lookup: "dataRowIndex:dataColIndex" → summary descriptor.
    // Always built so the protection pre-scan can identify ColumnSummary destination
    // cells; the formula-writing path also uses this map when exportFormulas is true.
    const summaryMap = new Map();
    const columnSummaries = dataProvider.getColumnSummaries();
    columnSummaries.forEach((summaryRaw)=>{
        const summary = summaryRaw;
        summaryMap.set(`${summary.destRow}:${summary.destCol}`, summary);
    });
    const sourceData = exportFormulas ? dataProvider.getSourceData() : null;
    const formulasSeparator = exportFormulas ? dataProvider.getFormulasSeparator() : ',';
    const hasColumnHeaders = columnHeaders.length > 0;
    const hasRowHeaders = rowHeaders.length > 0;
    const useNestedHeaders = nestedColumnHeaders !== null && hasColumnHeaders;
    let headerRowCount = 0;
    if (useNestedHeaders) {
        headerRowCount = nestedColumnHeaders.length;
    } else if (hasColumnHeaders) {
        headerRowCount = 1;
    }
    const dataRowOffset = headerRowCount + 1;
    const dataColOffset = hasRowHeaders ? 2 : 1;
    const excludedHiddenRows = exportFormulas ? dataProvider.getExcludedHiddenRows() : null;
    const excludedHiddenCols = exportFormulas ? dataProvider.getExcludedHiddenColumns() : null;
    const hiddenRowIndices = dataProvider.getHiddenRowDataIndices();
    const hiddenColIndices = dataProvider.getHiddenColumnDataIndices();
    const { rootDocument, rootWindow } = dataProvider.hot;
    _class_private_method_get(this, _applyColumnWidths, applyColumnWidths).call(this, worksheet, columnsWidths, hasRowHeaders);
    if (hiddenColIndices.length > 0) {
        _class_private_method_get(this, _applyHiddenColumns, applyHiddenColumns).call(this, worksheet, hiddenColIndices, hasRowHeaders);
    }
    _class_private_method_get(this, _applyWorksheetViews, applyWorksheetViews).call(this, worksheet, frozenRows, frozenColumns, headerRowCount, hasRowHeaders, isRtl);
    const headerFill = _class_private_method_get(this, _buildHeaderFill, buildHeaderFill).call(this, options.headerStyle);
    const headerBorder = _class_private_method_get(this, _buildHeaderBorder, buildHeaderBorder).call(this, options.headerStyle);
    if (useNestedHeaders) {
        _class_private_method_get(this, _writeNestedColumnHeaders, writeNestedColumnHeaders).call(this, worksheet, nestedColumnHeaders, hasRowHeaders, dataColOffset, headerFill, headerBorder);
        _class_private_method_get(this, _applyNestedHeaderMerges, applyNestedHeaderMerges).call(this, worksheet, nestedColumnHeaders, hasRowHeaders, dataColOffset);
    } else if (hasColumnHeaders) {
        _class_private_method_get(this, _writeColumnHeaders, writeColumnHeaders).call(this, worksheet, columnHeaders, columnHeadersClassNames, hasRowHeaders, dataColOffset, headerFill, headerBorder);
    }
    // Pre-scan: only write cell.protection when the sheet actually contains cells
    // that should be locked in Excel. Two reasons to skip it entirely:
    //
    // 1. When ColumnSummary is active, its destination cells (and any adjacent label
    //    cells marked readOnly) are readOnly only to prevent in-grid editing. Locking
    //    those cells in Excel surprises users and adds no value, so protection is
    //    suppressed whenever ColumnSummary is present.
    //
    // 2. Setting protection on every cell — even { locked: false } — causes ExcelJS to
    //    initialise font/fill/border to sentinel values, which adds noise to the file
    //    and breaks styling assertions in tests.
    const hasColumnSummary = summaryMap.size > 0;
    const hasReadOnlyCells = !hasColumnSummary && cellsMeta.some((row)=>row.some((meta)=>meta?.readOnly === true));
    const context = {
        exportFormulas,
        formulasSeparator,
        dataRowOffset,
        dataColOffset,
        excludedHiddenRows,
        excludedHiddenCols,
        summaryMap,
        sourceData,
        hasRowHeaders,
        headerFill,
        headerBorder,
        hasReadOnlyCells,
        rootDocument,
        rootWindow,
        validationMap,
        cellsMeta,
        cellElements,
        rowHeaders,
        rowsHeights
    };
    _class_private_method_get(this, _writeDataRows, writeDataRows).call(this, worksheet, data, context);
    if (hiddenRowIndices.length > 0) {
        _class_private_method_get(this, _applyHiddenRows, applyHiddenRows).call(this, worksheet, hiddenRowIndices, dataRowOffset);
    }
    if (hasReadOnlyCells) {
        // Protect the worksheet so that locked cells become read-only in Excel.
        // No password is set — users can unprotect at any time.
        // The permissive options keep non-editing actions (select, sort, filter,
        // resize) available so the spreadsheet remains usable.
        worksheet.protect('', {
            selectLockedCells: true,
            selectUnlockedCells: true,
            formatColumns: true,
            formatRows: true,
            sort: true,
            autoFilter: true
        });
    }
    mergeCells.forEach((merge)=>{
        const startRow = merge.row + dataRowOffset;
        const startCol = merge.col + dataColOffset;
        const endRow = startRow + merge.rowspan - 1;
        const endCol = startCol + merge.colspan - 1;
        worksheet.mergeCells(startRow, startCol, endRow, endCol);
    });
    const { conditionalFormatting } = options;
    if (conditionalFormatting && conditionalFormatting.length > 0) {
        _class_private_method_get(this, _applyConditionalFormatting, applyConditionalFormatting).call(this, worksheet, conditionalFormatting, dataRowOffset, dataColOffset, data.length, data[0]?.length ?? 0);
    }
    return data.length;
}
function writeDataRows(worksheet, data, context) {
    for(let rowIndex = 0; rowIndex < data.length; rowIndex++){
        const rowData = data[rowIndex];
        const excelRowNumber = rowIndex + context.dataRowOffset;
        const row = worksheet.getRow(excelRowNumber);
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(context.rowsHeights[rowIndex])) {
            row.height = context.rowsHeights[rowIndex] * PIXELS_TO_POINTS_RATIO;
        }
        if (context.hasRowHeaders) {
            const rowHeaderCell = row.getCell(1);
            rowHeaderCell.value = context.rowHeaders[rowIndex] ?? null;
            if (context.headerFill) {
                rowHeaderCell.fill = context.headerFill;
            }
            if (context.headerBorder) {
                rowHeaderCell.border = context.headerBorder;
            }
        }
        _class_private_method_get(this, _writeRowCells, writeRowCells).call(this, row, rowData, rowIndex, context);
        row.commit();
    }
}
function writeRowCells(row, rowData, rowIndex, context) {
    for(let colIndex = 0; colIndex < rowData.length; colIndex++){
        const cellValue = rowData[colIndex];
        const cell = row.getCell(colIndex + context.dataColOffset);
        const meta = context.cellsMeta[rowIndex][colIndex];
        const summary = context.summaryMap.get(`${rowIndex}:${colIndex}`);
        const sourceValue = context.sourceData?.[rowIndex][colIndex];
        const { value, numFmt } = _class_private_method_get(this, _resolveCellValue, resolveCellValue).call(this, cellValue, meta, sourceValue, summary, context);
        cell.value = value;
        if (numFmt) {
            cell.numFmt = numFmt;
        }
        const cssStyle = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getCssStyleFromElement"])(context.cellElements[rowIndex][colIndex], meta.className, context.rootDocument, context.rootWindow);
        _class_private_method_get(this, _writeCellStyling, writeCellStyling).call(this, cell, meta, cssStyle, context);
        if (context.hasReadOnlyCells) {
            cell.protection = {
                locked: meta.readOnly === true
            };
        }
    }
}
function writeCellStyling(cell, meta, cssStyle, context) {
    const alignment = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getAlignmentFromMeta"])(meta);
    if (alignment) {
        cell.alignment = alignment;
    }
    const border = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getBorderFromMeta"])(meta);
    if (border) {
        cell.border = border;
    }
    const font = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getFontFromMeta"])(meta, cssStyle);
    if (font) {
        cell.font = font;
    }
    const fill = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getFillFromMeta"])(meta, cssStyle);
    if (fill) {
        cell.fill = fill;
    }
    const rangeRef = Array.isArray(meta.source) ? context.validationMap.get(JSON.stringify(meta.source)) ?? null : null;
    const dropdownValidation = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getDropdownValidation"])(meta, rangeRef);
    if (dropdownValidation) {
        cell.dataValidation = dropdownValidation;
    }
    if (meta.comment?.value) {
        cell.note = String(meta.comment.value);
    }
}
function resolveCellValue(cellValue, meta, sourceValue, summary, context) {
    const { exportFormulas, formulasSeparator, dataRowOffset, dataColOffset, excludedHiddenRows, excludedHiddenCols } = context;
    if (summary && exportFormulas) {
        const formula = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$formula$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["buildSummaryFormula"])(summary, dataRowOffset, dataColOffset);
        const fallback = _class_private_method_get(this, _getCellValue, getCellValue).call(this, cellValue, meta);
        return {
            value: formula ? {
                ...formula,
                result: cellValue
            } : fallback,
            numFmt: null
        };
    }
    if (exportFormulas && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$formula$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isFormulaValue"])(sourceValue)) {
        return {
            value: {
                formula: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$formula$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["normalizeFormula"])(sourceValue, formulasSeparator, dataRowOffset - 1, dataColOffset - 1, excludedHiddenRows ?? undefined, excludedHiddenCols ?? undefined)
            },
            numFmt: null
        };
    }
    if (meta.type === 'date' || meta.type === 'intl-date') {
        const serial = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$date$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["parseIsoStringToSerial"])(cellValue);
        if (serial !== null) {
            return {
                value: serial,
                numFmt: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$date$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getDateNumFmt"])()
            };
        }
    }
    if (meta.type === 'time' || meta.type === 'intl-time') {
        const serial = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$date$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["parseTimeStringToSerial"])(cellValue);
        if (serial !== null) {
            return {
                value: serial,
                numFmt: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$date$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getTimeNumFmt"])()
            };
        }
    }
    if (meta.type === 'checkbox') {
        return {
            value: _class_private_method_get(this, _getCheckboxValue, getCheckboxValue).call(this, cellValue, meta),
            numFmt: null
        };
    }
    if (meta.type === 'multiselect') {
        return {
            value: _class_private_method_get(this, _getMultiSelectExportValue, getMultiSelectExportValue).call(this, cellValue),
            numFmt: null
        };
    }
    if (meta.type === 'numeric') {
        return {
            value: _class_private_method_get(this, _getCellValue, getCellValue).call(this, cellValue, meta),
            numFmt: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$numeric$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["intlNumFormatToExcelNumFmt"])(meta.numericFormat, meta.locale)
        };
    }
    return {
        value: _class_private_method_get(this, _getCellValue, getCellValue).call(this, cellValue, meta),
        numFmt: null
    };
}
function getCellValue(value, meta) {
    if (value === null || value === undefined) {
        return null;
    }
    if (meta.type === 'numeric') {
        if (typeof value === 'number') {
            return value;
        }
        const numericValue = Number(value);
        return Number.isNaN(numericValue) ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["stringify"])(value) : numericValue;
    }
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["stringify"])(value);
}
function getCheckboxValue(value, meta) {
    const checkedTemplate = meta.checkedTemplate ?? true;
    return value === checkedTemplate;
}
function getMultiSelectExportValue(value) {
    if (!Array.isArray(value)) {
        return value === null || value === undefined ? null : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["stringify"])(value);
    }
    if (value.length === 0) {
        return null;
    }
    return value.map((item)=>item !== null && typeof item === 'object' ? String(item.value) : String(item)).join(', ');
}
function buildHeaderFill(headerStyle) {
    if (!headerStyle?.backgroundColor) {
        return null;
    }
    return {
        type: 'pattern',
        pattern: 'solid',
        fgColor: {
            argb: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["cssColorToArgb"])(headerStyle.backgroundColor)
        }
    };
}
function buildHeaderBorder(headerStyle) {
    if (!headerStyle?.border) {
        return null;
    }
    const { style = 'thin', color } = headerStyle.border;
    const side = color ? {
        style,
        color: {
            argb: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["cssColorToArgb"])(color)
        }
    } : {
        style
    };
    return {
        top: side,
        bottom: side,
        left: side,
        right: side
    };
}
function getWriteOptions() {
    const { compression } = this.options;
    let level = null;
    if (compression === true) {
        level = 6;
    } else if (typeof compression === 'number' && compression >= 1 && compression <= 9) {
        level = compression;
    }
    if (level === null) {
        return {};
    }
    return {
        zip: {
            compression: 'DEFLATE',
            compressionOptions: {
                level
            }
        }
    };
}
function applyConditionalFormatting(worksheet, cfRules, dataRowOffset, dataColOffset, dataRows, dataCols) {
    if (dataRows === 0 || dataCols === 0) {
        return;
    }
    cfRules.forEach(({ rows, cols, rules })=>{
        if (!rules || rules.length === 0) {
            return;
        }
        const startRow = (rows ? rows[0] : 0) + dataRowOffset;
        const endRow = (rows ? rows[1] : dataRows - 1) + dataRowOffset;
        const startCol = (cols ? cols[0] : 0) + dataColOffset;
        const endCol = (cols ? cols[1] : dataCols - 1) + dataColOffset;
        worksheet.addConditionalFormatting({
            ref: _class_private_method_get(this, _buildRangeRef, buildRangeRef).call(this, startRow, startCol, endRow, endCol),
            rules
        });
    });
}
function buildRangeRef(startRow, startCol, endRow, endCol) {
    return `${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$formula$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["colIndexToLetter"])(startCol)}${startRow}:${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$formula$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["colIndexToLetter"])(endCol)}${endRow}`;
}
function applyColumnWidths(worksheet, widths, hasRowHeaders) {
    if (hasRowHeaders) {
        worksheet.getColumn(1).width = ROW_HEADER_DEFAULT_WIDTH;
    }
    const offset = hasRowHeaders ? 1 : 0;
    for(let index = 0; index < widths.length; index++){
        worksheet.getColumn(index + 1 + offset).width = Math.max(widths[index] / PIXELS_PER_EXCEL_COLUMN_WIDTH_UNIT, 1);
    }
}
function applyHiddenColumns(worksheet, hiddenColIndices, hasRowHeaders) {
    const offset = hasRowHeaders ? 1 : 0;
    hiddenColIndices.forEach((dataColIndex)=>{
        worksheet.getColumn(dataColIndex + 1 + offset).hidden = true;
    });
}
function applyHiddenRows(worksheet, hiddenRowIndices, dataRowOffset) {
    hiddenRowIndices.forEach((dataRowIndex)=>{
        worksheet.getRow(dataRowIndex + dataRowOffset).hidden = true;
    });
}
function applyWorksheetViews(worksheet, frozenRows, frozenColumns, headerRowCount, hasRowHeaders, isRtl) {
    const hasFrozenPanes = frozenRows > 0 || frozenColumns > 0;
    if (!hasFrozenPanes && !isRtl) {
        return;
    }
    const view = {};
    if (isRtl) {
        view.rightToLeft = true;
    }
    if (hasFrozenPanes) {
        view.state = 'frozen';
        view.xSplit = frozenColumns + (hasRowHeaders ? 1 : 0);
        view.ySplit = frozenRows + headerRowCount;
    }
    worksheet.views = [
        view
    ];
}
function writeColumnHeaders(worksheet, columnHeaders, classNames, hasRowHeaders, dataColOffset, headerFill, headerBorder) {
    const headerRow = worksheet.getRow(1);
    if (hasRowHeaders) {
        const cornerCell = headerRow.getCell(1);
        cornerCell.value = '';
        if (headerFill) {
            cornerCell.fill = headerFill;
        }
        if (headerBorder) {
            cornerCell.border = headerBorder;
        }
    }
    for(let index = 0; index < columnHeaders.length; index++){
        const cell = headerRow.getCell(index + dataColOffset);
        cell.value = columnHeaders[index] ?? null;
        const alignment = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getAlignmentFromClassName"])(classNames[index]);
        if (alignment) {
            cell.alignment = alignment;
        }
        if (headerFill) {
            cell.fill = headerFill;
        }
        if (headerBorder) {
            cell.border = headerBorder;
        }
    }
    headerRow.commit();
}
function writeNestedColumnHeaders(worksheet, nestedColumnHeaders, hasRowHeaders, dataColOffset, headerFill, headerBorder) {
    for(let layerIndex = 0; layerIndex < nestedColumnHeaders.length; layerIndex++){
        const layerHeaders = nestedColumnHeaders[layerIndex];
        const row = worksheet.getRow(layerIndex + 1);
        if (hasRowHeaders && layerIndex === 0) {
            const cornerCell = row.getCell(1);
            cornerCell.value = '';
            if (headerFill) {
                cornerCell.fill = headerFill;
            }
            if (headerBorder) {
                cornerCell.border = headerBorder;
            }
        }
        let colPos = dataColOffset;
        layerHeaders.forEach((header)=>{
            const cell = row.getCell(colPos);
            cell.value = header.label ?? null;
            const alignment = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$cell$2d$style$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["getAlignmentFromClassName"])(header.className);
            if (alignment) {
                cell.alignment = alignment;
            }
            if (headerFill) {
                cell.fill = headerFill;
            }
            if (headerBorder) {
                cell.border = headerBorder;
            }
            colPos += header.colspan;
        });
        row.commit();
    }
}
function applyNestedHeaderMerges(worksheet, nestedColumnHeaders, hasRowHeaders, dataColOffset) {
    if (hasRowHeaders && nestedColumnHeaders.length > 1) {
        worksheet.mergeCells(1, 1, nestedColumnHeaders.length, 1);
    }
    for(let layerIndex = 0; layerIndex < nestedColumnHeaders.length; layerIndex++){
        const layerHeaders = nestedColumnHeaders[layerIndex];
        const excelRow = layerIndex + 1;
        let colPos = dataColOffset;
        layerHeaders.forEach((header)=>{
            if (header.colspan > 1) {
                worksheet.mergeCells(excelRow, colPos, excelRow, colPos + header.colspan - 1);
            }
            colPos += header.colspan;
        });
    }
}
function buildValidationSheet(workbook, cellsMeta) {
    const sourceMap = new Map();
    for(let rowIndex = 0; rowIndex < cellsMeta.length; rowIndex++){
        for(let colIndex = 0; colIndex < cellsMeta[rowIndex].length; colIndex++){
            const meta = cellsMeta[rowIndex][colIndex];
            const isDropdown = meta.type === 'dropdown' || meta.type === 'autocomplete';
            if (isDropdown && Array.isArray(meta.source) && meta.source.length > 0) {
                const key = JSON.stringify(meta.source);
                if (!sourceMap.has(key)) {
                    sourceMap.set(key, meta.source);
                }
            }
        }
    }
    if (sourceMap.size === 0) {
        return new Map();
    }
    const existingNames = new Set(workbook.worksheets.map((ws)=>ws.name));
    let sheetName = '_HotValidation';
    let suffix = 1;
    while(existingNames.has(sheetName)){
        sheetName = `_HotValidation${suffix}`;
        suffix += 1;
    }
    const validationSheet = workbook.addWorksheet(sheetName);
    validationSheet.state = 'veryHidden';
    const validationMap = new Map();
    let colNumber = 1;
    sourceMap.forEach((source, key)=>{
        for(let rowNumber = 0; rowNumber < source.length; rowNumber++){
            const item = source[rowNumber];
            validationSheet.getCell(rowNumber + 1, colNumber).value = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$object$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isKeyValueObject"])(item) ? String(item.value) : String(item);
        }
        const colLetter = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$plugins$2f$exportFile$2f$types$2f$xlsx$2f$formula$2d$utils$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["colIndexToLetter"])(colNumber);
        const escapedName = sheetName.replaceAll('\'', '\'\'');
        const rangeRef = `'${escapedName}'!$${colLetter}$1:$${colLetter}$${source.length}`;
        validationMap.set(key, rangeRef);
        colNumber += 1;
    });
    return validationMap;
}
var __TURBOPACK__default__export__ = Xlsx;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/types/xlsx/cell-style.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "clearStyleCaches",
    ()=>clearStyleCaches,
    "cssColorToArgb",
    ()=>cssColorToArgb,
    "getAlignmentFromClassName",
    ()=>getAlignmentFromClassName,
    "getAlignmentFromMeta",
    ()=>getAlignmentFromMeta,
    "getBorderFromMeta",
    ()=>getBorderFromMeta,
    "getCssStyleFromElement",
    ()=>getCssStyleFromElement,
    "getDropdownValidation",
    ()=>getDropdownValidation,
    "getFillFromMeta",
    ()=>getFillFromMeta,
    "getFontFromMeta",
    ()=>getFontFromMeta
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/mixed.mjs [client] (ecmascript)");
;
// Default ARGB colors applied to read-only cells when no explicit styling is set.
// Values match the Handsontable design-system tokens for dimmed/disabled cell state.
const READ_ONLY_BG_ARGB = 'FFF0F0F0';
const READ_ONLY_TEXT_ARGB = 'FF808080';
/**
 * Normalises a cell `className` meta value to a flat array of non-empty class strings.
 * Accepts a space-separated string, an array of strings, or a nullish value.
 *
 * @param {string|string[]|null|undefined} className The cell's `className` meta value.
 * @returns {string[]}
 */ function normalizeClassNames(className) {
    if (Array.isArray(className)) {
        return className.filter((c)=>typeof c === 'string' && c.length > 0);
    }
    if (typeof className === 'string') {
        return className.split(' ').filter((c)=>c.length > 0);
    }
    return [];
}
// Per-export cache for detectExplicitBackgroundColor results.
// Keyed by document (WeakMap — avoids leaking document references) then by the
// joined className string. Cleared at the start of each export so CSS rule
// changes between exports are always picked up.
const backgroundColorByDoc = new WeakMap();
// Per-export cache for getCssStyleFromProbe results (used when the real cell element
// is not available — i.e. the cell is outside the render viewport).
const cssStyleProbeByDoc = new WeakMap();
function clearStyleCaches(doc) {
    backgroundColorByDoc.delete(doc);
    cssStyleProbeByDoc.delete(doc);
}
// Handsontable alignment class names — cells that carry only these classes have no
// custom CSS color, so reading `color` from them would yield only the inherited default.
const ALIGNMENT_CLASS_NAMES = new Set([
    'htLeft',
    'htRight',
    'htCenter',
    'htJustify',
    'htTop',
    'htMiddle',
    'htBottom'
]);
/**
 * Converts a `getComputedStyle` color string (`rgb(r, g, b)` / `rgba(r, g, b, a)`)
 * to a CSS hex string (`#RRGGBB`), or `null` when the color is fully transparent.
 *
 * @private
 * @param {string} rgbStr Computed color string.
 * @returns {string|null}
 */ function rgbComputedToHex(rgbStr) {
    if (!rgbStr) {
        return null;
    }
    const m = rgbStr.match(/rgba?\((\d+),\s*(\d+),\s*(\d+)(?:,\s*([\d.]+))?\)/);
    if (!m) {
        return null;
    }
    const alpha = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$mixed$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isDefined"])(m[4]) ? Number.parseFloat(m[4]) : 1;
    if (alpha === 0) {
        return null;
    }
    const r = Number.parseInt(m[1], 10).toString(16).padStart(2, '0');
    const g = Number.parseInt(m[2], 10).toString(16).padStart(2, '0');
    const b = Number.parseInt(m[3], 10).toString(16).padStart(2, '0');
    return `#${r}${g}${b}`.toUpperCase();
}
/**
 * Detects whether any of the provided `metaClasses` explicitly sets a
 * `background-color` by mounting two temporary off-screen `<td>` elements —
 * one with the full class list, one with only alignment classes — inside a
 * `.handsontable` probe div appended to `document.body`. This preserves the
 * same CSS-rule scope as the real table without touching any actual cell in
 * the DOM. The probe is removed immediately after reading.
 *
 * @private
 * @param {Document} doc The owner document of the rendered cell.
 * @param {string[]} metaClasses All classes from the cell's `className` meta value.
 * @param {Window} view The element's owner window.
 * @returns {string|null} CSS hex color string, or `null` when no explicit background.
 */ function detectExplicitBackgroundColor(doc, metaClasses, view) {
    const cacheKey = metaClasses.join(' ');
    let docCache = backgroundColorByDoc.get(doc);
    if (!docCache) {
        docCache = new Map();
        backgroundColorByDoc.set(doc, docCache);
    }
    if (docCache.has(cacheKey)) {
        return docCache.get(cacheKey) ?? null;
    }
    // Off-screen probe with Handsontable CSS context so scoped rules (e.g.
    // `.handsontable td { … }`) apply to the temporary cells.
    const probe = doc.createElement('div');
    probe.className = 'handsontable';
    probe.style.cssText = 'position:absolute;top:-99999px;left:-99999px;visibility:hidden;pointer-events:none;';
    const table = doc.createElement('table');
    const tbody = doc.createElement('tbody');
    const tr = doc.createElement('tr');
    // Cell with all meta classes — the candidate background.
    const tdFull = doc.createElement('td');
    tdFull.className = cacheKey;
    // Baseline cell with only alignment classes — the default background.
    const tdBase = doc.createElement('td');
    tdBase.className = metaClasses.filter((c)=>ALIGNMENT_CLASS_NAMES.has(c)).join(' ');
    tr.appendChild(tdFull);
    tr.appendChild(tdBase);
    tbody.appendChild(tr);
    table.appendChild(tbody);
    probe.appendChild(table);
    doc.body.appendChild(probe);
    const bgFull = view.getComputedStyle(tdFull).backgroundColor;
    const bgBase = view.getComputedStyle(tdBase).backgroundColor;
    doc.body.removeChild(probe);
    const result = bgFull !== bgBase ? rgbComputedToHex(bgFull) : null;
    docCache.set(cacheKey, result);
    return result;
}
/**
 * Derives full CSS style information for a cell that has no rendered DOM element
 * (e.g. a cell outside the virtual-scroll viewport) by mounting a temporary probe
 * `<td>` with the same `metaClasses` inside a `.handsontable` context.
 *
 * Both a "full" cell (with all custom classes) and a "baseline" cell (with only
 * alignment classes) are probed simultaneously so that inherited-default values
 * (color, background) are not incorrectly attributed to the custom class.
 *
 * Results are cached per document + className so subsequent calls for the same
 * className are free.
 *
 * @private
 * @param {Document} doc The document to build the probe in.
 * @param {Window} view The associated window (for `getComputedStyle`).
 * @param {string[]} metaClasses All classes from the cell's `className` meta value.
 * @returns {{ fontBold: boolean, fontItalic: boolean, fontUnderline: boolean,
 *             fontColor: string|null, backgroundColor: string|null }}
 */ function getCssStyleFromProbe(doc, view, metaClasses) {
    const cacheKey = metaClasses.join(' ');
    let docCache = cssStyleProbeByDoc.get(doc);
    if (!docCache) {
        docCache = new Map();
        cssStyleProbeByDoc.set(doc, docCache);
    }
    if (docCache.has(cacheKey)) {
        return docCache.get(cacheKey);
    }
    const probe = doc.createElement('div');
    probe.className = 'handsontable';
    probe.style.cssText = 'position:absolute;top:-99999px;left:-99999px;visibility:hidden;pointer-events:none;';
    const table = doc.createElement('table');
    const tbody = doc.createElement('tbody');
    const tr = doc.createElement('tr');
    // Cell with all meta classes — resolves the custom styling.
    const tdFull = doc.createElement('td');
    tdFull.className = cacheKey;
    // Baseline cell with only alignment classes — resolves inherited defaults.
    const tdBase = doc.createElement('td');
    tdBase.className = metaClasses.filter((c)=>ALIGNMENT_CLASS_NAMES.has(c)).join(' ');
    tr.appendChild(tdFull);
    tr.appendChild(tdBase);
    tbody.appendChild(tr);
    table.appendChild(tbody);
    probe.appendChild(table);
    doc.body.appendChild(probe);
    const styleFull = view.getComputedStyle(tdFull);
    const styleBase = view.getComputedStyle(tdBase);
    const bgFull = styleFull.backgroundColor;
    const bgBase = styleBase.backgroundColor;
    const colorFull = styleFull.color;
    const colorBase = styleBase.color;
    doc.body.removeChild(probe);
    const result = {
        fontBold: Number.parseInt(styleFull.fontWeight, 10) >= 700 || styleFull.fontWeight === 'bold',
        fontItalic: styleFull.fontStyle === 'italic',
        fontUnderline: (styleFull.textDecorationLine || styleFull.textDecoration || '').includes('underline'),
        fontColor: colorFull !== colorBase ? rgbComputedToHex(colorFull) : null,
        backgroundColor: bgFull !== bgBase ? rgbComputedToHex(bgFull) : null
    };
    docCache.set(cacheKey, result);
    return result;
}
function getCssStyleFromElement(element, className, rootDocument = null, rootWindow = null) {
    if (!element) {
        if (!rootDocument || !rootWindow) {
            return null;
        }
        const metaClasses = normalizeClassNames(className);
        if (!metaClasses.some((c)=>!ALIGNMENT_CLASS_NAMES.has(c))) {
            return null;
        }
        return getCssStyleFromProbe(rootDocument, rootWindow, metaClasses);
    }
    const view = element.ownerDocument?.defaultView;
    if (!view) {
        return null;
    }
    const style = view.getComputedStyle(element);
    const metaClasses = normalizeClassNames(className);
    const hasCustomClass = metaClasses.some((c)=>!ALIGNMENT_CLASS_NAMES.has(c));
    return {
        fontBold: Number.parseInt(style.fontWeight, 10) >= 700 || style.fontWeight === 'bold',
        fontItalic: style.fontStyle === 'italic',
        fontUnderline: (style.textDecorationLine || style.textDecoration || '').includes('underline'),
        fontColor: hasCustomClass ? rgbComputedToHex(style.color) : null,
        backgroundColor: hasCustomClass ? detectExplicitBackgroundColor(element.ownerDocument, metaClasses, view) : null
    };
}
function cssColorToArgb(color) {
    if (!color || typeof color !== 'string') {
        return 'FF000000';
    }
    const hex = color.startsWith('#') ? color.slice(1) : color;
    if (hex.length === 3) {
        const r = hex[0] + hex[0];
        const g = hex[1] + hex[1];
        const b = hex[2] + hex[2];
        return `FF${r}${g}${b}`.toUpperCase();
    }
    if (hex.length === 6) {
        return `FF${hex}`.toUpperCase();
    }
    if (hex.length === 8) {
        return hex.toUpperCase();
    }
    return 'FF000000';
}
function getAlignmentFromClassName(className) {
    if (!className) {
        return null;
    }
    return getAlignmentFromMeta({
        className
    });
}
function getAlignmentFromMeta(meta) {
    if (!meta?.className) {
        return null;
    }
    const classes = normalizeClassNames(meta.className);
    const alignment = {};
    if (classes.includes('htLeft')) {
        alignment.horizontal = 'left';
    } else if (classes.includes('htCenter')) {
        alignment.horizontal = 'center';
    } else if (classes.includes('htRight')) {
        alignment.horizontal = 'right';
    } else if (classes.includes('htJustify')) {
        alignment.horizontal = 'justify';
    }
    if (classes.includes('htTop')) {
        alignment.vertical = 'top';
    } else if (classes.includes('htMiddle')) {
        alignment.vertical = 'middle';
    } else if (classes.includes('htBottom')) {
        alignment.vertical = 'bottom';
    }
    return Object.keys(alignment).length > 0 ? alignment : null;
}
/**
 * Maps a Handsontable border pixel width to the closest ExcelJS border style string.
 *
 * Excel does not support arbitrary pixel widths; it uses a fixed set of named styles.
 * The mapping used here:
 * - `1` → `'thin'`  (≈ 0.5 pt in Excel)
 * - `2` → `'medium'` (≈ 1.5 pt in Excel)
 * - `3+` → `'thick'` (≈ 2.25 pt in Excel)
 *
 * @private
 * @param {number} width Border width in pixels.
 * @returns {string} ExcelJS border style name.
 */ function borderWidthToExcelStyle(width) {
    if (width >= 3) {
        return 'thick';
    }
    if (width === 2) {
        return 'medium';
    }
    return 'thin';
}
function getBorderFromMeta(meta) {
    if (!meta?.borders) {
        return null;
    }
    const { borders } = meta;
    const excelBorder = {};
    [
        'top',
        'bottom',
        'left',
        'right'
    ].forEach((side)=>{
        if (borders[side] && borders[side].width > 0) {
            excelBorder[side] = {
                style: borderWidthToExcelStyle(borders[side].width),
                color: {
                    argb: cssColorToArgb(borders[side].color)
                }
            };
        }
    });
    return Object.keys(excelBorder).length > 0 ? excelBorder : null;
}
function getFontFromMeta(meta, cssStyle = null) {
    const isReadOnly = meta?.readOnly === true;
    const bold = cssStyle?.fontBold || false;
    const italic = cssStyle?.fontItalic || false;
    const underline = cssStyle?.fontUnderline || false;
    const colorHex = cssStyle?.fontColor ?? null;
    if (!bold && !italic && !underline && !colorHex && !isReadOnly) {
        return null;
    }
    const font = {};
    if (bold) {
        font.bold = true;
    }
    if (italic) {
        font.italic = true;
    }
    if (underline) {
        font.underline = true;
    }
    if (colorHex) {
        font.color = {
            argb: cssColorToArgb(colorHex)
        };
    } else if (isReadOnly) {
        font.color = {
            argb: READ_ONLY_TEXT_ARGB
        };
    }
    return Object.keys(font).length > 0 ? font : null;
}
function getFillFromMeta(meta, cssStyle = null) {
    let bgColor = null;
    if (cssStyle?.backgroundColor) {
        bgColor = cssColorToArgb(cssStyle.backgroundColor);
    } else if (meta?.readOnly === true) {
        bgColor = READ_ONLY_BG_ARGB;
    }
    if (!bgColor) {
        return null;
    }
    return {
        type: 'pattern',
        pattern: 'solid',
        fgColor: {
            argb: bgColor
        }
    };
}
function getDropdownValidation(meta, rangeRef) {
    if (!meta || !rangeRef) {
        return null;
    }
    const isDropdownType = meta.type === 'dropdown' || meta.type === 'autocomplete';
    if (!isDropdownType || !Array.isArray(meta.source)) {
        return null;
    }
    return {
        type: 'list',
        allowBlank: true,
        formulae: [
            rangeRef
        ]
    };
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/types/xlsx/date-utils.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getDateNumFmt",
    ()=>getDateNumFmt,
    "getTimeNumFmt",
    ()=>getTimeNumFmt,
    "parseIsoStringToSerial",
    ()=>parseIsoStringToSerial,
    "parseTimeStringToSerial",
    ()=>parseTimeStringToSerial,
    "toExcelDateSerial",
    ()=>toExcelDateSerial
]);
const EXCEL_EPOCH_UTC = Date.UTC(1899, 11, 30);
const MS_PER_DAY = 86400000;
function toExcelDateSerial(date) {
    const localDateUtc = Date.UTC(date.getFullYear(), date.getMonth(), date.getDate());
    return Math.round((localDateUtc - EXCEL_EPOCH_UTC) / MS_PER_DAY);
}
function parseIsoStringToSerial(value) {
    if (!value) {
        return null;
    }
    const match = String(value).match(/^(\d{4})-(\d{2})-(\d{2})$/);
    if (!match) {
        return null;
    }
    const year = parseInt(match[1], 10);
    const month = parseInt(match[2], 10);
    const day = parseInt(match[3], 10);
    const date = new Date(year, month - 1, day);
    if (Number.isNaN(date.getTime())) {
        return null;
    }
    return toExcelDateSerial(date);
}
function parseTimeStringToSerial(value) {
    if (!value) {
        return null;
    }
    const str = String(value).trim();
    const match24 = str.match(/^(\d{1,2}):(\d{2})(?::(\d{2}))?$/);
    if (match24) {
        const hours = parseInt(match24[1], 10);
        const minutes = parseInt(match24[2], 10);
        const seconds = match24[3] ? parseInt(match24[3], 10) : 0;
        if (hours > 23 || minutes > 59 || seconds > 59) {
            return null;
        }
        return (hours * 3600 + minutes * 60 + seconds) / 86400;
    }
    const match12 = str.match(/^(\d{1,2}):(\d{2})(?::(\d{2}))?\s*(AM|PM)$/i);
    if (match12) {
        let hours = parseInt(match12[1], 10);
        const minutes = parseInt(match12[2], 10);
        const seconds = match12[3] ? parseInt(match12[3], 10) : 0;
        const period = match12[4].toUpperCase();
        if (hours > 12 || minutes > 59 || seconds > 59) {
            return null;
        }
        if (period === 'AM' && hours === 12) {
            hours = 0;
        } else if (period === 'PM' && hours !== 12) {
            hours += 12;
        }
        return (hours * 3600 + minutes * 60 + seconds) / 86400;
    }
    return null;
}
function getDateNumFmt() {
    return 'mm-dd-yy';
}
function getTimeNumFmt() {
    return 'h:mm:ss';
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/types/xlsx/formula-utils.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "buildSummaryFormula",
    ()=>buildSummaryFormula,
    "colIndexToLetter",
    ()=>colIndexToLetter,
    "colLetterToIndex",
    ()=>colLetterToIndex,
    "isFormulaValue",
    ()=>isFormulaValue,
    "normalizeFormula",
    ()=>normalizeFormula,
    "replaceSeparatorOutsideStrings",
    ()=>replaceSeparatorOutsideStrings
]);
/**
 * Maps ColumnSummary type names to their Excel function equivalents.
 *
 * `custom` is intentionally omitted: a `customFunction` is arbitrary JavaScript
 * and has no generic Excel formula equivalent.  When `exportFormulas` is `true`,
 * cells with a `custom` summary type fall back to their pre-calculated static value.
 */ const SUMMARY_TYPE_TO_EXCEL_FN = {
    sum: 'SUM',
    min: 'MIN',
    max: 'MAX',
    count: 'COUNT',
    average: 'AVERAGE'
};
function colIndexToLetter(colIndex) {
    let letter = '';
    let n = colIndex;
    while(n > 0){
        const remainder = (n - 1) % 26;
        letter = String.fromCharCode(65 + remainder) + letter;
        n = Math.floor((n - 1) / 26);
    }
    return letter;
}
function colLetterToIndex(letters) {
    let index = 0;
    for(let i = 0; i < letters.length; i++){
        index = index * 26 + (letters.charCodeAt(i) - 64);
    }
    return index;
}
function isFormulaValue(value) {
    return typeof value === 'string' && value.startsWith('=');
}
function buildSummaryFormula(summary, dataRowOffset, dataColOffset) {
    const excelFn = SUMMARY_TYPE_TO_EXCEL_FN[summary.type];
    if (!excelFn) {
        return null;
    }
    const colLetter = colIndexToLetter(summary.sourceCol + dataColOffset);
    const rangeRefs = summary.sourceRanges.map(([start, end])=>{
        const startExcelRow = start + dataRowOffset;
        const endExcelRow = end + dataRowOffset;
        return startExcelRow === endExcelRow ? `${colLetter}${startExcelRow}` : `${colLetter}${startExcelRow}:${colLetter}${endExcelRow}`;
    });
    return {
        formula: `${excelFn}(${rangeRefs.join(',')})`
    };
}
function replaceSeparatorOutsideStrings(str, from, to) {
    let result = '';
    let inString = false;
    let stringChar = '';
    for(let i = 0; i < str.length; i++){
        const ch = str[i];
        if (inString) {
            result += ch;
            if (ch === stringChar) {
                if (str[i + 1] === stringChar) {
                    // Escaped quote (e.g. "" inside a double-quoted string) — consume
                    // both characters and remain in string mode.
                    result += str[i + 1];
                    i += 1;
                } else {
                    inString = false;
                }
            }
        } else if (ch === '"' || ch === '\'') {
            inString = true;
            stringChar = ch;
            result += ch;
        } else if (str.startsWith(from, i)) {
            result += to;
            i += from.length - 1;
        } else {
            result += ch;
        }
    }
    return result;
}
/**
 * Counts how many values in a `Set` are strictly less than `threshold`.
 *
 * @private
 * @param {Set<number>} set Set of numbers.
 * @param {number} threshold Upper bound (exclusive).
 * @returns {number}
 */ function countBelow(set, threshold) {
    let count = 0;
    set.forEach((value)=>{
        if (value < threshold) {
            count += 1;
        }
    });
    return count;
}
function normalizeFormula(formulaStr, separator, rowOffset, colOffset, excludedHiddenRows, excludedHiddenCols) {
    let formula = formulaStr.startsWith('=') ? formulaStr.slice(1) : formulaStr;
    const hasRowExclusions = (excludedHiddenRows?.size ?? 0) > 0;
    const hasColExclusions = (excludedHiddenCols?.size ?? 0) > 0;
    if (rowOffset !== 0 || colOffset !== 0 || hasRowExclusions || hasColExclusions) {
        // The leading alternative matches string literals (double-quoted Excel strings use "" to escape a quote;
        // single-quoted tokens are sheet name references like 'Sheet 1'!A1). When matched, they are returned
        // as-is so that cell-reference-like patterns inside string values are not offset.
        formula = formula.replace(/("(?:[^"]|"")*"|'[^']*')|(?<!\d)(\$?)([A-Z]{1,3})(\$?)(\d{1,7})(?!\()/g, (match, strLiteral, colAbs, colLetters, rowAbs, rowStr)=>{
            if (strLiteral !== undefined) {
                return strLiteral;
            }
            const hotPhysCol = colLetterToIndex(colLetters) - 1; // 0-based physical HOT column
            const hotPhysRow = Number.parseInt(rowStr, 10) - 1; // 0-based physical HOT row
            const hiddenColsBefore = hasColExclusions && excludedHiddenCols ? countBelow(excludedHiddenCols, hotPhysCol) : 0;
            const hiddenRowsBefore = hasRowExclusions && excludedHiddenRows ? countBelow(excludedHiddenRows, hotPhysRow) : 0;
            const newCol = colLetterToIndex(colLetters) + colOffset - hiddenColsBefore;
            const newRow = Number.parseInt(rowStr, 10) + rowOffset - hiddenRowsBefore;
            return `${colAbs}${colIndexToLetter(newCol)}${rowAbs}${newRow}`;
        });
    }
    if (separator && separator !== ',') {
        formula = replaceSeparatorOutsideStrings(formula, separator, ',');
    }
    return formula;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/types/xlsx/numeric-utils.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "intlNumFormatToExcelNumFmt",
    ()=>intlNumFormatToExcelNumFmt
]);
/**
 * Resolves the currency symbol and its position (prefix or suffix) for a given
 * ISO 4217 currency code and locale using the `Intl.NumberFormat` API.
 *
 * @private
 * @param {string} currency ISO 4217 currency code (e.g. `'USD'`, `'EUR'`).
 * @param {string|undefined} locale BCP 47 locale tag (e.g. `'en-US'`).
 * @returns {{ symbol: string, isPrefix: boolean }}
 */ function getCurrencyInfo(currency, locale) {
    try {
        const parts = new Intl.NumberFormat(locale || 'en', {
            style: 'currency',
            currency,
            minimumFractionDigits: 0,
            maximumFractionDigits: 0
        }).formatToParts(0);
        const symbol = parts.find((p)=>p.type === 'currency')?.value ?? currency;
        const currencyIndex = parts.findIndex((p)=>p.type === 'currency');
        const integerIndex = parts.findIndex((p)=>p.type === 'integer');
        const isPrefix = currencyIndex < integerIndex;
        return {
            symbol,
            isPrefix
        };
    } catch  {
        // Intl.NumberFormat throws for unrecognised currency codes. Fall back to
        // the raw currency string placed as a prefix so the export still produces
        // a usable result.
        return {
            symbol: currency,
            isPrefix: true
        };
    }
}
function intlNumFormatToExcelNumFmt(numericFormat, locale) {
    if (!numericFormat) {
        return null;
    }
    const { style, currency, minimumFractionDigits = 0, maximumFractionDigits, useGrouping = true } = numericFormat;
    const fractionDigits = maximumFractionDigits ?? minimumFractionDigits;
    const decimalPart = fractionDigits > 0 ? `.${'0'.repeat(fractionDigits)}` : '';
    const intPart = useGrouping !== false ? '#,##0' : '0';
    if (style === 'percent') {
        return `${intPart}${decimalPart}%`;
    }
    if (style === 'currency' && currency) {
        const { symbol, isPrefix } = getCurrencyInfo(currency, locale);
        // @TODO: handle locale-specific spacing between symbol and number
        // (e.g. non-breaking space in fr-FR). Excel numFmt requires "\ " for a
        // literal space, which varies per locale and is left for a follow-up.
        return isPrefix ? `${symbol}${intPart}${decimalPart}` : `${intPart}${decimalPart}${symbol}`;
    }
    return `${intPart}${decimalPart}`;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/handsontable/plugins/exportFile/utils.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "buildExportDialogContent",
    ()=>buildExportDialogContent
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/templateLiteralTag.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/handsontable/helpers/constants.mjs [client] (ecmascript)");
;
;
function buildExportDialogContent(title) {
    // Spinner SVG reused from the Loading plugin — same arc shape, same CSS class so the
    // `ht-loading__icon-svg` spin animation (defined in handsontable.css) applies automatically.
    const { fragment } = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$templateLiteralTag$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["html"]`
    <div class="${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["LOADING_CLASS_NAME"]}__content">
      <i class="${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["LOADING_CLASS_NAME"]}__icon">
        <svg class="${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["LOADING_CLASS_NAME"]}__icon-svg"
          xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 16 16">
          <path stroke="currentColor" stroke-width="2" d="M15 8a7 7 0 1 1-3.5-6.062"></path>
        </svg>
      </i>
      <div class="${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["LOADING_CLASS_NAME"]}__text">
        <h2 class="${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$handsontable$2f$helpers$2f$constants$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["LOADING_CLASS_NAME"]}__title">${title}</h2>
      </div>
    </div>
  `;
    return fragment;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
]);

//# sourceMappingURL=node_modules_handsontable_plugins_exportFile_59e2ab26.async.js.map